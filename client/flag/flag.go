// Code generated by client/gen; DO NOT EDIT.

// Package flag is the typed client for the "flag" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: flag's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on flag's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package flag

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "flag"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.FlagsBoard,
	client.FlagsHold,
	client.FlagsSet,
	client.FlagsValue,
}

// FlagsBoard the operator switchboard for the caller's org.
//
// Calls client.FlagsBoard on flag over the peer plane.
func FlagsBoard(ctx context.Context, in *client.Unit) (*client.FlagBoard, error) {
	return client.Call[client.Unit, client.FlagBoard](ctx, App, client.FlagsBoard, in)
}

// FlagsHold whether the caller's org holds one flag.
//
// Calls client.FlagsHold on flag over the peer plane.
func FlagsHold(ctx context.Context, in *client.FlagIn) (*client.Flag, error) {
	return client.Call[client.FlagIn, client.Flag](ctx, App, client.FlagsHold, in)
}

// FlagsSet write one platform switch.
//
// Calls client.FlagsSet on flag over the peer plane.
func FlagsSet(ctx context.Context, in *client.FlagSetIn) (*client.Unit, error) {
	return client.Call[client.FlagSetIn, client.Unit](ctx, App, client.FlagsSet, in)
}

// FlagsValue what an operator has set one flag to.
//
// Calls client.FlagsValue on flag over the peer plane.
func FlagsValue(ctx context.Context, in *client.FlagValueIn) (*client.FlagValue, error) {
	return client.Call[client.FlagValueIn, client.FlagValue](ctx, App, client.FlagsValue, in)
}
