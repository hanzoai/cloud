// Code generated by client/gen; DO NOT EDIT.

// Package framework is the typed client for the "framework" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: framework's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on framework's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package framework

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "framework"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.FrameworkDoc,
	client.FrameworkDocs,
	client.FrameworkFind,
	client.FrameworkIngest,
	client.FrameworkInstalled,
}

// FrameworkDoc read one document in the caller's org.
//
// Calls client.FrameworkDoc on framework over the peer plane.
func FrameworkDoc(ctx context.Context, in *client.FwDocIn) (*client.FwDoc, error) {
	return client.Call[client.FwDocIn, client.FwDoc](ctx, App, client.FrameworkDoc, in)
}

// FrameworkDocs list one DocType's documents in the caller's org.
//
// Calls client.FrameworkDocs on framework over the peer plane.
func FrameworkDocs(ctx context.Context, in *client.FwDocsIn) (*client.FwDocs, error) {
	return client.Call[client.FwDocsIn, client.FwDocs](ctx, App, client.FrameworkDocs, in)
}

// FrameworkFind find a document by one field's value in the caller's org.
//
// Calls client.FrameworkFind on framework over the peer plane.
func FrameworkFind(ctx context.Context, in *client.FwFindIn) (*client.FwFound, error) {
	return client.Call[client.FwFindIn, client.FwFound](ctx, App, client.FrameworkFind, in)
}

// FrameworkIngest create a document from trusted fields in the caller's org.
//
// Calls client.FrameworkIngest on framework over the peer plane.
func FrameworkIngest(ctx context.Context, in *client.FwIngestIn) (*client.FwIngested, error) {
	return client.Call[client.FwIngestIn, client.FwIngested](ctx, App, client.FrameworkIngest, in)
}

// FrameworkInstalled whether the caller's org holds a DocType.
//
// Calls client.FrameworkInstalled on framework over the peer plane.
func FrameworkInstalled(ctx context.Context, in *client.FwRef) (*client.FwInstalled, error) {
	return client.Call[client.FwRef, client.FwInstalled](ctx, App, client.FrameworkInstalled, in)
}
