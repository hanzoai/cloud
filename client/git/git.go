// Code generated by client/gen; DO NOT EDIT.

// Package git is the typed client for the "git" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: git's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on git's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package git

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "git"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.GitFigures,
	client.GitFiles,
	client.GitImport,
	client.GitInbound,
	client.GitMirror,
	client.GitPublish,
	client.GitRev,
	client.GitStatus,
}

// GitFigures the caller's headline git figures.
//
// Calls client.GitFigures on git over the peer plane.
func GitFigures(ctx context.Context, in *client.FiguresIn) (*client.FiguresOut, error) {
	return client.Call[client.FiguresIn, client.FiguresOut](ctx, App, client.GitFigures, in)
}

// GitFiles a repo's files at one revision.
//
// Calls client.GitFiles on git over the peer plane.
func GitFiles(ctx context.Context, in *client.FilesIn) (*client.Files, error) {
	return client.Call[client.FilesIn, client.Files](ctx, App, client.GitFiles, in)
}

// GitImport create a repo and mirror an upstream into it.
//
// Calls client.GitImport on git over the peer plane.
func GitImport(ctx context.Context, in *client.ImportIn) (*client.Imported, error) {
	return client.Call[client.ImportIn, client.Imported](ctx, App, client.GitImport, in)
}

// GitInbound advance one branch from an upstream push.
//
// Calls client.GitInbound on git over the peer plane.
func GitInbound(ctx context.Context, in *client.InboundIn) (*client.Synced, error) {
	return client.Call[client.InboundIn, client.Synced](ctx, App, client.GitInbound, in)
}

// GitMirror declare or remove a repo's outbound mirror target.
//
// Calls client.GitMirror on git over the peer plane.
func GitMirror(ctx context.Context, in *client.MirrorIn) (*client.Mirrored, error) {
	return client.Call[client.MirrorIn, client.Mirrored](ctx, App, client.GitMirror, in)
}

// GitPublish reconcile a project's repo visibility.
//
// Calls client.GitPublish on git over the peer plane.
func GitPublish(ctx context.Context, in *client.Visibility) (*struct{}, error) {
	return client.Call[client.Visibility, struct{}](ctx, App, client.GitPublish, in)
}

// GitRev the commit a ref resolves to.
//
// Calls client.GitRev on git over the peer plane.
func GitRev(ctx context.Context, in *client.RevIn) (*client.Rev, error) {
	return client.Call[client.RevIn, client.Rev](ctx, App, client.GitRev, in)
}

// GitStatus which of these repos are imported, and which are in conflict.
//
// Calls client.GitStatus on git over the peer plane.
func GitStatus(ctx context.Context, in *client.StatusIn) (*client.Statuses, error) {
	return client.Call[client.StatusIn, client.Statuses](ctx, App, client.GitStatus, in)
}
