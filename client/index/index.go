// Code generated by client/gen; DO NOT EDIT.

// Package index is the typed client for the "index" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: index's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on index's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package index

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "index"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.IndexQuery,
	client.IndexReconcile,
}

// IndexQuery search one index in the caller's org.
//
// Calls client.IndexQuery on index over the peer plane.
func IndexQuery(ctx context.Context, in *client.IndexQueryIn) (*client.IndexQueryOut, error) {
	return client.Call[client.IndexQueryIn, client.IndexQueryOut](ctx, App, client.IndexQuery, in)
}

// IndexReconcile replace one index's whole corpus in the caller's org.
//
// Calls client.IndexReconcile on index over the peer plane.
func IndexReconcile(ctx context.Context, in *client.IndexReconcileIn) (*client.IndexReconcileOut, error) {
	return client.Call[client.IndexReconcileIn, client.IndexReconcileOut](ctx, App, client.IndexReconcile, in)
}
