// Code generated by client/gen; DO NOT EDIT.

// Package kms is the typed client for the "kms" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: kms's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on kms's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package kms

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "kms"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.KMSDel,
	client.KMSGet,
	client.KMSPut,
	client.KMSSign,
}

// KMSDel forget one secret.
//
// Calls client.KMSDel on kms over the peer plane.
func KMSDel(ctx context.Context, in *client.SecretIn) (*client.Secret, error) {
	return client.Call[client.SecretIn, client.Secret](ctx, App, client.KMSDel, in)
}

// KMSGet read one secret.
//
// Calls client.KMSGet on kms over the peer plane.
func KMSGet(ctx context.Context, in *client.SecretIn) (*client.Secret, error) {
	return client.Call[client.SecretIn, client.Secret](ctx, App, client.KMSGet, in)
}

// KMSPut write one secret.
//
// Calls client.KMSPut on kms over the peer plane.
func KMSPut(ctx context.Context, in *client.SecretIn) (*client.Secret, error) {
	return client.Call[client.SecretIn, client.Secret](ctx, App, client.KMSPut, in)
}

// KMSSign sign a payload with a key that never leaves this process.
//
// Calls client.KMSSign on kms over the peer plane.
func KMSSign(ctx context.Context, in *client.SecretIn) (*client.Secret, error) {
	return client.Call[client.SecretIn, client.Secret](ctx, App, client.KMSSign, in)
}
