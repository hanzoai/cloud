// Code generated by client/gen; DO NOT EDIT.

// Package iam is the typed client for the "iam" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: iam's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on iam's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package iam

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "iam"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.IAMApproval,
	client.IAMEmail,
	client.IAMFederated,
	client.IAMGrant,
	client.IAMMailable,
	client.IAMMembers,
	client.IAMProjects,
	client.IAMRoles,
	client.IAMSeats,
}

// IAMApproval whether the caller is off the waitlist.
//
// Calls client.IAMApproval on iam over the peer plane.
func IAMApproval(ctx context.Context) (*client.Approval, error) {
	return client.Call[struct{}, client.Approval](ctx, App, client.IAMApproval, &struct{}{})
}

// IAMEmail the caller's address, and whether they have proved it.
//
// Calls client.IAMEmail on iam over the peer plane.
func IAMEmail(ctx context.Context) (*client.Email, error) {
	return client.Call[struct{}, client.Email](ctx, App, client.IAMEmail, &struct{}{})
}

// IAMFederated the caller's org member behind an external identity.
//
// Calls client.IAMFederated on iam over the peer plane.
func IAMFederated(ctx context.Context, in *client.FederatedIn) (*client.Federated, error) {
	return client.Call[client.FederatedIn, client.Federated](ctx, App, client.IAMFederated, in)
}

// IAMGrant record that a user may act in a scope of the caller's org.
//
// Calls client.IAMGrant on iam over the peer plane.
func IAMGrant(ctx context.Context, in *client.GrantIn) (*struct{}, error) {
	return client.Call[client.GrantIn, struct{}](ctx, App, client.IAMGrant, in)
}

// IAMMailable who this org may mail.
//
// Calls client.IAMMailable on iam over the peer plane.
func IAMMailable(ctx context.Context) (*client.Roster, error) {
	return client.Call[struct{}, client.Roster](ctx, App, client.IAMMailable, &struct{}{})
}

// IAMMembers who may act in one scope of the caller's org.
//
// Calls client.IAMMembers on iam over the peer plane.
func IAMMembers(ctx context.Context, in *client.Scope) (*client.Memberships, error) {
	return client.Call[client.Scope, client.Memberships](ctx, App, client.IAMMembers, in)
}

// IAMProjects projects this org owns.
//
// Calls client.IAMProjects on iam over the peer plane.
func IAMProjects(ctx context.Context) (*client.Projects, error) {
	return client.Call[struct{}, client.Projects](ctx, App, client.IAMProjects, &struct{}{})
}

// IAMRoles the caller's role names in their own org.
//
// Calls client.IAMRoles on iam over the peer plane.
func IAMRoles(ctx context.Context) (*client.Roles, error) {
	return client.Call[struct{}, client.Roles](ctx, App, client.IAMRoles, &struct{}{})
}

// IAMSeats the caller's org's billable people.
//
// Calls client.IAMSeats on iam over the peer plane.
func IAMSeats(ctx context.Context) (*client.Seats, error) {
	return client.Call[struct{}, client.Seats](ctx, App, client.IAMSeats, &struct{}{})
}
