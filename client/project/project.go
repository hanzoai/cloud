// Code generated by client/gen; DO NOT EDIT.

// Package project is the typed client for the "project" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: project's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on project's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package project

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "project"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.ProjectsFigures,
	client.ProjectsOwnership,
	client.ProjectsResolveKey,
	client.SitesLive,
	client.SitesResolve,
	client.SitesResolveOrg,
}

// ProjectsFigures the caller's headline project figures.
//
// Calls client.ProjectsFigures on project over the peer plane.
func ProjectsFigures(ctx context.Context, in *client.FiguresIn) (*client.FiguresOut, error) {
	return client.Call[client.FiguresIn, client.FiguresOut](ctx, App, client.ProjectsFigures, in)
}

// ProjectsOwnership report whether the calling org owns a project, and whether another o....
//
// Calls client.ProjectsOwnership on project over the peer plane.
func ProjectsOwnership(ctx context.Context, in *client.OwnerIn) (*client.Ownership, error) {
	return client.Call[client.OwnerIn, client.Ownership](ctx, App, client.ProjectsOwnership, in)
}

// ProjectsResolveKey resolve a publishable ingest key to the project that minted it.
//
// Calls client.ProjectsResolveKey on project over the peer plane.
func ProjectsResolveKey(ctx context.Context, in *client.KeyIn) (*client.Attribution, error) {
	return client.Call[client.KeyIn, client.Attribution](ctx, App, client.ProjectsResolveKey, in)
}

// SitesLive every serving site, across orgs.
//
// Calls client.SitesLive on project over the peer plane.
func SitesLive(ctx context.Context, in *client.LiveSitesIn) (*client.LiveSitesOut, error) {
	return client.Call[client.LiveSitesIn, client.LiveSitesOut](ctx, App, client.SitesLive, in)
}

// SitesResolve resolve a published site by host label.
//
// Calls client.SitesResolve on project over the peer plane.
func SitesResolve(ctx context.Context, in *client.SiteIn) (*client.Site, error) {
	return client.Call[client.SiteIn, client.Site](ctx, App, client.SitesResolve, in)
}

// SitesResolveOrg resolve a published site pinned to one org.
//
// Calls client.SitesResolveOrg on project over the peer plane.
func SitesResolveOrg(ctx context.Context, in *client.SiteIn) (*client.Site, error) {
	return client.Call[client.SiteIn, client.Site](ctx, App, client.SitesResolveOrg, in)
}
