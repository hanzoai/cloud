// Code generated by client/gen; DO NOT EDIT.

// Package integration is the typed client for the "integration" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: integration's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on integration's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package integration

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "integration"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.ChatIdentity,
	client.ChatSend,
	client.IntegrationsConnection,
	client.IntegrationsSlackSend,
}

// ChatIdentity resolve the Hanzo account a chat user has linked.
//
// Calls client.ChatIdentity on integration over the peer plane.
func ChatIdentity(ctx context.Context, in *client.ChatIdentityIn) (*client.ChatIdentityOut, error) {
	return client.Call[client.ChatIdentityIn, client.ChatIdentityOut](ctx, App, client.ChatIdentity, in)
}

// ChatSend post one message back to a chat platform as the org.
//
// Calls client.ChatSend on integration over the peer plane.
func ChatSend(ctx context.Context, in *client.ChatSendIn) (*client.ChatSendOut, error) {
	return client.Call[client.ChatSendIn, client.ChatSendOut](ctx, App, client.ChatSend, in)
}

// IntegrationsConnection whether the caller's org has connected a provider.
//
// Calls client.IntegrationsConnection on integration over the peer plane.
func IntegrationsConnection(ctx context.Context, in *client.ConnectionIn) (*client.Connection, error) {
	return client.Call[client.ConnectionIn, client.Connection](ctx, App, client.IntegrationsConnection, in)
}

// IntegrationsSlackSend post to an org's Slack channel via the org's KMS-custodied bot token.
//
// Calls client.IntegrationsSlackSend on integration over the peer plane.
func IntegrationsSlackSend(ctx context.Context, in *client.SlackSendIn) (*client.SlackSent, error) {
	return client.Call[client.SlackSendIn, client.SlackSent](ctx, App, client.IntegrationsSlackSend, in)
}
