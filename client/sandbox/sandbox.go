// Code generated by client/gen; DO NOT EDIT.

// Package sandbox is the typed client for the "sandbox" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: sandbox's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package client) and never on sandbox's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package sandbox

import (
	"context"

	"github.com/hanzoai/cloud/client"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "sandbox"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	client.SandboxAttach,
	client.SandboxEnd,
	client.SandboxLease,
	client.SandboxRead,
	client.SandboxRun,
	client.SandboxStop,
	client.SandboxWrite,
}

// SandboxAttach report that somebody is watching a project.
//
// Calls client.SandboxAttach on sandbox over the peer plane.
func SandboxAttach(ctx context.Context, in *client.AttachIn) (*client.Attached, error) {
	return client.Call[client.AttachIn, client.Attached](ctx, App, client.SandboxAttach, in)
}

// SandboxEnd end a sandbox's lease.
//
// Calls client.SandboxEnd on sandbox over the peer plane.
func SandboxEnd(ctx context.Context, in *client.EndIn) (*struct{}, error) {
	return client.Call[client.EndIn, struct{}](ctx, App, client.SandboxEnd, in)
}

// SandboxLease lease a sandbox, or resume one.
//
// Calls client.SandboxLease on sandbox over the peer plane.
func SandboxLease(ctx context.Context, in *client.LeaseIn) (*client.Leased, error) {
	return client.Call[client.LeaseIn, client.Leased](ctx, App, client.SandboxLease, in)
}

// SandboxRead read a file, or list a directory.
//
// Calls client.SandboxRead on sandbox over the peer plane.
func SandboxRead(ctx context.Context, in *client.PathIn) (*client.Blob, error) {
	return client.Call[client.PathIn, client.Blob](ctx, App, client.SandboxRead, in)
}

// SandboxRun run a command in a sandbox.
//
// Calls client.SandboxRun on sandbox over the peer plane.
func SandboxRun(ctx context.Context, in *client.RunIn) (*client.Ran, error) {
	return client.Call[client.RunIn, client.Ran](ctx, App, client.SandboxRun, in)
}

// SandboxStop interrupt what a sandbox is running.
//
// Calls client.SandboxStop on sandbox over the peer plane.
func SandboxStop(ctx context.Context, in *client.StopIn) (*client.Stopped, error) {
	return client.Call[client.StopIn, client.Stopped](ctx, App, client.SandboxStop, in)
}

// SandboxWrite write a file in a sandbox.
//
// Calls client.SandboxWrite on sandbox over the peer plane.
func SandboxWrite(ctx context.Context, in *client.WriteIn) (*client.Wrote, error) {
	return client.Call[client.WriteIn, client.Wrote](ctx, App, client.SandboxWrite, in)
}
