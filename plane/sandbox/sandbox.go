// Code generated by plane/gen; DO NOT EDIT.

// Package sandbox is the typed client for the "sandbox" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: sandbox's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package plane) and never on sandbox's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package sandbox

import (
	"context"

	"github.com/hanzoai/cloud/plane"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "sandbox"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	plane.SandboxAttach,
	plane.SandboxEnd,
	plane.SandboxLease,
	plane.SandboxRead,
	plane.SandboxRun,
	plane.SandboxStop,
	plane.SandboxWrite,
}

// SandboxAttach report that somebody is watching a project.
//
// Calls plane.SandboxAttach on sandbox over the peer plane.
func SandboxAttach(ctx context.Context, in *plane.AttachIn) (*plane.Attached, error) {
	return plane.Ask[plane.AttachIn, plane.Attached](ctx, App, plane.SandboxAttach, in)
}

// SandboxEnd end a sandbox's lease.
//
// Calls plane.SandboxEnd on sandbox over the peer plane.
func SandboxEnd(ctx context.Context, in *plane.EndIn) (*struct{}, error) {
	return plane.Ask[plane.EndIn, struct{}](ctx, App, plane.SandboxEnd, in)
}

// SandboxLease lease a sandbox, or resume one.
//
// Calls plane.SandboxLease on sandbox over the peer plane.
func SandboxLease(ctx context.Context, in *plane.LeaseIn) (*plane.Leased, error) {
	return plane.Ask[plane.LeaseIn, plane.Leased](ctx, App, plane.SandboxLease, in)
}

// SandboxRead read a file, or list a directory.
//
// Calls plane.SandboxRead on sandbox over the peer plane.
func SandboxRead(ctx context.Context, in *plane.PathIn) (*plane.Blob, error) {
	return plane.Ask[plane.PathIn, plane.Blob](ctx, App, plane.SandboxRead, in)
}

// SandboxRun run a command in a sandbox.
//
// Calls plane.SandboxRun on sandbox over the peer plane.
func SandboxRun(ctx context.Context, in *plane.RunIn) (*plane.Ran, error) {
	return plane.Ask[plane.RunIn, plane.Ran](ctx, App, plane.SandboxRun, in)
}

// SandboxStop interrupt what a sandbox is running.
//
// Calls plane.SandboxStop on sandbox over the peer plane.
func SandboxStop(ctx context.Context, in *plane.StopIn) (*plane.Stopped, error) {
	return plane.Ask[plane.StopIn, plane.Stopped](ctx, App, plane.SandboxStop, in)
}

// SandboxWrite write a file in a sandbox.
//
// Calls plane.SandboxWrite on sandbox over the peer plane.
func SandboxWrite(ctx context.Context, in *plane.WriteIn) (*plane.Wrote, error) {
	return plane.Ask[plane.WriteIn, plane.Wrote](ctx, App, plane.SandboxWrite, in)
}
