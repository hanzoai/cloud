// Code generated by plane/gen; DO NOT EDIT.

// Package framework is the typed client for the "framework" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: framework's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package plane) and never on framework's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package framework

import (
	"context"

	"github.com/hanzoai/cloud/plane"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "framework"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	plane.FrameworkDoc,
	plane.FrameworkDocs,
	plane.FrameworkFind,
	plane.FrameworkIngest,
	plane.FrameworkInstalled,
}

// FrameworkDoc read one document in the caller's org.
//
// Calls plane.FrameworkDoc on framework over the peer plane.
func FrameworkDoc(ctx context.Context, in *plane.FwDocIn) (*plane.FwDoc, error) {
	return plane.Ask[plane.FwDocIn, plane.FwDoc](ctx, App, plane.FrameworkDoc, in)
}

// FrameworkDocs list one DocType's documents in the caller's org.
//
// Calls plane.FrameworkDocs on framework over the peer plane.
func FrameworkDocs(ctx context.Context, in *plane.FwDocsIn) (*plane.FwDocs, error) {
	return plane.Ask[plane.FwDocsIn, plane.FwDocs](ctx, App, plane.FrameworkDocs, in)
}

// FrameworkFind find a document by one field's value in the caller's org.
//
// Calls plane.FrameworkFind on framework over the peer plane.
func FrameworkFind(ctx context.Context, in *plane.FwFindIn) (*plane.FwFound, error) {
	return plane.Ask[plane.FwFindIn, plane.FwFound](ctx, App, plane.FrameworkFind, in)
}

// FrameworkIngest create a document from trusted fields in the caller's org.
//
// Calls plane.FrameworkIngest on framework over the peer plane.
func FrameworkIngest(ctx context.Context, in *plane.FwIngestIn) (*plane.FwIngested, error) {
	return plane.Ask[plane.FwIngestIn, plane.FwIngested](ctx, App, plane.FrameworkIngest, in)
}

// FrameworkInstalled whether the caller's org holds a DocType.
//
// Calls plane.FrameworkInstalled on framework over the peer plane.
func FrameworkInstalled(ctx context.Context, in *plane.FwRef) (*plane.FwInstalled, error) {
	return plane.Ask[plane.FwRef, plane.FwInstalled](ctx, App, plane.FrameworkInstalled, in)
}
