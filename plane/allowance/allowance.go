// Code generated by plane/gen; DO NOT EDIT.

// Package allowance is the typed client for the "allowance" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: allowance's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package plane) and never on allowance's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package allowance

import (
	"context"

	"github.com/hanzoai/cloud/plane"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "allowance"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	plane.AllowanceRead,
	plane.AllowanceTake,
}

// AllowanceRead read what a subject has left of their plan's free calls.
//
// Calls plane.AllowanceRead on allowance over the peer plane.
func AllowanceRead(ctx context.Context, in *plane.AllowanceIn) (*plane.Allowance, error) {
	return plane.Ask[plane.AllowanceIn, plane.Allowance](ctx, App, plane.AllowanceRead, in)
}

// AllowanceTake count one served free call against a subject's plan allowance.
//
// Calls plane.AllowanceTake on allowance over the peer plane.
func AllowanceTake(ctx context.Context, in *plane.AllowanceIn) (*plane.Allowance, error) {
	return plane.Ask[plane.AllowanceIn, plane.Allowance](ctx, App, plane.AllowanceTake, in)
}
