// Code generated by plane/gen; DO NOT EDIT.

// Package channel is the typed client for the "channel" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: channel's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package plane) and never on channel's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package channel

import (
	"context"

	"github.com/hanzoai/cloud/plane"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "channel"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	plane.ChannelsAgent,
	plane.ChannelsIngest,
	plane.ChannelsRecent,
}

// ChannelsAgent the agent that answers one room.
//
// Calls plane.ChannelsAgent on channel over the peer plane.
func ChannelsAgent(ctx context.Context, in *plane.AgentForIn) (*plane.AgentFor, error) {
	return plane.Ask[plane.AgentForIn, plane.AgentFor](ctx, App, plane.ChannelsAgent, in)
}

// ChannelsIngest take one authenticated inbound chat event from a platform adapter.
//
// Calls plane.ChannelsIngest on channel over the peer plane.
func ChannelsIngest(ctx context.Context, in *plane.ChannelsIngestIn) (*plane.ChannelsIngestOut, error) {
	return plane.Ask[plane.ChannelsIngestIn, plane.ChannelsIngestOut](ctx, App, plane.ChannelsIngest, in)
}

// ChannelsRecent the last turns of one room, oldest first.
//
// Calls plane.ChannelsRecent on channel over the peer plane.
func ChannelsRecent(ctx context.Context, in *plane.RecentIn) (*plane.Recent, error) {
	return plane.Ask[plane.RecentIn, plane.Recent](ctx, App, plane.ChannelsRecent, in)
}
