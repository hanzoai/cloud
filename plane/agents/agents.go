// Code generated by plane/gen; DO NOT EDIT.

// Package agents is the typed client for the "agents" app's internal ops.
//
// Every call here is a ZAP call BY NAME over the peer's unix socket — no URL, no
// env var, no HTTP round trip, and no way to reach the public edge by accident.
// That last one is not a style preference: agents's own callers used to reach it
// through CLOUD_COMMERCE_HTTP_URL, which defaulted to the api.hanzo.ai edge —
// the SAME binary — so the forwarder re-entered itself and the billing gate died
// of it. A call by name cannot express that mistake.
//
// It depends on the CONTRACT (package plane) and never on agents's implementation,
// so importing it costs a caller nothing but the types it is already sending.
package agents

import (
	"context"

	"github.com/hanzoai/cloud/plane"
)

// App is the peer's name, which is also the socket it answers on. It is the
// ONE place this package spells it.
const App = "agents"

// Ops is every op this peer answers, in the spelling the wire uses.
//
// It exists so a test can hold this generated surface against the LIVE plane
// registry and fail when they disagree — which is what lets this be generated
// from source without the document drifting away from the program. See
// plane_registry_test.go.
var Ops = []string{
	plane.AgentsSessionsCount,
	plane.AgentsSessionsStop,
}

// AgentsSessionsCount count the live sessions a match selects.
//
// Calls plane.AgentsSessionsCount on agents over the peer plane.
func AgentsSessionsCount(ctx context.Context, in *plane.SessionMatchIn) (*plane.SessionCount, error) {
	return plane.Ask[plane.SessionMatchIn, plane.SessionCount](ctx, App, plane.AgentsSessionsCount, in)
}

// AgentsSessionsStop stop the live sessions a credential revoke tears down.
//
// Calls plane.AgentsSessionsStop on agents over the peer plane.
func AgentsSessionsStop(ctx context.Context, in *plane.SessionMatchIn) (*plane.SessionCount, error) {
	return plane.Ask[plane.SessionMatchIn, plane.SessionCount](ctx, App, plane.AgentsSessionsStop, in)
}
