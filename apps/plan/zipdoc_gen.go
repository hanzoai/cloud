// Code generated by zipdoc; DO NOT EDIT.

package plan

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/plans/health", zip.Doc{
		Description: "health reports that the plans subsystem is mounted and answering. It runs no JS\nand reads no catalog, so it stays true when the bundle cannot resolve a plan.",
		Fields: map[string]string{
			"healthView.service": "Service names which subsystem answered, so a shadowing route is visible.",
			"healthView.status":  "Status is \"ok\" whenever the route answers — the probe is native, so it needs\nneither the bundle nor a catalog to reply.",
		},
		Response: json.RawMessage(`{"status":"ok","service":"plans"}`),
	})
}
