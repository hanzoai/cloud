// Code generated by zipdoc; DO NOT EDIT.

package connectorruntime

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("POST /v1/auto/connectors/:id/run", zip.Doc{
		Description: "Run executes one connector action in-process and answers the outcome. The\ncaller's resolved credential travels in `auth`, delivered to the action\nverbatim — the runtime resolves no credential itself. An action that ran and\nfailed (or an action name the connector does not have) answers ok:false with\nthe failure message, not an HTTP error; an unknown connector is 404 and a\nmissing action 422.",
		Fields: map[string]string{
			"runIn.action":   "Action is the name of the connector action to invoke.",
			"runIn.auth":     "Auth is the caller's resolved credential for the connector, handed to the\naction verbatim. Its shape is whatever the connector's auth descriptor\ndeclares (a token string, an object), so it is opaque here.",
			"runIn.id":       "ID is the connector to run, from the path.",
			"runIn.props":    "Props are the action's input properties, keyed by property name.",
			"runResp.error":  "Error is the connector-level failure message when not ok.",
			"runResp.ok":     "Ok reports whether the action ran to completion.",
			"runResp.output": "Output is the action's result when ok. Its shape is the action's own.",
		},
		Example: json.RawMessage(`{"id":"notion","action":"create_page","auth":"secret-token","props":{"title":"Hello"}}`),
	})
}
