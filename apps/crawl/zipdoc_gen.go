// Code generated by zipdoc; DO NOT EDIT.

package crawl

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("POST /v1/crawl", zip.Doc{
		Description: "handle fetches ONE url and returns it as markdown plus the metadata the page\ndeclared about itself, archiving the result under the caller's own corpus. A\npage that cannot be fetched is a 200 with success:false and the reason — the\nask was well-formed, the page was not there — so a non-2xx from this route\nalways means the request itself was refused.",
		Fields: map[string]string{
			"CrawlRequest.url":  "URL is the page to fetch. Required. It is fetched from inside the cluster,\nso it is checked against the address guard before any connection is made.",
			"CrawlView.data":    "Data is the crawled page, present only on success.",
			"CrawlView.error":   "Error says why the fetch failed — refused host, unreachable, wrong content\ntype — verbatim, so a caller debugging a crawl can tell those apart.",
			"CrawlView.success": "Success is true when the page was fetched and extracted. False with a 200 is\na normal answer: the ask was well-formed and the page was unreachable.",
			"Document.markdown": "Markdown is the readable content. Empty is a legitimate result for a page\nthat carries none.",
			"Document.metadata": "Metadata is what the page declared about itself (og:*, description, …).",
			"Document.title":    "Title is the document title, best-effort from <title> or og:title.",
			"Document.url":      "URL is the FINAL url after redirects, not the one asked for.",
		},
		Example:  json.RawMessage(`{"url":"https://hanzo.ai/about"}`),
		Response: json.RawMessage(`{"success":true,"data":{"url":"https://hanzo.ai/about","title":"About Hanzo","markdown":"# About Hanzo\n…"}}`),
	})
}
