// Code generated by zipdoc; DO NOT EDIT.

package ingress

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/ingress/middlewares/:id", zip.Doc{
		Description: "DeleteMiddleware removes one of the caller org's edge transforms and hot-applies\nthe change. Routes still naming it stop being served (they compile as skipped)\nuntil they name a transform that exists. Answers 204; an id this org does not\nhold is 404.",
		Fields: map[string]string{
			"objRef.id": "ID is the object to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"strip-api"}`),
	})
	zip.Describe("DELETE /v1/ingress/routes/:id", zip.Doc{
		Description: "DeleteRoute removes one of the caller org's routing rules and hot-applies the\nshrunken table, freeing its host for another claim. Answers 204; an id this org\ndoes not hold is 404.",
		Fields: map[string]string{
			"objRef.id": "ID is the object to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"web"}`),
	})
	zip.Describe("DELETE /v1/ingress/services/:id", zip.Doc{
		Description: "DeleteService removes one of the caller org's backend pools and hot-applies the\nchange. Routes still pointing at it stop being served (they compile as skipped)\nuntil they name a pool that exists. Answers 204; an id this org does not hold\nis 404.",
		Fields: map[string]string{
			"objRef.id": "ID is the object to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"app-pool"}`),
	})
	zip.Describe("GET /v1/ingress/middlewares", zip.Doc{
		Description: "ListMiddlewares returns every edge transform the caller's org has configured,\nordered by id. A route names the ones it wants, in order.",
		Fields: map[string]string{
			"Middleware.config":              "Config is the transform's parameters: redirectScheme takes scheme (default\nhttps) and permanent (\"true\" ⇒ 301, else 302); stripPrefix REQUIRES\nprefixes (comma-separated, first match wins); addPrefix REQUIRES prefix;\nheaders is a header→value map set on the response.",
			"Middleware.id":                  "ID identifies the transform within the org: [A-Za-z0-9-_.], at most 128\nchars. A create that omits it gets a generated one. Routes reference it by\nthis id.",
			"Middleware.type":                "Type is the transform: redirectScheme, stripPrefix, addPrefix or headers.",
			"ingressMiddlewares.middlewares": "Middlewares is the org's middlewares, ordered by id.",
		},
	})
	zip.Describe("GET /v1/ingress/middlewares/:id", zip.Doc{
		Description: "GetMiddleware returns one of the caller org's edge transforms by id.",
		Fields: map[string]string{
			"Middleware.config": "Config is the transform's parameters: redirectScheme takes scheme (default\nhttps) and permanent (\"true\" ⇒ 301, else 302); stripPrefix REQUIRES\nprefixes (comma-separated, first match wins); addPrefix REQUIRES prefix;\nheaders is a header→value map set on the response.",
			"Middleware.id":     "ID identifies the transform within the org: [A-Za-z0-9-_.], at most 128\nchars. A create that omits it gets a generated one. Routes reference it by\nthis id.",
			"Middleware.type":   "Type is the transform: redirectScheme, stripPrefix, addPrefix or headers.",
			"objRef.id":         "ID is the object to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"strip-api"}`),
	})
	zip.Describe("GET /v1/ingress/routes", zip.Doc{
		Description: "ListRoutes returns every routing rule the caller's org has configured, ordered\nby id. A route maps an exact Host (and optional path prefix) to a service.",
		Fields: map[string]string{
			"Route.host":           "Host is the exact hostname this route matches, lowercased with any trailing\ndot stripped. It is a GLOBALLY unique claim — one route across the whole\nedge may hold a host, so no tenant can hijack another's.",
			"Route.id":             "ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars.\nA create that omits it gets a generated one.",
			"Route.middlewares":    "Middlewares are the ids of the edge transforms to apply, in this order,\nbefore the request reaches the service. At most 16.",
			"Route.pathPrefix":     "PathPrefix narrows the match to requests under this path; it must start\nwith \"/\". Empty matches every path on the host.",
			"Route.priority":       "Priority orders routes that share a host: higher wins, and equal priorities\nfall back to the longer PathPrefix.",
			"Route.service":        "Service is the id of the backend pool this route dispatches to. A route\nnaming a service that does not exist is skipped at compile, not served.",
			"Route.tls":            "TLS asks the edge to terminate TLS for Host with an ACME-managed certificate.",
			"ingressRoutes.routes": "Routes is the org's routes, ordered by id.",
		},
	})
	zip.Describe("GET /v1/ingress/routes/:id", zip.Doc{
		Description: "GetRoute returns one of the caller org's routing rules by id.",
		Fields: map[string]string{
			"Route.host":        "Host is the exact hostname this route matches, lowercased with any trailing\ndot stripped. It is a GLOBALLY unique claim — one route across the whole\nedge may hold a host, so no tenant can hijack another's.",
			"Route.id":          "ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars.\nA create that omits it gets a generated one.",
			"Route.middlewares": "Middlewares are the ids of the edge transforms to apply, in this order,\nbefore the request reaches the service. At most 16.",
			"Route.pathPrefix":  "PathPrefix narrows the match to requests under this path; it must start\nwith \"/\". Empty matches every path on the host.",
			"Route.priority":    "Priority orders routes that share a host: higher wins, and equal priorities\nfall back to the longer PathPrefix.",
			"Route.service":     "Service is the id of the backend pool this route dispatches to. A route\nnaming a service that does not exist is skipped at compile, not served.",
			"Route.tls":         "TLS asks the edge to terminate TLS for Host with an ACME-managed certificate.",
			"objRef.id":         "ID is the object to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"a1b2c3d4e5f60718"}`),
	})
	zip.Describe("GET /v1/ingress/services", zip.Doc{
		Description: "ListServices returns every backend pool the caller's org has configured,\nordered by id. A service is the weighted round-robin target a route dispatches\nto.",
		Fields: map[string]string{
			"Backend.url":              "URL is the upstream server, http(s)://host[:port].",
			"Backend.weight":           "Weight is this member's share of the round-robin; must be >= 0.",
			"Service.backends":         "Backends are the upstream servers to balance across: 1..32 of them.",
			"Service.id":               "ID identifies the pool within the org: [A-Za-z0-9-_.], at most 128 chars.\nA create that omits it gets a generated one. Routes reference it by this id.",
			"Service.passHostHeader":   "PassHostHeader forwards the client's original Host header upstream instead\nof rewriting it to the backend's.",
			"ingressServices.services": "Services is the org's services, ordered by id.",
		},
	})
	zip.Describe("GET /v1/ingress/services/:id", zip.Doc{
		Description: "GetService returns one of the caller org's backend pools by id.",
		Fields: map[string]string{
			"Backend.url":            "URL is the upstream server, http(s)://host[:port].",
			"Backend.weight":         "Weight is this member's share of the round-robin; must be >= 0.",
			"Service.backends":       "Backends are the upstream servers to balance across: 1..32 of them.",
			"Service.id":             "ID identifies the pool within the org: [A-Za-z0-9-_.], at most 128 chars.\nA create that omits it gets a generated one. Routes reference it by this id.",
			"Service.passHostHeader": "PassHostHeader forwards the client's original Host header upstream instead\nof rewriting it to the backend's.",
			"objRef.id":              "ID is the object to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"app-pool"}`),
	})
	zip.Describe("GET /v1/ingress/status", zip.Doc{
		Description: "Status reports the ingress edge's live posture.\nThat is the role this instance runs in (app or edge), whether its listeners are\nbound and on which addresses, the ACME posture (staging flag and certificate cache\ndirectory), how many hosts the compiled route table currently serves, and how many\nthe ACME HostPolicy will issue a certificate for.",
		Fields: map[string]string{
			"ingressStatus.acmeCacheDir": "ACMECacheDir is where autocert persists accounts and certificates.",
			"ingressStatus.acmeStaging":  "ACMEStaging is true when certificates are issued from Let's Encrypt staging.",
			"ingressStatus.edgeEnabled":  "EdgeEnabled is true when the edge listeners are actually bound.",
			"ingressStatus.httpAddr":     "HTTPAddr is the address the ACME HTTP-01 + HTTP router listens on.",
			"ingressStatus.httpsAddr":    "HTTPSAddr is the address the SNI TLS terminator listens on.",
			"ingressStatus.liveHosts":    "LiveHosts is how many hosts the compiled table routes.",
			"ingressStatus.proxy":        "Proxy names the reverse-proxy implementation behind every route.",
			"ingressStatus.role":         "Role is \"edge\" when CLOUD_INGRESS_EDGE_ENABLED is set, else \"app\".",
			"ingressStatus.tlsHosts":     "TLSHosts is how many hosts the ACME HostPolicy will issue a certificate\nfor. NOT a subset of LiveHosts: an extraHost owns no route, and a TLS route\nnaming a missing service is skipped while its host still wants a cert.",
		},
	})
	zip.Describe("GET /v1/ingress/tls", zip.Doc{
		Description: "GetTLS returns the caller org's ACME intent and the edge-wide TLS facts it lands in.\nThose are: which role this instance runs in, whether its listeners are bound, every\nhost the ACME HostPolicy will issue a certificate for (the union across ALL orgs of\nTLS-marked routes and configured extraHosts, because one process holds one\ncertificate cache), and the ACME directory and account email the process was\nstarted with.",
		Fields: map[string]string{
			"TLSConfig.acmeEmail":      "ACMEEmail is the ACME account email. It binds an account for the lifetime\nof an edge process, so it applies only when the edge (re)starts.",
			"TLSConfig.extraHosts":     "ExtraHosts get certificates without owning a route — at most 256. They feed\nthe ACME HostPolicy and hot-apply on the next reload.",
			"TLSConfig.staging":        "Staging issues from Let's Encrypt's staging directory (untrusted certs, high\nrate limits). Like ACMEEmail it applies only when the edge (re)starts.",
			"ingressTLS.acmeDirectory": "ACMEDirectory is the ACME endpoint in use: the staging URL, or\n\"letsencrypt-production\".",
			"ingressTLS.acmeEmail":     "ACMEEmail is the account email the PROCESS was started with\n(CLOUD_INGRESS_ACME_EMAIL), not the stored config's.",
			"ingressTLS.config":        "Config is the caller org's stored ACME intent.",
			"ingressTLS.edgeEnabled":   "EdgeEnabled is true when the edge listeners are actually bound.",
			"ingressTLS.managedHosts":  "ManagedHosts is every host the ACME HostPolicy will issue a certificate for\n— the union across ALL orgs of TLS-marked routes and configured extraHosts,\nbecause one process holds one certificate cache.",
			"ingressTLS.note":          "Note states which fields hot-apply and which need an edge restart.",
			"ingressTLS.role":          "Role is \"edge\" when this instance binds the listeners, else \"app\".",
		},
	})
	zip.Describe("POST /v1/ingress/middlewares", zip.Doc{
		Description: "PutMiddleware creates or replaces one edge transform and hot-applies it. POST\nmints an id when the body omits one; PUT takes the id from the URL, which wins\nover any id in the body. type must be one of redirectScheme, stripPrefix,\naddPrefix or headers, and stripPrefix/addPrefix each require their config key.",
		Fields: map[string]string{
			"Middleware.config": "Config is the transform's parameters: redirectScheme takes scheme (default\nhttps) and permanent (\"true\" ⇒ 301, else 302); stripPrefix REQUIRES\nprefixes (comma-separated, first match wins); addPrefix REQUIRES prefix;\nheaders is a header→value map set on the response.",
			"Middleware.id":     "ID identifies the transform within the org: [A-Za-z0-9-_.], at most 128\nchars. A create that omits it gets a generated one. Routes reference it by\nthis id.",
			"Middleware.type":   "Type is the transform: redirectScheme, stripPrefix, addPrefix or headers.",
		},
		Example: json.RawMessage(`{"id":"strip-api","type":"stripPrefix","config":{"prefixes":"/api"}}`),
	})
	zip.Describe("POST /v1/ingress/routes", zip.Doc{
		Description: "PutRoute creates or replaces one routing rule and hot-applies the new table —\nthere is no config file and no restart. POST mints an id when the body omits\none; PUT takes the id from the URL, which wins over any id in the body. A\nroute's host is a GLOBALLY unique DNS claim: a host another org's route already\nholds is refused 409, so no tenant can hijack another's hostname.",
		Fields: map[string]string{
			"Route.host":        "Host is the exact hostname this route matches, lowercased with any trailing\ndot stripped. It is a GLOBALLY unique claim — one route across the whole\nedge may hold a host, so no tenant can hijack another's.",
			"Route.id":          "ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars.\nA create that omits it gets a generated one.",
			"Route.middlewares": "Middlewares are the ids of the edge transforms to apply, in this order,\nbefore the request reaches the service. At most 16.",
			"Route.pathPrefix":  "PathPrefix narrows the match to requests under this path; it must start\nwith \"/\". Empty matches every path on the host.",
			"Route.priority":    "Priority orders routes that share a host: higher wins, and equal priorities\nfall back to the longer PathPrefix.",
			"Route.service":     "Service is the id of the backend pool this route dispatches to. A route\nnaming a service that does not exist is skipped at compile, not served.",
			"Route.tls":         "TLS asks the edge to terminate TLS for Host with an ACME-managed certificate.",
		},
		Example: json.RawMessage(`{"id":"web","host":"app.example.com","service":"app-pool","tls":true}`),
	})
	zip.Describe("POST /v1/ingress/services", zip.Doc{
		Description: "PutService creates or replaces one backend pool and hot-applies it. POST mints\nan id when the body omits one; PUT takes the id from the URL, which wins over\nany id in the body. A pool needs at least one backend and every backend URL\nmust be http(s)://host[:port].",
		Fields: map[string]string{
			"Backend.url":            "URL is the upstream server, http(s)://host[:port].",
			"Backend.weight":         "Weight is this member's share of the round-robin; must be >= 0.",
			"Service.backends":       "Backends are the upstream servers to balance across: 1..32 of them.",
			"Service.id":             "ID identifies the pool within the org: [A-Za-z0-9-_.], at most 128 chars.\nA create that omits it gets a generated one. Routes reference it by this id.",
			"Service.passHostHeader": "PassHostHeader forwards the client's original Host header upstream instead\nof rewriting it to the backend's.",
		},
		Example: json.RawMessage(`{"id":"app-pool","backends":[{"url":"http://10.0.0.7:8000","weight":1}]}`),
	})
	zip.Describe("PUT /v1/ingress/middlewares/:id", zip.Doc{
		Description: "PutMiddleware creates or replaces one edge transform and hot-applies it. POST\nmints an id when the body omits one; PUT takes the id from the URL, which wins\nover any id in the body. type must be one of redirectScheme, stripPrefix,\naddPrefix or headers, and stripPrefix/addPrefix each require their config key.",
		Fields: map[string]string{
			"Middleware.config": "Config is the transform's parameters: redirectScheme takes scheme (default\nhttps) and permanent (\"true\" ⇒ 301, else 302); stripPrefix REQUIRES\nprefixes (comma-separated, first match wins); addPrefix REQUIRES prefix;\nheaders is a header→value map set on the response.",
			"Middleware.id":     "ID identifies the transform within the org: [A-Za-z0-9-_.], at most 128\nchars. A create that omits it gets a generated one. Routes reference it by\nthis id.",
			"Middleware.type":   "Type is the transform: redirectScheme, stripPrefix, addPrefix or headers.",
		},
		Example: json.RawMessage(`{"id":"strip-api","type":"stripPrefix","config":{"prefixes":"/api"}}`),
	})
	zip.Describe("PUT /v1/ingress/routes/:id", zip.Doc{
		Description: "PutRoute creates or replaces one routing rule and hot-applies the new table —\nthere is no config file and no restart. POST mints an id when the body omits\none; PUT takes the id from the URL, which wins over any id in the body. A\nroute's host is a GLOBALLY unique DNS claim: a host another org's route already\nholds is refused 409, so no tenant can hijack another's hostname.",
		Fields: map[string]string{
			"Route.host":        "Host is the exact hostname this route matches, lowercased with any trailing\ndot stripped. It is a GLOBALLY unique claim — one route across the whole\nedge may hold a host, so no tenant can hijack another's.",
			"Route.id":          "ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars.\nA create that omits it gets a generated one.",
			"Route.middlewares": "Middlewares are the ids of the edge transforms to apply, in this order,\nbefore the request reaches the service. At most 16.",
			"Route.pathPrefix":  "PathPrefix narrows the match to requests under this path; it must start\nwith \"/\". Empty matches every path on the host.",
			"Route.priority":    "Priority orders routes that share a host: higher wins, and equal priorities\nfall back to the longer PathPrefix.",
			"Route.service":     "Service is the id of the backend pool this route dispatches to. A route\nnaming a service that does not exist is skipped at compile, not served.",
			"Route.tls":         "TLS asks the edge to terminate TLS for Host with an ACME-managed certificate.",
		},
		Example: json.RawMessage(`{"id":"web","host":"app.example.com","service":"app-pool","tls":true}`),
	})
	zip.Describe("PUT /v1/ingress/services/:id", zip.Doc{
		Description: "PutService creates or replaces one backend pool and hot-applies it. POST mints\nan id when the body omits one; PUT takes the id from the URL, which wins over\nany id in the body. A pool needs at least one backend and every backend URL\nmust be http(s)://host[:port].",
		Fields: map[string]string{
			"Backend.url":            "URL is the upstream server, http(s)://host[:port].",
			"Backend.weight":         "Weight is this member's share of the round-robin; must be >= 0.",
			"Service.backends":       "Backends are the upstream servers to balance across: 1..32 of them.",
			"Service.id":             "ID identifies the pool within the org: [A-Za-z0-9-_.], at most 128 chars.\nA create that omits it gets a generated one. Routes reference it by this id.",
			"Service.passHostHeader": "PassHostHeader forwards the client's original Host header upstream instead\nof rewriting it to the backend's.",
		},
		Example: json.RawMessage(`{"id":"app-pool","backends":[{"url":"http://10.0.0.7:8000","weight":1}]}`),
	})
	zip.Describe("PUT /v1/ingress/tls", zip.Doc{
		Description: "PutTLS replaces the caller org's ACME intent and hot-applies what can be\nhot-applied. extraHosts are normalized and validated, then feed the ACME\nHostPolicy on the reload this op performs, alongside the per-route tls flags.\nacmeEmail and staging bind an ACME account for the lifetime of an edge process,\nso they only take effect when the edge (re)starts — the returned note says so.",
		Fields: map[string]string{
			"TLSConfig.acmeEmail":  "ACMEEmail is the ACME account email. It binds an account for the lifetime\nof an edge process, so it applies only when the edge (re)starts.",
			"TLSConfig.extraHosts": "ExtraHosts get certificates without owning a route — at most 256. They feed\nthe ACME HostPolicy and hot-apply on the next reload.",
			"TLSConfig.staging":    "Staging issues from Let's Encrypt's staging directory (untrusted certs, high\nrate limits). Like ACMEEmail it applies only when the edge (re)starts.",
		},
		Example: json.RawMessage(`{"extraHosts":["www.example.com"]}`),
	})
}
