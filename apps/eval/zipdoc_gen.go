// Code generated by zipdoc; DO NOT EDIT.

package eval

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/evals/datasets/:name", zip.Doc{
		Description: "deleteDataset removes one of the caller org's datasets and answers 204. A dataset\nbelonging to another org reads as not found and is left untouched.",
		Fields: map[string]string{
			"DatasetRef.name": "Name is the dataset name from the path.",
		},
		Example: json.RawMessage(`{"name":"support-golden"}`),
	})
	zip.Describe("GET /v1/evals/dataset-items", zip.Doc{
		Description: "listItems returns one dataset's rows, active and archived alike. The dataset is\nresolved within the caller's org, so another org's dataset reads as empty.",
		Fields: map[string]string{
			"ItemList.data":         "Data is the items, newest first.",
			"ItemQuery.datasetName": "Dataset is the dataset name to read. Required.",
			"ItemQuery.limit":       "Limit caps the rows returned.",
		},
		Example: json.RawMessage(`{"datasetName":"support-golden","limit":100}`),
	})
	zip.Describe("GET /v1/evals/datasets", zip.Doc{
		Description: "listDatasets returns the caller org's datasets. Item counts are not computed here —\nread one dataset for its count.",
		Fields: map[string]string{
			"DatasetList.data": "Data is the datasets, newest first. Item counts are omitted here — read one\ndataset to get its count.",
			"Page.limit":       "Limit caps the rows returned; 0 or below means the default and anything\nabove the maximum is clamped to it.",
		},
	})
	zip.Describe("GET /v1/evals/datasets/:name", zip.Doc{
		Description: "getDataset returns one of the caller org's datasets with its live item count. A\ndataset belonging to another org reads as not found.",
		Fields: map[string]string{
			"DatasetRef.name": "Name is the dataset name from the path.",
		},
		Example: json.RawMessage(`{"name":"support-golden"}`),
	})
	zip.Describe("GET /v1/evals/evaluators", zip.Doc{
		Description: "listEvaluators returns the caller org's LLM-as-judge definitions.",
		Fields: map[string]string{
			"EvaluatorList.data": "Data is the evaluators, newest first.",
			"Page.limit":         "Limit caps the rows returned; 0 or below means the default and anything\nabove the maximum is clamped to it.",
		},
	})
	zip.Describe("GET /v1/evals/metrics", zip.Doc{
		Description: "metricsBoard serves the AI observability dashboard for the caller's org. It\nreports request counts, cost, tokens, error and success rate, and latency\npercentiles over a window, bucketed and broken down by model. A validated\nSuperAdmin sees every org. Tenant\nisolation is the principal gate: no validated principal is a 403. When telemetry is\ndisabled, or the caller's project is a named sub-scope the ledger does not yet\nattribute, the board is honest-empty — a valid all-zero board, never a fabricated\nnumber and never a 503.",
		Fields: map[string]string{
			"BoardPoint.t":            "RFC3339 (UTC) bucket start",
			"BoardQuery.interval":     "Interval overrides the bucket: hour or day. Anything else keeps the\npreset's own bucket.",
			"BoardQuery.range":        "Range is a fixed window preset: 24h, 7d or 30d. Anything else means 24h.",
			"BoardRange.end":          "RFC3339 (UTC)",
			"BoardRange.interval":     "hour | day",
			"BoardRange.range":        "echoed label (24h | 7d | 30d | custom)",
			"BoardRange.start":        "RFC3339 (UTC)",
			"BoardScope.org":          "\"\" when AllOrgs",
			"BoardTotals.successRate": "0..1",
			"ModelStat.costPct":       "share of total spend, 0..100",
			"ModelStat.errorRate":     "0..1",
			"ModelStat.modelCount":    ">0 only on the \"other\" fold",
		},
		Example: json.RawMessage(`{"range":"7d","interval":"day"}`),
	})
	zip.Describe("GET /v1/evals/runs", zip.Doc{
		Description: "listRuns returns the caller org's eval-run history. Each row carries what was\nscored, by which judge, and the mean over the items that actually scored. Read\nfrom the metastore, so it is available whether or not telemetry is wired.",
		Fields: map[string]string{
			"RunList.data":         "Data is the runs, newest first.",
			"RunQuery.datasetName": "Dataset narrows to one dataset's runs.",
			"RunQuery.limit":       "Limit caps the rows returned.",
			"RunView.avgScore":     "AvgScore is the mean over the scored items only.",
			"RunView.createdAt":    "CreatedAt and UpdatedAt are RFC3339 timestamps.",
			"RunView.dataset":      "Dataset is the dataset the run scored.",
			"RunView.items":        "Items is how many dataset rows the run attempted, Scored how many produced a\nreal score — the two differ whenever items errored.",
			"RunView.model":        "Model is the model under test; JudgeModel is the LLM-as-judge that scored it.",
			"RunView.runName":      "RunName is the run's name — the key scores and traces are tagged with.",
		},
		Example: json.RawMessage(`{"datasetName":"support-golden","limit":50}`),
	})
	zip.Describe("GET /v1/evals/score-configs", zip.Doc{
		Description: "listScoreConfigs returns the caller org's score configs — the rules every recorded\nscore of that name is validated against.",
		Fields: map[string]string{
			"Page.limit":           "Limit caps the rows returned; 0 or below means the default and anything\nabove the maximum is clamped to it.",
			"ScoreConfigList.data": "Data is the configs, newest first.",
		},
	})
	zip.Describe("GET /v1/evals/scores", zip.Doc{
		Description: "listScores returns the caller org's recorded scores, newest first. The org is read\nfrom the validated principal, never a client header, so the filters narrow within\none tenant and can never widen past it. Requires telemetry.",
		Fields: map[string]string{
			"ScoreList.data":     "Data is the score events, newest first.",
			"ScoreQuery.limit":   "Limit caps the rows returned.",
			"ScoreQuery.name":    "Name narrows to one score name.",
			"ScoreQuery.runName": "RunName narrows to one eval run.",
			"ScoreQuery.traceId": "TraceID narrows to one trace.",
		},
		Example: json.RawMessage(`{"runName":"run-20260729T101500Z","limit":100}`),
	})
	zip.Describe("GET /v1/evals/traces", zip.Doc{
		Description: "listTraces returns the caller org's traces, newest first. Both tenant keys are\nserver-side: the org from the validated principal and the project from its minted\nscope, so a caller cannot read another tenant's or another project's traces.\nRequires telemetry.",
		Fields: map[string]string{
			"TraceList.data":         "Data is the traces, newest first.",
			"TraceQuery.datasetName": "Dataset narrows to the traces of one dataset's items.",
			"TraceQuery.limit":       "Limit caps the rows returned.",
			"TraceQuery.runName":     "RunName narrows to one eval run.",
			"TraceQuery.sessionId":   "SessionID narrows to one session.",
			"TraceView.apiKeyHash":   "APIKeyHash is the non-reversible credential ref (never a plaintext key).",
			"TraceView.latencyMs":    "LatencyMs is EndTime-StartTime in milliseconds, nil when the trace carries\nno timing (so the console renders \"—\", never a fabricated 0).",
		},
		Example: json.RawMessage(`{"runName":"run-20260729T101500Z","limit":50}`),
	})
	zip.Describe("POST /v1/evals/dataset-items", zip.Doc{
		Description: "createItem adds one row to a dataset and answers 201. The dataset must already exist\nin the caller's org — an item can never be attached to a dataset the caller does not\nown. An id already used by a different dataset is a conflict.",
		Fields: map[string]string{
			"ItemInput.datasetName":    "Dataset is the name of the dataset the item belongs to. Required, and it\nmust already exist in the caller's org.",
			"ItemInput.expectedOutput": "Expected is the arbitrary JSON golden answer.",
			"ItemInput.id":             "ID is the item id; empty mints one. Must be a slug when given.",
			"ItemInput.input":          "Input is the arbitrary JSON the model under test is given.",
			"ItemInput.metadata":       "Metadata is an arbitrary JSON object stored alongside the item.",
			"ItemInput.status":         "Status is ACTIVE or ARCHIVED; empty means ACTIVE. Only ACTIVE items are run.",
		},
		Example: json.RawMessage(`{"datasetName":"support-golden","input":{"question":"how do I reset my key?"},"expectedOutput":{"answer":"rotate it in the console"},"status":"ACTIVE"}`),
	})
	zip.Describe("POST /v1/evals/datasets", zip.Doc{
		Description: "createDataset registers a dataset in the caller's org and answers 201. The name must\nbe a slug and unique within the org; a repeat upserts the description and metadata\nrather than duplicating the row.",
		Fields: map[string]string{
			"DatasetInput.description": "Description is free text describing what the dataset holds.",
			"DatasetInput.metadata":    "Metadata is an arbitrary JSON object stored alongside the dataset.",
			"DatasetInput.name":        "Name is the dataset name, unique within the org.",
		},
		Example: json.RawMessage(`{"name":"support-golden","description":"Golden answers for support triage","metadata":{"owner":"support"}}`),
	})
	zip.Describe("POST /v1/evals/evaluators", zip.Doc{
		Description: "createEvaluator registers an LLM-as-judge in the caller's org, answering 201.\nAn evaluator is a judge model plus the rubric it scores against; a repeat with\nthe same name upserts.",
		Fields: map[string]string{
			"EvaluatorInput.criteria":  "Criteria is the judging rubric handed to the judge model.",
			"EvaluatorInput.model":     "Model is the judge model id.",
			"EvaluatorInput.name":      "Name is the evaluator name, unique within the org.",
			"EvaluatorInput.scoreName": "ScoreName is the score this evaluator writes; empty means the evaluator name.",
		},
		Example: json.RawMessage(`{"name":"helpfulness","model":"zen5-pro","criteria":"Score 1 if the answer resolves the question, else 0.","scoreName":"helpfulness"}`),
	})
	zip.Describe("POST /v1/evals/score-configs", zip.Doc{
		Description: "createScoreConfig declares what a score of this name may carry. That is its type\nplus its numeric bounds or its allowed labels, and it answers 201. Once a config\nexists it is AUTHORITATIVE: a later score of that name is validated against it and\ncannot claim a different type.",
		Fields: map[string]string{
			"ScoreConfigInput.categories": "Categories is the allowed label set; required for CATEGORICAL.",
			"ScoreConfigInput.dataType":   "DataType is NUMERIC, CATEGORICAL or BOOLEAN; empty means NUMERIC.",
			"ScoreConfigInput.minValue":   "MinValue and MaxValue bound a NUMERIC score. Null leaves that end unbounded.",
			"ScoreConfigInput.name":       "Name is the score name this config governs, unique within the org.",
		},
		Example: json.RawMessage(`{"name":"helpfulness","dataType":"NUMERIC","minValue":0,"maxValue":1}`),
	})
	zip.Describe("POST /v1/evals/scores", zip.Doc{
		Description: "createScore records one score event against a trace, run or dataset item. It\nanswers 201. Score integrity is enforced here: the value must be finite, and when a\nscore config of this name exists it is authoritative — the value must sit inside its\nbounds, the label inside its category set, and a caller cannot claim a different\ntype than the org configured. Requires telemetry; without a datastore the score has\nnowhere durable to land, so the call fails closed rather than reporting a score it\ndid not store.",
		Fields: map[string]string{
			"ScoreInput.comment":     "Comment is free text stored with the score.",
			"ScoreInput.dataType":    "DataType is NUMERIC, CATEGORICAL or BOOLEAN; a configured score overrides it.",
			"ScoreInput.datasetName": "Dataset and ItemID tie the score to one dataset row.",
			"ScoreInput.name":        "Name is the score name; a score config of this name, if any, governs it.",
			"ScoreInput.runName":     "RunName ties the score to one eval run.",
			"ScoreInput.stringValue": "StringValue is the label. Required for CATEGORICAL, and it must be in the\nconfigured category set.",
			"ScoreInput.traceId":     "TraceID ties the score to one trace.",
			"ScoreInput.value":       "Value is the numeric score. Required for NUMERIC (finite) and BOOLEAN (0 or 1).",
		},
		Example: json.RawMessage(`{"name":"helpfulness","traceId":"tr_9f2a1c7d4e8b0a6f","runName":"run-20260729T101500Z","value":1,"comment":"resolved the question"}`),
	})
}
