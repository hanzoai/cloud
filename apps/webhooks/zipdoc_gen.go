// Code generated by zipdoc; DO NOT EDIT.

package webhooks

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/webhooks/:id", zip.Doc{
		Description: "deleteEndpoint unregisters one endpoint and answers 204. Delivery stops immediately;\nthe recorded delivery log goes with it.",
		Fields: map[string]string{
			"endpointRef.id": "ID is the endpoint id from the path, as returned by create.",
		},
		Example: json.RawMessage(`{"id":"wh_4c1e9b7a2d6f0538"}`),
	})
	zip.Describe("GET /v1/webhooks/", zip.Doc{
		Description: "listEndpoints returns the caller org's registered webhook endpoints. Each carries its\ntrailing-7-day delivery and failure counts, and signing secrets are never included — a\nsecret leaves the server only on create and on rotate.",
		Fields: map[string]string{
			"Endpoint.deliveries7d": "Deliveries7d / Failures7d are cheap usage counters computed from the delivery log\nover usageWindow (not stored columns) and populated ONLY on list/get. They are 0\nwhen there is no delivery history — never omitempty, so the console always sees them.",
			"endpointList.data":     "Data is the endpoints, each with its trailing-7-day delivery counters and no secret.",
		},
		Response: json.RawMessage(`{"data":[{"id":"wh_4c1e9b7a","org":"acme","url":"https://acme.example/hooks","events":["commerce.>"],"status":"active","created":"2026-01-01T00:00:00Z","updated":"2026-01-01T00:00:00Z","deliveries7d":412,"failures7d":3}]}`),
	})
	zip.Describe("GET /v1/webhooks/:id", zip.Doc{
		Description: "getEndpoint returns one of the caller org's endpoints with its 7-day counters. The\nsigning secret is never included, and another org's id is a 404.",
		Fields: map[string]string{
			"Endpoint.deliveries7d": "Deliveries7d / Failures7d are cheap usage counters computed from the delivery log\nover usageWindow (not stored columns) and populated ONLY on list/get. They are 0\nwhen there is no delivery history — never omitempty, so the console always sees them.",
			"endpointRef.id":        "ID is the endpoint id from the path, as returned by create.",
		},
		Example: json.RawMessage(`{"id":"wh_4c1e9b7a2d6f0538"}`),
	})
	zip.Describe("GET /v1/webhooks/:id/deliveries", zip.Doc{
		Description: "listDeliveries returns one endpoint's per-attempt delivery log, newest first. It is\norg-scoped like every other read — another org's id is a 404, never a window onto its\nlogs — and each row carries the attempt number, HTTP status, duration and any error.",
		Fields: map[string]string{
			"deliveryList.data":    "Data is the attempts, newest first.",
			"deliveryQuery.id":     "ID is the endpoint id from the path.",
			"deliveryQuery.limit":  "Limit caps the attempts returned; 0 means 50 and nothing above 200 is honoured.",
			"deliveryQuery.status": "Status narrows the log to one outcome: ok, retrying or failed.",
		},
		Example: json.RawMessage(`{"id":"wh_4c1e9b7a2d6f0538","status":"failed","limit":100}`),
	})
	zip.Describe("POST /v1/webhooks/", zip.Doc{
		Description: "createEndpoint registers a webhook endpoint and reveals its signing secret. This is one of only two responses that ever carry the secret (the\nother is rotate), so a caller that does not store it here must rotate to get another.",
		Fields: map[string]string{
			"Endpoint.deliveries7d":     "Deliveries7d / Failures7d are cheap usage counters computed from the delivery log\nover usageWindow (not stored columns) and populated ONLY on list/get. They are 0\nwhen there is no delivery history — never omitempty, so the console always sees them.",
			"endpointInput.description": "Description is a free-text label for the endpoint.",
			"endpointInput.events":      "Events is the subject patterns to subscribe to, NATS wildcard syntax. Empty means every event.",
			"endpointInput.status":      "Status is active or disabled. Empty means active.",
			"endpointInput.url":         "URL is the https:// address deliveries are POSTed to. Required; cleartext http is refused.",
		},
		Example: json.RawMessage(`{"url":"https://acme.example/hooks","events":["commerce.>"],"status":"active","description":"billing events"}`),
	})
	zip.Describe("POST /v1/webhooks/:id/rotate-secret", zip.Doc{
		Description: "rotateSecret mints a new signing secret and reveals it once. The old secret is invalid\nthe instant this returns — every subsequent delivery and test signs with the new one,\nwith no overlap window — so a subscriber should call this only when it is ready to\nswap the secret on its own side.",
		Fields: map[string]string{
			"Endpoint.deliveries7d": "Deliveries7d / Failures7d are cheap usage counters computed from the delivery log\nover usageWindow (not stored columns) and populated ONLY on list/get. They are 0\nwhen there is no delivery history — never omitempty, so the console always sees them.",
			"endpointRef.id":        "ID is the endpoint id from the path, as returned by create.",
		},
		Example: json.RawMessage(`{"id":"wh_4c1e9b7a2d6f0538"}`),
	})
	zip.Describe("POST /v1/webhooks/:id/test", zip.Doc{
		Description: "testEndpoint sends one signed test event and reports the outcome inline. It runs the\nsame attempt path the dispatcher uses — single attempt, 10s timeout, no retry ladder —\nand records the delivery row, so the result is exactly what a real event would see. It\nworks on a DISABLED endpoint too, since validating an endpoint you have paused is the\nwhole point.",
		Fields: map[string]string{
			"endpointRef.id": "ID is the endpoint id from the path, as returned by create.",
		},
		Example:  json.RawMessage(`{"id":"wh_4c1e9b7a2d6f0538"}`),
		Response: json.RawMessage(`{"delivered":true,"httpStatus":200,"durationMs":84}`),
	})
	zip.Describe("PUT /v1/webhooks/:id", zip.Doc{
		Description: "updateEndpoint replaces one endpoint's url, events, status and description. It is a\nfull write, not a patch: an omitted field is cleared. The signing secret and the\ncreation time are immutable, and the endpoint updated is the one the PATH names.",
		Fields: map[string]string{
			"Endpoint.deliveries7d":      "Deliveries7d / Failures7d are cheap usage counters computed from the delivery log\nover usageWindow (not stored columns) and populated ONLY on list/get. They are 0\nwhen there is no delivery history — never omitempty, so the console always sees them.",
			"endpointUpdate.description": "Description is a free-text label for the endpoint.",
			"endpointUpdate.events":      "Events is the subject patterns to subscribe to, NATS wildcard syntax. Empty means every event.",
			"endpointUpdate.id":          "ID is the endpoint id from the path.",
			"endpointUpdate.status":      "Status is active or disabled. Empty means active.",
			"endpointUpdate.url":         "URL is the https:// address deliveries are POSTed to. Required; cleartext http is refused.",
		},
		Example: json.RawMessage(`{"id":"wh_4c1e9b7a2d6f0538","url":"https://acme.example/hooks/v2","events":["commerce.invoice.>"],"status":"disabled"}`),
	})
}
