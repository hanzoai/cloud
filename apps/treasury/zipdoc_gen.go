// Code generated by zipdoc; DO NOT EDIT.

package treasury

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/admin/treasury", zip.Doc{
		Description: "report returns the fund, its journal and its anchor status in one read. The journal\nis the recent double-entry postings; the anchor is the Hanzo L1 commit state of the\ncurrent ledger root. SuperAdmin only.",
		Fields: map[string]string{
			"AnchorStatus.chainId":       "ChainID is the Hanzo L1 chain the root is committed to.",
			"AnchorStatus.contract":      "Contract is the deployed anchor contract address, empty until deployed.",
			"AnchorStatus.currentRoot":   "CurrentRoot is the 0x… root of the journal as it stands now.",
			"AnchorStatus.entryCount":    "EntryCount is how many journal entries the current root covers.",
			"AnchorStatus.lastAt":        "LastAt is when it landed, unix seconds.",
			"AnchorStatus.lastBlock":     "LastBlock is the block it landed in.",
			"AnchorStatus.lastRoot":      "LastRoot is the last root committed on-chain; empty before the first submit.",
			"AnchorStatus.lastTxHash":    "LastTxHash is that commit's transaction hash.",
			"AnchorStatus.note":          "Note states the remaining step, in plain words.",
			"AnchorStatus.rpcConfigured": "RPCConfigured and SignerConfigured report which half of the chain path is wired.",
			"AnchorStatus.status":        "Status is pending, anchored or error.",
			"AnchorStatus.synced":        "Synced is true when the last anchored root equals the current root.",
			"JournalQuery.limit":         "Limit caps the journal entries returned. 0 or below means the default;\nanything above the maximum is clamped to it.",
			"TreasuryReport.anchor":      "Anchor is the Hanzo L1 anchor status for the current ledger root.",
			"TreasuryReport.journal":     "Journal is the most recent double-entry postings, newest first.",
			"TreasuryReport.report":      "Report is the reserve-fund health snapshot.",
		},
		Example: json.RawMessage(`{"limit":50}`),
	})
	zip.Describe("GET /v1/finance/accounts", zip.Doc{
		Description: "accounts lists the caller's ledger accounts and their balances. Tenant isolation is\nenforced SERVER-SIDE: a per-org caller sees ONLY accounts under its own\n\"org:<tenant>:\" prefix, never house accounts and never another tenant's. SuperAdmin\nmay widen with scope=house or org=<tenant> — the ONLY way to cross the tenant\nboundary. Honest empty until a tenant has ledger postings.",
		Fields: map[string]string{
			"AccountsQuery.org":        "Org reads one specific tenant's accounts. SuperAdmin only.",
			"AccountsQuery.scope":      "Scope is \"house\" to read the reserve/revenue/payout house accounts.\nSuperAdmin only.",
			"AccountsView.accounts":    "Accounts is one row per ledger account in scope.",
			"AccountsView.scope":       "Scope is the scope that was served: \"org\" or \"house\".",
			"AccountsView.tenant":      "Tenant is the org the accounts belong to; empty under house scope.",
			"accountView.address":      "Address is the ledger account address, e.g. \"fund:reserve\" or \"org:acme:wallet\".",
			"accountView.balanceCents": "BalanceCents is that account's balance in minor units.",
		},
		Example:  json.RawMessage(`{"scope":"house"}`),
		Response: json.RawMessage(`{"scope":"house","tenant":"hanzo","accounts":[{"address":"fund:reserve","balanceCents":1250000}]}`),
	})
	zip.Describe("GET /v1/finance/treasury", zip.Doc{
		Description: "treasury reports the reserve fund's health and the revenue-share policy. Any\nvalidated caller may read it: this is a TRANSPARENCY view — a partner or author can\nsee the pool that backs their payouts is solvent — not per-org money, which is the\ncustomer's commerce balance at /v1/billing/balance. Policy is read-only here; only\nSuperAdmin sets it.",
		Response:    json.RawMessage(`{"reserveCents":1250000,"accruedCents":4000000,"paidCents":2750000,"byProgramCents":{"referral":2750000},"policy":{"revenueShareBps":2000,"updatedAt":1780000000},"solventForPayout":true}`),
	})
	zip.Describe("POST /v1/admin/treasury/anchor", zip.Doc{
		Description: "anchor commits the current ledger root to Hanzo L1. It answers with the anchor\nstatus. SuperAdmin only. When the chain path is wired it signs and submits the\nanchor transaction and records it; otherwise it reports the root that WOULD be\ncommitted plus the exact remaining step, and records nothing false. A failed submit\nanswers status=error with the reason rather than a fabricated anchored state.",
		Fields: map[string]string{
			"AnchorData.anchor":          "Anchor is the anchor status after the attempt.",
			"AnchorStatus.chainId":       "ChainID is the Hanzo L1 chain the root is committed to.",
			"AnchorStatus.contract":      "Contract is the deployed anchor contract address, empty until deployed.",
			"AnchorStatus.currentRoot":   "CurrentRoot is the 0x… root of the journal as it stands now.",
			"AnchorStatus.entryCount":    "EntryCount is how many journal entries the current root covers.",
			"AnchorStatus.lastAt":        "LastAt is when it landed, unix seconds.",
			"AnchorStatus.lastBlock":     "LastBlock is the block it landed in.",
			"AnchorStatus.lastRoot":      "LastRoot is the last root committed on-chain; empty before the first submit.",
			"AnchorStatus.lastTxHash":    "LastTxHash is that commit's transaction hash.",
			"AnchorStatus.note":          "Note states the remaining step, in plain words.",
			"AnchorStatus.rpcConfigured": "RPCConfigured and SignerConfigured report which half of the chain path is wired.",
			"AnchorStatus.status":        "Status is pending, anchored or error.",
			"AnchorStatus.synced":        "Synced is true when the last anchored root equals the current root.",
		},
	})
	zip.Describe("POST /v1/admin/treasury/bind-anchor", zip.Doc{
		Description: "bindAnchor binds the treasury MPC wallet as the on-chain anchor signer. Subsequent\nanchor commits are then signed BY THE QUORUM WALLET instead of the lone KMS key. It\nprovisions-or-resolves the caller org's treasury wallet on the deployed MPC\nring and installs it. SuperAdmin only, and idempotent — a repeat resolves the same\nwallet. Answers with the bound EVM address so the operator can fund it for gas.",
		Fields: map[string]string{
			"BoundSigner.boundAnchorSigner": "BoundAnchorSigner is the EVM address of the bound MPC wallet — fund it for gas.",
			"BoundSigner.chainId":           "ChainID is the Hanzo L1 chain the signer was bound for.",
			"BoundSigner.org":               "Org is the org whose treasury wallet was resolved.",
		},
	})
	zip.Describe("POST /v1/admin/treasury/policy", zip.Doc{
		Description: "setPolicy sets the revenue-share basis points a sweep accrues. The value is 0–10000\nand the answer carries the policy as it now stands. SuperAdmin only; the write is\naudited.",
		Fields: map[string]string{
			"PolicyData.policy":             "Policy is the revenue-share policy now in force.",
			"policyRequest.revenueShareBps": "RevenueShareBps is the share of net platform revenue a sweep accrues into\nthe reserve fund, in basis points (0–10000).",
		},
		Example: json.RawMessage(`{"revenueShareBps":2000}`),
	})
	zip.Describe("POST /v1/admin/treasury/seed", zip.Doc{
		Description: "seed injects bootstrap capital into the reserve fund. It is how backed payouts\nbegin before the first revenue-share sweep. Idempotent by ref: a repeat with the\nsame ref returns created=false and posts nothing. SuperAdmin only; the injection is\naudited.",
		Fields: map[string]string{
			"SeedData.created":        "Created is false when this ref was already seeded (idempotent no-op).",
			"SeedData.entry":          "Entry is the journal entry the injection posted.",
			"SeedData.reserveCents":   "ReserveCents is the fund balance after the injection.",
			"seedRequest.amountCents": "AmountCents is the capital to inject, in minor units. Must be > 0.",
			"seedRequest.memo":        "Memo describes the injection; empty defaults to \"reserve capital injection\".",
			"seedRequest.ref":         "Ref is an idempotency key. Without one each seed is a distinct injection.",
		},
		Example: json.RawMessage(`{"amountCents":1000000,"memo":"seed round","ref":"seed-2026-07"}`),
	})
	zip.Describe("POST /v1/admin/treasury/sweep", zip.Doc{
		Description: "sweep posts one period's revenue-share accrual into the fund. It reports the fund\nbalance after it. Idempotent per period: a repeat returns created=false and posts\nnothing. SuperAdmin only; the accrual is audited. Treasury does the accounting, not\nthe metering — the caller supplies the measured revenue.",
		Fields: map[string]string{
			"SweepData.accruedCents":    "AccruedCents is the amount posted into the reserve fund.",
			"SweepData.created":         "Created is false when this period was already swept (idempotent no-op).",
			"SweepData.period":          "Period is the period that was accrued.",
			"SweepData.reserveCents":    "ReserveCents is the fund balance after the accrual.",
			"SweepData.revenueCents":    "RevenueCents echoes the revenue the accrual was computed from.",
			"sweepRequest.period":       "Period is the accounting period, \"YYYY-MM\"; empty means the current UTC month.",
			"sweepRequest.revenueCents": "RevenueCents is the net platform revenue measured for that period, in minor\nunits. Must be >= 0.",
		},
		Example: json.RawMessage(`{"period":"2026-07","revenueCents":4000000}`),
	})
}
