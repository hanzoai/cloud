// Code generated by zipdoc; DO NOT EDIT.

package treasury

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/admin/treasury", zip.Doc{
		Description: "GetAdminTreasury returns the whole treasury board for a SuperAdmin: the reserve\nfund report, the recent double-entry journal, and the Hanzo L1 anchor status of\nthe ledger root. ?limit= bounds the journal page.",
		Fields: map[string]string{
			"Currency.Code":                   "ISO-4217 alpha code or custom (\"USD\", \"HUSD\")",
			"Currency.Decimals":               "fractional digits of the smallest unit",
			"Currency.Numeric":                "ISO-4217 numeric code (\"840\"); \"\" for custom",
			"Currency.Symbol":                 "display grapheme (\"$\", \"€\", \"₿\")",
			"JournalEntry.amount":             "the entry's magnitude (display; postings hold the signed truth)",
			"JournalEntry.program":            "referral|affiliate|author for payouts; \"\" otherwise",
			"JournalEntry.ref":                "idempotency ref (unique within Kind+Program)",
			"Posting.amount":                  "signed 18-decimal USD (1e-18); Σ over an entry == 0",
			"TreasuryReport.accruedCents":     "lifetime revenue-share into the fund",
			"TreasuryReport.byProgramCents":   "program → lifetime paid",
			"TreasuryReport.paidCents":        "lifetime backed payouts out of the fund",
			"TreasuryReport.policy":           "current revenue-share policy",
			"TreasuryReport.reserveCents":     "fund:reserve balance (available now)",
			"TreasuryReport.solventForPayout": "reserve > 0: at least some payout is backable",
			"adminReportData.anchor":          "Anchor is the Hanzo L1 anchoring status of the ledger root.",
			"adminReportData.journal":         "Journal is the recent double-entry entries, newest first.",
			"adminReportData.report":          "Report is the reserve-fund snapshot — available, accrued, paid, per-program, policy.",
			"adminReportOut.data":             "Data is the treasury board.",
			"adminReportOut.msg":              "Msg carries an operator-facing note; empty on success.",
			"adminReportOut.status":           "Status is \"ok\" on success; the transport maps a non-ok envelope to an error.",
			"anchorStatus.currentRoot":        "0x… root of the journal as it stands now",
			"anchorStatus.lastRoot":           "The last committed on-chain anchor (nil-fields until the first successful submit).",
			"anchorStatus.status":             "pending | anchored | error",
			"anchorStatus.synced":             "true when the last anchored root == the current root",
			"journalIn.limit":                 "Limit caps the journal entries returned. Out of range or unparseable takes the default.",
		},
	})
	zip.Describe("GET /v1/finance/accounts", zip.Doc{
		Description: "ListFinanceAccounts returns the ledger accounts the caller may see, with their\nbalances. It is tenant-isolated SERVER-SIDE: an ordinary caller sees ONLY\naccounts under its own \"org:<tenant>:\" prefix, never house accounts and never\nanother tenant's. A SuperAdmin may widen with ?scope=house (the reserve,\nrevenue and payout house accounts) or ?org=<tenant> — the only way to cross the\ntenant boundary, and only for platform sudo. The answer is honestly empty until\na tenant has ledger postings.",
		Fields: map[string]string{
			"accountView.address":      "Address is the ledger account address (\"org:acme:wallet\", \"fund:reserve\", …).",
			"accountView.balanceCents": "BalanceCents is that account's signed balance in minor units.",
			"accountsIn.org":           "Org names another tenant to read. SuperAdmin only; ignored when scope=house.",
			"accountsIn.scope":         "Scope is \"house\" to read the reserve/revenue/payout house accounts. SuperAdmin only.",
			"accountsOut.accounts":     "Accounts are the ledger accounts in scope with their balances.",
			"accountsOut.scope":        "Scope is the scope actually served: \"org\" or \"house\".",
			"accountsOut.tenant":       "Tenant is the org whose accounts these are (empty for the house scope's own rows).",
		},
	})
	zip.Describe("GET /v1/finance/treasury", zip.Doc{
		Description: "GetTreasury returns the reserve fund's health and the current revenue-share\npolicy for any validated caller. It is a TRANSPARENCY view — a partner or\nauthor can see that the pool backing their payouts is solvent — and NOT per-org\nmoney, which is the customer's own commerce balance at /v1/billing/balance. The\npolicy is read-only here; only a SuperAdmin sets it.",
		Fields: map[string]string{
			"TreasuryReport.accruedCents":     "lifetime revenue-share into the fund",
			"TreasuryReport.byProgramCents":   "program → lifetime paid",
			"TreasuryReport.paidCents":        "lifetime backed payouts out of the fund",
			"TreasuryReport.policy":           "current revenue-share policy",
			"TreasuryReport.reserveCents":     "fund:reserve balance (available now)",
			"TreasuryReport.solventForPayout": "reserve > 0: at least some payout is backable",
		},
	})
	zip.Describe("POST /treasury/reserve", zip.Doc{
		Description: "TreasuryReserve returns the reserve fund's available balance — the pool that\nbacks every referral, affiliate and author payout. SuperAdmin only, re-checked\non this side against the principal that asked.\n\n(A named function, not the closure it replaced: zipdoc harvests the doc comment\nof the HANDLER, and a function literal has none, so this op used to register\nwith an empty description on every projection that reads one.)",
	})
	zip.Describe("POST /v1/admin/treasury/anchor", zip.Doc{
		Description: "AnchorTreasury commits the current ledger root to Hanzo L1, making the books\ntamper-evident on chain, and returns the anchoring status. When the chain path\nis wired it signs and submits the anchor transaction and records it; when it is\nnot, it returns the root that WOULD be committed plus the exact remaining\nwiring step and records nothing false. A submit that fails still answers 200\nwith the anchor's own status set to \"error\" — the attempt is the product.\nSuperAdmin only.",
		Fields: map[string]string{
			"anchorData.anchor":        "Anchor is the Hanzo L1 anchoring status of the ledger root after this call.",
			"anchorOut.data":           "Data is the anchoring status.",
			"anchorOut.msg":            "Msg carries an operator-facing note; empty on success.",
			"anchorOut.status":         "Status is \"ok\" on success. A submit that failed still answers ok with the\nanchor's own status set to \"error\" — the attempt is the product.",
			"anchorStatus.currentRoot": "0x… root of the journal as it stands now",
			"anchorStatus.lastRoot":    "The last committed on-chain anchor (nil-fields until the first successful submit).",
			"anchorStatus.status":      "pending | anchored | error",
			"anchorStatus.synced":      "true when the last anchored root == the current root",
		},
	})
	zip.Describe("POST /v1/admin/treasury/bind-anchor", zip.Doc{
		Description: "BindTreasuryAnchorSigner makes the reserve's threshold MPC wallet the signer\nfor on-chain anchors, and returns its EVM address so an operator can fund it\nfor gas. It provisions-or-resolves the caller org's treasury wallet on the\ndeployed MPC ring and installs it, so every later anchor commits the ledger\nroot SIGNED BY THE QUORUM WALLET instead of a lone KMS key. Idempotent: a\nrepeat resolves the same wallet. SuperAdmin only.",
		Fields: map[string]string{
			"bindData.boundAnchorSigner": "BoundAnchorSigner is the EVM address now signing anchors. Fund it for gas.",
			"bindData.chainId":           "ChainID is the EVM chain the signer is bound for.",
			"bindData.org":               "Org is the org whose treasury wallet was resolved.",
			"bindOut.data":               "Data is the bound signer.",
			"bindOut.msg":                "Msg carries an operator-facing note; empty on success.",
			"bindOut.status":             "Status is \"ok\" on success.",
		},
	})
	zip.Describe("POST /v1/admin/treasury/policy", zip.Doc{
		Description: "SetTreasuryPolicy sets the revenue-share basis points a sweep accrues into the\nreserve fund and returns the stored policy. 0–10000; the change is audited.\nSuperAdmin only.",
		Fields: map[string]string{
			"policyData.policy":             "Policy is the revenue-share configuration as stored.",
			"policyOut.data":                "Data is the stored policy.",
			"policyOut.msg":                 "Msg carries an operator-facing note; empty on success.",
			"policyOut.status":              "Status is \"ok\" on success.",
			"policyRequest.revenueShareBps": "RevenueShareBps is the share of net platform revenue a sweep accrues into the\nreserve fund, in basis points. 0–10000; 2000 (20%) is the platform default.",
		},
		Example: json.RawMessage(`{"revenueShareBps":2000}`),
	})
	zip.Describe("POST /v1/admin/treasury/seed", zip.Doc{
		Description: "SeedTreasury injects bootstrap capital into the reserve fund so backed payouts\ncan begin before the first revenue-share sweep, and returns the journal entry\nit wrote. A repeat of the same ref is at-most-once and reports created=false.\nSuperAdmin only.",
		Fields: map[string]string{
			"Currency.Code":           "ISO-4217 alpha code or custom (\"USD\", \"HUSD\")",
			"Currency.Decimals":       "fractional digits of the smallest unit",
			"Currency.Numeric":        "ISO-4217 numeric code (\"840\"); \"\" for custom",
			"Currency.Symbol":         "display grapheme (\"$\", \"€\", \"₿\")",
			"JournalEntry.amount":     "the entry's magnitude (display; postings hold the signed truth)",
			"JournalEntry.program":    "referral|affiliate|author for payouts; \"\" otherwise",
			"JournalEntry.ref":        "idempotency ref (unique within Kind+Program)",
			"Posting.amount":          "signed 18-decimal USD (1e-18); Σ over an entry == 0",
			"seedData.created":        "Created is false when this ref had already been seeded — the injection is at-most-once.",
			"seedData.entry":          "Entry is the journal entry the injection wrote.",
			"seedData.reserveCents":   "ReserveCents is the fund balance after the injection.",
			"seedOut.data":            "Data is the injection result.",
			"seedOut.msg":             "Msg carries an operator-facing note; empty on success.",
			"seedOut.status":          "Status is \"ok\" on success.",
			"seedRequest.amountCents": "AmountCents is the capital to inject, in minor units. Must be > 0.",
			"seedRequest.memo":        "Memo is the operator's note on the entry. Empty takes \"reserve capital injection\".",
			"seedRequest.ref":         "Ref is an idempotency key. Without one each seed is a distinct injection.",
		},
		Example: json.RawMessage(`{"amountCents":500000,"memo":"founding capital","ref":"seed:2026-q3"}`),
	})
	zip.Describe("POST /v1/admin/treasury/sweep", zip.Doc{
		Description: "SweepTreasury posts the revenue-share accrual for one period — revenue into the\nreserve fund, at the current policy's basis points — and returns what it moved.\nIt is idempotent per period: a re-run of a period already swept accrues nothing\nand reports created=false. SuperAdmin only.",
		Fields: map[string]string{
			"sweepData.accruedCents":    "AccruedCents is the amount moved into the reserve fund.",
			"sweepData.created":         "Created is false when this period had already been swept — the accrual is idempotent.",
			"sweepData.period":          "Period is the period actually accrued.",
			"sweepData.reserveCents":    "ReserveCents is the fund balance after the accrual.",
			"sweepData.revenueCents":    "RevenueCents is the revenue the share was computed from.",
			"sweepOut.data":             "Data is the accrual result.",
			"sweepOut.msg":              "Msg carries an operator-facing note; empty on success.",
			"sweepOut.status":           "Status is \"ok\" on success.",
			"sweepRequest.period":       "Period is the accrual period as YYYY-MM. Empty takes the current UTC month.",
			"sweepRequest.revenueCents": "RevenueCents is the net platform revenue measured for the period, in minor units. Must be >= 0.",
		},
		Example: json.RawMessage(`{"period":"2026-07","revenueCents":100000}`),
	})
}
