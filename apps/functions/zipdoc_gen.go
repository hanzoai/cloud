// Code generated by zipdoc; DO NOT EDIT.

package functions

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/functions DELETE /v1/function/:name", zip.Doc{
		Description: "Removes one of the caller org's functions and answers 204.\n\nA name this org does not hold is 404 — never a silent success — and a name\nbelonging to another tenant is the same 404, because the delete is predicated on\nthe validated org.",
		Fields: map[string]string{
			"fnRef.name": "Name is the function the URL names.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions GET /v1/function", zip.Doc{
		Description: "Is every serverless function the caller's org has published, each with its\nreal 7-day rollup.\n\nA row carries the function's runtime, resource limits, deployment target and its\ninvoke endpoint, plus envCount — how many secrets it mounts. The rollup fields\nare ABSENT rather than zero when the function has not run in the window, so a\nconsole renders \"—\" instead of a fabricated 0.\n\nRequires a validated principal; the listing is scoped to its org.",
		Fields: map[string]string{
			"fnList.functions":            "Functions is one row per published function.",
			"functionView.avgDurationMs":  "mean wall-clock of those runs",
			"functionView.createdAt":      "when it was first published",
			"functionView.endpoint":       "the path that invokes it",
			"functionView.envCount":       "how many secret NAMES it mounts; values are never carried",
			"functionView.environment":    "the language it runs under",
			"functionView.errors7d":       "how many of those runs failed",
			"functionView.image":          "the prebuilt image it runs, when it runs one instead of source",
			"functionView.invocations7d":  "runs in the last 7 days; ABSENT, never 0, when it has not run",
			"functionView.lastDeployedAt": "when its code last changed",
			"functionView.memoryLimit":    "the memory it runs with, and the multiplier on its compute charge",
			"functionView.name":           "the function's org-unique handle",
			"functionView.namespace":      "the display group it belongs to; the org is the isolation key",
			"functionView.status":         "whether it is ready to serve",
			"functionView.successRate":    "share of those runs that succeeded, 0..1",
			"functionView.target":         "where it runs: empty for the sandbox, \"fleet\" for the org's GPU fleet",
			"functionView.timeoutSec":     "its per-invocation deadline",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions GET /v1/function/:name", zip.Doc{
		Description: "Is one function with everything a detail page needs in one round-trip: its\ndefinition, its 7-day rollup, its trigger, its twenty most recent invocations\nand the NAMES of the secrets it mounts.\n\nSecret values are never read or returned. A name the caller's org does not hold\nis 404, which is also what another tenant's function looks like from here.",
		Fields: map[string]string{
			"fnRef.name":                       "Name is the function the URL names.",
			"functionDetail.recentInvocations": "RecentInvocations is its twenty most recent runs, newest first.",
			"functionDetail.secrets":           "Secrets are the NAMES it mounts. Values are never read or returned.",
			"functionDetail.triggers":          "Triggers is how this function is reached.",
			"functionView.avgDurationMs":       "mean wall-clock of those runs",
			"functionView.createdAt":           "when it was first published",
			"functionView.endpoint":            "the path that invokes it",
			"functionView.envCount":            "how many secret NAMES it mounts; values are never carried",
			"functionView.environment":         "the language it runs under",
			"functionView.errors7d":            "how many of those runs failed",
			"functionView.image":               "the prebuilt image it runs, when it runs one instead of source",
			"functionView.invocations7d":       "runs in the last 7 days; ABSENT, never 0, when it has not run",
			"functionView.lastDeployedAt":      "when its code last changed",
			"functionView.memoryLimit":         "the memory it runs with, and the multiplier on its compute charge",
			"functionView.name":                "the function's org-unique handle",
			"functionView.namespace":           "the display group it belongs to; the org is the isolation key",
			"functionView.status":              "whether it is ready to serve",
			"functionView.successRate":         "share of those runs that succeeded, 0..1",
			"functionView.target":              "where it runs: empty for the sandbox, \"fleet\" for the org's GPU fleet",
			"functionView.timeoutSec":          "its per-invocation deadline",
			"invocationView.durationMs":        "how long it took",
			"invocationView.id":                "the invocation's handle",
			"invocationView.method":            "the HTTP method that triggered it",
			"invocationView.status":            "how the run ended: ok, error or timeout",
			"invocationView.statusCode":        "Code is the status the function's OWN code answered with, which is not the\nstatus of the reply — a program can answer 500 through a healthy sandbox.",
			"invocationView.time":              "when it ran, RFC3339",
			"triggerView.enabled":              "whether it currently fires",
			"triggerView.functionName":         "the function it calls",
			"triggerView.id":                   "the trigger's handle",
			"triggerView.name":                 "a human label for it",
			"triggerView.target":               "the path it calls",
			"triggerView.type":                 "what kind of trigger it is; HTTP is the only one today",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions GET /v1/function/:name/invocations", zip.Doc{
		Description: "Is one function's past runs, newest first — each with its status,\nHTTP code, method, time and duration.\n\nThese are real recorded rows, not a projection: an invocation appears here only\nonce it actually ran. Requires a validated principal; the read is scoped to its\norg.",
		Fields: map[string]string{
			"invocationList.invocations": "Invocations is one row per past run, newest first.",
			"invocationPage.limit":       "Limit caps the page, defaulting to 100.",
			"invocationPage.name":        "Name is the function the URL names.",
			"invocationView.durationMs":  "how long it took",
			"invocationView.id":          "the invocation's handle",
			"invocationView.method":      "the HTTP method that triggered it",
			"invocationView.status":      "how the run ended: ok, error or timeout",
			"invocationView.statusCode":  "Code is the status the function's OWN code answered with, which is not the\nstatus of the reply — a program can answer 500 through a healthy sandbox.",
			"invocationView.time":        "when it ran, RFC3339",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions GET /v1/function/:name/logs", zip.Doc{
		Description: "Is the output of a function's most recent run — its error text when that\nrun failed, else what it printed.\n\nIt is the LAST run only, and it is empty when the function has never run. There\nis no log retention behind this beyond the recorded invocation itself.",
		Fields: map[string]string{
			"fnRef.name":    "Name is the function the URL names.",
			"logLines.logs": "Logs is that run's error text when it failed, else its output. It is empty\nwhen the function has never run.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions GET /v1/function/deployments", zip.Doc{
		Description: "Is what is live right now — each function's current record IS its\nlive deployment, so this is the deployment inventory.\n\nThere is no deployment history behind it: a function has one record, and\npublishing replaces it. The 7-day rollup is deliberately absent here, because\nthis read is about what is deployed rather than about how it has performed.",
		Fields: map[string]string{
			"fnList.functions":            "Functions is one row per published function.",
			"functionView.avgDurationMs":  "mean wall-clock of those runs",
			"functionView.createdAt":      "when it was first published",
			"functionView.endpoint":       "the path that invokes it",
			"functionView.envCount":       "how many secret NAMES it mounts; values are never carried",
			"functionView.environment":    "the language it runs under",
			"functionView.errors7d":       "how many of those runs failed",
			"functionView.image":          "the prebuilt image it runs, when it runs one instead of source",
			"functionView.invocations7d":  "runs in the last 7 days; ABSENT, never 0, when it has not run",
			"functionView.lastDeployedAt": "when its code last changed",
			"functionView.memoryLimit":    "the memory it runs with, and the multiplier on its compute charge",
			"functionView.name":           "the function's org-unique handle",
			"functionView.namespace":      "the display group it belongs to; the org is the isolation key",
			"functionView.status":         "whether it is ready to serve",
			"functionView.successRate":    "share of those runs that succeeded, 0..1",
			"functionView.target":         "where it runs: empty for the sandbox, \"fleet\" for the org's GPU fleet",
			"functionView.timeoutSec":     "its per-invocation deadline",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions GET /v1/function/metrics", zip.Doc{
		Description: "Is the org's serverless dashboard over a window: a per-function\ninvocation costLine and how those invocations ended.\n\nEvery point is a REAL count of rows that fell in that bucket — nothing is\ninterpolated or invented, so an empty window draws a flat line rather than a\nfabricated one.\n\ncostCents is null and stays null: there is no per-invocation cost source to read,\nand reporting a number computed some other way would be a guess presented as a\nmeasurement. Requires a validated principal; the read is scoped to its org.",
		Fields: map[string]string{
			"costLine.key":            "the function the line is about",
			"costLine.points":         "one point per bucket, oldest first",
			"metricsQuery.range":      "Range is 1H, 6H, 24H (the default), 7D or 30D. Anything else falls back to\n24H rather than failing.",
			"pointView.t":             "the bucket's start, RFC3339 (UTC)",
			"pointView.v":             "how many invocations fell in it — a real count, never interpolated",
			"statusBreakdown.error":   "invocations that ran and failed",
			"statusBreakdown.success": "invocations whose code ran and wrote nothing to stderr",
			"statusBreakdown.timeout": "invocations that hit their configured deadline",
			"usage.costCents":         "null — no per-invocation cost source",
			"usage.series":            "one line per function that ran in the window",
			"usage.status":            "how those invocations ended",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions GET /v1/function/secrets", zip.Doc{
		Description: "Is the NAMES of the secrets the caller org's functions mount.\n\nValues are NEVER read or returned — this surface knows which names a function\nasks for and nothing about what is behind them, which is what makes it safe to\nlist at all. One row per distinct (namespace, name).",
		Fields: map[string]string{
			"secretList.secrets":   "Secrets is one row per distinct (namespace, name) a function mounts. Values\nare NEVER read or returned.",
			"secretView.mountedBy": "MountedBy is a function that mounts it.",
			"secretView.name":      "Name is the environment variable the function mounts.",
			"secretView.namespace": "Namespace is the group the mounting function belongs to.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions GET /v1/function/triggers", zip.Doc{
		Description: "Is what calls the caller org's functions — one row per function.\n\nEvery function has exactly one trigger today, its HTTP invoke endpoint, so this\nis the function list read as \"how is each of these reached\".",
		Fields: map[string]string{
			"triggerList.triggers":     "Triggers is one row per function, describing how it is reached.",
			"triggerView.enabled":      "whether it currently fires",
			"triggerView.functionName": "the function it calls",
			"triggerView.id":           "the trigger's handle",
			"triggerView.name":         "a human label for it",
			"triggerView.target":       "the path it calls",
			"triggerView.type":         "what kind of trigger it is; HTTP is the only one today",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions POST /v1/function", zip.Doc{
		Description: "Publishes a serverless function under the caller's org and answers 201\nwith it.\n\nThe name is the key and is claimed once; the names that would shadow a\ncollection route are reserved. runtime and environment are the same field —\neither spelling is accepted — and default to node.\n\nBounds are clamped rather than refused where a clamp is honest: a timeout above\nthe 900-second ceiling becomes the ceiling instead of silently reverting to the\n30-second default, and an omitted memory limit becomes 256Mi. target=fleet runs\non the org's own GPU fleet and supports runtime=python only.\n\nRequires a validated principal; the function is owned by that principal's org.",
		Fields: map[string]string{
			"definition.code":             "Code is the source to run, capped so one function cannot amplify the store.",
			"definition.envNames":         "EnvNames are the secret NAMES to mount. Values live in the secret store and\nare never carried here.",
			"definition.environment":      "Environment is a second spelling of runtime, accepted so a console that says\n\"environment\" needs no translation.",
			"definition.handler":          "Handler is the entry point within the code.",
			"definition.image":            "Image names a prebuilt image to run instead of source.",
			"definition.memoryLimit":      "MemoryLimit is the memory the function runs with, defaulting to 256Mi. It is\nalso the multiplier on the GB-seconds compute charge.",
			"definition.name":             "Name is the function's org-unique handle and the segment that addresses it,\nmatching ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. The names that would shadow a\ncollection route are reserved.",
			"definition.namespace":        "Namespace groups functions for display. It is cosmetic — the org is the\nisolation key — and is normalised to a DNS-safe label.",
			"definition.runtime":          "Runtime is the language the code runs under: node, python or deno.",
			"definition.target":           "Target is where the function runs: sandbox (the default) or fleet, the org's\nown GPU fleet. fleet supports runtime=python only.",
			"definition.timeoutSec":       "TimeoutSec is the per-invocation deadline, defaulting to 30 and clamped at\n900 — a larger value is capped rather than reset to the default.",
			"functionView.avgDurationMs":  "mean wall-clock of those runs",
			"functionView.createdAt":      "when it was first published",
			"functionView.endpoint":       "the path that invokes it",
			"functionView.envCount":       "how many secret NAMES it mounts; values are never carried",
			"functionView.environment":    "the language it runs under",
			"functionView.errors7d":       "how many of those runs failed",
			"functionView.image":          "the prebuilt image it runs, when it runs one instead of source",
			"functionView.invocations7d":  "runs in the last 7 days; ABSENT, never 0, when it has not run",
			"functionView.lastDeployedAt": "when its code last changed",
			"functionView.memoryLimit":    "the memory it runs with, and the multiplier on its compute charge",
			"functionView.name":           "the function's org-unique handle",
			"functionView.namespace":      "the display group it belongs to; the org is the isolation key",
			"functionView.status":         "whether it is ready to serve",
			"functionView.successRate":    "share of those runs that succeeded, 0..1",
			"functionView.target":         "where it runs: empty for the sandbox, \"fleet\" for the org's GPU fleet",
			"functionView.timeoutSec":     "its per-invocation deadline",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/functions POST /v1/function/:name/invoke", zip.Doc{
		Description: "Runs a function and records a REAL invocation.\n\nThe answer is the invocation record whatever happened to it: 200 when the org's\ncode ran clean, 502 when it ran and failed, 503 when this deployment has no\nsandbox to run code in. The record IS the evidence, so it rides the failure\nrather than being replaced by an error envelope.\n\nBilling is two-part and both parts are prepaid-then-metered on the one shared\nmeter: a flat per-invocation request fee, gated BEFORE any sandbox compute runs\nso an unfunded org gets 402 and nothing executes, and a usage-native\nGB-seconds compute debit taken after the run. Either is independently free when\nits fee is zero, so an operator can bill by request alone, by compute alone, or\nby both — and a zero request fee removes the balance gate with it.\n\nA TRANSPORT failure is not charged: the sandbox being unreachable ran no\nbillable compute. Code that ran and exited non-zero IS charged — that is a\nsuccessful invocation of a failing program, not a billing failure.\n\nWhen the sandbox is not configured on this deployment, a non-fleet function\nfails closed before anything is recorded — no execution and no fabricated\noutput. Scoped to the caller's org; requires a validated principal.",
		Fields: map[string]string{
			"invocationView.durationMs": "how long it took",
			"invocationView.id":         "the invocation's handle",
			"invocationView.method":     "the HTTP method that triggered it",
			"invocationView.status":     "how the run ended: ok, error or timeout",
			"invocationView.statusCode": "Code is the status the function's OWN code answered with, which is not the\nstatus of the reply — a program can answer 500 through a healthy sandbox.",
			"invocationView.time":       "when it ran, RFC3339",
			"invokeReq.input":           "Input is what the function is given on stdin. It is opaque to this surface.",
		},
	})
}
