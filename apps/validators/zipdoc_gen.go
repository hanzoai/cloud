// Code generated by zipdoc; DO NOT EDIT.

package validators

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/validators", zip.Doc{
		Description: "listValidators returns the caller org's claimed validator slots, each with its\nnode status and its queued registration.",
		Fields: map[string]string{
			"Page.limit":              "Limit caps the rows returned; 0 means 200 and nothing above 1000 is honoured.",
			"RegistrationView.id":     "ID is the registration id.",
			"RegistrationView.nodeID": "NodeID is the node the registration would add.",
			"RegistrationView.status": "Status is the queue state; new registrations are pending_owner_approval.",
			"SlotList.data":           "Data is the page of claimed slots; empty when the org has claimed none.",
			"SlotList.network":        "Network is the network slug new nodes join (mainnet/testnet/devnet/localnet).",
			"SlotView.blsPubkey":      "BLSPubkey is the node's BLS public key, hex-encoded.",
			"SlotView.crName":         "CRName is the LuxNetwork custom resource written for the node.",
			"SlotView.createdAt":      "CreatedAt is unix seconds, server-assigned at claim time.",
			"SlotView.namespace":      "Namespace is the cluster namespace the node CR lives in.",
			"SlotView.network":        "Network is the network slug the node syncs.",
			"SlotView.nodeID":         "NodeID is the luxd node identity generated for the slot.",
			"SlotView.nodeStatus":     "NodeStatus is provisioning, node_created or node_pending (no cluster reached).",
			"SlotView.registration":   "Registration is the queued, owner-gated P-Chain registration; absent until enqueued.",
			"SlotView.slot":           "Slot is the validator slot number (the same value as tokenId).",
			"SlotView.tokenId":        "TokenID is the GenesisNFT id that entitles the slot.",
			"SlotView.updatedAt":      "UpdatedAt is unix seconds of the last status change.",
			"SlotView.wallet":         "Wallet is the lower-cased address that proved ownership of the NFT.",
		},
		Example: json.RawMessage(`{"limit":50}`),
	})
	zip.Describe("GET /v1/validators/:tokenId", zip.Doc{
		Description: "getValidator returns one of the caller org's claimed slots. A slot held by\nanother org reads as not found.",
		Fields: map[string]string{
			"RegistrationView.id":     "ID is the registration id.",
			"RegistrationView.nodeID": "NodeID is the node the registration would add.",
			"RegistrationView.status": "Status is the queue state; new registrations are pending_owner_approval.",
			"SlotRef.tokenId":         "TokenID is the GenesisNFT id of the slot; it IS the validator slot number.",
			"SlotView.blsPubkey":      "BLSPubkey is the node's BLS public key, hex-encoded.",
			"SlotView.crName":         "CRName is the LuxNetwork custom resource written for the node.",
			"SlotView.createdAt":      "CreatedAt is unix seconds, server-assigned at claim time.",
			"SlotView.namespace":      "Namespace is the cluster namespace the node CR lives in.",
			"SlotView.network":        "Network is the network slug the node syncs.",
			"SlotView.nodeID":         "NodeID is the luxd node identity generated for the slot.",
			"SlotView.nodeStatus":     "NodeStatus is provisioning, node_created or node_pending (no cluster reached).",
			"SlotView.registration":   "Registration is the queued, owner-gated P-Chain registration; absent until enqueued.",
			"SlotView.slot":           "Slot is the validator slot number (the same value as tokenId).",
			"SlotView.tokenId":        "TokenID is the GenesisNFT id that entitles the slot.",
			"SlotView.updatedAt":      "UpdatedAt is unix seconds of the last status change.",
			"SlotView.wallet":         "Wallet is the lower-cased address that proved ownership of the NFT.",
		},
		Example: json.RawMessage(`{"tokenId":42}`),
	})
	zip.Describe("GET /v1/validators/challenge", zip.Doc{
		Description: "issueChallenge issues a single-use, org-bound nonce and the EXACT message the\ncaller must personal_sign. Binding the nonce to (org, slot) here means a\nsignature can never be replayed for a different org, slot, or session.",
		Fields: map[string]string{
			"Challenge.expiresAt":  "ExpiresAt is the unix second after which the nonce is refused.",
			"Challenge.message":    "Message is the exact text the wallet must personal_sign, byte for byte.",
			"Challenge.nonce":      "Nonce is the single-use value bound to (org, slot); claiming burns it.",
			"Challenge.tokenId":    "TokenID echoes the slot the challenge is bound to.",
			"Challenge.ttlSeconds": "TTLSeconds is the lifetime the nonce was issued with.",
			"SlotRef.tokenId":      "TokenID is the GenesisNFT id of the slot; it IS the validator slot number.",
		},
		Example: json.RawMessage(`{"tokenId":42}`),
	})
	zip.Describe("POST /v1/validators", zip.Doc{
		Description: "provisionValidator claims a slot: it verifies the signature and on-chain NFT\nownership, generates a luxd staking identity into KMS, writes the node CR and\nENQUEUES an owner-gated registration. It fails CLOSED at every gate — bad\nsignature, non-owner, non-tier, KMS unavailable — none of which persist a claim\nor leak key material. Re-claiming a slot this org already holds re-applies the\nCR without regenerating keys.",
		Fields: map[string]string{
			"ProvisionRequest.nonce":     "Nonce is the value issued by GET /v1/validators/challenge. Required.",
			"ProvisionRequest.signature": "Signature is the wallet's personal_sign over the challenge message. Required.",
			"ProvisionRequest.tokenId":   "TokenID is the Validator-tier GenesisNFT id being claimed. Required.",
			"RegistrationView.id":        "ID is the registration id.",
			"RegistrationView.nodeID":    "NodeID is the node the registration would add.",
			"RegistrationView.status":    "Status is the queue state; new registrations are pending_owner_approval.",
			"SlotView.blsPubkey":         "BLSPubkey is the node's BLS public key, hex-encoded.",
			"SlotView.crName":            "CRName is the LuxNetwork custom resource written for the node.",
			"SlotView.createdAt":         "CreatedAt is unix seconds, server-assigned at claim time.",
			"SlotView.namespace":         "Namespace is the cluster namespace the node CR lives in.",
			"SlotView.network":           "Network is the network slug the node syncs.",
			"SlotView.nodeID":            "NodeID is the luxd node identity generated for the slot.",
			"SlotView.nodeStatus":        "NodeStatus is provisioning, node_created or node_pending (no cluster reached).",
			"SlotView.registration":      "Registration is the queued, owner-gated P-Chain registration; absent until enqueued.",
			"SlotView.slot":              "Slot is the validator slot number (the same value as tokenId).",
			"SlotView.tokenId":           "TokenID is the GenesisNFT id that entitles the slot.",
			"SlotView.updatedAt":         "UpdatedAt is unix seconds of the last status change.",
			"SlotView.wallet":            "Wallet is the lower-cased address that proved ownership of the NFT.",
		},
		Example: json.RawMessage(`{"tokenId":42,"nonce":"deadbeefcafef00d","signature":"0x1c…"}`),
	})
}
