// Code generated by zipdoc; DO NOT EDIT.

package link

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/links/:id", zip.Doc{
		Description: "revokeLink revokes one account link and stops the sessions running on it. The row\nis kept and marked revoked so the history stays inspectable, and a link of another\nuser or org is reported not-found.",
		Fields: map[string]string{
			"linkRef.id":       "ID is the link id from the path.",
			"linkView.billing": "BillingMode(Kind) — how this account's usage bills",
		},
		Example: json.RawMessage(`{"id":"link_4c1e9b7a2d6f0538"}`),
	})
	zip.Describe("GET /v1/links", zip.Doc{
		Description: "listLinks lists the caller's own account links, flat and grouped by device.\nIt answers only for the calling subject in the calling org — another user's\nlinks are never included.",
		Fields: map[string]string{
			"linkList.devices": "Devices are the same links grouped by machine, one entry per device.",
			"linkList.links":   "Links are every account link the caller has, newest first.",
			"linkView.billing": "BillingMode(Kind) — how this account's usage bills",
		},
		Response: json.RawMessage(`{"links":[{"id":"link_4c1e","user":"z@hanzo.ai","machine":"m_7f31","host":"spark","provider":"anthropic","kind":"subscription","billing":"plan","status":"linked","createdAt":"2026-07-29T11:00:00Z","updatedAt":"2026-07-29T11:00:00Z"}],"devices":[]}`),
	})
	zip.Describe("GET /v1/links/:id", zip.Doc{
		Description: "getLink reads one of the caller's own account links. A link belonging to another\nuser or org is reported not-found, so the id space leaks no existence.",
		Fields: map[string]string{
			"linkRef.id":       "ID is the link id from the path.",
			"linkView.billing": "BillingMode(Kind) — how this account's usage bills",
		},
		Example: json.RawMessage(`{"id":"link_4c1e9b7a2d6f0538"}`),
	})
	zip.Describe("GET /v1/links/devices/:machine", zip.Doc{
		Description: "deviceDetail reads one of the caller's devices: every provider account linked on\nthat machine, plus how many sessions the caller currently runs there.",
		Fields: map[string]string{
			"deviceRef.machine": "Machine is the machine id from the path.",
			"linkView.billing":  "BillingMode(Kind) — how this account's usage bills",
		},
		Example: json.RawMessage(`{"machine":"m_7f31"}`),
	})
	zip.Describe("GET /v1/links/route", zip.Doc{
		Description: "routePlan ranks the caller's linked accounts into a redundancy plan. It says which\none to send the next request to and what to fall back to: subscription accounts\ncome before metered API keys, and within a group the most headroom wins.",
		Fields: map[string]string{
			"RouteCandidate.billing":     "plan | commerce (BillingMode(Kind))",
			"RouteCandidate.headroomPct": "remaining capacity 0..100",
			"RouteCandidate.kind":        "subscription | apikey",
			"RouteCandidate.reason":      "why unavailable, when Available=false",
		},
		Response: json.RawMessage(`{"candidates":[{"provider":"anthropic","kind":"subscription","billing":"plan","available":true,"headroomPct":53,"linkId":"link_4c1e"}],"generatedAt":"2026-07-29T11:00:00Z"}`),
	})
	zip.Describe("GET /v1/links/usage", zip.Doc{
		Description: "usageDash reports one connected account's consumption of its OWN provider plan. It\nsays how far through the current window it is and when that resets: current is the\nlive state of each lane, windows is the history behind it, and available:false\nmeans the warehouse could not be read, which is not the same as no usage.",
		Fields: map[string]string{
			"dashQuery.account":  "Account narrows to one account of that provider; empty covers them all.",
			"dashQuery.provider": "Provider is the account's provider, e.g. anthropic or openai. Required.",
			"dashQuery.range":    "Range is the history period to read, e.g. 24h; empty means 24h.",
			"dashQuery.window":   "Window narrows to one window class: 6h, day, week or month.",
			"planDash.available": "false = warehouse unavailable, NOT \"no usage\"",
			"planDash.current":   "the live state of each lane: the dash headline",
			"planDash.scope":     "user",
			"planDash.source":    "account — the provider's own meter",
			"planDash.windows":   "every instance in range, newest first",
		},
		Example: json.RawMessage(`{"provider":"anthropic","account":"z@hanzo.ai","window":"6h","range":"24h"}`),
	})
	zip.Describe("GET /v1/links/usage/accounts", zip.Doc{
		Description: "usageAccounts breaks the caller's Hanzo-ROUTED usage down per linked account.\nThis is what Hanzo itself served and charged for, not what a provider's own plan\nmetered; it answers only for the calling subject.",
		Response:    json.RawMessage(`{"scope":"user","source":"routed","total":{"accounts":1,"requests":128,"promptTokens":41200,"completionTokens":9800,"totalTokens":51000,"costCents":316},"accounts":[]}`),
	})
	zip.Describe("GET /v1/links/usage/summary", zip.Doc{
		Description: "usageSummary totals the caller's provider accounts beside their org's Hanzo usage.\nBoth sides land on one board for one period and each reports its own availability,\nso half a warehouse never turns the other half into zeros, and both resolve the\nSAME window from one resolver.",
		Fields: map[string]string{
			"rangeQuery.range":    "Range is the period to total over, e.g. 24h; empty means 24h.",
			"sourceState.source":  "the table of record",
			"summaryResp.account": "Per-source availability: a partial warehouse never fabricates the other half.",
			"totalView.scope":     "user | org",
			"totalView.source":    "account | hanzo",
		},
		Example: json.RawMessage(`{"range":"24h"}`),
	})
	zip.Describe("POST /v1/links", zip.Doc{
		Description: "upsertLink registers or refreshes one machine's provider account for the caller.\nMachine and provider identify the link, so reporting the same pair again updates\nthe existing row rather than adding a second one.",
		Fields: map[string]string{
			"linkView.billing": "BillingMode(Kind) — how this account's usage bills",
		},
		Example: json.RawMessage(`{"machine":"m_7f31","host":"spark","os":"linux","provider":"anthropic","account":"z@hanzo.ai","plan":"max","kind":"subscription"}`),
	})
	zip.Describe("POST /v1/links/devices/:machine/revoke", zip.Doc{
		Description: "revokeDevice revokes every account the caller linked on one machine. It stops the\nsessions they were running there and is the \"I lost this laptop\" verb, and other\nusers' links on the same machine are untouched.",
		Fields: map[string]string{
			"deviceRef.machine": "Machine is the machine id from the path.",
			"linkView.billing":  "BillingMode(Kind) — how this account's usage bills",
		},
		Example: json.RawMessage(`{"machine":"m_7f31"}`),
	})
}
