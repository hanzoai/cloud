// Code generated by zipdoc; DO NOT EDIT.

package link

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/link/:id", zip.Doc{
		Description: "Logs out one account and stops the sessions it was running.\n\nIt revokes a single linked account and stops the agent sessions that ran under\nit, answering with the revoked row and how many sessions stopped. The link is\nRETAINED with a revoked status rather than deleted, so its usage history and\nthe audit trail survive the log-out — which also means a revoked account still\nappears in the list, and is excluded from the route plan rather than absent\nfrom it. The session stop is narrowed to the revoking user's own sessions on\nthat device, provider and account, and a stop that fails does not fail the\nrevoke: the revoked row is the durable truth. An id that does not exist, or\nbelongs to another user or org, is the same 404.",
		Fields: map[string]string{
			"linkRef.id":                 "ID is the link to act on, from the path. It is scoped to the caller, so\nanother user's or org's id is a 404.",
			"linkView.account":           "Account is the provider-side account identifier, when the collector knows it.",
			"linkView.billing":           "Billing is how this account's inference bills — plan (the user's own\nsubscription, metered here for visibility only) or commerce (the gateway\npath). Derived from Kind, never stored.",
			"linkView.createdAt":         "CreatedAt is when the link was first registered, RFC 3339 UTC.",
			"linkView.host":              "Host is the machine's human hostname label, from its most recent report.",
			"linkView.id":                "ID is the link's opaque handle (\"link_\" + 32 hex chars).",
			"linkView.kind":              "Kind is how the account authenticates: subscription or apikey.",
			"linkView.lastSeen":          "LastSeen is when the account last reported, RFC 3339 UTC.",
			"linkView.machine":           "Machine is the stable machine identifier the collector reports.",
			"linkView.os":                "OS is the machine's operating system label.",
			"linkView.plan":              "Plan is the provider plan label (e.g. \"Claude Max\").",
			"linkView.provider":          "Provider is the AI provider this account belongs to (claude, openai, hanzo…).",
			"linkView.status":            "Status is linked or revoked. Revoked rows are retained for history.",
			"linkView.updatedAt":         "UpdatedAt is when the link was last refreshed, RFC 3339 UTC.",
			"linkView.usage":             "Usage is the last good usage snapshot, clamped and re-serialized to known\nfields at ingest.",
			"linkView.user":              "User is the owning subject — the validated caller who registered the link.",
			"revokeResp.links":           "Links is each revoked row with its new status — retained, not deleted, so\nusage history and the audit trail survive the log-out.",
			"revokeResp.revoked":         "Revoked is how many links this call revoked.",
			"revokeResp.sessionsStopped": "SessionsStopped is how many of the caller's own agent sessions stopped. A\nstop that fails does not fail the revoke, so this may honestly report fewer.",
		},
	})
	zip.Describe("GET /v1/link", zip.Doc{
		Description: "Lists your linked accounts and the devices they sit on.\n\nIt answers the caller's own links plus a devices projection of the same rows\nfolded per machine — the cross-machine \"AI Providers / Accounts\" view. A\ndevice is a projection, not a stored entity: its labels come from its\nmost-recently-seen account, so there is no device to create and none to\ngarbage-collect. Revoked links are INCLUDED rather than dropped, because a\nlogged-out account keeps its usage history and audit trail. Scoped to the\ncaller: a validated principal and a non-empty org, else 403.",
		Fields: map[string]string{
			"deviceView.accounts":       "Accounts is every account the caller has signed in on this machine.",
			"deviceView.activeSessions": "ActiveSessions is how many agent sessions the caller currently has running\non this machine; 0 where the agent plane is not mounted.",
			"deviceView.host":           "Host is the machine's hostname label, from its most-recently-seen account.",
			"deviceView.lastSeen":       "LastSeen is when any account on this machine last reported, RFC 3339 UTC.",
			"deviceView.machine":        "Machine is the stable machine identifier.",
			"deviceView.os":             "OS is the machine's operating system label.",
			"linkList.devices":          "Devices is the same rows folded per machine — the cross-machine \"AI\nProviders / Accounts\" view.",
			"linkList.links":            "Links is every link the caller registered, newest first. Revoked links are\nINCLUDED rather than dropped, because a logged-out account keeps its usage\nhistory and audit trail.",
			"linkView.account":          "Account is the provider-side account identifier, when the collector knows it.",
			"linkView.billing":          "Billing is how this account's inference bills — plan (the user's own\nsubscription, metered here for visibility only) or commerce (the gateway\npath). Derived from Kind, never stored.",
			"linkView.createdAt":        "CreatedAt is when the link was first registered, RFC 3339 UTC.",
			"linkView.host":             "Host is the machine's human hostname label, from its most recent report.",
			"linkView.id":               "ID is the link's opaque handle (\"link_\" + 32 hex chars).",
			"linkView.kind":             "Kind is how the account authenticates: subscription or apikey.",
			"linkView.lastSeen":         "LastSeen is when the account last reported, RFC 3339 UTC.",
			"linkView.machine":          "Machine is the stable machine identifier the collector reports.",
			"linkView.os":               "OS is the machine's operating system label.",
			"linkView.plan":             "Plan is the provider plan label (e.g. \"Claude Max\").",
			"linkView.provider":         "Provider is the AI provider this account belongs to (claude, openai, hanzo…).",
			"linkView.status":           "Status is linked or revoked. Revoked rows are retained for history.",
			"linkView.updatedAt":        "UpdatedAt is when the link was last refreshed, RFC 3339 UTC.",
			"linkView.usage":            "Usage is the last good usage snapshot, clamped and re-serialized to known\nfields at ingest.",
			"linkView.user":             "User is the owning subject — the validated caller who registered the link.",
		},
	})
	zip.Describe("GET /v1/link/:id", zip.Doc{
		Description: "Reads one linked account.\n\nIt answers a single link — its device, provider, account, plan, how it bills,\nits status and its latest usage snapshot. An id that does not exist, or\nbelongs to another user or org, is the same 404: the scope is a bound\npredicate on the read, so a wrong id and a foreign id are indistinguishable\nand neither confirms the other's existence. The static paths on this\ncollection — route, usage, devices — register before this one and win\nfirst-match, so a link whose id collided with one of those words could not be\naddressed here.",
		Fields: map[string]string{
			"linkRef.id":         "ID is the link to act on, from the path. It is scoped to the caller, so\nanother user's or org's id is a 404.",
			"linkView.account":   "Account is the provider-side account identifier, when the collector knows it.",
			"linkView.billing":   "Billing is how this account's inference bills — plan (the user's own\nsubscription, metered here for visibility only) or commerce (the gateway\npath). Derived from Kind, never stored.",
			"linkView.createdAt": "CreatedAt is when the link was first registered, RFC 3339 UTC.",
			"linkView.host":      "Host is the machine's human hostname label, from its most recent report.",
			"linkView.id":        "ID is the link's opaque handle (\"link_\" + 32 hex chars).",
			"linkView.kind":      "Kind is how the account authenticates: subscription or apikey.",
			"linkView.lastSeen":  "LastSeen is when the account last reported, RFC 3339 UTC.",
			"linkView.machine":   "Machine is the stable machine identifier the collector reports.",
			"linkView.os":        "OS is the machine's operating system label.",
			"linkView.plan":      "Plan is the provider plan label (e.g. \"Claude Max\").",
			"linkView.provider":  "Provider is the AI provider this account belongs to (claude, openai, hanzo…).",
			"linkView.status":    "Status is linked or revoked. Revoked rows are retained for history.",
			"linkView.updatedAt": "UpdatedAt is when the link was last refreshed, RFC 3339 UTC.",
			"linkView.usage":     "Usage is the last good usage snapshot, clamped and re-serialized to known\nfields at ingest.",
			"linkView.user":      "User is the owning subject — the validated caller who registered the link.",
		},
	})
	zip.Describe("GET /v1/link/devices/:machine", zip.Doc{
		Description: "Shows one machine: its accounts, usage and live sessions.\n\nIt answers one device — its host and OS labels, every account the caller has\nsigned in on that machine with its latest usage, and how many agent sessions\nthe caller currently has running on it. The device labels come from the\nmost-recently-seen account, since a device is a projection of its links rather\nthan a row of its own. A machine with none of the caller's accounts is 404,\nwhich is also the answer when the machine belongs to someone else — the scope\nmakes the two indistinguishable, deliberately. The session count reports 0\nwhere the agent plane is not mounted rather than failing the read.",
		Fields: map[string]string{
			"deviceView.accounts":       "Accounts is every account the caller has signed in on this machine.",
			"deviceView.activeSessions": "ActiveSessions is how many agent sessions the caller currently has running\non this machine; 0 where the agent plane is not mounted.",
			"deviceView.host":           "Host is the machine's hostname label, from its most-recently-seen account.",
			"deviceView.lastSeen":       "LastSeen is when any account on this machine last reported, RFC 3339 UTC.",
			"deviceView.machine":        "Machine is the stable machine identifier.",
			"deviceView.os":             "OS is the machine's operating system label.",
			"linkView.account":          "Account is the provider-side account identifier, when the collector knows it.",
			"linkView.billing":          "Billing is how this account's inference bills — plan (the user's own\nsubscription, metered here for visibility only) or commerce (the gateway\npath). Derived from Kind, never stored.",
			"linkView.createdAt":        "CreatedAt is when the link was first registered, RFC 3339 UTC.",
			"linkView.host":             "Host is the machine's human hostname label, from its most recent report.",
			"linkView.id":               "ID is the link's opaque handle (\"link_\" + 32 hex chars).",
			"linkView.kind":             "Kind is how the account authenticates: subscription or apikey.",
			"linkView.lastSeen":         "LastSeen is when the account last reported, RFC 3339 UTC.",
			"linkView.machine":          "Machine is the stable machine identifier the collector reports.",
			"linkView.os":               "OS is the machine's operating system label.",
			"linkView.plan":             "Plan is the provider plan label (e.g. \"Claude Max\").",
			"linkView.provider":         "Provider is the AI provider this account belongs to (claude, openai, hanzo…).",
			"linkView.status":           "Status is linked or revoked. Revoked rows are retained for history.",
			"linkView.updatedAt":        "UpdatedAt is when the link was last refreshed, RFC 3339 UTC.",
			"linkView.usage":            "Usage is the last good usage snapshot, clamped and re-serialized to known\nfields at ingest.",
			"linkView.user":             "User is the owning subject — the validated caller who registered the link.",
			"machineRef.machine":        "Machine is the machine to act on, from the path. It is scoped to the\ncaller, so a machine with none of the caller's accounts is a 404.",
		},
	})
	zip.Describe("GET /v1/link/route", zip.Doc{
		Description: "Gets the failover order across your linked accounts.\n\nIt answers an ordered redundancy plan over the caller's LINKED (not revoked)\naccounts: each candidate with its remaining rate-limit headroom, whether it is\nroutable right now, how it BILLS (plan or commerce), and a reason when it is\nnot — plus the primary to try first. It is what lets a router fail over from\none subscription to another and fall back to the metered API as the\nalways-available backstop, knowing the cost consequence before it dials.\n\nIt is POLICY, not execution: the plan is computed purely from the usage\nsnapshots already in the registry, never by probing a provider, so it is a\ntotal function of the links and costs nothing to ask for. Actually dialing,\ndetecting a live 429 and advancing to the next candidate belongs to the\ncaller. A link with no snapshot counts as full headroom.",
		Fields: map[string]string{
			"RouteCandidate.account":     "Account is the provider-side account identifier.",
			"RouteCandidate.available":   "Available reports whether the candidate is routable right now.",
			"RouteCandidate.billing":     "Billing is the cost consequence of dialing this candidate: plan (the\nuser's own subscription) or commerce (the metered gateway path).",
			"RouteCandidate.headroomPct": "HeadroomPct is the remaining rate-limit capacity, 0..100. A link with no\nsnapshot counts as full headroom.",
			"RouteCandidate.host":        "Host is that machine's hostname label.",
			"RouteCandidate.kind":        "Kind is how the account authenticates: subscription or apikey.",
			"RouteCandidate.linkId":      "LinkID is the underlying link's opaque handle.",
			"RouteCandidate.machine":     "Machine is the machine the account is signed in on.",
			"RouteCandidate.plan":        "Plan is the provider plan label the account is on.",
			"RouteCandidate.provider":    "Provider is the AI provider the candidate account belongs to.",
			"RouteCandidate.reason":      "Reason says why the candidate is not routable, when Available is false.",
			"RoutePlan.candidates":       "Candidates is every linked account in preference order: subscriptions\nfirst, then metered api-key accounts as the backstop.",
			"RoutePlan.generatedAt":      "GeneratedAt is when the plan was computed, RFC 3339 UTC.",
			"RoutePlan.primary":          "Primary is the first available candidate; absent when every account is\nrate-limited.",
		},
	})
	zip.Describe("GET /v1/link/usage", zip.Doc{
		Description: "Shows one provider account's own usage dashboard.\n\nIt answers the time series for a SINGLE provider account — the windows in\nrange plus the currently-open ones — as that provider's own meter reported it:\n\"my plan is 47% through its 6h window, resets at 14:20\". current is the newest\ninstance of each lane (the headline); windows is the history behind it, both\ncomputed from ONE deduped read. provider is required; an unknown window class\nor range is 400, never a quiet fallback to a different one. When no series is\navailable the response is a 200 with available:false and empty lists — an\nhonest \"we have no data\", which is a different claim from zero usage.",
		Fields: map[string]string{
			"boardResp.account":             "Account is the account the series narrows to, when one was named.",
			"boardResp.available":           "Available reports whether the warehouse answered; false is an honest \"we\nhave no data\", NOT zero usage.",
			"boardResp.current":             "Current is the live state of each lane — the dash headline.",
			"boardResp.from":                "From is when the resolved window opens, RFC 3339 UTC.",
			"boardResp.provider":            "Provider is the provider whose meter answered.",
			"boardResp.range":               "Range is the resolved period label.",
			"boardResp.scope":               "Scope is always \"user\": the caller's own linked accounts.",
			"boardResp.source":              "Source is always \"account\": the provider's own meter, not a Hanzo charge.",
			"boardResp.to":                  "To is where it closes, EXCLUSIVE, RFC 3339 UTC — the instant the read was\nserved, so the window walks forward with the clock and two reads a minute\napart do not cover the same period.",
			"boardResp.windows":             "Windows is every window instance in range, newest first.",
			"dashIn.account":                "Account narrows to one account when a user has several with the provider.",
			"dashIn.provider":               "Provider is the provider whose meter to read. Required.",
			"dashIn.range":                  "Range is the period, one of 1h, 24h, 7d or 30d; empty means 24h, and an\nunknown label is 400, never a quiet fallback.",
			"dashIn.window":                 "Window selects a window class: 6h, day, week or month. Empty reads all.",
			"readingView.account":           "Account is the provider-side account the sample belongs to.",
			"readingView.cachedInputTokens": "CachedInputTokens is the window's cached-prompt-token count.",
			"readingView.confidence":        "Confidence says whether the counters that remain mean anything, as the\nmeter graded itself.",
			"readingView.costCents":         "CostCents is the window's spend in cents, as the provider's meter states it.",
			"readingView.costLimitCents":    "CostLimitCents is the window's spend cap in cents, when the meter knows one.",
			"readingView.currency":          "Currency is the ISO currency the cost fields are stated in.",
			"readingView.inputTokens":       "InputTokens is the window's prompt-token count.",
			"readingView.lane":              "Lane names the meter's own lane label for this measurement.",
			"readingView.machine":           "Machine is the machine the collector observed the account on.",
			"readingView.outputTokens":      "OutputTokens is the window's completion-token count.",
			"readingView.plan":              "Plan is the provider plan label the account is on.",
			"readingView.requests":          "Requests is the window's request count.",
			"readingView.resetsAt":          "ResetsAt is when the window resets, RFC 3339 UTC.",
			"readingView.synthetic":         "Synthetic marks a sample the collector derived rather than observed.",
			"readingView.totalTokens":       "TotalTokens is the window's total token count.",
			"readingView.usedPct":           "UsedPct is how much of the window's allowance is consumed, 0..100.",
			"readingView.window":            "Window is the window class: 6h, day, week or month.",
			"readingView.windowMinutes":     "WindowMinutes is the window's length as the meter reported it.",
			"readingView.windowStart":       "WindowStart is when the measured window opened, RFC 3339 UTC.",
		},
	})
	zip.Describe("GET /v1/link/usage/accounts", zip.Doc{
		Description: "Breaks down what the gateway routed through each of your accounts.\n\nIt answers one row per linked account the GATEWAY actually routed through,\nplus their total — requests, prompt and completion tokens, and cost. This is\nthe routed ledger, the read twin of the counter the router writes, and it is\ndistinct from both of its neighbours: not the device collector's plan\nsnapshots, and not the org money ledger. The source and scope fields on the\nresponse say so on every payload. The same shape answers in the billing\nnamespace, from one shaping function, so the two mounts cannot drift.",
		Fields: map[string]string{
			"AccountsTotal.accounts":         "Accounts is how many linked accounts the total folds.",
			"AccountsTotal.completionTokens": "CompletionTokens is the total completion-token count.",
			"AccountsTotal.costCents":        "CostCents is the total cost in cents.",
			"AccountsTotal.promptTokens":     "PromptTokens is the total prompt-token count.",
			"AccountsTotal.requests":         "Requests is the total request count the gateway routed.",
			"AccountsTotal.totalTokens":      "TotalTokens is the total token count.",
			"AccountsUsage.accounts":         "Accounts is one row per linked account the gateway actually routed through.",
			"AccountsUsage.scope":            "Scope is always \"user\": the caller's own linked accounts.",
			"AccountsUsage.source":           "Source is always \"routed\": the gateway's own routed ledger.",
			"AccountsUsage.total":            "Total is the honest sum across the rows.",
			"RoutedUsage.account":            "Account is the provider-side account identifier.",
			"RoutedUsage.billing":            "Billing is how the routed inference bills: plan or commerce.",
			"RoutedUsage.completionTokens":   "CompletionTokens is the routed completion-token count.",
			"RoutedUsage.costCents":          "CostCents is the routed cost in cents.",
			"RoutedUsage.kind":               "Kind is how the account authenticates: subscription or apikey.",
			"RoutedUsage.promptTokens":       "PromptTokens is the routed prompt-token count.",
			"RoutedUsage.provider":           "Provider is the AI provider the row's account belongs to.",
			"RoutedUsage.requests":           "Requests is how many requests the gateway routed through this account.",
			"RoutedUsage.totalTokens":        "TotalTokens is the routed total token count.",
		},
	})
	zip.Describe("GET /v1/link/usage/summary", zip.Doc{
		Description: "Shows plan consumption and Hanzo spend side by side.\n\nIt answers the global usage board over one window: the caller's own linked\naccounts, metered from each provider's own login, alongside their org's\nHanzo-routed inference. These come from different ledgers and mean different\nthings, so every row is LABELLED by source, by scope and by availability, and\nTHE TWO ARE NEVER SUMMED — a plan's percentage is not money, and a provider's\nown spend is not a Hanzo charge. The rows sit side by side and say what they\nare.\n\nOne resolver fixes the window for both halves, so the two sets always cover\nthe same period. range is one of 1h, 24h, 7d or 30d and defaults to 24h;\nanything else is 400 rather than a silent substitution. A ledger that cannot\nanswer reports available:false instead of a zero that would read as \"no usage\".",
		Fields: map[string]string{
			"sourceState.available": "Available reports whether this ledger answered; false is honest\n\"unavailable\", never a zero that would read as no usage.",
			"sourceState.note":      "Note says in prose what the ledger's numbers mean.",
			"sourceState.scope":     "Scope is whose usage the ledger measures: user or org.",
			"sourceState.source":    "Source is the table of record behind the ledger.",
			"summaryIn.range":       "Range is the period, one of 1h, 24h, 7d or 30d; empty means 24h, and an\nunknown label is 400, never a silent substitution.",
			"summaryResp.account":   "Account reports the linked-accounts ledger's own availability, so a partial\nanswer never fabricates this half. It is scoped to the CALLER: the accounts\nthey linked, metered from each provider's own login.",
			"summaryResp.from":      "From is when the window opens, RFC 3339 UTC. ONE resolver fixes it for both\nledgers, so the account rows and the Hanzo rows always cover the same period\n— two resolvers could drift and turn the union into a lie.",
			"summaryResp.hanzo":     "Hanzo reports the same for the Hanzo-routed ledger, which is scoped to the\nORG rather than the caller — a different question over the same window. The\ntwo are independent: either can be unavailable while the other answers, and\nRows then carries only the half that did.",
			"summaryResp.range":     "Range is the resolved period label.",
			"summaryResp.rows":      "Rows is the union of both ledgers, each row labelled by source and scope —\nconcatenated, NEVER summed: a plan's percentage is not money.",
			"summaryResp.to":        "To is where the window closes, EXCLUSIVE, RFC 3339 UTC — the instant the read\nwas served. Shared by both ledgers, for the reason From gives.",
			"totalView.confidence":  "Confidence says how real the row's numbers are.",
			"totalView.costCents":   "CostCents is the period's spend in cents, in the row's own ledger.",
			"totalView.provider":    "Provider is the provider the row totals.",
			"totalView.requests":    "Requests is the period's request count.",
			"totalView.scope":       "Scope is whose usage the row measures: user or org.",
			"totalView.source":      "Source is whose meter the row came from: account or hanzo.",
			"totalView.tokens":      "Tokens is the period's total token count.",
			"totalView.usedPct":     "UsedPct is the plan consumption percentage, on the account side.",
			"totalView.window":      "Window is the window class the row totals, on the account side.",
			"totalView.windows":     "Windows is how many window instances the row folds.",
		},
	})
	zip.Describe("POST /v1/link", zip.Doc{
		Description: "Registers a signed-in AI provider account on a machine.\n\nIt records that a developer has signed into one provider account on one\nmachine — a Claude Max or ChatGPT Plus subscription, a Hanzo key, a raw\nprovider key — and answers 201 with the stored link. Re-reporting the same\n(machine, provider, account) UPDATES that link rather than creating a second,\nso a collector may call this on every heartbeat. machine and provider are\nrequired (400 otherwise), as is a valid kind, and every field is\nlength-bounded. Scoped to the caller: a validated principal and a non-empty\norg, else 403, so a caller writes only their OWN accounts within their own org.",
		Fields: map[string]string{
			"enrollReq.account":  "Account is the provider-side account identifier.",
			"enrollReq.host":     "Host is the machine's human hostname label.",
			"enrollReq.kind":     "Kind decides how the account's inference BILLS and defaults to\nsubscription: a subscription account bills the user's own monthly plan and\nis metered here for visibility only, while an apikey account bills through\ncommerce on the gateway path.",
			"enrollReq.machine":  "Machine is the stable machine identifier. Required, length-bounded.",
			"enrollReq.os":       "OS is the machine's operating system label.",
			"enrollReq.plan":     "Plan is the provider plan label (e.g. \"Claude Max\").",
			"enrollReq.provider": "Provider is the AI provider the account belongs to. Required, length-bounded.",
			"enrollReq.usage":    "Usage is an optional usage snapshot, clamped and re-serialized to known\nfields; omitting it keeps the last good one.",
			"linkView.account":   "Account is the provider-side account identifier, when the collector knows it.",
			"linkView.billing":   "Billing is how this account's inference bills — plan (the user's own\nsubscription, metered here for visibility only) or commerce (the gateway\npath). Derived from Kind, never stored.",
			"linkView.createdAt": "CreatedAt is when the link was first registered, RFC 3339 UTC.",
			"linkView.host":      "Host is the machine's human hostname label, from its most recent report.",
			"linkView.id":        "ID is the link's opaque handle (\"link_\" + 32 hex chars).",
			"linkView.kind":      "Kind is how the account authenticates: subscription or apikey.",
			"linkView.lastSeen":  "LastSeen is when the account last reported, RFC 3339 UTC.",
			"linkView.machine":   "Machine is the stable machine identifier the collector reports.",
			"linkView.os":        "OS is the machine's operating system label.",
			"linkView.plan":      "Plan is the provider plan label (e.g. \"Claude Max\").",
			"linkView.provider":  "Provider is the AI provider this account belongs to (claude, openai, hanzo…).",
			"linkView.status":    "Status is linked or revoked. Revoked rows are retained for history.",
			"linkView.updatedAt": "UpdatedAt is when the link was last refreshed, RFC 3339 UTC.",
			"linkView.usage":     "Usage is the last good usage snapshot, clamped and re-serialized to known\nfields at ingest.",
			"linkView.user":      "User is the owning subject — the validated caller who registered the link.",
		},
	})
	zip.Describe("POST /v1/link/devices/:machine/revoke", zip.Doc{
		Description: "Logs out every account on one machine and stops its sessions.\n\nIt revokes every one of the caller's accounts on one machine and stops the\nagent sessions they were running, answering with how many of each. This is the\n\"I lost that laptop\" button. Revoked links are RETAINED, not deleted, so usage\nhistory and the audit trail survive a log-out — the rows come back in the\nresponse with their new status. The session stop reaches only the REVOKING\nuser's own sessions, so a shared machine name can never be used to stop a\nco-tenant's work, and a stop that fails does not fail the revoke: the revoked\nrow is the durable truth and the count then honestly reports fewer. A machine\nwith nothing left to revoke is 404.",
		Fields: map[string]string{
			"linkView.account":           "Account is the provider-side account identifier, when the collector knows it.",
			"linkView.billing":           "Billing is how this account's inference bills — plan (the user's own\nsubscription, metered here for visibility only) or commerce (the gateway\npath). Derived from Kind, never stored.",
			"linkView.createdAt":         "CreatedAt is when the link was first registered, RFC 3339 UTC.",
			"linkView.host":              "Host is the machine's human hostname label, from its most recent report.",
			"linkView.id":                "ID is the link's opaque handle (\"link_\" + 32 hex chars).",
			"linkView.kind":              "Kind is how the account authenticates: subscription or apikey.",
			"linkView.lastSeen":          "LastSeen is when the account last reported, RFC 3339 UTC.",
			"linkView.machine":           "Machine is the stable machine identifier the collector reports.",
			"linkView.os":                "OS is the machine's operating system label.",
			"linkView.plan":              "Plan is the provider plan label (e.g. \"Claude Max\").",
			"linkView.provider":          "Provider is the AI provider this account belongs to (claude, openai, hanzo…).",
			"linkView.status":            "Status is linked or revoked. Revoked rows are retained for history.",
			"linkView.updatedAt":         "UpdatedAt is when the link was last refreshed, RFC 3339 UTC.",
			"linkView.usage":             "Usage is the last good usage snapshot, clamped and re-serialized to known\nfields at ingest.",
			"linkView.user":              "User is the owning subject — the validated caller who registered the link.",
			"machineRef.machine":         "Machine is the machine to act on, from the path. It is scoped to the\ncaller, so a machine with none of the caller's accounts is a 404.",
			"revokeResp.links":           "Links is each revoked row with its new status — retained, not deleted, so\nusage history and the audit trail survive the log-out.",
			"revokeResp.revoked":         "Revoked is how many links this call revoked.",
			"revokeResp.sessionsStopped": "SessionsStopped is how many of the caller's own agent sessions stopped. A\nstop that fails does not fail the revoke, so this may honestly report fewer.",
		},
	})
	zip.Describe("POST /v1/link/usage", zip.Doc{
		Description: "Reports usage samples from the device collector.\n\nIt ingests a batch of usage samples and answers with how many were accepted,\nwhether history was durably stored, and the links they refreshed. A report\nalso REFRESHES one link per distinct (machine, provider, account) it names, so\na running collector keeps the accounts overview current without a separate\nregistration call.\n\nA caller can only ever report for THEMSELVES: org and subject come from the\nvalidated bearer, never from the body, so no sample can be attributed to\nanother user or tenant. History is FAIL-SOFT and stored says which happened —\na warehouse outage still accepts the report and refreshes the links rather\nthan failing the device, and answers 202 either way. Send either one sample\ninline or up to 256 in samples; an empty batch or an over-long one is 400, as\nis a provider, window class or kind outside the closed vocabulary — an\nunrecognized window is refused rather than rewritten, because a silently\nreclassified sample would fill a dashboard with a class nobody reported.",
		Fields: map[string]string{
			"ingestReq.samples":            "Samples is the batch form, up to 256 samples; leave it empty to send one\nsample inline on the same fields.",
			"ingestResp.accepted":          "Accepted is how many samples this report landed.",
			"ingestResp.links":             "Links is the link row each distinct (machine, provider, account) in the\nbatch refreshed.",
			"ingestResp.stored":            "Stored reports whether history was durably written; false means the\nwarehouse was unavailable and only the link rows were refreshed.",
			"linkView.account":             "Account is the provider-side account identifier, when the collector knows it.",
			"linkView.billing":             "Billing is how this account's inference bills — plan (the user's own\nsubscription, metered here for visibility only) or commerce (the gateway\npath). Derived from Kind, never stored.",
			"linkView.createdAt":           "CreatedAt is when the link was first registered, RFC 3339 UTC.",
			"linkView.host":                "Host is the machine's human hostname label, from its most recent report.",
			"linkView.id":                  "ID is the link's opaque handle (\"link_\" + 32 hex chars).",
			"linkView.kind":                "Kind is how the account authenticates: subscription or apikey.",
			"linkView.lastSeen":            "LastSeen is when the account last reported, RFC 3339 UTC.",
			"linkView.machine":             "Machine is the stable machine identifier the collector reports.",
			"linkView.os":                  "OS is the machine's operating system label.",
			"linkView.plan":                "Plan is the provider plan label (e.g. \"Claude Max\").",
			"linkView.provider":            "Provider is the AI provider this account belongs to (claude, openai, hanzo…).",
			"linkView.status":              "Status is linked or revoked. Revoked rows are retained for history.",
			"linkView.updatedAt":           "UpdatedAt is when the link was last refreshed, RFC 3339 UTC.",
			"linkView.usage":               "Usage is the last good usage snapshot, clamped and re-serialized to known\nfields at ingest.",
			"linkView.user":                "User is the owning subject — the validated caller who registered the link.",
			"readingReq.account":           "Account is the provider-side account the sample belongs to.",
			"readingReq.cachedInputTokens": "CachedInputTokens is the window's cached-prompt-token count.",
			"readingReq.confidence":        "Confidence says how real the counters are, as the meter graded itself.",
			"readingReq.costCents":         "CostCents is the window's spend in cents, as the provider's meter states it.",
			"readingReq.costLimitCents":    "CostLimitCents is the window's spend cap in cents, when the meter knows one.",
			"readingReq.currency":          "Currency is the ISO currency the cost fields are stated in.",
			"readingReq.inputTokens":       "InputTokens is the window's prompt-token count.",
			"readingReq.kind":              "Kind is subscription or apikey; anything else is refused.",
			"readingReq.lane":              "Lane names the meter's own lane label for this measurement.",
			"readingReq.machine":           "Machine is the machine the collector observed the account on. Required.",
			"readingReq.outputTokens":      "OutputTokens is the window's completion-token count.",
			"readingReq.plan":              "Plan is the provider plan label the account is on.",
			"readingReq.provider":          "Provider is the AI provider whose meter reported this sample. Required.",
			"readingReq.requests":          "Requests is the window's request count.",
			"readingReq.resetsAt":          "ResetsAt is when the window resets, RFC 3339, bounded.",
			"readingReq.synthetic":         "Synthetic marks a sample the collector derived rather than observed.",
			"readingReq.totalTokens":       "TotalTokens is the window's total token count.",
			"readingReq.usedPct":           "UsedPct is how much of the window's allowance is consumed, clamped 0..100.",
			"readingReq.window":            "Window is the window class, one of 6h, day, week, month; anything else is\nrefused rather than silently reclassified.",
			"readingReq.windowMinutes":     "WindowMinutes is the window's length as the meter reported it.",
			"readingReq.windowStart":       "WindowStart is when the measured window opened, RFC 3339, bounded to a\nsane interval around now.",
		},
	})
}
