// Code generated by zipdoc; DO NOT EDIT.

package dataroom

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/dataroom/analytics/dataroom/:dataroomId", zip.Doc{
		Description: "Rolls up every share link pointing at one data room:\nsession and page-view totals for the room, plus the per-page breakdown for each\nlink beneath it.\n\nA room id outside the caller's own tenant store is not found. Only links that\nNAME the room are counted — a link created over a single document contributes\nnothing here, even when that document also sits in the room.",
		Fields: map[string]string{
			"dataroomLinkStats.linkId":         "LinkId is the link these counts are for.",
			"dataroomLinkStats.pages":          "Pages is the per-page breakdown, in page order.",
			"dataroomLinkStats.totalPageViews": "TotalPageViews is how many page views the link received.",
			"dataroomLinkStats.totalViews":     "TotalViews is how many viewing sessions the link opened.",
			"dataroomPageStat.avgDuration":     "AvgDuration is totalDuration divided by views, rounded; 0 when unviewed.",
			"dataroomPageStat.pageNumber":      "PageNumber is the page these counts are for.",
			"dataroomPageStat.totalDuration":   "TotalDuration is the summed dwell measure reported for the page.",
			"dataroomPageStat.views":           "Views is how many times the page was viewed.",
			"dataroomStats.dataroomId":         "DataroomId is the room these counts are for.",
			"dataroomStats.links":              "Links is the same per-page breakdown for each link into the room.",
			"dataroomStats.totalPageViews":     "TotalPageViews is the room's page views across every link.",
			"dataroomStats.totalViews":         "TotalViews is the room's viewing sessions across every link.",
			"roomStatsRef.dataroomId":          "DataroomID is the room to report on. It is the path segment, resolved in\nthe caller's own tenant store.",
		},
	})
	zip.Describe("GET /v1/dataroom/analytics/link/:linkId", zip.Doc{
		Description: "Reports how one share link was actually read: total viewing\nsessions, total page views, and per page the view count, the summed dwell\nmeasure and its average.\n\nThe link is resolved in the caller's OWN tenant store, so another org's link id\nis not found — knowing a link id is enough to OPEN the room it shares, and\nnever enough to read who has been reading it.",
		Fields: map[string]string{
			"dataroomLinkStats.linkId":         "LinkId is the link these counts are for.",
			"dataroomLinkStats.pages":          "Pages is the per-page breakdown, in page order.",
			"dataroomLinkStats.totalPageViews": "TotalPageViews is how many page views the link received.",
			"dataroomLinkStats.totalViews":     "TotalViews is how many viewing sessions the link opened.",
			"dataroomPageStat.avgDuration":     "AvgDuration is totalDuration divided by views, rounded; 0 when unviewed.",
			"dataroomPageStat.pageNumber":      "PageNumber is the page these counts are for.",
			"dataroomPageStat.totalDuration":   "TotalDuration is the summed dwell measure reported for the page.",
			"dataroomPageStat.views":           "Views is how many times the page was viewed.",
			"linkRef.linkId":                   "LinkID is the link to report on. It is the path segment, resolved in the\ncaller's own tenant store.",
		},
	})
	zip.Describe("GET /v1/dataroom/datarooms", zip.Doc{
		Description: "Returns every data room in the caller org's own store, newest\nfirst, with its short public id, name, description and timestamps.\n\nDocuments are not included — a room's contents come from reading the single\nroom.",
		Fields: map[string]string{
			"dataroomRoom.createdAt":   "CreatedAt is when the room was created, in unix milliseconds.",
			"dataroomRoom.description": "Description is the room's description, null when none was given.",
			"dataroomRoom.id":          "ID is the room id, which is what other dataroom calls address it by.",
			"dataroomRoom.name":        "Name is the room's display name.",
			"dataroomRoom.pId":         "PId is the room's short public identifier, unique within the tenant.",
			"dataroomRoom.updatedAt":   "UpdatedAt is when the room last changed, in unix milliseconds.",
			"dataroomRooms.datarooms":  "Datarooms is every data room in the caller's own store, newest first.",
		},
	})
	zip.Describe("GET /v1/dataroom/datarooms/:id", zip.Doc{
		Description: "Reads one of the caller org's data rooms together with every\ndocument in it, each carrying its membership id and order index.\n\nThe documents are sorted by that index with unordered ones last and creation\ntime breaking ties — the SAME order a link's visitor sees, so this is what the\nroom looks like from the outside. A room id outside the caller's own tenant\nstore is not found.",
		Fields: map[string]string{
			"dataroomMember.contentType":        "ContentType is the mime type recorded at upload, null when none was sent.",
			"dataroomMember.createdAt":          "CreatedAt is when the document was uploaded, in unix milliseconds.",
			"dataroomMember.dataroomDocumentId": "DataroomDocumentId is the membership id — this document's place in THIS\nroom, distinct from the document id.",
			"dataroomMember.fileKey":            "FileKey is the opaque object-storage key the bytes are stored under.",
			"dataroomMember.fileSize":           "FileSize is the stored byte count, null when it was not recorded.",
			"dataroomMember.id":                 "ID is the document id.",
			"dataroomMember.name":               "Name is the document's display name.",
			"dataroomMember.numPages":           "NumPages is the page count, null when it was not supplied at upload.",
			"dataroomMember.orderIndex":         "OrderIndex is the document's place in the viewer's list, null when it was\nadded without one. Unordered documents sort last.",
			"dataroomMember.type":               "Type is the document's kind, null when it was not recorded.",
			"dataroomMember.updatedAt":          "UpdatedAt is when the document row last changed, in unix milliseconds.",
			"dataroomRef.id":                    "ID is the room to read. It is the path segment: the URL is the addressing\nauthority, and the org it is resolved in comes from the caller's principal,\nso an id from another tenant is simply not found.",
			"dataroomRoomDetail.createdAt":      "CreatedAt is when the room was created, in unix milliseconds.",
			"dataroomRoomDetail.description":    "Description is the room's description, null when none was given.",
			"dataroomRoomDetail.documents":      "Documents is every document in the room, in the order a visitor sees them.",
			"dataroomRoomDetail.id":             "ID is the room id.",
			"dataroomRoomDetail.name":           "Name is the room's display name.",
			"dataroomRoomDetail.pId":            "PId is the room's short public identifier.",
			"dataroomRoomDetail.updatedAt":      "UpdatedAt is when the room last changed, in unix milliseconds.",
			"dataroomRoomDetailOne.dataroom":    "Dataroom is the room and its contents.",
		},
	})
	zip.Describe("GET /v1/dataroom/documents", zip.Doc{
		Description: "Returns every document in the caller org's own store, newest\nfirst — name, opaque storage key, content type, page count, size and\ntimestamps.\n\nTenant isolation is the per-org store itself: there is one SQLite file per org\nand the org is never a parameter, so no input the caller controls can address\nanother tenant's documents. Metadata only — the bytes come from the file route.",
		Fields: map[string]string{
			"dataroomDocument.contentType": "ContentType is the mime type recorded at upload, null when none was sent.",
			"dataroomDocument.createdAt":   "CreatedAt is when the document was uploaded, in unix milliseconds.",
			"dataroomDocument.fileKey":     "FileKey is the opaque object-storage key the bytes are stored under. It is\nscoped to the tenant's own key prefix and is not a URL.",
			"dataroomDocument.fileSize":    "FileSize is the stored byte count, null when it was not recorded.",
			"dataroomDocument.id":          "ID is the document id.",
			"dataroomDocument.name":        "Name is the document's display name.",
			"dataroomDocument.numPages":    "NumPages is the page count, null when it was not supplied at upload.",
			"dataroomDocument.type":        "Type is the document's kind, null when it was not recorded.",
			"dataroomDocument.updatedAt":   "UpdatedAt is when the document row last changed, in unix milliseconds.",
			"dataroomDocuments.documents":  "Documents is every document in the caller's own store, newest first.",
		},
	})
	zip.Describe("GET /v1/dataroom/documents/:id", zip.Doc{
		Description: "Reads one of the caller org's documents — its name, opaque storage\nkey, content type, page count, size and timestamps.\n\nThe lookup runs in the caller's own tenant store, so an id belonging to another\norg is not found exactly like one that never existed. Metadata only: the bytes\nare a separate read.",
		Fields: map[string]string{
			"dataroomDocument.contentType": "ContentType is the mime type recorded at upload, null when none was sent.",
			"dataroomDocument.createdAt":   "CreatedAt is when the document was uploaded, in unix milliseconds.",
			"dataroomDocument.fileKey":     "FileKey is the opaque object-storage key the bytes are stored under. It is\nscoped to the tenant's own key prefix and is not a URL.",
			"dataroomDocument.fileSize":    "FileSize is the stored byte count, null when it was not recorded.",
			"dataroomDocument.id":          "ID is the document id.",
			"dataroomDocument.name":        "Name is the document's display name.",
			"dataroomDocument.numPages":    "NumPages is the page count, null when it was not supplied at upload.",
			"dataroomDocument.type":        "Type is the document's kind, null when it was not recorded.",
			"dataroomDocument.updatedAt":   "UpdatedAt is when the document row last changed, in unix milliseconds.",
			"dataroomDocumentOne.document": "Document is the document itself.",
			"documentRef.id":               "ID is the document to read. It is the path segment: the URL is the\naddressing authority, and the org it is resolved in comes from the caller's\nprincipal, so an id from another tenant is simply not found.",
		},
	})
	zip.Describe("GET /v1/dataroom/documents/:id/file", zip.Doc{
		Description: "Streams a document's bytes to an authenticated owner.",
	})
	zip.Describe("GET /v1/dataroom/links", zip.Doc{
		Description: "Returns every live share link in the caller org's own store,\nnewest first, with the controls a visitor will meet: whether an address is\nrequired, whether a password is set, the allow and deny lists, whether download\nis permitted, and when the link expires.\n\nArchived links are omitted entirely. A link reports only THAT a password is\nset — the stored form is a bcrypt hash and no route returns it.",
		Fields: map[string]string{
			"dataroomLink.allowDownload":  "AllowDownload is whether a visitor may download, rather than only view.",
			"dataroomLink.allowList":      "AllowList narrows which addresses pass the email gate. An entry may be a\nfull address, an \"@domain.com\" suffix, or a bare \"domain.com\". An EMPTY\nlist admits everyone.",
			"dataroomLink.createdAt":      "CreatedAt is when the link was minted, in unix milliseconds.",
			"dataroomLink.dataroomId":     "DataroomId is the room the link opens, null for a single-document link.",
			"dataroomLink.denyList":       "DenyList rejects addresses, in the same three forms as the allow list, and\nis checked BEFORE it — so deny always wins.",
			"dataroomLink.documentId":     "DocumentId is the document the link opens, null for a room link.",
			"dataroomLink.emailProtected": "EmailProtected is whether a visitor must state an address to enter.",
			"dataroomLink.expiresAt":      "ExpiresAt is when the link closes, in unix milliseconds; null never expires.",
			"dataroomLink.hasPassword":    "HasPassword reports THAT a password is set. The stored form is a bcrypt\nhash and no route returns it.",
			"dataroomLink.id":             "ID is the link id — the public token a visitor opens the room with.",
			"dataroomLink.isArchived":     "IsArchived is whether the link has been retired.",
			"dataroomLink.linkType":       "LinkType is DATAROOM_LINK or DOCUMENT_LINK.",
			"dataroomLink.name":           "Name is the link's label, null when none was given.",
			"dataroomLink.updatedAt":      "UpdatedAt is when the link last changed, in unix milliseconds.",
			"dataroomLinks.links":         "Links is every non-archived link, newest first.",
		},
	})
	zip.Describe("GET /v1/dataroom/view/:linkId/document/:documentId/file", zip.Doc{
		Description: "Streams a document's bytes to an authorised viewer.",
	})
	zip.Describe("POST /v1/dataroom/datarooms", zip.Doc{
		Description: "Opens a new data room for the caller org and answers with it,\nincluding the short public id it is addressed by.\n\n`name` is required; without it the call is refused and the tenant store is\nuntouched, because a dispatch answering 4xx rolls its transaction back. A new\nroom holds no documents and is reachable by NOBODY until a share link is\ncreated over it — opening a room and granting access are two separate acts, so\na room cannot leak by existing.",
		Fields: map[string]string{
			"dataroomCreate.description": "Description is the room's description. Optional; any JSON scalar is\naccepted and stored as its text, and omitting it leaves the room with none.",
			"dataroomCreate.name":        "Name is the room's display name. Required, and a room without one is\nrefused with the data room's own validation error.",
			"dataroomRoom.createdAt":     "CreatedAt is when the room was created, in unix milliseconds.",
			"dataroomRoom.description":   "Description is the room's description, null when none was given.",
			"dataroomRoom.id":            "ID is the room id, which is what other dataroom calls address it by.",
			"dataroomRoom.name":          "Name is the room's display name.",
			"dataroomRoom.pId":           "PId is the room's short public identifier, unique within the tenant.",
			"dataroomRoom.updatedAt":     "UpdatedAt is when the room last changed, in unix milliseconds.",
			"dataroomRoomOne.dataroom":   "Dataroom is the room itself.",
		},
	})
	zip.Describe("POST /v1/dataroom/datarooms/:id/documents", zip.Doc{
		Description: "Puts an already-uploaded document into one of the caller\norg's data rooms and answers with the new membership id.\n\nIt ATTACHES, it never uploads: the bytes must already be stored, so the usual\norder is upload the document, then add it to the room. Both the room and the\ndocument must exist in the caller's own store — either missing is not found —\nand a document already in the room is refused as a conflict rather than\nduplicated.",
		Fields: map[string]string{
			"dataroomAddDocument.documentId":        "DocumentId is the document to attach. Required, and it must already exist\nin the caller's own store — this route attaches, it never uploads.",
			"dataroomAddDocument.id":                "ID is the room to add to. It is the path segment: the URL is the addressing\nauthority, and the org it is resolved in comes from the caller's principal,\nso an id from another tenant is simply not found.",
			"dataroomAddDocument.orderIndex":        "OrderIndex fixes the document's place in the viewer's list. Optional; a\nnumber or a numeric string is accepted, and anything that is not a number\nleaves the document unordered, which sorts it last.",
			"dataroomMembership.dataroomDocumentId": "DataroomDocumentId is the new membership id.",
			"dataroomMembership.dataroomId":         "DataroomId is the room the document was added to.",
			"dataroomMembership.documentId":         "DocumentId is the document that was added.",
		},
	})
	zip.Describe("POST /v1/dataroom/documents", zip.Doc{
		Description: "Stores the request body (the file bytes) on the object-storage\nseam, then records the metadata row via the bundle. The file is the raw request\nbody; ?name= names it, Content-Type carries the mime type, ?numPages= is optional.",
	})
	zip.Describe("POST /v1/dataroom/links", zip.Doc{
		Description: "Grants access: it mints a public share link over one data\nroom (`dataroomId`) or one document (`documentId`) — one of the two is\nrequired — and answers with the link, whose `id` is the token a visitor opens\nit with.\n\nThis is how a party is let in. The controls are declared HERE and enforced on\nthe viewer surface: `password` is hashed with bcrypt before storage and is\nnever readable back, `emailProtected` (on by default) makes a visitor state an\naddress, `allowList`/`denyList` narrow which addresses pass, `allowDownload`\n(off by default) governs downloads, and `expiresAt` closes the link. The target\nroom or document must exist in the caller's own store or it is not found.\n\nCreating a link also writes dataroom's ONE cross-tenant row: the link id to\nowning org mapping an anonymous visitor is routed through. That write is part\nof the operation — if it fails the call is 500 — so a link that no visitor\ncould open is never handed back as usable.\n\nThe address a visitor later states is recorded UNVERIFIED, so a link gated only\nby email is openable by anyone the link reaches. Use a password for a link that\nmust not travel.",
		Fields: map[string]string{
			"dataroomLink.allowDownload":        "AllowDownload is whether a visitor may download, rather than only view.",
			"dataroomLink.allowList":            "AllowList narrows which addresses pass the email gate. An entry may be a\nfull address, an \"@domain.com\" suffix, or a bare \"domain.com\". An EMPTY\nlist admits everyone.",
			"dataroomLink.createdAt":            "CreatedAt is when the link was minted, in unix milliseconds.",
			"dataroomLink.dataroomId":           "DataroomId is the room the link opens, null for a single-document link.",
			"dataroomLink.denyList":             "DenyList rejects addresses, in the same three forms as the allow list, and\nis checked BEFORE it — so deny always wins.",
			"dataroomLink.documentId":           "DocumentId is the document the link opens, null for a room link.",
			"dataroomLink.emailProtected":       "EmailProtected is whether a visitor must state an address to enter.",
			"dataroomLink.expiresAt":            "ExpiresAt is when the link closes, in unix milliseconds; null never expires.",
			"dataroomLink.hasPassword":          "HasPassword reports THAT a password is set. The stored form is a bcrypt\nhash and no route returns it.",
			"dataroomLink.id":                   "ID is the link id — the public token a visitor opens the room with.",
			"dataroomLink.isArchived":           "IsArchived is whether the link has been retired.",
			"dataroomLink.linkType":             "LinkType is DATAROOM_LINK or DOCUMENT_LINK.",
			"dataroomLink.name":                 "Name is the link's label, null when none was given.",
			"dataroomLink.updatedAt":            "UpdatedAt is when the link last changed, in unix milliseconds.",
			"dataroomLinkCreate.allowDownload":  "AllowDownload permits downloading rather than viewing only. Optional and\nOFF by default; any truthy JSON value turns it on.",
			"dataroomLinkCreate.allowList":      "AllowList narrows which addresses pass the email gate. Optional; an entry\nmay be a full address (\"ada@example.com\"), an \"@domain.com\" suffix, or a\nbare \"domain.com\". An omitted or EMPTY list admits everyone, so a link with\nno list enforces the email gate alone.",
			"dataroomLinkCreate.dataroomId":     "DataroomId is the room to share. One of dataroomId or documentId is\nrequired, and the target must exist in the caller's own store.",
			"dataroomLinkCreate.denyList":       "DenyList rejects addresses, in the same three forms as the allow list.\nOptional. It is checked BEFORE the allow list, so deny always wins.",
			"dataroomLinkCreate.documentId":     "DocumentId shares a SINGLE document instead of a room. One of dataroomId or\ndocumentId is required.",
			"dataroomLinkCreate.emailProtected": "EmailProtected makes a visitor state an address before entering. Optional\nand ON by default: it is disabled only by the JSON literal false, so any\nother value — including the string \"false\" — leaves the gate on.",
			"dataroomLinkCreate.expiresAt":      "ExpiresAt closes the link, in unix milliseconds. Optional; a number or a\nnumeric string is accepted, and omitting it means the link never expires.",
			"dataroomLinkCreate.name":           "Name labels the link. Optional; any JSON scalar is stored as its text.",
			"dataroomLinkCreate.password":       "Password gates the link. Optional; it is hashed with bcrypt before storage\nand is never readable back through any route.",
			"dataroomLinkOne.link":              "Link is the link itself, including the id a visitor opens it with.",
		},
	})
}
