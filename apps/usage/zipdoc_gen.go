// Code generated by zipdoc; DO NOT EDIT.

package usage

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/usage GET /v1/usage/analytics", zip.Doc{
		Description: "Is the entitlement-GATED per-provider breakdown of the caller org's LLM\nusage — the paid lens over the same warehouse ledger GET /v1/usage/summary reads\nits totals from. Basic own-org usage stays ungated at /v1/usage/summary.\n\nA plan that does not grant the analytics datastore is refused with 402, and an\nunresolvable plan fails closed to the free floor, which does not grant it. The\nwindow is clamped forward to the plan's retention entitlement, so a tenant can\nnever read older than its plan allows even with a custom start. The response is\nmarked no-store.\n\nINTERIM (mirrors apps/world's limits echo): no org→plan resolver exists in cloud\nyet — the subscription lookup is owned by the billing plane and the gateway\nprincipal carries no plan claim — so the caller passes the plan and the gate\nresolves THAT plan's access.",
		Fields: map[string]string{
			"ProviderBreakdown.available":      "Available is false when the warehouse could not be read, which means \"no\nanswer\" and NOT \"no usage\" — Items is then empty for a reason.",
			"ProviderBreakdown.items":          "Items is one row per provider, most tokens first.",
			"ProviderBreakdown.source":         "Source names the warehouse table the rows came from.",
			"ProviderRow.costCents":            "CostCents is what they cost the org, in US cents.",
			"ProviderRow.provider":             "Provider is the upstream the requests were routed to, e.g. anthropic.",
			"ProviderRow.requests":             "Requests is how many completions the org made against that provider.",
			"ProviderRow.tokens":               "Tokens is the total tokens those completions consumed, prompt plus\ncompletion.",
			"usageAnalyticsQuery.end":          "End is the exclusive window end, RFC3339. Read only when Range is custom.",
			"usageAnalyticsQuery.plan":         "Plan is the plan id whose entitlement decides access and retention. INTERIM:\ncloud has no org-to-plan resolver yet, so the caller names the plan; when\nthat resolver lands this becomes the caller org's own plan.",
			"usageAnalyticsQuery.range":        "Range is the window: a count and a unit — 24h, 7d, 90d, any <N>h or <N>d —\nor day, week, month, all, custom. Empty means 24h. The window is then\nclamped forward to the plan's retention entitlement.",
			"usageAnalyticsQuery.start":        "Start is the inclusive window start, RFC3339. Read only when Range is\ncustom, and clamped forward to the plan's retention floor.",
			"usageAnalyticsView.end":           "End is the window's exclusive end, RFC3339 UTC.",
			"usageAnalyticsView.export":        "Export is whether the resolved plan allows exporting these rows.",
			"usageAnalyticsView.plan":          "Plan echoes the plan id the entitlement was resolved from.",
			"usageAnalyticsView.providers":     "Providers is the per-provider roll-up over the window.",
			"usageAnalyticsView.range":         "Range is the label that was ASKED for. A plan whose retention is shorter\nthan that window is served the retention instead, so read start and end for\nthe window the rows actually cover and retentionDays for the reason — on a\nclamped read the label is longer than what was served.",
			"usageAnalyticsView.retentionDays": "RetentionDays is how far back the resolved plan allows reading.",
			"usageAnalyticsView.scope":         "Scope is the tenant the rows were read under — the validated principal's org.",
			"usageAnalyticsView.start":         "Start is the window's inclusive start, RFC3339 UTC, AFTER the retention\nclamp — so it may be later than the start that was asked for.",
			"usageScope.org":                   "Org is the IAM org slug the rows were read under — the validated\nprincipal's, never a client header.",
			"usageScope.user":                  "User is the caller's own subject, whose linked-account rows the accounts\nblock carries. Absent on a read that is org-scoped only.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/usage GET /v1/usage/analytics/access", zip.Doc{
		Description: "Echoes a plan's resolved analytics entitlement so a dashboard can\nconfigure itself against the LIVE catalog instead of hardcoding tier numbers. An\nempty plan resolves the free floor, and a catalog resolution failure serves that\nsame floor rather than erroring — so this always answers 200. It is a read-only\ncontract echo and carries no tenant data.",
		Fields: map[string]string{
			"usageAnalyticsAccess.access":       "Access is what that plan grants.",
			"usageAnalyticsAccess.plan":         "Plan echoes the plan id that was resolved, exactly as it was asked for.",
			"usageAnalyticsGrant.datastore":     "Datastore is whether the plan may read GET /v1/usage/analytics at all. The\nfree floor is false, and that is what a catalog outage resolves to.",
			"usageAnalyticsGrant.export":        "Export is whether the plan may export the analytics it can read.",
			"usageAnalyticsGrant.retentionDays": "RetentionDays is how far back the plan may read. GET /v1/usage/analytics\nclamps a custom window's start to this, so an older `start` returns the\nclamped window rather than an error.",
			"usagePlanQuery.plan":               "Plan is a plan id from the live @hanzo/plans catalog. Empty resolves the\nfree floor, and so does an id the catalog does not know — this never fails\non an unknown plan.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/usage GET /v1/usage/samples", zip.Doc{
		Description: "Is the PER-PROVIDER view: one connected account's own consumption of its\nown plan — \"my plan is 47% through its 6h window, resets at 14:20\".\n\n`current` is the newest instance of each lane (the headline); `windows` is the\nhistory behind it. Both come from ONE deduped read, so they can never disagree.\nThe rows are the caller's OWN linked accounts, scoped to the validated principal\nand its subject — never another user's, and never another org's.",
		Fields: map[string]string{
			"dashResp.account":                  "Account is the linked account that was asked about, when one was named.",
			"dashResp.available":                "Available is false when the warehouse could not be read. That means \"no\nanswer\", NOT \"no usage\" — the two lists below are then empty for a reason.",
			"dashResp.current":                  "Current is the newest window instance of each lane — the dash headline.",
			"dashResp.from":                     "From is the inclusive start of that window, RFC3339 UTC.",
			"dashResp.provider":                 "Provider is the upstream that was asked about, echoed back.",
			"dashResp.range":                    "Range is the window that was served: 1h, 24h, 7d or 30d.",
			"dashResp.scope":                    "Scope says whose rows these are: the caller's own linked accounts.",
			"dashResp.source":                   "Source names the meter of record — the provider's own login, not Hanzo.",
			"dashResp.to":                       "To is the exclusive end of that window, RFC3339 UTC.",
			"dashResp.windows":                  "Windows is every instance in range, newest first — the history behind it.",
			"usageSamplesQuery.account":         "Account narrows to ONE linked account of that provider. Empty covers every\naccount the caller has linked there.",
			"usageSamplesQuery.provider":        "Provider is the upstream to read, e.g. anthropic. Required.",
			"usageSamplesQuery.range":           "Range is the window to read: a count and a unit — 1h, 24h, 90d, any <N>h or\n<N>d — or day, week, month, all. Empty means 24h. A label that is not a\ncount, or one reaching past the 730-day horizon, is refused rather than\nsilently replaced.",
			"usageSamplesQuery.window":          "Window narrows to ONE window class: 6h, day, week or month. Empty covers\nevery class.",
			"usageWindowView.account":           "Account is the linked provider account the window belongs to.",
			"usageWindowView.cachedInputTokens": "CachedInputTokens is the prompt tokens served from the provider's cache;\nomitted when unknown.",
			"usageWindowView.confidence":        "Confidence says how much the counters beside it mean — a meter that\nreported only a percentage leaves them at zero, and this is how a reader\ntells that from a true zero.",
			"usageWindowView.costCents":         "CostCents is what the window cost on the PROVIDER's own plan, in US cents.\nIt is not a Hanzo charge.",
			"usageWindowView.costLimitCents":    "CostLimitCents is the plan's spend ceiling for the window, in US cents.",
			"usageWindowView.currency":          "Currency is the provider's currency when it is not US cents.",
			"usageWindowView.inputTokens":       "InputTokens is prompt tokens consumed in the window; omitted when unknown.",
			"usageWindowView.lane":              "Lane is the meter lane this instance belongs to, e.g. a provider's own\nrolling-window meter.",
			"usageWindowView.machine":           "Machine is the host whose meter reported the window.",
			"usageWindowView.outputTokens":      "OutputTokens is completion tokens produced in the window; omitted when\nunknown.",
			"usageWindowView.plan":              "Plan is the subscription plan the account is on, as the provider names it.",
			"usageWindowView.requests":          "Requests is how many requests were made in the window; omitted when the\nmeter did not report it.",
			"usageWindowView.resetsAt":          "ResetsAt is when this window rolls over, RFC3339 UTC; omitted when unknown.",
			"usageWindowView.synthetic":         "Synthetic marks an instance the meter inferred rather than read.",
			"usageWindowView.totalTokens":       "TotalTokens is the window's total tokens; omitted when unknown.",
			"usageWindowView.usedPct":           "UsedPct is how much of the window's allowance is consumed, 0–100.",
			"usageWindowView.window":            "Window is the window class: 6h, day, week or month.",
			"usageWindowView.windowMinutes":     "WindowMinutes is the window's real length in minutes when the meter\nreported one; omitted when it did not.",
			"usageWindowView.windowStart":       "WindowStart is when this window opened, RFC3339 UTC; omitted when unknown.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/usage GET /v1/usage/summary", zip.Doc{
		Description: "Answers GET /v1/usage/summary: the caller's own usage footprint over one\nwindow — the categorized spend roll-up from the commerce ledger, the org's LLM\nusage totals from the warehouse, and the caller's OWN linked provider accounts\nbeside the org's Hanzo-routed usage.\n\nEvery source degrades INDEPENDENTLY to honest zeros and says so in `sources` and\nin its own `available` flag, so a partial deploy reports \"no data\" rather than\nfabricating spend. The account rows and the Hanzo rows are concatenated and never\nsummed: a plan's percent is not money.\n\nThe response is org-scoped from the validated principal and marked no-store — a\nsigned-out caller is refused.",
		Fields: map[string]string{
			"Accounts.account":       "Account is the state of the caller's own linked-account side.",
			"Accounts.hanzo":         "Hanzo is the state of the org's Hanzo-routed side.",
			"Accounts.rows":          "Rows is the two row sets CONCATENATED, never summed — each row says which\nside it came from. A percent is not money and a provider's own spend is not\na Hanzo charge, so adding them would produce a number that means nothing.",
			"CategorySpend.category": "Category is the bucket the ledger's own tag mapped to. An untagged or\nunrecognised line gets its own honest bucket rather than being folded away.",
			"CategorySpend.cents":    "Cents is what the org spent in that bucket over the window, in US cents.",
			"CategorySpend.count":    "Count is how many ledger lines rolled up into it.",
			"LLM.available":          "Available is false when the warehouse was not connected or a query blipped.\nThe totals below are then honest zeros, NOT measured ones.",
			"LLM.completionTokens":   "CompletionTokens is the output half.",
			"LLM.costCents":          "CostCents is what they cost the org, in US cents. This IS a Hanzo charge.",
			"LLM.models":             "Models is how many distinct models were used.",
			"LLM.promptTokens":       "PromptTokens is the input half of that total.",
			"LLM.requests":           "Requests is how many completions the org made in the window.",
			"LLM.source":             "Source names the warehouse table the totals came from.",
			"LLM.tokens":             "Tokens is the total tokens those completions consumed.",
			"SourceState.available":  "Available is whether this side's ledger answered. False means its rows are\nmissing, not that there were none.",
			"SourceState.note":       "Note is the human sentence that says what this side's numbers mean, so a\nboard cannot present a plan percentage as a Hanzo charge.",
			"SourceState.scope":      "Scope is whose rows this side carries: \"user\" or \"org\".",
			"SourceState.source":     "Source is the table of record the rows came from.",
			"Sources.commerce":       "Commerce is whether the billing ledger answered the spend block.",
			"Sources.warehouse":      "Warehouse is whether the usage warehouse answered the LLM block.",
			"Spend.available":        "Available is false when the commerce ledger was unconfigured or\nunreachable. Every number below is then an honest zero, NOT a measured one.",
			"Spend.availableCents":   "AvailableCents is what of that balance is still spendable.",
			"Spend.balanceCents":     "BalanceCents is the prepaid wallet's balance, in US cents.",
			"Spend.byCategory":       "ByCategory is the window's spend split by ledger category, largest first.",
			"Spend.mtdCents":         "MTDCents is commerce's authoritative month-to-date consumed figure, which\nis a different period from the window and is not derived from it.",
			"Spend.overageCents":     "OverageCents is month-to-date consumption beyond the plan's allowance.",
			"Spend.series":           "Series is the window's spend over time, gap-filled at the window's\ninterval.",
			"Spend.source":           "Source names where the roll-up came from.",
			"Spend.totalCents":       "TotalCents is consumption over the requested window, in US cents. It is\nself-consistent with ByCategory and Series.",
			"SpendPoint.cents":       "Cents is the consumption recorded in that bucket, in US cents.",
			"SpendPoint.t":           "T is the bucket's start instant, RFC3339 UTC. Buckets are gap-filled, so a\nwindow with no spend still has its points.",
			"TotalView.confidence":   "Confidence says how much the counters mean; a percentage-only meter leaves\nthem at zero.",
			"TotalView.costCents":    "CostCents is the row's cost in US cents. For an \"account\" row this is the\nPROVIDER's own charge, not a Hanzo one.",
			"TotalView.provider":     "Provider is the upstream the usage was measured against.",
			"TotalView.requests":     "Requests is how many requests the row covers.",
			"TotalView.scope":        "Scope is whose row it is: \"user\" for the caller's own linked accounts,\n\"org\" for the whole tenant's Hanzo-routed usage.",
			"TotalView.source":       "Source is where the row came from: \"account\" is the provider's own meter\non the caller's linked account, \"hanzo\" is Hanzo-routed inference. The two\nare never summed.",
			"TotalView.tokens":       "Tokens is the total tokens the row covers.",
			"TotalView.usedPct":      "UsedPct is how much of a plan window the row consumed, 0–100. It is a\nshare, never money.",
			"TotalView.window":       "Window is the meter window class the row rolls up, when it has one.",
			"TotalView.windows":      "Windows is how many window instances rolled up into the row.",
			"usageScope.org":         "Org is the IAM org slug the rows were read under — the validated\nprincipal's, never a client header.",
			"usageScope.user":        "User is the caller's own subject, whose linked-account rows the accounts\nblock carries. Absent on a read that is org-scoped only.",
			"usageSummary.accounts":  "Accounts is the caller's own linked provider accounts beside the org's\nHanzo-routed usage, labelled row by row and never summed together.",
			"usageSummary.end":       "End is the window's exclusive end, RFC3339 UTC.",
			"usageSummary.interval":  "Interval is the bucket width the spend series is gap-filled at.",
			"usageSummary.llm":       "LLM is the org's Hanzo-routed inference totals from the warehouse.",
			"usageSummary.range":     "Range is the window label that was served.",
			"usageSummary.scope":     "Scope is the tenant and subject the roll-up was answered for.",
			"usageSummary.sources":   "Sources says which upstreams actually answered, so a zero can be read as\n\"no data yet\" rather than as a measurement.",
			"usageSummary.spend":     "Spend is the categorized cost roll-up from the billing ledger.",
			"usageSummary.start":     "Start is the window's inclusive start, RFC3339 UTC.",
			"usageWindowQuery.end":   "End is the exclusive window end, RFC3339. Read only when Range is custom.",
			"usageWindowQuery.range": "Range is the window: a count and a unit — 24h, 7d, 90d, any <N>h or <N>d —\nor day, week, month, all, custom. Empty means 24h. A label this surface\ndoes not know, or one reaching past the 730-day horizon, is refused rather\nthan silently replaced.",
			"usageWindowQuery.start": "Start is the inclusive window start, RFC3339. Read only when Range is\ncustom.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/usage POST /v1/usage", zip.Doc{
		Description: "Ingests a batch of account-usage samples — what a developer's OWN AI\naccounts have consumed of their OWN plans, metered from each provider's own\nlogin — and appends them to the warehouse series. Answers 202.\n\nSend either a `samples` array or one sample's fields at the top level. Every\nsample needs a provider, a machine and a known window class; an unknown window or\nkind is refused rather than silently rewritten, because a dash filled with a class\nnobody reported is worse than an error. There is no timestamp field: the server\nowns the observation clock, and a sample says which window it measured with\nwindowStart or resetsAt.\n\nIt is FAIL-SOFT on storage: a warehouse outage costs a poll of history\n(stored:false), never a failed request. It records usage ONLY — the link registry\nis refreshed separately via POST /v1/link, so there is one and only one way to\nupdate an account row.",
		Fields: map[string]string{
			"reportReq.account":           "Account is the linked account the window was metered from.",
			"reportReq.cachedInputTokens": "CachedInputTokens is the prompt tokens the provider served from cache.",
			"reportReq.confidence":        "Confidence says how much the counters below mean.",
			"reportReq.costCents":         "CostCents is what the window cost on the PROVIDER's own plan, in US cents.",
			"reportReq.costLimitCents":    "CostLimitCents is the plan's spend ceiling for the window, in US cents.",
			"reportReq.currency":          "Currency is the provider's currency when it is not US cents.",
			"reportReq.inputTokens":       "InputTokens is prompt tokens consumed in the window.",
			"reportReq.kind":              "Kind is subscription or apikey. Empty is accepted; anything else is\nrefused.",
			"reportReq.lane":              "Lane is the meter lane within the account.",
			"reportReq.machine":           "Machine is the host whose meter read the window. Required on every sample.",
			"reportReq.outputTokens":      "OutputTokens is completion tokens produced in the window.",
			"reportReq.plan":              "Plan is the subscription plan the account is on, as the provider names it.",
			"reportReq.provider":          "Provider is the upstream the account belongs to, e.g. anthropic. Required\non every sample.",
			"reportReq.requests":          "Requests is how many requests the window covers.",
			"reportReq.resetsAt":          "ResetsAt is when the measured window rolls over, RFC3339.",
			"reportReq.samples":           "Samples is the batch form: every lane a poller measured, in one call. When\nit is non-empty the top-level sample fields are ignored.",
			"reportReq.synthetic":         "Synthetic marks a window the meter inferred rather than read.",
			"reportReq.totalTokens":       "TotalTokens is the window's total tokens.",
			"reportReq.usedPct":           "UsedPct is how much of the window's allowance is consumed, 0–100.",
			"reportReq.window":            "Window is the window class: 6h, day, week or month. Required, and a class\nthis surface does not know is refused rather than rewritten.",
			"reportReq.windowMinutes":     "WindowMinutes is the window's real length in minutes, as the meter reports\nit.",
			"reportReq.windowStart":       "WindowStart is when the measured window opened, RFC3339. This is how a\nbackfill states WHICH window it measured; the server always owns the\nobservation clock, so there is no timestamp field.",
			"reportResp.accepted":         "Accepted is how many samples passed validation. Every one of them was\naccepted, or the whole report was refused — there is no partial success.",
			"reportResp.stored":           "Stored is whether the warehouse actually persisted them. False means the\ndatastore was unavailable and the poll of history was lost; the request\nstill succeeded, so a device retries without being blocked.",
			"sampleReq.account":           "Account is the linked account the window was metered from.",
			"sampleReq.cachedInputTokens": "CachedInputTokens is the prompt tokens the provider served from cache.",
			"sampleReq.confidence":        "Confidence says how much the counters below mean.",
			"sampleReq.costCents":         "CostCents is what the window cost on the PROVIDER's own plan, in US cents.",
			"sampleReq.costLimitCents":    "CostLimitCents is the plan's spend ceiling for the window, in US cents.",
			"sampleReq.currency":          "Currency is the provider's currency when it is not US cents.",
			"sampleReq.inputTokens":       "InputTokens is prompt tokens consumed in the window.",
			"sampleReq.kind":              "Kind is subscription or apikey. Empty is accepted; anything else is\nrefused.",
			"sampleReq.lane":              "Lane is the meter lane within the account.",
			"sampleReq.machine":           "Machine is the host whose meter read the window. Required.",
			"sampleReq.outputTokens":      "OutputTokens is completion tokens produced in the window.",
			"sampleReq.plan":              "Plan is the subscription plan the account is on, as the provider names it.",
			"sampleReq.provider":          "Provider is the upstream the account belongs to, e.g. anthropic. Required.",
			"sampleReq.requests":          "Requests is how many requests the window covers.",
			"sampleReq.resetsAt":          "ResetsAt is when the measured window rolls over, RFC3339. Empty is\nallowed; anything else that is not RFC3339 is refused.",
			"sampleReq.synthetic":         "Synthetic marks a window the meter inferred rather than read.",
			"sampleReq.totalTokens":       "TotalTokens is the window's total tokens.",
			"sampleReq.usedPct":           "UsedPct is how much of the window's allowance is consumed, 0–100.",
			"sampleReq.window":            "Window is the window class: 6h, day, week or month. Required, and a class\nthis surface does not know is refused rather than rewritten.",
			"sampleReq.windowMinutes":     "WindowMinutes is the window's real length in minutes, as the meter reports\nit.",
			"sampleReq.windowStart":       "WindowStart is when the measured window opened, RFC3339. Empty is allowed;\nanything else that is not RFC3339 is refused.",
		},
	})
}
