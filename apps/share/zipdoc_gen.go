// Code generated by zipdoc; DO NOT EDIT.

package share

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/share", zip.Doc{
		Description: "listShares returns the caller org's active shares.\n\nA READ degrades to an honest-empty list when the org has no share account yet\nor the controller is unreachable, so the console never error-toasts on load.",
		Fields: map[string]string{
			"Share.backend":     "Backend is the local endpoint the share proxies to.",
			"Share.backendMode": "BackendMode is how the share serves its backend, e.g. proxy or web.",
			"Share.createdAt":   "CreatedAt is the share's creation time, unix seconds.",
			"Share.token":       "Token is the share's zrok token, the key in its public URL.",
			"Share.url":         "URL is the share's public URL.",
			"ShareList.shares":  "Shares is one row per active share across the org's environments.",
		},
		Response: json.RawMessage(`{"shares":[{"token":"abc123","url":"https://abc123.share.hanzo.ai","backendMode":"proxy","backend":"http://localhost:3000","createdAt":1780000000}]}`),
	})
	zip.Describe("POST /v1/share/enable", zip.Doc{
		Description: "enable provisions the caller org's share account and returns its credential.\n\nProvisioning is idempotent and the account is keyed deterministically off the\norg, so this is a pure function of the validated identity — a caller can only\never provision their OWN org's account.",
		Fields: map[string]string{
			"Credential.accountToken": "AccountToken is the org's zrok account token the CLI enables with.",
			"Credential.controller":   "Controller is the public zrok controller endpoint to enable against.",
			"Credential.namespace":    "Namespace is the public frontend the share is published under.",
			"Credential.urlTemplate":  "URLTemplate is the share URL shape, with the token placeholder left in.",
		},
		Response: json.RawMessage(`{"accountToken":"tok_live","controller":"https://share.hanzo.ai","namespace":"public","urlTemplate":"https://{token}.share.hanzo.ai"}`),
	})
}
