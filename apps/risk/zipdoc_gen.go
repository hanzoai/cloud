// Code generated by zipdoc; DO NOT EDIT.

package risk

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/risk/features", zip.Doc{
		Description: "Features is the feature catalogue in its two honest lenses.\n\nThe MODEL lens is the governed inventory: one entry per dimension of the model\nspace, each carrying the typology it serves, the supervisor's own words for the\nindicator, and the published standard those words come from — so a coverage\nclaim is checkable rather than asserted. It is the same for every organisation.\n\nThe SURFACE lens is what THIS organisation's own event surface actually carries,\nmeasured over the window: how many of its buckets carry each dimension at all,\nand what the dimension reads where it is present. A dimension present in no\nbucket is BLIND, and saying so is the difference between no risk and no data.",
		Fields: map[string]string{
			"riskBand.day":               "Day is the day the band covers.",
			"riskBand.dim":               "Dim is the dimension, named as this API publishes it.",
			"riskBand.kind":              "Kind is the subject kind it was computed over.",
			"riskBand.n":                 "N is how many subject-days went into it.",
			"riskBand.orgs":              "Orgs is how many organisations contributed, each weighted exactly one vote\nwhatever its size. It is published so a reader can judge the band rather\nthan trust it.",
			"riskBand.q10":               "Q10 is the quiet end of the network's day: a tenth of contributing\norganisations sit at or below it.",
			"riskBand.q50":               "Q50 is the network's median day.",
			"riskBand.q90":               "Q90 is the busy end: a tenth of contributing organisations sit at or above\nit. It is the highest level published.",
			"riskCatalog.gap":            "Gap says why a lens could not be measured, when that is the case. Each\nreason names its own lens, because \"the surface is unreadable\" and \"the\nnetwork baseline is unreadable\" are different facts.",
			"riskCatalog.model":          "Model is the governed inventory: one entry per dimension of the model\nspace, each carrying the typology it serves and the published standard that\nasks for it. It is the same for every organisation, because it is the\nmodel's shape.",
			"riskCatalog.network":        "Network is the published cross-organisation baseline over the same window,\nso the surface above has something to be read AGAINST. It is the same for\nevery caller and it names nobody.\n\nIt carries no tenant and cannot be made to: the table it reads has no org\ncolumn, every figure is a quantile over at least kAnonOrgs organisations\nweighted one vote each, and a band that does not meet that floor is dropped\non the way out.",
			"riskCatalog.surface":        "Surface is what this organisation's own event surface carries, per\ndimension, measured over the window. A dimension present in no bucket is\nblind here — the model reads its neutral value and a reviewer has to be able\nto see that.",
			"riskCatalog.tenant":         "Tenant is whose surface was measured.",
			"riskCatalogIn.days":         "Days is how far back to measure the organisation's own coverage, 1 to 400.\nZero takes thirty.",
			"riskModelFeature.blind":     "Blind is how often this dimension took that neutral value for THIS\norganisation.",
			"riskModelFeature.citation":  "Citation is where those words come from, so the claim is checkable rather\nthan asserted.",
			"riskModelFeature.indicator": "Indicator is the supervisor's own words for the thing being looked for.",
			"riskModelFeature.name":      "Name is the dimension.",
			"riskModelFeature.neutral":   "Neutral is the value the coordinate takes when the data cannot support it.",
			"riskModelFeature.severity":  "Severity is how much weight an alert on it carries.",
			"riskModelFeature.typology":  "Typology is the pattern this dimension detects.",
			"riskModelFeature.unit":      "Unit is how to read the raw number, which is what turns a coordinate into a\nsentence an investigator can put in a file.",
			"riskModelFeature.window":    "Window is the sliding aggregate it reads.",
			"riskOrgFeature.blind":       "Blind is true when the dimension is present in no bucket at all: this\norganisation's surface does not carry it, and saying so is the difference\nbetween no risk and no data.",
			"riskOrgFeature.buckets":     "Buckets is how many five-minute buckets of this organisation's surface were\nmeasured.",
			"riskOrgFeature.max":         "Max is the largest value it reached in the window.",
			"riskOrgFeature.mean":        "Mean is the dimension's average where it was present.",
			"riskOrgFeature.name":        "Name is the dimension as this API publishes it.",
			"riskOrgFeature.present":     "Present is in how many of them the dimension carried a value at all.",
			"riskOrgFeature.source":      "Source names the plane it is rolled up from, so a dimension that reads zero\neverywhere traces to a plane the organisation does not use rather than to a\ndefect.",
			"riskOrgFeature.unit":        "Unit is how to read the numbers below.",
		},
		Example: json.RawMessage(`{"days":30}`),
	})
	zip.Describe("GET /v1/risk/health", zip.Doc{
		Description: "Is a REAL probe: it reports whether the model plane exists, whether the\nper-org shelves can be written, and whether the event surface behind the\nfeature plane is reachable. 200 only when the model plane can actually work.\n\nThe warehouse being DOWN is reported and is NOT a failure: scoring reads\nin-memory rings and never the warehouse, so a warm that cannot run degrades the\nmoat and does not stop a decision. Saying so is the difference between a probe\nand status theatre.",
	})
	zip.Describe("GET /v1/risk/policy", zip.Doc{
		Description: "Policy reports the caller organisation's own decision-regime history: every\ndistinct regime it has adopted, which version is in force, and what retention\nhas taken.\n\nWHY IT EXISTS. Every score cites the version it was decided under\n([riskScoreOut.Policy]), and the threshold that score was measured against is\nderived from the appetite that version states. Restate the appetite and, without\nthis record, every earlier decision becomes unreconstructible — the cut it was\njudged by no longer exists anywhere. An adverse decision that cannot be\nexplained against the policy in force when it was taken cannot be defended.\n\nIt covers ONE organisation. The history is on that organisation's own shelf, so\nanother's versions are not filtered out of the answer — they are not in the file\nthe answer is read from.",
		Fields: map[string]string{
			"riskPolicyOut.changes":     "Changes is how many DISTINCT regimes may be adopted per Window. A restatement\nidentical to the regime in force mints no version and is not counted against\nit.",
			"riskPolicyOut.disposed":    "Disposed is how many versions retention has taken. It is NOT a silence: a\nhistory bounded on disk must say what it no longer holds, because a decision\nciting a disposed version can no longer be reconstructed from this record.",
			"riskPolicyOut.history":     "History is the retained versions, newest first.",
			"riskPolicyOut.retained":    "Retained is how many versions this organisation's history holds at most,\nderived from the byte budget its rows are a multiple of.",
			"riskPolicyOut.version":     "Version is the version in force — the one every score currently cites. Zero\nmeans no regime has ever been stated and the default posture, shadow, is in\nforce.",
			"riskPolicyOut.window":      "Window is the period Changes is measured over.",
			"riskPolicyVersion.at":      "At is when it entered force, RFC 3339, from the server clock.",
			"riskPolicyVersion.by":      "By is the identity that stated it, stamped server-side from the validated\nprincipal at the moment it entered force.",
			"riskPolicyVersion.live":    "Live is whether the model was permitted to change an outcome under it.",
			"riskPolicyVersion.review":  "Review is the share of the stream the regime states may be examined. The\nthreshold in force is derived from it, which is why a decision is only\ndefensible against the version that produced it.",
			"riskPolicyVersion.sample":  "Sample is the share of below-the-line events the regime retains for review.",
			"riskPolicyVersion.version": "Version names this regime in this organisation's history.",
		},
	})
	zip.Describe("GET /v1/risk/search/:id", zip.Doc{
		Description: "Reads back one search run: every shape tried over this\norganisation's own history, best first, and the one that fit.\n\nA run another organisation started is simply not there — the same 404 an\nunknown id gives, so the read is not a probe oracle.",
		Fields: map[string]string{
			"riskModelValue.address":   "Address names this value by its own content: the model's shape, the geometry\nseed, its position in the window, its threshold, its masses as IEEE-754 bits\nand the fold watermark behind them. Nothing else — no clock, no counter and\ndeliberately NOT the organisation, so an identical model has one name and a\nname is never an authority. Holding another organisation's address resolves\nnothing.",
			"riskModelValue.at":        "At is when it was published, RFC 3339, on the server clock. You do not supply\nit: a record whose date the audited party chose is not a record.",
			"riskModelValue.learned":   "Learned is how many events are behind the masses.",
			"riskModelValue.sequence":  "Sequence is this value's place in YOUR organisation's own history, from 1 and\ncontiguous until retention disposes of the oldest.",
			"riskModelValue.shape":     "Shape NAMES the model space the masses are only meaningful against, as\n`<family>:<digest>` — the KIND of model, and that family's own digest over the\nfeature inventory in order and the detector's geometry parameters. Compare it\nwith the `shape` on your model state (GET /v1/risk/state): equal means adopting\nthis value restores masses into the space already running, and different means\nadopting it REPLANTS the model into the space this value describes. That is what\nmakes a searched shape installable.\n\nA DIFFERENT FAMILY IS NOT ADOPTABLE AT ALL, and that is the one difference the\nfamily term makes here: a different geometry in the same family is a replant, and\na different family is a refusal naming both — its masses do not describe your\nmodel in any space.",
			"riskModelValue.warmed":    "Warmed is how far your own event surface had been folded in when this value\nwas published, RFC 3339. It is part of the address because two models with\nidentical masses reached by different routes disagree about what is left to\nfold, and one of them will re-teach history the other will not.",
			"riskRunRef.id":            "ID is the run, taken from the path. A run another organisation started is\nsimply not there — the same answer an unknown id gives.",
			"riskSearchReport.done":    "Done is false while the run is still going; the trials below are then the\nones finished so far.",
			"riskSearchReport.ended":   "Ended is when it finished, RFC 3339. Absent while it is still going.",
			"riskSearchReport.events":  "Events is how much of this organisation's history was replayed.",
			"riskSearchReport.fitted":  "Fitted is the winning shape FITTED over your own history and published as one of\nyour organisation's own model values. Name its address on PUT\n/v1/risk/state/model and the winning shape becomes the model you are running.\n\nIt is why this op answers something you can act on. A trial keeps counts and not\nthe model that produced them, so a report without this named a shape nobody could\ninstall — and the adoption path refused a shape change besides. Fitting the winner\nonce is a sixty-fifth pass over the same history; keeping all sixty-four fitted\nmodels resident instead would cost a measured 21 MiB per run for sixty-three\nshapes nobody adopts.\n\nTwo things about it are worth knowing before you adopt it. Its realised rate can\ndiffer from the winner's above, because the ranking measures every candidate under\none fixed reference geometry so the comparison is a comparison, while this is\nfitted under YOUR geometry — the one an outsider cannot predict. And it has\nlearned the window this search replayed and nothing older, so adopting it trades\nhistory for fit.",
			"riskSearchReport.gap":     "Gap says why the winning shape could not be fitted into an adoptable value, when\nit could not. It is separate from Refusal because they are different facts: a\nrefusal means the ranking below proves nothing, a gap means the ranking stands and\nonly the value is missing.",
			"riskSearchReport.id":      "ID is the run.",
			"riskSearchReport.refusal": "Refusal says why the run proves nothing, when it does. An empty history is\nREFUSED rather than reported as zero alerts: \"no alerts\" is exactly what a\nquiet model looks like, and choosing a shape on the strength of an empty\nreplay is the failure a sandbox exists to prevent.",
			"riskSearchReport.started": "Started is when the run was accepted, RFC 3339.",
			"riskSearchReport.trials":  "Trials is every shape tried, best first.",
			"riskSearchReport.winner":  "Winner is the best-fitting shape, absent when nothing fit.",
			"riskTopology.blend":       "Blend is how much of a closing window folds into the reference: 1 replaces\nit outright, less makes the reference expensive to move.",
			"riskTopology.depth":       "Depth is how deep each tree is. With Trees it sets how finely the space is\npartitioned, and therefore how much history it takes to fill.",
			"riskTopology.family":      "Family is the KIND of model this candidate is: `halfspace` is an ensemble of\nhalf-space trees whose masses are counters, and it is the family this search\ngrid ranks. The parameters below are that family's own — a family that does not\npartition space with trees has different ones — so read them against this.",
			"riskTopology.review":      "Review is the appetite this shape was tried at.",
			"riskTopology.trees":       "Trees is how many half-space trees the ensemble holds.",
			"riskTopology.window":      "Window is how many events make one reference window.",
			"riskTrial.alerted":        "Alerted is how many of those it would have raised.",
			"riskTrial.curve":          "Curve is the realised alert rate over successive tenths of the history —\nthe learning curve, which says whether the shape settled or is still moving.",
			"riskTrial.fit":            "Fit ranks the shape, smaller being better: the relative miss of the stated\nappetite, plus flat penalties for never warming and for saturating, plus the\nshare of coordinates that were blind.",
			"riskTrial.learned":        "Learned is how many events the shape learned from during the replay.",
			"riskTrial.realised":       "Realised is what that appetite actually produced. The distance between the\ntwo is what the search is searching over.",
			"riskTrial.saturated":      "Saturated is whether the appetite could not be honoured by any threshold,\nwhich is a shape that alerts on nothing and reads like a quiet one.",
			"riskTrial.scored":         "Scored is how many it was able to score.",
			"riskTrial.stated":         "Stated is the appetite the shape was tried at.",
			"riskTrial.topology":       "Topology is the shape.",
			"riskTrial.warm":           "Warm is whether the shape learned enough to have an opinion at all over\nthis organisation's whole history.",
		},
		Example: json.RawMessage(`{"id":"srch_2f6a1c"}`),
	})
	zip.Describe("GET /v1/risk/state", zip.Doc{
		Description: "State reports the caller organisation's own model: what it has learned, whether\nit is live or still in shadow, the threshold in force, the appetite it stated\nbeside the share it actually realised, every refusal by reason, every feature\nthat read blind, and how much of the organisation's own event surface has been\nfolded in.\n\nIt covers ONE organisation. A caller cannot learn another's volumes, alert rate\nor behaviour from it, because the state is read out of a model that holds only\nits own.",
		Fields: map[string]string{
			"riskAggregates.bound":      "Bound is the most they can hold. It is a per-organisation bound: at it, this\norganisation degrades and no other one notices.",
			"riskAggregates.forgotten":  "Forgotten is how many of its own subjects have been dropped to stay inside\nthat bound. Each one reads as inactive until it is active again.",
			"riskAggregates.saturated":  "Saturated is whether the bound is binding right now. The two counts are its\nevidence; this is the state to act on.",
			"riskAggregates.subjects":   "Subjects is how many of this organisation's subjects the aggregates hold.",
			"riskModelState.aggregates": "Aggregates reports the pressure on this organisation's own sliding\naggregates, and whether they have started forgetting subjects to stay inside\ntheir bound.",
			"riskModelState.blind":      "Blind counts, per feature, how often it took its neutral value for want of\ndata. A feature blind on most traffic is not contributing whatever the\ninventory claims for it.",
			"riskModelState.cut":        "Cut is the threshold in force, derived from Stated as a quantile of the\nscores actually observed.",
			"riskModelState.descends":   "Descends is the published value the working model grew out of: the newest one\nwhose mass count it has reached or passed. Empty when nothing has been\npublished yet.\n\nIt is DERIVED from the count and never stored, so an instant rollback is right\nfor free — adopting an older value moves the count backward and this answers\nwith that older value, where a stored pointer would be a second fact to keep\nin step. Read with Learned it is also the DRIFT: this model is Descends plus\nhowever many events the two counts differ by.",
			"riskModelState.disposed":   "Disposed is how many published values retention has taken. It is DERIVED from\nthe lowest surviving sequence, so it cannot drift from what it describes, and\nit is reported because a retention that binds is a fact an operator must be\nable to read rather than a silence.",
			"riskModelState.learned":    "Learned is how many events the model has learned from.",
			"riskModelState.live":       "Live is false while the model is in shadow — scoring, learning and\nrecording what it WOULD have alerted on, and changing no outcome. Shadow is\nthe default for a new tenant.",
			"riskModelState.policy":     "Policy is the version of the decision regime this model is deciding under,\nfrom your organisation's own policy history (GET /v1/risk/policy). Every\nscore cites it, so it is the join between a past decision and the appetite\nthat produced its threshold. Zero means no regime has ever been stated and\nthe default posture — shadow — is in force.",
			"riskModelState.realised":   "Realised is the share that actually was. Reading it beside Stated is what\nmakes the appetite a measured commitment rather than an intention.",
			"riskModelState.refused":    "Refused counts events the model would not score, by reason. None of them\nwas examined; a refusal is counted, never silent.",
			"riskModelState.sample":     "Sample is the share of below-the-line events retained for review, which is\nhow the miss rate is measured rather than assumed.",
			"riskModelState.saturated":  "Saturated means no threshold can honour the stated appetite because too\nmuch of the stream scores in the top bucket, so the model is alerting on\nnothing — the one state that must never be mistaken for quiet.",
			"riskModelState.shape":      "Shape is the model's identity, as `<family>:<digest>`: the KIND of model, and\nthat family's own digest over the inventory in order and the detector's geometry\nparameters. It is what an auditor pins an alert to, because learned state is\nonly meaningful against the space that produced it — and the family leads it\nbecause two families' masses are not fitted differently, they are different\nkinds of number.",
			"riskModelState.stated":     "Stated is the share of the stream this organisation said may be examined.",
			"riskModelState.surface":    "Surface reports what of the tenant's OWN event surface has been folded in.",
			"riskModelState.tenant":     "Tenant is the qualified key the model is held under — the brand whose\nissuer vouched for the caller and the organisation it acts for. It is\nechoed so a reader can see the answer is its own and not a parameter it\npassed.",
			"riskModelState.values":     "Values is your organisation's own published model values, newest first —\nevery state it deliberately named, each addressed by its own content and\nimmutable. This is what PUT /v1/risk/state/model names, so it is reported\nHERE rather than behind an address of its own: they are part of what a review\nof one model reads, and a list of names is a few hundred bytes.\n\nCompare each one's `shape` with the `shape` above: equal means adopting it\nrestores masses into the space this model already runs, and different means\nadopting it REPLANTS the model into the space that value describes — which is how\nthe shape a search found becomes the shape you are running.\n\nThe working model is NOT in it. Publication is a boundary somebody marked; the\nstate between two boundaries is in-process counters, and calling those a value\nwould be a claim about reproducibility that nothing could honour.",
			"riskModelState.warm":       "Warm is whether that is enough for the model to have an opinion at all.\nBelow it the model declines to score, which is an ordinary state and is not\na clean bill of health.",
			"riskModelValue.address":    "Address names this value by its own content: the model's shape, the geometry\nseed, its position in the window, its threshold, its masses as IEEE-754 bits\nand the fold watermark behind them. Nothing else — no clock, no counter and\ndeliberately NOT the organisation, so an identical model has one name and a\nname is never an authority. Holding another organisation's address resolves\nnothing.",
			"riskModelValue.at":         "At is when it was published, RFC 3339, on the server clock. You do not supply\nit: a record whose date the audited party chose is not a record.",
			"riskModelValue.learned":    "Learned is how many events are behind the masses.",
			"riskModelValue.sequence":   "Sequence is this value's place in YOUR organisation's own history, from 1 and\ncontiguous until retention disposes of the oldest.",
			"riskModelValue.shape":      "Shape NAMES the model space the masses are only meaningful against, as\n`<family>:<digest>` — the KIND of model, and that family's own digest over the\nfeature inventory in order and the detector's geometry parameters. Compare it\nwith the `shape` on your model state (GET /v1/risk/state): equal means adopting\nthis value restores masses into the space already running, and different means\nadopting it REPLANTS the model into the space this value describes. That is what\nmakes a searched shape installable.\n\nA DIFFERENT FAMILY IS NOT ADOPTABLE AT ALL, and that is the one difference the\nfamily term makes here: a different geometry in the same family is a replant, and\na different family is a refusal naming both — its masses do not describe your\nmodel in any space.",
			"riskModelValue.warmed":     "Warmed is how far your own event surface had been folded in when this value\nwas published, RFC 3339. It is part of the address because two models with\nidentical masses reached by different routes disagree about what is left to\nfold, and one of them will re-teach history the other will not.",
			"riskSurface.folded":        "Folded is how many buckets of the tenant's own feature surface were folded\ninto the model when it became resident.",
			"riskSurface.gap":           "Gap says why the fold did not happen or did not complete, when that is the\ncase. An empty surface and an unreachable warehouse are different facts and\na model must not report them as the same one.",
			"riskSurface.refused":       "Refused is how many buckets of this organisation's own surface the fold\ncould not fold, because a subject on them is longer than this plane's own\nfield bound. It is history the model does not have, said out loud.",
			"riskSurface.replayed":      "Replayed is how many of this organisation's own recorded observations\nrebuilt its sliding aggregates when the model became resident. It is what\nsays a rollout was a rebuild rather than a blindness: the aggregates are a\nprojection of a durable record, so a restart costs a replay and not a\ncontrol.",
			"riskSurface.rolled":        "Rolled is how many windows of this organisation's own source planes —\nproduct events, captured failures, metered inference — were rolled up into\nits feature surface before that fold. Zero with no gap means the surface was\nalready current, which is a different fact from the rollup never running.",
			"riskSurface.window":        "Window is the lookback the fold covered.",
		},
	})
	zip.Describe("POST /risk/decide", zip.Doc{
		Description: "Judges one subject against the CALLING organisation's own model and answers\nwhat to do about it. It learns nothing, records nothing and moves no counter:\nthe numbers it reads are that organisation's history as it stands.\n\nThe organisation is the CALLER's, minted from the plane principal and never\nfrom this body — there is no field here that could name one. A model is trained\non one organisation's own behaviour, so choosing which model answers would be\nthe only cross-tenant read this plane has to offer.\n\nA model still WARMING declines with a reason and no score. That is the whole\ncontract of the answer: the engine computes a score before it checks whether it\nhas learned enough to have an opinion, so a refusal carries a populated number\nthat means nothing, and publishing it would turn \"no opinion\" into \"this is\nfine\". Read `refusal` first; `scored` in the HTTP twin of this op says the same\nthing.\n\nA named handler, not a closure, so zipdoc can lift this prose into the registry.",
		Fields: map[string]string{
			"RiskDecideIn.kind":    "Kind is whose behaviour this is — person, session or account. It namespaces\nthe subject, so a person and an account sharing an identifier stay two\nsubjects.",
			"RiskDecideIn.signals": "Signals are the facts the gate observed. The scorer reads the names above;\nthe rest are the asking gate's own record of why it asked.",
			"RiskDecideIn.stage":   "Stage is the lifecycle moment, from cloud's closed set: signup, usage or\npayment. The scorer REFUSES a stage it does not recognise rather than\njudging a moment it does not model — the two ends of this call must agree on\nwhat is being asked before the answer means anything.",
			"RiskDecideIn.subject": "Subject is the identifier on that kind, within the caller's own tenant.",
			"RiskDecided.action":   "Action is what to do, from cloud's action vocabulary: allow, review,\nchallenge, restrict or block.",
			"RiskDecided.cause":    "Cause is the scorer's short reason, for the record the gate writes.",
			"RiskDecided.policy":   "Policy is the version of that organisation's decision regime the verdict was\nreached under. Zero means no regime was ever stated and the default posture —\nshadow — was in force.",
			"RiskDecided.refusal":  "Refusal names why this is NOT a scored answer — warming, unusable or\nunidentified — and is empty when it is one. None of them is a clean bill of\nhealth.",
			"RiskDecided.score":    "Score is where the event sat in that organisation's own density, in [0,1].\nPresent only on a scored answer.",
			"RiskDecided.shape":    "Shape is the model SPACE the verdict was reached in, `<family>:<digest>`. It\nis what pins an adverse decision to a model: a score is only meaningful\nagainst the space that produced it.",
		},
	})
	zip.Describe("POST /risk/observe", zip.Doc{
		Description: "Teaches the CALLING organisation's own model from something that settled.\n\n# Why the plane needs a learn door at all\n\nThe aggregate halves of the credit door's rule — pace and fan-out — read what an\nevent's identifiers had ALREADY done. Nothing was teaching them. [planeDecide]\nrecords nothing by design, the published learn door is an organisation calling\nitself over HTTP, and the organisation at a self-serve credit door IS the payer:\na fresh org signs up, tops up, and teaches its model nothing at all. So the two\nhalves read an empty history for precisely the subject they were built for, and a\npayment split into five pieces looked like five first payments.\n\nThis is the source that fills them, and it has the three properties that make an\naccrual worth reading:\n\n\tIT IS THE SERVER'S OBSERVATION, NOT A CLAIM. The caller is a process in this\n\tfleet reporting what it watched settle. The value is the amount that moved at\n\tthe gateway, the moment is a server clock, and the subject is the one the\n\tcredit landed on. None of it is a field the paying customer filled in, which is\n\tthe difference between a velocity bound and a velocity suggestion.\n\n\tIT IS IDEMPOTENT ON THE SETTLEMENT. [plane.note] deduplicates on (tenant, id)\n\tand this states the settlement's own identifier as that id, so a retried\n\trequest, a replayed webhook and a redelivered event converge on ONE\n\tobservation. Learned=0 is the answer for a settlement already held — an\n\thonest receipt rather than a second count of the same money.\n\n\tIT CANNOT BE PRE-EMPTED. Dedupe means the FIRST writer of an id wins, so an id\n\ta customer could guess is an id a customer could claim in advance, after which\n\tthe real settlement is silently inert — velocity switched off by the party it\n\tbounds. The id lands in [reserved] namespace and the public learn door refuses\n\tthat namespace outright ([riskEvent.observation]), so the only writer of a\n\tsettlement observation is a settlement.\n\n# It is a LEARN and it is priced like one — which is to say, not\n\nThe HTTP learn door meters per event ([ops.learn] calls pay). This one does not,\nfor [planeDecide]'s reason one step further on: the settlement it records is a\ncustomer's payment ARRIVING, so the balance a charge would be taken from is the\nbalance being filled. Charging for the record of a payment is a fee on paying.\nThe per-tenant in-flight slot still applies, because that is a bound on this\nprocess rather than a price.\n\nA named handler, not a closure, so zipdoc can lift this prose into the registry.",
		Fields: map[string]string{
			"RiskObserveIn.kind":       "Kind is whose behaviour this is. A settled payment is KindPayer.",
			"RiskObserveIn.settlement": "Settlement identifies what settled, and it is the IDEMPOTENCY KEY: the\nmodel's own record deduplicates on it, so a retried request or a replayed\nwebhook converges instead of counting the same money twice.\n\nIt must be the SETTLEMENT's own identifier — the processor's reference for\nthe charge, or the ledger receipt where the processor states none — and never\na counter, a timestamp or anything a caller of the settling door chose. A key\nthe payer can predict is a key the payer can claim first, after which the\nreal observation is inert.",
			"RiskObserveIn.signals":    "Signals are the facts the settling process observed, in the scorer's own\nvocabulary: the value that moved, the counterparty, the device, when.",
			"RiskObserveIn.stage":      "Stage is the lifecycle moment this happened at, from cloud's closed set.",
			"RiskObserveIn.subject":    "Subject is the identifier on that kind, within the caller's own tenant.",
			"RiskObserved.learned":     "Learned is how many observations entered the model on this call: one, or zero\nfor a settlement already recorded.",
		},
	})
	zip.Describe("POST /v1/risk/learn", zip.Doc{
		Description: "Learn records a batch of events into the caller organisation's own aggregates\nand lets its model learn from them. It answers how many it learned from.\n\nIT DOES NOT SCORE, AND THAT IS THE POINT. An observation is a value you record;\nlearning is a transformation over observations; a verdict is a query against the\nresult. This op is the first two. [ops.score] is the third, it is pure, and it\nis the ONE door to a verdict. They were one call, which meant you could not\nrecord without training and could not train without being answered — and the\nmodel ran twice over every event to produce a verdict the response carried and\nno caller read.\n\nTO OBSERVE AND JUDGE, COMPOSE THE TWO, and mind the order. Score FIRST, then\nlearn: the score is then the model's opinion of an event it has not yet learned\nfrom, which is the question worth asking. The other order answers for a model\nthat has already absorbed the event it is judging.\n\nThis is the training path, and there is no job behind it: the model IS a set of\nmass counters over half-space trees, so learning is an increment and the model\nis current the instant the last event lands. Nothing from any other\norganisation is in it, and nothing from this organisation leaves it.\n\nA RETRY IS INERT. The record deduplicates on the event id you send, and an event\nalready in it moves nothing, costs nothing and is not counted — so a client that\ntimed out can send the same batch again and its model holds what it holds.\nWithout an id of your own there is nothing to converge on: two identical bodies\nare two events.",
		Fields: map[string]string{
			"riskEvent.at":         "At is when it happened, RFC 3339. Empty means now. It must sit inside the\nthirty-day window the aggregates keep and no more than two minutes ahead of\nthis plane's clock; anything outside that is REFUSED rather than quietly\naccepted, because a future timestamp moves the aggregates' leading edge and\nleaves every later event for that subject reading as though it never\nhappened. History older than the window is folded in from your own event\nsurface, not through this door.",
			"riskEvent.device":     "Device is the device fingerprint, if any. It is the axis that surfaces\nseveral nominally unrelated subjects acting as one.",
			"riskEvent.id":         "ID is the caller's own stable identifier for the event. It selects the\nbelow-the-line review sample by hash, so a counter would make the sample\nsteerable — use the id the event already has.",
			"riskEvent.kind":       "Kind is whose behaviour this is: person, session or account. It namespaces\nthe subject, so a person and an account that share an identifier stay two\nsubjects.",
			"riskEvent.nano":       "Nano is the value moved, in nano-USD. Omit it for an event that moves no\nmoney: the value features then read BLIND rather than being told the amount\nwas zero, and the difference is reported on the model state.",
			"riskEvent.peer":       "Peer is the counterparty, if any. It is an aggregation axis of its own —\n\"unfamiliar\" is a fact about a relationship and not about either party.",
			"riskEvent.subject":    "Subject is the identifier on that kind.",
			"riskLearnIn.events":   "Events are the things that happened, oldest first. An empty batch is\nrefused: learning nothing is not an operation.",
			"riskLearnOut.learned": "Learned is how many of the events the model actually learned from, and is\nalso what the call is metered at: one screen per event learned from. It is\nthe batch minus the events already in this organisation's record, so a\nretried batch reports — and is charged — zero.",
		},
		Example: json.RawMessage(`{"events":[{"id":"tx_9","kind":"account","subject":"u_412","nano":420000000}]}`),
	})
	zip.Describe("POST /v1/risk/score", zip.Doc{
		Description: "Score judges one event against the caller organisation's OWN model and learns\nnothing from it. It is how a candidate is tried against real behaviour before\nanything depends on the answer, and it is the model's analogue of testing a\nrule.\n\nBecause it records nothing, the aggregates it reads do not include the event:\nthe numbers are the organisation's history as it stands. A model still warming\ndeclines with a reason rather than answering zero, because silence must never\nread as a clean result.",
		Fields: map[string]string{
			"riskCause.baseline":   "Baseline is the number it was measured against — always this\norganisation's own history, never a fixed limit and never another\norganisation's.",
			"riskCause.citation":   "Citation is where those words come from, so the claim is checkable rather\nthan asserted — which is what a chargeback network or a regulator asks for.",
			"riskCause.feature":    "Feature is the dimension that contributed.",
			"riskCause.indicator":  "Indicator is the supervisor's own words for the thing being looked for.",
			"riskCause.observed":   "Observed is the raw number the coordinate was computed from.",
			"riskCause.severity":   "Severity is how much weight this dimension carries.",
			"riskCause.share":      "Share is this feature's part of the score, in [0,1]. Zero across every\ncause means no single feature accounts for the alert and the combination\ndoes; the causes are then ordered by how far each sits from unremarkable.",
			"riskCause.typology":   "Typology is the laundering or abuse pattern this dimension detects.",
			"riskCause.unit":       "Unit is how to read Observed, which is what turns a coordinate into a\nsentence.",
			"riskCause.without":    "Without is the score the same event would have received with this\ncoordinate at its neutral value — the counterfactual itself.",
			"riskEvent.at":         "At is when it happened, RFC 3339. Empty means now. It must sit inside the\nthirty-day window the aggregates keep and no more than two minutes ahead of\nthis plane's clock; anything outside that is REFUSED rather than quietly\naccepted, because a future timestamp moves the aggregates' leading edge and\nleaves every later event for that subject reading as though it never\nhappened. History older than the window is folded in from your own event\nsurface, not through this door.",
			"riskEvent.device":     "Device is the device fingerprint, if any. It is the axis that surfaces\nseveral nominally unrelated subjects acting as one.",
			"riskEvent.id":         "ID is the caller's own stable identifier for the event. It selects the\nbelow-the-line review sample by hash, so a counter would make the sample\nsteerable — use the id the event already has.",
			"riskEvent.kind":       "Kind is whose behaviour this is: person, session or account. It namespaces\nthe subject, so a person and an account that share an identifier stay two\nsubjects.",
			"riskEvent.nano":       "Nano is the value moved, in nano-USD. Omit it for an event that moves no\nmoney: the value features then read BLIND rather than being told the amount\nwas zero, and the difference is reported on the model state.",
			"riskEvent.peer":       "Peer is the counterparty, if any. It is an aggregation axis of its own —\n\"unfamiliar\" is a fact about a relationship and not about either party.",
			"riskEvent.subject":    "Subject is the identifier on that kind.",
			"riskScoreIn.event":    "Event is the thing to judge. It is judged against the caller's OWN model\nand nothing is learned from it.",
			"riskScoreOut.alert":   "Alert is whether this would become evidence. It is false in shadow however\nhigh the score.",
			"riskScoreOut.causes":  "Causes is the per-feature attribution, ordered by contribution. Each is a\nCOUNTERFACTUAL on the model that produced the score — the coordinate moved\nto its neutral value and the event rescored — so the explanation is the\nsame arithmetic the score came from.",
			"riskScoreOut.cut":     "Cut is the threshold in force, derived from the stated appetite as a\nquantile of the scores actually observed rather than fixed at a number.",
			"riskScoreOut.policy":  "Policy is the version of your organisation's decision regime this verdict\nwas reached under, from its own policy history (GET /v1/risk/policy). Cut is\nderived from the appetite that version states, so it is the record that makes\nthis decision reconstructible after the appetite is restated. Zero means no\nregime has ever been stated and the default posture — shadow — was in force.",
			"riskScoreOut.refusal": "Refusal names why the model declined, when it did.",
			"riskScoreOut.score":   "Score is where the event sits in the tenant's own density: 0 where its\nrecent behaviour is densest, 1 where there is none of it.",
			"riskScoreOut.scored":  "Scored is false when the model declined, and Refusal says which refusal it\nwas: warming, unusable or unidentified. None of them is a clean bill of\nhealth, which is why the refusal is stated rather than rendered as a score\nof zero.",
			"riskScoreOut.shadow":  "Shadow is whether the model is testing rather than deciding — scoring,\nlearning and recording what it WOULD have alerted on, and changing no\noutcome. It is the default for a model no one has reviewed yet.",
			"riskScoreOut.shape":   "Shape is the model space this verdict was reached in, as `<family>:<digest>`:\nthe KIND of model, and that family's own digest over your organisation's feature\ninventory in order and the detector's geometry parameters. It is what pins an\nadverse decision to a model — a score is only meaningful against the space that\nproduced it, and without this the only answer to \"which model decided this\" was\n\"the one that was running\", which is not an answer.\n\nThe family leads it because everything after it is one family's arithmetic. Two\nspaces are the same space only if they are the same family, so comparing this\nwith the `shape` on your model state or on a published value is a comparison\nthat holds ACROSS families and not only inside one.\n\nIt names the SPACE, not the learned state, and that is deliberate. The masses\nat the instant of a score are in-process counters somewhere between two\npublished values, so citing a published address here would claim that value\nproduced this score — true only for the score taken the instant after a\npublication. This, the policy version and the event's own time are what IS\ntrue, and the published history's clock (GET /v1/risk/state) brackets the\ndecision between two named values from there.",
			"riskScoreOut.values":  "Values is every coordinate, including the ones that contributed nothing, so\na reviewer sees what the model read and not only what it concluded.",
			"riskValue.baseline":   "Baseline is what Observed was measured against: this organisation's own\nhistory for this subject.",
			"riskValue.blind":      "Blind marks a coordinate that could not be computed and took its neutral\nvalue. A model silently reading neutral for a dimension it never has data\nfor is indistinguishable from one reading a genuine absence of risk.",
			"riskValue.feature":    "Feature is the dimension.",
			"riskValue.observed":   "Observed is the raw number X was computed from, quoted so the coordinate\nreads back as a sentence rather than a bare ratio.",
			"riskValue.unit":       "Unit is how to read Observed.",
			"riskValue.x":          "X is the coordinate in the model space, always dimensionless.",
		},
		Example: json.RawMessage(`{"event":{"id":"tx_9","kind":"account","subject":"u_412","nano":420000000}}`),
	})
	zip.Describe("POST /v1/risk/search", zip.Doc{
		Description: "Search runs an exhaustive search for the model shape that best fits the caller\norganisation's own history, and answers 202 with the run to read back.\n\nEvery candidate is replayed over that organisation's OWN feature surface in its\nown sandbox — its own aggregates, its own model, neither of them the live one —\nso a run cannot move a live threshold and cannot see another organisation's\ndata. The result is the learning curve for each shape and the one that fit\nbest, ranked on how closely it honoured the stated appetite, whether it warmed\nat all, whether it saturated, and how much of the coordinate space it left\nblind.\n\nAn empty history is REFUSED rather than reported as zero alerts, because \"no\nalerts\" is exactly what a quiet model looks like.",
		Fields: map[string]string{
			"riskSearchIn.days":        "Days is how much of the organisation's own history to replay, 1 to 400.\nZero takes thirty.",
			"riskSearchRun.candidates": "Candidates is how many model shapes will be tried.",
			"riskSearchRun.events":     "Events is how much of the organisation's own history the run will replay.",
			"riskSearchRun.id":         "ID addresses the run. Read the result back with it.",
		},
		Example: json.RawMessage(`{"days":30}`),
	})
	zip.Describe("POST /v1/risk/state/model", zip.Doc{
		Description: "Publishes your organisation's model as a NAMED VALUE, so a decision\ntaken today can be reconstructed tomorrow and a change made today can be undone.\n\nIt answers with a NAME and not with the state. The masses stay on your\norganisation's own encrypted store and are referred to by an address computed\nfrom their own content: the shape, the geometry seed, the position in the window,\nthe threshold, the masses themselves as IEEE-754 bits, and the fold watermark\nbehind them. That is what makes the value nameable without making the caller its\ncustodian.\n\nIT IS IDEMPOTENT ON THE VALUE. A model that has not changed publishes to the name\nit already has and mints nothing, reporting minted=false — so publishing at every\nboundary that matters is free. Ten values are retained per organisation, bounded\nin BYTES rather than in rows, and the oldest is disposed of past that.\n\nA model that has learned nothing is refused: planted is not learned, and a value\nthat reproduces nothing is not a value.\n\nIt is POST and PUT on one address because they are one plane's two verbs over one\nkind of thing: POST mints a value from the model in force, PUT puts a value in\nforce. They were /v1/risk/state/snapshot and /v1/risk/state/restore — two addresses\nnamed after the operation rather than after the thing, which is how a reader ends\nup asking what the difference between a snapshot and a value is.",
		Fields: map[string]string{
			"riskModelValue.address":  "Address names this value by its own content: the model's shape, the geometry\nseed, its position in the window, its threshold, its masses as IEEE-754 bits\nand the fold watermark behind them. Nothing else — no clock, no counter and\ndeliberately NOT the organisation, so an identical model has one name and a\nname is never an authority. Holding another organisation's address resolves\nnothing.",
			"riskModelValue.at":       "At is when it was published, RFC 3339, on the server clock. You do not supply\nit: a record whose date the audited party chose is not a record.",
			"riskModelValue.learned":  "Learned is how many events are behind the masses.",
			"riskModelValue.sequence": "Sequence is this value's place in YOUR organisation's own history, from 1 and\ncontiguous until retention disposes of the oldest.",
			"riskModelValue.shape":    "Shape NAMES the model space the masses are only meaningful against, as\n`<family>:<digest>` — the KIND of model, and that family's own digest over the\nfeature inventory in order and the detector's geometry parameters. Compare it\nwith the `shape` on your model state (GET /v1/risk/state): equal means adopting\nthis value restores masses into the space already running, and different means\nadopting it REPLANTS the model into the space this value describes. That is what\nmakes a searched shape installable.\n\nA DIFFERENT FAMILY IS NOT ADOPTABLE AT ALL, and that is the one difference the\nfamily term makes here: a different geometry in the same family is a replant, and\na different family is a refusal naming both — its masses do not describe your\nmodel in any space.",
			"riskModelValue.warmed":   "Warmed is how far your own event surface had been folded in when this value\nwas published, RFC 3339. It is part of the address because two models with\nidentical masses reached by different routes disagree about what is left to\nfold, and one of them will re-teach history the other will not.",
			"riskPublishOut.minted":   "Minted is false when your model was ALREADY published under this name and\nnothing was written. Publication is idempotent on the value itself, which is\nwhat a content address is for — publishing at every boundary costs nothing\nrather than being the cheapest way to fill a disk.",
			"riskPublishOut.tenant":   "Tenant is whose history it entered.",
			"riskPublishOut.value":    "Value is the published value: its name and what it is, never its masses.",
		},
	})
	zip.Describe("PUT /v1/risk/policy", zip.Doc{
		Description: "States the decision regime the caller organisation's model decides\nunder: how much of its own stream may be sent for examination, how much of the\nrest is sampled to measure what was missed, and whether the model may change an\noutcome at all.\n\nThe appetite is the decision a model is not permitted to make for itself: its\noutput is a probability, so how likely it is to MISS something is a matter of\npolicy that has to be stated, measured and reviewed rather than absorbed into a\nconstant. The alert threshold is derived from it as a quantile of the scores\nactually observed, which is what keeps its meaning as the distribution drifts.\n\nIt is DURABLE BEFORE IT IS IN FORCE. The regime is recorded as a new version on\nthe organisation's own shelf before anything in memory moves, so a policy that\ncannot be written down is refused rather than answered from state the next\nrollout would silently undo.\n\nARMING IS AN ADMIN ACT AND TUNING IS NOT. Setting `live` requires an admin of\nthis organisation; stating the appetite and the sample is self-service for any\nmember. Taking the model live decides whether it may change an OUTCOME at all —\na payment frozen, a grant refused — for every customer this organisation has,\nand that is a decision an organisation takes rather than one of its members.\n\nA RESTATEMENT OF THE REGIME IN FORCE MINTS NOTHING and answers the version\nalready in force. Compare the version you receive with the version you had:\nunchanged means the numbers were the same, which is why there is no flag for it.\n\nLearned state survives the change. The model's identity covers its SHAPE — the\ninventory and the geometry — and not its appetite, so restating policy unlearns\nnothing. It also does not REPORT the learned state: what the model is is read\nfrom the model.",
		Fields: map[string]string{
			"riskAppetiteIn.live":       "Live turns the model out of shadow. It defaults to FALSE on every call, so\ngoing live is always an explicit act and never a side effect of changing a\nnumber.\n\nSetting it requires an ADMIN of this organisation. Arming decides whether the\nmodel may change an outcome at all — a payment frozen, a grant refused — for\nevery customer this organisation has, which is a governance act rather than a\ntuning one. Stating the appetite and the sample needs no admin.",
			"riskAppetiteIn.review":     "Review is the share of the stream that may be sent for examination, in\n(0, 0.5]. The alert threshold is derived from it as a quantile of the scores\nactually observed, so the level is governed rather than tuned.",
			"riskAppetiteIn.sample":     "Sample is the share of below-the-line events retained for review, in\n[0, 1]. It is the instrument that measures what the model missed; there are\nno labels, so nothing else can.",
			"riskPolicyOut.changes":     "Changes is how many DISTINCT regimes may be adopted per Window. A restatement\nidentical to the regime in force mints no version and is not counted against\nit.",
			"riskPolicyOut.disposed":    "Disposed is how many versions retention has taken. It is NOT a silence: a\nhistory bounded on disk must say what it no longer holds, because a decision\nciting a disposed version can no longer be reconstructed from this record.",
			"riskPolicyOut.history":     "History is the retained versions, newest first.",
			"riskPolicyOut.retained":    "Retained is how many versions this organisation's history holds at most,\nderived from the byte budget its rows are a multiple of.",
			"riskPolicyOut.version":     "Version is the version in force — the one every score currently cites. Zero\nmeans no regime has ever been stated and the default posture, shadow, is in\nforce.",
			"riskPolicyOut.window":      "Window is the period Changes is measured over.",
			"riskPolicyVersion.at":      "At is when it entered force, RFC 3339, from the server clock.",
			"riskPolicyVersion.by":      "By is the identity that stated it, stamped server-side from the validated\nprincipal at the moment it entered force.",
			"riskPolicyVersion.live":    "Live is whether the model was permitted to change an outcome under it.",
			"riskPolicyVersion.review":  "Review is the share of the stream the regime states may be examined. The\nthreshold in force is derived from it, which is why a decision is only\ndefensible against the version that produced it.",
			"riskPolicyVersion.sample":  "Sample is the share of below-the-line events the regime retains for review.",
			"riskPolicyVersion.version": "Version names this regime in this organisation's history.",
		},
		Example: json.RawMessage(`{"review":0.01,"sample":0.001,"live":false}`),
	})
	zip.Describe("PUT /v1/risk/state/model", zip.Doc{
		Description: "Puts one of your organisation's OWN PUBLISHED VALUES in force, by name —\nwhich is what an instant rollback is, what promoting a challenger is, and what\ninstalling the shape a search found is.\n\nIT TAKES AN ADDRESS AND NEVER STATE. The masses are read from your own store, so\nnothing about your model has to be held by whatever is making this call. That\ncloses the sharpest edge the previous shape had: a body of counters is something\na caller can COMPOSE, and a region filled until activity inside it reads as\nordinary is a model that has been shaped rather than learned. The engine's mass\ninvariant was the only thing standing between a composed body and the model; with\nan address there is no body to compose.\n\nIT ADOPTS THE SHAPE, NOT ONLY THE MASSES. A value records the model space its\nmasses were taken in, and a value whose space differs from the one in force\nREPLANTS your model into that space before restoring them. That is what makes\nPOST /v1/risk/search actionable: a search answers with the shape that fits your own\nhistory best and publishes it fitted, and its address is what you name here. Before\nthis, a winning shape was advice nobody could take — the adoption path refused every\nshape change, and a winner is a different shape by definition.\n\nWHAT ADOPTING A SEARCHED SHAPE COSTS, SAID PLAINLY: the value a search fits has\nlearned the window the search replayed and nothing older, so installing it trades\nhistory for fit. Your appetite is untouched — that is your policy record's, with its\nown versions — and so is the geometry, which stays your own.\n\nAn address your organisation has not published is NOT FOUND. That includes one\nanother organisation published, and it is not a lookup that failed: the store is\nper organisation and the address is a name, never an authority.",
		Fields: map[string]string{
			"riskAdoptIn.address":       "Address is one of YOUR organisation's own published values (GET\n/v1/risk/state reports them, and a search reports the one it fitted for you).\nAn address your organisation has not published is NOT FOUND — including one\nanother organisation published, because an address names a value and never\nauthorises reading it.",
			"riskAggregates.bound":      "Bound is the most they can hold. It is a per-organisation bound: at it, this\norganisation degrades and no other one notices.",
			"riskAggregates.forgotten":  "Forgotten is how many of its own subjects have been dropped to stay inside\nthat bound. Each one reads as inactive until it is active again.",
			"riskAggregates.saturated":  "Saturated is whether the bound is binding right now. The two counts are its\nevidence; this is the state to act on.",
			"riskAggregates.subjects":   "Subjects is how many of this organisation's subjects the aggregates hold.",
			"riskModelState.aggregates": "Aggregates reports the pressure on this organisation's own sliding\naggregates, and whether they have started forgetting subjects to stay inside\ntheir bound.",
			"riskModelState.blind":      "Blind counts, per feature, how often it took its neutral value for want of\ndata. A feature blind on most traffic is not contributing whatever the\ninventory claims for it.",
			"riskModelState.cut":        "Cut is the threshold in force, derived from Stated as a quantile of the\nscores actually observed.",
			"riskModelState.descends":   "Descends is the published value the working model grew out of: the newest one\nwhose mass count it has reached or passed. Empty when nothing has been\npublished yet.\n\nIt is DERIVED from the count and never stored, so an instant rollback is right\nfor free — adopting an older value moves the count backward and this answers\nwith that older value, where a stored pointer would be a second fact to keep\nin step. Read with Learned it is also the DRIFT: this model is Descends plus\nhowever many events the two counts differ by.",
			"riskModelState.disposed":   "Disposed is how many published values retention has taken. It is DERIVED from\nthe lowest surviving sequence, so it cannot drift from what it describes, and\nit is reported because a retention that binds is a fact an operator must be\nable to read rather than a silence.",
			"riskModelState.learned":    "Learned is how many events the model has learned from.",
			"riskModelState.live":       "Live is false while the model is in shadow — scoring, learning and\nrecording what it WOULD have alerted on, and changing no outcome. Shadow is\nthe default for a new tenant.",
			"riskModelState.policy":     "Policy is the version of the decision regime this model is deciding under,\nfrom your organisation's own policy history (GET /v1/risk/policy). Every\nscore cites it, so it is the join between a past decision and the appetite\nthat produced its threshold. Zero means no regime has ever been stated and\nthe default posture — shadow — is in force.",
			"riskModelState.realised":   "Realised is the share that actually was. Reading it beside Stated is what\nmakes the appetite a measured commitment rather than an intention.",
			"riskModelState.refused":    "Refused counts events the model would not score, by reason. None of them\nwas examined; a refusal is counted, never silent.",
			"riskModelState.sample":     "Sample is the share of below-the-line events retained for review, which is\nhow the miss rate is measured rather than assumed.",
			"riskModelState.saturated":  "Saturated means no threshold can honour the stated appetite because too\nmuch of the stream scores in the top bucket, so the model is alerting on\nnothing — the one state that must never be mistaken for quiet.",
			"riskModelState.shape":      "Shape is the model's identity, as `<family>:<digest>`: the KIND of model, and\nthat family's own digest over the inventory in order and the detector's geometry\nparameters. It is what an auditor pins an alert to, because learned state is\nonly meaningful against the space that produced it — and the family leads it\nbecause two families' masses are not fitted differently, they are different\nkinds of number.",
			"riskModelState.stated":     "Stated is the share of the stream this organisation said may be examined.",
			"riskModelState.surface":    "Surface reports what of the tenant's OWN event surface has been folded in.",
			"riskModelState.tenant":     "Tenant is the qualified key the model is held under — the brand whose\nissuer vouched for the caller and the organisation it acts for. It is\nechoed so a reader can see the answer is its own and not a parameter it\npassed.",
			"riskModelState.values":     "Values is your organisation's own published model values, newest first —\nevery state it deliberately named, each addressed by its own content and\nimmutable. This is what PUT /v1/risk/state/model names, so it is reported\nHERE rather than behind an address of its own: they are part of what a review\nof one model reads, and a list of names is a few hundred bytes.\n\nCompare each one's `shape` with the `shape` above: equal means adopting it\nrestores masses into the space this model already runs, and different means\nadopting it REPLANTS the model into the space that value describes — which is how\nthe shape a search found becomes the shape you are running.\n\nThe working model is NOT in it. Publication is a boundary somebody marked; the\nstate between two boundaries is in-process counters, and calling those a value\nwould be a claim about reproducibility that nothing could honour.",
			"riskModelState.warm":       "Warm is whether that is enough for the model to have an opinion at all.\nBelow it the model declines to score, which is an ordinary state and is not\na clean bill of health.",
			"riskModelValue.address":    "Address names this value by its own content: the model's shape, the geometry\nseed, its position in the window, its threshold, its masses as IEEE-754 bits\nand the fold watermark behind them. Nothing else — no clock, no counter and\ndeliberately NOT the organisation, so an identical model has one name and a\nname is never an authority. Holding another organisation's address resolves\nnothing.",
			"riskModelValue.at":         "At is when it was published, RFC 3339, on the server clock. You do not supply\nit: a record whose date the audited party chose is not a record.",
			"riskModelValue.learned":    "Learned is how many events are behind the masses.",
			"riskModelValue.sequence":   "Sequence is this value's place in YOUR organisation's own history, from 1 and\ncontiguous until retention disposes of the oldest.",
			"riskModelValue.shape":      "Shape NAMES the model space the masses are only meaningful against, as\n`<family>:<digest>` — the KIND of model, and that family's own digest over the\nfeature inventory in order and the detector's geometry parameters. Compare it\nwith the `shape` on your model state (GET /v1/risk/state): equal means adopting\nthis value restores masses into the space already running, and different means\nadopting it REPLANTS the model into the space this value describes. That is what\nmakes a searched shape installable.\n\nA DIFFERENT FAMILY IS NOT ADOPTABLE AT ALL, and that is the one difference the\nfamily term makes here: a different geometry in the same family is a replant, and\na different family is a refusal naming both — its masses do not describe your\nmodel in any space.",
			"riskModelValue.warmed":     "Warmed is how far your own event surface had been folded in when this value\nwas published, RFC 3339. It is part of the address because two models with\nidentical masses reached by different routes disagree about what is left to\nfold, and one of them will re-teach history the other will not.",
			"riskSurface.folded":        "Folded is how many buckets of the tenant's own feature surface were folded\ninto the model when it became resident.",
			"riskSurface.gap":           "Gap says why the fold did not happen or did not complete, when that is the\ncase. An empty surface and an unreachable warehouse are different facts and\na model must not report them as the same one.",
			"riskSurface.refused":       "Refused is how many buckets of this organisation's own surface the fold\ncould not fold, because a subject on them is longer than this plane's own\nfield bound. It is history the model does not have, said out loud.",
			"riskSurface.replayed":      "Replayed is how many of this organisation's own recorded observations\nrebuilt its sliding aggregates when the model became resident. It is what\nsays a rollout was a rebuild rather than a blindness: the aggregates are a\nprojection of a durable record, so a restart costs a replay and not a\ncontrol.",
			"riskSurface.rolled":        "Rolled is how many windows of this organisation's own source planes —\nproduct events, captured failures, metered inference — were rolled up into\nits feature surface before that fold. Zero with no gap means the surface was\nalready current, which is a different fact from the rollup never running.",
			"riskSurface.window":        "Window is the lookback the fold covered.",
		},
	})
}
