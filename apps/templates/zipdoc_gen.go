// Code generated by zipdoc; DO NOT EDIT.

package templates

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/templates/:slug", zip.Doc{
		Description: "remove deletes the caller org's own template and answers 204.\nA slug they do not own is a 404, never a delete: the DELETE binds org.",
		Fields: map[string]string{
			"TemplateRef.slug": "Slug is the template's id, lowercase alphanumeric with dashes.",
		},
		Example: json.RawMessage(`{"slug":"acme-landing"}`),
	})
	zip.Describe("GET /v1/templates", zip.Doc{
		Description: "list returns the public starter-kit catalog plus, for a validated caller, that org's own templates.\nNo request field can widen the scope: the org comes from the gateway-minted,\nJWT-validated principal, so an anonymous or cross-org caller structurally sees\nthe public catalog only.",
		Fields: map[string]string{
			"Template.category":    "Category groups the entry in the gallery browser.",
			"Template.demo":        "live demo (<slug>.hanzo.app), when deployed",
			"Template.description": "Description is the gallery blurb, max 4096 characters.",
			"Template.features":    "Features are the selling-point bullets, max 32.",
			"Template.framework":   "Framework is the stack the kit is built on (\"next\", \"astro\", …).",
			"Template.org":         "owner of a PRIVATE template; empty in the public catalog",
			"Template.preview":     "Preview is the screenshot or preview URL, max 4096 characters.",
			"Template.rating":      "Rating is a public-gallery curation score; server-owned, ignored on a write.",
			"Template.slug":        "Slug is the template's id: lowercase alphanumeric with dashes, max 40. On a\nreplace the path owns it and a body slug is ignored.",
			"Template.source":      "Source is the repository the fork clones from, max 4096 characters.",
			"Template.tier":        "Tier is a public-gallery curation rank; server-owned, ignored on a write.",
			"Template.title":       "Title is the display name; required, max 200 characters.",
			"Template.useCase":     "UseCase is what the kit is for, in one phrase.",
			"Template.variants":    "the shapes this template ships in",
			"TemplateList.data":    "Data is the templates, public entries first.",
			"Variant.framework":    "only when it differs from the template's",
			"Variant.id":           "selector, unique within the template (\"react\", \"grid-3-fluid\")",
			"Variant.kind":         "the axis it varies: format | page | theme",
			"Variant.label":        "human label for the picker",
		},
		Response: json.RawMessage(`{"data":[{"slug":"folio","title":"Portfolio","framework":"next"}]}`),
	})
	zip.Describe("GET /v1/templates/:slug", zip.Doc{
		Description: "get returns one template: the caller org's own if they have that slug, else the public one.\nAn unknown slug is a 404; an anonymous caller resolves against the public catalog only.",
		Fields: map[string]string{
			"Template.category":    "Category groups the entry in the gallery browser.",
			"Template.demo":        "live demo (<slug>.hanzo.app), when deployed",
			"Template.description": "Description is the gallery blurb, max 4096 characters.",
			"Template.features":    "Features are the selling-point bullets, max 32.",
			"Template.framework":   "Framework is the stack the kit is built on (\"next\", \"astro\", …).",
			"Template.org":         "owner of a PRIVATE template; empty in the public catalog",
			"Template.preview":     "Preview is the screenshot or preview URL, max 4096 characters.",
			"Template.rating":      "Rating is a public-gallery curation score; server-owned, ignored on a write.",
			"Template.slug":        "Slug is the template's id: lowercase alphanumeric with dashes, max 40. On a\nreplace the path owns it and a body slug is ignored.",
			"Template.source":      "Source is the repository the fork clones from, max 4096 characters.",
			"Template.tier":        "Tier is a public-gallery curation rank; server-owned, ignored on a write.",
			"Template.title":       "Title is the display name; required, max 200 characters.",
			"Template.useCase":     "UseCase is what the kit is for, in one phrase.",
			"Template.variants":    "the shapes this template ships in",
			"TemplateRef.slug":     "Slug is the template's id, lowercase alphanumeric with dashes.",
			"Variant.framework":    "only when it differs from the template's",
			"Variant.id":           "selector, unique within the template (\"react\", \"grid-3-fluid\")",
			"Variant.kind":         "the axis it varies: format | page | theme",
			"Variant.label":        "human label for the picker",
		},
		Example: json.RawMessage(`{"slug":"folio"}`),
	})
	zip.Describe("POST /v1/templates", zip.Doc{
		Description: "publish creates a template private to the caller's org and answers 201.\nThe slug must be free in both layers — publishing over a public-catalog slug is\n409 — and the owner, tier and rating are stamped by the server, never by the body.",
		Fields: map[string]string{
			"Template.category":    "Category groups the entry in the gallery browser.",
			"Template.demo":        "live demo (<slug>.hanzo.app), when deployed",
			"Template.description": "Description is the gallery blurb, max 4096 characters.",
			"Template.features":    "Features are the selling-point bullets, max 32.",
			"Template.framework":   "Framework is the stack the kit is built on (\"next\", \"astro\", …).",
			"Template.org":         "owner of a PRIVATE template; empty in the public catalog",
			"Template.preview":     "Preview is the screenshot or preview URL, max 4096 characters.",
			"Template.rating":      "Rating is a public-gallery curation score; server-owned, ignored on a write.",
			"Template.slug":        "Slug is the template's id: lowercase alphanumeric with dashes, max 40. On a\nreplace the path owns it and a body slug is ignored.",
			"Template.source":      "Source is the repository the fork clones from, max 4096 characters.",
			"Template.tier":        "Tier is a public-gallery curation rank; server-owned, ignored on a write.",
			"Template.title":       "Title is the display name; required, max 200 characters.",
			"Template.useCase":     "UseCase is what the kit is for, in one phrase.",
			"Template.variants":    "the shapes this template ships in",
			"Variant.framework":    "only when it differs from the template's",
			"Variant.id":           "selector, unique within the template (\"react\", \"grid-3-fluid\")",
			"Variant.kind":         "the axis it varies: format | page | theme",
			"Variant.label":        "human label for the picker",
		},
		Example: json.RawMessage(`{"slug":"acme-landing","title":"Acme Landing","framework":"next","source":"https://github.com/acme/landing"}`),
	})
	zip.Describe("PUT /v1/templates/:slug", zip.Doc{
		Description: "replace overwrites the caller org's own template at :slug, keeping the path's identity.\nA slug the caller does not own is a 404 — the UPDATE binds org, so a PUT can\nnever reach another org's row — and the body cannot rename the template.",
		Fields: map[string]string{
			"Template.category":    "Category groups the entry in the gallery browser.",
			"Template.demo":        "live demo (<slug>.hanzo.app), when deployed",
			"Template.description": "Description is the gallery blurb, max 4096 characters.",
			"Template.features":    "Features are the selling-point bullets, max 32.",
			"Template.framework":   "Framework is the stack the kit is built on (\"next\", \"astro\", …).",
			"Template.org":         "owner of a PRIVATE template; empty in the public catalog",
			"Template.preview":     "Preview is the screenshot or preview URL, max 4096 characters.",
			"Template.rating":      "Rating is a public-gallery curation score; server-owned, ignored on a write.",
			"Template.slug":        "Slug is the template's id: lowercase alphanumeric with dashes, max 40. On a\nreplace the path owns it and a body slug is ignored.",
			"Template.source":      "Source is the repository the fork clones from, max 4096 characters.",
			"Template.tier":        "Tier is a public-gallery curation rank; server-owned, ignored on a write.",
			"Template.title":       "Title is the display name; required, max 200 characters.",
			"Template.useCase":     "UseCase is what the kit is for, in one phrase.",
			"Template.variants":    "the shapes this template ships in",
			"Variant.framework":    "only when it differs from the template's",
			"Variant.id":           "selector, unique within the template (\"react\", \"grid-3-fluid\")",
			"Variant.kind":         "the axis it varies: format | page | theme",
			"Variant.label":        "human label for the picker",
		},
		Example: json.RawMessage(`{"slug":"acme-landing","title":"Acme Landing v2","framework":"next"}`),
	})
}
