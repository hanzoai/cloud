// Code generated by zipdoc; DO NOT EDIT.

package billing

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/billing/usage/accounts", zip.Doc{
		Description: "usageAccounts breaks the caller's own routed spend down per linked account —\nrequests, tokens and cost cents for each, plus their total. It reports only the\ncaller's own accounts; a deployment without the linked-account plane answers 501\nrather than a fabricated empty breakdown.",
		Response:    json.RawMessage(`{"scope":"user","source":"routed","total":{"accounts":1,"requests":42,"costCents":130},"accounts":[{"account":"anthropic","requests":42,"costCents":130}]}`),
	})
	zip.Describe("GET /v1/finance/balance", zip.Doc{
		Description: "financeBalance answers the caller org's prepaid wallet: what is spendable now,\nwhat is held, and what is owed (always 0 — the wallet is prepaid). It is the SAME\nwallet the spend gate reads, so the number shown is the number that admits or\nrefuses a request.",
		Fields: map[string]string{
			"financeBalanceView.asOf":           "AsOf is when the balance was read, RFC3339 UTC.",
			"financeBalanceView.availableCents": "AvailableCents is the spendable prepaid balance, net of holds, never negative.",
			"financeBalanceView.currency":       "Currency is always \"usd\" — the wallet's only denomination today.",
			"financeBalanceView.dueCents":       "DueCents is what the org owes; always 0 on a prepaid wallet.",
			"financeBalanceView.pendingCents":   "PendingCents is prepaid funds authorized but not yet settled.",
		},
		Response: json.RawMessage(`{"currency":"usd","availableCents":998800,"pendingCents":0,"dueCents":0,"asOf":"2026-07-29T18:00:00Z"}`),
	})
	zip.Describe("GET /v1/finance/credits", zip.Doc{
		Description: "financeCredits lists the credit grants on the caller org's wallet — every\nstaff/promo grant and top-up, each positive. Consumption is usage, not a credit,\nand is excluded. An org with no grants gets an empty array, never a fabricated row.",
		Fields: map[string]string{
			"financeCredit.cents":          "Cents is the grant's magnitude, always positive.",
			"financeCredit.expiresAt":      "ExpiresAt is when the grant lapses; absent when it does not.",
			"financeCredit.grantedAt":      "GrantedAt is when the grant posted, RFC3339.",
			"financeCredit.id":             "ID is the ledger row this grant came from.",
			"financeCredit.label":          "Label is the grant's note or tag, else \"Credit\".",
			"financeCredit.remainingCents": "RemainingCents is what is left of the grant when the ledger tracks it.",
		},
		Response: json.RawMessage(`[{"id":"txn_9f21","label":"Launch credit","cents":50000,"grantedAt":"2026-05-01T00:00:00Z"}]`),
	})
	zip.Describe("GET /v1/finance/invoices", zip.Doc{
		Description: "financeInvoices lists the caller org's issued invoices. It is empty by\nconstruction today: the wallet is prepaid (deposits and withdraws), so no invoice\nis ever issued — an honest empty array rather than a fabricated figure.",
		Fields: map[string]string{
			"financeInvoice.cents":    "Cents is the invoice total.",
			"financeInvoice.currency": "Currency is the invoice's denomination.",
			"financeInvoice.date":     "Date is when the invoice was issued.",
			"financeInvoice.dueDate":  "DueDate is when payment is due.",
			"financeInvoice.id":       "ID is the invoice's identifier.",
			"financeInvoice.number":   "Number is the human-facing invoice number.",
			"financeInvoice.status":   "Status is the invoice's state, e.g. paid or open.",
			"financeInvoice.url":      "URL links to the rendered invoice.",
		},
		Response: json.RawMessage(`[]`),
	})
	zip.Describe("GET /v1/finance/ledger", zip.Doc{
		Description: "financeLedger lists the caller org's money movements over the window as signed\npostings: a deposit credits the wallet (positive), a withdraw debits it (negative).",
		Fields: map[string]string{
			"financeLedgerEntry.account":      "Account is the org-scoped book the posting hit: \"credits:<org>\" or \"usage:<org>\".",
			"financeLedgerEntry.balanceCents": "BalanceCents is the running balance after the posting, when known.",
			"financeLedgerEntry.cents":        "Cents is the signed move: positive for a deposit, negative for a withdraw.",
			"financeLedgerEntry.currency":     "Currency is the posting's denomination, lowercased.",
			"financeLedgerEntry.date":         "Date is when the posting landed, RFC3339.",
			"financeLedgerEntry.description":  "Description is the posting's note, tag, or its type.",
			"financeLedgerEntry.id":           "ID is the ledger row.",
			"financeWindow.range":             "Range is how far back to look: 24h, 7d, 30d or 90d. Anything else, including\nan absent value, means 30d.",
		},
		Example:  json.RawMessage(`{"range":"30d"}`),
		Response: json.RawMessage(`[{"id":"txn_9f21","date":"2026-07-28T12:00:00Z","account":"usage:acme","description":"ai","cents":-1240,"currency":"usd"}]`),
	})
	zip.Describe("GET /v1/finance/payment-methods", zip.Doc{
		Description: "financePaymentMethods lists the caller org's saved cards as MASKED descriptors —\nbrand, last four and expiry. It re-masks whatever the processor returns, so a full\ncard number can never reach the response.",
		Fields: map[string]string{
			"financePaymentMethod.brand":     "Brand is the card network, e.g. visa.",
			"financePaymentMethod.expMonth":  "ExpMonth is the expiry month, 1-12.",
			"financePaymentMethod.expYear":   "ExpYear is the four-digit expiry year.",
			"financePaymentMethod.id":        "ID is the saved method's identifier at the payment processor.",
			"financePaymentMethod.isDefault": "IsDefault marks the method a charge falls back to.",
			"financePaymentMethod.last4":     "Last4 is at most the trailing four digits — the only digits ever surfaced.",
			"financePaymentMethod.type":      "Type is the instrument kind, e.g. \"card\".",
		},
		Response: json.RawMessage(`[{"id":"pm_1QX","type":"card","brand":"visa","last4":"4242","expMonth":4,"expYear":2029,"isDefault":true}]`),
	})
	zip.Describe("GET /v1/finance/usage", zip.Doc{
		Description: "financeUsage reports the caller org's metered spend over the window: a total, a\ntime series (hourly for 24h, daily otherwise) and a per-tag breakdown. It projects\nthe SAME priced ledger the wallet is debited from — never a second meter.",
		Fields: map[string]string{
			"financeUsageView.currency":   "Currency is always \"usd\".",
			"financeUsageView.end":        "End is the window's closing instant, RFC3339.",
			"financeUsageView.lines":      "Lines is the same spend broken out per charge tag.",
			"financeUsageView.series":     "Series is the spend over time, oldest bucket first.",
			"financeUsageView.start":      "Start is the window's opening instant, RFC3339.",
			"financeUsageView.totalCents": "TotalCents is the metered spend across the whole window.",
			"financeWindow.range":         "Range is how far back to look: 24h, 7d, 30d or 90d. Anything else, including\nan absent value, means 30d.",
			"usageLine.cents":             "Cents is the line's total spend.",
			"usageLine.label":             "Label is the charge tag the spend was grouped under, else \"Usage\".",
			"usageLine.tokens":            "Tokens is the token count when the upstream reports one.",
			"usageLine.units":             "Units is how many charges made up the line.",
			"usagePoint.cents":            "Cents is the spend inside that bucket.",
			"usagePoint.date":             "Date is the bucket's start, RFC3339 — hourly over a 24h range, else daily.",
		},
		Example:  json.RawMessage(`{"range":"7d"}`),
		Response: json.RawMessage(`{"totalCents":1240,"currency":"usd","start":"2026-07-22T18:00:00Z","end":"2026-07-29T18:00:00Z","series":[{"date":"2026-07-22T00:00:00Z","cents":400}],"lines":[{"label":"ai","units":12,"cents":1240}]}`),
	})
}
