// Code generated by zipdoc; DO NOT EDIT.

package graph

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/indexers", zip.Doc{
		Description: "listIndexers reports the deployment's chain indexer(s) and how far each has indexed.\n\nIdentity and health come from the indexer's /health; the latest indexed block\n(height and time) from /v1/explorer/blocks.\nThe row EXISTS if EITHER call reaches the indexer; when the indexer is entirely\nunreachable it degrades to an honest-EMPTY list (200), not a console-error 502.\nNo chain HEAD is exposed by the indexer REST, so `lag` is honestly omitted\nrather than fabricated.",
		Fields: map[string]string{
			"Indexer.chain":        "Chain is the indexed chain's name as the indexer reports it.",
			"Indexer.height":       "Height is the latest indexed block height; empty when nothing is indexed yet.",
			"Indexer.id":           "ID identifies the indexer — its chain name, else chain id, else the brand.",
			"Indexer.lag":          "Lag is always absent: the indexer REST exposes no chain HEAD, so lag is not\nderivable and is omitted rather than invented.",
			"Indexer.network":      "Network is the deployment's network tier: mainnet, testnet or devnet.",
			"Indexer.status":       "Status is active, or degraded when /health explicitly reports unhealthy.",
			"Indexer.updatedAt":    "UpdatedAt is the latest indexed block's time, RFC3339 UTC.",
			"IndexerList.indexers": "Indexers is one row per chain indexer this deployment is wired to; empty\nwhen no indexer is reachable, never a fabricated row.",
		},
		Response: json.RawMessage(`{"indexers":[{"id":"Lux C-Chain","chain":"Lux C-Chain","network":"mainnet","height":"12345","status":"active","updatedAt":"2024-04-01T00:00:00Z"}]}`),
	})
	zip.Describe("GET /v1/oracles", zip.Doc{
		Description: "listOracles reports the on-chain price and data oracles the graph indexes.\n\nThe source is luxfi/graph's O-Chain PriceFeed registry — a REAL registry, the\ngraph's own oracle resolver.\nA reachable graph with no feeds returns an honest empty list; an unreachable or\nerroring graph likewise degrades to an honest-EMPTY list (200), not a 502.\nNo feed is ever fabricated.",
		Fields: map[string]string{
			"Oracle.feed":        "Feed is the trading pair the oracle prices, e.g. LUX/USD.",
			"Oracle.id":          "ID identifies the feed — its upstream id, else the trading pair.",
			"Oracle.name":        "Name is the human label for the feed, normally the trading pair.",
			"Oracle.source":      "Source is the oracle network the feed originates from.",
			"Oracle.status":      "Status is active for any listed feed.",
			"Oracle.updatedAt":   "UpdatedAt is the feed's own timestamp, RFC3339 UTC.",
			"Oracle.value":       "Value is the feed's latest price, verbatim from the graph.",
			"OracleList.oracles": "Oracles is one row per on-chain price feed; empty when the graph is\nunreachable or carries no feeds, never a fabricated feed.",
		},
		Response: json.RawMessage(`{"oracles":[{"id":"LUX-USD","name":"LUX/USD","feed":"LUX/USD","value":"2.50","source":"O-Chain","status":"active","updatedAt":"2024-04-01T00:00:00Z"}]}`),
	})
}
