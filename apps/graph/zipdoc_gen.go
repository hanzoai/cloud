// Code generated by zipdoc; DO NOT EDIT.

package graph

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/graph", zip.Doc{
		Fields: map[string]string{
			"graphReadIn.as_of":       "AsOf bounds the read to what was knowable at an instant, RFC 3339. Absent\nreads everything this plane holds.",
			"graphReadIn.entity":      "Entity narrows to what was asserted ABOUT one entity. Absent matches every\nentity.",
			"graphReadIn.limit":       "Limit caps how many assertions come back. Absent, zero, or anything above\nthe walk ceiling is the ceiling.",
			"graphReadIn.relation":    "Relation narrows to one relation. Absent matches every relation.",
			"graphReadIn.value":       "Value narrows to assertions pointing AT one value, which is how the edges\ninto an entity are read.",
			"graphReadOut.assertions": "Assertions are the matching rows in the order they were written, oldest\nfirst. Every version is here: this read resolves nothing and withholds\nnothing, so a superseded claim and the one that superseded it both appear.",
			"wireFact.at":             "At is when the thing was so, RFC 3339, as the asserter gave it.",
			"wireFact.by":             "By is the identity that filed it — `owner` or `owner/user` — stamped from\nthe validated principal at the write, never from the body.",
			"wireFact.confidence":     "Confidence in [0,1] as the asserter gave it; absent is 0. It breaks a tie\nbetween two assertions equally knowable and decides nothing else.",
			"wireFact.entity":         "Entity is the thing described, in the organization's own namespace.",
			"wireFact.evidence":       "Evidence points at the record the claim came from. Absent when the asserter\ngave none.",
			"wireFact.id":             "ID is the assertion's content address, minted by the server from what was\nasserted. Two callers who assert the identical thing land on one ID and one\nrow; changing any asserted field makes a different ID and a second row.",
			"wireFact.knowable":       "Knowable is the first instant this plane could have answered with the\nassertion, RFC 3339: the later of Seen and the server's clock at the write.\nDerived and never supplied, which is what stops history filed today from\nbeing backdated into a past read.",
			"wireFact.names":          "Names true means the assertion is an edge and Value is an entity. A walk\nreads only these.",
			"wireFact.relation":       "Relation is what was asserted of it.",
			"wireFact.seen":           "Seen is when the asserter says it became knowable, RFC 3339. Provenance\nonly — Knowable is what an as-of read is bounded by.",
			"wireFact.source":         "Source names who asserted, as the caller gave it. This plane ranks no\nsource above another, so it never outweighs a later Knowable.",
			"wireFact.value":          "Value is what the relation points at: another entity's key when Names is\ntrue, otherwise a scalar.",
		},
	})
	zip.Describe("GET /v1/graph/search", zip.Doc{
		Description: "Finds assertions by their text where read finds them by their keys.\n\nIt is the READ with one more term, not a second way to leave the store: same\norder, same ceiling, same tenancy, and searching composes with narrowing by\nrelation and by instant because all of them are terms of one filter.\n\nIt resolves nothing. What matches is what was asserted, including claims that\nwere later corrected — which is the honest answer to \"where is this mentioned\"\nand the reason the caller then asks resolve about what it found.",
		Fields: map[string]string{
			"graphReadOut.assertions": "Assertions are the matching rows in the order they were written, oldest\nfirst. Every version is here: this read resolves nothing and withholds\nnothing, so a superseded claim and the one that superseded it both appear.",
			"graphSearchIn.as_of":     "AsOf bounds the search to what was knowable at an instant, RFC 3339. Absent\nsearches everything this plane holds.",
			"graphSearchIn.limit":     "Limit caps how many assertions come back. Absent, zero, or anything above\nthe walk ceiling is the ceiling.",
			"graphSearchIn.q":         "Q is what to look for: words, matched as prefixes, all of them required.\nPunctuation is text here rather than syntax, so an entity key searches as\nitself.",
			"graphSearchIn.relation":  "Relation narrows to one relation. Absent matches every relation.",
			"wireFact.at":             "At is when the thing was so, RFC 3339, as the asserter gave it.",
			"wireFact.by":             "By is the identity that filed it — `owner` or `owner/user` — stamped from\nthe validated principal at the write, never from the body.",
			"wireFact.confidence":     "Confidence in [0,1] as the asserter gave it; absent is 0. It breaks a tie\nbetween two assertions equally knowable and decides nothing else.",
			"wireFact.entity":         "Entity is the thing described, in the organization's own namespace.",
			"wireFact.evidence":       "Evidence points at the record the claim came from. Absent when the asserter\ngave none.",
			"wireFact.id":             "ID is the assertion's content address, minted by the server from what was\nasserted. Two callers who assert the identical thing land on one ID and one\nrow; changing any asserted field makes a different ID and a second row.",
			"wireFact.knowable":       "Knowable is the first instant this plane could have answered with the\nassertion, RFC 3339: the later of Seen and the server's clock at the write.\nDerived and never supplied, which is what stops history filed today from\nbeing backdated into a past read.",
			"wireFact.names":          "Names true means the assertion is an edge and Value is an entity. A walk\nreads only these.",
			"wireFact.relation":       "Relation is what was asserted of it.",
			"wireFact.seen":           "Seen is when the asserter says it became knowable, RFC 3339. Provenance\nonly — Knowable is what an as-of read is bounded by.",
			"wireFact.source":         "Source names who asserted, as the caller gave it. This plane ranks no\nsource above another, so it never outweighs a later Knowable.",
			"wireFact.value":          "Value is what the relation points at: another entity's key when Names is\ntrue, otherwise a scalar.",
		},
	})
	zip.Describe("GET /v1/graph/vocabulary", zip.Doc{
		Fields: map[string]string{
			"graphVocabularyOut.bound":     "Bound is the ceiling on one walk.",
			"graphVocabularyOut.relations": "Relations is what this organization has actually asserted, which is the\nonly vocabulary there is: this plane declares none of its own.",
			"graphVocabularyOut.rule":      "Rule names the terms of the precedence order, in the order they apply. A\nreader who is told a winner without the rule cannot check it.",
		},
	})
	zip.Describe("POST /v1/graph", zip.Doc{
		Fields: map[string]string{
			"graphAssertIn.assertions": "Assertions is the batch. Each member is judged on its own: one refusal\ndoes not discard the rest, because a caller redelivering five facts must\nnot lose four of them to one malformed fifth.",
			"graphAssertOut.duplicate": "Duplicate is how many members this plane already held. A redelivery\ncollides on its content address and is counted here, not refused: it is\nthe success a retrying caller depends on.",
			"graphAssertOut.reasons":   "Reasons names why each refused member was refused, in the order sent.",
			"graphAssertOut.recorded":  "Recorded is how many members became new rows.",
			"graphAssertOut.refused":   "Refused is how many members were turned away on arrival, before the store\nwas touched — a missing entity, a timestamp that is not RFC 3339, a\nconfidence outside [0,1]. The rest of the batch was still recorded.",
			"graphFact.at":             "At is when the thing was so, RFC 3339. Required, and refused when it sits\nmore than five minutes ahead of the server clock — an assertion dated\nfurther out would never mature and would skew every read until it did.",
			"graphFact.confidence":     "Confidence in [0,1]. A tie-breaker within the order, never a substitute\nfor it. Absent is 0, the weakest an assertion can be.",
			"graphFact.entity":         "Entity is the thing being described, in the organization's own namespace.\nIt is not created: an entity exists because something was asserted about it.\nRequired, 512 bytes at most.",
			"graphFact.evidence":       "Evidence points at the record this claim came from, 512 bytes at most. An\nassertion without one is admitted and carries no defence.",
			"graphFact.names":          "Names says the value is an entity. A walk reads only the edges, so this is\na declaration and never a guess about the value's shape.",
			"graphFact.relation":       "Relation is what is being asserted — `depends`, `owner`, `same`, `title`.\nIt is open: this plane holds no vocabulary of its own. Required, 128 bytes\nat most.",
			"graphFact.seen":           "Seen is when this assertion became knowable, RFC 3339. Defaults to At and\nmay not precede it. It is provenance and it decides nothing: the instant\nevery read uses is derived as the later of Seen and the server's own clock.",
			"graphFact.source":         "Source names who asserted. Required, because an assertion nobody is named\nfor cannot be weighed against one that is. Open text: this plane ranks no\nsource above another.",
			"graphFact.value":          "Value is what the relation points at. When Names is true it is another\nentity's key and the assertion is an EDGE; otherwise it is a scalar and\nthe assertion is a property. 2048 bytes at most, or 512 when it names an\nentity.",
		},
	})
	zip.Describe("POST /v1/graph/graphql", zip.Doc{
		Description: "Is the endpoint. It answers 200 with a GraphQL error list for a query\nthat cannot run, which is the wire every GraphQL client parses — a transport\nerror would be read as the server being down rather than the query being\nwrong.",
	})
	zip.Describe("POST /v1/graph/neighbors", zip.Doc{
		Fields: map[string]string{
			"graphNeighborsIn.as_of":      "AsOf walks the graph as it stood at an instant, RFC 3339. Absent walks it\nas it stands now.",
			"graphNeighborsIn.depth":      "Depth is how many hops. Absent is one.",
			"graphNeighborsIn.direction":  "Direction is out, in or both. Out follows an edge from its entity to its\nvalue — what the node points at; in follows it the other way — what points\nat the node; both is the union of the two, not a third rule. Absent is out.",
			"graphNeighborsIn.relation":   "Relation narrows the walk to one edge relation. Absent follows all. Only\nedges are ever followed: an assertion whose value is a scalar is a property\nand is never a hop.",
			"graphNeighborsIn.seeds":      "Seeds is where the walk starts. At least one.",
			"graphNeighborsOut.bound":     "Bound is the ceiling this walk was held to, the same for every caller, so\nTruncated can be read against a number rather than guessed at.",
			"graphNeighborsOut.depth":     "Depth is the deepest hop count actually reached. It is at most the depth\nasked for, and smaller when the walk ran out of edges first.",
			"graphNeighborsOut.entities":  "Entities is everything reached, the seeds included, ordered by the fewest\nhops that reach each one and then by key.",
			"graphNeighborsOut.truncated": "Truncated says the bound stopped the walk. The bound is part of the answer\nrather than a silent short read.",
		},
	})
	zip.Describe("POST /v1/graph/resolve", zip.Doc{
		Fields: map[string]string{
			"graphResolveIn.as_of":      "AsOf is the instant to answer at, RFC 3339. Absent means now.",
			"graphResolveIn.entity":     "Entity is the thing to answer about. Required.",
			"graphResolveIn.relation":   "Relation is the one relation to settle. Required: this answers a single\n(entity, relation) pair, never a whole entity at once.",
			"graphResolveOut.as_of":     "AsOf is the instant this answer was taken at, RFC 3339: the one asked for,\nor the server's clock when none was.",
			"graphResolveOut.conflicts": "Conflicts is every OTHER assertion knowable at AsOf, strongest first. They\nare not all disagreements: one that repeats the winner's value ranks below\nit and is listed here too.",
			"graphResolveOut.contested": "Contested is true when at least one conflict claims a value different from\nthe winner's. Any number of conflicts that all agree leaves it false.",
			"graphResolveOut.entity":    "Entity is the entity the question named, echoed so a stored answer still\nsays what it is about.",
			"graphResolveOut.known":     "Known is false when this plane held nothing knowable at AsOf. That is an\nanswer, not an error.",
			"graphResolveOut.relation":  "Relation is the relation the question named, echoed for the same reason.",
			"graphResolveOut.truncated": "Truncated says this pair holds more assertions than one read returns, so\nthe winner was decided from the most recent ceiling-full of them. It is\nreported because a provenance plane that trims silently is a plane that\nanswers confidently and wrongly; narrow the question with as_of to see\nwhat it dropped.",
			"graphResolveOut.winner":    "Winner is the assertion in force — the strongest of those knowable at AsOf\nunder the order `rule` names. Absent exactly when Known is false.",
			"wireFact.at":               "At is when the thing was so, RFC 3339, as the asserter gave it.",
			"wireFact.by":               "By is the identity that filed it — `owner` or `owner/user` — stamped from\nthe validated principal at the write, never from the body.",
			"wireFact.confidence":       "Confidence in [0,1] as the asserter gave it; absent is 0. It breaks a tie\nbetween two assertions equally knowable and decides nothing else.",
			"wireFact.entity":           "Entity is the thing described, in the organization's own namespace.",
			"wireFact.evidence":         "Evidence points at the record the claim came from. Absent when the asserter\ngave none.",
			"wireFact.id":               "ID is the assertion's content address, minted by the server from what was\nasserted. Two callers who assert the identical thing land on one ID and one\nrow; changing any asserted field makes a different ID and a second row.",
			"wireFact.knowable":         "Knowable is the first instant this plane could have answered with the\nassertion, RFC 3339: the later of Seen and the server's clock at the write.\nDerived and never supplied, which is what stops history filed today from\nbeing backdated into a past read.",
			"wireFact.names":            "Names true means the assertion is an edge and Value is an entity. A walk\nreads only these.",
			"wireFact.relation":         "Relation is what was asserted of it.",
			"wireFact.seen":             "Seen is when the asserter says it became knowable, RFC 3339. Provenance\nonly — Knowable is what an as-of read is bounded by.",
			"wireFact.source":           "Source names who asserted, as the caller gave it. This plane ranks no\nsource above another, so it never outweighs a later Knowable.",
			"wireFact.value":            "Value is what the relation points at: another entity's key when Names is\ntrue, otherwise a scalar.",
		},
	})
}
