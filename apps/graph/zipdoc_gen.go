// Code generated by zipdoc; DO NOT EDIT.

package graph

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/indexers", zip.Doc{
		Description: "Reports the deployment's chain indexer(s) and how far each has\nindexed. Identity and health come from the indexer's /health; the latest indexed\nblock (height + time) from its /v1/explorer/blocks. The row EXISTS if EITHER call\nreaches the indexer; when the indexer is entirely unreachable the answer degrades\nto an honest-EMPTY list at 200, not a 502. No chain HEAD is exposed by the indexer\nREST, so `lag` is honestly omitted rather than fabricated.",
		Fields: map[string]string{
			"indexerView.chain":     "Chain is the chain this indexer indexes, as the indexer names it.",
			"indexerView.height":    "Height is the latest INDEXED block height, as a decimal string. Absent when\nnothing has been indexed yet.",
			"indexerView.id":        "ID identifies the indexer: its chain name, else its chain id, else the brand.",
			"indexerView.lag":       "Lag is how far behind the chain HEAD this indexer is. The indexer REST does not\nexpose the head, so it is always absent rather than a fabricated zero.",
			"indexerView.network":   "Network is the deployment's network tier: mainnet, testnet or devnet.",
			"indexerView.status":    "Status is \"degraded\" when /health explicitly reports unhealthy, else \"active\".",
			"indexerView.updatedAt": "UpdatedAt is the latest indexed block's timestamp, RFC 3339 UTC.",
			"indexersOut.indexers":  "Indexers is one row per reachable chain indexer, or an empty list when the\nindexer is unreachable — never a fabricated row.",
		},
	})
	zip.Describe("GET /v1/oracles", zip.Doc{
		Description: "Reports the on-chain price/data oracles from the graph's O-Chain\nPriceFeed registry. A reachable graph with no feeds answers an honest empty list;\nan unreachable or erroring graph likewise degrades to an empty list at 200 rather\nthan a 502, so the console never error-toasts. No feed is ever fabricated.",
		Fields: map[string]string{
			"oracleView.feed":      "Feed is the trading pair this feed prices.",
			"oracleView.id":        "ID is the feed's own id, else its trading pair.",
			"oracleView.name":      "Name is the feed's display name — the trading pair, e.g. \"LUX/USD\".",
			"oracleView.source":    "Source is the oracle network the feed originates from; \"O-Chain\" by default.",
			"oracleView.status":    "Status is \"active\" for a listed feed.",
			"oracleView.updatedAt": "UpdatedAt is the feed's own timestamp, RFC 3339 UTC.",
			"oracleView.value":     "Value is the feed's price, verbatim as the registry carries it.",
			"oraclesOut.oracles":   "Oracles is one row per on-chain price feed, or an empty list when the graph is\nunreachable or carries none — never a fabricated feed.",
		},
	})
}
