// Code generated by zipdoc; DO NOT EDIT.

package security

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/security/findings", zip.Doc{
		Description: "listFindings returns the caller org's findings, optionally narrowed by scan and severity.",
		Fields: map[string]string{
			"Finding.createdAt":        "CreatedAt is the finding's scan time, unix milliseconds.",
			"Finding.fingerprint":      "Fingerprint is the SHA-256 of the raw secret, for dedupe across scans.",
			"Finding.id":               "ID is the finding id.",
			"Finding.line":             "Line is the 1-based line the secret was found on.",
			"Finding.path":             "Path is the file the secret was found in.",
			"Finding.preview":          "Preview is the masked excerpt; it never carries the secret.",
			"Finding.ruleId":           "RuleID is the detection rule that fired.",
			"Finding.ruleName":         "RuleName is that rule's human name.",
			"Finding.scanId":           "ScanID is the scan this finding came from.",
			"Finding.severity":         "Severity is critical, high, medium or low.",
			"FindingList.data":         "Data is the findings, worst severity first.",
			"FindingQuery.limit":       "Limit caps the rows returned; 0 means the store's default.",
			"FindingQuery.minSeverity": "MinSeverity drops anything below it: critical, high, medium or low.",
			"FindingQuery.scanId":      "ScanID restricts the listing to one scan.",
		},
		Response: json.RawMessage(`{"data":[{"id":"fnd_1","scanId":"scan_9f2c","ruleId":"aws-access-key","ruleName":"AWS Access Key ID","severity":"critical","path":"config.py","line":1,"preview":"aws_key = \"AKIA****\"","fingerprint":"9c1185a5c5e9fc54","createdAt":1780000000000}]}`),
	})
	zip.Describe("GET /v1/security/findings/:id", zip.Doc{
		Description: "getFinding returns one of the caller org's findings, redacted.\n\nA finding belonging to another org answers 404, never a peek.",
		Fields: map[string]string{
			"Finding.createdAt":   "CreatedAt is the finding's scan time, unix milliseconds.",
			"Finding.fingerprint": "Fingerprint is the SHA-256 of the raw secret, for dedupe across scans.",
			"Finding.id":          "ID is the finding id.",
			"Finding.line":        "Line is the 1-based line the secret was found on.",
			"Finding.path":        "Path is the file the secret was found in.",
			"Finding.preview":     "Preview is the masked excerpt; it never carries the secret.",
			"Finding.ruleId":      "RuleID is the detection rule that fired.",
			"Finding.ruleName":    "RuleName is that rule's human name.",
			"Finding.scanId":      "ScanID is the scan this finding came from.",
			"Finding.severity":    "Severity is critical, high, medium or low.",
			"FindingRef.id":       "ID is the finding id.",
		},
		Example:  json.RawMessage(`{"id":"fnd_1"}`),
		Response: json.RawMessage(`{"id":"fnd_1","scanId":"scan_9f2c","ruleId":"aws-access-key","ruleName":"AWS Access Key ID","severity":"critical","path":"config.py","line":1,"preview":"aws_key = \"AKIA****\"","fingerprint":"9c1185a5c5e9fc54","createdAt":1780000000000}`),
	})
	zip.Describe("GET /v1/security/health", zip.Doc{
		Description: "health reports whether the scanner is up and how many rules it loaded.\n\nIt is fail-open: the subsystem has no external dependency, so it reports ok\nwhenever the store opened.\nRegistered before the generic liveness route so the real probe is not shadowed.",
		Fields: map[string]string{
			"Health.rules":  "Rules is the number of detection rules loaded.",
			"Health.status": "Status is ok whenever the findings store opened.",
		},
		Response: json.RawMessage(`{"status":"ok","rules":42}`),
	})
	zip.Describe("GET /v1/security/rules", zip.Doc{
		Description: "listRules returns the secret-detection rule catalog.\n\nThere is no tenant scope: the catalog is the same for every caller and\ndiscloses nothing tenant-specific.",
		Fields: map[string]string{
			"RuleList.data": "Data is the rule catalog, worst severity first.",
		},
		Response: json.RawMessage(`{"data":[{"id":"aws-access-key","name":"AWS Access Key ID","severity":"critical","description":"AWS access key identifier"}]}`),
	})
	zip.Describe("GET /v1/security/scans", zip.Doc{
		Description: "listScans returns the caller org's scans, most recent first.",
		Fields: map[string]string{
			"ScanList.data":         "Data is the scan summaries, most recent first.",
			"ScanQuery.limit":       "Limit caps the rows returned; 0 means the store's default.",
			"ScanSummary.createdAt": "CreatedAt is the scan time, unix milliseconds.",
			"ScanSummary.critical":  "Critical counts the critical-severity findings.",
			"ScanSummary.files":     "Files counts the files submitted.",
			"ScanSummary.findings":  "Findings counts every finding across all severities.",
			"ScanSummary.high":      "High counts the high-severity findings.",
			"ScanSummary.id":        "ID is the scan id, the key for its findings.",
			"ScanSummary.low":       "Low counts the low-severity findings.",
			"ScanSummary.medium":    "Medium counts the medium-severity findings.",
			"ScanSummary.project":   "Project is the sub-scope the scan ran under, absent at org level.",
		},
		Response: json.RawMessage(`{"data":[{"id":"scan_9f2c","files":1,"findings":1,"critical":1,"high":0,"medium":0,"low":0,"createdAt":1780000000000}]}`),
	})
	zip.Describe("GET /v1/security/scans/:id", zip.Doc{
		Description: "getScan returns one of the caller org's scans with its findings.\n\nThe findings ride along so the detail view is one round-trip; a scan belonging\nto another org answers 404, never a peek.",
		Fields: map[string]string{
			"Finding.createdAt":     "CreatedAt is the finding's scan time, unix milliseconds.",
			"Finding.fingerprint":   "Fingerprint is the SHA-256 of the raw secret, for dedupe across scans.",
			"Finding.id":            "ID is the finding id.",
			"Finding.line":          "Line is the 1-based line the secret was found on.",
			"Finding.path":          "Path is the file the secret was found in.",
			"Finding.preview":       "Preview is the masked excerpt; it never carries the secret.",
			"Finding.ruleId":        "RuleID is the detection rule that fired.",
			"Finding.ruleName":      "RuleName is that rule's human name.",
			"Finding.scanId":        "ScanID is the scan this finding came from.",
			"Finding.severity":      "Severity is critical, high, medium or low.",
			"ScanDetail.findings":   "Findings are every finding this scan produced, redacted.",
			"ScanDetail.scan":       "Scan is the scan's summary and severity tally.",
			"ScanRef.id":            "ID is the scan id returned by submit.",
			"ScanSummary.createdAt": "CreatedAt is the scan time, unix milliseconds.",
			"ScanSummary.critical":  "Critical counts the critical-severity findings.",
			"ScanSummary.files":     "Files counts the files submitted.",
			"ScanSummary.findings":  "Findings counts every finding across all severities.",
			"ScanSummary.high":      "High counts the high-severity findings.",
			"ScanSummary.id":        "ID is the scan id, the key for its findings.",
			"ScanSummary.low":       "Low counts the low-severity findings.",
			"ScanSummary.medium":    "Medium counts the medium-severity findings.",
			"ScanSummary.project":   "Project is the sub-scope the scan ran under, absent at org level.",
		},
		Example:  json.RawMessage(`{"id":"scan_9f2c"}`),
		Response: json.RawMessage(`{"scan":{"id":"scan_9f2c","files":1,"findings":1,"critical":1,"high":0,"medium":0,"low":0,"createdAt":1780000000000},"findings":[]}`),
	})
	zip.Describe("POST /v1/security/scans", zip.Doc{
		Description: "submitScan scans the submitted files for secrets and records the redacted findings.\n\nThe raw content is scanned in memory and never stored; only the masked preview\nand the secret's fingerprint are persisted.\nOne scan is one metered unit, and the scan's tally is written to the audit log.",
		Fields: map[string]string{
			"ScanRequest.files":     "Files are the files to scan; at least one, at most 500, 8 MiB of content\nin total.",
			"ScanRequest.project":   "Project is the optional sub-scope within the org; defaults to the caller's\nX-Project-Id, and must match ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$.",
			"ScanSummary.createdAt": "CreatedAt is the scan time, unix milliseconds.",
			"ScanSummary.critical":  "Critical counts the critical-severity findings.",
			"ScanSummary.files":     "Files counts the files submitted.",
			"ScanSummary.findings":  "Findings counts every finding across all severities.",
			"ScanSummary.high":      "High counts the high-severity findings.",
			"ScanSummary.id":        "ID is the scan id, the key for its findings.",
			"ScanSummary.low":       "Low counts the low-severity findings.",
			"ScanSummary.medium":    "Medium counts the medium-severity findings.",
			"ScanSummary.project":   "Project is the sub-scope the scan ran under, absent at org level.",
			"SourceFile.content":    "Content is the file's raw text. It is scanned in memory and never persisted.",
			"SourceFile.path":       "Path is the file's path, reported verbatim on any finding it produces.",
		},
		Example:  json.RawMessage(`{"project":"api","files":[{"path":"config.py","content":"aws_key = \"AKIAIOSFODNN7EXAMPLE\""}]}`),
		Response: json.RawMessage(`{"id":"scan_9f2c","project":"api","files":1,"findings":1,"critical":1,"high":0,"medium":0,"low":0,"createdAt":1780000000000}`),
	})
}
