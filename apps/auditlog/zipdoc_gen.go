// Code generated by zipdoc; DO NOT EDIT.

package auditlog

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/audit", zip.Doc{
		Description: "List reads the caller's OWN org audit trail, newest first, with the total the\nfilter matched so a console can page it.\n\nEvery filter is optional and applies WITHIN the caller's org — the org itself is\nthe validated principal's and can never be widened by a request. Fails closed:\nan absent principal is a true \"not signed in\" (401), and a deployment with no\nlocal tamper-evident store answers an honest 501 rather than silently serving\nsomebody else's trail.",
		Fields: map[string]string{
			"Wire.home":             "Home is present ONLY on a cross-org action: the org the actor came FROM,\nwhile Org is the org they acted IN. A console row carrying `home` is a\nplatform-admin impersonation and should be rendered as one.",
			"trailPage.data":        "Data is one page of the org's events, newest first. Empty, never null.",
			"trailPage.msg":         "Msg is the envelope's message slot, empty on success.",
			"trailPage.status":      "Status is the envelope's status slot, \"ok\" on success.",
			"trailPage.total":       "Total is how many events match the filter, across all pages — what a pager\nneeds to size itself.",
			"trailQuery.action":     "Action narrows it to one action name, e.g. \"machine.create\".",
			"trailQuery.p":          "Page is the 1-based page number, driving the offset. Anything below 2 reads\nthe first page.",
			"trailQuery.pageSize":   "PageSize is rows per page, default 100. A value that is not a positive\ninteger falls back to the default.",
			"trailQuery.resource":   "Resource narrows it to one resource TYPE, e.g. \"apikey\".",
			"trailQuery.resourceId": "ResourceID narrows it to one resource instance.",
			"trailQuery.result":     "Result narrows it to one outcome: \"success\", \"deny\" or \"error\".",
			"trailQuery.since":      "Since is the inclusive lower time bound, RFC3339. An unparseable value is\nignored rather than refused — one malformed filter must not hide the trail.",
			"trailQuery.sub":        "Sub narrows the trail to one actor — the validated subject that made the\nrequest. Blank means every actor in the org.",
			"trailQuery.until":      "Until is the upper time bound, RFC3339, with the same tolerance.",
		},
		Example: json.RawMessage(`{"action":"machine.create","result":"success","since":"2026-07-01T00:00:00Z","pageSize":"50"}`),
	})
}
