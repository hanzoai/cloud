// Code generated by zipdoc; DO NOT EDIT.

package auditlog

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/audit", zip.Doc{
		Description: "list reads the caller's OWN org audit trail, newest first. Every row carries its\nown hash-chain linkage.\n\nThe org is the validated principal's, pinned server-side, so a caller can only\never read its own tenant. Answers 501 when no local tamper-evident store is\nconfigured, rather than falling back to a different trail.",
		Fields: map[string]string{
			"Trail.data":            "Data is the page of records, newest first. Never null; [] when the org has no\nmatching events.",
			"Trail.data2":           "Data2 is the total number of matching records, before paging.",
			"Trail.msg":             "Msg is empty on a served read.",
			"Trail.status":          "Status is \"ok\" on a served read.",
			"TrailQuery.action":     "Action restricts it to one action name, e.g. \"kms.secret.read\".",
			"TrailQuery.p":          "Page is the 1-based page number, driving the offset.",
			"TrailQuery.pageSize":   "PageSize is rows per page; absent or non-positive means 100.",
			"TrailQuery.resource":   "Resource restricts it to one resource kind, e.g. \"secret\".",
			"TrailQuery.resourceId": "ResourceID restricts it to one resource instance.",
			"TrailQuery.result":     "Result restricts it to \"success\", \"deny\" or \"error\".",
			"TrailQuery.since":      "Since is the inclusive lower time bound, RFC3339. An unparseable value is\nignored rather than refused — one malformed filter must not hide the trail.",
			"TrailQuery.sub":        "Sub restricts the trail to one actor — the validated subject that made the\nrequest.",
			"TrailQuery.until":      "Until is the upper time bound, RFC3339, with the same tolerance.",
		},
		Example:  json.RawMessage(`{"action":"kms.secret.read","result":"deny","since":"2026-07-01T00:00:00Z","pageSize":50}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"seq":41,"time":"2026-07-26T18:00:00.123456789Z","org":"acme","sub":"z@hanzo.ai","action":"kms.secret.read","resource":"secret","result":"deny","status":403,"isAdmin":false,"hash":"9f2c","prevHash":"41ab"}],"data2":1}`),
	})
}
