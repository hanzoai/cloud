// Code generated by zipdoc; DO NOT EDIT.

package mq

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/mq/streams/:name", zip.Doc{
		Description: "Removes a stream with all its messages and consumers. Irreversible.",
		Fields: map[string]string{
			"nameIn.name": "Name is the stream name, from the path.",
		},
	})
	zip.Describe("DELETE /v1/mq/streams/:name/messages/:seq", zip.Doc{
		Description: "Erases one message by sequence; the sequence gap remains.",
		Fields: map[string]string{
			"seqIn.name": "Name is the stream name, from the path.",
			"seqIn.seq":  "Seq is the message's stream sequence, from the path.",
		},
	})
	zip.Describe("DELETE /v1/mq/streams/:stream/consumers/:name", zip.Doc{
		Description: "Removes a consumer and its delivery state; unacknowledged messages\nstay in the stream.",
		Fields: map[string]string{
			"twoIn.name":   "Name is the consumer name, from the path.",
			"twoIn.stream": "Stream is the stream name, from the path.",
		},
	})
	zip.Describe("GET /v1/mq/health", zip.Doc{
		Description: "Reports whether the message plane behind this surface answers.",
		Fields: map[string]string{
			"Health.status":  "Status is ok when the message plane answers, degraded otherwise.",
			"Health.uptime":  "Uptime is how long this surface has been mounted.",
			"Health.version": "Version is the connected broker's server version; empty while degraded.",
		},
	})
	zip.Describe("GET /v1/mq/info", zip.Doc{
		Description: "Returns the broker's identity and the org's stream count.",
		Fields: map[string]string{
			"infoOut.jetstream":   "JetStream is true when durable streams are enabled.",
			"infoOut.max_payload": "MaxPayload is the broker's message-size ceiling in bytes.",
			"infoOut.server_id":   "Server is the broker's server id.",
			"infoOut.server_name": "Name is the broker's server name.",
			"infoOut.streams":     "Streams is the org's stream count.",
			"infoOut.version":     "Version is the broker's server version.",
		},
	})
	zip.Describe("GET /v1/mq/streams", zip.Doc{
		Description: "Returns the org's streams, name-ordered, with their live state.",
		Fields: map[string]string{
			"Config.max_age":       "MaxAge caps message age, e.g. \"24h\" or \"7d\"; \"0\" (default) is unlimited.",
			"Config.max_bytes":     "MaxBytes caps the stream's total stored bytes; -1 (default) is unlimited.",
			"Config.max_msg_size":  "MaxMsgSize caps one message's size in bytes; -1 (default) is the broker's limit.",
			"Config.max_msgs":      "MaxMsgs caps the number of stored messages; -1 (default) is unlimited.",
			"Config.name":          "Name is the stream name, unique within the org (alphanumeric, hyphens, underscores).",
			"Config.num_replicas":  "Replicas is the number of stream replicas (1–5); this plane runs 1.",
			"Config.retention":     "Retention is the retention policy: limits (default), interest, or workqueue.",
			"Config.storage":       "Storage is the storage backend: file (default) or memory.",
			"Config.subjects":      "Subjects are the org-relative subjects bound to this stream (wildcards supported). Default: the stream name.",
			"State.bytes":          "Bytes is the total stored size.",
			"State.consumer_count": "Consumers is the number of consumers attached to this stream.",
			"State.first_seq":      "FirstSeq is the sequence of the first stored message.",
			"State.first_ts":       "FirstTS is the timestamp of the first stored message.",
			"State.last_seq":       "LastSeq is the sequence of the last stored message.",
			"State.last_ts":        "LastTS is the timestamp of the last stored message.",
			"State.messages":       "Messages is the number of messages currently stored.",
			"State.num_deleted":    "Deleted is the number of deleted messages (sequence gaps).",
			"State.num_subjects":   "Subjects is the number of distinct subjects stored.",
			"Stream.config":        "Config is the stream's configuration.",
			"Stream.created":       "Created is when the stream was created.",
			"Stream.name":          "Name is the stream name within the org.",
			"Stream.state":         "State is the stream's current state.",
			"Streams.streams":      "Streams is the page, ordered by name.",
			"Streams.total":        "Total is the org's stream count before paging.",
			"listIn.limit":         "Limit caps the streams returned (1–1000, default 100).",
			"listIn.offset":        "Offset skips that many streams, name-ordered.",
		},
	})
	zip.Describe("GET /v1/mq/streams/:name", zip.Doc{
		Description: "Returns one stream's configuration and live state.",
		Fields: map[string]string{
			"Config.max_age":       "MaxAge caps message age, e.g. \"24h\" or \"7d\"; \"0\" (default) is unlimited.",
			"Config.max_bytes":     "MaxBytes caps the stream's total stored bytes; -1 (default) is unlimited.",
			"Config.max_msg_size":  "MaxMsgSize caps one message's size in bytes; -1 (default) is the broker's limit.",
			"Config.max_msgs":      "MaxMsgs caps the number of stored messages; -1 (default) is unlimited.",
			"Config.name":          "Name is the stream name, unique within the org (alphanumeric, hyphens, underscores).",
			"Config.num_replicas":  "Replicas is the number of stream replicas (1–5); this plane runs 1.",
			"Config.retention":     "Retention is the retention policy: limits (default), interest, or workqueue.",
			"Config.storage":       "Storage is the storage backend: file (default) or memory.",
			"Config.subjects":      "Subjects are the org-relative subjects bound to this stream (wildcards supported). Default: the stream name.",
			"State.bytes":          "Bytes is the total stored size.",
			"State.consumer_count": "Consumers is the number of consumers attached to this stream.",
			"State.first_seq":      "FirstSeq is the sequence of the first stored message.",
			"State.first_ts":       "FirstTS is the timestamp of the first stored message.",
			"State.last_seq":       "LastSeq is the sequence of the last stored message.",
			"State.last_ts":        "LastTS is the timestamp of the last stored message.",
			"State.messages":       "Messages is the number of messages currently stored.",
			"State.num_deleted":    "Deleted is the number of deleted messages (sequence gaps).",
			"State.num_subjects":   "Subjects is the number of distinct subjects stored.",
			"Stream.config":        "Config is the stream's configuration.",
			"Stream.created":       "Created is when the stream was created.",
			"Stream.name":          "Name is the stream name within the org.",
			"Stream.state":         "State is the stream's current state.",
			"nameIn.name":          "Name is the stream name, from the path.",
		},
	})
	zip.Describe("GET /v1/mq/streams/:name/messages", zip.Doc{
		Description: "Reads stored messages without a consumer: by sequence, by newest on a\nsubject, or walking a subject forward from a sequence.",
		Fields: map[string]string{
			"Delivery.data":          "Data is the payload, base64-encoded.",
			"Delivery.headers":       "Headers are the message headers, when any were published.",
			"Delivery.num_delivered": "Delivered is how many times a consumer has been handed this message (pulls only).",
			"Delivery.num_pending":   "Remaining is how many messages follow this one for the consumer (pulls only).",
			"Delivery.sequence":      "Sequence is the message's stream sequence.",
			"Delivery.subject":       "Subject is the org-relative subject the message was stored under.",
			"Delivery.timestamp":     "Timestamp is when the broker stored the message.",
			"readIn.last_by_subject": "LastBySubject reads the newest message on this org-relative subject.",
			"readIn.limit":           "Limit caps a next_by_subject walk (1–1000, default 100).",
			"readIn.name":            "Name is the stream name, from the path.",
			"readIn.next_by_subject": "NextBySubject walks forward from seq collecting messages on this org-relative subject (wildcards supported).",
			"readIn.seq":             "Seq reads the message at this sequence (with next_by_subject: the walk's start).",
			"readOut.messages":       "Messages is what was read, stream-ordered.",
		},
	})
	zip.Describe("GET /v1/mq/streams/:stream/consumers", zip.Doc{
		Description: "Returns a stream's consumers, name-ordered, with delivery state.",
		Fields: map[string]string{
			"Consumer.ack_floor":       "AckFloor is the highest contiguously acknowledged sequence pair.",
			"Consumer.config":          "Config is the consumer's configuration.",
			"Consumer.created":         "Created is when the consumer was created.",
			"Consumer.delivered":       "Delivered is the highest delivered sequence pair.",
			"Consumer.name":            "Name is the consumer name.",
			"Consumer.num_ack_pending": "AckPending is the number of delivered, not yet acknowledged messages.",
			"Consumer.num_pending":     "Pending is the number of messages yet to be delivered.",
			"Consumer.num_redelivered": "Redelivered is the number of messages currently being redelivered.",
			"Consumer.num_waiting":     "Waiting is the number of pull requests waiting for messages.",
			"Consumer.stream_name":     "Stream is the stream this consumer reads.",
			"Durable.ack_policy":       "Ack is the acknowledgment policy: explicit (default), all, or none.",
			"Durable.ack_wait":         "AckWait is how long the broker waits for an ack before redelivering, e.g. \"30s\" (default).",
			"Durable.deliver_policy":   "Deliver is where delivery starts: all (default), last, new, by_start_sequence, by_start_time, or last_per_subject.",
			"Durable.description":      "Description says what this consumer is for.",
			"Durable.durable_name":     "Name is the durable consumer name (alphanumeric, hyphens, underscores).",
			"Durable.filter_subject":   "Filter delivers only messages on this org-relative subject (wildcards supported).",
			"Durable.max_ack_pending":  "MaxAckPending caps unacknowledged messages in flight (default 1000).",
			"Durable.max_deliver":      "MaxDeliver caps delivery attempts per message; -1 (default) is unlimited.",
			"Durable.opt_start_seq":    "StartSeq is the starting sequence for deliver_policy by_start_sequence.",
			"Durable.opt_start_time":   "StartTime is the starting instant for deliver_policy by_start_time.",
			"Durable.replay_policy":    "Replay is the replay pacing: instant (default) or original.",
			"Sequences.consumer_seq":   "Consumer is the consumer's own sequence.",
			"Sequences.stream_seq":     "Stream is the corresponding stream sequence.",
			"pickIn.limit":             "Limit caps the consumers returned (1–1000, default 100).",
			"pickIn.offset":            "Offset skips that many consumers, name-ordered.",
			"pickIn.stream":            "Stream is the stream name, from the path.",
			"pickOut.consumers":        "Consumers is the page, ordered by name.",
			"pickOut.total":            "Total is the stream's consumer count before paging.",
		},
	})
	zip.Describe("GET /v1/mq/streams/:stream/consumers/:name", zip.Doc{
		Description: "Returns one consumer's configuration and delivery state.",
		Fields: map[string]string{
			"Consumer.ack_floor":       "AckFloor is the highest contiguously acknowledged sequence pair.",
			"Consumer.config":          "Config is the consumer's configuration.",
			"Consumer.created":         "Created is when the consumer was created.",
			"Consumer.delivered":       "Delivered is the highest delivered sequence pair.",
			"Consumer.name":            "Name is the consumer name.",
			"Consumer.num_ack_pending": "AckPending is the number of delivered, not yet acknowledged messages.",
			"Consumer.num_pending":     "Pending is the number of messages yet to be delivered.",
			"Consumer.num_redelivered": "Redelivered is the number of messages currently being redelivered.",
			"Consumer.num_waiting":     "Waiting is the number of pull requests waiting for messages.",
			"Consumer.stream_name":     "Stream is the stream this consumer reads.",
			"Durable.ack_policy":       "Ack is the acknowledgment policy: explicit (default), all, or none.",
			"Durable.ack_wait":         "AckWait is how long the broker waits for an ack before redelivering, e.g. \"30s\" (default).",
			"Durable.deliver_policy":   "Deliver is where delivery starts: all (default), last, new, by_start_sequence, by_start_time, or last_per_subject.",
			"Durable.description":      "Description says what this consumer is for.",
			"Durable.durable_name":     "Name is the durable consumer name (alphanumeric, hyphens, underscores).",
			"Durable.filter_subject":   "Filter delivers only messages on this org-relative subject (wildcards supported).",
			"Durable.max_ack_pending":  "MaxAckPending caps unacknowledged messages in flight (default 1000).",
			"Durable.max_deliver":      "MaxDeliver caps delivery attempts per message; -1 (default) is unlimited.",
			"Durable.opt_start_seq":    "StartSeq is the starting sequence for deliver_policy by_start_sequence.",
			"Durable.opt_start_time":   "StartTime is the starting instant for deliver_policy by_start_time.",
			"Durable.replay_policy":    "Replay is the replay pacing: instant (default) or original.",
			"Sequences.consumer_seq":   "Consumer is the consumer's own sequence.",
			"Sequences.stream_seq":     "Stream is the corresponding stream sequence.",
			"twoIn.name":               "Name is the consumer name, from the path.",
			"twoIn.stream":             "Stream is the stream name, from the path.",
		},
	})
	zip.Describe("POST /v1/mq/streams", zip.Doc{
		Description: "Creates a durable stream in the org's namespace and returns it.",
		Fields: map[string]string{
			"Config.max_age":       "MaxAge caps message age, e.g. \"24h\" or \"7d\"; \"0\" (default) is unlimited.",
			"Config.max_bytes":     "MaxBytes caps the stream's total stored bytes; -1 (default) is unlimited.",
			"Config.max_msg_size":  "MaxMsgSize caps one message's size in bytes; -1 (default) is the broker's limit.",
			"Config.max_msgs":      "MaxMsgs caps the number of stored messages; -1 (default) is unlimited.",
			"Config.name":          "Name is the stream name, unique within the org (alphanumeric, hyphens, underscores).",
			"Config.num_replicas":  "Replicas is the number of stream replicas (1–5); this plane runs 1.",
			"Config.retention":     "Retention is the retention policy: limits (default), interest, or workqueue.",
			"Config.storage":       "Storage is the storage backend: file (default) or memory.",
			"Config.subjects":      "Subjects are the org-relative subjects bound to this stream (wildcards supported). Default: the stream name.",
			"State.bytes":          "Bytes is the total stored size.",
			"State.consumer_count": "Consumers is the number of consumers attached to this stream.",
			"State.first_seq":      "FirstSeq is the sequence of the first stored message.",
			"State.first_ts":       "FirstTS is the timestamp of the first stored message.",
			"State.last_seq":       "LastSeq is the sequence of the last stored message.",
			"State.last_ts":        "LastTS is the timestamp of the last stored message.",
			"State.messages":       "Messages is the number of messages currently stored.",
			"State.num_deleted":    "Deleted is the number of deleted messages (sequence gaps).",
			"State.num_subjects":   "Subjects is the number of distinct subjects stored.",
			"Stream.config":        "Config is the stream's configuration.",
			"Stream.created":       "Created is when the stream was created.",
			"Stream.name":          "Name is the stream name within the org.",
			"Stream.state":         "State is the stream's current state.",
		},
	})
	zip.Describe("POST /v1/mq/streams/:name/purge", zip.Doc{
		Description: "Removes messages from a stream, leaving its consumers in place.",
		Fields: map[string]string{
			"Purge.filter":    "Filter purges only messages on this org-relative subject (wildcards supported).",
			"Purge.keep":      "Keep retains that many newest messages.",
			"Purge.name":      "Name is the stream name, from the path.",
			"purgeOut.purged": "Purged is the number of messages removed.",
		},
	})
	zip.Describe("POST /v1/mq/streams/:stream/consumers", zip.Doc{
		Description: "Creates a durable pull consumer on a stream and returns it.",
		Fields: map[string]string{
			"Consumer.ack_floor":       "AckFloor is the highest contiguously acknowledged sequence pair.",
			"Consumer.config":          "Config is the consumer's configuration.",
			"Consumer.created":         "Created is when the consumer was created.",
			"Consumer.delivered":       "Delivered is the highest delivered sequence pair.",
			"Consumer.name":            "Name is the consumer name.",
			"Consumer.num_ack_pending": "AckPending is the number of delivered, not yet acknowledged messages.",
			"Consumer.num_pending":     "Pending is the number of messages yet to be delivered.",
			"Consumer.num_redelivered": "Redelivered is the number of messages currently being redelivered.",
			"Consumer.num_waiting":     "Waiting is the number of pull requests waiting for messages.",
			"Consumer.stream_name":     "Stream is the stream this consumer reads.",
			"Durable.ack_policy":       "Ack is the acknowledgment policy: explicit (default), all, or none.",
			"Durable.ack_wait":         "AckWait is how long the broker waits for an ack before redelivering, e.g. \"30s\" (default).",
			"Durable.deliver_policy":   "Deliver is where delivery starts: all (default), last, new, by_start_sequence, by_start_time, or last_per_subject.",
			"Durable.description":      "Description says what this consumer is for.",
			"Durable.durable_name":     "Name is the durable consumer name (alphanumeric, hyphens, underscores).",
			"Durable.filter_subject":   "Filter delivers only messages on this org-relative subject (wildcards supported).",
			"Durable.max_ack_pending":  "MaxAckPending caps unacknowledged messages in flight (default 1000).",
			"Durable.max_deliver":      "MaxDeliver caps delivery attempts per message; -1 (default) is unlimited.",
			"Durable.opt_start_seq":    "StartSeq is the starting sequence for deliver_policy by_start_sequence.",
			"Durable.opt_start_time":   "StartTime is the starting instant for deliver_policy by_start_time.",
			"Durable.replay_policy":    "Replay is the replay pacing: instant (default) or original.",
			"Sequences.consumer_seq":   "Consumer is the consumer's own sequence.",
			"Sequences.stream_seq":     "Stream is the corresponding stream sequence.",
			"makeIn.stream":            "Stream is the stream name, from the path.",
		},
	})
	zip.Describe("POST /v1/mq/streams/:stream/consumers/:name/next", zip.Doc{
		Description: "Pulls the consumer's next batch. Delivered messages are acknowledged on\ndelivery — the broker will not redeliver what this call returns; an empty\nwait answers 408.",
		Fields: map[string]string{
			"Delivery.data":          "Data is the payload, base64-encoded.",
			"Delivery.headers":       "Headers are the message headers, when any were published.",
			"Delivery.num_delivered": "Delivered is how many times a consumer has been handed this message (pulls only).",
			"Delivery.num_pending":   "Remaining is how many messages follow this one for the consumer (pulls only).",
			"Delivery.sequence":      "Sequence is the message's stream sequence.",
			"Delivery.subject":       "Subject is the org-relative subject the message was stored under.",
			"Delivery.timestamp":     "Timestamp is when the broker stored the message.",
			"nextIn.batch":           "Batch is how many messages to pull (1–1000, default 1).",
			"nextIn.expires":         "Expires is how long to wait for messages, e.g. \"5s\" (default \"30s\", max \"60s\").",
			"nextIn.name":            "Name is the consumer name, from the path.",
			"nextIn.no_wait":         "NoWait answers immediately with whatever is available instead of waiting.",
			"nextIn.stream":          "Stream is the stream name, from the path.",
			"readOut.messages":       "Messages is what was read, stream-ordered.",
		},
	})
	zip.Describe("PUT /v1/mq/streams/:name", zip.Doc{
		Description: "Reconfigures an existing stream; the path names the stream, and the\nimmutable fields (storage, retention) must restate what they are.",
		Fields: map[string]string{
			"Config.max_age":       "MaxAge caps message age, e.g. \"24h\" or \"7d\"; \"0\" (default) is unlimited.",
			"Config.max_bytes":     "MaxBytes caps the stream's total stored bytes; -1 (default) is unlimited.",
			"Config.max_msg_size":  "MaxMsgSize caps one message's size in bytes; -1 (default) is the broker's limit.",
			"Config.max_msgs":      "MaxMsgs caps the number of stored messages; -1 (default) is unlimited.",
			"Config.name":          "Name is the stream name, unique within the org (alphanumeric, hyphens, underscores).",
			"Config.num_replicas":  "Replicas is the number of stream replicas (1–5); this plane runs 1.",
			"Config.retention":     "Retention is the retention policy: limits (default), interest, or workqueue.",
			"Config.storage":       "Storage is the storage backend: file (default) or memory.",
			"Config.subjects":      "Subjects are the org-relative subjects bound to this stream (wildcards supported). Default: the stream name.",
			"State.bytes":          "Bytes is the total stored size.",
			"State.consumer_count": "Consumers is the number of consumers attached to this stream.",
			"State.first_seq":      "FirstSeq is the sequence of the first stored message.",
			"State.first_ts":       "FirstTS is the timestamp of the first stored message.",
			"State.last_seq":       "LastSeq is the sequence of the last stored message.",
			"State.last_ts":        "LastTS is the timestamp of the last stored message.",
			"State.messages":       "Messages is the number of messages currently stored.",
			"State.num_deleted":    "Deleted is the number of deleted messages (sequence gaps).",
			"State.num_subjects":   "Subjects is the number of distinct subjects stored.",
			"Stream.config":        "Config is the stream's configuration.",
			"Stream.created":       "Created is when the stream was created.",
			"Stream.name":          "Name is the stream name within the org.",
			"Stream.state":         "State is the stream's current state.",
		},
	})
}
