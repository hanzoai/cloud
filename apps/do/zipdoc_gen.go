// Code generated by zipdoc; DO NOT EDIT.

package do

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/load-balancers/:id", zip.Doc{
		Description: "deleteLB deletes one load balancer the caller's org owns. Ownership is confirmed\nbefore the delete, so a cross-tenant id is a not-found rather than another org's loss.",
		Fields: map[string]string{
			"lbRef.id": "ID is the DigitalOcean load balancer id from the path.",
		},
		Example: json.RawMessage(`{"id":"1b2c3d4e-5f60-7182-93a4-b5c6d7e8f901"}`),
	})
	zip.Describe("DELETE /v1/vpcs/:id", zip.Doc{
		Description: "deleteVPC deletes one VPC the caller's org owns. Ownership is confirmed before\nthe delete, so a cross-tenant id is a not-found rather than another org's loss.",
		Fields: map[string]string{
			"vpcRef.id": "ID is the DigitalOcean VPC id from the path.",
		},
		Example: json.RawMessage(`{"id":"8a1b2c3d-4e5f-6071-8293-a4b5c6d7e8f9"}`),
	})
	zip.Describe("GET /v1/load-balancers", zip.Doc{
		Description: "listLBs lists the load balancers the caller's org owns, under their friendly names.\nThe account-wide DigitalOcean inventory is filtered to the caller's org prefix, so\nanother tenant's load balancers are invisible rather than forbidden.",
		Fields: map[string]string{
			"lbList.loadBalancers": "LoadBalancers are the caller's own load balancers; another org's are invisible.",
			"lbView.id":            "ID is DigitalOcean's own load balancer id — the value GET and DELETE address.",
			"lbView.ip":            "IP is the load balancer's public address, empty while it is still provisioning.",
			"lbView.name":          "Name is the FRIENDLY name the tenant supplied, with the org prefix stripped.",
			"lbView.status":        "Status is DO's own lifecycle value, e.g. new or active.",
			"lbView.targets":       "Targets is the count of backend droplets attached to the load balancer.",
			"lbView.type":          "Type is DO's load balancer type, e.g. REGIONAL or GLOBAL.",
		},
		Response: json.RawMessage(`{"loadBalancers":[{"id":"1b2c3d4e-5f60-7182-93a4-b5c6d7e8f901","name":"edge","type":"REGIONAL","targets":3,"ip":"203.0.113.7","status":"active"}]}`),
	})
	zip.Describe("GET /v1/load-balancers/:id", zip.Doc{
		Description: "getLB reads one load balancer the caller's org owns. A load balancer in another\norg's namespace is reported not-found, never forbidden.",
		Fields: map[string]string{
			"lbRef.id":       "ID is the DigitalOcean load balancer id from the path.",
			"lbView.id":      "ID is DigitalOcean's own load balancer id — the value GET and DELETE address.",
			"lbView.ip":      "IP is the load balancer's public address, empty while it is still provisioning.",
			"lbView.name":    "Name is the FRIENDLY name the tenant supplied, with the org prefix stripped.",
			"lbView.status":  "Status is DO's own lifecycle value, e.g. new or active.",
			"lbView.targets": "Targets is the count of backend droplets attached to the load balancer.",
			"lbView.type":    "Type is DO's load balancer type, e.g. REGIONAL or GLOBAL.",
		},
		Example: json.RawMessage(`{"id":"1b2c3d4e-5f60-7182-93a4-b5c6d7e8f901"}`),
	})
	zip.Describe("GET /v1/vpcs", zip.Doc{
		Description: "listVPCs lists the VPCs owned by the caller's org, under their friendly names.\nThe account-wide DigitalOcean inventory is filtered to the caller's org prefix,\nso another tenant's VPCs are invisible rather than forbidden.",
		Fields: map[string]string{
			"vpcList.vpcs":    "VPCs are the caller's own VPCs; another org's are invisible, not forbidden.",
			"vpcView.cidr":    "CIDR is the VPC's IP range in CIDR notation.",
			"vpcView.id":      "ID is DigitalOcean's own VPC id — the value GET and DELETE address.",
			"vpcView.name":    "Name is the FRIENDLY name the tenant supplied, with the org prefix stripped.",
			"vpcView.region":  "Region is the DigitalOcean region slug the VPC lives in.",
			"vpcView.status":  "Status is always \"active\" — DO VPCs have no lifecycle state.",
			"vpcView.subnets": "Subnets is always empty: a DO VPC is a single IP range with no sub-network\nobjects in the API.",
		},
		Response: json.RawMessage(`{"vpcs":[{"id":"8a1b2c3d-4e5f-6071-8293-a4b5c6d7e8f9","name":"web","cidr":"10.10.0.0/20","region":"nyc3","subnets":[],"status":"active"}]}`),
	})
	zip.Describe("GET /v1/vpcs/:id", zip.Doc{
		Description: "getVPC reads one VPC the caller's org owns. A VPC in another org's namespace is\nreported not-found, never forbidden, so the id space leaks no existence.",
		Fields: map[string]string{
			"vpcRef.id":       "ID is the DigitalOcean VPC id from the path.",
			"vpcView.cidr":    "CIDR is the VPC's IP range in CIDR notation.",
			"vpcView.id":      "ID is DigitalOcean's own VPC id — the value GET and DELETE address.",
			"vpcView.name":    "Name is the FRIENDLY name the tenant supplied, with the org prefix stripped.",
			"vpcView.region":  "Region is the DigitalOcean region slug the VPC lives in.",
			"vpcView.status":  "Status is always \"active\" — DO VPCs have no lifecycle state.",
			"vpcView.subnets": "Subnets is always empty: a DO VPC is a single IP range with no sub-network\nobjects in the API.",
		},
		Example: json.RawMessage(`{"id":"8a1b2c3d-4e5f-6071-8293-a4b5c6d7e8f9"}`),
	})
	zip.Describe("POST /v1/load-balancers", zip.Doc{
		Description: "createLB creates a load balancer in the caller's org under a friendly name. Name\nand region are required; omitted forwarding rules default to plain HTTP 80→80, so\na minimal create still yields a usable load balancer, and a name already taken in\nthis org is a 409.",
		Fields: map[string]string{
			"createLBReq.forwarding_rules": "ForwardingRules are the entry→target port mappings; empty defaults to HTTP 80→80.",
			"createLBReq.name":             "Name is the friendly load balancer name, a DNS-safe slug of at most 40\ncharacters. It is namespaced to the caller's org before it reaches DigitalOcean.",
			"createLBReq.region":           "Region is the DigitalOcean region slug to create the load balancer in. Required.",
			"createLBReq.size":             "Size is DO's size slug; empty takes DO's default.",
			"createLBReq.type":             "Type is DO's load balancer type; empty takes DO's default (REGIONAL).",
			"fwdRule.entry_port":           "EntryPort is the port the load balancer listens on.",
			"fwdRule.entry_protocol":       "EntryProtocol is the protocol the load balancer listens with, e.g. http or https.",
			"fwdRule.target_port":          "TargetPort is the port on the backend droplets.",
			"fwdRule.target_protocol":      "TargetProtocol is the protocol used to reach the backend droplets.",
			"lbView.id":                    "ID is DigitalOcean's own load balancer id — the value GET and DELETE address.",
			"lbView.ip":                    "IP is the load balancer's public address, empty while it is still provisioning.",
			"lbView.name":                  "Name is the FRIENDLY name the tenant supplied, with the org prefix stripped.",
			"lbView.status":                "Status is DO's own lifecycle value, e.g. new or active.",
			"lbView.targets":               "Targets is the count of backend droplets attached to the load balancer.",
			"lbView.type":                  "Type is DO's load balancer type, e.g. REGIONAL or GLOBAL.",
		},
		Example: json.RawMessage(`{"name":"edge","region":"nyc3","size":"lb-small","forwarding_rules":[{"entry_protocol":"http","entry_port":80,"target_protocol":"http","target_port":8080}]}`),
	})
	zip.Describe("POST /v1/vpcs", zip.Doc{
		Description: "createVPC creates a VPC in the caller's org under a friendly name.\nName and region are required; an empty ip_range lets DigitalOcean pick the CIDR.\nA name already taken in this org is a 409.",
		Fields: map[string]string{
			"createVPCReq.ip_range": "IPRange is the VPC's CIDR; empty lets DigitalOcean assign one.",
			"createVPCReq.name":     "Name is the friendly VPC name, a DNS-safe slug of at most 40 characters.\nIt is namespaced to the caller's org before it reaches DigitalOcean.",
			"createVPCReq.region":   "Region is the DigitalOcean region slug to create the VPC in. Required.",
			"vpcView.cidr":          "CIDR is the VPC's IP range in CIDR notation.",
			"vpcView.id":            "ID is DigitalOcean's own VPC id — the value GET and DELETE address.",
			"vpcView.name":          "Name is the FRIENDLY name the tenant supplied, with the org prefix stripped.",
			"vpcView.region":        "Region is the DigitalOcean region slug the VPC lives in.",
			"vpcView.status":        "Status is always \"active\" — DO VPCs have no lifecycle state.",
			"vpcView.subnets":       "Subnets is always empty: a DO VPC is a single IP range with no sub-network\nobjects in the API.",
		},
		Example: json.RawMessage(`{"name":"web","region":"nyc3","ip_range":"10.10.0.0/20"}`),
	})
}
