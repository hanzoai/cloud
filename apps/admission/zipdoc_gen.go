// Code generated by zipdoc; DO NOT EDIT.

package admission

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/flags/waitlist", zip.Doc{
		Description: "waitlistMode reports whether one host is behind its service's waitlist. It resolves\nhost→service through the launch registry, then reads the waitlist.<service> switch.\nPublic (in-cluster) read: it answers for the ONE queried host and never enumerates\nthe registry. An empty host falls back to the host the request was sent to.",
		Fields: map[string]string{
			"HostQuery.host":            "Host is the hostname to resolve, e.g. \"chat.hanzo.ai\". Empty means the\nhost the request itself was sent to.",
			"WaitlistMode.host":         "Host is the normalized (lowercased, port-stripped) host that was resolved.",
			"WaitlistMode.known":        "Known is false when no registry entry claims the host.",
			"WaitlistMode.service":      "Service is the registry service governing that host, empty when unknown.",
			"WaitlistMode.waitlistMode": "WaitlistMode is true when the service is gated behind its waitlist.",
		},
		Example:  json.RawMessage(`{"host":"chat.hanzo.ai"}`),
		Response: json.RawMessage(`{"host":"chat.hanzo.ai","service":"chat","waitlistMode":true,"known":true}`),
	})
}
