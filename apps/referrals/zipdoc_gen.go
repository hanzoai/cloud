// Code generated by zipdoc; DO NOT EDIT.

package referrals

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/admin/referrals/bonuses", zip.Doc{
		Description: "adminList returns every one-time referral bonus — both orgs, grant amounts and\nledger transaction ids — plus a fleet summary. SuperAdmin only.",
		Fields: map[string]string{
			"AdminPage.limit":           "Limit caps the rows returned; 0 means 500 and nothing above 1000 is honoured.",
			"BonusBoard.referrals":      "Referrals is every referral row, both orgs exposed.",
			"BonusBoard.summary":        "Summary is the fleet tally, including total promo credit granted.",
			"BonusBoardEnvelope.data":   "Data is the bonus board.",
			"BonusBoardEnvelope.msg":    "Msg carries the failure text when status is not ok.",
			"BonusBoardEnvelope.status": "Status is \"ok\" on success; the transport surfaces anything else as an error.",
		},
		Example: json.RawMessage(`{"limit":500}`),
	})
	zip.Describe("GET /v1/referrals", zip.Doc{
		Description: "myReferrals returns the caller org's referral code and link, the standing bonus\namounts, the credit earned so far, and every referral they have made. It also\nopportunistically qualify-checks the caller's own pending referees, so the page\nis self-updating.",
		Fields: map[string]string{
			"MyReferrals.code":               "Code is the caller org's stable referral code.",
			"MyReferrals.counts":             "Counts tallies the caller's referrals by status.",
			"MyReferrals.creditsEarnedCents": "CreditsEarnedCents is the promo credit this org has earned in total.",
			"MyReferrals.link":               "Link is the shareable signup link carrying that code.",
			"MyReferrals.refereeBonusCents":  "RefereeBonusCents is what the referee earns on qualification.",
			"MyReferrals.referrals":          "Referrals is the caller's referrals, one row per referee.",
			"MyReferrals.referrerBonusCents": "ReferrerBonusCents is what the referrer earns per qualified referee.",
			"myReferralView.creditsCents":    "what I earned from this referral",
			"myReferralView.referee":         "the org that signed up via my code",
		},
		Response: json.RawMessage(`{"code":"ACME1234","link":"https://hanzo.ai/?ref=ACME1234","referrerBonusCents":1000,"refereeBonusCents":500,"creditsEarnedCents":1000,"counts":{"total":1,"signedUp":0,"qualified":0,"credited":1},"referrals":[]}`),
	})
	zip.Describe("POST /v1/admin/referrals/sweep", zip.Doc{
		Description: "adminSweep qualify-checks every pending referral and grants the bonuses that\nnow qualify — the periodic path a cron or an operator drives. The grant is\nlatched at-most-once, so a re-run never double-pays. SuperAdmin only.",
		Fields: map[string]string{
			"SweepEnvelope.data":   "Data is the sweep result.",
			"SweepEnvelope.msg":    "Msg carries the failure text when status is not ok.",
			"SweepEnvelope.status": "Status is \"ok\" on success; the transport surfaces anything else as an error.",
			"SweepResult.credited": "Credited is how many of them qualified and were granted on this pass.",
			"SweepResult.swept":    "Swept is how many pending referrals were qualify-checked.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"swept":12,"credited":3}}`),
	})
	zip.Describe("POST /v1/referrals/claim", zip.Doc{
		Description: "claim records a referral from a referrer's code. The REFEREE is the validated\ncaller (never client-supplied); the referrer is resolved from the code. It is\nidempotent — one referral per referee ever, first touch wins — and refuses a\nself-referral or an unknown code.",
		Fields: map[string]string{
			"ClaimRequest.code":     "Code is the referrer's referral code, as carried by the ?ref= link.",
			"ClaimResult.code":      "Code is the referrer code the referral was recorded against.",
			"ClaimResult.created":   "Created is false when the caller had already claimed a referral; the\nfirst-touch row is returned unchanged and the response is 200, not 201.",
			"ClaimResult.createdAt": "CreatedAt is the unix second the referral was first recorded.",
			"ClaimResult.id":        "ID is the referral id.",
			"ClaimResult.status":    "Status is the referral's lifecycle state, signed_up on a fresh claim.",
		},
		Example:  json.RawMessage(`{"code":"ACME1234"}`),
		Response: json.RawMessage(`{"id":"ref_4c1e9b7a2d6f0538e4a7c9b1d3f5027a","code":"ACME1234","status":"signed_up","created":true,"createdAt":1780000000}`),
	})
}
