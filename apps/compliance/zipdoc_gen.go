// Code generated by zipdoc; DO NOT EDIT.

package compliance

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/compliance/accreditation", zip.Doc{
		Description: "Returns the org's tracked accreditation-state records, newest\nfirst — evidence entries the org keeps, never a platform certification.",
		Fields: map[string]string{
			"accList.data":          "Data is the org's tracked accreditation records, newest first.",
			"accList.disclaimer":    "Disclaimer states that statuses are tracked or provider-reported, never a\nplatform assertion of legal or regulatory compliance.",
			"accView.basis":         "Basis is the qualification category: income, net_worth, professional_license,\nor entity.",
			"accView.createdAt":     "CreatedAt is the unix second the record was created.",
			"accView.evidenceDocId": "EvidenceDocID references an evidence document in the org's sealed data room.",
			"accView.expiresAt":     "ExpiresAt is the unix second a confirmation ages out; 0 means none.",
			"accView.id":            "ID is the accreditation record's opaque id.",
			"accView.method":        "Method is how the state was established: self_attested, third_party_letter,\nor provider_verified.",
			"accView.note":          "Note is a non-PII operator note.",
			"accView.reviewerSub":   "ReviewerSub is the org user who recorded a decision on this record.",
			"accView.status":        "Status is the tracked state: asserted, provider_verified, reviewer_confirmed,\nrejected, or expired.",
			"accView.subjectId":     "SubjectID is the opaque id of the subject the record is about.",
			"accView.updatedAt":     "UpdatedAt is the unix second the record last changed.",
			"listIn.limit":          "Limit caps the rows returned; non-positive means the server default.",
		},
	})
	zip.Describe("GET /v1/compliance/accreditation/:id", zip.Doc{
		Description: "Returns one tracked accreditation record.",
		Fields: map[string]string{
			"accView.basis":         "Basis is the qualification category: income, net_worth, professional_license,\nor entity.",
			"accView.createdAt":     "CreatedAt is the unix second the record was created.",
			"accView.evidenceDocId": "EvidenceDocID references an evidence document in the org's sealed data room.",
			"accView.expiresAt":     "ExpiresAt is the unix second a confirmation ages out; 0 means none.",
			"accView.id":            "ID is the accreditation record's opaque id.",
			"accView.method":        "Method is how the state was established: self_attested, third_party_letter,\nor provider_verified.",
			"accView.note":          "Note is a non-PII operator note.",
			"accView.reviewerSub":   "ReviewerSub is the org user who recorded a decision on this record.",
			"accView.status":        "Status is the tracked state: asserted, provider_verified, reviewer_confirmed,\nrejected, or expired.",
			"accView.subjectId":     "SubjectID is the opaque id of the subject the record is about.",
			"accView.updatedAt":     "UpdatedAt is the unix second the record last changed.",
			"accreditationRef.id":   "ID is the accreditation record to read, from the path.",
		},
		Example: json.RawMessage(`{"id":"acc_1"}`),
	})
	zip.Describe("GET /v1/compliance/audit", zip.Doc{
		Description: "AuditRead is the compliance read of the SHARED tamper-evident audit plane —\nthe SOC 2 posture surface (privileged actions: who started/decided what, when). The\norg is PINNED to the caller's validated org and the rows are narrowed to\ncompliance.* actions. Fail-closed: no principal is a 403, no configured audit\nstore a 501.",
		Fields: map[string]string{
			"Wire.action":          "Action is the verb that was performed. It is the event's name, not the HTTP\nmethod — a request-sourced record carries both, and the pair is what makes a\nrow readable (\"grant.create\" at POST /v1/admin/grants).",
			"Wire.authMethod":      "Auth is the credential the actor presented: \"jwt\", \"api-key\", or \"none\".",
			"Wire.email":           "Email is the actor's validated address, absent when the credential carried\nnone. It comes from the verified token, never from a client header.",
			"Wire.hash":            "Hash is this record's SHA-256 over its own canonical JSON with both hash\nfields cleared, folded with prevHash. Recomputing it from the row's other\nfields is what proves the row has not been edited.",
			"Wire.home":            "Home is present ONLY on a cross-org action: the org the actor came FROM,\nwhile Org is the org they acted IN. A console row carrying `home` is a\nplatform-admin impersonation and should be rendered as one.",
			"Wire.isAdmin":         "IsAdmin is the VALIDATED platform-SuperAdmin bit at decision time (membership\nof the reserved admin org), never the client's own claim to be one.",
			"Wire.method":          "Method is the HTTP verb, on a record a request produced. Absent on an event\nemitted from inside the binary with no request behind it.",
			"Wire.org":             "Org is the tenant the action was taken IN — the effective org, which for\neveryone but an impersonating SuperAdmin is also the actor's own. Empty on an\nunauthenticated request.",
			"Wire.path":            "Path is the request's route. Any segment shaped like a credential is replaced\nbefore the record is written, so a key that rides in a path is not preserved\nhere by the very control meant to watch it.",
			"Wire.prevHash":        "PrevHash is the hash of record seq-1, which is what links the rows into a\nchain: a deleted or reordered record breaks the recomputation at that point.\nThe first record of a chain carries 64 zeros rather than an empty string, so\n\"start of chain\" and \"field missing\" cannot look alike.",
			"Wire.reason":          "Reason is a short explanation for a deny or an error (\"SuperAdmin required\",\n\"insufficient_balance\"). It is never a secret and never a raw upstream error\nbody; absent on a success.",
			"Wire.requestId":       "RequestID ties this row to the request-line log and any downstream trace — the\nX-Request-Id the pipeline minted for that request.",
			"Wire.resource":        "Resource is the KIND of thing acted upon (\"org\", \"role\", \"secret\",\n\"provider-config\", \"credit\"). Where a mutation has no finer semantics than its\nroute, this is the route family and resourceId is empty — the action and the\npath already pin the object.",
			"Wire.resourceId":      "ResourceID is the specific instance, absent when the kind alone identifies it.",
			"Wire.result":          "Result is how the action ended: \"success\", \"deny\" or \"error\". A deny is a\ndecision this binary made and is as much evidence as a success.",
			"Wire.seq":             "Seq is the record's position in the chain, 0-based and gapless. The Recorder\nassigns it under its own lock, so it is a true total order: seq n+1 was\nwritten after seq n, and a missing number is a missing record.",
			"Wire.sourceIp":        "SourceIP is the client address the edge resolved for the request, after the\nproxy chain — the address a responder would act on, not the socket peer.",
			"Wire.status":          "Status is the HTTP status the caller received. It is the outcome as the client\nsaw it, so a 200 carrying a domain refusal still reads 200 here.",
			"Wire.sub":             "Sub is the acting user (the IAM subject). Empty for a machine principal or an\nanonymous request, which is how a service action is told from a person's.",
			"Wire.time":            "Time is when the action happened, RFC3339Nano in UTC. The stored column has\nthe same precision and sorts the same way, so a client can range and order on\nthis string verbatim.",
			"Wire.userAgent":       "UserAgent is the client the request announced itself as. Client-supplied, so\nit is evidence about what claimed to act, not proof of it.",
			"auditIn.result":       "Result filters rows by outcome result: success, deny, or error; empty means all.",
			"auditList.data":       "Data is the org's compliance.* audit rows, newest first.",
			"auditList.disclaimer": "Disclaimer states that statuses are provider-reported or tracked, never a\nplatform assertion of legal or regulatory compliance.",
		},
	})
	zip.Describe("GET /v1/compliance/health", zip.Doc{
		Description: "Health reports subsystem liveness and the wired verification provider. Fail-open\non purpose: it never probes the external provider, so a provider outage cannot\nfail liveness.",
		Fields: map[string]string{
			"healthView.provider": "Provider is the wired verification provider's name (\"manual\" by default).",
			"healthView.status":   "Status is \"ok\" when the subsystem is live.",
		},
	})
	zip.Describe("GET /v1/compliance/records", zip.Doc{
		Description: "ListRecords is the unified compliance-record view for the org: its verifications\nand accreditation records together, each provider-reported or tracked, never\nplatform-asserted. PII stays in the subject store; records carry only opaque ids\nand statuses.",
		Fields: map[string]string{
			"accView.basis":            "Basis is the qualification category: income, net_worth, professional_license,\nor entity.",
			"accView.createdAt":        "CreatedAt is the unix second the record was created.",
			"accView.evidenceDocId":    "EvidenceDocID references an evidence document in the org's sealed data room.",
			"accView.expiresAt":        "ExpiresAt is the unix second a confirmation ages out; 0 means none.",
			"accView.id":               "ID is the accreditation record's opaque id.",
			"accView.method":           "Method is how the state was established: self_attested, third_party_letter,\nor provider_verified.",
			"accView.note":             "Note is a non-PII operator note.",
			"accView.reviewerSub":      "ReviewerSub is the org user who recorded a decision on this record.",
			"accView.status":           "Status is the tracked state: asserted, provider_verified, reviewer_confirmed,\nrejected, or expired.",
			"accView.subjectId":        "SubjectID is the opaque id of the subject the record is about.",
			"accView.updatedAt":        "UpdatedAt is the unix second the record last changed.",
			"checkView.createdAt":      "CreatedAt is the unix second the verification was started.",
			"checkView.decidedAt":      "DecidedAt is the unix second a terminal status was recorded.",
			"checkView.decidedBy":      "DecidedBy records who settled a terminal status: the provider name, or a\nreviewer's user id for a recorded manual decision.",
			"checkView.id":             "ID is the verification's opaque id.",
			"checkView.kind":           "Kind is the party type: \"individual\" (KYC) or \"business\" (KYB).",
			"checkView.provider":       "Provider is the verification provider this check runs through.",
			"checkView.status":         "Status is the check's state: pending, provider_verified, provider_rejected,\nmanual_review, or expired (provider-reported), or reviewer_confirmed — the\none value a privileged human reviewer records, never a provider.",
			"checkView.subjectId":      "SubjectID is the opaque id of the subject under verification.",
			"checkView.updatedAt":      "UpdatedAt is the unix second the verification last changed.",
			"checkView.verifyUrl":      "VerifyURL is the provider's hosted verification flow for the subject, when one exists.",
			"listIn.limit":             "Limit caps the rows returned; non-positive means the server default.",
			"recordList.accreditation": "Accreditation is the org's tracked accreditation-state records.",
			"recordList.disclaimer":    "Disclaimer states that statuses are provider-reported or tracked, never a\nplatform assertion of legal or regulatory compliance.",
			"recordList.verifications": "Verifications is the org's KYC/KYB checks, provider-reported statuses only.",
		},
	})
	zip.Describe("GET /v1/compliance/status", zip.Doc{
		Description: "Status is the org's honest posture read: the wired provider and the per-status\ntally of its verifications. It is deliberately NOT a boolean \"compliant\" — it\nreports counts of provider-reported states and carries the boundary disclaimer.",
		Fields: map[string]string{
			"statusView.disclaimer":      "Disclaimer states that statuses are provider-reported, never a platform\nassertion of legal or regulatory compliance.",
			"statusView.provider":        "Provider is the wired verification provider's name.",
			"statusView.verifications":   "Verifications tallies the org's verifications by provider-reported status.",
			"verificationTally.byStatus": "ByStatus tallies the org's verifications by provider-reported status.",
			"verificationTally.total":    "Total is the sum over every status.",
		},
	})
	zip.Describe("GET /v1/compliance/subjects", zip.Doc{
		Description: "Returns the org's subjects as PII-MINIMIZED summaries — no name or\nemail, only whether an email is on file. The full record is returned only by the\nexplicit single-subject read.",
		Fields: map[string]string{
			"listIn.limit":             "Limit caps the rows returned; non-positive means the server default.",
			"subjectList.data":         "Data is the org's subjects, newest first, without contact PII.",
			"subjectSummary.createdAt": "CreatedAt is the unix second the subject was recorded.",
			"subjectSummary.hasEmail":  "HasEmail reports whether a contact email is on file, without exposing it.",
			"subjectSummary.id":        "ID is the subject's opaque id.",
			"subjectSummary.kind":      "Kind is the party type: \"individual\" (KYC) or \"business\" (KYB).",
			"subjectSummary.ref":       "Ref is the org's own opaque external id for this subject.",
		},
	})
	zip.Describe("GET /v1/compliance/subjects/:id", zip.Doc{
		Description: "Returns one subject WITH its contact PII — the only surface that\nreturns it, and only to the owning org. The response is never cached by any\nintermediary.",
		Fields: map[string]string{
			"Subject.createdAt": "CreatedAt is when the subject was first recorded, Unix SECONDS.",
			"Subject.email":     "Email is the party's address, when the org supplied one. It is PII: sealed at\nrest, returned only to the owning org, and never copied into a check record.",
			"Subject.id":        "ID is the opaque handle every other record uses to point at this party. It is\nthe only reference that leaves this type, which is what keeps the PII in one\nplace: a check, an accreditation and an audit row all carry the id and none of\nthem carry the name.",
			"Subject.kind":      "Kind is what is being verified: \"individual\" (a natural person, so KYC) or\n\"business\" (a legal entity, so KYB). It decides which provider flow runs.",
			"Subject.name":      "Name is the party's name, under the same PII rule as Email. For a business it\nis the legal entity name rather than a trading name, since that is what a\nprovider verifies against.",
			"Subject.org":       "Org is the tenant that is doing the verifying — the party who must answer for\nthis record, not the party being verified. A subject is returned only to it.",
			"Subject.ref":       "Ref is the org's OWN identifier for this party, carried so a caller can match a\nsubject back to their system without keeping a second mapping. Opaque here:\nnothing in this plane parses or enforces it.",
			"Subject.updatedAt": "UpdatedAt is when the subject's own fields last changed, Unix seconds. A check\nmoving to a new status does not touch it — that history lives on the check.",
			"subjectRef.id":     "ID is the subject to read, from the path.",
		},
		Example: json.RawMessage(`{"id":"sub_1"}`),
	})
	zip.Describe("GET /v1/compliance/verifications", zip.Doc{
		Description: "Returns the org's KYC/KYB verifications, newest first — opaque\nsubject references and provider-reported statuses only, no subject PII.",
		Fields: map[string]string{
			"checkList.data":       "Data is the org's verifications, newest first, without subject PII.",
			"checkList.disclaimer": "Disclaimer states that statuses are provider-reported, never a platform\nassertion of legal or regulatory compliance.",
			"checkView.createdAt":  "CreatedAt is the unix second the verification was started.",
			"checkView.decidedAt":  "DecidedAt is the unix second a terminal status was recorded.",
			"checkView.decidedBy":  "DecidedBy records who settled a terminal status: the provider name, or a\nreviewer's user id for a recorded manual decision.",
			"checkView.id":         "ID is the verification's opaque id.",
			"checkView.kind":       "Kind is the party type: \"individual\" (KYC) or \"business\" (KYB).",
			"checkView.provider":   "Provider is the verification provider this check runs through.",
			"checkView.status":     "Status is the check's state: pending, provider_verified, provider_rejected,\nmanual_review, or expired (provider-reported), or reviewer_confirmed — the\none value a privileged human reviewer records, never a provider.",
			"checkView.subjectId":  "SubjectID is the opaque id of the subject under verification.",
			"checkView.updatedAt":  "UpdatedAt is the unix second the verification last changed.",
			"checkView.verifyUrl":  "VerifyURL is the provider's hosted verification flow for the subject, when one exists.",
			"listIn.limit":         "Limit caps the rows returned; non-positive means the server default.",
		},
	})
	zip.Describe("GET /v1/compliance/verifications/:id", zip.Doc{
		Description: "Returns one verification — its opaque subject reference and\nprovider-reported status, no subject PII.",
		Fields: map[string]string{
			"checkView.createdAt": "CreatedAt is the unix second the verification was started.",
			"checkView.decidedAt": "DecidedAt is the unix second a terminal status was recorded.",
			"checkView.decidedBy": "DecidedBy records who settled a terminal status: the provider name, or a\nreviewer's user id for a recorded manual decision.",
			"checkView.id":        "ID is the verification's opaque id.",
			"checkView.kind":      "Kind is the party type: \"individual\" (KYC) or \"business\" (KYB).",
			"checkView.provider":  "Provider is the verification provider this check runs through.",
			"checkView.status":    "Status is the check's state: pending, provider_verified, provider_rejected,\nmanual_review, or expired (provider-reported), or reviewer_confirmed — the\none value a privileged human reviewer records, never a provider.",
			"checkView.subjectId": "SubjectID is the opaque id of the subject under verification.",
			"checkView.updatedAt": "UpdatedAt is the unix second the verification last changed.",
			"checkView.verifyUrl": "VerifyURL is the provider's hosted verification flow for the subject, when one exists.",
			"verificationRef.id":  "ID is the verification to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"chk_1"}`),
	})
	zip.Describe("POST /v1/compliance/accreditation", zip.Doc{
		Description: "Records an ASSERTED accreditation state for a subject — the\nsubject's own assertion, with no verifier. Every CONFIRMED state\n(provider_verified, reviewer_confirmed) and every rejected/expired state is a\nDECISION recorded via the decision endpoint, attributed to the reviewer — a\ncreate can never stamp a confirmation. The underlying figures (income, net\nworth) are never stored; only the method, category, and state.",
		Fields: map[string]string{
			"accView.basis":                  "Basis is the qualification category: income, net_worth, professional_license,\nor entity.",
			"accView.createdAt":              "CreatedAt is the unix second the record was created.",
			"accView.evidenceDocId":          "EvidenceDocID references an evidence document in the org's sealed data room.",
			"accView.expiresAt":              "ExpiresAt is the unix second a confirmation ages out; 0 means none.",
			"accView.id":                     "ID is the accreditation record's opaque id.",
			"accView.method":                 "Method is how the state was established: self_attested, third_party_letter,\nor provider_verified.",
			"accView.note":                   "Note is a non-PII operator note.",
			"accView.reviewerSub":            "ReviewerSub is the org user who recorded a decision on this record.",
			"accView.status":                 "Status is the tracked state: asserted, provider_verified, reviewer_confirmed,\nrejected, or expired.",
			"accView.subjectId":              "SubjectID is the opaque id of the subject the record is about.",
			"accView.updatedAt":              "UpdatedAt is the unix second the record last changed.",
			"accreditationReq.basis":         "Basis is the qualification category: income, net_worth, professional_license,\nor entity.",
			"accreditationReq.evidenceDocId": "EvidenceDocID references an evidence document in the org's sealed data room.",
			"accreditationReq.expiresAt":     "ExpiresAt is the unix second a confirmation ages out; 0 means none.",
			"accreditationReq.method":        "Method is how the state was established: self_attested, third_party_letter,\nor provider_verified.",
			"accreditationReq.note":          "Note is a non-PII operator note.",
			"accreditationReq.status":        "Status may only be \"asserted\" (empty reads as asserted); every confirmed,\nrejected or expired state is recorded via the decision endpoint.",
			"accreditationReq.subjectId":     "SubjectID names the subject this record is about; it must exist within the org.",
		},
		Example: json.RawMessage(`{"subjectId":"sub_1","method":"self_attested","basis":"income"}`),
	})
	zip.Describe("POST /v1/compliance/accreditation/:id/decision", zip.Doc{
		Description: "Records an org reviewer's decision on an accreditation\nrecord — a reviewer confirmation, a provider verification the reviewer has\nevidence of (a CPA/attorney letter, a verifier report), a rejection, or an\nexpiry. ROLE-GATED (an org admin or platform reviewer) and ATTRIBUTED: the\nreviewer's identity is recorded as ReviewerSub and audited. Human-in-the-loop:\nthe platform never confirms on its own, and even a provider_verified state\ncarries the reviewer who recorded it.",
		Fields: map[string]string{
			"accView.basis":                "Basis is the qualification category: income, net_worth, professional_license,\nor entity.",
			"accView.createdAt":            "CreatedAt is the unix second the record was created.",
			"accView.evidenceDocId":        "EvidenceDocID references an evidence document in the org's sealed data room.",
			"accView.expiresAt":            "ExpiresAt is the unix second a confirmation ages out; 0 means none.",
			"accView.id":                   "ID is the accreditation record's opaque id.",
			"accView.method":               "Method is how the state was established: self_attested, third_party_letter,\nor provider_verified.",
			"accView.note":                 "Note is a non-PII operator note.",
			"accView.reviewerSub":          "ReviewerSub is the org user who recorded a decision on this record.",
			"accView.status":               "Status is the tracked state: asserted, provider_verified, reviewer_confirmed,\nrejected, or expired.",
			"accView.subjectId":            "SubjectID is the opaque id of the subject the record is about.",
			"accView.updatedAt":            "UpdatedAt is the unix second the record last changed.",
			"accreditationDecision.id":     "ID is the accreditation record to decide, from the path.",
			"accreditationDecision.status": "Status is the decision being recorded: reviewer_confirmed, provider_verified,\nrejected, or expired.",
		},
		Example: json.RawMessage(`{"id":"acc_1","status":"reviewer_confirmed"}`),
	})
	zip.Describe("POST /v1/compliance/subjects", zip.Doc{
		Description: "Records a party the org is verifying as part of its own\nonboarding/compliance — a team member, vendor, customer, or counterparty. The\nsubject's contact PII (name/email) is sealed at rest and returned only to the\nowning org; downstream records reference the subject by opaque id.",
		Fields: map[string]string{
			"Subject.createdAt": "CreatedAt is when the subject was first recorded, Unix SECONDS.",
			"Subject.email":     "Email is the party's address, when the org supplied one. It is PII: sealed at\nrest, returned only to the owning org, and never copied into a check record.",
			"Subject.id":        "ID is the opaque handle every other record uses to point at this party. It is\nthe only reference that leaves this type, which is what keeps the PII in one\nplace: a check, an accreditation and an audit row all carry the id and none of\nthem carry the name.",
			"Subject.kind":      "Kind is what is being verified: \"individual\" (a natural person, so KYC) or\n\"business\" (a legal entity, so KYB). It decides which provider flow runs.",
			"Subject.name":      "Name is the party's name, under the same PII rule as Email. For a business it\nis the legal entity name rather than a trading name, since that is what a\nprovider verifies against.",
			"Subject.org":       "Org is the tenant that is doing the verifying — the party who must answer for\nthis record, not the party being verified. A subject is returned only to it.",
			"Subject.ref":       "Ref is the org's OWN identifier for this party, carried so a caller can match a\nsubject back to their system without keeping a second mapping. Opaque here:\nnothing in this plane parses or enforces it.",
			"Subject.updatedAt": "UpdatedAt is when the subject's own fields last changed, Unix seconds. A check\nmoving to a new status does not touch it — that history lives on the check.",
			"subjectReq.email":  "Email is the subject's contact email, sealed at rest.",
			"subjectReq.kind":   "Kind is the party type: \"individual\" (KYC) or \"business\" (KYB).",
			"subjectReq.name":   "Name is the subject's name, sealed at rest.",
			"subjectReq.ref":    "Ref is the org's own opaque external id for this subject.",
		},
		Example: json.RawMessage(`{"kind":"individual","email":"founder@example.com","name":"Ada"}`),
	})
	zip.Describe("POST /v1/compliance/verifications", zip.Doc{
		Description: "Begins a KYC/KYB verification of a subject through the wired\nprovider — an existing subject by id, or one created inline from the request.\nThe returned status is provider-reported and never terminal on a fresh start:\nstarting a verification can never yield a verified record, and a provider error\nis a 502, never a verification.",
		Fields: map[string]string{
			"checkView.createdAt":       "CreatedAt is the unix second the verification was started.",
			"checkView.decidedAt":       "DecidedAt is the unix second a terminal status was recorded.",
			"checkView.decidedBy":       "DecidedBy records who settled a terminal status: the provider name, or a\nreviewer's user id for a recorded manual decision.",
			"checkView.id":              "ID is the verification's opaque id.",
			"checkView.kind":            "Kind is the party type: \"individual\" (KYC) or \"business\" (KYB).",
			"checkView.provider":        "Provider is the verification provider this check runs through.",
			"checkView.status":          "Status is the check's state: pending, provider_verified, provider_rejected,\nmanual_review, or expired (provider-reported), or reviewer_confirmed — the\none value a privileged human reviewer records, never a provider.",
			"checkView.subjectId":       "SubjectID is the opaque id of the subject under verification.",
			"checkView.updatedAt":       "UpdatedAt is the unix second the verification last changed.",
			"checkView.verifyUrl":       "VerifyURL is the provider's hosted verification flow for the subject, when one exists.",
			"verificationReq.email":     "Email is an inline subject's contact email, sealed at rest.",
			"verificationReq.kind":      "Kind is an inline subject's party type: \"individual\" (KYC) or \"business\" (KYB).",
			"verificationReq.name":      "Name is an inline subject's name, sealed at rest.",
			"verificationReq.ref":       "Ref is the org's own opaque external id for an inline subject.",
			"verificationReq.subjectId": "SubjectID names an existing subject to verify; empty creates one inline.",
		},
		Example: json.RawMessage(`{"subjectId":"sub_1"}`),
	})
	zip.Describe("POST /v1/compliance/verifications/:id/decision", zip.Doc{
		Description: "Records a privileged reviewer's MANUAL decision on a\nverification — the human-in-the-loop path, and the ONLY route to a passing status\nwhen no real provider is wired. It produces a DISTINCT reviewer_confirmed, never\na provider_verified (a provider decision is the provider's to report, via the\nwebhook or a reconcile), and it is ROLE-GATED (an org admin or platform reviewer)\nAND ATTRIBUTED (the reviewer's user id is DecidedBy), so a manual pass is always\naccountable.",
		Fields: map[string]string{
			"checkView.createdAt":         "CreatedAt is the unix second the verification was started.",
			"checkView.decidedAt":         "DecidedAt is the unix second a terminal status was recorded.",
			"checkView.decidedBy":         "DecidedBy records who settled a terminal status: the provider name, or a\nreviewer's user id for a recorded manual decision.",
			"checkView.id":                "ID is the verification's opaque id.",
			"checkView.kind":              "Kind is the party type: \"individual\" (KYC) or \"business\" (KYB).",
			"checkView.provider":          "Provider is the verification provider this check runs through.",
			"checkView.status":            "Status is the check's state: pending, provider_verified, provider_rejected,\nmanual_review, or expired (provider-reported), or reviewer_confirmed — the\none value a privileged human reviewer records, never a provider.",
			"checkView.subjectId":         "SubjectID is the opaque id of the subject under verification.",
			"checkView.updatedAt":         "UpdatedAt is the unix second the verification last changed.",
			"checkView.verifyUrl":         "VerifyURL is the provider's hosted verification flow for the subject, when one exists.",
			"verificationDecision.id":     "ID is the verification to decide, from the path.",
			"verificationDecision.status": "Status is the reviewer's decision: \"reviewer_confirmed\" (a pass) or\n\"manual_review\" (withheld for review) — never a provider status.",
		},
		Example: json.RawMessage(`{"id":"chk_1","status":"reviewer_confirmed"}`),
	})
	zip.Describe("POST /v1/compliance/verifications/:id/refresh", zip.Doc{
		Description: "Polls the wired provider for its current decision and\nrecords it, ATTRIBUTED to the provider — the internal PULL reconcile. For the\nManual provider the check stays pending; for a hosted provider it reflects the\nprovider's settled status. A poll error is a 502, never a verification.",
		Fields: map[string]string{
			"checkView.createdAt": "CreatedAt is the unix second the verification was started.",
			"checkView.decidedAt": "DecidedAt is the unix second a terminal status was recorded.",
			"checkView.decidedBy": "DecidedBy records who settled a terminal status: the provider name, or a\nreviewer's user id for a recorded manual decision.",
			"checkView.id":        "ID is the verification's opaque id.",
			"checkView.kind":      "Kind is the party type: \"individual\" (KYC) or \"business\" (KYB).",
			"checkView.provider":  "Provider is the verification provider this check runs through.",
			"checkView.status":    "Status is the check's state: pending, provider_verified, provider_rejected,\nmanual_review, or expired (provider-reported), or reviewer_confirmed — the\none value a privileged human reviewer records, never a provider.",
			"checkView.subjectId": "SubjectID is the opaque id of the subject under verification.",
			"checkView.updatedAt": "UpdatedAt is the unix second the verification last changed.",
			"checkView.verifyUrl": "VerifyURL is the provider's hosted verification flow for the subject, when one exists.",
			"verificationRef.id":  "ID is the verification to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"chk_1"}`),
	})
	zip.Describe("POST /v1/compliance/verifications/webhook", zip.Doc{
		Description: "Is the external PUSH reconcile: a provider (or a Hanzo relay)\nsignals that a verification settled. It authenticates by HMAC SIGNATURE (not an\ninternal principal — an external caller has no validated org), locates the check by\nthe provider reference the signed payload names, and RECONCILES the status from the\nprovider API. The body carries no trusted decision, so a valid signature cannot\nforce a status — the wired provider is the source of truth, and Manual stays\npending. Disabled (501) unless a webhook secret is configured.\n\nUNTYPED on purpose: the HMAC is computed over the RAW body bytes and verified\nBEFORE anything parses, and an unknown reference answers a benign 200 no-op whose\nshape differs from the reconciled check. A typed op would decode its In first —\nreordering the authentication — and cannot answer two 200 shapes.",
	})
}
