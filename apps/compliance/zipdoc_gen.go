// Code generated by zipdoc; DO NOT EDIT.

package compliance

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/compliance/accreditation", zip.Doc{
		Description: "listAccreditation returns the caller org's accreditation records with their\nmethod, basis and current state.",
		Fields: map[string]string{
			"AccreditationList.data":          "Data is one row per accreditation record.",
			"AccreditationList.disclaimer":    "Disclaimer is the boundary made visible on the wire.",
			"AccreditationView.basis":         "Basis is what qualifies the subject.",
			"AccreditationView.createdAt":     "CreatedAt is the unix second the record was created.",
			"AccreditationView.evidenceDocId": "EvidenceDocID references the supporting document, when one is named.",
			"AccreditationView.expiresAt":     "ExpiresAt is the unix second the record lapses, when one is set.",
			"AccreditationView.id":            "ID is the accreditation id.",
			"AccreditationView.method":        "Method is how the state was established.",
			"AccreditationView.note":          "Note is a non-PII operator note, when one is set.",
			"AccreditationView.reviewerSub":   "ReviewerSub is the org user who recorded the decision, once decided.",
			"AccreditationView.status":        "Status is the tracked state: asserted until a reviewer decides.",
			"AccreditationView.subjectId":     "SubjectID is the opaque subject the record is about.",
			"AccreditationView.updatedAt":     "UpdatedAt is the unix second of the last recorded change.",
			"Page.limit":                      "Limit caps the rows returned; 0 means the store's own default.",
		},
		Example: json.RawMessage(`{"limit":50}`),
	})
	zip.Describe("GET /v1/compliance/accreditation/:id", zip.Doc{
		Description: "getAccreditation returns one of the caller org's accreditation records.",
		Fields: map[string]string{
			"AccreditationRef.id":             "ID is the accreditation id from the path, as returned by create.",
			"AccreditationView.basis":         "Basis is what qualifies the subject.",
			"AccreditationView.createdAt":     "CreatedAt is the unix second the record was created.",
			"AccreditationView.evidenceDocId": "EvidenceDocID references the supporting document, when one is named.",
			"AccreditationView.expiresAt":     "ExpiresAt is the unix second the record lapses, when one is set.",
			"AccreditationView.id":            "ID is the accreditation id.",
			"AccreditationView.method":        "Method is how the state was established.",
			"AccreditationView.note":          "Note is a non-PII operator note, when one is set.",
			"AccreditationView.reviewerSub":   "ReviewerSub is the org user who recorded the decision, once decided.",
			"AccreditationView.status":        "Status is the tracked state: asserted until a reviewer decides.",
			"AccreditationView.subjectId":     "SubjectID is the opaque subject the record is about.",
			"AccreditationView.updatedAt":     "UpdatedAt is the unix second of the last recorded change.",
		},
		Example: json.RawMessage(`{"id":"acc_4c1e9b7a2d6f0538e4a7c9b1d3f5027a"}`),
	})
	zip.Describe("GET /v1/compliance/audit", zip.Doc{
		Description: "auditRead returns the caller org's compliance actions from the shared\ntamper-evident audit trail — who started or decided what, and when. The org is\npinned to the caller's own; 501 when no audit store is configured.",
		Fields: map[string]string{
			"AuditFilter.result":    "Result keeps only rows with that outcome (success, denied, ...).",
			"AuditTrail.data":       "Data is one row per privileged compliance action: who started or decided\nwhat, and when. It is pinned to the caller's own org.",
			"AuditTrail.disclaimer": "Disclaimer is the boundary made visible on the wire.",
		},
		Example: json.RawMessage(`{"result":"success"}`),
	})
	zip.Describe("GET /v1/compliance/health", zip.Doc{
		Description: "health reports that the compliance surface is serving and which identity\nprovider is wired. It does NOT probe that provider — a provider outage must not\nfail liveness.",
		Fields: map[string]string{
			"ProviderHealth.provider": "Provider is the wired identity-verification provider.",
			"ProviderHealth.status":   "Status is \"ok\" while the surface is serving.",
		},
		Response: json.RawMessage(`{"status":"ok","provider":"manual"}`),
	})
	zip.Describe("GET /v1/compliance/records", zip.Doc{
		Description: "listRecords returns the caller org's verifications and accreditation records\ntogether. Every row carries opaque ids and statuses only — subject PII stays in\nthe subject store.",
		Fields: map[string]string{
			"AccreditationView.basis":         "Basis is what qualifies the subject.",
			"AccreditationView.createdAt":     "CreatedAt is the unix second the record was created.",
			"AccreditationView.evidenceDocId": "EvidenceDocID references the supporting document, when one is named.",
			"AccreditationView.expiresAt":     "ExpiresAt is the unix second the record lapses, when one is set.",
			"AccreditationView.id":            "ID is the accreditation id.",
			"AccreditationView.method":        "Method is how the state was established.",
			"AccreditationView.note":          "Note is a non-PII operator note, when one is set.",
			"AccreditationView.reviewerSub":   "ReviewerSub is the org user who recorded the decision, once decided.",
			"AccreditationView.status":        "Status is the tracked state: asserted until a reviewer decides.",
			"AccreditationView.subjectId":     "SubjectID is the opaque subject the record is about.",
			"AccreditationView.updatedAt":     "UpdatedAt is the unix second of the last recorded change.",
			"CheckView.createdAt":             "CreatedAt is the unix second the verification was started.",
			"CheckView.decidedAt":             "DecidedAt is the unix second it reached a terminal status.",
			"CheckView.decidedBy":             "DecidedBy names who settled it: the provider, or the reviewer's user id.",
			"CheckView.id":                    "ID is the verification id.",
			"CheckView.kind":                  "Kind is individual or business.",
			"CheckView.provider":              "Provider is the identity provider that holds the verification.",
			"CheckView.status":                "Status is provider-reported or pending — never platform-asserted.",
			"CheckView.subjectId":             "SubjectID is the opaque subject this verifies.",
			"CheckView.updatedAt":             "UpdatedAt is the unix second of the last recorded change.",
			"CheckView.verifyUrl":             "VerifyURL is where the subject completes verification, while one is open.",
			"Page.limit":                      "Limit caps the rows returned; 0 means the store's own default.",
			"Records.accreditation":           "Accreditation is the org's accreditation records, tracked.",
			"Records.disclaimer":              "Disclaimer is the boundary made visible on the wire.",
			"Records.verifications":           "Verifications are the org's KYC/KYB checks, provider-reported.",
		},
		Example: json.RawMessage(`{"limit":50}`),
	})
	zip.Describe("GET /v1/compliance/status", zip.Doc{
		Description: "status returns the org's compliance posture: the wired provider and the count of\nits verifications per provider-reported status. It is deliberately NOT a boolean\n\"compliant\" — the platform reports what providers said, nothing more.",
		Fields: map[string]string{
			"Posture.disclaimer":         "Disclaimer is the boundary made visible on the wire: Hanzo records what\nlicensed providers report; it does not itself determine compliance.",
			"Posture.provider":           "Provider is the wired identity-verification provider.",
			"Posture.verifications":      "Verifications is the per-status tally of the org's verifications.",
			"VerificationTally.byStatus": "ByStatus is the count per status, one key per status seen.",
			"VerificationTally.total":    "Total is the sum across statuses.",
		},
		Response: json.RawMessage(`{"provider":"manual","verifications":{"byStatus":{"pending":2},"total":2},"disclaimer":"..."}`),
	})
	zip.Describe("GET /v1/compliance/subjects", zip.Doc{
		Description: "listSubjects returns the caller org's subjects without their contact PII: a row\nsays whether an email is on file, never what it is.",
		Fields: map[string]string{
			"Page.limit":               "Limit caps the rows returned; 0 means the store's own default.",
			"SubjectList.data":         "Data is one row per subject. It carries no name or email — only whether an\nemail is on file — so a list read never sprays PII.",
			"subjectSummary.createdAt": "CreatedAt is the unix second the subject was recorded.",
			"subjectSummary.hasEmail":  "HasEmail reports whether contact email is on file, without disclosing it.",
			"subjectSummary.id":        "ID is the subject id, the value every verification and accreditation names.",
			"subjectSummary.kind":      "Kind is individual or business.",
			"subjectSummary.ref":       "Ref is the org's own opaque external id for this subject.",
		},
		Example: json.RawMessage(`{"limit":50}`),
	})
	zip.Describe("GET /v1/compliance/subjects/:id", zip.Doc{
		Description: "getSubject returns one of the caller org's subjects INCLUDING its contact PII.\nThis is the only surface that discloses it, and only to the owning org.",
		Fields: map[string]string{
			"Subject.ref":   "the org's own opaque external id for this subject",
			"SubjectRef.id": "ID is the subject id from the path, as returned by create.",
		},
		Example: json.RawMessage(`{"id":"sub_4c1e9b7a2d6f0538e4a7c9b1d3f5027a"}`),
	})
	zip.Describe("GET /v1/compliance/verifications", zip.Doc{
		Description: "listVerifications returns the caller org's verifications, each with its\nprovider-reported status and no subject PII.",
		Fields: map[string]string{
			"CheckView.createdAt":         "CreatedAt is the unix second the verification was started.",
			"CheckView.decidedAt":         "DecidedAt is the unix second it reached a terminal status.",
			"CheckView.decidedBy":         "DecidedBy names who settled it: the provider, or the reviewer's user id.",
			"CheckView.id":                "ID is the verification id.",
			"CheckView.kind":              "Kind is individual or business.",
			"CheckView.provider":          "Provider is the identity provider that holds the verification.",
			"CheckView.status":            "Status is provider-reported or pending — never platform-asserted.",
			"CheckView.subjectId":         "SubjectID is the opaque subject this verifies.",
			"CheckView.updatedAt":         "UpdatedAt is the unix second of the last recorded change.",
			"CheckView.verifyUrl":         "VerifyURL is where the subject completes verification, while one is open.",
			"Page.limit":                  "Limit caps the rows returned; 0 means the store's own default.",
			"VerificationList.data":       "Data is one row per verification, each carrying only opaque ids and the\nprovider-reported status — never subject PII.",
			"VerificationList.disclaimer": "Disclaimer is the boundary made visible on the wire.",
		},
		Example: json.RawMessage(`{"limit":50}`),
	})
	zip.Describe("GET /v1/compliance/verifications/:id", zip.Doc{
		Description: "getVerification returns one of the caller org's verifications with its current\nrecorded status.",
		Fields: map[string]string{
			"CheckRef.id":         "ID is the verification id from the path, as returned by start.",
			"CheckView.createdAt": "CreatedAt is the unix second the verification was started.",
			"CheckView.decidedAt": "DecidedAt is the unix second it reached a terminal status.",
			"CheckView.decidedBy": "DecidedBy names who settled it: the provider, or the reviewer's user id.",
			"CheckView.id":        "ID is the verification id.",
			"CheckView.kind":      "Kind is individual or business.",
			"CheckView.provider":  "Provider is the identity provider that holds the verification.",
			"CheckView.status":    "Status is provider-reported or pending — never platform-asserted.",
			"CheckView.subjectId": "SubjectID is the opaque subject this verifies.",
			"CheckView.updatedAt": "UpdatedAt is the unix second of the last recorded change.",
			"CheckView.verifyUrl": "VerifyURL is where the subject completes verification, while one is open.",
		},
		Example: json.RawMessage(`{"id":"chk_4c1e9b7a2d6f0538e4a7c9b1d3f5027a"}`),
	})
	zip.Describe("POST /v1/compliance/accreditation", zip.Doc{
		Description: "createAccreditation records an ASSERTED accreditation state for one of the org's\nsubjects. A create can only ever record an assertion — every confirmed, rejected\nor expired state goes through the decision endpoint, so it always names the\nreviewer who recorded it.",
		Fields: map[string]string{
			"AccreditationRequest.basis":         "Basis is what qualifies the subject: income, net_worth,\nprofessional_license or entity.",
			"AccreditationRequest.evidenceDocId": "EvidenceDocID references the supporting document, if any.",
			"AccreditationRequest.expiresAt":     "ExpiresAt is the unix second the assertion lapses, 0 for none.",
			"AccreditationRequest.method":        "Method is how the state was established: self_attested, third_party_letter\nor provider_verified.",
			"AccreditationRequest.note":          "Note is a non-PII operator note.",
			"AccreditationRequest.status":        "Status may only be asserted (or empty, which means asserted). Every\nconfirmed, rejected or expired state is set through the decision endpoint,\nso it always carries the reviewer who recorded it.",
			"AccreditationRequest.subjectId":     "SubjectID is the subject the assertion is about; it must exist in the org.",
			"AccreditationView.basis":            "Basis is what qualifies the subject.",
			"AccreditationView.createdAt":        "CreatedAt is the unix second the record was created.",
			"AccreditationView.evidenceDocId":    "EvidenceDocID references the supporting document, when one is named.",
			"AccreditationView.expiresAt":        "ExpiresAt is the unix second the record lapses, when one is set.",
			"AccreditationView.id":               "ID is the accreditation id.",
			"AccreditationView.method":           "Method is how the state was established.",
			"AccreditationView.note":             "Note is a non-PII operator note, when one is set.",
			"AccreditationView.reviewerSub":      "ReviewerSub is the org user who recorded the decision, once decided.",
			"AccreditationView.status":           "Status is the tracked state: asserted until a reviewer decides.",
			"AccreditationView.subjectId":        "SubjectID is the opaque subject the record is about.",
			"AccreditationView.updatedAt":        "UpdatedAt is the unix second of the last recorded change.",
		},
		Example: json.RawMessage(`{"subjectId":"sub_4c1e9b7a2d6f0538e4a7c9b1d3f5027a","method":"self_attested","basis":"income"}`),
	})
	zip.Describe("POST /v1/compliance/accreditation/:id/decision", zip.Doc{
		Description: "decideAccreditation records a reviewer's decision on an accreditation record and\nattributes it to them. It requires an org admin or platform reviewer: the\nplatform never confirms on its own, and even a provider_verified state carries\nthe reviewer who recorded the evidence.",
		Fields: map[string]string{
			"AccreditationDecision.id":        "ID is the accreditation id from the path; a body value is ignored.",
			"AccreditationDecision.status":    "Status must be reviewer_confirmed, provider_verified, rejected or expired.",
			"AccreditationView.basis":         "Basis is what qualifies the subject.",
			"AccreditationView.createdAt":     "CreatedAt is the unix second the record was created.",
			"AccreditationView.evidenceDocId": "EvidenceDocID references the supporting document, when one is named.",
			"AccreditationView.expiresAt":     "ExpiresAt is the unix second the record lapses, when one is set.",
			"AccreditationView.id":            "ID is the accreditation id.",
			"AccreditationView.method":        "Method is how the state was established.",
			"AccreditationView.note":          "Note is a non-PII operator note, when one is set.",
			"AccreditationView.reviewerSub":   "ReviewerSub is the org user who recorded the decision, once decided.",
			"AccreditationView.status":        "Status is the tracked state: asserted until a reviewer decides.",
			"AccreditationView.subjectId":     "SubjectID is the opaque subject the record is about.",
			"AccreditationView.updatedAt":     "UpdatedAt is the unix second of the last recorded change.",
		},
		Example: json.RawMessage(`{"id":"acc_4c1e9b7a2d6f0538e4a7c9b1d3f5027a","status":"reviewer_confirmed"}`),
	})
	zip.Describe("POST /v1/compliance/subjects", zip.Doc{
		Description: "createSubject records a person or business to verify. It needs an email or the\norg's own ref. Contact PII is sealed at rest and never returned by a list read.",
		Fields: map[string]string{
			"Subject.ref":          "the org's own opaque external id for this subject",
			"SubjectRequest.email": "Email is contact PII, sealed at rest and returned only on a single-subject\nread to the owning org. Required when no ref is given.",
			"SubjectRequest.kind":  "Kind is individual or business, required.",
			"SubjectRequest.name":  "Name is contact PII, sealed at rest.",
			"SubjectRequest.ref":   "Ref is the org's own opaque external id; required when no email is given.",
		},
		Example: json.RawMessage(`{"kind":"individual","ref":"cust_814","email":"ada@acme.com","name":"Ada Lovelace"}`),
	})
	zip.Describe("POST /v1/compliance/verifications", zip.Doc{
		Description: "startVerification opens a KYC/KYB verification for a subject through the wired\nprovider, creating the subject inline when no subjectId is given. A start is\nnever a decision: the record comes back pending even if the provider reports\notherwise, so no path here yields a verified check.",
		Fields: map[string]string{
			"CheckView.createdAt":                "CreatedAt is the unix second the verification was started.",
			"CheckView.decidedAt":                "DecidedAt is the unix second it reached a terminal status.",
			"CheckView.decidedBy":                "DecidedBy names who settled it: the provider, or the reviewer's user id.",
			"CheckView.id":                       "ID is the verification id.",
			"CheckView.kind":                     "Kind is individual or business.",
			"CheckView.provider":                 "Provider is the identity provider that holds the verification.",
			"CheckView.status":                   "Status is provider-reported or pending — never platform-asserted.",
			"CheckView.subjectId":                "SubjectID is the opaque subject this verifies.",
			"CheckView.updatedAt":                "UpdatedAt is the unix second of the last recorded change.",
			"CheckView.verifyUrl":                "VerifyURL is where the subject completes verification, while one is open.",
			"StartVerificationRequest.email":     "Email is contact PII, for an inline subject; sealed at rest.",
			"StartVerificationRequest.kind":      "Kind is individual or business, for an inline subject.",
			"StartVerificationRequest.name":      "Name is contact PII, for an inline subject; sealed at rest.",
			"StartVerificationRequest.ref":       "Ref is the org's own opaque external id, for an inline subject.",
			"StartVerificationRequest.subjectId": "SubjectID verifies an existing subject. When empty, one is created inline\nfrom kind/ref/email/name.",
		},
		Example: json.RawMessage(`{"kind":"individual","email":"ada@acme.com","name":"Ada Lovelace"}`),
	})
	zip.Describe("POST /v1/compliance/verifications/:id/decision", zip.Doc{
		Description: "decideVerification records a reviewer's manual decision on a verification — the\nhuman-in-the-loop path, and the only route to a passing status when no real\nprovider is wired. It requires an org admin or platform reviewer and is\nattributed to them, and it can only record reviewer_confirmed or manual_review.",
		Fields: map[string]string{
			"CheckDecision.id":     "ID is the verification id from the path; a body value is ignored.",
			"CheckDecision.status": "Status must be reviewer_confirmed (a pass) or manual_review (withhold). A\nprovider_verified or provider_rejected status is the provider's to report and\nis refused here.",
			"CheckView.createdAt":  "CreatedAt is the unix second the verification was started.",
			"CheckView.decidedAt":  "DecidedAt is the unix second it reached a terminal status.",
			"CheckView.decidedBy":  "DecidedBy names who settled it: the provider, or the reviewer's user id.",
			"CheckView.id":         "ID is the verification id.",
			"CheckView.kind":       "Kind is individual or business.",
			"CheckView.provider":   "Provider is the identity provider that holds the verification.",
			"CheckView.status":     "Status is provider-reported or pending — never platform-asserted.",
			"CheckView.subjectId":  "SubjectID is the opaque subject this verifies.",
			"CheckView.updatedAt":  "UpdatedAt is the unix second of the last recorded change.",
			"CheckView.verifyUrl":  "VerifyURL is where the subject completes verification, while one is open.",
		},
		Example: json.RawMessage(`{"id":"chk_4c1e9b7a2d6f0538e4a7c9b1d3f5027a","status":"reviewer_confirmed"}`),
	})
	zip.Describe("POST /v1/compliance/verifications/:id/refresh", zip.Doc{
		Description: "refreshVerification is the internal PULL reconcile: an org member triggers a poll of\nthe wired provider for the current decision. For the Manual provider this stays\npending; for a hosted provider it reflects the provider's settled status,\nprovider-attributed. A poll error is a 502, never a verification.\nrefreshVerification polls the wired provider for a verification's current\ndecision and records it, attributed to the provider. With the manual provider it\nstays pending; a poll error is a 502 and never a verification.",
		Fields: map[string]string{
			"CheckRef.id":         "ID is the verification id from the path, as returned by start.",
			"CheckView.createdAt": "CreatedAt is the unix second the verification was started.",
			"CheckView.decidedAt": "DecidedAt is the unix second it reached a terminal status.",
			"CheckView.decidedBy": "DecidedBy names who settled it: the provider, or the reviewer's user id.",
			"CheckView.id":        "ID is the verification id.",
			"CheckView.kind":      "Kind is individual or business.",
			"CheckView.provider":  "Provider is the identity provider that holds the verification.",
			"CheckView.status":    "Status is provider-reported or pending — never platform-asserted.",
			"CheckView.subjectId": "SubjectID is the opaque subject this verifies.",
			"CheckView.updatedAt": "UpdatedAt is the unix second of the last recorded change.",
			"CheckView.verifyUrl": "VerifyURL is where the subject completes verification, while one is open.",
		},
		Example: json.RawMessage(`{"id":"chk_4c1e9b7a2d6f0538e4a7c9b1d3f5027a"}`),
	})
}
