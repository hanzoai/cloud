// Code generated by zipdoc; DO NOT EDIT.

package visor

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/visor/clusters/:clusterId/pools/:poolId", zip.Doc{
		Description: "Removes a node pool from one of the caller org's clusters. The owner\nscopes the delete to the caller's tenant; provider+clusterId drive the\nprovider-side removal. Answers 204.",
		Fields: map[string]string{
			"poolRef.clusterId": "ClusterID and PoolID address the pool, from the URL path.",
			"poolRef.provider":  "Provider is the cloud the cluster lives on, from ?provider=. Required.",
		},
	})
	zip.Describe("DELETE /v1/visor/clusters/:id", zip.Doc{
		Description: "Removes a BYO cluster from the caller org's fleet. It only ever\ntouches BYO clusters — a managed cluster's nodes are removed through the node-pool\nroutes — and answers 404 when the name is not in this org's fleet.",
		Fields: map[string]string{
			"clusterDetached.detached": "Detached is the lower-cased fleet name that was removed.",
			"clusterRef.id":            "ID is the cluster's fleet name (the `name` it was attached under), matched\nlower-cased.",
		},
		Response: json.RawMessage(`{"detached":"lab"}`),
	})
	zip.Describe("DELETE /v1/visor/compute/bots/:id", zip.Doc{
		Description: "Tears down both halves of a bot: it unbinds the agent (best-effort — a\nbot with no binding still deletes), then terminates the machine. Answers 204.",
		Fields: map[string]string{
			"botRef.id": "ID is the bot machine's id — the same id the machines surface addresses it\nby. Scoped to the caller's org upstream, so another tenant's id is 404.",
		},
	})
	zip.Describe("DELETE /v1/visor/k8s/clusters/:id", zip.Doc{
		Description: "Destroys a DOKS cluster by id and answers 204. ADMIN-GATED, like\ncreate. Visor scopes the delete to the org (refuses a foreign id), so this can\nonly ever remove the caller org's own cluster.",
		Fields: map[string]string{
			"k8sClusterRef.id": "ID is the provider's DOKS cluster id. Visor scopes the lookup to the caller's\norg, so another tenant's id resolves to not-found rather than their cluster.",
		},
	})
	zip.Describe("DELETE /v1/visor/machines/:id", zip.Doc{
		Description: "Terminates one of the caller org's machines. Visor takes the\nmachine identity as owner+name, and the owner is the validated principal, so a\ncaller can only ever terminate its own tenant's machine. Answers 204.",
		Fields: map[string]string{
			"machineRef.id": "ID is the machine's org-scoped NAME — the stable key Visor addresses a\nmachine by (owner/name), not the ephemeral provider id.",
		},
	})
	zip.Describe("DELETE /v1/visor/machines/:id/agent", zip.Doc{
		Description: "Detaches the agent runtime from one of the caller org's\nmachines. The machine stays — this halts the bot, it does not terminate the\ncompute. Answers 204.",
		Fields: map[string]string{
			"machineRef.id": "ID is the machine's org-scoped NAME — the stable key Visor addresses a\nmachine by (owner/name), not the ephemeral provider id.",
		},
	})
	zip.Describe("GET /v1/visor/clusters", zip.Doc{
		Description: "Returns the caller org's clusters from both sources: the managed\nclusters projected from Visor's node pools, and the BYO clusters attached to the\ncaller's project. A Visor outage costs the managed half only — the BYO half\nstill lists, because a page that 502s on an optional provider is worse than a\npage that shows what it can.",
		Fields: map[string]string{
			"clusterList.clusters":      "Clusters is the merged fleet — kind \"managed\" for Visor-provisioned, \"byo\"\nfor an attached kubeconfig.",
			"clusterList.degraded":      "Degraded names any source that did not answer, so an empty Clusters means\n\"you have none\" only when this is absent. Omitted when everything answered,\nso a healthy response is unchanged. See degraded.go.",
			"clusterView.amdGpu":        "AmdGPU is the same count for `amd.com/gpu`: AMD accelerators across the BYO\ncluster's nodes, as of the attach.",
			"clusterView.createdAt":     "CreatedAt is when the cluster started existing: the earliest creation time\namong its pools for a managed cluster, and for a BYO one the RFC 3339 moment\nit was attached. Empty when the source states none.",
			"clusterView.doClusterId":   "DoClusterID carries the SAME id as DoksClusterID. Both names exist because\nthe console's Cluster type reads either one; neither is a second identifier.",
			"clusterView.doksClusterId": "DoksClusterID is the provider's own id for the cluster, and the value the\n/v1/visor/k8s/clusters/:id routes take. Empty for a BYO cluster: an attached\nkubeconfig was never provisioned, so there is no provider id to state.",
			"clusterView.kind":          "Kind says which of the two kinds of cluster this row is, and there are only\ntwo: \"managed\" — Visor provisioned it and Hanzo's account pays the provider —\nor \"byo\", an existing cluster the org attached by kubeconfig.",
			"clusterView.name":          "Name is the cluster's name: the provider's for a managed cluster, and for a\nBYO one the lower-cased fleet name it was attached under — which is also how\nthe detach route addresses it.",
			"clusterView.nodeCount":     "NodeCount is how many worker nodes the cluster has — the sum over its pools\nfor a managed cluster, and for a BYO one the node count read off the cluster\nwhen it was attached.",
			"clusterView.nodePools":     "NodePools is the authoritative node inventory — every pool, each with its own\nsize and count. It is empty in two cases that are not \"no pools\": a row from\nthe /v1/visor/k8s/clusters LIST, which is deliberately lightweight and whose\n:id detail carries them, and a BYO cluster, whose pools were never read.",
			"clusterView.nodeSize":      "NodeSize is a display convenience: the size slug of the FIRST pool. A cluster\nmixing sizes has more than one, and NodePools is where they all are.",
			"clusterView.nvidiaGpu":     "NvidiaGPU is how many NVIDIA accelerators the cluster's nodes advertise, the\nsum of `nvidia.com/gpu` allocatable across them. BYO only, and counted ONCE\nwhen the cluster was attached — it is an inventory, not live capacity.",
			"clusterView.region":        "Region is the provider region slug for a managed cluster. A BYO cluster has\nno region we can read, so it carries the free-form `provider` label the\nattach named it with (\"gke\", \"on-prem\") instead.",
			"clusterView.status":        "Status is the cluster's state: the provider's own word for a managed cluster\n(\"running\", \"provisioning\"), \"unknown\" when the provider stated none, and\nalways \"attached\" for a BYO cluster — that one says the kubeconfig is on\nfile, not that the cluster is reachable this second.",
			"nodePoolView.autoScale":    "AutoScale reports whether the provider's cluster autoscaler owns this pool's\nsize, moving Count between MinNodes and MaxNodes as workloads demand. False\nmeans Count changes only when someone scales the pool.",
			"nodePoolView.count":        "Count is how many nodes the pool has right now. Always present, so 0 means a\npool that is genuinely empty rather than a figure the provider withheld.",
			"nodePoolView.maxNodes":     "MaxNodes is the ceiling the autoscaler will not grow the pool past, and so\nthe bound on what this pool can cost. Read it only with AutoScale set.",
			"nodePoolView.minNodes":     "MinNodes is the floor the autoscaler will not shrink the pool below. Read it\nonly with AutoScale set — the provider ignores it otherwise.",
			"nodePoolView.name":         "Name is the pool's name as the provider knows it.",
			"nodePoolView.poolId":       "PoolID is the provider's id for the pool — the value the scale and delete\nroutes address it by. It falls back to the pool's name when the provider\nanswered without one, so it is always something the routes accept.",
			"nodePoolView.size":         "Size is the provider size slug every node in the pool runs at\n(\"s-4vcpu-8gb\", \"gpu-h100x8-640gb\"). One pool is one size — a mixed cluster\nis several pools.",
			"sourceFailure.reason":      "Reason is a terse, log-safe summary — never the upstream's response body.",
			"sourceFailure.source":      "Source is the dependency that failed, named as an operator names it.",
		},
		Response: json.RawMessage(`{"clusters":[{"doksClusterId":"cl-1","name":"prod","status":"running","nodePools":[{"poolId":"p-1","name":"gpu","size":"gpu-h100x8-640gb","count":2}],"nodeSize":"gpu-h100x8-640gb","nodeCount":2,"kind":"managed"}]}`),
	})
	zip.Describe("GET /v1/visor/compute/bots", zip.Doc{
		Description: "Returns the caller org's bot machines — the kind=bot machines — each\njoined with the agent binding that says which cloud Agent it runs.\n\nThe bindings are read ONCE and joined by machine id, so the list is O(1) upstream\ncalls, not N+1. A bindings read that fails only costs the reconciled status: a bot\nstill lists without it.",
		Fields: map[string]string{
			"agentBinding.agentName":   "AgentName is the cloud Agent (/v1/agents) this machine runs — the agent a\nmessage to the bot is actually run against. It is the one field that decides\nwhat the bot DOES.",
			"agentBinding.botVersion":  "BotVersion pins the @hanzo/bot runtime version the machine runs. Empty means\nthe machine took the default in force when it was bound.",
			"agentBinding.createdTime": "CreatedTime is when the binding was first made.",
			"agentBinding.machineId":   "MachineId is the bound machine as vm addresses it, owner-qualified\n(\"<org>/<machine>\"). The unqualified half is what this surface's :id routes\ntake.",
			"agentBinding.message":     "Message is vm's human-readable detail on Status (\"machine provisioning;\n@hanzo/bot runtime not yet confirmed\") — the reason behind the state, not a\nsecond state.",
			"agentBinding.name":        "Name is the binding's own key, which is the machine's id: a machine hosts at\nmost one agent, so the binding is named for it. This is the key a bots list\njoins bindings onto machines by.",
			"agentBinding.org":         "Org is the Hanzo tenant the binding belongs to.",
			"agentBinding.owner":       "Owner is the tenant vm filed the binding under, resolved from the ?owner it\nwas called with — which is the caller's validated org and never a body field.",
			"agentBinding.provider":    "Provider is the cloud the bound machine runs on, carried here so a bindings\nlist says where each bot lives without a second read per machine.",
			"agentBinding.publicIp":    "PublicIp is the bound machine's public address as vm recorded it on the\nbinding. Empty while the machine has none yet.",
			"agentBinding.status":      "Status is the binding's lifecycle in VM's OWN words — \"Pending\" while the\nmachine provisions and the runtime is unconfirmed, \"running\" once vm has\nconfirmed it. The vocabulary is vm's and passes through unmapped, which is\nwhy its capitalization does not match the machine states beside it, and it is\nvm's reconciled reading rather than anything asserted here.",
			"agentBinding.updatedTime": "UpdatedTime is when vm last reconciled it — the age of Status.",
			"botList.bots":             "Bots is one row per kind=bot machine, each joined with its agent binding\nwhen it has one.",
			"botView.agent":            "Agent is the cloud Agent this machine runs, lifted out of the binding so a\nlist of bots reads without following one. Empty means the machine is a bot\nmachine with nothing bound — it costs money and answers nothing.",
			"botView.binding":          "Binding is the record joining this machine to that agent, carrying vm's own\nreconciled status and its reason. Absent means no runtime is bound, which is\nalso what a stopped bot looks like: stopping unbinds and leaves the machine\nrunning.",
			"machineView.createdTime":  "CreatedTime is when the machine came into being: the provider's own creation\ntimestamp for a Visor machine, passed through in whatever form it states it,\nand for a BYO machine the RFC 3339 moment it first dialed in.",
			"machineView.gpu":          "GPU names the accelerators this machine holds (\"H100\", or \"2× NVIDIA GB10\"\nfor a BYO machine reporting a matched pair). Empty means the machine is not\na GPU machine — the size slug does not parse as one, or nvidia-smi found\nnothing.",
			"machineView.id":           "ID addresses this machine on the /v1/visor/machines/:id routes: the\norg-scoped NAME Visor keys a machine by, falling back to the provider id for\na machine that has no name. A BYO machine's is the id it dialed in under.",
			"machineView.image":        "Image is the OS image the machine booted from, as the provider names it.",
			"machineView.mem":          "Mem is system RAM rendered for a human (\"8 GB\"), not a number to compute\nwith. Empty when the provider's figure is ambiguous, or when the only figure\navailable is a GPU slug's gb — that is VRAM, and reporting it as system RAM\nwould be a fabrication. A BYO machine's RAM is on /v1/visor/fleet/workers.",
			"machineView.name":         "Name is the label to show a human — Visor's displayName, or the machine name\nwhen it carries none. A BYO machine's is its hostname. It is not an address:\nID is what the routes take.",
			"machineView.os":           "Os is the operating system on the machine — Visor's record for a provisioned\none, the host's own report (linux, darwin, windows) for a BYO one.",
			"machineView.privateIp":    "PrivateIp is the address on the provider's own network, reachable from the\norg's other machines in the same region. Empty on the same terms as PublicIp.",
			"machineView.provider":     "Provider is the cloud that runs the machine (\"digitalocean\"), or \"byo\" for\none the operator dialed in with `hanzo link`.",
			"machineView.publicIp":     "PublicIp is the internet-facing address the provider assigned. Empty while a\nmachine is still provisioning, and empty for a BYO machine — it dials out\nfrom behind NAT, so no address is ever learned for it.",
			"machineView.region":       "Region is the provider region slug (\"sfo3\"), or the zone when the provider\nreports only that. \"on-prem\" for a BYO machine, which has no cloud region.",
			"machineView.status":       "Status is the lifecycle state in the PROVIDER's own words (\"active\",\n\"running\", \"off\"), passed through rather than mapped onto a vocabulary of\nours. A BYO machine's is \"online\" or \"offline\", decided by whether its last\nheartbeat is within 90s.",
			"machineView.type":         "Type is the provider SIZE SLUG the machine runs at (\"s-2vcpu-4gb\",\n\"gpu-h100x8-640gb\") — the value a launch asks for, and what Vcpu/Mem/GPU are\nread out of when the provider states them no other way. \"byo-gpu\" for a\ndialed-in machine, which was never bought from a size catalog.",
			"machineView.vcpu":         "Vcpu is logical cores — the provider's own cpuSize when that is a clean\ninteger, else the count read out of the size slug (4 from \"s-4vcpu-8gb\").\nABSENT, never 0, when neither says. A BYO machine leaves it absent here; its\nreal core count is on GET /v1/visor/fleet/workers.",
		},
		Response: json.RawMessage(`{"bots":[{"id":"drop-a","name":"bot-a","status":"running","agent":"bot-a","binding":{"machineId":"drop-a","agentName":"bot-a","status":"running"}}]}`),
	})
	zip.Describe("GET /v1/visor/compute/bots/:id", zip.Doc{
		Description: "Returns one of the caller org's bot machines with its agent binding.\n\nA machine counts as a Bot if it carries the hanzo-kind:bot tag OR has an agent\nbinding — either signal is authoritative, so a bot resolves even before its\ncloud-init has stamped every tag. A machine that is neither is 404: this route\nanswers for bots, not for machines.",
		Fields: map[string]string{
			"agentBinding.agentName":   "AgentName is the cloud Agent (/v1/agents) this machine runs — the agent a\nmessage to the bot is actually run against. It is the one field that decides\nwhat the bot DOES.",
			"agentBinding.botVersion":  "BotVersion pins the @hanzo/bot runtime version the machine runs. Empty means\nthe machine took the default in force when it was bound.",
			"agentBinding.createdTime": "CreatedTime is when the binding was first made.",
			"agentBinding.machineId":   "MachineId is the bound machine as vm addresses it, owner-qualified\n(\"<org>/<machine>\"). The unqualified half is what this surface's :id routes\ntake.",
			"agentBinding.message":     "Message is vm's human-readable detail on Status (\"machine provisioning;\n@hanzo/bot runtime not yet confirmed\") — the reason behind the state, not a\nsecond state.",
			"agentBinding.name":        "Name is the binding's own key, which is the machine's id: a machine hosts at\nmost one agent, so the binding is named for it. This is the key a bots list\njoins bindings onto machines by.",
			"agentBinding.org":         "Org is the Hanzo tenant the binding belongs to.",
			"agentBinding.owner":       "Owner is the tenant vm filed the binding under, resolved from the ?owner it\nwas called with — which is the caller's validated org and never a body field.",
			"agentBinding.provider":    "Provider is the cloud the bound machine runs on, carried here so a bindings\nlist says where each bot lives without a second read per machine.",
			"agentBinding.publicIp":    "PublicIp is the bound machine's public address as vm recorded it on the\nbinding. Empty while the machine has none yet.",
			"agentBinding.status":      "Status is the binding's lifecycle in VM's OWN words — \"Pending\" while the\nmachine provisions and the runtime is unconfirmed, \"running\" once vm has\nconfirmed it. The vocabulary is vm's and passes through unmapped, which is\nwhy its capitalization does not match the machine states beside it, and it is\nvm's reconciled reading rather than anything asserted here.",
			"agentBinding.updatedTime": "UpdatedTime is when vm last reconciled it — the age of Status.",
			"botRef.id":                "ID is the bot machine's id — the same id the machines surface addresses it\nby. Scoped to the caller's org upstream, so another tenant's id is 404.",
			"botView.agent":            "Agent is the cloud Agent this machine runs, lifted out of the binding so a\nlist of bots reads without following one. Empty means the machine is a bot\nmachine with nothing bound — it costs money and answers nothing.",
			"botView.binding":          "Binding is the record joining this machine to that agent, carrying vm's own\nreconciled status and its reason. Absent means no runtime is bound, which is\nalso what a stopped bot looks like: stopping unbinds and leaves the machine\nrunning.",
			"machineView.createdTime":  "CreatedTime is when the machine came into being: the provider's own creation\ntimestamp for a Visor machine, passed through in whatever form it states it,\nand for a BYO machine the RFC 3339 moment it first dialed in.",
			"machineView.gpu":          "GPU names the accelerators this machine holds (\"H100\", or \"2× NVIDIA GB10\"\nfor a BYO machine reporting a matched pair). Empty means the machine is not\na GPU machine — the size slug does not parse as one, or nvidia-smi found\nnothing.",
			"machineView.id":           "ID addresses this machine on the /v1/visor/machines/:id routes: the\norg-scoped NAME Visor keys a machine by, falling back to the provider id for\na machine that has no name. A BYO machine's is the id it dialed in under.",
			"machineView.image":        "Image is the OS image the machine booted from, as the provider names it.",
			"machineView.mem":          "Mem is system RAM rendered for a human (\"8 GB\"), not a number to compute\nwith. Empty when the provider's figure is ambiguous, or when the only figure\navailable is a GPU slug's gb — that is VRAM, and reporting it as system RAM\nwould be a fabrication. A BYO machine's RAM is on /v1/visor/fleet/workers.",
			"machineView.name":         "Name is the label to show a human — Visor's displayName, or the machine name\nwhen it carries none. A BYO machine's is its hostname. It is not an address:\nID is what the routes take.",
			"machineView.os":           "Os is the operating system on the machine — Visor's record for a provisioned\none, the host's own report (linux, darwin, windows) for a BYO one.",
			"machineView.privateIp":    "PrivateIp is the address on the provider's own network, reachable from the\norg's other machines in the same region. Empty on the same terms as PublicIp.",
			"machineView.provider":     "Provider is the cloud that runs the machine (\"digitalocean\"), or \"byo\" for\none the operator dialed in with `hanzo link`.",
			"machineView.publicIp":     "PublicIp is the internet-facing address the provider assigned. Empty while a\nmachine is still provisioning, and empty for a BYO machine — it dials out\nfrom behind NAT, so no address is ever learned for it.",
			"machineView.region":       "Region is the provider region slug (\"sfo3\"), or the zone when the provider\nreports only that. \"on-prem\" for a BYO machine, which has no cloud region.",
			"machineView.status":       "Status is the lifecycle state in the PROVIDER's own words (\"active\",\n\"running\", \"off\"), passed through rather than mapped onto a vocabulary of\nours. A BYO machine's is \"online\" or \"offline\", decided by whether its last\nheartbeat is within 90s.",
			"machineView.type":         "Type is the provider SIZE SLUG the machine runs at (\"s-2vcpu-4gb\",\n\"gpu-h100x8-640gb\") — the value a launch asks for, and what Vcpu/Mem/GPU are\nread out of when the provider states them no other way. \"byo-gpu\" for a\ndialed-in machine, which was never bought from a size catalog.",
			"machineView.vcpu":         "Vcpu is logical cores — the provider's own cpuSize when that is a clean\ninteger, else the count read out of the size slug (4 from \"s-4vcpu-8gb\").\nABSENT, never 0, when neither says. A BYO machine leaves it absent here; its\nreal core count is on GET /v1/visor/fleet/workers.",
		},
		Response: json.RawMessage(`{"id":"drop-a","name":"bot-a","status":"running","agent":"bot-a","binding":{"machineId":"drop-a","agentName":"bot-a","status":"running"}}`),
	})
	zip.Describe("GET /v1/visor/compute/regions", zip.Doc{
		Description: "Regions lists the regions a machine can be launched in.\n\nThe catalog is GLOBAL — identical for every tenant — so no owner is forwarded\nupstream. It is still org-gated, because a catalog is a map of what this\ndeployment can spend money in and an anonymous caller has no business reading it.",
	})
	zip.Describe("GET /v1/visor/compute/sizes", zip.Doc{
		Description: "Sizes lists the machine sizes available to launch, with their specifications.\n\nGlobal and org-gated, exactly as the region catalog is, and for the same reasons.",
	})
	zip.Describe("GET /v1/visor/fleet", zip.Doc{
		Description: "Returns every compute unit the caller's org has, from every source, each\ncarrying its latest utilization: agent run-targets, the BYO machines that dialed\nin, attached BYO clusters and Visor-provisioned machines.\n\nA unit with a live snapshot of its own keeps it; the rest are overlaid from the\nutilization series, and only when the sample agrees about the SOURCE — two planes\ncould mint the same unit id, and a board must never show one machine's load on\nanother's row. BYO GPU units also carry their gpu-jobs queue depth. Every source\nis folded in independently: a broken one costs its own rows and nothing else.",
		Fields: map[string]string{
			"fleetBoard.units":     "Units is the union across sources — agent run-targets, BYO workers, BYO\nclusters and Visor machines — each row naming the source it came from.",
			"fleetMetrics.at":      "At is when this reading was MEASURED, RFC 3339 in UTC — not when the board\nwas built. A console decides staleness by comparing it to now; the board\ndeliberately does not decide that for it.",
			"fleetMetrics.gpuUtil": "GPUUtil is aggregate accelerator utilization as a FRACTION of 1 — 0.42 is\n42% busy, never 42. Across all of the unit's cards, not one of them.",
			"fleetMetrics.load1":   "Load1 is the host's 1-minute load average — runnable processes, not a\npercentage, so it is read against the unit's core count and can exceed 1.",
			"fleetMetrics.memFree": "MemFree is host memory still available, in BYTES. It is what the source\nreported, not fleetSpec.Memory minus MemUsed.",
			"fleetMetrics.memUsed": "MemUsed is host memory in use, in BYTES.",
			"fleetSpec.arch":       "Arch is the CPU architecture, amd64 or arm64, and it is what decides whether\na binary built for the fleet will run here. Only the sources that report one\ncarry it (a linked run-target, a BYO worker).",
			"fleetSpec.cpus":       "CPUs is logical cores on the unit.",
			"fleetSpec.gpuModel":   "GPUModel names the FIRST accelerator (\"NVIDIA GB10\") as the representative of\nthe set; GPUs carries how many. Empty for a cluster, whose cards are counted\nrather than modelled, and for a unit with none.",
			"fleetSpec.gpus":       "GPUs is how many accelerators the unit has. For a cluster it is the vendor\ntotals summed across every node, so it counts cards, not machines.",
			"fleetSpec.memory":     "Memory is total system RAM in BYTES — not GB, and not what is free right now\n(fleetMetrics carries that). Absent when the source reports no RAM figure.",
			"fleetSpec.os":         "OS is the operating system the unit runs: linux, darwin or windows. Empty\nwhen the source does not report one — a cluster row does not.",
			"fleetUnit.host":       "Host is the unit's hostname. Empty for a unit that is not one host: a cluster\nrow has no hostname to report.",
			"fleetUnit.kind":       "Kind is what the unit IS: laptop, cloud, gpu, cluster, machine or worker.",
			"fleetUnit.label":      "Label is the name to show a human — a target's label, a worker's hostname, a\nmachine's display name. Empty when the source has none to give.",
			"fleetUnit.metrics":    "Metrics is the unit's latest utilization: its own live snapshot when it keeps\none (a run-target's heartbeat wins), else the newest sample from the series\nfor the SAME source. Absent means nothing is known about this unit's load —\nwhich is deliberately not the same as a reading of zero.",
			"fleetUnit.queued":     "Queued is how many renders are waiting on THIS GPU's own lane in the org's\ngpu-jobs queue. BYO units only — an agent run-target dispatches, it does not\nqueue — and omitted when nothing is waiting.",
			"fleetUnit.running":    "Running is what the unit is executing right now: agent sessions in flight for\na run-target, claimed renders for a BYO GPU.",
			"fleetUnit.sessions":   "Sessions is how many agent sessions are open on this unit. Always present,\nand 0 for a source that cannot host agent sessions at all — a fact about that\nplane, not a gap in the reading.",
			"fleetUnit.source":     "Source is the plane this row came from: \"agent\" (a linked run-target), \"byo\"\n(a worker or cluster the org dialed in) or \"visor\" (a machine Hanzo\nprovisioned). It is half the row's identity, and it says which face owns the\nunit — /v1/agents/targets, /v1/visor/fleet/workers, /v1/visor/machines.",
			"fleetUnit.spec":       "Spec is the unit's static capability. Absent when the source reported none —\nunknown capability, never a zeroed one.",
			"fleetUnit.status":     "Status is liveness in the SOURCE's own vocabulary, because each plane decides\nit differently: a run-target's is derived from its heartbeat, a BYO worker's\nis online/offline on the 90s window, a BYO cluster's is \"attached\", and a\nVisor machine's is the provider's word for its lifecycle state.",
			"fleetUnit.unit":       "Unit is the SOURCE's own id for this unit — a run-target id, a BYO worker id,\na Visor machine name — so a row links straight back to the face that owns it.\nIt is unique within a source, not across them: two planes may mint the same\nid, which is why (source, unit) together is the identity.",
		},
		Response: json.RawMessage(`{"units":[{"source":"byo","unit":"spark","kind":"worker","label":"spark","host":"spark","status":"online","spec":{"os":"linux","arch":"arm64","cpus":20,"gpus":1,"gpuModel":"NVIDIA GB10"},"metrics":{"gpuUtil":0.42,"at":"2026-07-27T09:00:00Z"},"sessions":0,"running":1,"queued":2}]}`),
	})
	zip.Describe("GET /v1/visor/fleet/jobs", zip.Doc{
		Description: "Returns the caller org's gpu-jobs render queue, each row tagged with\nthe GPU it targets (empty = the shared any-GPU lane) and the node claiming it,\noptionally narrowed to one GPU's queue and/or one status.\n\nA job whose worker died — STARTED with an elapsed lease and not yet reclaimed —\nreads \"stalled\", not \"running\". Fail-soft: an unavailable tasks engine yields an\nempty queue rather than an error.",
		Fields: map[string]string{
			"gpuJob.attempt":       "Attempt is which try this is, counting from 1. Above 1 means the job was\nretried after a failed or abandoned run.",
			"gpuJob.closeTime":     "CloseTime is when the job reached a terminal state, RFC 3339. Empty means it\nis still live — queued, running or stalled.",
			"gpuJob.failureCause":  "FailureCause is the engine's reason the job failed. Empty unless it did.",
			"gpuJob.gpu":           "GPU is the node this job is aimed AT — the lane \"gpu:<node>\" it was submitted\non. Empty means the shared any-GPU lane: it was not aimed anywhere and the\nfirst free worker takes it.",
			"gpuJob.id":            "ID is the job's id, and the id the cancel route takes. The dispatcher sets it\nequal to the render's prompt id, so it is the same value the studio knows the\njob by.",
			"gpuJob.label":         "Label is the cheap human name for the render — the output filename prefix\nlifted out of the submitted graph. Empty when the graph carried none. The\ngraph itself is never in this list; the tasks describe endpoint serves it.",
			"gpuJob.lastHeartbeat": "LastHeartbeat is the claiming worker's most recent beat on this job, RFC 3339\n— the evidence a long render is still alive rather than wedged.",
			"gpuJob.leaseExpiry":   "LeaseExpiry is when the worker's claim lapses, RFC 3339. Past it with the job\nstill STARTED, the claimant is presumed dead and Status reads \"stalled\".",
			"gpuJob.runId":         "RunID identifies this execution of the job. It equals ID for a job the\ndispatcher submitted, which is why a cancel that omits it still works.",
			"gpuJob.startTime":     "StartTime is when a worker began executing the job, RFC 3339. Empty while it\nis still queued.",
			"gpuJob.status":        "Status is the job's lifecycle state: queued, running, completed, failed or\ncanceled — plus \"stalled\", which is this surface's own reading of a job that\nis STARTED whose worker died: its lease has elapsed and no reaper has taken\nit back yet. Without it such a job reads \"running\" forever. An engine state\nthis surface does not recognize passes through lower-cased rather than being\ncoerced into one of these.",
			"gpuJob.type":          "Type is the work being done (\"studio.render\") — what the claiming worker has\nto be able to execute.",
			"gpuJob.worker":        "Worker is the node that actually CLAIMED the job, which is not always the one\nit was aimed at: a shared-lane job has no GPU but does have a Worker once\npicked up. Empty while the job is still waiting.",
			"jobFilter.gpu":        "GPU selects one node's lane: jobs TARGETED at it (gpu:<node>) or CLAIMED by\nit. The literal \"shared\" selects the any-GPU lane — no target, no claimant.\nMatched case-insensitively.",
			"jobFilter.status":     "Status selects one lifecycle state: queued, running, stalled, completed,\nfailed or canceled.",
			"jobList.jobs":         "Jobs is the queue, most-recent-first. Every LIVE job is present; terminal\nhistory is capped, so a busy org's running work is never crowded out.",
		},
		Response: json.RawMessage(`{"jobs":[{"id":"job-1","runId":"job-1","type":"studio.render","status":"running","gpu":"spark","worker":"spark","label":"hero","attempt":1}]}`),
	})
	zip.Describe("GET /v1/visor/fleet/samples", zip.Doc{
		Description: "Returns the caller org's utilization series, oldest first.\n\nA rejected narrower is a 400 carrying its own reason (the vocabulary is ours and\nsafe to echo); a warehouse failure is logged and answered 503 \"unavailable\",\nbecause a chart that silently reads \"no load\" when the truth is \"we cannot tell\"\nis worse than one that says so. An ABSENT warehouse is different again: it returns\nan empty series, which renders honestly as \"no samples yet\".",
		Fields: map[string]string{
			"sampleList.samples":   "Samples are the readings, OLDEST first — the order a chart plots.",
			"sampleQuery.range":    "Range is the lookback window (e.g. \"1h\", \"24h\", \"7d\"); empty takes the\nwarehouse default.",
			"sampleQuery.source":   "Source selects one plane: \"agent\", \"byo\" or \"visor\".",
			"sampleQuery.unit":     "Unit selects one compute unit's series by its source-local id.",
			"sampleView.at":        "At is when the reading was MEASURED, RFC 3339 in UTC — the x-axis a chart\nplots against. The series is returned oldest first, so it only increases.",
			"sampleView.costCents": "CostCents is what this unit resold for over the hour the reading falls in, in\nwhole US cents. 0 means UNPRICED, not free: the operator's own machines — a\nlinked run-target, a dialed-in BYO worker — are metered for utilization and\nnever resold, so only a priced source ever fills it.",
			"sampleView.cpus":      "CPUs is logical cores. The static capability rides every row on purpose: a\nchart can size load against cores without joining a registry whose row may\nsince have been rewritten or the unit deregistered.",
			"sampleView.gpuModel":  "GPUModel names the representative accelerator (\"GB10\"); GPUs carries how many.",
			"sampleView.gpuUtil":   "GPUUtil is aggregate accelerator utilization as a FRACTION of 1 — 0.42 is\n42% busy. Anything a reporter sends outside 0..1 is clamped into it on write.",
			"sampleView.gpus":      "GPUs is how many accelerators the reading covers.",
			"sampleView.host":      "Host is the hostname the unit reported at the time of the reading.",
			"sampleView.kind":      "Kind is what the measured unit is: laptop, cloud, gpu, cluster, machine or\nworker.",
			"sampleView.load1":     "Load1 is the 1-minute load average — runnable processes, not a percentage.",
			"sampleView.load15":    "Load15 is the 15-minute load average, the same units as Load1.",
			"sampleView.load5":     "Load5 is the 5-minute load average, the same units as Load1.",
			"sampleView.memFree":   "MemFree is host memory available, in BYTES, as reported rather than derived.",
			"sampleView.memUsed":   "MemUsed is host memory in use, in BYTES.",
			"sampleView.memory":    "Memory is total system RAM in BYTES at the time of the reading.",
			"sampleView.source":    "Source is the plane that reported the reading: \"agent\", \"byo\" or \"visor\" —\nthe same vocabulary the board's rows carry, and what ?source= narrows on.",
			"sampleView.unit":      "Unit is the source's own id for the measured unit. With Source it is the key\nthe chart groups by, and the key the board joins a unit's latest reading on.",
		},
		Response: json.RawMessage(`{"samples":[{"source":"byo","unit":"spark","kind":"worker","host":"spark","at":"2026-07-27T09:00:00Z","gpuUtil":0.42,"gpus":1,"gpuModel":"GB10"}]}`),
	})
	zip.Describe("GET /v1/visor/fleet/workers", zip.Doc{
		Description: "Returns the caller org's BYO machines — the ones that dialed in\nvia `hanzo link` — with everything each host reported about itself. The Machines\nand GPUs pages fold the same data into their normalized shapes; this is the\ncanonical raw list a fleet view (or the CLI's `status`) reads.",
		Fields: map[string]string{
			"byoGPU.arch":                "Arch is the card's native compile target (\"gfx1151\"), which is what a kernel\nhas to be built for. AMD reports one; NVIDIA cards leave it empty.",
			"byoGPU.memoryTotal":         "MemoryTotal is the card's VRAM in the units the host reported it in\n(\"122880 MiB\") — a display string, not a byte count. On a unified part it is\nthe shared CPU/GPU pool, so it is not memory reserved for the GPU.",
			"byoGPU.name":                "Name is the card's model exactly as its own tooling named it (\"NVIDIA\nGB10\"), never normalized — an operator matches what they see here against\nwhat nvidia-smi tells them on the box.",
			"byoGPU.unified":             "Unified reports that CPU and GPU share one memory pool (an APU or SoC), so\nMemoryTotal is not private to the GPU and the host competes for it.",
			"byoWorker.arch":             "Arch/CPUs/Memory are the connecting host's static CPU spec, mirrored from the\nregistration: Arch is runtime.GOARCH (amd64 | arm64), Memory is total RAM in\nBYTES — the same fields a code-linked run-target carries, so the /v1/visor/fleet\nboard renders a linked node's arch + cores + RAM like any other unit.",
			"byoWorker.capabilities":     "Capabilities is what this worker offers the org: \"studio.render\" when the\nnode can render, \"engine.serve\" when it serves a model endpoint. A node\nadvertises one only once it can honour it, so an absent list means a node\nthat has dialed in but is not ready to serve any of them yet.",
			"byoWorker.cpuModel":         "CPUModel is the processor as the host names it (\"Apple M3 Max\"), for display.",
			"byoWorker.cpus":             "CPUs is the host's logical core count.",
			"byoWorker.cuda":             "Cuda is the host's CUDA toolkit version. NVIDIA hosts report it.",
			"byoWorker.driver":           "Driver is the host's NVIDIA kernel driver version — distinct from Cuda, and\nthe one that bounds which CUDA versions can run on this box.",
			"byoWorker.engine":           "Engine is the hanzo-engine model server this node runs, when it runs one\n(`hanzo link --serve-engine`). Absent means the node takes jobs but serves\nno model endpoint.",
			"byoWorker.firstSeen":        "FirstSeen is when this node first dialed in, RFC 3339 — the start of its\npresence record, which `hanzo unlink` ends.",
			"byoWorker.gpus":             "GPUs are the accelerators the host found on itself. Empty is a real answer:\na CPU-only machine can dial in and take non-GPU work.",
			"byoWorker.hip":              "Hip is the host's HIP runtime version, the AMD counterpart to Cuda.",
			"byoWorker.hostname":         "Hostname is what the host calls itself. It equals ID for any hostname already\nin the [a-z0-9-] alphabet, and differs when sanitizing had to change it.",
			"byoWorker.id":               "ID is the node's id in the fleet — the sanitized hostname it registered\nunder, which is also the `unit` its samples and its gpu-jobs lane key on.\nThis is the id to use everywhere else on the compute surface.",
			"byoWorker.jobQueue":         "JobQueue is the tasks NAMESPACE this worker claims render jobs out of —\n\"gpu-jobs\" unless `hanzo link` was pointed at another. Within it, a job aimed\nat this node alone rides the task-queue value \"gpu:<id>\".",
			"byoWorker.lastHeartbeat":    "LastHeartbeat is the most recent beat this node sent, RFC 3339. It is what\nStatus is computed from, so a reader can check the judgement.",
			"byoWorker.location":         "Location is always \"on-prem\" — a machine that dialed in has no cloud region,\nand inventing one would put it somewhere it is not.",
			"byoWorker.memory":           "Memory is the host's total RAM in BYTES.",
			"byoWorker.os":               "Os is the host's operating system: linux, darwin or windows.",
			"byoWorker.provider":         "Provider is always \"byo\": this machine is the operator's, not one Hanzo\nprovisioned. It exists so a fold into the machines/GPUs pages says which\nrows are rented and which are the customer's own.",
			"byoWorker.rocm":             "Rocm is the host's ROCm version. AMD hosts report it; empty otherwise.",
			"byoWorker.status":           "Status is \"online\" when the last heartbeat landed within 90s, else\n\"offline\" — so it is a fact about heartbeat freshness, not about the box\nbeing powered on. A worker that has never beaten reads offline.",
			"byoWorker.version":          "Version is the `hanzo` CLI version running on the node. It is what to check\nwhen a worker is missing a field a newer registration reports.",
			"engineAdvertisement.apis":   "APIs are the wire formats the engine serves on that one port: \"openai\",\n\"anthropic\", or both.",
			"engineAdvertisement.models": "Models are the model ids the node's own GET /v1/models answered with — what\nthis GPU can actually be asked for.",
			"engineAdvertisement.status": "Status is \"ready\" when the node's engine answered, \"unreachable\" when it did\nnot. Advertised is not the same as serving, and this is the difference.",
			"engineAdvertisement.url":    "URL is the base address the node advertised its engine on — where a model\ncall to this GPU is sent. The node chose it, so reaching it is a question\nabout the node's network, not about this surface.",
			"workerList.workers":         "Workers is one row per connected BYO machine, each carrying the host's own\nreport (GPUs, driver versions, capabilities) rather than a normalized view.",
		},
		Response: json.RawMessage(`{"workers":[{"id":"spark","hostname":"spark","provider":"byo","location":"on-prem","status":"online","gpus":[{"name":"NVIDIA GB10","memoryTotal":"122880 MiB"}],"arch":"arm64","cpus":20}]}`),
	})
	zip.Describe("GET /v1/visor/gpus", zip.Doc{
		Description: "Returns one row per physical accelerator the caller's org has, derived\nfrom its real GPU machines (the size slug says how many cards a node holds) and\nfrom the accelerators BYO workers report through nvidia-smi.\n\nLive telemetry is absent on Visor rows because Visor's machine object carries\nnone — an honest omission the console renders as \"—\", never a fabricated 0.",
		Fields: map[string]string{
			"gpuList.gpus":     "GPUs is every accelerator the org has, from Visor GPU droplets and from BYO\nworkers alike.",
			"gpuView.id":       "ID is the card's address: its host machine's id, \"#\", and the card's ordinal\nwithin that machine (\"gpu-1#0\"). Stable for as long as the machine is, and\nthe only id a single accelerator has — providers do not name cards.",
			"gpuView.location": "Location is where the card physically sits, which for every source today is\nthe same value Region carries — the console renders it in its own column.",
			"gpuView.machine":  "Machine is the id of the machine holding this card, addressable as-is on\n/v1/visor/machines/:id.",
			"gpuView.memory":   "Memory is the card's VRAM as its own tooling reported it (\"122880 MiB\") — a\ndisplay string in the reporter's units, not a byte count. BYO cards carry it\n(nvidia-smi); Visor's machine object states no VRAM, so a rented card leaves\nit empty and the console renders \"—\" rather than a fabricated 0.",
			"gpuView.model":    "Model is the accelerator: the model token read out of the size slug for a\nVisor GPU droplet (\"H100\", \"MI300X\"), or the name nvidia-smi reported for a\nBYO card (\"NVIDIA GB10\").",
			"gpuView.name":     "Name is the HOST MACHINE's display name, not the card's — every card in a\ngpu-h100x8 node repeats it. Model is what says which accelerator this is.",
			"gpuView.provider": "Provider distinguishes a BYO accelerator (\"byo\") from a Visor-provisioned one\n(the host machine's real provider). It is what tells a card the org owns from\na card the org rents.",
			"gpuView.region":   "Region is the host machine's provider region slug; \"on-prem\" for a BYO card.",
			"gpuView.status":   "Status is the HOST MACHINE's lifecycle state, because nothing upstream\nreports a card's own health. A card reads running because its machine does.",
		},
		Response: json.RawMessage(`{"gpus":[{"id":"gpu-1#0","name":"gpu-1","model":"H100","region":"nyc2","status":"running","machine":"gpu-1","provider":"digitalocean"}]}`),
	})
	zip.Describe("GET /v1/visor/gpus/alerts", zip.Doc{
		Description: "Is an HONEST empty surface: Visor exposes no GPU alert inventory, so\nthis returns [] rather than fabricating alerts. It stays a real, tenant-gated\nroute so the console's alerts fetch resolves (200 [], not a 404) — an honest\n\"no alerts\", the same discipline the rest of the surface follows.",
		Fields: map[string]string{
			"gpuAlertList.alerts": "Alerts is always empty, and typed as a raw list because Visor exposes no\nalert inventory for this surface to shape: there is nothing to describe\nuntil there is something to return.",
		},
		Response: json.RawMessage(`{"alerts":[]}`),
	})
	zip.Describe("GET /v1/visor/k8s/clusters", zip.Doc{
		Description: "Lists the org's DOKS clusters (Visor, house account) folded with\nthe org's BYO clusters — ONE fleet cluster view under the unified k8s noun. A Visor\noutage is logged and skipped so a down optional provider never hides the BYO list.",
		Fields: map[string]string{
			"clusterList.clusters":      "Clusters is the merged fleet — kind \"managed\" for Visor-provisioned, \"byo\"\nfor an attached kubeconfig.",
			"clusterList.degraded":      "Degraded names any source that did not answer, so an empty Clusters means\n\"you have none\" only when this is absent. Omitted when everything answered,\nso a healthy response is unchanged. See degraded.go.",
			"clusterView.amdGpu":        "AmdGPU is the same count for `amd.com/gpu`: AMD accelerators across the BYO\ncluster's nodes, as of the attach.",
			"clusterView.createdAt":     "CreatedAt is when the cluster started existing: the earliest creation time\namong its pools for a managed cluster, and for a BYO one the RFC 3339 moment\nit was attached. Empty when the source states none.",
			"clusterView.doClusterId":   "DoClusterID carries the SAME id as DoksClusterID. Both names exist because\nthe console's Cluster type reads either one; neither is a second identifier.",
			"clusterView.doksClusterId": "DoksClusterID is the provider's own id for the cluster, and the value the\n/v1/visor/k8s/clusters/:id routes take. Empty for a BYO cluster: an attached\nkubeconfig was never provisioned, so there is no provider id to state.",
			"clusterView.kind":          "Kind says which of the two kinds of cluster this row is, and there are only\ntwo: \"managed\" — Visor provisioned it and Hanzo's account pays the provider —\nor \"byo\", an existing cluster the org attached by kubeconfig.",
			"clusterView.name":          "Name is the cluster's name: the provider's for a managed cluster, and for a\nBYO one the lower-cased fleet name it was attached under — which is also how\nthe detach route addresses it.",
			"clusterView.nodeCount":     "NodeCount is how many worker nodes the cluster has — the sum over its pools\nfor a managed cluster, and for a BYO one the node count read off the cluster\nwhen it was attached.",
			"clusterView.nodePools":     "NodePools is the authoritative node inventory — every pool, each with its own\nsize and count. It is empty in two cases that are not \"no pools\": a row from\nthe /v1/visor/k8s/clusters LIST, which is deliberately lightweight and whose\n:id detail carries them, and a BYO cluster, whose pools were never read.",
			"clusterView.nodeSize":      "NodeSize is a display convenience: the size slug of the FIRST pool. A cluster\nmixing sizes has more than one, and NodePools is where they all are.",
			"clusterView.nvidiaGpu":     "NvidiaGPU is how many NVIDIA accelerators the cluster's nodes advertise, the\nsum of `nvidia.com/gpu` allocatable across them. BYO only, and counted ONCE\nwhen the cluster was attached — it is an inventory, not live capacity.",
			"clusterView.region":        "Region is the provider region slug for a managed cluster. A BYO cluster has\nno region we can read, so it carries the free-form `provider` label the\nattach named it with (\"gke\", \"on-prem\") instead.",
			"clusterView.status":        "Status is the cluster's state: the provider's own word for a managed cluster\n(\"running\", \"provisioning\"), \"unknown\" when the provider stated none, and\nalways \"attached\" for a BYO cluster — that one says the kubeconfig is on\nfile, not that the cluster is reachable this second.",
			"nodePoolView.autoScale":    "AutoScale reports whether the provider's cluster autoscaler owns this pool's\nsize, moving Count between MinNodes and MaxNodes as workloads demand. False\nmeans Count changes only when someone scales the pool.",
			"nodePoolView.count":        "Count is how many nodes the pool has right now. Always present, so 0 means a\npool that is genuinely empty rather than a figure the provider withheld.",
			"nodePoolView.maxNodes":     "MaxNodes is the ceiling the autoscaler will not grow the pool past, and so\nthe bound on what this pool can cost. Read it only with AutoScale set.",
			"nodePoolView.minNodes":     "MinNodes is the floor the autoscaler will not shrink the pool below. Read it\nonly with AutoScale set — the provider ignores it otherwise.",
			"nodePoolView.name":         "Name is the pool's name as the provider knows it.",
			"nodePoolView.poolId":       "PoolID is the provider's id for the pool — the value the scale and delete\nroutes address it by. It falls back to the pool's name when the provider\nanswered without one, so it is always something the routes accept.",
			"nodePoolView.size":         "Size is the provider size slug every node in the pool runs at\n(\"s-4vcpu-8gb\", \"gpu-h100x8-640gb\"). One pool is one size — a mixed cluster\nis several pools.",
			"sourceFailure.reason":      "Reason is a terse, log-safe summary — never the upstream's response body.",
			"sourceFailure.source":      "Source is the dependency that failed, named as an operator names it.",
		},
		Response: json.RawMessage(`{"clusters":[{"doksClusterId":"cl-1","doClusterId":"cl-1","name":"prod","region":"nyc3","status":"running","nodePools":[],"nodeCount":0,"kind":"managed"}]}`),
	})
	zip.Describe("GET /v1/visor/k8s/clusters/:id", zip.Doc{
		Description: "Returns one cluster's detail: node pools + worker nodes. Visor scopes\nthe lookup to the org (a foreign or missing id resolves to not-found), so a tenant\ncan never read another tenant's cluster by guessing an id.",
		Fields: map[string]string{
			"clusterDetailView.nodes":   "Nodes is every worker node in the cluster, each in the same shape the\nmachines surface uses — a node IS a machine, addressable by its own id. This\nis the individual hardware behind the pool counts above.",
			"clusterView.amdGpu":        "AmdGPU is the same count for `amd.com/gpu`: AMD accelerators across the BYO\ncluster's nodes, as of the attach.",
			"clusterView.createdAt":     "CreatedAt is when the cluster started existing: the earliest creation time\namong its pools for a managed cluster, and for a BYO one the RFC 3339 moment\nit was attached. Empty when the source states none.",
			"clusterView.doClusterId":   "DoClusterID carries the SAME id as DoksClusterID. Both names exist because\nthe console's Cluster type reads either one; neither is a second identifier.",
			"clusterView.doksClusterId": "DoksClusterID is the provider's own id for the cluster, and the value the\n/v1/visor/k8s/clusters/:id routes take. Empty for a BYO cluster: an attached\nkubeconfig was never provisioned, so there is no provider id to state.",
			"clusterView.kind":          "Kind says which of the two kinds of cluster this row is, and there are only\ntwo: \"managed\" — Visor provisioned it and Hanzo's account pays the provider —\nor \"byo\", an existing cluster the org attached by kubeconfig.",
			"clusterView.name":          "Name is the cluster's name: the provider's for a managed cluster, and for a\nBYO one the lower-cased fleet name it was attached under — which is also how\nthe detach route addresses it.",
			"clusterView.nodeCount":     "NodeCount is how many worker nodes the cluster has — the sum over its pools\nfor a managed cluster, and for a BYO one the node count read off the cluster\nwhen it was attached.",
			"clusterView.nodePools":     "NodePools is the authoritative node inventory — every pool, each with its own\nsize and count. It is empty in two cases that are not \"no pools\": a row from\nthe /v1/visor/k8s/clusters LIST, which is deliberately lightweight and whose\n:id detail carries them, and a BYO cluster, whose pools were never read.",
			"clusterView.nodeSize":      "NodeSize is a display convenience: the size slug of the FIRST pool. A cluster\nmixing sizes has more than one, and NodePools is where they all are.",
			"clusterView.nvidiaGpu":     "NvidiaGPU is how many NVIDIA accelerators the cluster's nodes advertise, the\nsum of `nvidia.com/gpu` allocatable across them. BYO only, and counted ONCE\nwhen the cluster was attached — it is an inventory, not live capacity.",
			"clusterView.region":        "Region is the provider region slug for a managed cluster. A BYO cluster has\nno region we can read, so it carries the free-form `provider` label the\nattach named it with (\"gke\", \"on-prem\") instead.",
			"clusterView.status":        "Status is the cluster's state: the provider's own word for a managed cluster\n(\"running\", \"provisioning\"), \"unknown\" when the provider stated none, and\nalways \"attached\" for a BYO cluster — that one says the kubeconfig is on\nfile, not that the cluster is reachable this second.",
			"k8sClusterRef.id":          "ID is the provider's DOKS cluster id. Visor scopes the lookup to the caller's\norg, so another tenant's id resolves to not-found rather than their cluster.",
			"machineView.createdTime":   "CreatedTime is when the machine came into being: the provider's own creation\ntimestamp for a Visor machine, passed through in whatever form it states it,\nand for a BYO machine the RFC 3339 moment it first dialed in.",
			"machineView.gpu":           "GPU names the accelerators this machine holds (\"H100\", or \"2× NVIDIA GB10\"\nfor a BYO machine reporting a matched pair). Empty means the machine is not\na GPU machine — the size slug does not parse as one, or nvidia-smi found\nnothing.",
			"machineView.id":            "ID addresses this machine on the /v1/visor/machines/:id routes: the\norg-scoped NAME Visor keys a machine by, falling back to the provider id for\na machine that has no name. A BYO machine's is the id it dialed in under.",
			"machineView.image":         "Image is the OS image the machine booted from, as the provider names it.",
			"machineView.mem":           "Mem is system RAM rendered for a human (\"8 GB\"), not a number to compute\nwith. Empty when the provider's figure is ambiguous, or when the only figure\navailable is a GPU slug's gb — that is VRAM, and reporting it as system RAM\nwould be a fabrication. A BYO machine's RAM is on /v1/visor/fleet/workers.",
			"machineView.name":          "Name is the label to show a human — Visor's displayName, or the machine name\nwhen it carries none. A BYO machine's is its hostname. It is not an address:\nID is what the routes take.",
			"machineView.os":            "Os is the operating system on the machine — Visor's record for a provisioned\none, the host's own report (linux, darwin, windows) for a BYO one.",
			"machineView.privateIp":     "PrivateIp is the address on the provider's own network, reachable from the\norg's other machines in the same region. Empty on the same terms as PublicIp.",
			"machineView.provider":      "Provider is the cloud that runs the machine (\"digitalocean\"), or \"byo\" for\none the operator dialed in with `hanzo link`.",
			"machineView.publicIp":      "PublicIp is the internet-facing address the provider assigned. Empty while a\nmachine is still provisioning, and empty for a BYO machine — it dials out\nfrom behind NAT, so no address is ever learned for it.",
			"machineView.region":        "Region is the provider region slug (\"sfo3\"), or the zone when the provider\nreports only that. \"on-prem\" for a BYO machine, which has no cloud region.",
			"machineView.status":        "Status is the lifecycle state in the PROVIDER's own words (\"active\",\n\"running\", \"off\"), passed through rather than mapped onto a vocabulary of\nours. A BYO machine's is \"online\" or \"offline\", decided by whether its last\nheartbeat is within 90s.",
			"machineView.type":          "Type is the provider SIZE SLUG the machine runs at (\"s-2vcpu-4gb\",\n\"gpu-h100x8-640gb\") — the value a launch asks for, and what Vcpu/Mem/GPU are\nread out of when the provider states them no other way. \"byo-gpu\" for a\ndialed-in machine, which was never bought from a size catalog.",
			"machineView.vcpu":          "Vcpu is logical cores — the provider's own cpuSize when that is a clean\ninteger, else the count read out of the size slug (4 from \"s-4vcpu-8gb\").\nABSENT, never 0, when neither says. A BYO machine leaves it absent here; its\nreal core count is on GET /v1/visor/fleet/workers.",
			"nodePoolView.autoScale":    "AutoScale reports whether the provider's cluster autoscaler owns this pool's\nsize, moving Count between MinNodes and MaxNodes as workloads demand. False\nmeans Count changes only when someone scales the pool.",
			"nodePoolView.count":        "Count is how many nodes the pool has right now. Always present, so 0 means a\npool that is genuinely empty rather than a figure the provider withheld.",
			"nodePoolView.maxNodes":     "MaxNodes is the ceiling the autoscaler will not grow the pool past, and so\nthe bound on what this pool can cost. Read it only with AutoScale set.",
			"nodePoolView.minNodes":     "MinNodes is the floor the autoscaler will not shrink the pool below. Read it\nonly with AutoScale set — the provider ignores it otherwise.",
			"nodePoolView.name":         "Name is the pool's name as the provider knows it.",
			"nodePoolView.poolId":       "PoolID is the provider's id for the pool — the value the scale and delete\nroutes address it by. It falls back to the pool's name when the provider\nanswered without one, so it is always something the routes accept.",
			"nodePoolView.size":         "Size is the provider size slug every node in the pool runs at\n(\"s-4vcpu-8gb\", \"gpu-h100x8-640gb\"). One pool is one size — a mixed cluster\nis several pools.",
		},
		Response: json.RawMessage(`{"doksClusterId":"cl-1","name":"prod","region":"nyc3","status":"running","nodePools":[{"poolId":"p-1","name":"gpu","size":"gpu-h100x8-640gb","count":1}],"nodeSize":"gpu-h100x8-640gb","nodeCount":1,"kind":"managed","nodes":[{"id":"node-1","name":"node-1","status":"active"}]}`),
	})
	zip.Describe("GET /v1/visor/k8s/nodes", zip.Doc{
		Description: "Returns every DOKS worker node in the org's clusters as a machine —\nthe SAME set the fleet folds in (managedMachines), exposed directly under the k8s\nnoun. House account (hanzo-org cluster tag) + BYOC, deduped by Visor.",
		Fields: map[string]string{
			"machineView.createdTime": "CreatedTime is when the machine came into being: the provider's own creation\ntimestamp for a Visor machine, passed through in whatever form it states it,\nand for a BYO machine the RFC 3339 moment it first dialed in.",
			"machineView.gpu":         "GPU names the accelerators this machine holds (\"H100\", or \"2× NVIDIA GB10\"\nfor a BYO machine reporting a matched pair). Empty means the machine is not\na GPU machine — the size slug does not parse as one, or nvidia-smi found\nnothing.",
			"machineView.id":          "ID addresses this machine on the /v1/visor/machines/:id routes: the\norg-scoped NAME Visor keys a machine by, falling back to the provider id for\na machine that has no name. A BYO machine's is the id it dialed in under.",
			"machineView.image":       "Image is the OS image the machine booted from, as the provider names it.",
			"machineView.mem":         "Mem is system RAM rendered for a human (\"8 GB\"), not a number to compute\nwith. Empty when the provider's figure is ambiguous, or when the only figure\navailable is a GPU slug's gb — that is VRAM, and reporting it as system RAM\nwould be a fabrication. A BYO machine's RAM is on /v1/visor/fleet/workers.",
			"machineView.name":        "Name is the label to show a human — Visor's displayName, or the machine name\nwhen it carries none. A BYO machine's is its hostname. It is not an address:\nID is what the routes take.",
			"machineView.os":          "Os is the operating system on the machine — Visor's record for a provisioned\none, the host's own report (linux, darwin, windows) for a BYO one.",
			"machineView.privateIp":   "PrivateIp is the address on the provider's own network, reachable from the\norg's other machines in the same region. Empty on the same terms as PublicIp.",
			"machineView.provider":    "Provider is the cloud that runs the machine (\"digitalocean\"), or \"byo\" for\none the operator dialed in with `hanzo link`.",
			"machineView.publicIp":    "PublicIp is the internet-facing address the provider assigned. Empty while a\nmachine is still provisioning, and empty for a BYO machine — it dials out\nfrom behind NAT, so no address is ever learned for it.",
			"machineView.region":      "Region is the provider region slug (\"sfo3\"), or the zone when the provider\nreports only that. \"on-prem\" for a BYO machine, which has no cloud region.",
			"machineView.status":      "Status is the lifecycle state in the PROVIDER's own words (\"active\",\n\"running\", \"off\"), passed through rather than mapped onto a vocabulary of\nours. A BYO machine's is \"online\" or \"offline\", decided by whether its last\nheartbeat is within 90s.",
			"machineView.type":        "Type is the provider SIZE SLUG the machine runs at (\"s-2vcpu-4gb\",\n\"gpu-h100x8-640gb\") — the value a launch asks for, and what Vcpu/Mem/GPU are\nread out of when the provider states them no other way. \"byo-gpu\" for a\ndialed-in machine, which was never bought from a size catalog.",
			"machineView.vcpu":        "Vcpu is logical cores — the provider's own cpuSize when that is a clean\ninteger, else the count read out of the size slug (4 from \"s-4vcpu-8gb\").\nABSENT, never 0, when neither says. A BYO machine leaves it absent here; its\nreal core count is on GET /v1/visor/fleet/workers.",
			"nodeList.nodes":          "Nodes is one row per worker node, in the SAME machineView shape the machines\nsurface emits — a node IS a machine.",
		},
		Response: json.RawMessage(`{"nodes":[{"id":"node-1","name":"node-1","region":"nyc3","type":"s-4vcpu-8gb","status":"active","vcpu":4}]}`),
	})
	zip.Describe("GET /v1/visor/machines", zip.Doc{
		Description: "Returns every machine the caller's org has — Visor's registry, the\nlive DigitalOcean droplets and the DOKS worker nodes (deduped into one union),\nplus the BYO machines that dialed in via `hanzo link` (provider \"byo\").\n\nA source Visor cannot answer for is logged and skipped, never an error: one\nwedged upstream must not hide the machines the other sources can see.",
		Fields: map[string]string{
			"machineList.machines":    "Machines is every machine the org has: Visor-provisioned and BYO together.",
			"machineView.createdTime": "CreatedTime is when the machine came into being: the provider's own creation\ntimestamp for a Visor machine, passed through in whatever form it states it,\nand for a BYO machine the RFC 3339 moment it first dialed in.",
			"machineView.gpu":         "GPU names the accelerators this machine holds (\"H100\", or \"2× NVIDIA GB10\"\nfor a BYO machine reporting a matched pair). Empty means the machine is not\na GPU machine — the size slug does not parse as one, or nvidia-smi found\nnothing.",
			"machineView.id":          "ID addresses this machine on the /v1/visor/machines/:id routes: the\norg-scoped NAME Visor keys a machine by, falling back to the provider id for\na machine that has no name. A BYO machine's is the id it dialed in under.",
			"machineView.image":       "Image is the OS image the machine booted from, as the provider names it.",
			"machineView.mem":         "Mem is system RAM rendered for a human (\"8 GB\"), not a number to compute\nwith. Empty when the provider's figure is ambiguous, or when the only figure\navailable is a GPU slug's gb — that is VRAM, and reporting it as system RAM\nwould be a fabrication. A BYO machine's RAM is on /v1/visor/fleet/workers.",
			"machineView.name":        "Name is the label to show a human — Visor's displayName, or the machine name\nwhen it carries none. A BYO machine's is its hostname. It is not an address:\nID is what the routes take.",
			"machineView.os":          "Os is the operating system on the machine — Visor's record for a provisioned\none, the host's own report (linux, darwin, windows) for a BYO one.",
			"machineView.privateIp":   "PrivateIp is the address on the provider's own network, reachable from the\norg's other machines in the same region. Empty on the same terms as PublicIp.",
			"machineView.provider":    "Provider is the cloud that runs the machine (\"digitalocean\"), or \"byo\" for\none the operator dialed in with `hanzo link`.",
			"machineView.publicIp":    "PublicIp is the internet-facing address the provider assigned. Empty while a\nmachine is still provisioning, and empty for a BYO machine — it dials out\nfrom behind NAT, so no address is ever learned for it.",
			"machineView.region":      "Region is the provider region slug (\"sfo3\"), or the zone when the provider\nreports only that. \"on-prem\" for a BYO machine, which has no cloud region.",
			"machineView.status":      "Status is the lifecycle state in the PROVIDER's own words (\"active\",\n\"running\", \"off\"), passed through rather than mapped onto a vocabulary of\nours. A BYO machine's is \"online\" or \"offline\", decided by whether its last\nheartbeat is within 90s.",
			"machineView.type":        "Type is the provider SIZE SLUG the machine runs at (\"s-2vcpu-4gb\",\n\"gpu-h100x8-640gb\") — the value a launch asks for, and what Vcpu/Mem/GPU are\nread out of when the provider states them no other way. \"byo-gpu\" for a\ndialed-in machine, which was never bought from a size catalog.",
			"machineView.vcpu":        "Vcpu is logical cores — the provider's own cpuSize when that is a clean\ninteger, else the count read out of the size slug (4 from \"s-4vcpu-8gb\").\nABSENT, never 0, when neither says. A BYO machine leaves it absent here; its\nreal core count is on GET /v1/visor/fleet/workers.",
		},
		Response: json.RawMessage(`{"machines":[{"id":"web-1","name":"Web 1","region":"sfo3","type":"s-2vcpu-4gb","status":"running","provider":"digitalocean","publicIp":"1.2.3.4","vcpu":2,"mem":"4 GB"}]}`),
	})
	zip.Describe("GET /v1/visor/machines/:id", zip.Doc{
		Description: "Returns one of the caller org's machines by its org-scoped name.\nVisor keys the lookup by owner/name, so an id belonging to another tenant\nresolves to not-found rather than another org's machine.",
		Fields: map[string]string{
			"machineRef.id":           "ID is the machine's org-scoped NAME — the stable key Visor addresses a\nmachine by (owner/name), not the ephemeral provider id.",
			"machineView.createdTime": "CreatedTime is when the machine came into being: the provider's own creation\ntimestamp for a Visor machine, passed through in whatever form it states it,\nand for a BYO machine the RFC 3339 moment it first dialed in.",
			"machineView.gpu":         "GPU names the accelerators this machine holds (\"H100\", or \"2× NVIDIA GB10\"\nfor a BYO machine reporting a matched pair). Empty means the machine is not\na GPU machine — the size slug does not parse as one, or nvidia-smi found\nnothing.",
			"machineView.id":          "ID addresses this machine on the /v1/visor/machines/:id routes: the\norg-scoped NAME Visor keys a machine by, falling back to the provider id for\na machine that has no name. A BYO machine's is the id it dialed in under.",
			"machineView.image":       "Image is the OS image the machine booted from, as the provider names it.",
			"machineView.mem":         "Mem is system RAM rendered for a human (\"8 GB\"), not a number to compute\nwith. Empty when the provider's figure is ambiguous, or when the only figure\navailable is a GPU slug's gb — that is VRAM, and reporting it as system RAM\nwould be a fabrication. A BYO machine's RAM is on /v1/visor/fleet/workers.",
			"machineView.name":        "Name is the label to show a human — Visor's displayName, or the machine name\nwhen it carries none. A BYO machine's is its hostname. It is not an address:\nID is what the routes take.",
			"machineView.os":          "Os is the operating system on the machine — Visor's record for a provisioned\none, the host's own report (linux, darwin, windows) for a BYO one.",
			"machineView.privateIp":   "PrivateIp is the address on the provider's own network, reachable from the\norg's other machines in the same region. Empty on the same terms as PublicIp.",
			"machineView.provider":    "Provider is the cloud that runs the machine (\"digitalocean\"), or \"byo\" for\none the operator dialed in with `hanzo link`.",
			"machineView.publicIp":    "PublicIp is the internet-facing address the provider assigned. Empty while a\nmachine is still provisioning, and empty for a BYO machine — it dials out\nfrom behind NAT, so no address is ever learned for it.",
			"machineView.region":      "Region is the provider region slug (\"sfo3\"), or the zone when the provider\nreports only that. \"on-prem\" for a BYO machine, which has no cloud region.",
			"machineView.status":      "Status is the lifecycle state in the PROVIDER's own words (\"active\",\n\"running\", \"off\"), passed through rather than mapped onto a vocabulary of\nours. A BYO machine's is \"online\" or \"offline\", decided by whether its last\nheartbeat is within 90s.",
			"machineView.type":        "Type is the provider SIZE SLUG the machine runs at (\"s-2vcpu-4gb\",\n\"gpu-h100x8-640gb\") — the value a launch asks for, and what Vcpu/Mem/GPU are\nread out of when the provider states them no other way. \"byo-gpu\" for a\ndialed-in machine, which was never bought from a size catalog.",
			"machineView.vcpu":        "Vcpu is logical cores — the provider's own cpuSize when that is a clean\ninteger, else the count read out of the size slug (4 from \"s-4vcpu-8gb\").\nABSENT, never 0, when neither says. A BYO machine leaves it absent here; its\nreal core count is on GET /v1/visor/fleet/workers.",
		},
		Response: json.RawMessage(`{"id":"web-1","name":"Web 1","region":"sfo3","type":"s-2vcpu-4gb","status":"running","publicIp":"1.2.3.4","vcpu":2}`),
	})
	zip.Describe("GET /v1/visor/machines/:id/agent", zip.Doc{
		Description: "Returns the agent binding of one of the caller org's\nmachines, or 404 when the machine runs no bot runtime.",
		Fields: map[string]string{
			"agentBinding.agentName":   "AgentName is the cloud Agent (/v1/agents) this machine runs — the agent a\nmessage to the bot is actually run against. It is the one field that decides\nwhat the bot DOES.",
			"agentBinding.botVersion":  "BotVersion pins the @hanzo/bot runtime version the machine runs. Empty means\nthe machine took the default in force when it was bound.",
			"agentBinding.createdTime": "CreatedTime is when the binding was first made.",
			"agentBinding.machineId":   "MachineId is the bound machine as vm addresses it, owner-qualified\n(\"<org>/<machine>\"). The unqualified half is what this surface's :id routes\ntake.",
			"agentBinding.message":     "Message is vm's human-readable detail on Status (\"machine provisioning;\n@hanzo/bot runtime not yet confirmed\") — the reason behind the state, not a\nsecond state.",
			"agentBinding.name":        "Name is the binding's own key, which is the machine's id: a machine hosts at\nmost one agent, so the binding is named for it. This is the key a bots list\njoins bindings onto machines by.",
			"agentBinding.org":         "Org is the Hanzo tenant the binding belongs to.",
			"agentBinding.owner":       "Owner is the tenant vm filed the binding under, resolved from the ?owner it\nwas called with — which is the caller's validated org and never a body field.",
			"agentBinding.provider":    "Provider is the cloud the bound machine runs on, carried here so a bindings\nlist says where each bot lives without a second read per machine.",
			"agentBinding.publicIp":    "PublicIp is the bound machine's public address as vm recorded it on the\nbinding. Empty while the machine has none yet.",
			"agentBinding.status":      "Status is the binding's lifecycle in VM's OWN words — \"Pending\" while the\nmachine provisions and the runtime is unconfirmed, \"running\" once vm has\nconfirmed it. The vocabulary is vm's and passes through unmapped, which is\nwhy its capitalization does not match the machine states beside it, and it is\nvm's reconciled reading rather than anything asserted here.",
			"agentBinding.updatedTime": "UpdatedTime is when vm last reconciled it — the age of Status.",
			"machineRef.id":            "ID is the machine's org-scoped NAME — the stable key Visor addresses a\nmachine by (owner/name), not the ephemeral provider id.",
		},
		Response: json.RawMessage(`{"machineId":"drop-a","agentName":"bot-a","status":"running","botVersion":"1.4.0"}`),
	})
	zip.Describe("GET /v1/visor/machines/agents", zip.Doc{
		Description: "Returns every agent↔machine binding in the caller's org — which\nmachines are running which cloud Agent, with vm's own reconciled status.",
		Fields: map[string]string{
			"agentBinding.agentName":    "AgentName is the cloud Agent (/v1/agents) this machine runs — the agent a\nmessage to the bot is actually run against. It is the one field that decides\nwhat the bot DOES.",
			"agentBinding.botVersion":   "BotVersion pins the @hanzo/bot runtime version the machine runs. Empty means\nthe machine took the default in force when it was bound.",
			"agentBinding.createdTime":  "CreatedTime is when the binding was first made.",
			"agentBinding.machineId":    "MachineId is the bound machine as vm addresses it, owner-qualified\n(\"<org>/<machine>\"). The unqualified half is what this surface's :id routes\ntake.",
			"agentBinding.message":      "Message is vm's human-readable detail on Status (\"machine provisioning;\n@hanzo/bot runtime not yet confirmed\") — the reason behind the state, not a\nsecond state.",
			"agentBinding.name":         "Name is the binding's own key, which is the machine's id: a machine hosts at\nmost one agent, so the binding is named for it. This is the key a bots list\njoins bindings onto machines by.",
			"agentBinding.org":          "Org is the Hanzo tenant the binding belongs to.",
			"agentBinding.owner":        "Owner is the tenant vm filed the binding under, resolved from the ?owner it\nwas called with — which is the caller's validated org and never a body field.",
			"agentBinding.provider":     "Provider is the cloud the bound machine runs on, carried here so a bindings\nlist says where each bot lives without a second read per machine.",
			"agentBinding.publicIp":     "PublicIp is the bound machine's public address as vm recorded it on the\nbinding. Empty while the machine has none yet.",
			"agentBinding.status":       "Status is the binding's lifecycle in VM's OWN words — \"Pending\" while the\nmachine provisions and the runtime is unconfirmed, \"running\" once vm has\nconfirmed it. The vocabulary is vm's and passes through unmapped, which is\nwhy its capitalization does not match the machine states beside it, and it is\nvm's reconciled reading rather than anything asserted here.",
			"agentBinding.updatedTime":  "UpdatedTime is when vm last reconciled it — the age of Status.",
			"bindingList.agentBindings": "AgentBindings is one row per bound machine, emitted verbatim as vm reports\nit.",
		},
		Response: json.RawMessage(`{"agentBindings":[{"machineId":"drop-a","agentName":"bot-a","status":"running","publicIp":"1.2.3.4"}]}`),
	})
	zip.Describe("POST /v1/visor/clusters", zip.Doc{
		Description: "Attaches a BYO cluster to the caller's org — the kubeconfig is\nvalidated, KMS-sealed and added to the fleet — and answers 201 with the cluster\nas it now appears on GET /v1/visor/clusters. Billed the nominal management fee: the\ncustomer brings the compute, Hanzo meters the management plane.",
		Fields: map[string]string{
			"clusterAttach.default":     "Default marks this the org's default cluster for scheduling.",
			"clusterAttach.kubeconfig":  "Kubeconfig is the cluster's kubeconfig, verbatim. Required — a body without\none is not an attach.",
			"clusterAttach.name":        "Name is the fleet-local name for the cluster; lower-cased, and the key the\ndetach route addresses it by. Required.",
			"clusterAttach.provider":    "Provider is a free-form label for where the cluster runs (\"gke\", \"on-prem\");\nit is display only, not a routing key.",
			"clusterView.amdGpu":        "AmdGPU is the same count for `amd.com/gpu`: AMD accelerators across the BYO\ncluster's nodes, as of the attach.",
			"clusterView.createdAt":     "CreatedAt is when the cluster started existing: the earliest creation time\namong its pools for a managed cluster, and for a BYO one the RFC 3339 moment\nit was attached. Empty when the source states none.",
			"clusterView.doClusterId":   "DoClusterID carries the SAME id as DoksClusterID. Both names exist because\nthe console's Cluster type reads either one; neither is a second identifier.",
			"clusterView.doksClusterId": "DoksClusterID is the provider's own id for the cluster, and the value the\n/v1/visor/k8s/clusters/:id routes take. Empty for a BYO cluster: an attached\nkubeconfig was never provisioned, so there is no provider id to state.",
			"clusterView.kind":          "Kind says which of the two kinds of cluster this row is, and there are only\ntwo: \"managed\" — Visor provisioned it and Hanzo's account pays the provider —\nor \"byo\", an existing cluster the org attached by kubeconfig.",
			"clusterView.name":          "Name is the cluster's name: the provider's for a managed cluster, and for a\nBYO one the lower-cased fleet name it was attached under — which is also how\nthe detach route addresses it.",
			"clusterView.nodeCount":     "NodeCount is how many worker nodes the cluster has — the sum over its pools\nfor a managed cluster, and for a BYO one the node count read off the cluster\nwhen it was attached.",
			"clusterView.nodePools":     "NodePools is the authoritative node inventory — every pool, each with its own\nsize and count. It is empty in two cases that are not \"no pools\": a row from\nthe /v1/visor/k8s/clusters LIST, which is deliberately lightweight and whose\n:id detail carries them, and a BYO cluster, whose pools were never read.",
			"clusterView.nodeSize":      "NodeSize is a display convenience: the size slug of the FIRST pool. A cluster\nmixing sizes has more than one, and NodePools is where they all are.",
			"clusterView.nvidiaGpu":     "NvidiaGPU is how many NVIDIA accelerators the cluster's nodes advertise, the\nsum of `nvidia.com/gpu` allocatable across them. BYO only, and counted ONCE\nwhen the cluster was attached — it is an inventory, not live capacity.",
			"clusterView.region":        "Region is the provider region slug for a managed cluster. A BYO cluster has\nno region we can read, so it carries the free-form `provider` label the\nattach named it with (\"gke\", \"on-prem\") instead.",
			"clusterView.status":        "Status is the cluster's state: the provider's own word for a managed cluster\n(\"running\", \"provisioning\"), \"unknown\" when the provider stated none, and\nalways \"attached\" for a BYO cluster — that one says the kubeconfig is on\nfile, not that the cluster is reachable this second.",
			"nodePoolView.autoScale":    "AutoScale reports whether the provider's cluster autoscaler owns this pool's\nsize, moving Count between MinNodes and MaxNodes as workloads demand. False\nmeans Count changes only when someone scales the pool.",
			"nodePoolView.count":        "Count is how many nodes the pool has right now. Always present, so 0 means a\npool that is genuinely empty rather than a figure the provider withheld.",
			"nodePoolView.maxNodes":     "MaxNodes is the ceiling the autoscaler will not grow the pool past, and so\nthe bound on what this pool can cost. Read it only with AutoScale set.",
			"nodePoolView.minNodes":     "MinNodes is the floor the autoscaler will not shrink the pool below. Read it\nonly with AutoScale set — the provider ignores it otherwise.",
			"nodePoolView.name":         "Name is the pool's name as the provider knows it.",
			"nodePoolView.poolId":       "PoolID is the provider's id for the pool — the value the scale and delete\nroutes address it by. It falls back to the pool's name when the provider\nanswered without one, so it is always something the routes accept.",
			"nodePoolView.size":         "Size is the provider size slug every node in the pool runs at\n(\"s-4vcpu-8gb\", \"gpu-h100x8-640gb\"). One pool is one size — a mixed cluster\nis several pools.",
		},
		Example:  json.RawMessage(`{"name":"lab","kubeconfig":"apiVersion: v1\nkind: Config\n...","provider":"on-prem","default":false}`),
		Response: json.RawMessage(`{"name":"lab","region":"on-prem","status":"attached","nodePools":[],"nodeCount":3,"kind":"byo","nvidiaGpu":2}`),
	})
	zip.Describe("POST /v1/visor/clusters/:clusterId/pools", zip.Doc{
		Description: "Adds a node pool to one of the caller org's clusters and answers 201\nwith the created pool. Only the CreateNodePoolSpec fields are forwarded;\nowner/provider/clusterId ride in the query exactly as Visor expects them.",
		Fields: map[string]string{
			"nodePoolView.autoScale": "AutoScale reports whether the provider's cluster autoscaler owns this pool's\nsize, moving Count between MinNodes and MaxNodes as workloads demand. False\nmeans Count changes only when someone scales the pool.",
			"nodePoolView.count":     "Count is how many nodes the pool has right now. Always present, so 0 means a\npool that is genuinely empty rather than a figure the provider withheld.",
			"nodePoolView.maxNodes":  "MaxNodes is the ceiling the autoscaler will not grow the pool past, and so\nthe bound on what this pool can cost. Read it only with AutoScale set.",
			"nodePoolView.minNodes":  "MinNodes is the floor the autoscaler will not shrink the pool below. Read it\nonly with AutoScale set — the provider ignores it otherwise.",
			"nodePoolView.name":      "Name is the pool's name as the provider knows it.",
			"nodePoolView.poolId":    "PoolID is the provider's id for the pool — the value the scale and delete\nroutes address it by. It falls back to the pool's name when the provider\nanswered without one, so it is always something the routes accept.",
			"nodePoolView.size":      "Size is the provider size slug every node in the pool runs at\n(\"s-4vcpu-8gb\", \"gpu-h100x8-640gb\"). One pool is one size — a mixed cluster\nis several pools.",
			"poolCreate.autoScale":   "AutoScale turns the provider's cluster autoscaler on for this pool.",
			"poolCreate.clusterId":   "ClusterID is the cluster to add the pool to, from the URL path.",
			"poolCreate.count":       "Count is how many nodes the pool starts with.",
			"poolCreate.maxNodes":    "MaxNodes is the ceiling the autoscaler may not grow the pool past, and so the\nbound on what this pool can spend. Ignored unless AutoScale is set.",
			"poolCreate.minNodes":    "MinNodes is the floor the autoscaler may not shrink the pool below. Ignored\nunless AutoScale is set.",
			"poolCreate.name":        "Name is the pool's name.",
			"poolCreate.provider":    "Provider is the cloud the cluster lives on (e.g. \"digitalocean\"). Required —\nVisor routes the create by it. Accepted from the body or ?provider=.",
			"poolCreate.size":        "Size is the provider size slug for each node (e.g. \"s-4vcpu-8gb\").",
		},
		Example:  json.RawMessage(`{"provider":"digitalocean","name":"gpu","size":"gpu-h100x8-640gb","count":2,"autoScale":false}`),
		Response: json.RawMessage(`{"poolId":"p-1","name":"gpu","size":"gpu-h100x8-640gb","count":2}`),
	})
	zip.Describe("POST /v1/visor/clusters/:clusterId/pools/:poolId/scale", zip.Doc{
		Description: "Resizes a node pool to an absolute node count and returns the pool as\nVisor reports it after the change.",
		Fields: map[string]string{
			"nodePoolView.autoScale": "AutoScale reports whether the provider's cluster autoscaler owns this pool's\nsize, moving Count between MinNodes and MaxNodes as workloads demand. False\nmeans Count changes only when someone scales the pool.",
			"nodePoolView.count":     "Count is how many nodes the pool has right now. Always present, so 0 means a\npool that is genuinely empty rather than a figure the provider withheld.",
			"nodePoolView.maxNodes":  "MaxNodes is the ceiling the autoscaler will not grow the pool past, and so\nthe bound on what this pool can cost. Read it only with AutoScale set.",
			"nodePoolView.minNodes":  "MinNodes is the floor the autoscaler will not shrink the pool below. Read it\nonly with AutoScale set — the provider ignores it otherwise.",
			"nodePoolView.name":      "Name is the pool's name as the provider knows it.",
			"nodePoolView.poolId":    "PoolID is the provider's id for the pool — the value the scale and delete\nroutes address it by. It falls back to the pool's name when the provider\nanswered without one, so it is always something the routes accept.",
			"nodePoolView.size":      "Size is the provider size slug every node in the pool runs at\n(\"s-4vcpu-8gb\", \"gpu-h100x8-640gb\"). One pool is one size — a mixed cluster\nis several pools.",
			"poolScale.clusterId":    "ClusterID is the cluster holding the pool, from the URL path.",
			"poolScale.count":        "Count is the node count to scale TO — an absolute target, not a delta, and\nnever negative.",
			"poolScale.poolId":       "PoolID is the pool to resize, from the URL path — the `poolId` a cluster read\nreports for it. Required.",
			"poolScale.provider":     "Provider is the cloud the cluster lives on. Required; body or ?provider=.",
		},
		Example:  json.RawMessage(`{"provider":"digitalocean","count":4}`),
		Response: json.RawMessage(`{"poolId":"p-1","name":"gpu","size":"gpu-h100x8-640gb","count":4}`),
	})
	zip.Describe("POST /v1/visor/fleet/jobs/:id/cancel", zip.Doc{
		Description: "Cancels a queued or running render in the caller's org. The engine\ncancel is org-scoped, so a tenant can only ever cancel its OWN job: a job in\nanother tenant's shard is 404, exactly like one that never existed. An\nalready-finished job is 409.",
		Fields: map[string]string{
			"jobCancel.id":         "ID is the job (activity) id, from the URL path.",
			"jobCancel.reason":     "Reason is recorded on the cancellation; empty records \"canceled from console\".",
			"jobCancel.run":        "Run is the run id; empty defaults to the job id, which is what the dispatcher\nsets (runId == activityId == prompt_id), so the common case sends no body.",
			"jobCanceled.canceled": "Canceled is the job id that was canceled.",
			"jobCanceled.run":      "Run is the run id the cancel was applied to.",
		},
		Example:  json.RawMessage(`{"reason":"superseded"}`),
		Response: json.RawMessage(`{"canceled":"job-1","run":"job-1"}`),
	})
	zip.Describe("POST /v1/visor/fleet/samples", zip.Doc{
		Description: "Records a BYO worker's live GPU utilization into the SAME series the\nfleet board overlays. The org is the validated principal and source/kind are fixed\nserver-side, so a worker names only its own metrics — never another tenant or\nanother source. Answers 202: the warehouse write is DETACHED (its own bounded\ncontext, never in the response path), so a slow or absent warehouse cannot stall a\nheartbeat.",
		Fields: map[string]string{
			"sampleAccepted.recorded": "Recorded is always true: the response is an acknowledgement, and the\nwarehouse write is detached, so it reports acceptance, not durability.",
			"sampleIngest.gpuModel":   "GPUModel names the representative accelerator (\"GB10\"); GPUs carries how\nmany. A heterogeneous host names its first card rather than inventing a\nsummary.",
			"sampleIngest.gpuUtil":    "GPUUtil is accelerator utilization as a fraction 0..1; the warehouse clamps\nanything outside that.",
			"sampleIngest.gpus":       "GPUs is how many accelerators this reading covers.",
			"sampleIngest.host":       "Host is the node's hostname, for display.",
			"sampleIngest.memFree":    "MemFree is host memory still available, in BYTES.",
			"sampleIngest.memUsed":    "MemUsed is host memory in use, in BYTES.",
			"sampleIngest.unit":       "Unit is the reporting node's own id — the same id it registered under, and\nthe key the board joins this series onto. Required.",
		},
		Example:  json.RawMessage(`{"unit":"spark","host":"spark","gpuUtil":0.42,"gpus":1,"gpuModel":"GB10","memUsed":100,"memFree":200}`),
		Response: json.RawMessage(`{"recorded":true}`),
	})
	zip.Describe("POST /v1/visor/k8s/clusters", zip.Doc{
		Description: "Provisions a DOKS cluster for the caller's org and answers 201.\nADMIN-GATED — a SuperAdmin, or an OrgAdmin of the caller's own org — because\nprovisioning spends real infrastructure on the house account. The request is\nvalidated at this boundary, then Visor owns provisioning and the hanzo-org\nownership tag.",
		Fields: map[string]string{
			"clusterView.amdGpu":        "AmdGPU is the same count for `amd.com/gpu`: AMD accelerators across the BYO\ncluster's nodes, as of the attach.",
			"clusterView.createdAt":     "CreatedAt is when the cluster started existing: the earliest creation time\namong its pools for a managed cluster, and for a BYO one the RFC 3339 moment\nit was attached. Empty when the source states none.",
			"clusterView.doClusterId":   "DoClusterID carries the SAME id as DoksClusterID. Both names exist because\nthe console's Cluster type reads either one; neither is a second identifier.",
			"clusterView.doksClusterId": "DoksClusterID is the provider's own id for the cluster, and the value the\n/v1/visor/k8s/clusters/:id routes take. Empty for a BYO cluster: an attached\nkubeconfig was never provisioned, so there is no provider id to state.",
			"clusterView.kind":          "Kind says which of the two kinds of cluster this row is, and there are only\ntwo: \"managed\" — Visor provisioned it and Hanzo's account pays the provider —\nor \"byo\", an existing cluster the org attached by kubeconfig.",
			"clusterView.name":          "Name is the cluster's name: the provider's for a managed cluster, and for a\nBYO one the lower-cased fleet name it was attached under — which is also how\nthe detach route addresses it.",
			"clusterView.nodeCount":     "NodeCount is how many worker nodes the cluster has — the sum over its pools\nfor a managed cluster, and for a BYO one the node count read off the cluster\nwhen it was attached.",
			"clusterView.nodePools":     "NodePools is the authoritative node inventory — every pool, each with its own\nsize and count. It is empty in two cases that are not \"no pools\": a row from\nthe /v1/visor/k8s/clusters LIST, which is deliberately lightweight and whose\n:id detail carries them, and a BYO cluster, whose pools were never read.",
			"clusterView.nodeSize":      "NodeSize is a display convenience: the size slug of the FIRST pool. A cluster\nmixing sizes has more than one, and NodePools is where they all are.",
			"clusterView.nvidiaGpu":     "NvidiaGPU is how many NVIDIA accelerators the cluster's nodes advertise, the\nsum of `nvidia.com/gpu` allocatable across them. BYO only, and counted ONCE\nwhen the cluster was attached — it is an inventory, not live capacity.",
			"clusterView.region":        "Region is the provider region slug for a managed cluster. A BYO cluster has\nno region we can read, so it carries the free-form `provider` label the\nattach named it with (\"gke\", \"on-prem\") instead.",
			"clusterView.status":        "Status is the cluster's state: the provider's own word for a managed cluster\n(\"running\", \"provisioning\"), \"unknown\" when the provider stated none, and\nalways \"attached\" for a BYO cluster — that one says the kubeconfig is on\nfile, not that the cluster is reachable this second.",
			"createClusterReq.name":     "Name is the cluster's name. Required.",
			"createClusterReq.nodePool": "NodePool is the ONE pool the cluster is born with — a cluster with no nodes\nruns nothing, so it is not optional. More pools are added afterwards through\nPOST /v1/visor/clusters/:clusterId/pools.",
			"createClusterReq.region":   "Region is the provider region slug (e.g. \"nyc3\"). Required.",
			"createClusterReq.version":  "Version is the Kubernetes version slug; empty takes the provider default.",
			"nodePoolView.autoScale":    "AutoScale reports whether the provider's cluster autoscaler owns this pool's\nsize, moving Count between MinNodes and MaxNodes as workloads demand. False\nmeans Count changes only when someone scales the pool.",
			"nodePoolView.count":        "Count is how many nodes the pool has right now. Always present, so 0 means a\npool that is genuinely empty rather than a figure the provider withheld.",
			"nodePoolView.maxNodes":     "MaxNodes is the ceiling the autoscaler will not grow the pool past, and so\nthe bound on what this pool can cost. Read it only with AutoScale set.",
			"nodePoolView.minNodes":     "MinNodes is the floor the autoscaler will not shrink the pool below. Read it\nonly with AutoScale set — the provider ignores it otherwise.",
			"nodePoolView.name":         "Name is the pool's name as the provider knows it.",
			"nodePoolView.poolId":       "PoolID is the provider's id for the pool — the value the scale and delete\nroutes address it by. It falls back to the pool's name when the provider\nanswered without one, so it is always something the routes accept.",
			"nodePoolView.size":         "Size is the provider size slug every node in the pool runs at\n(\"s-4vcpu-8gb\", \"gpu-h100x8-640gb\"). One pool is one size — a mixed cluster\nis several pools.",
		},
		Example:  json.RawMessage(`{"name":"prod","region":"nyc3","nodePool":{"name":"gpu","size":"gpu-h100x8-640gb","count":2}}`),
		Response: json.RawMessage(`{"doksClusterId":"cl-1","doClusterId":"cl-1","name":"prod","region":"nyc3","status":"provisioning","nodePools":[],"nodeCount":0,"kind":"managed"}`),
	})
	zip.Describe("PUT /v1/visor/machines/:id/agent", zip.Doc{
		Description: "Binds a cloud Agent to one of the caller org's machines: the\nmachine is recorded as running that Agent's @hanzo/bot runtime. The owning org is\nthe validated tenant, never a client field.",
		Fields: map[string]string{
			"agentBinding.agentName":   "AgentName is the cloud Agent (/v1/agents) this machine runs — the agent a\nmessage to the bot is actually run against. It is the one field that decides\nwhat the bot DOES.",
			"agentBinding.botVersion":  "BotVersion pins the @hanzo/bot runtime version the machine runs. Empty means\nthe machine took the default in force when it was bound.",
			"agentBinding.createdTime": "CreatedTime is when the binding was first made.",
			"agentBinding.machineId":   "MachineId is the bound machine as vm addresses it, owner-qualified\n(\"<org>/<machine>\"). The unqualified half is what this surface's :id routes\ntake.",
			"agentBinding.message":     "Message is vm's human-readable detail on Status (\"machine provisioning;\n@hanzo/bot runtime not yet confirmed\") — the reason behind the state, not a\nsecond state.",
			"agentBinding.name":        "Name is the binding's own key, which is the machine's id: a machine hosts at\nmost one agent, so the binding is named for it. This is the key a bots list\njoins bindings onto machines by.",
			"agentBinding.org":         "Org is the Hanzo tenant the binding belongs to.",
			"agentBinding.owner":       "Owner is the tenant vm filed the binding under, resolved from the ?owner it\nwas called with — which is the caller's validated org and never a body field.",
			"agentBinding.provider":    "Provider is the cloud the bound machine runs on, carried here so a bindings\nlist says where each bot lives without a second read per machine.",
			"agentBinding.publicIp":    "PublicIp is the bound machine's public address as vm recorded it on the\nbinding. Empty while the machine has none yet.",
			"agentBinding.status":      "Status is the binding's lifecycle in VM's OWN words — \"Pending\" while the\nmachine provisions and the runtime is unconfirmed, \"running\" once vm has\nconfirmed it. The vocabulary is vm's and passes through unmapped, which is\nwhy its capitalization does not match the machine states beside it, and it is\nvm's reconciled reading rather than anything asserted here.",
			"agentBinding.updatedTime": "UpdatedTime is when vm last reconciled it — the age of Status.",
			"bindAgentReq.agentName":   "AgentName is the cloud Agent (/v1/agents) the machine will run. Required.",
			"bindAgentReq.botVersion":  "BotVersion pins the @hanzo/bot runtime version; empty takes the default.",
			"bindAgentReq.id":          "ID is the machine to bind, from the URL path.",
		},
		Example:  json.RawMessage(`{"agentName":"bot-a","botVersion":"1.4.0"}`),
		Response: json.RawMessage(`{"machineId":"drop-a","agentName":"bot-a","status":"binding"}`),
	})
}
