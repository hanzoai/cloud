// Code generated by zipdoc; DO NOT EDIT.

package projects

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/platform/sites/:slug", zip.Doc{
		Description: "del deletes one project of the caller's org and everything it published. The\nsubdomain binding, the release history and the object-store content all go, so\nthe slug is free to reclaim and a reclaimed slug inherits no rollback menu.",
		Fields: map[string]string{
			"projectRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("DELETE /v1/platform/sites/:slug/domains/:host", zip.Doc{
		Description: "releaseDomain gives a custom host back so it can be reused. A claim is FIRST-COME\nand global, so until this existed a bound or merely pending host was held\nforever: setDomains only ever adds\n(an empty list is a 400, not a clear), delete-project unbinds the site's own bare\nslug and nothing else, and there was no third writer. A customer who mistyped a\ndomain, or claimed one they later moved elsewhere, could neither reuse it nor let\nanyone else — the row outlived every path that could reach it. Add-only ownership\nof a global namespace is not ownership, it is a leak.\n\nScoped to (host, org, slug) by UnbindHost, so a release can only ever drop THIS\ntenant's own claim; another org's identical-host row is untouched (it cannot exist\n— the host is unique — but the scoping states the guarantee). Idempotent: releasing\na host we do not hold is a clean 204, never a 404 that would let a caller probe\nwhich hosts other tenants hold.",
		Fields: map[string]string{
			"domainRef.host": "Host is the custom hostname from the path.",
			"domainRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","host":"launch.example.com"}`),
	})
	zip.Describe("DELETE /v1/projects/:slug", zip.Doc{
		Description: "del deletes one project of the caller's org and everything it published. The\nsubdomain binding, the release history and the object-store content all go, so\nthe slug is free to reclaim and a reclaimed slug inherits no rollback menu.",
		Fields: map[string]string{
			"projectRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("DELETE /v1/projects/:slug/domains/:host", zip.Doc{
		Description: "releaseDomain gives a custom host back so it can be reused. A claim is FIRST-COME\nand global, so until this existed a bound or merely pending host was held\nforever: setDomains only ever adds\n(an empty list is a 400, not a clear), delete-project unbinds the site's own bare\nslug and nothing else, and there was no third writer. A customer who mistyped a\ndomain, or claimed one they later moved elsewhere, could neither reuse it nor let\nanyone else — the row outlived every path that could reach it. Add-only ownership\nof a global namespace is not ownership, it is a leak.\n\nScoped to (host, org, slug) by UnbindHost, so a release can only ever drop THIS\ntenant's own claim; another org's identical-host row is untouched (it cannot exist\n— the host is unique — but the scoping states the guarantee). Idempotent: releasing\na host we do not hold is a clean 204, never a 404 that would let a caller probe\nwhich hosts other tenants hold.",
		Fields: map[string]string{
			"domainRef.host": "Host is the custom hostname from the path.",
			"domainRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","host":"launch.example.com"}`),
	})
	zip.Describe("GET /v1/platform/sites", zip.Doc{
		Description: "list lists every project the caller's org owns, newest first. Org is the bound\nisolation boundary, so another tenant's projects are never returned.",
		Response:    json.RawMessage(`[{"id":"prj_8a1b","slug":"spring-launch","name":"Spring Launch","status":"live","url":"https://spring-launch.hanzo.app","visibility":"private"}]`),
	})
	zip.Describe("GET /v1/platform/sites/:slug", zip.Doc{
		Description: "get reads one project of the caller's org. It returns the status, live URL,\nframework and linked repo, and a slug in another org is reported not-found.",
		Fields: map[string]string{
			"projectRef.slug":          "Slug is the project slug from the path.",
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("GET /v1/platform/sites/:slug/deployments", zip.Doc{
		Description: "listDeployments lists one project's deployments, newest first. Each row carries\nthe version, status, source and the live URL that deployment produced.",
		Fields: map[string]string{
			"projectRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("GET /v1/platform/sites/:slug/deployments/:id", zip.Doc{
		Description: "getDeployment reads one deployment of one project: its version, status, source\ncommit, byte and file counts, and the URL it went live at.",
		Fields: map[string]string{
			"deploymentRef.id":      "ID is the deployment id from the path.",
			"deploymentRef.slug":    "Slug is the project slug from the path.",
			"siteDeployment.upload": "Upload is the prefix-scoped, short-lived S3 write grant handed to CI with a\nqueued git deployment, so it needs no bucket credential (grant.go). Present\nONLY on the 202 that creates the deployment — it is never stored and never\nreplayed on a later read, so a grant cannot outlive the build it was minted\nfor by being fetched again.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","id":"dep_2f9c1a"}`),
	})
	zip.Describe("GET /v1/platform/sites/:slug/domains", zip.Doc{
		Description: "listDomains returns every host this site holds. The live ones come back beside\nany pending claim with the DNS records it still owes.",
		Fields: map[string]string{
			"projectRef.slug":     "Slug is the project slug from the path.",
			"siteDomains.claims":  "Claims is every host on record, verified or still pending its DNS challenge.",
			"siteDomains.domains": "Domains are the hosts that are verified and routing.",
			"siteDomains.org":     "Org is the owning tenant.",
			"siteDomains.slug":    "Slug is the site the hosts belong to.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("GET /v1/platform/sites/:slug/releases", zip.Doc{
		Description: "listReleases returns a site's releases newest-first, marking the active one.\nIt is the rollback menu: any row here can be activated.",
		Fields: map[string]string{
			"projectRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("GET /v1/projects", zip.Doc{
		Description: "list lists every project the caller's org owns, newest first. Org is the bound\nisolation boundary, so another tenant's projects are never returned.",
		Response:    json.RawMessage(`[{"id":"prj_8a1b","slug":"spring-launch","name":"Spring Launch","status":"live","url":"https://spring-launch.hanzo.app","visibility":"private"}]`),
	})
	zip.Describe("GET /v1/projects/:slug", zip.Doc{
		Description: "get reads one project of the caller's org. It returns the status, live URL,\nframework and linked repo, and a slug in another org is reported not-found.",
		Fields: map[string]string{
			"projectRef.slug":          "Slug is the project slug from the path.",
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("GET /v1/projects/:slug/deployments", zip.Doc{
		Description: "listDeployments lists one project's deployments, newest first. Each row carries\nthe version, status, source and the live URL that deployment produced.",
		Fields: map[string]string{
			"projectRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("GET /v1/projects/:slug/deployments/:id", zip.Doc{
		Description: "getDeployment reads one deployment of one project: its version, status, source\ncommit, byte and file counts, and the URL it went live at.",
		Fields: map[string]string{
			"deploymentRef.id":      "ID is the deployment id from the path.",
			"deploymentRef.slug":    "Slug is the project slug from the path.",
			"siteDeployment.upload": "Upload is the prefix-scoped, short-lived S3 write grant handed to CI with a\nqueued git deployment, so it needs no bucket credential (grant.go). Present\nONLY on the 202 that creates the deployment — it is never stored and never\nreplayed on a later read, so a grant cannot outlive the build it was minted\nfor by being fetched again.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","id":"dep_2f9c1a"}`),
	})
	zip.Describe("GET /v1/projects/:slug/domains", zip.Doc{
		Description: "listDomains returns every host this site holds. The live ones come back beside\nany pending claim with the DNS records it still owes.",
		Fields: map[string]string{
			"projectRef.slug":     "Slug is the project slug from the path.",
			"siteDomains.claims":  "Claims is every host on record, verified or still pending its DNS challenge.",
			"siteDomains.domains": "Domains are the hosts that are verified and routing.",
			"siteDomains.org":     "Org is the owning tenant.",
			"siteDomains.slug":    "Slug is the site the hosts belong to.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("GET /v1/sites", zip.Doc{
		Description: "listSites lists the org's deployed sites at their public URLs. Only projects\nthat are actually live appear; one that never deployed is not a site yet.",
		Response:    json.RawMessage(`[{"slug":"spring-launch","url":"https://spring-launch.hanzo.app","name":"Spring Launch","status":"live","updatedAt":1780000000}]`),
	})
	zip.Describe("GET /v1/sites/:slug/releases", zip.Doc{
		Description: "listReleases returns a site's releases newest-first, marking the active one.\nIt is the rollback menu: any row here can be activated.",
		Fields: map[string]string{
			"projectRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("PATCH /v1/platform/sites/:slug", zip.Doc{
		Description: "update edits one project of the caller's org. Every field is a pointer, so an\nabsent one is left alone and an explicit null or empty clears it; hidden and\nhiddenReason are moderation and are ignored unless the caller is an operator.",
		Fields: map[string]string{
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
			"siteUpdate.hidden":        "Hidden is MODERATION, and the only admin-gated field on this body: it pulls\na public project out of the catalogue from admin.hanzo.ai without editing\nthe publisher's own visibility choice, so un-hiding restores exactly what\nthey asked for. A tenant sending it is ignored.",
			"siteUpdate.slug":          "Slug is the project slug from the path — which project to edit.",
			"siteUpdate.upstream":      "Upstream/License credit the third-party work this app was published from —\nsettable after the fact, because the demos that need crediting most are the\nones already live. Pointers so \"\" clears a credit and absent leaves it.",
			"siteUpdate.visibility":    "Visibility flips an existing project between \"public\" and \"private\". Same\nONE rule as at create: public is free, private needs a paid plan.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","description":"campaign microsite","visibility":"public","upstream":"UI8 — Fitness Pro Website UI Kit"}`),
	})
	zip.Describe("PATCH /v1/projects/:slug", zip.Doc{
		Description: "update edits one project of the caller's org. Every field is a pointer, so an\nabsent one is left alone and an explicit null or empty clears it; hidden and\nhiddenReason are moderation and are ignored unless the caller is an operator.",
		Fields: map[string]string{
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
			"siteUpdate.hidden":        "Hidden is MODERATION, and the only admin-gated field on this body: it pulls\na public project out of the catalogue from admin.hanzo.ai without editing\nthe publisher's own visibility choice, so un-hiding restores exactly what\nthey asked for. A tenant sending it is ignored.",
			"siteUpdate.slug":          "Slug is the project slug from the path — which project to edit.",
			"siteUpdate.upstream":      "Upstream/License credit the third-party work this app was published from —\nsettable after the fact, because the demos that need crediting most are the\nones already live. Pointers so \"\" clears a credit and absent leaves it.",
			"siteUpdate.visibility":    "Visibility flips an existing project between \"public\" and \"private\". Same\nONE rule as at create: public is free, private needs a paid plan.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","description":"campaign microsite","visibility":"public","upstream":"UI8 — Fitness Pro Website UI Kit"}`),
	})
	zip.Describe("POST /v1/platform/sites", zip.Doc{
		Description: "create registers a new project (a static site) in the caller's org. The slug is\nderived from the name when omitted, must be free in this org, and becomes the\nproject's public subdomain, so a reserved label is refused.",
		Fields: map[string]string{
			"siteCreate.analytics":     "Analytics is the opt-OUT for the wired-by-default analytics beacon: absent\n(nil) ⇒ ON (the default); explicit false ⇒ off. A pointer so \"unset\" is\ndistinguishable from \"false\" — the only way to turn the default off.",
			"siteCreate.upstream":      "Upstream/License credit the third-party work this project was published\nfrom. Taken from any caller: disclaiming authorship can only cost the\npublisher credit, so it needs no gate (see Project.Upstream).",
			"siteCreate.visibility":    "Visibility is \"public\" (the default when absent) or \"private\". Publishing\npublicly is ungated — that is the point of a community. Going PRIVATE is\nthe paid feature, so an unfunded org asking for it is refused rather than\nsilently downgraded (see resolve).",
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
		},
		Example: json.RawMessage(`{"name":"Spring Launch","slug":"spring-launch","description":"campaign microsite","framework":"next","visibility":"private"}`),
	})
	zip.Describe("POST /v1/platform/sites/:slug/domains", zip.Doc{
		Description: "setDomains attaches custom public hostnames to this org's static site. Binding a\nhost you do not own would let you shadow it at the edge, so which outcome you get\ndepends on whether ownership is already established:\n\n  - SuperAdmin or the platform-operator org → bound VERIFIED immediately. The\n    operator manages customer DNS, so its bind is itself the vouch.\n  - any other org → the host is CLAIMED as pending and the response carries the\n    DNS challenge. The claim HOLDS the name so nobody else can take it, but it\n    does not route until POST .../domains/{host}/verify proves control.\n\nBefore this was wired, a non-operator org got a flat 403 and could not bind at\nall; the only way onto a custom domain was to ask an operator. Now it is the\nsame DNS-01 self-serve the apps path has always had, over the same primitives.\n\nFirst-come and reserved-label guards are the store's. Claims and binds are\nidempotent for the same (org, slug) — a redeploy or repeat call is safe, and a\nre-claim returns the SAME token rather than invalidating a record the customer\nhas already published.",
		Fields: map[string]string{
			"boundDomains.bound":    "Bound is one claim view per host in the request, with its DNS challenge.",
			"boundDomains.domains":  "Domains are the site's hosts that are verified and routing.",
			"boundDomains.org":      "Org is the owning tenant.",
			"boundDomains.slug":     "Slug is the site the hosts were bound to.",
			"setDomainsReq.domains": "Domains are the custom hostnames to attach. At least one is required.",
			"setDomainsReq.slug":    "Slug is the project slug from the path — which site to bind the hosts to.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","domains":["launch.example.com"]}`),
	})
	zip.Describe("POST /v1/platform/sites/:slug/domains/:host/verify", zip.Doc{
		Description: "verifyDomain resolves the DNS challenge for a pending custom-host claim. On\nsuccess the host is promoted and begins routing at the edge; on not-yet it\nreturns the honest still-pending view rather than an error, because the check\nran and simply did not find the record, so the customer retries once DNS\npropagates.",
		Fields: map[string]string{
			"domainRef.host": "Host is the custom hostname from the path.",
			"domainRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","host":"launch.example.com"}`),
	})
	zip.Describe("POST /v1/platform/sites/:slug/purge", zip.Doc{
		Description: "purge flushes a project's edge cache without redeploying it. The object-store\norigin is never written or deleted, so the live build keeps serving and only\nstale edge copies drop; an edge miss is non-fatal and the updated project is\nreturned either way, so the caller sees the new lastPurgeAt.",
		Fields: map[string]string{
			"projectRef.slug":          "Slug is the project slug from the path.",
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("POST /v1/platform/sites/:slug/releases/:release/activate", zip.Doc{
		Description: "activateRelease flips the site's pointer to an existing release. It is the\ngo-live and equally the ROLLBACK — aim it at an older release and nothing is\nrebuilt or re-copied, because releases are immutable and retained to the\nretention depth, so it is not billed: only a pointer moves.",
		Fields: map[string]string{
			"releaseRef.release": "Release is the release id from the path.",
			"releaseRef.slug":    "Slug is the site slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","release":"rel_7c31a9"}`),
	})
	zip.Describe("POST /v1/projects", zip.Doc{
		Description: "create registers a new project (a static site) in the caller's org. The slug is\nderived from the name when omitted, must be free in this org, and becomes the\nproject's public subdomain, so a reserved label is refused.",
		Fields: map[string]string{
			"siteCreate.analytics":     "Analytics is the opt-OUT for the wired-by-default analytics beacon: absent\n(nil) ⇒ ON (the default); explicit false ⇒ off. A pointer so \"unset\" is\ndistinguishable from \"false\" — the only way to turn the default off.",
			"siteCreate.upstream":      "Upstream/License credit the third-party work this project was published\nfrom. Taken from any caller: disclaiming authorship can only cost the\npublisher credit, so it needs no gate (see Project.Upstream).",
			"siteCreate.visibility":    "Visibility is \"public\" (the default when absent) or \"private\". Publishing\npublicly is ungated — that is the point of a community. Going PRIVATE is\nthe paid feature, so an unfunded org asking for it is refused rather than\nsilently downgraded (see resolve).",
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
		},
		Example: json.RawMessage(`{"name":"Spring Launch","slug":"spring-launch","description":"campaign microsite","framework":"next","visibility":"private"}`),
	})
	zip.Describe("POST /v1/projects/:slug/deployments/:id/complete", zip.Doc{
		Description: "completeDeployment is the CI completion hook for the git path. After CI syncs the\nbuilt site to object storage this flips the queued deployment to live or error,\nand only a live completion is billed; keys is the manifest CI uploaded, and\nsupplying it lets cloud delete what CI replaced.",
		Fields: map[string]string{
			"completeReq.id":        "ID is the queued deployment id from the path.",
			"completeReq.keys":      "Keys is the manifest CI just uploaded, RELATIVE to the deployment prefix. It\nis what replaces `aws s3 sync --delete`: an upload grant authorizes writes\nonly, so CI cannot remove a file, and cloud reconciles the prefix against\nthis list instead (grant.go). Omit it and nothing is deleted — the prefix\nonly grows, which is the old pre-grant behaviour and a safe default.",
			"completeReq.slug":      "Slug is the project slug from the path.",
			"completeReq.status":    "live | error",
			"siteDeployment.upload": "Upload is the prefix-scoped, short-lived S3 write grant handed to CI with a\nqueued git deployment, so it needs no bucket credential (grant.go). Present\nONLY on the 202 that creates the deployment — it is never stored and never\nreplayed on a later read, so a grant cannot outlive the build it was minted\nfor by being fetched again.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","id":"dep_2f9c1a","status":"live","commit":"9f2c1ab","files":42,"bytes":1048576}`),
	})
	zip.Describe("POST /v1/projects/:slug/domains", zip.Doc{
		Description: "setDomains attaches custom public hostnames to this org's static site. Binding a\nhost you do not own would let you shadow it at the edge, so which outcome you get\ndepends on whether ownership is already established:\n\n  - SuperAdmin or the platform-operator org → bound VERIFIED immediately. The\n    operator manages customer DNS, so its bind is itself the vouch.\n  - any other org → the host is CLAIMED as pending and the response carries the\n    DNS challenge. The claim HOLDS the name so nobody else can take it, but it\n    does not route until POST .../domains/{host}/verify proves control.\n\nBefore this was wired, a non-operator org got a flat 403 and could not bind at\nall; the only way onto a custom domain was to ask an operator. Now it is the\nsame DNS-01 self-serve the apps path has always had, over the same primitives.\n\nFirst-come and reserved-label guards are the store's. Claims and binds are\nidempotent for the same (org, slug) — a redeploy or repeat call is safe, and a\nre-claim returns the SAME token rather than invalidating a record the customer\nhas already published.",
		Fields: map[string]string{
			"boundDomains.bound":    "Bound is one claim view per host in the request, with its DNS challenge.",
			"boundDomains.domains":  "Domains are the site's hosts that are verified and routing.",
			"boundDomains.org":      "Org is the owning tenant.",
			"boundDomains.slug":     "Slug is the site the hosts were bound to.",
			"setDomainsReq.domains": "Domains are the custom hostnames to attach. At least one is required.",
			"setDomainsReq.slug":    "Slug is the project slug from the path — which site to bind the hosts to.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","domains":["launch.example.com"]}`),
	})
	zip.Describe("POST /v1/projects/:slug/domains/:host/verify", zip.Doc{
		Description: "verifyDomain resolves the DNS challenge for a pending custom-host claim. On\nsuccess the host is promoted and begins routing at the edge; on not-yet it\nreturns the honest still-pending view rather than an error, because the check\nran and simply did not find the record, so the customer retries once DNS\npropagates.",
		Fields: map[string]string{
			"domainRef.host": "Host is the custom hostname from the path.",
			"domainRef.slug": "Slug is the project slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","host":"launch.example.com"}`),
	})
	zip.Describe("POST /v1/projects/:slug/purge", zip.Doc{
		Description: "purge flushes a project's edge cache without redeploying it. The object-store\norigin is never written or deleted, so the live build keeps serving and only\nstale edge copies drop; an edge miss is non-fatal and the updated project is\nreturned either way, so the caller sees the new lastPurgeAt.",
		Fields: map[string]string{
			"projectRef.slug":          "Slug is the project slug from the path.",
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch"}`),
	})
	zip.Describe("POST /v1/projects/fork", zip.Doc{
		Description: "fork creates a real project seeded from a published example. The parent is a\nstarter-kit template from the embedded gallery catalog or any live project on\nthe platform, it funnels through the SAME create path so slug validation and\nconflict handling are not duplicated, and the parent it actually resolved is\nstamped on the child, so attribution is recorded at fork time rather than\nreconstructed later.",
		Fields: map[string]string{
			"siteFork.name":            "target project name (optional; defaults to the parent's title)",
			"siteFork.slug":            "parent slug to fork — catalog template or published project (required)",
			"siteFork.target":          "Target overrides the derived project slug (optional; defaults to the\nparent slug). Kept distinct from Slug so callers can rename on fork.",
			"siteFork.variant":         "Variant picks a template's format/page/theme (optional; defaults to the\ntemplate's first shape). This is the axis the catalog used to spend\nsibling slugs on, so it is expressed here, where the user's preference is.",
			"siteProject.analytics":    "Analytics is the wired-by-default web-analytics flag (default true). It is the\nvalue the app's static-builder reads as deployment.analytics to inject the\nbeacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed\nsite posts form/forum/data submissions to under /v1/base.",
			"siteProject.cacheControl": "Cache is the site's edge-cache state: the HTML/document Cache-Control policy\nin effect (TTL) and the last edge-purge time, so a console can show freshness.",
			"siteProject.forkedFrom":   "ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a\npublished project, or a catalog template slug) — the attribution edge a\ngallery credits.",
			"siteProject.upstream":     "Upstream/License credit the third-party work this project was published\nfrom, and the terms it carries. Omitted when nothing is declared: an absent\ncredit means \"nobody has said\", not \"there is nothing to say\".",
			"siteProject.visibility":   "Visibility is \"public\" or \"private\", and Hidden reports platform\nmoderation. Both are always present (never omitempty) so a consumer can\ntell a real answer from \"this API is too old to say\" — and so a console\nnever renders a project as public because a field was missing.\n\nAuthorship is deliberately absent: it is Org, above.",
		},
		Example: json.RawMessage(`{"slug":"portfolio","name":"My Portfolio","variant":"dark","target":"my-portfolio"}`),
	})
	zip.Describe("POST /v1/sites/:slug/releases/:release/activate", zip.Doc{
		Description: "activateRelease flips the site's pointer to an existing release. It is the\ngo-live and equally the ROLLBACK — aim it at an older release and nothing is\nrebuilt or re-copied, because releases are immutable and retained to the\nretention depth, so it is not billed: only a pointer moves.",
		Fields: map[string]string{
			"releaseRef.release": "Release is the release id from the path.",
			"releaseRef.slug":    "Slug is the site slug from the path.",
		},
		Example: json.RawMessage(`{"slug":"spring-launch","release":"rel_7c31a9"}`),
	})
}
