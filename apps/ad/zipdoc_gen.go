// Code generated by zipdoc; DO NOT EDIT.

package ad

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/ad/campaigns/:id", zip.Doc{
		Description: "Removes one of the caller org's campaigns and answers 204 with\nno body. It deletes the stored record only: a campaign already launched keeps\nrunning on the ad network, which must be stopped there. An id another org owns\nreads as not found.",
		Example:     json.RawMessage(`{"id":"camp_2f9c1d"}`),
	})
	zip.Describe("GET /v1/ad/campaigns", zip.Doc{
		Description: "Returns the caller org's ad campaigns, most recently updated\nfirst, optionally narrowed to one lifecycle status. The listing is bounded by\nthe org: another tenant's campaigns are not reachable from here at all.",
		Fields: map[string]string{
			"AdCampaign.account":     "Account is the provider ad-account the campaign runs under, in Meta's\nact_<id> form. Empty until the org supplies one or a launch resolves it.",
			"AdCampaign.budget":      "Budget is the campaign's authorized spend in MINOR units (cents). Negative\nclamps to 0. It is the org's stored plan: a Meta launch creates the campaign\nobject only, and the delivering budget lives on the ad set.",
			"AdCampaign.createdAt":   "CreatedAt is when the campaign was first stored, in unix seconds. It never\nchanges, including across a full-replace update.",
			"AdCampaign.externalId":  "ExternalID is the ad network's own campaign id, written by a successful\nlaunch and by nothing else — an update never touches it. Empty means this\ncampaign has never reached its network.",
			"AdCampaign.id":          "ID is the campaign's server-minted handle, \"camp_\" + 32 hex. A create body\ncannot choose it, and it is the id every other route addresses.",
			"AdCampaign.name":        "Name is the campaign's display label, and the name Meta creates the campaign\nobject under at launch. Required; trimmed and bounded to 1024 bytes.",
			"AdCampaign.objective":   "Objective is the campaign goal spelled as the provider names it\n(\"conversions\", \"OUTCOME_TRAFFIC\"), passed through to the network verbatim at\nlaunch — Meta defaults an empty one to OUTCOME_TRAFFIC. Free text, bounded to\n1024 bytes; no vocabulary is enforced here.",
			"AdCampaign.platform":    "Platform is the ad network: meta, google, tiktok or x, and nothing else — a\nwrite naming another is 400. Empty stores as meta. Only meta executes today;\nlaunching any of the other three is 501.",
			"AdCampaign.spend":       "Spend is spend-to-date in MINOR units (cents), as last written through create\nor update. Negative clamps to 0. It is NOT read back from the network — that\nis a separate insights call — so 0 means nothing was recorded here, not that\nnothing was spent.",
			"AdCampaign.status":      "Status is the lifecycle: draft, active, paused or completed, and nothing else\n— a write naming another is 400. Empty stores as draft; a successful launch\nsets active. It records what this deployment did, not what the ad network\ncurrently reports.",
			"AdCampaign.updatedAt":   "UpdatedAt is when the row was last written, in unix seconds — set by create,\nupdate and launch. Listings are ordered by it, newest first.",
			"campaignList.data":      "Data is the matching campaigns, newest-updated first.",
			"listCampaignsIn.limit":  "Limit caps how many campaigns come back: default 200, maximum 1000. A\nvalue that is not a positive integer reads as the default.",
			"listCampaignsIn.status": "Status filters to one lifecycle state (draft, active, paused, completed).\nEmpty returns every campaign the org has.",
		},
		Example: json.RawMessage(`{"status":"active","limit":50}`),
	})
	zip.Describe("GET /v1/ad/campaigns/:id", zip.Doc{
		Description: "Returns one of the caller org's campaigns. An id another org owns\nreads as not found, so the response cannot confirm that it exists.",
		Fields: map[string]string{
			"AdCampaign.account":    "Account is the provider ad-account the campaign runs under, in Meta's\nact_<id> form. Empty until the org supplies one or a launch resolves it.",
			"AdCampaign.budget":     "Budget is the campaign's authorized spend in MINOR units (cents). Negative\nclamps to 0. It is the org's stored plan: a Meta launch creates the campaign\nobject only, and the delivering budget lives on the ad set.",
			"AdCampaign.createdAt":  "CreatedAt is when the campaign was first stored, in unix seconds. It never\nchanges, including across a full-replace update.",
			"AdCampaign.externalId": "ExternalID is the ad network's own campaign id, written by a successful\nlaunch and by nothing else — an update never touches it. Empty means this\ncampaign has never reached its network.",
			"AdCampaign.id":         "ID is the campaign's server-minted handle, \"camp_\" + 32 hex. A create body\ncannot choose it, and it is the id every other route addresses.",
			"AdCampaign.name":       "Name is the campaign's display label, and the name Meta creates the campaign\nobject under at launch. Required; trimmed and bounded to 1024 bytes.",
			"AdCampaign.objective":  "Objective is the campaign goal spelled as the provider names it\n(\"conversions\", \"OUTCOME_TRAFFIC\"), passed through to the network verbatim at\nlaunch — Meta defaults an empty one to OUTCOME_TRAFFIC. Free text, bounded to\n1024 bytes; no vocabulary is enforced here.",
			"AdCampaign.platform":   "Platform is the ad network: meta, google, tiktok or x, and nothing else — a\nwrite naming another is 400. Empty stores as meta. Only meta executes today;\nlaunching any of the other three is 501.",
			"AdCampaign.spend":      "Spend is spend-to-date in MINOR units (cents), as last written through create\nor update. Negative clamps to 0. It is NOT read back from the network — that\nis a separate insights call — so 0 means nothing was recorded here, not that\nnothing was spent.",
			"AdCampaign.status":     "Status is the lifecycle: draft, active, paused or completed, and nothing else\n— a write naming another is 400. Empty stores as draft; a successful launch\nsets active. It records what this deployment did, not what the ad network\ncurrently reports.",
			"AdCampaign.updatedAt":  "UpdatedAt is when the row was last written, in unix seconds — set by create,\nupdate and launch. Listings are ordered by it, newest first.",
		},
		Example: json.RawMessage(`{"id":"camp_2f9c1d"}`),
	})
	zip.Describe("GET /v1/ad/summary", zip.Doc{
		Description: "Rolls the caller org's ad campaigns up into four numbers: how many\ncampaigns exist, how many are active, and the summed budget and spend across\nall of them. Budget and spend are MINOR units (cents), the same units the\ncampaign rows carry. It counts only this org's campaigns.",
		Fields: map[string]string{
			"adSummary.active":    "Active is how many of those campaigns are in the active state.",
			"adSummary.budget":    "Budget is the summed budget of every campaign in the org, in cents.",
			"adSummary.campaigns": "Campaigns is how many campaigns the org has, in every state.",
			"adSummary.spend":     "Spend is the summed spend of every campaign in the org, in cents.",
		},
	})
	zip.Describe("POST /v1/ad/campaigns", zip.Doc{
		Description: "Registers a new ad campaign for the caller's org and answers\n201 with the stored row. It only records the campaign — nothing is sent to the\nad network until POST /v1/ad/campaigns/{id}/launch runs it. The org is\nstamped by the server from the validated principal, so a body can never place\na campaign in another tenant.",
		Fields: map[string]string{
			"AdCampaign.account":      "Account is the provider ad-account the campaign runs under, in Meta's\nact_<id> form. Empty until the org supplies one or a launch resolves it.",
			"AdCampaign.budget":       "Budget is the campaign's authorized spend in MINOR units (cents). Negative\nclamps to 0. It is the org's stored plan: a Meta launch creates the campaign\nobject only, and the delivering budget lives on the ad set.",
			"AdCampaign.createdAt":    "CreatedAt is when the campaign was first stored, in unix seconds. It never\nchanges, including across a full-replace update.",
			"AdCampaign.externalId":   "ExternalID is the ad network's own campaign id, written by a successful\nlaunch and by nothing else — an update never touches it. Empty means this\ncampaign has never reached its network.",
			"AdCampaign.id":           "ID is the campaign's server-minted handle, \"camp_\" + 32 hex. A create body\ncannot choose it, and it is the id every other route addresses.",
			"AdCampaign.name":         "Name is the campaign's display label, and the name Meta creates the campaign\nobject under at launch. Required; trimmed and bounded to 1024 bytes.",
			"AdCampaign.objective":    "Objective is the campaign goal spelled as the provider names it\n(\"conversions\", \"OUTCOME_TRAFFIC\"), passed through to the network verbatim at\nlaunch — Meta defaults an empty one to OUTCOME_TRAFFIC. Free text, bounded to\n1024 bytes; no vocabulary is enforced here.",
			"AdCampaign.platform":     "Platform is the ad network: meta, google, tiktok or x, and nothing else — a\nwrite naming another is 400. Empty stores as meta. Only meta executes today;\nlaunching any of the other three is 501.",
			"AdCampaign.spend":        "Spend is spend-to-date in MINOR units (cents), as last written through create\nor update. Negative clamps to 0. It is NOT read back from the network — that\nis a separate insights call — so 0 means nothing was recorded here, not that\nnothing was spent.",
			"AdCampaign.status":       "Status is the lifecycle: draft, active, paused or completed, and nothing else\n— a write naming another is 400. Empty stores as draft; a successful launch\nsets active. It records what this deployment did, not what the ad network\ncurrently reports.",
			"AdCampaign.updatedAt":    "UpdatedAt is when the row was last written, in unix seconds — set by create,\nupdate and launch. Listings are ordered by it, newest first.",
			"campaignInput.account":   "Account is the provider ad-account this campaign runs on (Meta act_<id>). Optional.",
			"campaignInput.budget":    "Budget is the campaign budget in MINOR units (cents). Negative values clamp to 0.",
			"campaignInput.name":      "Name is the campaign's display label. Required; trimmed and bounded to 1024 bytes.",
			"campaignInput.objective": "Objective is the campaign goal as the provider names it. Optional, bounded to 1024 bytes.",
			"campaignInput.platform":  "Platform is the ad network: meta, google, tiktok or x. Empty defaults to meta.",
			"campaignInput.spend":     "Spend is the amount spent so far in MINOR units (cents). Negative values clamp to 0.",
			"campaignInput.status":    "Status is the lifecycle state: draft, active, paused or completed. Empty defaults to draft.",
		},
		Example: json.RawMessage(`{"name":"Spring Launch","platform":"meta","objective":"conversions","budget":50000}`),
	})
	zip.Describe("POST /v1/ad/campaigns/:id/launch", zip.Doc{
		Description: "Runs a stored ad campaign on its provider using the ORG'S\nconnected ad-account token. It is the standalone proof that /v1/ad consumes the\nconnector plane: no token is held here — LaunchPaid (provider.go) resolves it\nfrom KMS through integrations.TokenFor and FAILS CLOSED when the org has not\nconnected the platform (424), so a launch can never spend on a connection the\norg did not make. On success the provider campaign id is recorded (MarkLaunched)\nand the campaign goes active. An optional body {account} sets/overrides the\ntarget ad account when the stored campaign has none.",
	})
	zip.Describe("PUT /v1/ad/campaigns/:id", zip.Doc{
		Description: "Replaces the user-owned fields of one of the caller org's\ncampaigns and answers the stored row. It is a full replace, not a patch: every\nfield is written from the request, so an omitted one is cleared. externalId is\nlaunch-owned and is never touched here, so editing a campaign cannot break its\nlink to a live provider execution.",
		Fields: map[string]string{
			"AdCampaign.account":         "Account is the provider ad-account the campaign runs under, in Meta's\nact_<id> form. Empty until the org supplies one or a launch resolves it.",
			"AdCampaign.budget":          "Budget is the campaign's authorized spend in MINOR units (cents). Negative\nclamps to 0. It is the org's stored plan: a Meta launch creates the campaign\nobject only, and the delivering budget lives on the ad set.",
			"AdCampaign.createdAt":       "CreatedAt is when the campaign was first stored, in unix seconds. It never\nchanges, including across a full-replace update.",
			"AdCampaign.externalId":      "ExternalID is the ad network's own campaign id, written by a successful\nlaunch and by nothing else — an update never touches it. Empty means this\ncampaign has never reached its network.",
			"AdCampaign.id":              "ID is the campaign's server-minted handle, \"camp_\" + 32 hex. A create body\ncannot choose it, and it is the id every other route addresses.",
			"AdCampaign.name":            "Name is the campaign's display label, and the name Meta creates the campaign\nobject under at launch. Required; trimmed and bounded to 1024 bytes.",
			"AdCampaign.objective":       "Objective is the campaign goal spelled as the provider names it\n(\"conversions\", \"OUTCOME_TRAFFIC\"), passed through to the network verbatim at\nlaunch — Meta defaults an empty one to OUTCOME_TRAFFIC. Free text, bounded to\n1024 bytes; no vocabulary is enforced here.",
			"AdCampaign.platform":        "Platform is the ad network: meta, google, tiktok or x, and nothing else — a\nwrite naming another is 400. Empty stores as meta. Only meta executes today;\nlaunching any of the other three is 501.",
			"AdCampaign.spend":           "Spend is spend-to-date in MINOR units (cents), as last written through create\nor update. Negative clamps to 0. It is NOT read back from the network — that\nis a separate insights call — so 0 means nothing was recorded here, not that\nnothing was spent.",
			"AdCampaign.status":          "Status is the lifecycle: draft, active, paused or completed, and nothing else\n— a write naming another is 400. Empty stores as draft; a successful launch\nsets active. It records what this deployment did, not what the ad network\ncurrently reports.",
			"AdCampaign.updatedAt":       "UpdatedAt is when the row was last written, in unix seconds — set by create,\nupdate and launch. Listings are ordered by it, newest first.",
			"updateCampaignIn.account":   "Account is the provider ad-account this campaign runs on (Meta act_<id>). Optional.",
			"updateCampaignIn.budget":    "Budget is the campaign budget in MINOR units (cents). Negative values clamp to 0.",
			"updateCampaignIn.name":      "Name is the campaign's display label. Required; trimmed and bounded to 1024 bytes.",
			"updateCampaignIn.objective": "Objective is the campaign goal as the provider names it. Optional, bounded to 1024 bytes.",
			"updateCampaignIn.platform":  "Platform is the ad network: meta, google, tiktok or x. Empty defaults to meta.",
			"updateCampaignIn.spend":     "Spend is the amount spent so far in MINOR units (cents). Negative values clamp to 0.",
			"updateCampaignIn.status":    "Status is the lifecycle state: draft, active, paused or completed. Empty defaults to draft.",
		},
		Example: json.RawMessage(`{"id":"camp_2f9c1d","name":"Spring Launch","platform":"meta","status":"paused","budget":75000}`),
	})
}
