// Code generated by zipdoc; DO NOT EDIT.

package world

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/world/limits", zip.Doc{
		Description: "getLimits echoes the World limits a plan grants, straight from the @hanzo/plans\ncatalog, so agents and dashboards configure themselves against the live catalog\ninstead of hardcoding tier numbers. It reports the requested plan, not the\ncaller's: it is a contract lookup, not an entitlement check. A catalog that\ncannot be read yields the free-tier floor rather than an error.",
		Fields: map[string]string{
			"limitsBlock.apiRateLimit": "APIRateLimit bounds REST /v1/world/* calls per minute.",
			"limitsBlock.maxAlerts":    "MaxAlerts bounds saved OSINT alert rules.",
			"limitsBlock.mcpRateLimit": "MCPRateLimit bounds MCP calls per minute.",
			"limitsBlock.modelApi":     "ModelAPI reports whether the plan grants /v1/world/model and the SSE stream.",
			"limitsView.limits":        "Limits is the resolved contract; -1 on a rate limit or count means unlimited.",
			"limitsView.plan":          "Plan is the plan id the limits were resolved for.",
			"limitsView.unit":          "Unit is the unit both rate limits are expressed in.",
			"planRef.plan":             "Plan is a World plan id (world-free, world-pro, world-team,\nworld-enterprise). Empty resolves world-free.",
		},
		Example:  json.RawMessage(`{"plan":"world-pro"}`),
		Response: json.RawMessage(`{"plan":"world-pro","unit":"requests/minute","limits":{"apiRateLimit":6000,"mcpRateLimit":3000,"maxAlerts":-1,"modelApi":true}}`),
	})
	zip.Describe("GET /v1/world/news", zip.Doc{
		Description: "getNews serves the merged, filtered, freshest-first news feed for the caller's\n(org, project). It loads the pipeline (or the default), fans out to GDELT (per\nkeyword) + RSS (per feed) concurrently, applies the project filters, dedupes,\nsorts by recency, caps the result at 50, and publishes a live refresh to the\nSSE stream. A source that fails is skipped, so the feed degrades to honest\npartial results rather than an error.",
		Fields: map[string]string{
			"NewsItem.image":     "Image is a lead-image URL, when the upstream carried one.",
			"NewsItem.lang":      "Lang is the upstream's language name, when it named one.",
			"NewsItem.link":      "Link is the article URL.",
			"NewsItem.pubDate":   "PubDate is RFC3339 UTC, or \"\" when the upstream gave no parseable date.\nItems sort freshest-first and a dateless item sorts last.",
			"NewsItem.source":    "Source names the upstream the item came from: the RSS channel title, or\nthe GDELT article's domain.",
			"NewsItem.title":     "Title is the headline; it is also the text keyword and region filters match on.",
			"NewsItem.tone":      "Tone is GDELT's sentiment score as text; absent for RSS/Atom items.",
			"newsResponse.items": "Items is the merged, filtered, deduped feed, freshest first, capped at 50.\nEmpty (never null) when no upstream answered.",
			"scopeRef.project":   "Project, when given, must equal the caller's authenticated project scope;\na mismatch is refused. Omit it to read the authenticated project.",
		},
		Response: json.RawMessage(`{"items":[{"source":"BBC News","title":"Border conflict escalates","link":"https://example.com/a","pubDate":"2026-07-07T12:00:00Z"}]}`),
	})
	zip.Describe("GET /v1/world/pipeline", zip.Doc{
		Description: "getPipeline reads the feed + filter configuration for the caller's (org,\nproject). A project that has saved none gets the built-in world-news feeds\nwith default:true, so a first read is never empty.",
		Fields: map[string]string{
			"pipelineView.createdAt": "CreatedAt and UpdatedAt are RFC3339 UTC, absent on a default pipeline.",
			"pipelineView.default":   "Default is true when no pipeline is stored and these are the built-in feeds.",
			"pipelineView.feeds":     "Feeds are the RSS/Atom feed URLs this project pulls, all allowlisted hosts.",
			"pipelineView.filters":   "Filters are the keyword/region/source narrowings applied to the merged feed.",
			"pipelineView.org":       "Org and Project are the tenant scope the pipeline belongs to, echoed from\nthe validated principal — never from the request.",
			"scopeRef.project":       "Project, when given, must equal the caller's authenticated project scope;\na mismatch is refused. Omit it to read the authenticated project.",
		},
		Response: json.RawMessage(`{"org":"acme","project":"default","feeds":["https://feeds.bbci.co.uk/news/world/rss.xml"],"filters":{"regions":[],"keywords":[],"sources":[]},"default":true}`),
	})
	zip.Describe("PUT /v1/world/pipeline", zip.Doc{
		Description: "putPipeline replaces the feed + filter configuration for the caller's (org,\nproject). Feed hosts are checked against the allowlist at this write boundary,\nso a stored pipeline can never carry an unfetchable or hostile feed. The\nstored pipeline is returned.",
		Fields: map[string]string{
			"pipelineReq.feeds":      "Feeds are the RSS/Atom feed URLs to pull. Each must be an http(s) URL whose\nhost is on the server's allowlist; max 64. An empty list clears the feeds.",
			"pipelineReq.filters":    "Filters narrow the merged feed. Each axis is AND'd, terms within an axis are\nOR'd, matching is case-insensitive substring; max 64 terms per axis.",
			"pipelineView.createdAt": "CreatedAt and UpdatedAt are RFC3339 UTC, absent on a default pipeline.",
			"pipelineView.default":   "Default is true when no pipeline is stored and these are the built-in feeds.",
			"pipelineView.feeds":     "Feeds are the RSS/Atom feed URLs this project pulls, all allowlisted hosts.",
			"pipelineView.filters":   "Filters are the keyword/region/source narrowings applied to the merged feed.",
			"pipelineView.org":       "Org and Project are the tenant scope the pipeline belongs to, echoed from\nthe validated principal — never from the request.",
		},
		Example: json.RawMessage(`{"feeds":["https://feeds.bbci.co.uk/news/world/rss.xml"],"filters":{"keywords":["conflict"]}}`),
	})
}
