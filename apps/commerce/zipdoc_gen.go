// Code generated by zipdoc; DO NOT EDIT.

package commerce

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /_/commerce/healthz", zip.Doc{
		Description: "health reports that the commerce surface is mounted and answering. It is\nregistered before the embed boots and touches no store, so a probe still gets\nan answer while the money plane is degraded — read /v1/health for that verdict.",
		Fields: map[string]string{
			"healthView.service": "Service names which subsystem answered, so a shadowing route is visible.",
			"healthView.status":  "Status is \"ok\" whenever the route answers; it is registered before the embed\nboots, so it stays true while the money plane is still coming up.",
		},
		Response: json.RawMessage(`{"status":"ok","service":"commerce"}`),
	})
}
