// Code generated by zipdoc; DO NOT EDIT.

package research

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/research GET /v1/research/artifacts", zip.Doc{
		Description: "Returns the caller org's research-diary feed newest-first —\nthe snapshots and reports tied to its runs, as metadata and content addresses;\nthe bytes themselves are fetched by hash. ?run= narrows to one run, ?project=\nto one project (default the caller's project scope), and ?since= to a unix second.",
		Fields: map[string]string{
			"ResearchArtifact.content":         "Content is the artifact's bytes, base64, on WRITE only, at most 16 MiB.\nNever returned: a read answers metadata and the address, and the bytes come\nfrom GET /v1/research/artifacts/{sha256}.",
			"ResearchArtifact.git_branch":      "GitBranch is the branch that commit was on.",
			"ResearchArtifact.git_dirty":       "GitDirty says the working tree carried uncommitted changes.",
			"ResearchArtifact.git_sha":         "GitSHA is the commit that produced the artifact. Unlike a run's, an\nartifact's provenance is NOT part of its identity: the bytes are.",
			"ResearchArtifact.kind":            "Kind is snapshot or report, and nothing else is accepted. It decides how\nthe bytes are served: image/png for a snapshot, application/octet-stream\nfor a report — from the recorded kind, never from sniffing the bytes.",
			"ResearchArtifact.lib_versions":    "LibVersions is the libraries in force, as a JSON object of name → version.\nAbsent is {}.",
			"ResearchArtifact.project":         "Project is the project the artifact is filed under — the SERVER's value on\nwrite. It is part of the read predicate too: fetching a hash from the wrong\nproject is the same 404 as a hash that was never recorded.",
			"ResearchArtifact.ref":             "Ref is the content address, \"sha256:<hash>\", composed by the server from\nthe same bytes. A ref sent in the body is discarded, so it can never name\nsomewhere else.",
			"ResearchArtifact.retention_class": "RetentionClass is the retention bucket the bytes fall in. Every write is\nrecorded as raw-artifact — the class for material that may be personal or\nlicensed — so it is the server's word, not the caller's.",
			"ResearchArtifact.run_id":          "RunID is the experiment stable id this artifact belongs to, which is what\nthe diary feed's ?run= filters on. It is free text: nothing checks that the\nrun exists.",
			"ResearchArtifact.sha256":          "SHA256 is the SERVER's hash of the submitted bytes, hex, and the artifact's\nidentity — the key the blob route is addressed by. A caller MAY send one;\nit is not trusted, only checked, and a mismatch with the bytes refuses the\nwrite.",
			"ResearchArtifact.ts":              "TS is when the artifact was recorded, unix SECONDS, as the caller gives it\n— it is what the feed sorts newest-first on and what ?since= is compared\nagainst. 0 leaves the run unordered against timestamped siblings.",
			"ResearchArtifact.visibility":      "Visibility is private, org or public. A write forces private; only a grant\nby sha256 moves it.",
			"artifactsIn.project":              "Project narrows to one project. Empty takes the caller's project scope.",
			"artifactsIn.run":                  "Run narrows to one run's artifacts by its stable id.",
			"artifactsIn.since":                "Since bounds the feed to artifacts recorded at or after this unix second.",
			"artifactsOut.data":                "Data are the artifacts, newest first. Content bytes are never returned here.",
			"artifactsOut.total":               "Total is len(data).",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/research GET /v1/research/artifacts/:sha256", zip.Doc{
		Description: "Serves one artifact's stored bytes, hash-addressed by :sha256 and\norg-scoped — the retrieval side of hash-addressing (the board fetches a snapshot by\nits content hash).\n\nIt is the ONE route on this surface that is not a typed op, and it cannot be one:\nit answers the artifact's RAW BYTES under the artifact's own Content-Type\n(image/png for a snapshot, application/octet-stream otherwise). A typed op\nserialises a Go value as application/json and has no vocabulary for a binary body,\nso typing this would change what every caller receives — a wire break, not a\ndescription. Its errors therefore stay in-band, as the rest of this file's did.",
	})
	zip.Describe("github.com/hanzoai/cloud/apps/research GET /v1/research/experiments", zip.Doc{
		Description: "Returns the caller org's CANONICAL experiments — the deterministic\ndeduped view over the versioned history. With no ?project= it reads the org's\nwhole set across projects (the ops board's cross-project view, since a project is\na sub-scope of the one tenant); ?project= narrows to one and ?kind= to one\ndiscriminator.",
		Fields: map[string]string{
			"Experiment.canonical":    "Canonical is DERIVED on read, never stored and never taken from an upload:\na version is canonical when it is the latest-appended non-retracted version\nof its stable id. The listing returns only canonical versions, so it is\ntrue on every row it answers with.",
			"Experiment.cost_usd":     "CostUSD is what the run cost in US DOLLARS (not cents), as the producer\nreports it. It is summed as-is into the per-project and per-kind totals.",
			"Experiment.endpoint":     "Endpoint is the optional BYO URL naming the arm the run measured. It is\nchecked at ingest — https only, and every address the host resolves to must\nbe publicly routable — and then dropped: nothing stores it, no read returns\nit, and this plane never dials it.",
			"Experiment.git_branch":   "GitBranch is the branch that commit was on.",
			"Experiment.git_dirty":    "GitDirty says the working tree carried uncommitted changes, so GitSHA does\nnot fully describe what ran.",
			"Experiment.git_sha":      "GitSHA is the commit the run was measured at. Provenance is part of the\nversion's content identity, so the same number measured on a different\ncommit is a new RETAINED version rather than an idempotent no-op.",
			"Experiment.id":           "ID is the run's stable id, `<kind>:<subject>:<task>` by convention. It is\nthe key versions accrue under: a correction re-sent with this same id\nappends a new version beside the old one rather than replacing it.\nRequired on ingest.",
			"Experiment.kind":         "Kind is the discriminator the whole plane is partitioned by: benchmark,\nkernel-perf, training, ablation, policy-eval, or ab for an A/B trial's\nper-arm evidence. Required on ingest, and open — an unknown kind is stored,\nnot refused.",
			"Experiment.lib_versions": "LibVersions is the libraries in force, as a JSON object of name → version\n({\"harness\":\"0.1.0\"}). It is a queryable column and part of the content\nidentity, which is what makes \"which lib version regressed this task\"\nanswerable over the longitudinal record. Absent is {}.",
			"Experiment.meta":         "Meta is whatever else the producer wants to carry, as a JSON object — the\nA/B rows put lift, z and p here. It is part of the version's content\nidentity, canonicalized (sorted keys, no whitespace) first so that a\nre-serialization of the same object does not mint a spurious version.\nAbsent or unparseable is stored as {}.",
			"Experiment.metric":       "Metric names the quantity Value carries — accuracy, tok/s, a conversion\nevent. It is the only place Value's unit is stated; this plane declares no\nvocabulary of metrics.",
			"Experiment.n":            "N is the sample size behind Value — items scored, or exposures for an A/B\narm.",
			"Experiment.n_total":      "NTotal is the denominator the producer reports N against — the size of the\nset N was drawn from. The plane derives neither from the other, so a\nproducer that reports only N leaves this 0.",
			"Experiment.project":      "Project is the sub-scope inside the org this run is filed under. On ingest\nit is the SERVER's value — the caller's project scope — and a project sent\nin the body is ignored; on a read it is the value the run was filed under.",
			"Experiment.publishable":  "Publishable is consent to publish this run to the commons. Ingest forces\nfalse; only a grant sets it.",
			"Experiment.revision":     "Revision is the producer's declared kind of this version: original,\ncorrected or retracted. Anything else, including empty, is read as\noriginal. A retracted latest version WITHDRAWS the id — it has no canonical\nversion at all — and every earlier version is still retained.",
			"Experiment.status":       "Status is the run's execution state: planning, running, complete or\nfaulted. Empty is complete (a backfilled historic run). An unrecognized\nvalue is kept verbatim rather than flattened, so a producer's own failure\nlabel is never displayed as a success. faulted and failed runs are retained\nbut are left out of the answered headline counts.",
			"Experiment.subject":      "Subject is what was measured: the model, the engine, the A/B arm. Required\non ingest.",
			"Experiment.task":         "Task is what it was measured ON — gpqa_diamond, livecodebench, prefill, or\nthe trial's id for A/B evidence.",
			"Experiment.trainable":    "Trainable is consent to train on this run. Ingest forces false; only a\ngrant sets it. An upload conveys no training right.",
			"Experiment.ts":           "TS is when the run was measured, unix SECONDS. It is display-only and it\ndoes NOT decide supersession — a server-assigned append clock does, so a\nlive correction that sends no ts still supersedes a backfill that stamped\none. 0 means the producer sent none.",
			"Experiment.value":        "Value is the measurement, in whatever unit Metric names. It is NEVER\nnormalized: one producer files accuracy as 0.81 and another as 94.3 and\nboth are stored as sent, so compare values only within one (subject, task,\nmetric).",
			"Experiment.visibility":   "Visibility is private, org or public. Ingest forces private whatever the\nbody says; only a grant moves it.",
			"experimentsOut.data":     "Data are the canonical experiment versions.",
			"experimentsOut.total":    "Total is len(data) — the rows in this answer, not the store's history.",
			"listIn.kind":             "Kind narrows to one discriminator: benchmark, kernel-perf, training, ablation or policy-eval.",
			"listIn.project":          "Project narrows to one project. Empty reads the org's whole set across projects.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/research GET /v1/research/projects", zip.Doc{
		Description: "Returns every research project in the caller's org with its\nreal totals — canonical and retained side by side — which is the ops board's\n\"every project + real totals\" view.",
		Fields: map[string]string{
			"ProjectSummary.attempts":             "Attempts counts the CANONICAL answered attempts, one per (benchmark, item,\nmodel).",
			"ProjectSummary.attempts_retained":    "AttemptsRetained counts every attempt version held, on the same terms as\nExperimentsRetained.",
			"ProjectSummary.benchmarks":           "Benchmarks is how many DISTINCT benchmarks those same attempts cover.",
			"ProjectSummary.cost_usd":             "CostUSD is US DOLLARS (not cents) summed over the canonical answered runs,\nso it is what the deduped view cost and not what the history cost.",
			"ProjectSummary.experiments":          "Experiments counts the CANONICAL answered runs: one per stable id, the\nlatest non-retracted version, faulted and failed left out.",
			"ProjectSummary.experiments_retained": "ExperimentsRetained counts every run version the store holds for the\nproject — superseded and retracted included. Retained is always ≥\ncanonical, and the gap is history rather than loss.",
			"ProjectSummary.kinds":                "Kinds are the discriminators seen in the project, sorted, over ALL retained\nversions — so a kind stays listed after its runs are superseded or\nretracted, even when it contributes nothing to Experiments.",
			"ProjectSummary.models":               "Models is how many DISTINCT models appear across the canonical answered\nattempts.",
			"ProjectSummary.project":              "Project is the project these totals cover. Rows are one per project the org\nhas evidence under, sorted by this.",
			"projectsOut.data":                    "Data are the org's projects with canonical + retained counts.",
			"projectsOut.total":                   "Total is len(data).",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/research GET /v1/research/totals", zip.Doc{
		Description: "Returns the caller org's headline aggregate plus a per-kind\nbreakdown — the observatory's poll target. Canonical and retained counts travel\ntogether, so a deduped view never reads as loss. ?project= narrows to one project.",
		Fields: map[string]string{
			"KindTotal.cost_usd":                  "CostUSD is US DOLLARS (not cents) summed over those same runs.",
			"KindTotal.experiments":               "Experiments counts the canonical answered runs of this kind — the same\npredicate as the headline count, sliced.",
			"KindTotal.kind":                      "Kind is the discriminator this row aggregates: benchmark, kernel-perf,\ntraining, ablation, policy-eval, ab. A kind appears as soon as any RETAINED\nversion carries it, so a kind whose every run was retracted or faulted is\nlisted with zero experiments rather than dropped.",
			"ResearchTotals.attempts":             "Attempts counts the CANONICAL answered attempts, one per (benchmark, item,\nmodel).",
			"ResearchTotals.attempts_retained":    "AttemptsRetained counts every attempt version held, on the same terms as\nExperimentsRetained.",
			"ResearchTotals.benchmarks":           "Benchmarks is how many DISTINCT benchmarks those same attempts cover.",
			"ResearchTotals.by_kind":              "ByKind is the same headline split per discriminator, ordered by kind.",
			"ResearchTotals.cost_usd":             "CostUSD is US DOLLARS (not cents) summed over the canonical answered runs.",
			"ResearchTotals.experiments":          "Experiments counts the CANONICAL answered runs: one per stable id, the\nlatest non-retracted version, faulted and failed left out.",
			"ResearchTotals.experiments_retained": "ExperimentsRetained counts every run version held — superseded and\nretracted included. Retained is always ≥ canonical, and the gap is history\nrather than loss, which is why both travel together.",
			"ResearchTotals.models":               "Models is how many DISTINCT models appear across the canonical answered\nattempts.",
			"ResearchTotals.project":              "Project echoes the project the aggregate was narrowed to. Absent means the\nfigures below span the whole org.",
			"ResearchTotals.projects":             "Projects is how many DISTINCT projects the counted evidence spans, so it is\n1 whenever Project narrows to one.",
			"totalsIn.project":                    "Project narrows the aggregate to one project. Empty aggregates the whole org.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/research POST /v1/research/artifacts", zip.Doc{
		Description: "Records one research-diary artifact — a board snapshot or a\ngenerated report — CONTENT-ADDRESSED inside the trust boundary. The caller submits\nthe bytes as base64 `content`; the SERVER hashes them and THAT hash is the identity\nand the ref, so the address can never be poisoned by a client-asserted one. A\nclient-supplied sha256, if present, must match the bytes. The project is the\nSERVER's value and visibility is forced private. Re-posting the same bytes is a\nno-op that reports created=false.",
		Fields: map[string]string{
			"ResearchArtifact.content":         "Content is the artifact's bytes, base64, on WRITE only, at most 16 MiB.\nNever returned: a read answers metadata and the address, and the bytes come\nfrom GET /v1/research/artifacts/{sha256}.",
			"ResearchArtifact.git_branch":      "GitBranch is the branch that commit was on.",
			"ResearchArtifact.git_dirty":       "GitDirty says the working tree carried uncommitted changes.",
			"ResearchArtifact.git_sha":         "GitSHA is the commit that produced the artifact. Unlike a run's, an\nartifact's provenance is NOT part of its identity: the bytes are.",
			"ResearchArtifact.kind":            "Kind is snapshot or report, and nothing else is accepted. It decides how\nthe bytes are served: image/png for a snapshot, application/octet-stream\nfor a report — from the recorded kind, never from sniffing the bytes.",
			"ResearchArtifact.lib_versions":    "LibVersions is the libraries in force, as a JSON object of name → version.\nAbsent is {}.",
			"ResearchArtifact.project":         "Project is the project the artifact is filed under — the SERVER's value on\nwrite. It is part of the read predicate too: fetching a hash from the wrong\nproject is the same 404 as a hash that was never recorded.",
			"ResearchArtifact.ref":             "Ref is the content address, \"sha256:<hash>\", composed by the server from\nthe same bytes. A ref sent in the body is discarded, so it can never name\nsomewhere else.",
			"ResearchArtifact.retention_class": "RetentionClass is the retention bucket the bytes fall in. Every write is\nrecorded as raw-artifact — the class for material that may be personal or\nlicensed — so it is the server's word, not the caller's.",
			"ResearchArtifact.run_id":          "RunID is the experiment stable id this artifact belongs to, which is what\nthe diary feed's ?run= filters on. It is free text: nothing checks that the\nrun exists.",
			"ResearchArtifact.sha256":          "SHA256 is the SERVER's hash of the submitted bytes, hex, and the artifact's\nidentity — the key the blob route is addressed by. A caller MAY send one;\nit is not trusted, only checked, and a mismatch with the bytes refuses the\nwrite.",
			"ResearchArtifact.ts":              "TS is when the artifact was recorded, unix SECONDS, as the caller gives it\n— it is what the feed sorts newest-first on and what ?since= is compared\nagainst. 0 leaves the run unordered against timestamped siblings.",
			"ResearchArtifact.visibility":      "Visibility is private, org or public. A write forces private; only a grant\nby sha256 moves it.",
			"artifactOut.created":              "Created is false when these exact bytes were already recorded — the write is a no-op.",
			"artifactOut.ref":                  "Ref is the content address, \"sha256:<hash>\".",
			"artifactOut.rolled_up":            "RolledUp is false when the OLAP roll-up was skipped; the SQLite write still stands.",
			"artifactOut.sha256":               "SHA256 is the SERVER's hash of the bytes — the artifact's identity.",
		},
		Example: json.RawMessage(`{"kind":"snapshot","content":"iVBORw0KGgo=","run_id":"benchmark:zen-1:mmlu"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/research POST /v1/research/experiments", zip.Doc{
		Description: "Appends one batch of experiment and attempt versions to the\ncaller org's evidence store, idempotently by content, then rolls it up to the\nanalytics plane best-effort. The project is the SERVER's value and visibility is\nforced private — an upload grants no training or publication right, which is a\nseparate call. A run carrying a BYO endpoint is SSRF-checked before the store is\ntouched. The answer carries BOTH the canonical (deduped) and retained (full\nhistory) counts, so a caller sees the versioned truth rather than a dedup that\nreads as loss.",
		Fields: map[string]string{
			"Attempt.answer":                  "Answer is what the model answered, after the harness extracted it from the\nraw generation.",
			"Attempt.benchmark":               "Benchmark is the suite the item belongs to — gpqa_diamond, livecodebench.\nPart of the stable id (project, benchmark, item, model). Required on ingest.",
			"Attempt.correct":                 "Correct is the producer's grade of Answer against Gold. This plane records\nthe verdict and re-grades nothing, so it is only ever as good as the\nharness that sent it.",
			"Attempt.gold":                    "Gold is the expected answer for the item, as the benchmark states it.",
			"Attempt.item":                    "Item is the one question or case that was scored, by the benchmark's own\nid for it. Part of the stable id. Required on ingest.",
			"Attempt.model":                   "Model is what answered the item. Part of the stable id. Required on ingest.",
			"Attempt.response":                "Response is the raw generation the model produced. It is its own retention\nclass — it may carry personal or licensed material — and it is part of the\nversion's content identity, so a re-run that produced different text is a\nnew retained version.",
			"Attempt.revision":                "Revision is the producer's declared kind of this version: original,\ncorrected or retracted. Anything else, including empty, is read as\noriginal. A retracted latest version withdraws the item's canonical answer;\nevery earlier version stays retained.",
			"Attempt.source":                  "Source names who measured this attempt. Empty is recorded as\nhanzo-measured; any other value is stored as sent, so an imported\nthird-party score is distinguishable from one this fleet ran.",
			"Attempt.status":                  "Status is this attempt's execution state, the same vocabulary a run uses:\nempty is complete, faulted retains a negative result, and an unrecognized\nvalue is kept verbatim. faulted and failed are excluded from the answered\ncounts but never dropped.",
			"Attempt.ts":                      "TS is when the attempt was measured, unix SECONDS. Display-only: a\nserver-assigned append clock, not this, decides which version is canonical.\n0 means the producer sent none.",
			"Experiment.canonical":            "Canonical is DERIVED on read, never stored and never taken from an upload:\na version is canonical when it is the latest-appended non-retracted version\nof its stable id. The listing returns only canonical versions, so it is\ntrue on every row it answers with.",
			"Experiment.cost_usd":             "CostUSD is what the run cost in US DOLLARS (not cents), as the producer\nreports it. It is summed as-is into the per-project and per-kind totals.",
			"Experiment.endpoint":             "Endpoint is the optional BYO URL naming the arm the run measured. It is\nchecked at ingest — https only, and every address the host resolves to must\nbe publicly routable — and then dropped: nothing stores it, no read returns\nit, and this plane never dials it.",
			"Experiment.git_branch":           "GitBranch is the branch that commit was on.",
			"Experiment.git_dirty":            "GitDirty says the working tree carried uncommitted changes, so GitSHA does\nnot fully describe what ran.",
			"Experiment.git_sha":              "GitSHA is the commit the run was measured at. Provenance is part of the\nversion's content identity, so the same number measured on a different\ncommit is a new RETAINED version rather than an idempotent no-op.",
			"Experiment.id":                   "ID is the run's stable id, `<kind>:<subject>:<task>` by convention. It is\nthe key versions accrue under: a correction re-sent with this same id\nappends a new version beside the old one rather than replacing it.\nRequired on ingest.",
			"Experiment.kind":                 "Kind is the discriminator the whole plane is partitioned by: benchmark,\nkernel-perf, training, ablation, policy-eval, or ab for an A/B trial's\nper-arm evidence. Required on ingest, and open — an unknown kind is stored,\nnot refused.",
			"Experiment.lib_versions":         "LibVersions is the libraries in force, as a JSON object of name → version\n({\"harness\":\"0.1.0\"}). It is a queryable column and part of the content\nidentity, which is what makes \"which lib version regressed this task\"\nanswerable over the longitudinal record. Absent is {}.",
			"Experiment.meta":                 "Meta is whatever else the producer wants to carry, as a JSON object — the\nA/B rows put lift, z and p here. It is part of the version's content\nidentity, canonicalized (sorted keys, no whitespace) first so that a\nre-serialization of the same object does not mint a spurious version.\nAbsent or unparseable is stored as {}.",
			"Experiment.metric":               "Metric names the quantity Value carries — accuracy, tok/s, a conversion\nevent. It is the only place Value's unit is stated; this plane declares no\nvocabulary of metrics.",
			"Experiment.n":                    "N is the sample size behind Value — items scored, or exposures for an A/B\narm.",
			"Experiment.n_total":              "NTotal is the denominator the producer reports N against — the size of the\nset N was drawn from. The plane derives neither from the other, so a\nproducer that reports only N leaves this 0.",
			"Experiment.project":              "Project is the sub-scope inside the org this run is filed under. On ingest\nit is the SERVER's value — the caller's project scope — and a project sent\nin the body is ignored; on a read it is the value the run was filed under.",
			"Experiment.publishable":          "Publishable is consent to publish this run to the commons. Ingest forces\nfalse; only a grant sets it.",
			"Experiment.revision":             "Revision is the producer's declared kind of this version: original,\ncorrected or retracted. Anything else, including empty, is read as\noriginal. A retracted latest version WITHDRAWS the id — it has no canonical\nversion at all — and every earlier version is still retained.",
			"Experiment.status":               "Status is the run's execution state: planning, running, complete or\nfaulted. Empty is complete (a backfilled historic run). An unrecognized\nvalue is kept verbatim rather than flattened, so a producer's own failure\nlabel is never displayed as a success. faulted and failed runs are retained\nbut are left out of the answered headline counts.",
			"Experiment.subject":              "Subject is what was measured: the model, the engine, the A/B arm. Required\non ingest.",
			"Experiment.task":                 "Task is what it was measured ON — gpqa_diamond, livecodebench, prefill, or\nthe trial's id for A/B evidence.",
			"Experiment.trainable":            "Trainable is consent to train on this run. Ingest forces false; only a\ngrant sets it. An upload conveys no training right.",
			"Experiment.ts":                   "TS is when the run was measured, unix SECONDS. It is display-only and it\ndoes NOT decide supersession — a server-assigned append clock does, so a\nlive correction that sends no ts still supersedes a backfill that stamped\none. 0 means the producer sent none.",
			"Experiment.value":                "Value is the measurement, in whatever unit Metric names. It is NEVER\nnormalized: one producer files accuracy as 0.81 and another as 94.3 and\nboth are stored as sent, so compare values only within one (subject, task,\nmetric).",
			"Experiment.visibility":           "Visibility is private, org or public. Ingest forces private whatever the\nbody says; only a grant moves it.",
			"IngestRequest.attempts":          "Attempts are the per-item attempt versions. Each needs a benchmark, an item\nand a model. The two arrays may not BOTH be empty, and they hold at most\n20000 members between them — split a larger upload.",
			"IngestRequest.experiments":       "Experiments are the run versions in this batch. Each needs an id, a kind\nand a subject. Re-sending byte-identical content appends nothing.",
			"ingestOut.attempts_ingested":     "AttemptsIngested is how many attempt versions this call appended.",
			"ingestOut.attempts_retained":     "AttemptsRetained is the full versioned attempt history the store now holds.",
			"ingestOut.canonical_attempts":    "CanonicalAttempts is the deduped attempt count the store now holds.",
			"ingestOut.canonical_experiments": "CanonicalExperiments is the deduped experiment count the store now holds.",
			"ingestOut.experiments_ingested":  "ExperimentsIngested is how many experiment versions this call appended.",
			"ingestOut.experiments_retained":  "ExperimentsRetained is the full versioned experiment history the store now holds.",
			"ingestOut.project":               "Project is the project the batch was filed under — the SERVER's value, never the body's.",
			"ingestOut.rolled_up":             "RolledUp is false when the OLAP roll-up was skipped; the SQLite write still stands.",
		},
		Example: json.RawMessage(`{"experiments":[{"id":"benchmark:zen-1:mmlu","kind":"benchmark","subject":"zen-1","metric":"accuracy","value":0.81,"ts":1750000000}],"attempts":[]}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/research POST /v1/research/grants", zip.Doc{
		Description: "Records the SEPARATE authorization an upload never\nimplies: a record's visibility (private, org or public) and, for a run, its\ntraining and commons-publication consent. Address a run by its stable id or an\nartifact by its sha256; an artifact grant sets visibility only. The ORG is the\ntenant boundary and comes from the validated principal, so a caller can only ever\ngrant within its own org; `project` locates WHICH record inside it and defaults to\nthe caller's project scope.",
		Fields: map[string]string{
			"GrantRequest.id":          "ID addresses an experiment (run) by its stable id. The grant lands on EVERY\nretained version of that id, because the decision is about the run and not\nabout one version of it.",
			"GrantRequest.project":     "Project locates WHICH project inside the caller's org holds the target;\nempty is the caller's own project scope. It is not a tenant key — the org\ncomes from the validated principal — so naming a project can only ever\naddress a record the caller's org already owns.",
			"GrantRequest.publishable": "Publishable is consent to publish the run to the commons. Null leaves it\nunchanged; an artifact grant ignores it.",
			"GrantRequest.sha256":      "SHA256 addresses an artifact by its content hash instead, and an artifact\ngrant sets visibility only. One of ID and SHA256 is required; when both are\nsent SHA256 wins and the run is untouched.",
			"GrantRequest.trainable":   "Trainable is consent to train on the run's records. Null leaves it\nunchanged; an artifact grant ignores it.",
			"GrantRequest.visibility":  "Visibility is private, org or public. Null leaves it unchanged, and it is\nthe one field an artifact grant requires.",
			"grantOut.updated":         "Updated is the number of records the grant applied to. Zero is a 404, never a silent no-op.",
		},
		Example: json.RawMessage(`{"id":"benchmark:zen-1:mmlu","visibility":"public","trainable":true}`),
	})
}
