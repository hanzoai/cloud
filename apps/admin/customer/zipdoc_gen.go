// Code generated by zipdoc; DO NOT EDIT.

package customer

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/admin/customer GET /v1/admin/customers", zip.Doc{
		Description: "Lists every customer org at a glance, sorted by slug: owner email, plan,\nsuspend status, member count, balance, month-to-date spend and MRR.\n\nEach row costs one IAM read plus the org's money reads, fanned out under a fixed\nconcurrency ceiling so a large fleet cannot stampede the upstreams. Every read is\nbest-effort per row: an upstream miss degrades THAT field to its honest zero rather\nthan failing the fleet.",
		Fields: map[string]string{
			"CustomerRow.balanceCents": "BalanceCents is the prepaid wallet the org still holds, in USD cents. What is left\nto spend, not what was granted.",
			"CustomerRow.created":      "Created is when IAM created the org, RFC3339 — the signup date.",
			"CustomerRow.display":      "Display is the org's display name, falling back to the slug when it has none. For\nhumans; never key on it.",
			"CustomerRow.lastActive":   "LastActive is the most recent sign-in across every member, RFC3339. The best\nactivity signal IAM carries, so it tracks people logging in, not API traffic. Empty\nwhen no member has ever signed in.",
			"CustomerRow.mrrCents":     "MRRCents is the org's monthly recurring revenue, in USD cents: commerce's own\nper-subscription figure, already interval-normalized and multiplied by seats, summed\nover the subscriptions commerce counts as revenue. A trial contributes zero.",
			"CustomerRow.org":          "Org is the tenant slug — the row's identity, and the value every per-customer route\ntakes in its path. The list is sorted by it.",
			"CustomerRow.ownerEmail":   "OwnerEmail is the org admin's email, or the first member with one. Empty when no\nmember carries an email at all — IAM does not require one.",
			"CustomerRow.plan":         "Plan is the name of the org's live subscription tier, or \"pay-as-you-go\" for a\nmetered customer with no subscription. A trial names its plan here even though it\ncontributes no MRR.",
			"CustomerRow.spendCents":   "SpendCents is metered consumption over the TRAILING 30 DAYS, in USD cents — the one\nwindow every fleet money surface means by \"spend\". Positive, and it does not reset\non the first of the month.",
			"CustomerRow.status":       "Status is `active` or `suspended`, derived from the members' IAM isForbidden flags.\nSuspended requires the org to have members and EVERY one of them to be forbidden —\na partly-forbidden org still reads active, because some of it can still sign in.",
			"CustomerRow.users":        "Users is how many members IAM lists for the org, up to the 200-member page this\nread takes. Every other IAM-derived field on the row is folded from that same page.",
			"CustomersOut.data":        "Data is every customer org, sorted by slug.",
			"CustomersOut.msg":         "Msg is the failure, and is empty on success.",
			"CustomersOut.status":      "Status is \"ok\" or \"error\". An error here means the IAM org directory could not be\nread; a per-org money or plan failure does not fail the fleet, it degrades that\nrow's field to its zero.",
			"CustomersOut.total":       "Total is len(data). The list is unpaginated, so it is a convenience, never a\ncount of rows withheld. Omitted on an error.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"org":"acme","display":"Acme","ownerEmail":"ada@acme.com","plan":"pro","status":"active","users":7,"balanceCents":5000,"spendCents":12500,"mrrCents":9900,"created":"2026-01-04T00:00:00Z","lastActive":"2026-07-26T18:00:00Z"}],"total":1}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin/customer GET /v1/admin/customers/:org", zip.Doc{
		Description: "Answers GET /v1/admin/customers/:org.",
		Fields: map[string]string{
			"CustomerDetailData.apiKeys":      "APIKeys is how many members hold an access key — the org's programmatic reach,\ncounted from users[] and never the keys themselves.",
			"CustomerDetailData.balanceCents": "BalanceCents is the prepaid wallet still held, in USD cents.",
			"CustomerDetailData.created":      "Created is when IAM created the org, RFC3339.",
			"CustomerDetailData.display":      "Display is the org's display name, falling back to the slug when it has none.",
			"CustomerDetailData.mrrCents":     "MRRCents is monthly recurring revenue, in USD cents, over the subscriptions\ncommerce counts as revenue. A trial contributes zero while still naming Plan.",
			"CustomerDetailData.org":          "Org is the tenant slug, echoed from the path.",
			"CustomerDetailData.ownerEmail":   "OwnerEmail is the org admin's email, or the first member with one. Empty when no\nmember carries an email.",
			"CustomerDetailData.plan":         "Plan is the live subscription tier's name, or \"pay-as-you-go\" with no subscription.",
			"CustomerDetailData.spendCents":   "SpendCents is metered consumption over the trailing 30 days, in USD cents.",
			"CustomerDetailData.status":       "Status is `active` or `suspended` — suspended only when the org has members and\nevery one of them is forbidden. The per-member truth is in users[].forbidden.",
			"CustomerDetailData.transactions": "Transactions is the org's ledger, newest first, capped at the last 50 entries. A\nsample of the history, not the whole of it, so the balance above cannot be\nrecomputed by folding this.",
			"CustomerDetailData.users":        "Users is the org's members, up to a 200-member page. Suspend and reactivate act on\nexactly this set.",
			"CustomerDetailOut.data":          "Data is the customer. Null exactly when Status is \"error\".",
			"CustomerDetailOut.msg":           "Msg is the failure, and is empty on success.",
			"CustomerDetailOut.status":        "Status is \"ok\" or \"error\". An unknown org is an error and also carries HTTP 404 —\nthe status code is the addition, not a different contract.",
			"CustomerTxn.cents":               "Cents is the entry's magnitude in minor units, and it is UNSIGNED — a withdraw\ndoes not arrive negative, so a running balance has to be folded by reading Type.",
			"CustomerTxn.currency":            "Currency is the entry's ISO code, lower-cased. \"usd\" throughout the fleet today.",
			"CustomerTxn.id":                  "ID is the commerce ledger entry id — what reconciliation against commerce joins on.",
			"CustomerTxn.notes":               "Notes is the free-text memo the entry was written with — for a staff grant, the\nreason an operator typed. Omitted when blank.",
			"CustomerTxn.time":                "Time is when the entry was recorded, RFC3339. The history is newest first.",
			"CustomerTxn.type":                "Type is `deposit` (money in: a top-up or a staff grant) or `withdraw` (money out:\nmetered usage). It is the only thing that gives Cents its direction.",
			"CustomerUser.created":            "Created is when IAM created the member, RFC3339.",
			"CustomerUser.email":              "Email is the member's address, empty when IAM holds none.",
			"CustomerUser.forbidden":          "Forbidden is IAM's isForbidden. True means IAM refuses this user at login AND at\ntoken issuance, so they can neither sign in nor mint a fresh one. It is what\nsuspend sets and reactivate clears, per user — which is why an org can sit in a\nmixed state after a partial failure.",
			"CustomerUser.hasApiKey":          "HasAPIKey reports that the member holds an access key. PRESENCE only: the key\nitself is never read onto this surface.",
			"CustomerUser.isAdmin":            "IsAdmin is admin OF THIS ORG — the customer's own account owner, who manages their\nmembers and apps. It is NOT platform privilege: SuperAdmin is membership of the\nreserved `admin` org and never appears on a customer row.",
			"CustomerUser.lastSignin":         "LastSignin is the member's most recent sign-in, RFC3339. Empty means never signed\nin — which is different from signed in long ago.",
			"CustomerUser.name":               "Name is the IAM username, unique within the org. `<org>/<name>` is the id the\nsuspend and reactivate actions address the user by.",
			"OrgIn.org":                       "Org is the tenant slug from the path.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin/customer GET /v1/admin/grants", zip.Doc{
		Description: "Reads the credit-grant ledger across ALL orgs, newest first — who granted what\nto whom, when, and from which money bucket.\n\nIt is a PROJECTION of the tamper-evident audit trail, not a second store: every grant\nis written there as action \"admin.customer.credit\", so this view cannot drift from\nwhat actually happened, and FAILED grants appear too.\n\nA deployment with no local audit store has no history to project, and says so with an\nempty list and a msg rather than an error.",
		Fields: map[string]string{
			"GrantRow.actor":         "staff email (or sub) who issued it",
			"GrantRow.amountCents":   "AmountCents is the credit in USD cents, always positive. On a successful grant it\nis what was actually written to the ledger; on a REFUSED one it is what was asked\nfor and never moved, so summing this column without filtering on Result overstates\nwhat the fleet gave away.",
			"GrantRow.createdAt":     "CreatedAt is when the grant was attempted, RFC3339 in UTC. The list is newest first.",
			"GrantRow.currency":      "Currency is the ISO code the grant was denominated in, lower-cased. Defaults to\n\"usd\" for a row that recorded none.",
			"GrantRow.org":           "Org is the TARGET tenant the credit landed on — the audit row's resource id, not\nthe actor's org. The `org` query filter matches the actor's, which is the one\nasymmetry on this surface.",
			"GrantRow.reason":        "Reason is the justification the operator typed. Omitted when blank — older rows\npredate the field.",
			"GrantRow.result":        "success | error",
			"GrantRow.source":        "\"trial\" | \"prepaid\"",
			"GrantRow.transactionId": "TransactionID is the commerce ledger entry the money landed in, for reconciliation.\nOmitted on a refused grant, which wrote no entry.",
			"GrantsIn.limit":         "Limit caps the rows returned. Default 200.",
			"GrantsIn.org":           "Org filters by the ACTOR's org (the staff org that issued the grant), which is\nrarely what a reader wants — the target org is a row field, not a filter.",
			"GrantsIn.result":        "Result filters by outcome: \"success\" or \"error\". Empty returns both, which is\nthe point of this view — a refused grant is as interesting as a granted one.",
			"GrantsOut.data":         "Data is the matching grants, newest first, capped by the request's limit. Refused\ngrants are included — a refusal is as much a fact about who tried as a success is.",
			"GrantsOut.msg":          "Msg carries that \"no audit store\" note ALONGSIDE an ok status. Everywhere else on\nthis app a non-empty msg means failure; here it means the history is unavailable,\nwhich is why an empty list must not be read as \"no grants were ever made\".",
			"GrantsOut.status":       "Status is \"ok\" or \"error\". A deployment with no durable audit store has no history\nto project and still answers ok — with an empty list and a msg saying so.",
			"GrantsOut.total":        "Total is the store's count for the filter, which EXCEEDS len(data) when limit\ntruncated. Omitted on an error.",
		},
		Example:  json.RawMessage(`{"result":"success","limit":"50"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"org":"acme","amountCents":5000,"currency":"usd","source":"trial","reason":"launch comp","actor":"z@hanzo.ai","createdAt":"2026-07-26T18:00:00Z","transactionId":"tx_01J","result":"success"}],"total":1}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin/customer POST /v1/admin/customers/:org/credit", zip.Doc{
		Description: "Issues a staff credit grant to the org named in the path — a comp, refund\nor promo — through the ONE credit-write path core.ApplyGrant, which validates the\namount against the per-grant cap, checks the org exists, moves the money and records\nthe tamper-evident audit row.\n\nThe credit lands on the account account.Payer resolves, NOT necessarily the org: name\na member of a pooled org and the pool is credited. The receipt echoes the subject so\nthe caller can see which.",
		Fields: map[string]string{
			"GrantIn.amountCents":       "AmountCents is the credit, in whole cents. Must be positive and within the\nper-grant cap.",
			"GrantIn.currency":          "Currency is the ISO code, lower-cased. Empty means usd.",
			"GrantIn.org":               "Org is the tenant to credit. Required.",
			"GrantIn.reason":            "Reason is the operator's justification, recorded on the audit row.",
			"GrantIn.source":            "Source is the money bucket: \"trial\" (default) for a non-cash comp that is never\nrefundable, or \"prepaid\" for real money. Anything unknown falls back to trial.",
			"GrantIn.user":              "User optionally names a MEMBER to credit, by bare IAM username. Empty credits\nthe org. Which of the two the money actually lands on is decided by\naccount.Payer, not here: a pooled org keeps one balance whatever is named.",
			"GrantOut.data":             "Data is the receipt: the account credited, the amount, and the balance after.\nNull exactly when Status is \"error\".",
			"GrantOut.msg":              "Msg is why the grant was refused, and is empty on success. Two refusals also carry\na non-200 HTTP status alongside this envelope: 404 for an unknown org, 503 when\nthere is nowhere durable to record the grant.",
			"GrantOut.status":           "Status is \"ok\" or \"error\". Error means NO money moved: every refusal — a\nnon-positive amount, one over the per-grant cap, an unknown org, a deployment with\nno durable audit store — is decided before the ledger is written.",
			"GrantResult.balanceCents":  "BalanceCents is the account balance AFTER the grant, in whole cents.",
			"GrantResult.balanceExact":  "BalanceExact is that same balance at full 18-decimal precision, so a sub-cent\ndebit is visible rather than rounded away.",
			"GrantResult.currency":      "Currency is the lower-cased ISO code the grant was denominated in.",
			"GrantResult.grantedCents":  "GrantedCents is the amount actually credited.",
			"GrantResult.org":           "Org is the tenant whose ledger was credited.",
			"GrantResult.source":        "Source is the money bucket: \"trial\" (non-cash comp) or \"prepaid\" (real money).",
			"GrantResult.subject":       "Subject is the ACCOUNT the credit landed on inside that ledger: the org slug for\na pooled org, \"<org>/<name>\" for a member of a per-member one. It is echoed\nbecause the operator does not choose it — account.Payer does — so naming a\nmember of a pooled org credits the pool and the receipt has to say so.",
			"GrantResult.transactionId": "TransactionID is the ledger entry id, for reconciliation against commerce.",
		},
		Example:  json.RawMessage(`{"amountCents":5000,"currency":"usd","reason":"launch comp","source":"trial"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"org":"acme","subject":"acme","grantedCents":5000,"currency":"usd","source":"trial","balanceCents":10000,"balanceExact":"100.000000000000000000","transactionId":"tx_01J"}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin/customer POST /v1/admin/customers/:org/reactivate", zip.Doc{
		Description: "Restores access for every member of the org, undoing a suspend. It\nreports the same per-user breakdown.",
		Fields: map[string]string{
			"AccessChange.affected":  "Affected lists the usernames that were updated.",
			"AccessChange.failed":    "Failed lists the usernames that were NOT updated. Non-empty means the org is in\na mixed state and the action should be retried.",
			"AccessChange.org":       "Org is the tenant acted on.",
			"AccessChange.suspended": "Suspended is the state applied: true for suspend, false for reactivate.",
			"AccessOut.data":         "Data is the per-user breakdown of what changed. Null exactly when Status is\n\"error\", which means no user was touched.",
			"AccessOut.msg":          "Msg is why the action could not run at all: no org named, the org unknown (also\nHTTP 404), or IAM's member list unreadable. Empty on success.",
			"AccessOut.status":       "Status is \"ok\" or \"error\", and it answers whether the action RAN, not whether every\nuser was updated. A partial failure is an ok answer with a non-empty data.failed —\nread that before treating the org as fully suspended.",
			"OrgIn.org":              "Org is the tenant slug from the path.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"org":"acme","suspended":false,"affected":["ada","bob"],"failed":[]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin/customer POST /v1/admin/customers/:org/suspend", zip.Doc{
		Description: "Cuts off every member of the org: IAM refuses a forbidden user at\nlogin AND at token issuance, so a suspended customer can neither sign in nor mint a\nfresh token. Fully reversible with ReactivateCustomer.\n\nThe result names every user updated and every user that was NOT — a partial failure\nleaves the org in a mixed state and says so instead of reporting a clean success.",
		Fields: map[string]string{
			"AccessChange.affected":  "Affected lists the usernames that were updated.",
			"AccessChange.failed":    "Failed lists the usernames that were NOT updated. Non-empty means the org is in\na mixed state and the action should be retried.",
			"AccessChange.org":       "Org is the tenant acted on.",
			"AccessChange.suspended": "Suspended is the state applied: true for suspend, false for reactivate.",
			"AccessOut.data":         "Data is the per-user breakdown of what changed. Null exactly when Status is\n\"error\", which means no user was touched.",
			"AccessOut.msg":          "Msg is why the action could not run at all: no org named, the org unknown (also\nHTTP 404), or IAM's member list unreadable. Empty on success.",
			"AccessOut.status":       "Status is \"ok\" or \"error\", and it answers whether the action RAN, not whether every\nuser was updated. A partial failure is an ok answer with a non-empty data.failed —\nread that before treating the org as fully suspended.",
			"OrgIn.org":              "Org is the tenant slug from the path.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"org":"acme","suspended":true,"affected":["ada","bob"],"failed":[]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin/customer POST /v1/admin/grants", zip.Doc{
		Description: "Issues a credit grant to any org from the operator Grants view, with the\ntarget named in the body. It funnels through the SAME core.ApplyGrant that\nPOST /v1/admin/customers/:org/credit uses, so there is exactly ONE credit-write path\nand one audit trail behind both.",
		Fields: map[string]string{
			"GrantIn.amountCents":       "AmountCents is the credit, in whole cents. Must be positive and within the\nper-grant cap.",
			"GrantIn.currency":          "Currency is the ISO code, lower-cased. Empty means usd.",
			"GrantIn.org":               "Org is the tenant to credit. Required.",
			"GrantIn.reason":            "Reason is the operator's justification, recorded on the audit row.",
			"GrantIn.source":            "Source is the money bucket: \"trial\" (default) for a non-cash comp that is never\nrefundable, or \"prepaid\" for real money. Anything unknown falls back to trial.",
			"GrantIn.user":              "User optionally names a MEMBER to credit, by bare IAM username. Empty credits\nthe org. Which of the two the money actually lands on is decided by\naccount.Payer, not here: a pooled org keeps one balance whatever is named.",
			"GrantOut.data":             "Data is the receipt: the account credited, the amount, and the balance after.\nNull exactly when Status is \"error\".",
			"GrantOut.msg":              "Msg is why the grant was refused, and is empty on success. Two refusals also carry\na non-200 HTTP status alongside this envelope: 404 for an unknown org, 503 when\nthere is nowhere durable to record the grant.",
			"GrantOut.status":           "Status is \"ok\" or \"error\". Error means NO money moved: every refusal — a\nnon-positive amount, one over the per-grant cap, an unknown org, a deployment with\nno durable audit store — is decided before the ledger is written.",
			"GrantResult.balanceCents":  "BalanceCents is the account balance AFTER the grant, in whole cents.",
			"GrantResult.balanceExact":  "BalanceExact is that same balance at full 18-decimal precision, so a sub-cent\ndebit is visible rather than rounded away.",
			"GrantResult.currency":      "Currency is the lower-cased ISO code the grant was denominated in.",
			"GrantResult.grantedCents":  "GrantedCents is the amount actually credited.",
			"GrantResult.org":           "Org is the tenant whose ledger was credited.",
			"GrantResult.source":        "Source is the money bucket: \"trial\" (non-cash comp) or \"prepaid\" (real money).",
			"GrantResult.subject":       "Subject is the ACCOUNT the credit landed on inside that ledger: the org slug for\na pooled org, \"<org>/<name>\" for a member of a per-member one. It is echoed\nbecause the operator does not choose it — account.Payer does — so naming a\nmember of a pooled org credits the pool and the receipt has to say so.",
			"GrantResult.transactionId": "TransactionID is the ledger entry id, for reconciliation against commerce.",
		},
		Example:  json.RawMessage(`{"org":"acme","amountCents":5000,"currency":"usd","reason":"launch comp","source":"trial"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"org":"acme","subject":"acme","grantedCents":5000,"currency":"usd","source":"trial","balanceCents":10000,"balanceExact":"100.000000000000000000","transactionId":"tx_01J"}}`),
	})
}
