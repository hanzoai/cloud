// Code generated by zipdoc; DO NOT EDIT.

package infra

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/admin/infra/droplets/:id", zip.Doc{
		Description: "Destroys a droplet the board has just proven is NOT a DOKS node. There\nis no snapshot-first undo for a droplet the way there is for a volume: the local disk\ngoes with it.",
		Fields: map[string]string{
			"DropletIn.disk":     "Disk requests a PERMANENT resize that grows the disk. DO can never resize such a\ndroplet down again, so it defaults false — a CPU/RAM-only change, reversible.",
			"DropletIn.id":       "ID is the DO droplet id, from the path. Numeric.",
			"DropletIn.size":     "Size is the target DigitalOcean size slug on resize, e.g. \"s-4vcpu-8gb\".",
			"MutationOut.data":   "Data is the per-action result, and is deliberately opaque: its keys differ by\naction, and each handler's own description names them. Null on a refusal or a\nfailure — and on a partial failure it may carry what DID happen, such as the\nsnapshotId of an undo taken before a delete that then failed.",
			"MutationOut.msg":    "Msg is why, and is empty on success. \"refusing: <reason>\" is the board's verdict;\nanything else is the change having been attempted and failed.",
			"MutationOut.status": "Status is \"ok\" or \"error\". A REFUSAL is an error too, and reads differently from a\nfailure: msg opens \"refusing: \" and means the board proved the change unsafe and\nnever attempted it, so nothing changed.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"deleted":true,"name":"worker-3","freedMonthlyCents":4800}}`),
	})
	zip.Describe("DELETE /v1/admin/infra/loadbalancers/:id", zip.Doc{
		Description: "Destroys a load balancer the board has just proven no live\ntype=LoadBalancer Service in any cluster targets.",
		Fields: map[string]string{
			"LoadBalancerIn.id":  "ID is the DO load balancer id, from the path.",
			"MutationOut.data":   "Data is the per-action result, and is deliberately opaque: its keys differ by\naction, and each handler's own description names them. Null on a refusal or a\nfailure — and on a partial failure it may carry what DID happen, such as the\nsnapshotId of an undo taken before a delete that then failed.",
			"MutationOut.msg":    "Msg is why, and is empty on success. \"refusing: <reason>\" is the board's verdict;\nanything else is the change having been attempted and failed.",
			"MutationOut.status": "Status is \"ok\" or \"error\". A REFUSAL is an error too, and reads differently from a\nfailure: msg opens \"refusing: \" and means the board proved the change unsafe and\nnever attempted it, so nothing changed.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"deleted":true,"name":"ingress-lb","ip":"1.2.3.4","freedMonthlyCents":1200}}`),
	})
	zip.Describe("DELETE /v1/admin/infra/volumes/:id", zip.Doc{
		Description: "Destroys a volume the board has just proven no PersistentVolume in any\ncluster references. Irreversible, so it snapshots first unless explicitly waived —\nthe snapshot IS the undo.",
		Fields: map[string]string{
			"MutationOut.data":   "Data is the per-action result, and is deliberately opaque: its keys differ by\naction, and each handler's own description names them. Null on a refusal or a\nfailure — and on a partial failure it may carry what DID happen, such as the\nsnapshotId of an undo taken before a delete that then failed.",
			"MutationOut.msg":    "Msg is why, and is empty on success. \"refusing: <reason>\" is the board's verdict;\nanything else is the change having been attempted and failed.",
			"MutationOut.status": "Status is \"ok\" or \"error\". A REFUSAL is an error too, and reads differently from a\nfailure: msg opens \"refusing: \" and means the board proved the change unsafe and\nnever attempted it, so nothing changed.",
			"VolumeIn.id":        "ID is the DO volume id, from the path.",
			"VolumeIn.name":      "Name is the snapshot name on the snapshot action. Blank gets a deterministic\n\"<volume>-predelete-<unix>\" so the undo is findable in the DO console.",
			"VolumeIn.sizeGiB":   "SizeGiB is the target size on the resize action. A volume only ever grows —\nExpandTo is the verdict that refuses a shrink, so this is not validated here.",
			"VolumeIn.snapshot":  "Snapshot is the snapshot-first switch on DELETE. Anything other than the literal\n\"false\" snapshots before destroying — the snapshot IS the undo, so waiving it is\ndeliberate and explicit.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"deleted":true,"name":"acme-data","sizeGiB":200,"freedMonthlyCents":2000,"snapshotId":"snap-01J"}}`),
	})
	zip.Describe("GET /v1/admin/infra", zip.Doc{
		Description: "Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS\nclusters and load balancers, each cross-referenced against every cluster's live\nKubernetes state so the board can say what is safe to destroy and what is not.\n\nIt is cached for up to a minute because one read is a fan-out over the DO API plus a\nfull pod/PV listing per cluster. Staleness is never load-bearing: every MUTATION\nre-scans from scratch and ignores this cache.\n\nOnly an unusable DO account is a hard failure. A partial read still produces a board,\nwith the failing source named in sources[] — except for clusters and volumes, which\nthe safety verdict depends on; without those the analysis degrades rather than\nclassifying anything it cannot prove.",
		Fields: map[string]string{
			"Cluster.id":                  "ID is the DOKS cluster UUID — the same UUID that appears in a resource's\n`k8s:<uuid>` tag, and the key every scan and every proven ownership joins on.",
			"Cluster.idlePVCs":            "IdlePVCs is how many of this cluster's volumes are Bound with no pod mounting them\n— its share of the review queue, not of the delete queue.",
			"Cluster.monthlyCents":        "MonthlyCents is this cluster's share of the bill: its droplets plus the volumes\nPROVEN to belong to it. Load balancers are not included — a Service's load balancer\nis attributed on the load-balancer row, not folded in here, so cluster costs never\ndouble-count against Cost.totalMonthly.",
			"Cluster.name":                "Name is the cluster's DO name (\"hanzo-k8s\"). It is what every human-facing message\non this board names a cluster by, including the refusal reasons.",
			"Cluster.nodePools":           "NodePools is how many pools the cluster has, which is len(pools).",
			"Cluster.nodes":               "Nodes is how many droplets carry this cluster's tag. It comes from the DO\ninventory, so it is populated even for a cluster that never answered.",
			"Cluster.pods":                "Pods is how many pods the scan listed, all namespaces. Zero when Scanned is false —\nunknown, not empty.",
			"Cluster.pools":               "Pools is the pools themselves, each carrying its own scale verdict.",
			"Cluster.pvcs":                "PVCs is how many PersistentVolumeClaims the scan listed.",
			"Cluster.pvs":                 "PVs is how many PersistentVolumes the scan listed. These are the claims that make a\nDO volume live; zero here with Scanned false is what forces the whole board\nincomplete.",
			"Cluster.region":              "Region is DO's region slug (\"nyc3\"). A volume can only ever attach to a droplet in\nits own region, which is why the region rides along on the row.",
			"Cluster.scanError":           "ScanError is why the scan failed, or \"not scanned\" when the cluster was never\nreached at all. Empty exactly when Scanned is true.",
			"Cluster.scanned":             "Scanned reports that this cluster's Kubernetes answered. False makes the whole\nboard incomplete, not just this row: a cluster that did not answer might hold any\nvolume on it.",
			"Cluster.status":              "Status is DOKS's own lifecycle state for the control plane (\"running\",\n\"provisioning\", \"degraded\", \"error\", \"deleting\"). It says nothing about whether\nthis board reached the cluster — Scanned answers that.",
			"Cluster.version":             "Version is the DOKS Kubernetes version DO reports (\"1.31.1-do.0\").",
			"Cost.dropletsMonthly":        "DropletsMonthly is DigitalOcean's list price for every droplet's size, summed.\nThe droplet's local disk is part of that price and is never billed again.",
			"Cost.loadBalancersMonthly":   "LoadBalancersMonthly is $12 per billed node per month, summed. DO's API does not\nprice load balancers, so this is derived from the size_unit it does report.",
			"Cost.reclaimableMonthly":     "ReclaimableMonthly is what deleting the unreferenced volumes would stop costing —\nthe one figure on this board that a button here can actually collect. Zero while\nComplete is false, because an unproven orphan is not an orphan.",
			"Cost.totalMonthly":           "TotalMonthly is droplets + volumes + load balancers. It is the fleet's recurring\nbill, not the DigitalOcean invoice: bandwidth, snapshots, spaces and registry are\nnot inventory this board reads. WastedMonthly is already inside VolumesMonthly and\nis never added again.",
			"Cost.volumesMonthly":         "VolumesMonthly is block storage at DO's rate of 10 cents per provisioned GiB per\nmonth, summed over every volume — attached, idle and orphaned alike, because DO\nbills for capacity, not for use.",
			"Cost.wastedMonthly":          "WastedMonthly is what the fleet pays every month for provisioned-but-empty space on\nthe volumes a kubelet actually measured.\n\nIt is NOT ReclaimableMonthly and must never be added to it. Reclaimable is money a\nbutton on this board collects, by deleting volumes proven to belong to no one.\nWasted is money locked inside volumes that are IN USE and holding live data:\nDigitalOcean can only ever grow a volume, so collecting it means copying a database\nonto a smaller one. See shrinkRecipe.\n\nIt is also a LOWER BOUND — unmeasured volumes contribute nothing.",
			"Finding.cluster":             "Cluster is the cluster's name where the finding is scoped to one, \"\" otherwise.",
			"Finding.detail":              "Detail is the full explanation. For an oversized volume it is the entire\ncopy-and-swap migration, written out as kubectl steps — this board prints the\nrecipe and deliberately refuses to run it.",
			"Finding.id":                  "ID is stable across scans and unique within one: `unref/<volume>`,\n`oversized/<volume>`, `pod/<cluster>/<ns>/<name>`, `image/<repo>`,\n`cost/node/<name>`. A consumer can key a dismissal off it.",
			"Finding.kind":                "Kind is the machine-readable class: `scan-incomplete`, `unreferenced-volume`,\n`released-pv`, `idle-pvc`, `oversized-volume`, `pod-unhealthy`, `unknown-image`,\n`cost-outlier`. Group and filter on this; Title is for a human.",
			"Finding.monthlyCents":        "MonthlyCents is the money at stake, and it is the secondary sort within a severity.\nIt carries what acting would actually SAVE, not the raw waste — an oversized volume\nreports the saving after keeping headroom, since a list sorted by money has to be\nsorted by money you could really get back. Zero when nothing is at stake.",
			"Finding.resource":            "Resource is what the finding is about, in that kind's own addressing: a volume id,\na `namespace/name` pod, an image repository. Empty on a fleet-wide finding.",
			"Finding.severity":            "Severity is one of `critical`, `warn`, `info`, and is the primary sort. Only an\nincomplete scan is critical, because only that invalidates the board's other\nanswers.",
			"Finding.title":               "Title is the one-line summary, already carrying the numbers that matter.",
			"LoadBalancer.blockedReason":  "BlockedReason is why not — which Service still wants it, or how many of its member\ndroplets sit outside every cluster and so cannot be vouched for. Empty exactly when\nDeletable is true.",
			"LoadBalancer.cluster":        "Cluster is the owning cluster's name — from the claiming Service where there is\none, otherwise from the cluster its member droplets belong to.",
			"LoadBalancer.deletable":      "Deletable is the standing verdict: no live Service claims it, every member droplet\nbelongs to a cluster this board scanned, and the scan was complete.",
			"LoadBalancer.droplets":       "Droplets is how many droplets it forwards to. NOT a liveness signal: a DOKS load\nbalancer lists every node in its cluster, so a leaked one still looks busy.",
			"LoadBalancer.id":             "ID is the DO load balancer UUID. DOKS writes it into the Service's\n`kubernetes.digitalocean.com/load-balancer-id` annotation, which is one of the two\nidentities the liveness index is keyed by.",
			"LoadBalancer.ip":             "IP is the public address it answers on — the address that black-holes if it is\ndeleted while a Service still wants it. It is also the second identity the\nliveness index matches on, for a Service that carries no annotation.",
			"LoadBalancer.monthlyCents":   "MonthlyCents is SizeUnit at $12 per node per month. Derived, because DO's API\nprices droplets but not load balancers.",
			"LoadBalancer.name":           "Name is DigitalOcean's own name for it. A DOKS-provisioned load balancer is named\nafter neither its Service nor its cluster, so this does not identify who uses it —\nService does.",
			"LoadBalancer.region":         "Region is DO's region slug (\"nyc3\").",
			"LoadBalancer.service":        "Service is the `namespace/name` of the live type=LoadBalancer Service that claims\nthis load balancer, proven from the cluster scan. Non-empty means IN USE.",
			"LoadBalancer.sizeUnit":       "SizeUnit is the number of billed load-balancer nodes DO reports. Never below 1: DO\nomits the field on the legacy single-node size, which is one node, not zero.",
			"LoadBalancer.status":         "Status is DO's own state for it (\"new\", \"active\", \"errored\").",
			"Machine.blockedReason":       "BlockedReason is why not, in the operator's language, and it names the lever that\nDOES work — scale or edit the node pool. Empty exactly when Mutable is true.",
			"Machine.cluster":             "Cluster is the DOKS cluster's name, or \"\" for a droplet no cluster owns — the only\nkind this board will ever delete.",
			"Machine.clusterId":           "ClusterID is that cluster's UUID, read from the droplet's own `k8s:<uuid>` tag. On\na DROPLET the tag is authoritative, unlike on a volume: DOKS creates the droplet\nand destroys it with the pool, so the tag cannot outlive what it describes.",
			"Machine.createdAt":           "CreatedAt is when DO created the droplet, RFC3339.",
			"Machine.id":                  "ID is the DigitalOcean droplet id. Numeric, and the id every droplet route takes.",
			"Machine.localDiskGiB":        "LocalDiskGiB is the droplet's own disk, in GiB. Its price is already inside\nMonthlyCents — DO does not bill it as block storage, and adding it to the volume\ntotals is how a fleet appears to hold terabytes it never pays for.",
			"Machine.memoryMiB":           "MemoryMiB is the plan's RAM, in MiB as DO reports it.",
			"Machine.monthlyCents":        "MonthlyCents is DO's list price for SizeSlug, in cents per month. It is the plan's\nprice, not a metered bill, so it does not move with how hard the machine works.",
			"Machine.mutable":             "Mutable reports whether this droplet may be changed DIRECTLY — deleted or resized.\nOne predicate covers both because one fact decides both: a DOKS node belongs to a\nnode pool, and the pool is the only thing allowed to change it.",
			"Machine.name":                "Name is the droplet name. For a DOKS node it is also the Kubernetes node name, and\nthat equality is the join: it is how Ready, Schedulable and Pods below get filled.",
			"Machine.pods":                "Pods is how many pods the scan found scheduled on this node.",
			"Machine.privateIp":           "PrivateIP is the droplet's VPC address, or \"\" when it has none.",
			"Machine.publicIp":            "PublicIP is the droplet's internet-facing address, or \"\" when it has none.",
			"Machine.ready":               "Ready is the Kubernetes node's own Ready condition, joined by Name. False for a\ndroplet that is not a node at all, and for a node in a cluster that did not answer\n— unknown reads as not-ready, which refuses more and is the safe direction.",
			"Machine.region":              "Region is DO's region slug (\"nyc3\").",
			"Machine.schedulable":         "Schedulable is the same node's cordon state, inverted: false means unschedulable.\nIt is what the cordon route sets, and only a Ready AND schedulable node counts\ntoward NodePool.clusterSchedulable.",
			"Machine.sizeSlug":            "SizeSlug is the DigitalOcean plan (\"s-4vcpu-8gb\"). It decides MonthlyCents and is\nwhat a resize sets.",
			"Machine.status":              "Status is DO's droplet state (\"new\", \"active\", \"off\", \"archive\"). A powered-off\ndroplet still bills, which is why \"off\" here and a cost row are not in tension.",
			"Machine.tags":                "Tags are DO's resource tags verbatim, including the DOKS stamps `k8s:<uuid>` and\n`k8s:worker`. Never null — an untagged droplet carries an empty list.",
			"Machine.vcpus":               "VCPUs is the plan's virtual CPU count.",
			"Machine.volumes":             "Volumes is how many block-storage volumes DO reports attached to this droplet.",
			"NodePool.blockedReason":      "BlockedReason is why not, in the operator's language. Empty exactly when Scalable\nis true — allowed carries no reason, blocked always names one.",
			"NodePool.cluster":            "Cluster is that cluster's name, carried on the row so a refusal can say which\ncluster a shrink would strand.",
			"NodePool.clusterId":          "ClusterID is the DOKS cluster this pool belongs to.",
			"NodePool.clusterSchedulable": "ClusterSchedulable is how many nodes in the WHOLE cluster are both Ready and\nschedulable — not just this pool's. Cordoned and NotReady nodes are excluded,\nbecause a shrink's evicted pods cannot land on them. It rides on the row so\nScaleTo is a pure method of the row and needs no second lookup.",
			"NodePool.count":              "Count is how many nodes the pool is currently set to. It is the DECLARED count, so\nit can lead reality while DOKS creates or drains machines.",
			"NodePool.id":                 "ID is the DO node pool UUID. The scale route accepts it or Name interchangeably,\nsince both are unique within a cluster.",
			"NodePool.name":               "Name is the pool's name (\"workers\") — what an operator reads off the board and\nwhat every message about a scale names.",
			"NodePool.scalable":           "Scalable is the standing verdict: whether a scale may be attempted at all. It\nreflects the shared completeness gate ONLY. Whether a PARTICULAR count is allowed\ndepends on the number asked for, which ScaleTo decides at request time.",
			"NodePool.size":               "Size is the DigitalOcean size slug every node in this pool is created at\n(\"s-4vcpu-8gb\"). One slug per pool: it is the pool, not the droplet, that declares\nwhat its machines are, which is why a hand-resized node gets reverted.",
			"ReadIn.refresh":              "Refresh, when present, forces a full re-scan instead of serving the cached\nsnapshot. Every MUTATION re-scans regardless — this is only for the reader.",
			"ReadOut.data":                "Data is the board. Null exactly when Status is \"error\".",
			"ReadOut.msg":                 "Msg is the failure, and is empty on success.",
			"ReadOut.status":              "Status is \"ok\" or \"error\". An error here means the DigitalOcean account itself is\nunreadable; a partial read still answers ok, with the failures named in\ndata.sources and the safety verdicts degraded via data.complete.",
			"Snapshot.at":                 "At is when the scan ran, RFC3339 in UTC. Analyze takes it as an argument instead of\nreading a clock, which is what makes the whole fold byte-reproducible in a test.",
			"Snapshot.clusters":           "Clusters is every DOKS cluster in the account, each with the rollup of its scan.\nA cluster DO reports appears here even when its Kubernetes never answered.",
			"Snapshot.complete":           "Complete reports that EVERY cluster answered. It is the gate in front of every\ndestructive verdict on this board: while it is false nothing is deletable,\nexpandable or scalable, because the cluster that stayed silent is exactly the one\nthat might be holding the volume.",
			"Snapshot.cost":               "Cost is what the fleet bills per month, in cents, plus what could be recovered.",
			"Snapshot.findings":           "Findings is the audit pass over everything above, sorted worst first: severity,\nthen money, then id. Empty means the fold found nothing worth a human's attention.",
			"Snapshot.incompleteReason":   "IncompleteReason names the clusters that did not answer and what their silence\ncosts, in the operator's language. Empty exactly when Complete is true, and it is\nthe same sentence every blocked row then repeats as its own BlockedReason.",
			"Snapshot.loadBalancers":      "LoadBalancers is every DO load balancer, attributed to a cluster by the Service\nthat claims it, or failing that by its member droplets.",
			"Snapshot.nodes":              "Nodes is EVERY droplet in the account, not only cluster members. A droplet no\ncluster claims has an empty cluster and is the only kind this board will delete.",
			"Snapshot.sources":            "Sources is one freshness row per upstream read: `do.clusters`, `do.droplets`,\n`do.volumes`, `do.loadBalancers`, and `k8s.<cluster name>` for each cluster scanned.\nA row with ok=false is usually why Complete is false.",
			"Snapshot.totals":             "Totals are the fleet counts — how many of each thing, and how much capacity.",
			"Snapshot.volumes":            "Volumes is every block-storage volume, each carrying the state machine's verdict\non who owns it. It is the list the safety rule exists for.",
			"SourceStatus.at":             "At is when the aggregator ran, RFC3339. It is the READ's timestamp, shared by every\nrow in the list — not a per-source last-success time, so it never implies a stale\nsource is fresh.",
			"SourceStatus.error":          "Error is why the read failed, and is empty exactly when OK is true.",
			"SourceStatus.name":           "Name identifies the upstream read, dotted by system: \"do.volumes\", \"do.balance\",\n\"k8s.hanzo-k8s\", \"iam.orgs\", \"billing.subscriptions\". It is stable, so a console\ncan keep per-source state across reads.",
			"SourceStatus.ok":             "OK is whether that read succeeded. False is the whole point of this row: an\naggregator answers with what it got rather than failing, so the only way a reader\ncan tell a real zero from a missing source is here.",
			"SourceStatus.rows":           "Rows is how many records came back. Zero with ok=true is a genuine empty result;\nzero with ok=false means nothing was read at all.",
			"Totals.attachedGiB":          "AttachedGiB is the provisioned capacity of the attached volumes, in GiB.",
			"Totals.attachedVolumes":      "AttachedVolumes counts the volumes DO reports attached to a droplet right now.",
			"Totals.clusters":             "Clusters is how many DOKS clusters DigitalOcean reports, scanned or not. It is the\ndenominator the completeness gate counts unanswered clusters against.",
			"Totals.detachedGiB":          "DetachedGiB is the provisioned capacity of the detached volumes, in GiB. With\nAttachedGiB it sums to VolumeGiB.",
			"Totals.detachedVolumes":      "DetachedVolumes is the exact complement of AttachedVolumes — bound, released and\nunreferenced together, so the two always sum to Volumes. Detached is NOT idle and\nNOT garbage: most of these are live data between mounts.",
			"Totals.idlePVCs":             "IdlePVCs counts volumes whose PVC is Bound but which no running pod mounts. A\nreview queue, never a delete queue — the usual member is a stopped database — and\ndeliberately excluded from ReclaimableMonthly.",
			"Totals.loadBalancers":        "LoadBalancers is every DO load balancer in the account.",
			"Totals.localDiskGiB":         "LocalDiskGiB is the droplets' own disk, summed. Broken out precisely so it is never\nadded to VolumeGiB: it is included in each droplet's own monthly price and DO does\nnot bill it as block storage.",
			"Totals.measuredGiB":          "MeasuredGiB is the PROVISIONED capacity of the measured volumes — how much of the\nfleet UsedGiB and WastedGiB were computed from, not how much of it is full.",
			"Totals.measuredVolumes":      "Fill. MeasuredVolumes/UnmeasuredVolumes are the honesty denominator: UsedGiB and\nWastedGiB describe the measured set ONLY, so a board showing waste must show how\nmuch of the fleet the figure was computed from. Unmeasured capacity contributes\nnothing to either — it is not assumed empty, and it is not assumed full.",
			"Totals.nodes":                "Nodes is every droplet in the account, cluster member or not — the same population\nas Snapshot.nodes, which is a superset of the machines Kubernetes knows about.",
			"Totals.unmeasuredGiB":        "UnmeasuredGiB is the provisioned capacity of the unmeasured volumes, in GiB — the\nsize of the blind spot behind UsedGiB and WastedGiB.",
			"Totals.unmeasuredVolumes":    "UnmeasuredVolumes counts volumes no kubelet reported a reading for. Not measured is\na different fact from empty, so these contribute to neither UsedGiB nor WastedGiB.",
			"Totals.unreferencedGiB":      "UnreferencedGiB is the provisioned capacity of the unreferenced volumes, in GiB —\nthe space a delete would hand back.",
			"Totals.unreferencedVolumes":  "UnreferencedVolumes counts the volumes no PV in any cluster names — the deletable\nset, and the only one. It is a count of CANDIDATES: whether the button is live\nstill depends on Complete.",
			"Totals.usedGiB":              "UsedGiB is what the measured filesystems actually hold. Summed in BYTES and\nconverted once at the end, so hundreds of sub-GiB readings do not each round to\nzero on the way in.",
			"Totals.volumeGiB":            "VolumeGiB is the PROVISIONED block-storage capacity, in whole GiB — the number\nDigitalOcean bills for. It excludes droplet local disk entirely; see LocalDiskGiB.",
			"Totals.volumes":              "Volumes is every block-storage volume in the account.",
			"Totals.wastedGiB":            "WastedGiB is provisioned minus measured across the measured set, in billed GiB. A\nLOWER BOUND on fleet waste, and not the same money as ReclaimableMonthly: this\nspace sits inside volumes holding live data. See Cost.wastedMonthly.",
			"Volume.blockedReason":        "BlockedReason is why not, in the operator's language — which cluster's PV holds it,\nor which clusters went unanswered. Empty exactly when Deletable is true.",
			"Volume.cluster":              "Cluster/ClusterID are the PROVEN owner — resolved through a PV that names this\nvolume, never through the tag.",
			"Volume.clusterId":            "ClusterID is that cluster's UUID. Falls back to the cluster of the droplet the\nvolume is physically attached to, which is evidence of the same quality: live\nkernel state, not a label someone wrote once.",
			"Volume.controller":           "Controller is the workload owning the pod that mounts this volume\n(\"StatefulSet/luxd\"), or \"\" when nothing mounts it. It names who has to act.",
			"Volume.createdAt":            "CreatedAt is when DO created the volume, RFC3339. Age is corroboration for a human\njudging an orphan, never an input to the deletable verdict.",
			"Volume.deletable":            "Deletable is the standing verdict: this volume is unreferenced AND the scan was\ncomplete enough to prove it. It is the only thing the delete route consults, and it\nis recomputed from a fresh scan at the moment the button is pressed.",
			"Volume.dropletIds":           "DropletIDs are the droplets DO reports this volume attached to. Non-empty is hard\nkernel-level evidence the volume is in use right now. Never null.",
			"Volume.expandBlockedReason":  "ExpandBlockedReason is why a grow is refused: a PV claims the volume but no PVC\ndoes, so growing the device would leave the PV declaring a capacity that is wrong.\nEmpty exactly when Expandable is true.",
			"Volume.expandable":           "Expandable/ExpandBlockedReason are the GROW verdict, kept separate from Deletable\nbecause the two ask opposite questions: a volume is deletable when nothing uses it,\nand expandable when something uses it in a way this board can grow completely.",
			"Volume.hasUsage":             "HasUsage reports whether a kubelet actually MEASURED this volume's filesystem.\n\nFalse means NOT MEASURED. It does NOT mean empty, and the three fields below are\nmeaningless — not zero — when it is false. A reading exists only while a running pod\nhas the volume mounted on a node that answered; a detached, idle or unreferenced\nvolume has none. Rendering an unmeasured volume as \"0 used / 100% wasted\" would\ninvent the single most expensive lie this board could tell, so every consumer must\nbranch on this flag and show unknown.",
			"Volume.id":                   "ID is the DigitalOcean volume id. It is also the `spec.csi.volumeHandle` a\nPersistentVolume names, which is what makes the cross-cluster liveness test an\nexact match rather than a guess.",
			"Volume.idle":                 "Idle is Bound with nothing mounting it: live data nobody is currently reading,\ntypically a stopped database. It is a queue for a human and is deliberately kept\nout of Cost.reclaimableMonthly.",
			"Volume.monthlyCents":         "MonthlyCents is SizeGiB at DO's rate of 10 cents per GiB per month. It is charged\non provisioned capacity, so an empty volume and a full one of the same size cost\nexactly the same.",
			"Volume.mountedBy":            "MountedBy lists the `namespace/name` of every running pod mounting this volume.\nEmpty on a Bound volume means idle — a review signal, never a delete signal.\nNever null.",
			"Volume.name":                 "Name is the DO volume name. DOKS-provisioned volumes are named `pvc-<uuid>`, which\nis a naming convention and NOT evidence of ownership — only a PV reference is.",
			"Volume.nodeName":             "NodeName is the name of the first attached droplet, or \"\" when detached. Carried so\na refusal can name the machine an operator would have to go look at.",
			"Volume.pv":                   "PV is the name of the PersistentVolume that claims this volume, or \"\" when no PV in\nany scanned cluster does. Non-empty is the proof that makes it undeletable.",
			"Volume.pvPhase":              "PVPhase is that PV's phase — `Bound`, `Released`, `Available` or `Failed`. Bound\nmeans live data; anything else means the claim is retired but the PV still exists,\nso a human retires the PV before the volume can go.",
			"Volume.pvcName":              "PVCName is that claim's name. It plus the namespace is what the fill reading and\nthe mounting pods are keyed by, so an empty PVCName means neither can exist.",
			"Volume.pvcNamespace":         "PVCNamespace is the namespace of the claim the PV is bound to, or \"\" when unbound.",
			"Volume.region":               "Region is DO's region slug (\"nyc3\"). A volume can only ever attach to a droplet in\nits own region, so this bounds where it could possibly be in use.",
			"Volume.sizeGiB":              "SizeGiB is the PROVISIONED size — what DigitalOcean bills. It is not the\nfilesystem's capacity, which is a few percent smaller after format overhead.",
			"Volume.state":                "State is the ownership verdict, one of `attached`, `bound`, `released`,\n`unreferenced`. The machine is total and ordered — attachment beats reference,\nreference beats absence — and `unreferenced` is the ONLY deletable state.",
			"Volume.tagCluster":           "TagCluster is the `k8s:<uuid>` tag. ADVISORY ONLY: it outlives the cluster that\nset it. Shown so the operator can see tag-vs-truth disagree, never acted on.",
			"Volume.usedBytes":            "UsedBytes is the measured filesystem usage. BYTES, not GiB: the volumes this exists\nto catch hold a fraction of a GiB in 200, and rounding that to an integer GiB would\nprint the very 0 the flag above exists to prevent.",
			"Volume.wastedGiB":            "WastedGiB is provisioned minus measured, in the unit DigitalOcean BILLS: whole GiB\nof the volume's own size, never the filesystem's capacity — a 200 GiB volume carries\na 196 GiB filesystem after format overhead, and the invoice says 200.",
			"Volume.wastedMonthlyCents":   "WastedMonthlyCents is WastedGiB at the same 10 cents per GiB per month. Money the\nfleet pays for empty space inside a volume that is IN USE — so it is not\ncollectable by any button here, only by the copy-and-swap migration the matching\nfinding spells out. Zero when HasUsage is false, which means unknown, not none.",
		},
		Example:  json.RawMessage(`{"refresh":"1"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"volumes":[],"nodes":[],"clusters":[],"loadBalancers":[],"sources":[{"name":"do.volumes","ok":true,"rows":2,"lastSync":"2026-07-27T00:00:00Z"}]}}`),
	})
	zip.Describe("POST /v1/admin/infra/clusters/:id/nodepools/:pool/scale", zip.Doc{
		Description: "Sets a node pool's node count — the ONE correct way to change how many\nnodes a DOKS cluster has.\n\nThe response states what the board could NOT prove: DOKS picks which nodes a shrink\nremoves, so no particular pod is shown to survive one. See NodePool.ScaleTo.",
		Fields: map[string]string{
			"MutationOut.data":   "Data is the per-action result, and is deliberately opaque: its keys differ by\naction, and each handler's own description names them. Null on a refusal or a\nfailure — and on a partial failure it may carry what DID happen, such as the\nsnapshotId of an undo taken before a delete that then failed.",
			"MutationOut.msg":    "Msg is why, and is empty on success. \"refusing: <reason>\" is the board's verdict;\nanything else is the change having been attempted and failed.",
			"MutationOut.status": "Status is \"ok\" or \"error\". A REFUSAL is an error too, and reads differently from a\nfailure: msg opens \"refusing: \" and means the board proved the change unsafe and\nnever attempted it, so nothing changed.",
			"ScaleIn.count":      "Count is the node count to set.",
			"ScaleIn.id":         "ID is the DOKS cluster id, from the path.",
			"ScaleIn.pool":       "Pool is the node pool, from the path. Its DO id or its name — both are unique\nwithin a cluster, and an operator reads the name off the board.",
		},
		Example:  json.RawMessage(`{"count":5}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"pool":"workers","cluster":"hanzo-k8s","from":3,"to":5}}`),
	})
	zip.Describe("POST /v1/admin/infra/droplets/:id/resize", zip.Doc{
		Description: "Changes a droplet's plan. Same refusal as delete and for the same\nreason: a DOKS node's size is the node pool's to declare.\n\ndisk=true is a PERMANENT resize — the disk grows and DO can never resize the droplet\nDOWN again. disk=false (the default) changes CPU/RAM only and is reversible. DO\nrequires the droplet to be powered off and applies the change asynchronously, so the\nresponse carries the action to poll, not a completed change.",
		Fields: map[string]string{
			"DropletIn.disk":     "Disk requests a PERMANENT resize that grows the disk. DO can never resize such a\ndroplet down again, so it defaults false — a CPU/RAM-only change, reversible.",
			"DropletIn.id":       "ID is the DO droplet id, from the path. Numeric.",
			"DropletIn.size":     "Size is the target DigitalOcean size slug on resize, e.g. \"s-4vcpu-8gb\".",
			"MutationOut.data":   "Data is the per-action result, and is deliberately opaque: its keys differ by\naction, and each handler's own description names them. Null on a refusal or a\nfailure — and on a partial failure it may carry what DID happen, such as the\nsnapshotId of an undo taken before a delete that then failed.",
			"MutationOut.msg":    "Msg is why, and is empty on success. \"refusing: <reason>\" is the board's verdict;\nanything else is the change having been attempted and failed.",
			"MutationOut.status": "Status is \"ok\" or \"error\". A REFUSAL is an error too, and reads differently from a\nfailure: msg opens \"refusing: \" and means the board proved the change unsafe and\nnever attempted it, so nothing changed.",
		},
		Example:  json.RawMessage(`{"size":"s-4vcpu-8gb","disk":false}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"name":"worker-3","from":"s-2vcpu-4gb","to":"s-4vcpu-8gb","permanent":false,"actionId":1234567,"actionStatus":"in-progress"}}`),
	})
	zip.Describe("POST /v1/admin/infra/nodes/:id/cordon", zip.Doc{
		Description: "Marks one cluster node unschedulable — or schedulable again — and can drain\nthe pods already on it.\n\nIt is the ONE infra change that does not go through the run discipline, because there\nis no destructive verdict to check: cordoning is reversible and evicting respects the\ncluster's own PodDisruptionBudgets. It reads the cached board for the same reason.\nThe outcome is audited either way, and the result reports how many pods were evicted.",
		Fields: map[string]string{
			"CordonIn.cordon":    "Cordon true marks the node unschedulable; false restores it.",
			"CordonIn.drain":     "Drain additionally evicts the pods already running there.",
			"CordonIn.id":        "ID is the node's droplet id, from the path.",
			"MutationOut.data":   "Data is the per-action result, and is deliberately opaque: its keys differ by\naction, and each handler's own description names them. Null on a refusal or a\nfailure — and on a partial failure it may carry what DID happen, such as the\nsnapshotId of an undo taken before a delete that then failed.",
			"MutationOut.msg":    "Msg is why, and is empty on success. \"refusing: <reason>\" is the board's verdict;\nanything else is the change having been attempted and failed.",
			"MutationOut.status": "Status is \"ok\" or \"error\". A REFUSAL is an error too, and reads differently from a\nfailure: msg opens \"refusing: \" and means the board proved the change unsafe and\nnever attempted it, so nothing changed.",
		},
		Example:  json.RawMessage(`{"cordon":true,"drain":true}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"name":"worker-3","schedulable":false,"evicted":7}}`),
	})
	zip.Describe("POST /v1/admin/infra/volumes/:id/resize", zip.Doc{
		Description: "Grows a volume. GROW ONLY — see Volume.ExpandTo for why the other\ndirection is a data migration this board deliberately refuses to run.\n\nThe MECHANISM follows the volume's owner, because there is exactly one way to grow each\nkind completely. A volume a PVC claims is grown by patching the claim: the CSI driver\nthen resizes the DigitalOcean device AND grows the filesystem on it, leaving claim, PV,\ndevice and filesystem all agreeing. Calling DigitalOcean directly for that volume would\ngrow the device while the PV kept declaring the old capacity and the filesystem never\ngrew at all. One operation, one correct mechanism per owner — not two ways to do it.",
		Fields: map[string]string{
			"MutationOut.data":   "Data is the per-action result, and is deliberately opaque: its keys differ by\naction, and each handler's own description names them. Null on a refusal or a\nfailure — and on a partial failure it may carry what DID happen, such as the\nsnapshotId of an undo taken before a delete that then failed.",
			"MutationOut.msg":    "Msg is why, and is empty on success. \"refusing: <reason>\" is the board's verdict;\nanything else is the change having been attempted and failed.",
			"MutationOut.status": "Status is \"ok\" or \"error\". A REFUSAL is an error too, and reads differently from a\nfailure: msg opens \"refusing: \" and means the board proved the change unsafe and\nnever attempted it, so nothing changed.",
			"VolumeIn.id":        "ID is the DO volume id, from the path.",
			"VolumeIn.name":      "Name is the snapshot name on the snapshot action. Blank gets a deterministic\n\"<volume>-predelete-<unix>\" so the undo is findable in the DO console.",
			"VolumeIn.sizeGiB":   "SizeGiB is the target size on the resize action. A volume only ever grows —\nExpandTo is the verdict that refuses a shrink, so this is not validated here.",
			"VolumeIn.snapshot":  "Snapshot is the snapshot-first switch on DELETE. Anything other than the literal\n\"false\" snapshots before destroying — the snapshot IS the undo, so waiving it is\ndeliberate and explicit.",
		},
	})
	zip.Describe("POST /v1/admin/infra/volumes/:id/snapshot", zip.Doc{
		Description: "Takes a point-in-time snapshot of one volume — the undo a delete relies\non, available on its own so an operator can take one before any risky change.\n\nIt re-scans the board first (never the cache) so the volume it snapshots is one that\nexists right now, and audits the outcome either way.",
		Fields: map[string]string{
			"Snapshot.ID":              "ID is DigitalOcean's snapshot id. It is what a restore takes, so it is the handle\nthe undo actually hangs on — a delete that snapshotted first returns it.",
			"Snapshot.Name":            "Name is the snapshot's name, as given or as minted `<volume>-predelete-<unix>` when\nthe caller left it blank. It is how the snapshot is found in the DO console.",
			"Snapshot.SizeGiB":         "SizeGiB is the snapshot's size, from DO's size_gigabytes: the PROVISIONED size of\nthe volume it was taken from, not the bytes the snapshot occupies. DO bills\nsnapshots on used space, which this does not report.",
			"VolumeIn.id":              "ID is the DO volume id, from the path.",
			"VolumeIn.name":            "Name is the snapshot name on the snapshot action. Blank gets a deterministic\n\"<volume>-predelete-<unix>\" so the undo is findable in the DO console.",
			"VolumeIn.sizeGiB":         "SizeGiB is the target size on the resize action. A volume only ever grows —\nExpandTo is the verdict that refuses a shrink, so this is not validated here.",
			"VolumeIn.snapshot":        "Snapshot is the snapshot-first switch on DELETE. Anything other than the literal\n\"false\" snapshots before destroying — the snapshot IS the undo, so waiving it is\ndeliberate and explicit.",
			"VolumeSnapshotOut.data":   "Data is the snapshot DigitalOcean created — the undo, addressable in the DO\nconsole. Null exactly when Status is \"error\", which means no snapshot exists.",
			"VolumeSnapshotOut.msg":    "Msg is why the snapshot failed, and is empty on success.",
			"VolumeSnapshotOut.status": "Status is \"ok\" or \"error\".",
		},
		Example:  json.RawMessage(`{"name":"acme-data-before-migration"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"id":"snap-01J","name":"acme-data-before-migration","sizeGiB":200,"created":"2026-07-27T00:00:00Z"}}`),
	})
}
