// Code generated by zipdoc; DO NOT EDIT.

package admin

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/admin DELETE /v1/admin/caps/:id", zip.Doc{
		Description: "Removes one cap by id, lifting the ceiling entirely.",
		Fields: map[string]string{
			"capIn.id":      "ID is the cap to edit or remove, from the path. Unused by the list and create ops.",
			"capIn.org":     "Org is the tenant to act on. Required for a SuperAdmin — they must name their\ntarget; ignored for a white-label admin, who always acts on their own org.",
			"rawOut.data":   "Data is the upstream's payload verbatim, left undescribed for the same reason\nas iamRowsOut: re-describing someone else's schema here would be a second copy\nof it, free to drift.",
			"rawOut.msg":    "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"rawOut.status": "Status is \"ok\" or \"error\", at HTTP 200 either way. It is THIS layer's verdict\non the forwarding; an upstream that answered carries its own status inside\ndata.",
			"rawOut.total":  "Total is the upstream's row count when it forwarded a list, and absent when it\nforwarded a single object — which is why it is optional rather than zero.",
		},
		Example:  json.RawMessage(`{"org":"acme","id":"cap_1"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"ok":true}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/aimetrics", zip.Doc{
		Description: "Is the fleet AI board: LLM generations over gen_ai spans (count, cost,\navg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and\nthe eval plane (traces, scores, score names, runs, and the average-score trend).\n\nEvery signal degrades INDEPENDENTLY — a table that is absent or errors contributes its\nzero value and the read still succeeds. Generation latency is a SEPARATE query from\ngenerations and cost on purpose: a duration/attribute mismatch there must not zero\nthe two numbers that did read.",
		Fields: map[string]string{
			"aiMetrics.end":              "End is the moment of this read, RFC 3339 UTC. The window is always right up\nto now; there is no lag to allow for.",
			"aiMetrics.evalRuns":         "EvalRuns is the twelve most RECENT eval runs, newest first — recency, not\nquality.",
			"aiMetrics.evals":            "Evals is the eval plane's band: the trace half and the score half.",
			"aiMetrics.o11yAi":           "O11yAI is the generation rollup over gen_ai spans. Its money is US DOLLARS,\nunlike Usage below — the two halves of this board come from different planes\nand each keeps its plane's unit.",
			"aiMetrics.o11yAiModels":     "O11yAIModels is the twelve busiest models by GENERATION count, from gen_ai\nspans. It answers the same question as TopModels from the other plane, and its\nmoney is US dollars.",
			"aiMetrics.range":            "Range is the window every figure below covers: 24h, 7d or 30d. It is the\nNORMALIZED value — an unrecognized ?range reads as 30d and says so here.",
			"aiMetrics.scoreNames":       "ScoreNames is the twelve busiest scorers, each with its own distribution.",
			"aiMetrics.scoreSeries":      "ScoreSeries is the average eval score bucketed over the window, oldest first.\nThis series IS the training/eval progress signal on this board.",
			"aiMetrics.start":            "Start is the window's lower bound, RFC 3339 UTC, derived from Range.",
			"aiMetrics.topActors":        "TopActors is per-PRINCIPAL spend from the same ledger — whose bill it is,\nwhich the per-model board cannot answer.",
			"aiMetrics.topModels":        "TopModels is the twelve busiest models by request count, from the billing\nledger. Money in these rows is US cents.",
			"aiMetrics.usage":            "Usage is the fleet LLM-usage band from the ledger the gateway writes. Its\nmoney is US CENTS.",
			"aimActorStat.actor":         "Actor is the principal the ledger recorded, \"<org>/<sub>\". A value naming\nan IAM APPLICATION rather than a person means that spend has no human\nowner — which is the row to read first.",
			"aimActorStat.costCents":     "CostCents is what they cost, in US cents, as the ledger recorded it.",
			"aimActorStat.requests":      "Requests is how many calls that principal made in the window.",
			"aimActorStat.tokens":        "Tokens is the total tokens those calls consumed.",
			"aimEvals.avgScore":          "AvgScore is the unweighted mean of every score value in the window,\nregardless of scorer, to 4dp. Because the scorers are pooled it also moves\nwhen the MIX of scorers changes, so read it as a trend and the per-scorer\nrows for the level.",
			"aimEvals.datasets":          "Datasets is how many distinct datasets those traces ran against.",
			"aimEvals.latencyMsAvg":      "LatencyMsAvg is the mean model-under-test call duration in milliseconds,\naveraged only over traces whose end time is after their start — an unfinished\ntrace does not drag it down.",
			"aimEvals.models":            "Models is how many distinct models were under test.",
			"aimEvals.runs":              "Runs is how many DISTINCT eval runs left traces in the window.",
			"aimEvals.scoreNames":        "ScoreNames is how many distinct scorers produced them.",
			"aimEvals.scores":            "Scores is how many individual score values were recorded, across every\nscorer.",
			"aimEvals.traces":            "Traces is how many eval traces were recorded — one per model-under-test call.",
			"aimLfModelStat.costUsd":     "CostUsd is their summed cost in US DOLLARS — the span attribute's unit, not\nthe cents the ledger leaderboard beside this uses.",
			"aimLfModelStat.generations": "Generations is how many gen_ai spans named it. The board ranks on this.",
			"aimLfModelStat.model":       "Model is the model the span reported answering as, falling back to the model\nit was asked for when the response named none.",
			"aimModelStat.costCents":     "CostCents is what they cost, in US cents.",
			"aimModelStat.model":         "Model is the model id the ledger recorded. Unattributed rows carry no model\nand are left out rather than ranked as a nameless one.",
			"aimModelStat.requests":      "Requests is how many calls went to it. The board ranks on this.",
			"aimModelStat.tokens":        "Tokens is what those calls consumed.",
			"aimO11yAI.costUsd":          "CostUsd is their summed cost in US DOLLARS, not cents: it is the span\nattribute's native unit and is carried through unconverted.",
			"aimO11yAI.generations":      "Generations is how many LLM calls the fleet made in the window — one per\ngen_ai span.",
			"aimO11yAI.latencyMsAvg":     "LatencyMsAvg is the mean generation duration in milliseconds, over spans that\nrecorded a duration at all.",
			"aimO11yAI.latencyMsP95":     "LatencyMsP95 is the 95th-percentile generation duration in milliseconds — the\ntail a user actually feels.",
			"aimRunStat.avgValue":        "AvgValue is their mean, to 4dp, pooled across the run's scorers.",
			"aimRunStat.dataset":         "Dataset is one dataset the run scored against — an arbitrary one of them if\nthe run spanned several, since a run is named per row here and a dataset is\nnot.",
			"aimRunStat.lastTs":          "LastTs is when the run last recorded a score, RFC 3339 UTC. The board is\nordered by it, so these rows are the most RECENT runs, not the best ones.",
			"aimRunStat.runName":         "RunName is the eval run's name, as the run recorded it. Unnamed runs are left\nout.",
			"aimRunStat.scores":          "Scores is how many score values the run recorded in the window.",
			"aimScorePoint.avgValue":     "AvgValue is the mean of every score in that bucket, to 4dp. This series IS\nthe eval-progress signal.",
			"aimScorePoint.count":        "Count is how many scores the mean was taken over — the weight behind the\npoint, and what tells a real dip from a thin bucket.",
			"aimScorePoint.ts":           "Ts is the bucket's start, RFC 3339 UTC. The bucket width follows the range —\nan hour at 24h, six hours at 7d, a day at 30d — so it is not a fixed unit.",
			"aimScoreStat.avgValue":      "AvgValue is their mean, to 4dp.",
			"aimScoreStat.count":         "Count is how many values this scorer produced in the window. The board ranks\non this, so a busy scorer leads whether or not it scores well.",
			"aimScoreStat.maxValue":      "MaxValue is the highest. With Min it bounds the spread the mean hides.",
			"aimScoreStat.minValue":      "MinValue is the lowest single value this scorer recorded in the window.",
			"aimScoreStat.name":          "Name is the scorer's name. Its scale is the scorer's own — there is no fleet\nconvention that these are 0..1 — so compare a name against itself over time,\nnever one name against another.",
			"aimUsage.completionTokens":  "CompletionTokens is the generated half of Tokens.",
			"aimUsage.costCents":         "CostCents is what those calls cost, in US CENTS — this half of the board is\nthe billing ledger, so it is cents while the gen_ai half is dollars.",
			"aimUsage.models":            "Models is how many DISTINCT models were called, not a count of calls.",
			"aimUsage.promptTokens":      "PromptTokens is the input half of Tokens.",
			"aimUsage.requests":          "Requests is how many calls the ledger recorded in the window, errored ones\nincluded.",
			"aimUsage.tokens":            "Tokens is prompt plus completion across them.",
			"aimetricsOut.data":          "Data is the board. A warehouse that is not connected, or a table that is\nmissing, yields the ZERO board rather than a failure — so zeros here mean\neither an idle fleet or a blind one, and this envelope cannot tell you which.",
			"aimetricsOut.msg":           "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"aimetricsOut.status":        "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"rangeIn.range":              "Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the\nboard's own default.",
		},
		Example:  json.RawMessage(`{"range":"7d"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"range":"7d","start":"2026-07-20T00:00:00Z","end":"2026-07-27T00:00:00Z","topModels":[],"o11yAiModels":[],"scoreNames":[],"evalRuns":[],"scoreSeries":[]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/analytics", zip.Doc{
		Description: "Is the SaaS product-analytics board over the caller's tenant window: active\ncustomers, new and churned, retention, MRR, ARPU, the usage trend and the top\ncustomers by spend — every number folded from the commerce ledger, not sampled.\n\nThe window is the caller's, not the fleet's: a SuperAdmin gets every org, a\nwhite-label admin only their own subtree (core.ScopedOrgs, the one scope predicate).\n\nsources[] carries each upstream's freshness so a partial read is VISIBLE rather than\nsilently low: a ledger that answered for only some orgs marks commerce-ledger degraded\ninstead of publishing an undercount as healthy.",
		Fields: map[string]string{
			"SeriesPoint.t":                     "T is the bucket key, and its shape says which interval the series was built at:\n\"2006-01-02\" for a day, the ISO week's Monday in the same form for a week,\n\"2006-01\" for a month. Buckets are enumerated end to end over the window, so the\naxis is continuous and successive points are one interval apart.",
			"SeriesPoint.value":                 "Value is the bucket's total, in the unit its own series carries: CENTS for spend,\nrevenue and usage; a COUNT for signups, active customers, cumulative customers and\nchurn. A bucket nothing landed in is an honest 0, not a gap — the axis was\nenumerated first and every bucket exists whether or not anything filled it.",
			"SourceStatus.at":                   "At is when the aggregator ran, RFC3339. It is the READ's timestamp, shared by every\nrow in the list — not a per-source last-success time, so it never implies a stale\nsource is fresh.",
			"SourceStatus.error":                "Error is why the read failed, and is empty exactly when OK is true.",
			"SourceStatus.name":                 "Name identifies the upstream read, dotted by system: \"do.volumes\", \"do.balance\",\n\"k8s.hanzo-k8s\", \"iam.orgs\", \"billing.subscriptions\". It is stable, so a console\ncan keep per-source state across reads.",
			"SourceStatus.ok":                   "OK is whether that read succeeded. False is the whole point of this row: an\naggregator answers with what it got rather than failing, so the only way a reader\ncan tell a real zero from a missing source is here.",
			"SourceStatus.rows":                 "Rows is how many records came back. Zero with ok=true is a genuine empty result;\nzero with ok=false means nothing was read at all.",
			"analyticsData.activeCustomers":     "ActiveCustomers is how many DISTINCT customers had at least one usage event\nin each bucket. Empty when the ledger returned no usage at all.",
			"analyticsData.arpuCents":           "ARPUCents is average revenue per active customer over the window, in US\ncents: window spend divided by MAU, falling back to TotalCustomers when\nnothing was active. Integer division, so it truncates.",
			"analyticsData.churn":               "Churn is LOGO churn per month — customers active in the previous month and\nnot in this one. It counts customers, not revenue. The first point is always\nzero because there is no month before it to compare against.",
			"analyticsData.churnRatePct":        "ChurnRatePct is churned customers as a percentage of the at-risk base,\npooled over the last SIX months rather than over Range.",
			"analyticsData.computed":            "Computed says which metrics on this board are backed by real data, keyed\ngrowth, retention, active, churn, usage, revenue, mrr, arpu, ltv, nrr. False\nmeans the number beside it is an honest zero because the ledger held nothing\nto fold — NOT that it measured zero. Read it before reading any of them.",
			"analyticsData.cumulativeCustomers": "CumulativeCustomers is the all-time customer count as of each bucket's END —\na running total, so it only rises and its last point is TotalCustomers.",
			"analyticsData.dau":                 "DAU is how many distinct customers had a usage event in the last 24 hours. A\nFIXED lookback — it ignores Range, so it does not change when the window\ndoes.",
			"analyticsData.generatedAt":         "GeneratedAt is when this board was computed, RFC 3339 UTC. Nothing is\ncached, so it is the moment of this request.",
			"analyticsData.growthRatePct":       "GrowthRatePct is the percentage change in signups against the immediately\npreceding window of the same length; negative when signups fell. It is 0\nwhen the prior window had none, which is not the same as flat.",
			"analyticsData.interval":            "Interval is the bucket width every series below uses: day, week or month,\nderived from Range (7d and 30d give day, 90d gives week, all gives month).",
			"analyticsData.ltvCents":            "LTVCents is estimated lifetime value per customer, in US cents: ARPU divided\nby the monthly churn rate. NULL until real churn is observed — with no churn\nthe formula divides by zero, and a huge number would read as good news.",
			"analyticsData.mau":                 "MAU is the same over the last 30 days, and it is the denominator ARPUCents\nuses.",
			"analyticsData.mrrCents":            "MRRCents is monthly recurring revenue as of NOW, in US cents, summed from\nactive subscriptions across the caller's window. Point-in-time: it is the\none revenue figure here that is not a fold of the usage window.",
			"analyticsData.newCustomers":        "NewCustomers is how many of them signed up inside the window.",
			"analyticsData.nrrPct":              "NRRPct is net revenue retention. Always NULL: it needs a history of MRR over\ntime and commerce keeps only the current value, so there is nothing honest to\ncompute.",
			"analyticsData.range":               "Range is the requested window: 7d, 30d, 90d, or all (the last two years).\nNORMALIZED — anything else reads as 30d and says so here.",
			"analyticsData.retention":           "Retention is the cohort triangle: signup month against months elapsed since,\nas percentages of each cohort.",
			"analyticsData.revenue":             "Revenue is realized revenue per bucket, in US cents. For a pay-as-you-go\nfleet it carries the same numbers as Usage; it is a separate field so a\nconsole can theme the two differently.",
			"analyticsData.signups":             "Signups is new orgs created per bucket, from IAM's own createdTime. It is\nthe one series here that is always real: a signup timestamp exists whether\nor not that tenant ever used anything.",
			"analyticsData.sources":             "Sources is one row per upstream this board read — iam and commerce-ledger —\nwith whether it answered and how many rows it gave. A ledger that answered\nfor only some orgs is marked degraded here, which is the only thing\ndistinguishing an undercount from a quiet month.",
			"analyticsData.topCustomers":        "TopCustomers is the ten biggest spenders in the window, largest first. A\ncustomer with no spend is left out rather than listed at zero.",
			"analyticsData.totalCustomers":      "TotalCustomers is how many customers in the caller's window carry a signup\ndate at all, over ALL time rather than within Range.",
			"analyticsData.usage":               "Usage is usage spend per bucket, in US cents, oldest first.",
			"analyticsData.wau":                 "WAU is the same over the last 7 days.",
			"analyticsOut.data":                 "Data is the board. Null when Status is \"error\".",
			"analyticsOut.msg":                  "Msg is the failure reason when Status is \"error\" — here, that the tenant\nwindow itself could not be resolved. A degraded upstream is not this: it is\nreported in data.sources with Status still \"ok\".",
			"analyticsOut.status":               "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"analyticsSlice.hint":               "Hint is the org slug behind Label, so two customers sharing a display name\nstay distinguishable and a row can be clicked through to its tenant. Absent\nwhen unknown.",
			"analyticsSlice.label":              "Label is the customer's display name — what to render.",
			"analyticsSlice.value":              "Value is that customer's spend over the window, in US cents.",
			"rangeIn.range":                     "Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the\nboard's own default.",
			"retentionCohort.cohort":            "Cohort is the signup month, YYYY-MM. Every customer who signed up in it is\nin this row.",
			"retentionCohort.size":              "Size is how many customers signed up in that month — the denominator behind\nevery percentage in Values.",
			"retentionCohort.values":            "Values is the percentage of the cohort ACTIVE in each month after signup,\n0..100, with values[0] the signup month itself. Its length varies by row: an\nolder cohort has more months behind it, which is what makes the grid a\ntriangle.",
			"retentionGrid.cohorts":             "Cohorts is one row per signup month, oldest first, capped at the last\ntwelve. A month in which nobody signed up is omitted rather than shown\nempty.",
			"retentionGrid.interval":            "Interval is the cohort width, always \"month\": the triangle is built from\nsignup months and active months, and no other bucketing is offered.",
			"retentionGrid.periods":             "Periods is the longest Values length across the cohorts — the width of the\ntriangle, so a renderer can size the grid before walking it.",
		},
		Example:  json.RawMessage(`{"range":"30d"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"range":"30d","interval":"day","generatedAt":"2026-07-27T00:00:00Z","sources":[{"name":"iam","ok":true,"rows":2,"lastSync":"2026-07-27T00:00:00Z"}]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/applications", zip.Doc{
		Description: "Lists IAM applications for one owner org, forwarded VERBATIM from IAM's\napplication list. These are the platform's OIDC clients — the console reads clientId\noff each row.",
		Fields: map[string]string{
			"iamPageIn.owner":    "Owner is the org whose rows to read. Defaults to the admin org, which owns the\nplatform's roles and applications.",
			"iamPageIn.p":        "Page is the 1-based page number. Forwarded only when set — IAM applies its own\ndefault otherwise.",
			"iamPageIn.pageSize": "PageSize is rows per page. Forwarded only when set.",
			"iamRowsOut.data":    "Data is IAM's own row array, forwarded byte-for-byte and therefore left\nundescribed here: IAM's schema has one home, and a second copy of it in this\ndocument would be free to drift from the one IAM serves. An absent page is\n[], never null.",
			"iamRowsOut.msg":     "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"iamRowsOut.status":  "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"iamRowsOut.total":   "Total is IAM's count across all pages. Absent when the read failed.",
		},
		Example:  json.RawMessage(`{"owner":"admin","p":"1","pageSize":"50"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"owner":"admin","name":"hanzo-cloud","clientId":"cid"}],"total":1}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/bases", zip.Doc{
		Description: "Lists the tenant Base instances in the caller's window — a SuperAdmin sees every\ntenant's, anyone else only their own subtree's.\n\nThe scope is enforced TWICE: the upstream is asked for the caller's org, AND every row\nit returns is re-checked against the resolved scope. An upstream that ignored the\nfilter therefore degrades to empty, never to a cross-tenant leak.\n\nThe Base engine is being embedded into cloud; until it lands this proxies\nBASE_ADMIN_URL and, when that is unset, answers 200 with an empty list and msg saying\nso — the honest not-yet state, never fabricated instances.",
		Fields: map[string]string{
			"baseInstance.created": "Created is when the instance was created, as the engine reports it.",
			"baseInstance.name":    "Name is the instance's name at the Base engine.",
			"baseInstance.org":     "Org is the tenant that owns the instance, and the key the scope filter runs\non. A row whose org is empty or outside the caller's subtree is DROPPED for\na non-SuperAdmin, so an instance the engine reports without an org is\nvisible only to a super.",
			"baseInstance.plan":    "Plan is the plan the instance runs on, as the engine names it.",
			"baseInstance.region":  "Region is where it is hosted, as the engine names it.",
			"baseInstance.status":  "Status is the engine's own lifecycle word for the instance — running and its\nsiblings. The vocabulary belongs to the Base engine, which is why this layer\nforwards it rather than mapping it.",
			"baseInstance.url":     "URL is where the instance is reached.",
			"basesOut.data":        "Data is the instances in the caller's window, after the scope re-check. Null\nwhen Status is \"error\".",
			"basesOut.msg":         "Msg is the failure reason when Status is \"error\" (the engine was unreachable\nor answered non-2xx). On a SUCCESS it may still carry an advisory: \"the Base\nengine is not yet embedded on this deployment\" beside an empty list, which\nis the honest not-yet state rather than a fleet with no instances.",
			"basesOut.status":      "Status is \"ok\" or \"error\", at HTTP 200 either way. An unconfigured Base\nengine answers \"ok\", not \"error\" — see Msg.",
			"basesOut.total":       "Total is len(data): the caller's whole window after scope filtering,\nunpaginated. Absent when the read failed.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"name":"acme-base","org":"acme","url":"https://acme.base.hanzo.ai","status":"running","plan":"pro","region":"nyc3","created":"2026-03-01T00:00:00Z"}],"total":1}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/caps", zip.Doc{
		Description: "Reads one org's usage caps: its spend alerts plus the derived period\nspend, over/warn state and reset time.\n\nThese are the SAME rows the customer edits in their own console — a platform override\nand a customer budget are one model, not two.",
		Fields: map[string]string{
			"capIn.id":      "ID is the cap to edit or remove, from the path. Unused by the list and create ops.",
			"capIn.org":     "Org is the tenant to act on. Required for a SuperAdmin — they must name their\ntarget; ignored for a white-label admin, who always acts on their own org.",
			"rawOut.data":   "Data is the upstream's payload verbatim, left undescribed for the same reason\nas iamRowsOut: re-describing someone else's schema here would be a second copy\nof it, free to drift.",
			"rawOut.msg":    "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"rawOut.status": "Status is \"ok\" or \"error\", at HTTP 200 either way. It is THIS layer's verdict\non the forwarding; an upstream that answered carries its own status inside\ndata.",
			"rawOut.total":  "Total is the upstream's row count when it forwarded a list, and absent when it\nforwarded a single object — which is why it is optional rather than zero.",
		},
		Example:  json.RawMessage(`{"org":"acme"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"id":"cap_1","limitCents":100000,"enforce":true,"periodSpendCents":42000,"over":false,"warn":false,"resetsAt":"2026-08-01T00:00:00Z"}],"total":0}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/compute", zip.Doc{
		Description: "Rolls the fleet's compute usage up to one row per (org, app, project, kind):\nhow many distinct machines ran in the window, how many are still active, what they\nbilled, and when each group last emitted an event. The console folds these into its\norg → app → project tree.\n\nA machine counts as ACTIVE when its LATEST lifecycle event is not a terminal one\n(stop/destroy/terminate/delete/off/shutdown/expire and their past tenses) — the same\nfold the console applies, done in the warehouse so the count is over every machine and\nnot just the page.\n\nHonest-empty when the warehouse is not connected or hanzo.compute_usage is not\nprovisioned yet: an empty list, never a fabricated fleet.",
		Fields: map[string]string{
			"computeIn.kind":         "Kind narrows to one workload class (bot | machine | cluster | nodepool |\ncontainer | function | …). An OPEN spectrum matched as a plain string, lowercased\nto the warehouse's convention; empty means every kind.",
			"computeIn.org":          "Org narrows to one tenant. Empty means every tenant — this board is\ncross-tenant by nature.",
			"computeIn.range":        "Range is the lower time bound: 24h, 7d or 30d. Anything else reads as 30d.",
			"computeLeaf.active":     "Active is how many of them are still running — the ones whose LATEST\nlifecycle event is not a terminal one (stop, destroy, terminate, delete,\noff, shutdown, expire and their past tenses). Decided in the warehouse over\nevery machine, not over a page.",
			"computeLeaf.app":        "App is the application within that tenant, as the emitter recorded it.",
			"computeLeaf.kind":       "Kind is the workload class: bot, machine, cluster, nodepool, container,\nfunction and whatever else the emitter writes. An OPEN set, lowercased, not\nan enum — a bot is a machine running the agent, a machine is raw compute —\nso match it as a string and expect values this list does not have.",
			"computeLeaf.lastTs":     "LastTs is the most recent event in the group, RFC 3339 UTC. For a group with\nno active machines it is when the last one stopped.",
			"computeLeaf.machines":   "Machines is how many DISTINCT machines of that kind ran in the window,\ncounted by machine id. Not a count of events.",
			"computeLeaf.org":        "Org is the tenant these workloads belong to.",
			"computeLeaf.project":    "Project is the project within that app. With Org and App and Kind it forms\nthe group this row folds — the console's org to app to project tree is these\nfour columns.",
			"computeLeaf.spendCents": "SpendCents is what the group billed over the window, in US cents, summed\nfrom the per-event prices.",
			"computeOut.data":        "Data is one row per group, biggest spender first. Null when Status is\n\"error\"; empty when there is genuinely nothing, or when the warehouse is\nunavailable.",
			"computeOut.msg":         "Msg is the failure reason when Status is \"error\" — the warehouse rejected\nthe query. A warehouse that is not connected, or a usage table not\nprovisioned yet, is a SUCCESS carrying an empty list instead.",
			"computeOut.status":      "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"computeOut.total":       "Total is len(data): the roll-up is one row per group, unpaginated. Absent\nwhen the read failed.",
		},
		Example:  json.RawMessage(`{"kind":"bot","org":"acme","range":"7d"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"org":"acme","app":"support","project":"default","kind":"bot","machines":4,"active":2,"spendCents":900,"lastTs":"2026-07-26T18:00:00Z"}],"total":1}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/flags", zip.Doc{
		Description: "Reads the platform control-plane board: every runtime launch/release\nswitch (waitlist, public signup, subsystem activation, gateway limits, network ids)\nwith its LIVE value and where that value came from — a stored definition or the\ncompiled-in default.",
		Fields: map[string]string{
			"FlagBoard.auditUrl":     "AuditURL is where the change log for those writes lives.",
			"FlagBoard.configured":   "Configured is whether the definition stores opened. False means every value\nbelow is a default or an env fallback and a write will be refused.",
			"FlagBoard.engine":       "Engine names the evaluator that answered.",
			"FlagBoard.manageUrl":    "ManageURL is where the definitions behind these switches are read and written.",
			"FlagBoard.switches":     "Switches is every declared switch with the value in force.",
			"FlagSwitch.category":    "Category groups it on the board.",
			"FlagSwitch.description": "Description says what turning it does.",
			"FlagSwitch.env":         "Env names the environment variable supplying a fallback, when one does.",
			"FlagSwitch.key":         "Key is the flag's key.",
			"FlagSwitch.label":       "Label is the switch's name as an operator reads it.",
			"FlagSwitch.readOnly":    "ReadOnly marks a switch the board shows but cannot write. NOT omitempty: the\nboard it replaces always wrote the field, and dropping a false would change\nthe shape a rendered switch has.",
			"FlagSwitch.source":      "Source is where that value came from: \"flags\", \"env\" or \"default\".",
			"FlagSwitch.type":        "Type is \"bool\", \"int\" or \"string\". The field is spelled as the admin board\nalready spells it on the wire, because this type replaces flags.SwitchView\nin that response and a rename here would move a live API.",
			"FlagSwitch.value":       "Value is the value in force.",
			"flagsOut.data":          "Data is the whole control-plane board — after the write, on a write, so a\ncaller sees the effect of its own flip without a second read. Null when the\nop failed.",
			"flagsOut.msg":           "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"flagsOut.status":        "Status is \"ok\" or \"error\", at HTTP 200 either way.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"switches":[{"key":"waitlist.chat","category":"launch","label":"Chat waitlist","description":"Gate chat behind the waitlist","value":true,"source":"default"}]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/me", zip.Doc{
		Description: "Answers with the validated operator identity — who the console is signed in as,\nwhich tier they are, and how wide their tenant window is. The fields come from the\nsanitized identity headers the gate just read, so they are authoritative and never\nclient-forgeable; nothing is looked up.",
		Fields: map[string]string{
			"adminMe.displayName":  "DisplayName is the label to render. It carries the same value as Name today:\nthis answer is assembled from the sanitized identity headers, which carry no\nseparate display name.",
			"adminMe.email":        "Email is the operator's email, and the actor an audited admin write is\nrecorded under.",
			"adminMe.isSuperAdmin": "IsSuperAdmin is the platform-sudo fact: true iff Owner is the reserved admin\norg. That is IAM's IsSuperAdmin predicate and the ONE thing every\ncross-tenant surface checks — an org's own isAdmin is a different power and\nnever grants it.",
			"adminMe.isWhiteLabel": "IsWhiteLabel marks the admitted NON-super tier: an admin of an enabled\nwhite-label tenant org. Mutually exclusive with IsSuperAdmin (the gate lets\nexactly one tier through). The operator SPA reads it to render the SUBTREE\ncockpit — the fleet god-view nav (finance/revenue/metrics/o11y/providers) is\nhidden — while a super sees the whole fleet.",
			"adminMe.name":         "Name is the IAM user name — the login handle, unique within Owner.",
			"adminMe.owner":        "Owner is the IAM org this operator belongs to: the reserved admin org for a\nSuperAdmin, the tenant's own slug for a white-label admin. It is read off the\nvalidated principal the gate just checked, never from a client claim.",
			"adminMe.scopeOrgs":    "ScopeOrgs is the caller's visible tenant window: empty for a SuperAdmin (means\nALL orgs), or the WL tenant's own subtree (today the singleton {org}). The SPA\nthreads it through the faceting/drill-down layer so a WL tenant can never widen\na filter past their subtree.",
			"meOut.data":           "Data is the caller's own identity and tenant window. Null when Status is\n\"error\".",
			"meOut.msg":            "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"meOut.status":         "Status is \"ok\" or \"error\". An error here is still HTTP 200 — the transport\nreads this field, not the status line.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"owner":"admin","name":"z","email":"z@hanzo.ai","displayName":"z","isSuperAdmin":true,"isWhiteLabel":false}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/money", zip.Doc{
		Description: "moneyBoardHandler answers GET /v1/admin/money.",
		Fields: map[string]string{
			"MoneyOut.data":                     "Data is the consolidated board. Omitted when the caller was refused.",
			"MoneyOut.msg":                      "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"MoneyOut.status":                   "Status is \"ok\" or \"error\", at HTTP 200 either way. A degraded upstream still\nanswers \"ok\" — it is reported in data.sources, because a consolidated board\nthat fails whole because one source is down is useless.",
			"SourceStatus.at":                   "At is when the aggregator ran, RFC3339. It is the READ's timestamp, shared by every\nrow in the list — not a per-source last-success time, so it never implies a stale\nsource is fresh.",
			"SourceStatus.error":                "Error is why the read failed, and is empty exactly when OK is true.",
			"SourceStatus.name":                 "Name identifies the upstream read, dotted by system: \"do.volumes\", \"do.balance\",\n\"k8s.hanzo-k8s\", \"iam.orgs\", \"billing.subscriptions\". It is stable, so a console\ncan keep per-source state across reads.",
			"SourceStatus.ok":                   "OK is whether that read succeeded. False is the whole point of this row: an\naggregator answers with what it got rather than failing, so the only way a reader\ncan tell a real zero from a missing source is here.",
			"SourceStatus.rows":                 "Rows is how many records came back. Zero with ok=true is a genuine empty result;\nzero with ok=false means nothing was read at all.",
			"Vendor.amountCents":                "Amount is the period's cost in USD cents, POSITIVE — money out is not negated\nhere. Summed across lines it is the platform COGS the margin subtracts.",
			"Vendor.note":                       "Note is commerce's own caveat on the line — usually how an estimate was reached.\nOmitted when there is none.",
			"Vendor.service":                    "Service is what we pay them FOR, so one vendor can bill several lines. Commerce\nchooses the vocabulary; this surface reports it verbatim.",
			"Vendor.source":                     "\"actual\" | \"estimated\"",
			"Vendor.vendor":                     "Name is who we pay — the vendor's own name (\"digitalocean\", \"anthropic\"). The wire\nspells it `vendor`; a line is identified by this together with Service.",
			"moneyBoard.byOrg":                  "ByOrg is one line per customer, biggest spender first. The row SET comes\nfrom revenue, so an org that was granted credit but no longer exists in IAM\nis inside the fleet totals above and has no line of its own.",
			"moneyBoard.credits":                "Credits is the granted-against-consumed ledger, including the balance\ncustomers still hold.",
			"moneyBoard.generatedAt":            "GeneratedAt is when the board was assembled, RFC 3339 UTC. Nothing here is\ncached.",
			"moneyBoard.infrastructure":         "Infra is what the platform costs to run: vendor COGS, the DigitalOcean\nburn-down, and the reserve backing the payout programs.",
			"moneyBoard.margin":                 "Margin is revenue against cost, carried through from the finance board\nunchanged so the two can never report different margins.",
			"moneyBoard.revenue":                "Revenue is what customers actually pay: realized consumption, recurring\ncontract value, and the counts behind them.",
			"moneyBoard.sources":                "Sources is one row per upstream, each name prefixed by the domain that read\nit (revenue:, finance:, grants:audit, treasury) — a bare \"commerce\" appears\nunder two domains and would otherwise collide. A not-ok row means the total\nit feeds is an UNDERCOUNT, not a smaller number.",
			"moneyCredits.consumedCents":        "ConsumedCents is realized spend, in US cents. A credit is consumed exactly\nwhen it is spent, so this is the same number revenue reports, stated once\nfrom one source.",
			"moneyCredits.grantedCents":         "GrantedCents is every credit successfully issued, in US cents, over the\nwhole grant history rather than a window. Refused grants are excluded — they\nmoved nothing.",
			"moneyCredits.grantedPrepaidCents":  "GrantedPrepaidCents is the half backed by real money the customer paid in, in\nUS cents. With GrantedTrialCents it sums to GrantedCents.",
			"moneyCredits.grantedTrialCents":    "GrantedTrialCents is the non-cash half — comps and promos we gave away — in\nUS cents. A grant whose source the ledger did not record counts here, since a\ngrant nobody paid for is the safer assumption.",
			"moneyCredits.grants":               "Grants is how many successful grants make up GrantedCents. If it reaches the\nscan bound the grants source is marked partial rather than the sum being\npresented as complete.",
			"moneyCredits.outstandingCents":     "OutstandingCents is the LIABILITY: prepaid and promo balance customers still\nhold and have not burned. It is not revenue, and it is not GrantedCents\nminus ConsumedCents — customers also add their own money.",
			"moneyInfra.doAvgDailyBurnCents":    "DOAvgDailyBurnCents is the average DigitalOcean spend per day, in US cents.\nDividing the credit above by it is the runway.",
			"moneyInfra.doCreditRemainingCents": "DOCreditRemainingCents is the promo credit left on the DigitalOcean account,\nin US cents — what is left to spend, not what was granted.",
			"moneyInfra.doMonthToDateCents":     "DOMonthToDateCents is DigitalOcean spend so far this month, in US cents.",
			"moneyInfra.period":                 "Period is the billing month the vendor COGS covers, \"2006-01\", as commerce\nreports it. It bounds VendorCogsCents only — the DigitalOcean figures below\nare month-to-date and the reserve is point-in-time.",
			"moneyInfra.treasuryReserveCents":   "TreasuryReserveCents is the platform reserve backing the payout programs, in\nUS cents, read live from the treasury app. Zero here can mean an empty\nreserve OR an unreachable treasury — the treasury row in sources is what\ntells them apart.",
			"moneyInfra.vendorCogsCents":        "VendorCogsCents is what the platform owes its vendors for Period, in US\ncents, POSITIVE — money out is not negated. This is the COGS the margin\nsubtracts.",
			"moneyInfra.vendors":                "Vendors is the COGS broken out per vendor and service line for Period, each\namount positive.",
			"moneyMargin.grossCents":            "GrossCents is realized revenue minus vendor COGS, in US cents. Negative\nmeans the fleet spent more on vendors than its customers consumed. GROSS\nonly: salaries, tooling and everything outside vendor COGS are not in it.",
			"moneyMargin.grossPct":              "GrossPct is that margin as a percentage of revenue — 42.5 means 42.5%, not\n0.425. Zero when revenue is zero, which is arithmetic rather than a claim of\nbreak-even.",
			"moneyMargin.profitable":            "Profitable is revenue strictly greater than COGS — the sign of GrossCents\nstated outright, so no consumer has to decide what zero means.",
			"moneyMargin.runwayDays":            "RunwayDays is DigitalOcean promo credit divided by average daily burn: how\nlong the credit lasts at today's rate, and nothing to do with the margin\nabove. NULL — never a large number — when DigitalOcean is unconfigured or\nburn is zero, because no honest projection exists and an infinity would read\nas safety.",
			"moneyOrgRow.balanceCents":          "BalanceCents is the prepaid wallet the tenant still holds, in US cents —\nmoney not yet earned.",
			"moneyOrgRow.display":               "Display is the org's display name, falling back to the slug.",
			"moneyOrgRow.grantedCents":          "GrantedCents is credit successfully granted TO this tenant, in US cents,\nacross all history. Zero for a tenant that was never granted any.",
			"moneyOrgRow.grants":                "Grants is how many grants that was.",
			"moneyOrgRow.mrrCents":              "MRRCents is the tenant's monthly recurring revenue, in US cents. Contract\nvalue, not cash collected.",
			"moneyOrgRow.org":                   "Org is the tenant slug — the row's identity.",
			"moneyOrgRow.plan":                  "Plan is the live subscription tier's name, or \"pay-as-you-go\" — which is\nalso what an org reads as when the subscriptions read failed, so it is not\nproof of no plan.",
			"moneyOrgRow.spendCents":            "SpendCents is realized consumption over the trailing 30 days, in US cents.\nThe board sorts on it, largest first.",
			"moneyRevenue.arpuCents":            "ARPUCents is realized spend divided by PAYING customers, in US cents — per\npaying customer, not per signup, so free signups cannot deflate it.",
			"moneyRevenue.arrCents":             "ARRCents is MRRCents times twelve, in US cents. A projection of today's\ncontracts, not a measurement of a year.",
			"moneyRevenue.customers":            "Customers is every org IAM lists, paying or not.",
			"moneyRevenue.mrrCents":             "MRRCents is fleet monthly recurring revenue, in US cents — contract value\nfrom subscriptions. Orthogonal to RealizedCents and never added to it.",
			"moneyRevenue.paying":               "Paying is how many of them have spend or MRR above zero. It is the ARPU\ndenominator.",
			"moneyRevenue.realizedCents":        "RealizedCents is credit customers have actually CONSUMED, fleet-wide, in US\ncents. This is the revenue figure — MRR beside it is contract value and the\ntwo are never added.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/o11y", zip.Doc{
		Description: "Is the fleet-wide observability board: LLM usage (requests, tokens, cost,\nerrors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms,\nerror rate, top services), fleet log volume, and the O11yAI generation rollup — all\naggregated across EVERY tenant, with no org filter applied.\n\nEvery signal degrades INDEPENDENTLY. A table that is absent or errors contributes its\nzero value and the read still succeeds, so the board renders exactly what the\nwarehouse holds rather than failing whole because one of four sources is missing.\nSame when the warehouse is not connected at all: the zero board, never a fabricated\nfleet.",
		Fields: map[string]string{
			"o11yGlobal.end":              "End is the moment of this read, RFC 3339 UTC.",
			"o11yGlobal.llm":              "LLM is the generation rollup over gen_ai spans. Its money is US DOLLARS,\nunlike Totals above — the two come from different planes and each keeps its\nplane's unit.",
			"o11yGlobal.logSeries":        "LogSeries is log volume over the SAME buckets as Series, so the two line up.",
			"o11yGlobal.range":            "Range is the window every figure below covers: 24h, 7d or 30d. It is the\nNORMALIZED value — an unrecognized ?range reads as 30d and says so here.",
			"o11yGlobal.series":           "Series is usage bucketed over the window, oldest first. A bucket with no\ntraffic has no point — the series is not padded to a fixed length.",
			"o11yGlobal.start":            "Start is the window's lower bound, RFC 3339 UTC, derived from Range.",
			"o11yGlobal.topModels":        "TopModels is the ten busiest models by request count.",
			"o11yGlobal.topOrgs":          "TopOrgs is the ten busiest tenants by request count.",
			"o11yGlobal.topServices":      "TopServices is the twelve busiest services by trace count.",
			"o11yGlobal.totals":           "Totals is the KPI band: the usage half, the trace half and log volume.",
			"o11yLLM.costUsd":             "CostUsd is their summed cost in US DOLLARS, not cents: the span attribute's\nnative unit, carried through unconverted.",
			"o11yLLM.generations":         "Generations is how many LLM calls the fleet made in the window — one per\ngen_ai span. It counts the same activity the usage totals do, from the trace\nplane instead of the billing ledger, so the two need not match exactly.",
			"o11yLogPoint.count":          "Count is the log lines emitted in that bucket, fleet-wide.",
			"o11yLogPoint.ts":             "Ts is the bucket's start, RFC 3339 UTC, on the same buckets as the usage\nseries so the two can be read together.",
			"o11yModelStat.costCents":     "CostCents is what they cost, in US cents.",
			"o11yModelStat.model":         "Model is the model id the ledger recorded. Unattributed rows carry no model\nand are left out rather than ranked as a nameless one.",
			"o11yModelStat.requests":      "Requests is how many calls went to it. The board ranks on this.",
			"o11yModelStat.tokens":        "Tokens is what those calls consumed.",
			"o11yOrgStat.costCents":       "CostCents is what they cost, in US cents.",
			"o11yOrgStat.org":             "Org is the tenant slug.",
			"o11yOrgStat.requests":        "Requests is how many calls that tenant made. The board ranks on this, so the\ntop rows are the busiest tenants and not necessarily the costliest.",
			"o11yOrgStat.tokens":          "Tokens is what those calls consumed.",
			"o11yOut.data":                "Data is the board. A warehouse that is not connected, or a table that is\nmissing, yields the ZERO board rather than a failure — so zeros here mean\neither a quiet fleet or a blind one, and this envelope cannot tell you\nwhich.",
			"o11yOut.msg":                 "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"o11yOut.status":              "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"o11ySeries.costCents":        "CostCents is what they cost, in US cents.",
			"o11ySeries.errors":           "Errors is how many of them failed.",
			"o11ySeries.requests":         "Requests is the calls served in that bucket.",
			"o11ySeries.tokens":           "Tokens is the tokens they consumed.",
			"o11ySeries.ts":               "Ts is the bucket's start, RFC 3339 UTC. The width follows the range — an\nhour at 24h, six hours at 7d, a day at 30d — so it is not a fixed unit.",
			"o11ySvcStat.errorRate":       "percent (0..100)",
			"o11ySvcStat.latencyP95Ms":    "LatencyP95Ms is that service's 95th-percentile span duration in\nmilliseconds.",
			"o11ySvcStat.requests":        "Requests is how many spans it emitted in the window. The board ranks on\nthis.",
			"o11ySvcStat.service":         "Service is the emitting service's name, from the span's own service column.\nThe whole cloud binary reports under ONE such name however many subsystems\nit mounts, which is what the per-subsystem board beside it exists to split\napart.",
			"o11yTotals.completionTokens": "CompletionTokens is the generated half of Tokens.",
			"o11yTotals.costCents":        "CostCents is what those calls cost, in US CENTS. The trace and gen_ai\nfigures elsewhere on this board are not in cents; this one is.",
			"o11yTotals.errors":           "Errors is how many of Requests the ledger marked failed.",
			"o11yTotals.latencyP50Ms":     "LatencyP50Ms is the median request duration in milliseconds, over spans\nrather than over ledger rows — so it covers every traced request, not only\nthe LLM ones counted above.",
			"o11yTotals.latencyP95Ms":     "LatencyP95Ms is the 95th percentile of the same durations.",
			"o11yTotals.latencyP99Ms":     "LatencyP99Ms is the 99th percentile — the tail worth paging on.",
			"o11yTotals.logVolume":        "LogVolume is how many log lines the fleet emitted in the window. Volume only\n— nothing here says how many of them were errors.",
			"o11yTotals.models":           "Models is how many distinct models were called.",
			"o11yTotals.orgs":             "Orgs is how many DISTINCT tenants appear in the window — the fleet's active\ntenant count, not a count of calls.",
			"o11yTotals.promptTokens":     "PromptTokens is the input half of Tokens.",
			"o11yTotals.requests":         "Requests is how many LLM calls the fleet served in the window, across every\ntenant, errored ones included. From the billing ledger.",
			"o11yTotals.services":         "Services is how many distinct services emitted a span in the window.",
			"o11yTotals.tokens":           "Tokens is prompt plus completion tokens across those calls.",
			"o11yTotals.traceCount":       "TraceCount is how many spans the fleet emitted in the window, across every\nservice. It counts TRACED REQUESTS, so it is a much larger number than\nRequests above and is not a superset of it: one is the trace plane, the other\nthe billing ledger.",
			"o11yTotals.traceErrorRate":   "percent (0..100)",
			"rangeIn.range":               "Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the\nboard's own default.",
		},
		Example:  json.RawMessage(`{"range":"7d"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"range":"7d","start":"2026-07-20T00:00:00Z","end":"2026-07-27T00:00:00Z","totals":{"requests":10420,"tokens":8100000,"costCents":41200,"errors":37},"series":[],"logSeries":[],"topOrgs":[],"topModels":[],"topServices":[],"llm":{"generations":0,"costUsd":0}}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/orgs", zip.Doc{
		Description: "Lists the tenant directory one row per org, sorted by slug: member count and the\norg's month-to-date spend and credit balance, read live from IAM and commerce.\n\nThe rows are the caller's tenant window, not the fleet: a SuperAdmin gets every org, a\nwhite-label admin only their own subtree. A per-org read that fails degrades THAT row\nto an honest zero — this panel carries no sources[] channel to report freshness on, so\nthe alternative would be a fleet total that silently reads healthy.",
		Fields: map[string]string{
			"orgRow.created":      "Created is when the org was created, as IAM records it (RFC 3339).",
			"orgRow.creditsCents": "CreditsCents is the credit balance the tenant still holds, in US cents, from\nthe commerce wallet. What they have left, not what they were given.",
			"orgRow.display":      "Display is the org's display name, falling back to the slug when IAM holds\nnone, so it is always renderable.",
			"orgRow.org":          "Org is the tenant slug — the key every other admin read joins on.",
			"orgRow.products":     "Products is always 0. The fleet workload registry is not attributed to\ntenants yet, and a count of the platform's own workloads on a tenant row\nwould be a number about somebody else.",
			"orgRow.spendCents":   "SpendCents is the tenant's AI spend over the trailing 30 days, in US cents,\nfrom the usage ledger. A tenant with no rows in that window reads a true zero.",
			"orgRow.tokens":       "Tokens is the tokens served for this tenant over that same 30-day window.",
			"orgRow.users":        "Users is the tenant's member count, from IAM's own total.",
			"orgsIn.p":            "Page is the 1-based page number. Defaults to \"1\".",
			"orgsIn.pageSize":     "PageSize is rows per page: 20 by default, 100 at most. It is deliberately\nbelow the 200 the rest of the admin surface uses, because a row here is not\na row of text — each one still costs its own wallet read. The page size IS\nthe fan-out width, so the directory costs the same at eighty tenants and at\neight thousand.",
			"orgsOut.data":        "Data is one row per tenant, sorted by slug. Null when the read failed, [] when\nthe caller's window is genuinely empty.",
			"orgsOut.msg":         "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"orgsOut.status":      "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"orgsOut.total":       "Total is the whole directory's row count BEFORE paging, so a console can page\nagainst it. Absent when the read failed, since there is then nothing to count.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"org":"acme","display":"Acme","users":7,"products":0,"spendCents":12500,"creditsCents":5000,"tokens":0,"created":"2026-01-04T00:00:00Z"}],"total":1}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/overview", zip.Doc{
		Description: "Is the Platform Overview tiles: how many orgs and users are in the caller's\ntenant window, the fleet workload counts, and month-to-date spend and credits.\n\nIt ALWAYS answers 200 — a tile board that fails as a whole because one upstream is\ndown is useless. Instead every upstream reports itself in sources[]: ok, degraded, or\nnot-configured. A commerce read that failed for ANY org marks that source degraded,\nbecause the spend/credits totals are then an undercount and must not read healthy.\n\nThe AI tiles — 30-day spend and tokens — come from the AI ledger (ledger.go), the\nplane that owns \"what was served\". They used to come from the money plane with the\ntoken counter hardcoded to zero, so the board read $0.00 and 0 tokens over a month in\nwhich the fleet served fifteen thousand requests. Credits still come from commerce,\nwhich owns the wallet.",
		Fields: map[string]string{
			"SourceStatus.at":             "At is when the aggregator ran, RFC3339. It is the READ's timestamp, shared by every\nrow in the list — not a per-source last-success time, so it never implies a stale\nsource is fresh.",
			"SourceStatus.error":          "Error is why the read failed, and is empty exactly when OK is true.",
			"SourceStatus.name":           "Name identifies the upstream read, dotted by system: \"do.volumes\", \"do.balance\",\n\"k8s.hanzo-k8s\", \"iam.orgs\", \"billing.subscriptions\". It is stable, so a console\ncan keep per-source state across reads.",
			"SourceStatus.ok":             "OK is whether that read succeeded. False is the whole point of this row: an\naggregator answers with what it got rather than failing, so the only way a reader\ncan tell a real zero from a missing source is here.",
			"SourceStatus.rows":           "Rows is how many records came back. Zero with ok=true is a genuine empty result;\nzero with ok=false means nothing was read at all.",
			"overviewData.activeProducts": "ActiveProducts is how many of those the operator reconciled to green health.\nThe gap against Products is everything not green, unknown health included.",
			"overviewData.creditsCents":   "CreditsCents is the credit balance customers still HOLD — the liability, in\nUS cents, summed over the window from the commerce wallet. It is not revenue\nand not spend.",
			"overviewData.drift":          "Drift is how many workloads the operator flagged as drifting — its declared\nimage and what is actually running disagree. It counts the operator's own\nverdict; this layer does not re-derive it.",
			"overviewData.lastSync":       "LastSync is when this answer was assembled, RFC 3339 UTC. admin caches\nnothing — the read IS the sync — so it is the time of this request and never\nof an earlier batch.",
			"overviewData.orgs":           "Orgs is how many tenants are in the caller's window: every org for a\nSuperAdmin, their own subtree for a white-label admin.",
			"overviewData.products":       "Products is how many workloads the operator is observing across the platform\nnamespaces — one per App CR.",
			"overviewData.sources":        "Sources is one row per upstream this board fanned out to — iam, commerce,\nusage, o11y, fleet — saying whether it answered, with how many rows, and why\nnot. It is what tells a zero above apart from a silence.",
			"overviewData.spendCents30d":  "SpendCents30d is AI spend over the trailing 30 days, in US cents, folded from\nthe usage ledger (one row per served request).",
			"overviewData.tokens30d":      "Tokens30d is total tokens served over that same 30-day window.",
			"overviewData.users":          "Users is the member count summed over those orgs. An org whose IAM read\nfailed contributes zero rather than failing the tile.",
			"overviewOut.data":            "Data is the tile board. Null only when the caller was refused outright.",
			"overviewOut.msg":             "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"overviewOut.status":          "Status is \"ok\" or \"error\", at HTTP 200 either way. This board answers \"ok\"\neven when upstreams are down — a tile board that fails whole because one\nsource is unreachable is useless — so a degraded read is reported in\ndata.sources, not here.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"orgs":2,"users":14,"products":31,"activeProducts":29,"drift":1,"spendCents30d":250000,"tokens30d":0,"creditsCents":10000,"lastSync":"2026-07-27T00:00:00Z","sources":[{"name":"iam","ok":true,"rows":2,"lastSync":"2026-07-27T00:00:00Z"}]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/products", zip.Doc{
		Description: "Lists the fleet workload registry: every operator App CR across the platform\nnamespaces with its declared vs running image tag, reconciled health/phase and drift\nverdict. Optionally narrowed by kind, tier or env, each an exact match.\n\nThe rows are the SAME observation /v1/platform/fleet renders — read through the in-process\nplatform client, not a second k8s client — so the two boards can never disagree about what\nthe fleet is. A PaaS plane that is not co-resident yields an honestly empty registry,\nnever a fabricated row.",
		Fields: map[string]string{
			"productRow.cluster":       "hanzo-k8s",
			"productRow.declaredTag":   "spec.image.tag on the App CR (declared truth)",
			"productRow.drift":         "any drift flag present",
			"productRow.driftSeverity": "ok|yellow|red (rolled-up)",
			"productRow.env":           "main|test|dev (lifecycle namespace)",
			"productRow.health":        "green|yellow|red|unknown",
			"productRow.kind":          "operator App CR spec.role (sql|kv|generic|ingress) or \"\"",
			"productRow.latestTag":     "newest released tag (GH release reader — empty until wired)",
			"productRow.name":          "Name is the operator App CR's name — the handle the operator, the PaaS board\nand this one all address the workload by. Unique within its namespace.",
			"productRow.namespace":     "k8s namespace",
			"productRow.org":           "image namespace (hanzoai|luxfi|docker.io/…)",
			"productRow.phase":         "operator status.phase (Running/Creating/…)",
			"productRow.repo":          "owner/repo image coordinate",
			"productRow.runningTag":    "observed from the live Deployment",
			"productRow.tier":          "derived: cloud|data|edge|daemon|paas|app (grouping)",
			"productRow.updated":       "Updated is always empty. The App CR carries no per-row reconcile timestamp and\nthis observation is taken live, so there is no moment to report; the time that\napplies to the whole answer is the overview's lastSync.",
			"productsIn.env":           "Env matches the lifecycle namespace (main|test|dev).",
			"productsIn.kind":          "Kind matches the operator App CR's declared spec.role (sql|kv|generic|ingress).",
			"productsIn.tier":          "Tier matches the derived infra grouping (cloud|data|edge|daemon|paas|app).",
			"productsOut.data":         "Data is the matching workload rows. Null when the read failed.",
			"productsOut.msg":          "Msg is the failure reason when Status is \"error\" — here, that the PaaS plane\ncould not be reached at all. A plane that answers with nothing observed yet is\na success carrying an empty list, which is a different fact.",
			"productsOut.status":       "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"productsOut.total":        "Total is len(data): the registry is the whole observed fleet after filtering,\nunpaginated. Absent when the read failed.",
		},
		Example:  json.RawMessage(`{"tier":"data","env":"main"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"name":"sql","kind":"sql","tier":"data","org":"hanzoai","cluster":"hanzo-k8s","env":"main","namespace":"hanzo","repo":"hanzoai/sql","phase":"Running","declaredTag":"v1.4.2","runningTag":"v1.4.2","latestTag":"","health":"green","drift":false,"driftSeverity":"ok","updated":""}],"total":1}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/promos", zip.Doc{
		Description: "Reads the current platform plan promo — the singleton discount offer, e.g.\nthe 50%-off launch promo. Commerce stores it in the reserved platform namespace, so\nthe org sent with the read is the admin org and the service token is what passes\ncommerce's own platform-admin gate.",
		Fields: map[string]string{
			"rawOut.data":   "Data is the upstream's payload verbatim, left undescribed for the same reason\nas iamRowsOut: re-describing someone else's schema here would be a second copy\nof it, free to drift.",
			"rawOut.msg":    "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"rawOut.status": "Status is \"ok\" or \"error\", at HTTP 200 either way. It is THIS layer's verdict\non the forwarding; an upstream that answered carries its own status inside\ndata.",
			"rawOut.total":  "Total is the upstream's row count when it forwarded a list, and absent when it\nforwarded a single object — which is why it is optional rather than zero.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"percentOff":50,"start":"2026-07-01T00:00:00Z","end":"2026-09-01T00:00:00Z","plans":["pro"],"active":true},"total":0}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/roles", zip.Doc{
		Description: "Lists IAM roles for one owner org, forwarded VERBATIM from IAM's role list.",
		Fields: map[string]string{
			"iamPageIn.owner":    "Owner is the org whose rows to read. Defaults to the admin org, which owns the\nplatform's roles and applications.",
			"iamPageIn.p":        "Page is the 1-based page number. Forwarded only when set — IAM applies its own\ndefault otherwise.",
			"iamPageIn.pageSize": "PageSize is rows per page. Forwarded only when set.",
			"iamRowsOut.data":    "Data is IAM's own row array, forwarded byte-for-byte and therefore left\nundescribed here: IAM's schema has one home, and a second copy of it in this\ndocument would be free to drift from the one IAM serves. An absent page is\n[], never null.",
			"iamRowsOut.msg":     "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"iamRowsOut.status":  "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"iamRowsOut.total":   "Total is IAM's count across all pages. Absent when the read failed.",
		},
		Example:  json.RawMessage(`{"owner":"admin","p":"1","pageSize":"50"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"owner":"admin","name":"ops","displayName":"Ops"}],"total":1}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/services", zip.Doc{
		Description: "Reads the launch board: every hosted service in the registry with its LIVE\nwaitlist mode, evaluated through the flag engine. This is the \"remove the waitlist one\nservice at a time\" view.",
		Fields: map[string]string{
			"ServiceRow.createdAt":     "CreatedAt is when the service was first registered, Unix seconds. A row from\nthe deployment's brand seed carries the boot that seeded it.",
			"ServiceRow.description":   "Description is one line saying what the service is.",
			"ServiceRow.displayName":   "DisplayName is the name the launch board shows.",
			"ServiceRow.hosts":         "Hosts is every request host that resolves to this service, normalized\n(lowercased, port stripped) and sorted. A host appears under exactly one\nservice; a request whose host is in no row is un-governed and passes through.",
			"ServiceRow.service":       "Service is the slug that identifies the service — lowercased, the registry's\nprimary key, and the name of the switch that governs it (waitlist.<service>).",
			"ServiceRow.updatedAt":     "UpdatedAt is when its metadata or hosts were last written, Unix seconds. It\ndoes NOT move when the waitlist mode is flipped — that write lands in the\nflag activity log, not here.",
			"ServiceRow.updatedBy":     "UpdatedBy is the email of the admin who last wrote the row, or \"seed\" for a\nrow this deployment created from its brand seed at boot.",
			"ServiceView.waitlistMode": "WaitlistMode is the mode IN FORCE: true means the service is gated to\napproved users, false means it is open to anyone. It is read from the\nwaitlist.<service> switch at the moment of the read, not stored on the\nregistry row, so it reflects a flip made anywhere in the fleet within one\nevaluation TTL.",
			"serviceList.services":     "Services is every registered service with its live waitlist mode.",
			"servicesOut.data":         "Data is the launch board. Null when the read failed.",
			"servicesOut.msg":          "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"servicesOut.status":       "Status is \"ok\" or \"error\", at HTTP 200 either way.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"services":[{"service":"chat","displayName":"Chat","description":"","hosts":["chat.hanzo.ai"],"waitlistMode":true}]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/subsystems", zip.Doc{
		Description: "subsystems answers GET /v1/admin/subsystems. ?range=24h|7d|30d bounds the telemetry\nwindow (default 30d) — the same enum, and the same helpers, as the o11y board.",
		Fields: map[string]string{
			"SourceStatus.at":               "At is when the aggregator ran, RFC3339. It is the READ's timestamp, shared by every\nrow in the list — not a per-source last-success time, so it never implies a stale\nsource is fresh.",
			"SourceStatus.error":            "Error is why the read failed, and is empty exactly when OK is true.",
			"SourceStatus.name":             "Name identifies the upstream read, dotted by system: \"do.volumes\", \"do.balance\",\n\"k8s.hanzo-k8s\", \"iam.orgs\", \"billing.subscriptions\". It is stable, so a console\ncan keep per-source state across reads.",
			"SourceStatus.ok":               "OK is whether that read succeeded. False is the whole point of this row: an\naggregator answers with what it got rather than failing, so the only way a reader\ncan tell a real zero from a missing source is here.",
			"SourceStatus.rows":             "Rows is how many records came back. Zero with ok=true is a genuine empty result;\nzero with ok=false means nothing was read at all.",
			"SubsystemsIn.range":            "Range bounds the telemetry window: 24h, 7d or 30d. Anything else, including\nempty, resolves to the default through the same window grammar the o11y board uses.",
			"SubsystemsOut.data":            "Data is the board. Null only when the caller was refused.",
			"SubsystemsOut.msg":             "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"SubsystemsOut.status":          "Status is \"ok\" or \"error\", at HTTP 200 either way. An absent warehouse still\nanswers \"ok\" with the full inventory — the telemetry gap is reported in\ndata.sources.",
			"subsystemBoard.end":            "End is the moment of this read, RFC 3339 UTC.",
			"subsystemBoard.range":          "Range is the telemetry window the RED columns cover: 24h, 7d or 30d,\nnormalized the same way the o11y board normalizes it. It does NOT bound the\ninventory — a subsystem appears whether or not it served anything.",
			"subsystemBoard.rows":           "Rows is one row per subsystem MOUNTED IN THIS PROCESS, in the composition\nroot's order. The row set comes from the mount index, not from the\nwarehouse, so a subsystem that served nothing appears as a real zero instead\nof vanishing.",
			"subsystemBoard.sources":        "Sources is one row per input: mount-index (always ok — it IS this process),\ntraces, and trace-errors. The last two go not-ok when the warehouse is\nabsent, which is what says \"telemetry unavailable\" instead of implying every\nsubsystem served nothing.",
			"subsystemBoard.start":          "Start is that window's lower bound, RFC 3339 UTC.",
			"subsystemBoard.totals":         "Totals is the KPI band above the table.",
			"subsystemRow.enabled":          "Enabled is whether it is switched ON in this process. Process-local and\nalways truthful — it needs no warehouse — which is what answers \"is that row\nempty because it is broken or because it is off\".",
			"subsystemRow.errorRate":        "percent (0..100)",
			"subsystemRow.errors":           "Errors is how many of those requests errored.",
			"subsystemRow.lastErrorAt":      "LastErrorAt is when this subsystem last errored in the window, RFC 3339 UTC.\nEmpty when it did not — or when the last-error read failed, which the\ntrace-errors source reports.",
			"subsystemRow.lastErrorMessage": "LastErrorMessage is the span's status message. Empty when the span recorded\nnone, which is common: a span can be marked errored without a message.",
			"subsystemRow.lastErrorRoute":   "LastErrorRoute is the route pattern that error was on, e.g. /v1/admin/orgs.\nEmpty when the span carried none.",
			"subsystemRow.lastErrorStatus":  "LastErrorStatus is the HTTP status code it answered, as a STRING — it is a\nspan attribute, and attribute values arrive as text.",
			"subsystemRow.latencyP50Ms":     "LatencyP50Ms is its median request duration in milliseconds.",
			"subsystemRow.latencyP95Ms":     "LatencyP95Ms is its 95th percentile in milliseconds.",
			"subsystemRow.latencyP99Ms":     "LatencyP99Ms is its 99th percentile in milliseconds.",
			"subsystemRow.name":             "Name is the subsystem's name in the mount index, and the label the request\nspan is stamped with. It is the join key between the inventory and the\nwarehouse: a warehouse row naming something this binary does not mount is\ndropped rather than shown.",
			"subsystemRow.prefixes":         "Prefixes is the route prefixes it answers on, e.g. /v1/admin. Always a list,\nnever null — empty means it mounts no HTTP routes of its own.",
			"subsystemRow.requests":         "Requests is the traced requests it served in the window. Zero when the\nwarehouse is unavailable, so read the board's sources before reading it as\nidle.",
			"subsystemRow.requestsPerMin":   "RequestsPerMin is Requests averaged over the WHOLE window, to 2dp — a flat\nmean, so a burst and a steady trickle of the same volume look alike here.",
			"subsystemTotals.disabled":      "Disabled is the rest. With Enabled it sums to Subsystems.",
			"subsystemTotals.enabled":       "Enabled is how many of them are switched ON.",
			"subsystemTotals.errorRate":     "percent (0..100)",
			"subsystemTotals.errors":        "Errors is how many of them errored.",
			"subsystemTotals.reporting":     "enabled AND served ≥1 traced request in the window",
			"subsystemTotals.requests":      "Requests is the traced requests every subsystem served in the window,\nsummed.",
			"subsystemTotals.subsystems":    "Subsystems is how many are mounted in this binary — the row count.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/usage", zip.Doc{
		Description: "Returns the trailing 30 days of AI usage: one org's when org names one, else the\nwhole fleet's — the spend, the tokens and the requests, the daily curve behind them,\nand the split by model.\n\nIt reads the AI ledger (ledger.go), which is the plane that owns this question. It used\nto ask the commerce billing API instead, once per org, and answer with a hardcoded\nempty series, zero tokens and zero requests, on the reasoning that a trend and a split\nwere \"not derivable from the commerce billing API\". They are not — but the question was\nnever commerce's. hanzo.cloud_usage carries a row per served request, so all three fall\nout of the same window the totals do.",
		Fields: map[string]string{
			"usageByModel.model":      "Model is the model id the ledger recorded. Rows the ledger left unattributed\nare dropped rather than rendered as a nameless slice.",
			"usageByModel.spendCents": "SpendCents is this model's share of the window's cost, in US cents.",
			"usageByModel.tokens":     "Tokens is this model's share of the tokens.",
			"usageData.byModel":       "ByModel is the window split by model, busiest first, capped at the top ten so\nthe ranking stays honest without paying for a tail nothing draws.",
			"usageData.series":        "Series is that window bucketed by day, oldest first. A day with no traffic has\nno point — the series is not padded.",
			"usageData.totals":        "Totals is the whole window folded into one row.",
			"usageIn.org":             "Org reads ONE tenant's trailing-30-day total instead of the fleet sum. Honoured\nfor a SuperAdmin only — a white-label admin always reads their own org.\n\nThe window is the one core.OrgMoney returns, and it is what the operator board\nbeside this already labelled (\"Daily, last 30 days\"). The wire used to say\nmonth-to-date while that UI said 30 days; they agree now. This comment is\nREGENERATED into plugin/admin/openapi.json and openapi.yaml as the ?org\nparameter description, so a stale word here ships as a contradiction inside\none spec file — which is the drift this whole change set exists to remove.",
			"usageOut.data":           "Data is the usage roll-up. A warehouse that is not connected yields zeros\nhere, not null — which reads as an idle fleet, so use the overview's\nsources[] to tell the two apart.",
			"usageOut.msg":            "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"usageOut.status":         "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"usagePoint.date":         "Date is the bucket's calendar day, YYYY-MM-DD in UTC. A day, not an instant:\nthe bucket covers the whole of it.",
			"usagePoint.requests":     "Requests is that day's served-request count.",
			"usagePoint.spendCents":   "SpendCents is that day's cost, in US cents.",
			"usagePoint.tokens":       "Tokens is that day's token count.",
			"usageTotals.requests":    "Requests is how many were served — including the ones that errored, which\nstill consumed the call.",
			"usageTotals.spendCents":  "SpendCents is what those requests cost, in US cents.",
			"usageTotals.tokens":      "Tokens is prompt plus completion tokens across them.",
		},
		Example:  json.RawMessage(`{"org":"acme"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"totals":{"spendCents":12500,"tokens":170000,"requests":42},"series":[{"date":"2026-08-13","spendCents":900,"tokens":12000,"requests":3}],"byModel":[{"model":"claude-opus-4-8","spendCents":9000,"tokens":80000}]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/users", zip.Doc{
		Description: "Lists the user directory across the caller's tenant window, one page at a time.\ntotal is IAM's REAL total, so the console can page through it.\n\nA SuperAdmin may aim the read at one tenant with org; a white-label admin cannot — for\nthem the owner is hard-pinned to their own org and org is ignored, which is what keeps\nthe directory from becoming a cross-tenant read.",
		Fields: map[string]string{
			"operatorUser.created":      "Created is when the account was created, as IAM records it (RFC 3339).",
			"operatorUser.displayName":  "DisplayName is the human name IAM holds. Often empty; render Name when it is.",
			"operatorUser.email":        "Email is the user's email address.",
			"operatorUser.forbidden":    "Forbidden is true when IAM has the account blocked: it exists and cannot sign\nin. Distinct from deleted, which simply would not appear here.",
			"operatorUser.isAdmin":      "IsAdmin is admin OF THEIR OWN ORG — they manage that org's users and apps. It\nis not platform privilege and grants nothing outside Owner.",
			"operatorUser.isSuperAdmin": "IsSuperAdmin is platform sudo: true iff Owner is the reserved admin org. It is\nderived here from Owner, which is the one predicate — never read off the user\nrow, so a tenant cannot set it on themselves.",
			"operatorUser.lastSignin":   "LastSignin is the last successful sign-in, as IAM records it. Empty for an\naccount that has never signed in — which is what an unused invite looks like.",
			"operatorUser.name":         "Name is the IAM user name — the login handle, unique within Owner.",
			"operatorUser.owner":        "Owner is the org this user belongs to. A user belongs to exactly one, so this\nis also the tenant column of the directory.",
			"operatorUser.tag":          "Tag is IAM's free-form label on the user. Empty unless the tenant uses it.",
			"usersIn.org":               "Org narrows the directory to ONE tenant. Honoured for a SuperAdmin only — a\nwhite-label admin is pinned to their own org and this is ignored.",
			"usersIn.p":                 "Page is the 1-based page number. Defaults to \"1\"; IAM returns zero rows AND a\nzero total when it is unset, so this layer never leaves it empty.",
			"usersIn.pageSize":          "PageSize is rows per page. Defaults to \"200\", the shared admin page size.",
			"usersIn.q":                 "Query is a free-text filter, matched by IAM as a \"contains\" over the user name.",
			"usersOut.data":             "Data is ONE page of users. Null when the read failed.",
			"usersOut.msg":              "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"usersOut.status":           "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"usersOut.total":            "Total is IAM's count across ALL pages, not len(data) — it is what the console\npages against. Absent when the read failed.",
		},
		Example:  json.RawMessage(`{"org":"acme","q":"ada","p":"1","pageSize":"50"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":[{"owner":"acme","name":"ada","email":"ada@acme.com","displayName":"Ada","isAdmin":true,"isSuperAdmin":false,"tag":"","created":"2026-01-04T00:00:00Z","lastSignin":"2026-07-01T09:12:00Z","forbidden":false}],"total":222}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/volumes", zip.Doc{
		Description: "Returns the realtime block-storage board: the DigitalOcean volume fleet\n(count, capacity, monthly list cost, per-volume region and attachment) plus the\nanalytics datastore's OWN fill, read from its system.disks.\n\nA volume's usedGiB and pct are null, always: DO exposes capacity and attachment but no\nfill, so the console renders \"—\" rather than a number nobody measured. The datastore\ncard is the one real fill here, and it is the number to scale on.\n\nThe two sources degrade independently — a DO outage still returns the datastore fill,\nand a disconnected datastore still returns the DO fleet. What a DO outage must NOT do\nis pass for an account with no volumes, so the fleet it could not read is marked\nincomplete rather than reported as a count of zero at a cost of zero.",
		Fields: map[string]string{
			"SourceStatus.at":                  "At is when the aggregator ran, RFC3339. It is the READ's timestamp, shared by every\nrow in the list — not a per-source last-success time, so it never implies a stale\nsource is fresh.",
			"SourceStatus.error":               "Error is why the read failed, and is empty exactly when OK is true.",
			"SourceStatus.name":                "Name identifies the upstream read, dotted by system: \"do.volumes\", \"do.balance\",\n\"k8s.hanzo-k8s\", \"iam.orgs\", \"billing.subscriptions\". It is stable, so a console\ncan keep per-source state across reads.",
			"SourceStatus.ok":                  "OK is whether that read succeeded. False is the whole point of this row: an\naggregator answers with what it got rather than failing, so the only way a reader\ncan tell a real zero from a missing source is here.",
			"SourceStatus.rows":                "Rows is how many records came back. Zero with ok=true is a genuine empty result;\nzero with ok=false means nothing was read at all.",
			"datastoreVolume.mount":            "Mount is its filesystem path on the datastore node.",
			"datastoreVolume.name":             "Name is the disk as the datastore names it.",
			"datastoreVolume.pct":              "Pct is UsedGiB as a percentage of SizeGiB, 0..100 to one decimal. The alert\nthresholds read this.",
			"datastoreVolume.sizeGiB":          "SizeGiB is its total capacity in GiB.",
			"datastoreVolume.usedGiB":          "UsedGiB is the space in use, in GiB to one decimal. This is the ONE real\nfill on the board and the number to scale on.",
			"storageAlert.level":               "Level is \"warn\" from 80% and \"critical\" from 90%. Below 80 no alert is\nemitted at all — there is no third level meaning fine.",
			"storageAlert.pct":                 "Pct is the fill that raised the alert, 0..100.",
			"storageAlert.volume":              "Volume is the volume the alert is about. Only the datastore carries a\nmeasured fill today, so it is the only name that appears here.",
			"storageFleet.count":               "Count is how many volumes the DigitalOcean account holds. A measurement only\nwhen the snapshot is complete — otherwise it is zero because nothing was\nread.",
			"storageFleet.monthlyUsd":          "MonthlyUsd is the fleet's block-storage cost in whole US DOLLARS per month —\ncapacity times DO's $0.10/GiB list price, rounded. A list-price estimate for\nthe ops budget, not an invoice line, and dollars rather than the cents the\nmoney boards use.",
			"storageFleet.pct":                 "Pct is null, for the same reason as UsedGiB.",
			"storageFleet.totalGiB":            "TotalGiB is their summed provisioned capacity in GiB.",
			"storageFleet.usedGiB":             "UsedGiB is null: with no per-volume fill there is no honest fleet total to\nsum.",
			"storageSnapshot.alerts":           "Alerts is the near-full warnings, derived from the datastore fill. Empty\nmeans nothing is near full AND also that there was no fill to judge —\nDatastore being null is what tells those apart.",
			"storageSnapshot.complete":         "Complete is whether the fleet roll-up may be read as a MEASUREMENT. False\nmeans DigitalOcean was not read, so the count, capacity and cost below are\nunknown rather than zero — and zero is exactly what an account with no\nvolumes looks like, which is why the distinction has to travel beside them.",
			"storageSnapshot.datastore":        "Datastore is the analytics datastore's own volume, the one card carrying a\nmeasured fill. NULL when the datastore is not connected or reported no\ncapacity — no card at all rather than a fabricated 0%.",
			"storageSnapshot.fleet":            "Fleet is the DigitalOcean roll-up: count, capacity and monthly list cost.",
			"storageSnapshot.incompleteReason": "IncompleteReason says which read did not happen and what that makes the\nnumbers, in a sentence fit to show an operator. Empty when Complete is true.",
			"storageSnapshot.sources":          "Sources is one row per upstream — do.volumes — with whether it answered, how\nmany rows it gave, and why not.",
			"storageSnapshot.volumes":          "Volumes is one row per DigitalOcean volume: capacity, region and attachment,\nwith fill absent.",
			"storageVolume.attached":           "Attached is whether at least one droplet has the volume mounted. A detached\nvolume still bills its full size, which is what makes this the reclaim\nsignal.",
			"storageVolume.id":                 "ID is DigitalOcean's own volume id.",
			"storageVolume.name":               "Name is the volume's name at DigitalOcean.",
			"storageVolume.pct":                "Pct is null on every row, for the same reason as UsedGiB. The console\nrenders an em dash rather than a number nobody took.",
			"storageVolume.region":             "Region is the DO region slug it lives in (nyc3, sfo3). A volume can only\nattach to a droplet in its own region.",
			"storageVolume.service":            "Service names the workload this volume backs. Empty on every row today:\nnothing tags a DO volume with its consumer, and guessing one from the name\nwould be a claim rather than a reading.",
			"storageVolume.sizeGiB":            "SizeGiB is the PROVISIONED capacity in GiB. It is what is billed, attached\nor not, used or not.",
			"storageVolume.usedGiB":            "UsedGiB is null on every row. DigitalOcean exposes capacity and attachment\nbut not fill, so nothing here has measured it — null means unmeasured, and\nis not 0.",
			"volumesOut.data":                  "Data is the board. Null only when the caller was refused.",
			"volumesOut.msg":                   "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"volumesOut.status":                "Status is \"ok\" or \"error\", at HTTP 200 either way. A DigitalOcean outage\nstill answers \"ok\": an operator reads this board DURING an incident, and the\ngap is named in data.incompleteReason rather than replacing the page.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"complete":true,"incompleteReason":"","sources":[{"name":"do.volumes","ok":true,"rows":2,"error":"","at":"2026-08-19T00:00:00Z"}],"fleet":{"count":2,"totalGiB":300,"usedGiB":null,"pct":null,"monthlyUsd":30},"datastore":{"name":"default","mount":"/var/lib/datastore","sizeGiB":200,"usedGiB":81.4,"pct":40.7},"volumes":[{"id":"v1","name":"datastore-data","region":"nyc3","sizeGiB":200,"usedGiB":null,"pct":null,"attached":true,"service":""}],"alerts":[]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin GET /v1/admin/waitlist", zip.Doc{
		Description: "Reads one waitlist's leaderboard from the Hanzo waitlist engine — position,\npoints and referral standing per entry — proxied server-authed with the engine secret,\nnever a client credential.\n\nThe engine's payload is forwarded VERBATIM as data; the console normalizes it. When\nthe engine is not configured on this deployment the read still succeeds, with an empty\nobject and a msg saying so, so the panel shows an honest not-wired state instead of an\nerror the operator would chase.",
		Fields: map[string]string{
			"rawOut.data":         "Data is the upstream's payload verbatim, left undescribed for the same reason\nas iamRowsOut: re-describing someone else's schema here would be a second copy\nof it, free to drift.",
			"rawOut.msg":          "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"rawOut.status":       "Status is \"ok\" or \"error\", at HTTP 200 either way. It is THIS layer's verdict\non the forwarding; an upstream that answered carries its own status inside\ndata.",
			"rawOut.total":        "Total is the upstream's row count when it forwarded a list, and absent when it\nforwarded a single object — which is why it is optional rather than zero.",
			"waitlistIn.page":     "Page is the 1-based page number.",
			"waitlistIn.pageSize": "PageSize is entries per page.",
			"waitlistIn.waitlist": "Waitlist is the waitlist slug to read (e.g. \"chat\"). The engine decides what an\nempty slug means.",
		},
		Example:  json.RawMessage(`{"waitlist":"chat","page":"1","pageSize":"50"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"entries":[{"email":"ada@acme.com","points":120,"position":7}],"total":842}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin PATCH /v1/admin/caps/:id", zip.Doc{
		Description: "Edits one cap by id — raise or lower the ceiling, flip enforcement. The\nbody is commerce's spend-alert patch contract, forwarded byte-for-byte.",
		Fields: map[string]string{
			"capIn.id":      "ID is the cap to edit or remove, from the path. Unused by the list and create ops.",
			"capIn.org":     "Org is the tenant to act on. Required for a SuperAdmin — they must name their\ntarget; ignored for a white-label admin, who always acts on their own org.",
			"rawOut.data":   "Data is the upstream's payload verbatim, left undescribed for the same reason\nas iamRowsOut: re-describing someone else's schema here would be a second copy\nof it, free to drift.",
			"rawOut.msg":    "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"rawOut.status": "Status is \"ok\" or \"error\", at HTTP 200 either way. It is THIS layer's verdict\non the forwarding; an upstream that answered carries its own status inside\ndata.",
			"rawOut.total":  "Total is the upstream's row count when it forwarded a list, and absent when it\nforwarded a single object — which is why it is optional rather than zero.",
		},
		Example:  json.RawMessage(`{"org":"acme","limitCents":250000,"enforce":false}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"id":"cap_1","limitCents":250000,"enforce":false},"total":0}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin POST /v1/admin/caps", zip.Doc{
		Description: "Sets a usage cap on one org — a platform override of a customer budget,\nwritten to the customer's own spend-alert rows. The body is commerce's spend-alert\ncontract, forwarded byte-for-byte.",
		Fields: map[string]string{
			"capIn.id":      "ID is the cap to edit or remove, from the path. Unused by the list and create ops.",
			"capIn.org":     "Org is the tenant to act on. Required for a SuperAdmin — they must name their\ntarget; ignored for a white-label admin, who always acts on their own org.",
			"rawOut.data":   "Data is the upstream's payload verbatim, left undescribed for the same reason\nas iamRowsOut: re-describing someone else's schema here would be a second copy\nof it, free to drift.",
			"rawOut.msg":    "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"rawOut.status": "Status is \"ok\" or \"error\", at HTTP 200 either way. It is THIS layer's verdict\non the forwarding; an upstream that answered carries its own status inside\ndata.",
			"rawOut.total":  "Total is the upstream's row count when it forwarded a list, and absent when it\nforwarded a single object — which is why it is optional rather than zero.",
		},
		Example:  json.RawMessage(`{"org":"acme","limitCents":100000,"enforce":true}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"id":"cap_1","limitCents":100000,"enforce":true},"total":0}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin POST /v1/admin/services", zip.Doc{
		Description: "Onboards a hosted service, or edits one, so a new host comes under the\nlaunch gate WITHOUT a redeploy. Re-registering an existing service PRESERVES its live\nswitch — editing the hosts of a service that is already open must not silently close\nit again.",
		Fields: map[string]string{
			"ServiceInput.description":  "Description is one line saying what the service is, for the board.",
			"ServiceInput.displayName":  "DisplayName is the name the launch board shows. It also titles the switch\n(\"Waitlist · <displayName>\") the first time the service is registered.",
			"ServiceInput.hosts":        "Hosts is every request host this service governs — \"chat.hanzo.ai\",\n\"hanzo.chat\". Each is normalized (lowercased, port stripped) and the set\nREPLACES the stored one, so an edit that omits a host stops governing it. A\nhost another service already claims is skipped: first claim wins.",
			"ServiceInput.service":      "Service is the slug to onboard or edit, lowercased on the way in. Required.\nIt is the registry key and it names the switch — waitlist.<service> — so a\nslug that already exists edits that service rather than creating a second.",
			"ServiceInput.waitlistMode": "WaitlistMode is the launch mode for a NEW service: true gates it to approved\nusers, false opens it. On a re-register it is IGNORED — the live switch is\npreserved, so editing the hosts of an opened service never silently closes it.",
			"ServiceRow.createdAt":      "CreatedAt is when the service was first registered, Unix seconds. A row from\nthe deployment's brand seed carries the boot that seeded it.",
			"ServiceRow.description":    "Description is one line saying what the service is.",
			"ServiceRow.displayName":    "DisplayName is the name the launch board shows.",
			"ServiceRow.hosts":          "Hosts is every request host that resolves to this service, normalized\n(lowercased, port stripped) and sorted. A host appears under exactly one\nservice; a request whose host is in no row is un-governed and passes through.",
			"ServiceRow.service":        "Service is the slug that identifies the service — lowercased, the registry's\nprimary key, and the name of the switch that governs it (waitlist.<service>).",
			"ServiceRow.updatedAt":      "UpdatedAt is when its metadata or hosts were last written, Unix seconds. It\ndoes NOT move when the waitlist mode is flipped — that write lands in the\nflag activity log, not here.",
			"ServiceRow.updatedBy":      "UpdatedBy is the email of the admin who last wrote the row, or \"seed\" for a\nrow this deployment created from its brand seed at boot.",
			"ServiceView.waitlistMode":  "WaitlistMode is the mode IN FORCE: true means the service is gated to\napproved users, false means it is open to anyone. It is read from the\nwaitlist.<service> switch at the moment of the read, not stored on the\nregistry row, so it reflects a flip made anywhere in the fleet within one\nevaluation TTL.",
			"serviceOne.service":        "Service is the row as it stands after the write, live mode included.",
			"serviceOut.data":           "Data is the one service the write touched, re-read after the write, so a\ncaller sees the effect of its own change without a second request. Null when\nthe write failed.",
			"serviceOut.msg":            "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"serviceOut.status":         "Status is \"ok\" or \"error\", at HTTP 200 either way.",
		},
		Example:  json.RawMessage(`{"service":"chat","displayName":"Chat","description":"Hanzo Chat","hosts":["chat.hanzo.ai"],"waitlistMode":true}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"service":{"service":"chat","displayName":"Chat","description":"Hanzo Chat","hosts":["chat.hanzo.ai"],"waitlistMode":true}}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin POST /v1/admin/services/:service/mode", zip.Doc{
		Description: "Flips ONE service's waitlist switch — the launch lever. Hot: it takes\neffect on this pod immediately and on peers within one evaluation TTL, with no\nredeploy. An unknown service is a 404, not a silent create; onboarding goes through\nupsertService.",
		Fields: map[string]string{
			"ServiceRow.createdAt":       "CreatedAt is when the service was first registered, Unix seconds. A row from\nthe deployment's brand seed carries the boot that seeded it.",
			"ServiceRow.description":     "Description is one line saying what the service is.",
			"ServiceRow.displayName":     "DisplayName is the name the launch board shows.",
			"ServiceRow.hosts":           "Hosts is every request host that resolves to this service, normalized\n(lowercased, port stripped) and sorted. A host appears under exactly one\nservice; a request whose host is in no row is un-governed and passes through.",
			"ServiceRow.service":         "Service is the slug that identifies the service — lowercased, the registry's\nprimary key, and the name of the switch that governs it (waitlist.<service>).",
			"ServiceRow.updatedAt":       "UpdatedAt is when its metadata or hosts were last written, Unix seconds. It\ndoes NOT move when the waitlist mode is flipped — that write lands in the\nflag activity log, not here.",
			"ServiceRow.updatedBy":       "UpdatedBy is the email of the admin who last wrote the row, or \"seed\" for a\nrow this deployment created from its brand seed at boot.",
			"ServiceView.waitlistMode":   "WaitlistMode is the mode IN FORCE: true means the service is gated to\napproved users, false means it is open to anyone. It is read from the\nwaitlist.<service> switch at the moment of the read, not stored on the\nregistry row, so it reflects a flip made anywhere in the fleet within one\nevaluation TTL.",
			"serviceModeIn.service":      "Service is the slug to flip, taken from the path.",
			"serviceModeIn.waitlistMode": "WaitlistMode is the new mode: true gates the service behind the waitlist, false\nopens it. This is the launch lever.",
			"serviceOne.service":         "Service is the row as it stands after the write, live mode included.",
			"serviceOut.data":            "Data is the one service the write touched, re-read after the write, so a\ncaller sees the effect of its own change without a second request. Null when\nthe write failed.",
			"serviceOut.msg":             "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"serviceOut.status":          "Status is \"ok\" or \"error\", at HTTP 200 either way.",
		},
		Example:  json.RawMessage(`{"waitlistMode":false}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"service":{"service":"chat","displayName":"Chat","description":"Hanzo Chat","hosts":["chat.hanzo.ai"],"waitlistMode":false}}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin POST /v1/admin/sync", zip.Doc{
		Description: "Answers the operator's \"Sync now\" button. There is nothing to kick: admin\naggregates LIVE on every read, so the button is just a re-read. It acknowledges\nhonestly with started:true rather than pretending a batch job was queued.",
		Fields: map[string]string{
			"syncOut.data":        "Data is the acknowledgement. Null when the caller was refused.",
			"syncOut.msg":         "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"syncOut.status":      "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"syncStarted.started": "Started is always true on a successful call. There is nothing to poll and\nnothing to cancel: the next read is the refresh.",
		},
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"started":true}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin POST /v1/admin/waitlist/boost", zip.Doc{
		Description: "Grants a user waitlist points, moving them up toward the access cutoff.\nThis is the access lever: the cutoff itself does not move, the person does.\n\nIt funnels through the engine's verified grant client (POST /v1/waitlist/award with\nsource=\"grant\" — the ONE path that honours an explicit points amount) and writes a\ntamper-evident audit row either way, so a FAILED grant is recorded too. The reason\nfield goes only to that row.",
		Fields: map[string]string{
			"rawOut.data":                   "Data is the upstream's payload verbatim, left undescribed for the same reason\nas iamRowsOut: re-describing someone else's schema here would be a second copy\nof it, free to drift.",
			"rawOut.msg":                    "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"rawOut.status":                 "Status is \"ok\" or \"error\", at HTTP 200 either way. It is THIS layer's verdict\non the forwarding; an upstream that answered carries its own status inside\ndata.",
			"rawOut.total":                  "Total is the upstream's row count when it forwarded a list, and absent when it\nforwarded a single object — which is why it is optional rather than zero.",
			"waitlistBoostRequest.email":    "Email identifies the entry to boost. Either this or RefCode is required.",
			"waitlistBoostRequest.points":   "Points is how many points to award. Must be positive — this client exists to move\nsomeone UP toward the cutoff.",
			"waitlistBoostRequest.reason":   "Reason is the operator's justification. Not sent to the engine; it is recorded on\nthe audit row, which is the point of asking for it.",
			"waitlistBoostRequest.refCode":  "RefCode identifies the entry by its referral code, when the email is unknown.",
			"waitlistBoostRequest.waitlist": "Waitlist is the waitlist slug the grant lands on. Required.",
		},
		Example:  json.RawMessage(`{"waitlist":"chat","email":"ada@acme.com","points":50,"reason":"design partner"}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"email":"ada@acme.com","points":170,"position":3}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin PUT /v1/admin/flags/:key", zip.Doc{
		Description: "Stores or overwrites ONE platform switch's definition and answers with the\nwhole board as it now stands. The flip is hot: this pod applies it immediately and\npeers converge within one evaluation TTL (15s by default), with no redeploy.\n\nThe body reaches the flag engine BYTE-FOR-BYTE — it is the engine's definition\nformat, not this layer's, so a field the engine understands and admin does not must\nstill arrive intact. setFlagIn names the two fields that matter for documentation; it\nis not a filter.\n\nThe write is recorded in the store's activity log against the caller's email.",
		Fields: map[string]string{
			"FlagBoard.auditUrl":     "AuditURL is where the change log for those writes lives.",
			"FlagBoard.configured":   "Configured is whether the definition stores opened. False means every value\nbelow is a default or an env fallback and a write will be refused.",
			"FlagBoard.engine":       "Engine names the evaluator that answered.",
			"FlagBoard.manageUrl":    "ManageURL is where the definitions behind these switches are read and written.",
			"FlagBoard.switches":     "Switches is every declared switch with the value in force.",
			"FlagSwitch.category":    "Category groups it on the board.",
			"FlagSwitch.description": "Description says what turning it does.",
			"FlagSwitch.env":         "Env names the environment variable supplying a fallback, when one does.",
			"FlagSwitch.key":         "Key is the flag's key.",
			"FlagSwitch.label":       "Label is the switch's name as an operator reads it.",
			"FlagSwitch.readOnly":    "ReadOnly marks a switch the board shows but cannot write. NOT omitempty: the\nboard it replaces always wrote the field, and dropping a false would change\nthe shape a rendered switch has.",
			"FlagSwitch.source":      "Source is where that value came from: \"flags\", \"env\" or \"default\".",
			"FlagSwitch.type":        "Type is \"bool\", \"int\" or \"string\". The field is spelled as the admin board\nalready spells it on the wire, because this type replaces flags.SwitchView\nin that response and a rename here would move a live API.",
			"FlagSwitch.value":       "Value is the value in force.",
			"flagsOut.data":          "Data is the whole control-plane board — after the write, on a write, so a\ncaller sees the effect of its own flip without a second read. Null when the\nop failed.",
			"flagsOut.msg":           "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"flagsOut.status":        "Status is \"ok\" or \"error\", at HTTP 200 either way.",
			"setFlagIn.active":       "Active is the switch itself: true enables the flag for every evaluation.",
			"setFlagIn.filters":      "Filters is the optional rollout/payload block of a VALUED switch, e.g.\n{\"groups\":[{\"properties\":[],\"rollout_percentage\":100}],\"payloads\":{\"true\":250}}.",
			"setFlagIn.key":          "Key is the switch to write, taken from the path (e.g. \"waitlist.chat\").",
		},
		Example:  json.RawMessage(`{"active":true,"filters":{"groups":[{"properties":[],"rollout_percentage":100}]}}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"switches":[{"key":"waitlist.chat","category":"launch","label":"Chat waitlist","description":"Gate chat behind the waitlist","value":true,"source":"stored"}]}}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/admin PUT /v1/admin/promos", zip.Doc{
		Description: "Upserts the platform plan promo — the ONE place the offer is configured.\n\nThe body is commerce's own promo contract and is forwarded BYTE-FOR-BYTE, so no field\ncommerce accepts is dropped in transit. promoIn names its documented fields.",
		Fields: map[string]string{
			"promoIn.active":     "Active is the master switch: false parks the offer without deleting it.",
			"promoIn.end":        "End is when the offer closes (RFC3339).",
			"promoIn.percentOff": "PercentOff is the discount, 0-100.",
			"promoIn.plans":      "Plans are the plan ids the offer applies to.",
			"promoIn.start":      "Start is when the offer opens (RFC3339).",
			"rawOut.data":        "Data is the upstream's payload verbatim, left undescribed for the same reason\nas iamRowsOut: re-describing someone else's schema here would be a second copy\nof it, free to drift.",
			"rawOut.msg":         "Msg is the failure reason when Status is \"error\", empty otherwise.",
			"rawOut.status":      "Status is \"ok\" or \"error\", at HTTP 200 either way. It is THIS layer's verdict\non the forwarding; an upstream that answered carries its own status inside\ndata.",
			"rawOut.total":       "Total is the upstream's row count when it forwarded a list, and absent when it\nforwarded a single object — which is why it is optional rather than zero.",
		},
		Example:  json.RawMessage(`{"percentOff":50,"start":"2026-07-01T00:00:00Z","end":"2026-09-01T00:00:00Z","plans":["pro"],"active":true}`),
		Response: json.RawMessage(`{"status":"ok","msg":"","data":{"percentOff":50,"start":"2026-07-01T00:00:00Z","end":"2026-09-01T00:00:00Z","plans":["pro"],"active":true},"total":0}`),
	})
}
