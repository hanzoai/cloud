// Code generated by zipdoc; DO NOT EDIT.

package lsp

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("POST /v1/lsp/complete", zip.Doc{
		Description: "Offers the candidates a language server has at a position, typed and\nresolved through the repository's dependencies rather than guessed from text.",
		Fields: map[string]string{
			"Answer.cold":         "Cold reports that this request paid to PREPARE the revision — the tree\nwrite, the dependency fetch and the language server's first index. It is\nthe billed event, surfaced so a caller can see what it was charged for.",
			"Answer.completions":  "Completions is complete's answer: the candidates at the position, typed and\nresolved through the repository's dependencies rather than guessed from text.",
			"Answer.diagnostics":  "Diagnostics is diagnostics' answer: every problem the server finds in the\nwhole file, position ignored. Empty means it found none.",
			"Answer.hover":        "Hover is hover's answer: the type and documentation as the language server\nitself renders them, so it is prose meant to be shown, not parsed.",
			"Answer.lang":         "Lang is the language the server that answered speaks (\"go\"), as the daemon\nreports it. Empty when the daemon named none.",
			"Answer.locations":    "Locations is locate's answer: where the symbol is defined, referenced, typed\nor implemented, per the relation asked for. Empty means the server resolved\nnothing there, which is an answer.",
			"Answer.op":           "Op is the question that was asked: hover, locate, symbols, diagnostics or\ncomplete. It names which result field below is the populated one.",
			"Answer.path":         "Path is the repo-relative file the question was about, echoed back.",
			"Answer.repo":         "Repo is the repository the question was about, echoed back.",
			"Answer.rev":          "Rev is the RESOLVED commit sha, never the branch or tag that was asked for.\nIt is what makes an answer re-askable: a branch moves, this does not.",
			"Answer.symbols":      "Symbols is symbols' answer: the file's whole outline, position ignored.",
			"Completion.detail":   "Detail is the server's short elaboration, typically the type or signature.",
			"Completion.kind":     "Kind is the LSP CompletionItemKind number (2 method, 3 function, 5 field, 6\nvariable, …), passed through as the protocol spells it.",
			"Completion.label":    "Label is the text a client would insert, and what an editor lists.",
			"Diagnostic.code":     "Code is the checker's own identifier for the rule, a string or a number\ndepending on the server. Absent when it published none.",
			"Diagnostic.message":  "Message is the problem in the server's own words, meant to be shown.",
			"Diagnostic.range":    "Range is the span the problem is about.",
			"Diagnostic.severity": "Severity is the LSP's: 1 error, 2 warning, 3 information, 4 hint. A file with\nonly 3s and 4s still compiles.",
			"Diagnostic.source":   "Source is which checker reported it (\"compiler\", \"go vet\", a linter's name),\nwhich is what separates a build error from a style opinion.",
			"Location.external":   "External is true when the answer left the repository — the case a static index\ncannot answer, and the reason this service resolves through dependencies.",
			"Location.path":       "Path is repo-relative while External is false, and the module coordinate\n(\"golang.org/x/mod@v0.14.0/semver/semver.go\") once it is true.",
			"Location.range":      "Range is the span inside that file, in LSP positions.",
			"Position.character":  "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification: not a byte offset and not a rune index. An emoji before the\ncursor counts as one here and as two in Go's arithmetic.",
			"Position.line":       "Line is 0-BASED, per the LSP specification — one less than the line an editor\nshows a human.",
			"Query.character":     "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification — not a byte offset and not a rune index.",
			"Query.line":          "Line is 0-based, per the LSP specification.",
			"Query.path":          "Path is the repo-relative file, e.g. \"apps/lsp/lsp.go\".",
			"Query.relation":      "Relation refines locate: definition, reference, type or implementation.\nEmpty means definition. Every other op ignores it.",
			"Query.repo":          "Repo is the repository NAME within the caller's own org, e.g. \"cloud\".\nNot a URL and not an owner/name pair: the owner is the validated\nprincipal's org, so this names a repository the caller already owns.",
			"Query.rev":           "Rev is a branch, tag or commit sha. Empty means the default branch. It is\nresolved to a commit before anything else happens, so an answer is always\nabout one immutable tree.",
			"Range.end":           "End is the position just past the span, excluded — the range is half-open, so\nan empty range has Start equal to End.",
			"Range.start":         "Start is the first position in the span, included.",
			"Symbol.detail":       "Detail is the server's short elaboration, typically the signature. Absent when\nit offered none.",
			"Symbol.kind":         "Kind is the LSP SymbolKind number (5 class, 6 method, 12 function, 23 struct,\n…), passed through rather than translated to a word — these callers already\nspeak LSP, and inventing a second vocabulary is how the two drift.",
			"Symbol.name":         "Name is the declared identifier.",
			"Symbol.range":        "Range is the declaration's span in the file.",
		},
		Example: json.RawMessage(`{"repo":"cloud","path":"apps/lsp/lsp.go","line":120,"character":18}`),
	})
	zip.Describe("POST /v1/lsp/diagnostics", zip.Doc{
		Description: "Reports every problem the language server finds in one file —\ncompile errors, type errors and lints, each with its span and its severity (1\nerror, 2 warning, 3 information, 4 hint). The position is ignored.",
		Fields: map[string]string{
			"Answer.cold":         "Cold reports that this request paid to PREPARE the revision — the tree\nwrite, the dependency fetch and the language server's first index. It is\nthe billed event, surfaced so a caller can see what it was charged for.",
			"Answer.completions":  "Completions is complete's answer: the candidates at the position, typed and\nresolved through the repository's dependencies rather than guessed from text.",
			"Answer.diagnostics":  "Diagnostics is diagnostics' answer: every problem the server finds in the\nwhole file, position ignored. Empty means it found none.",
			"Answer.hover":        "Hover is hover's answer: the type and documentation as the language server\nitself renders them, so it is prose meant to be shown, not parsed.",
			"Answer.lang":         "Lang is the language the server that answered speaks (\"go\"), as the daemon\nreports it. Empty when the daemon named none.",
			"Answer.locations":    "Locations is locate's answer: where the symbol is defined, referenced, typed\nor implemented, per the relation asked for. Empty means the server resolved\nnothing there, which is an answer.",
			"Answer.op":           "Op is the question that was asked: hover, locate, symbols, diagnostics or\ncomplete. It names which result field below is the populated one.",
			"Answer.path":         "Path is the repo-relative file the question was about, echoed back.",
			"Answer.repo":         "Repo is the repository the question was about, echoed back.",
			"Answer.rev":          "Rev is the RESOLVED commit sha, never the branch or tag that was asked for.\nIt is what makes an answer re-askable: a branch moves, this does not.",
			"Answer.symbols":      "Symbols is symbols' answer: the file's whole outline, position ignored.",
			"Completion.detail":   "Detail is the server's short elaboration, typically the type or signature.",
			"Completion.kind":     "Kind is the LSP CompletionItemKind number (2 method, 3 function, 5 field, 6\nvariable, …), passed through as the protocol spells it.",
			"Completion.label":    "Label is the text a client would insert, and what an editor lists.",
			"Diagnostic.code":     "Code is the checker's own identifier for the rule, a string or a number\ndepending on the server. Absent when it published none.",
			"Diagnostic.message":  "Message is the problem in the server's own words, meant to be shown.",
			"Diagnostic.range":    "Range is the span the problem is about.",
			"Diagnostic.severity": "Severity is the LSP's: 1 error, 2 warning, 3 information, 4 hint. A file with\nonly 3s and 4s still compiles.",
			"Diagnostic.source":   "Source is which checker reported it (\"compiler\", \"go vet\", a linter's name),\nwhich is what separates a build error from a style opinion.",
			"Location.external":   "External is true when the answer left the repository — the case a static index\ncannot answer, and the reason this service resolves through dependencies.",
			"Location.path":       "Path is repo-relative while External is false, and the module coordinate\n(\"golang.org/x/mod@v0.14.0/semver/semver.go\") once it is true.",
			"Location.range":      "Range is the span inside that file, in LSP positions.",
			"Position.character":  "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification: not a byte offset and not a rune index. An emoji before the\ncursor counts as one here and as two in Go's arithmetic.",
			"Position.line":       "Line is 0-BASED, per the LSP specification — one less than the line an editor\nshows a human.",
			"Query.character":     "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification — not a byte offset and not a rune index.",
			"Query.line":          "Line is 0-based, per the LSP specification.",
			"Query.path":          "Path is the repo-relative file, e.g. \"apps/lsp/lsp.go\".",
			"Query.relation":      "Relation refines locate: definition, reference, type or implementation.\nEmpty means definition. Every other op ignores it.",
			"Query.repo":          "Repo is the repository NAME within the caller's own org, e.g. \"cloud\".\nNot a URL and not an owner/name pair: the owner is the validated\nprincipal's org, so this names a repository the caller already owns.",
			"Query.rev":           "Rev is a branch, tag or commit sha. Empty means the default branch. It is\nresolved to a commit before anything else happens, so an answer is always\nabout one immutable tree.",
			"Range.end":           "End is the position just past the span, excluded — the range is half-open, so\nan empty range has Start equal to End.",
			"Range.start":         "Start is the first position in the span, included.",
			"Symbol.detail":       "Detail is the server's short elaboration, typically the signature. Absent when\nit offered none.",
			"Symbol.kind":         "Kind is the LSP SymbolKind number (5 class, 6 method, 12 function, 23 struct,\n…), passed through rather than translated to a word — these callers already\nspeak LSP, and inventing a second vocabulary is how the two drift.",
			"Symbol.name":         "Name is the declared identifier.",
			"Symbol.range":        "Range is the declaration's span in the file.",
		},
		Example: json.RawMessage(`{"repo":"cloud","path":"apps/lsp/lsp.go"}`),
	})
	zip.Describe("POST /v1/lsp/hover", zip.Doc{
		Description: "Renders the type and documentation of the symbol at a position, as the\nlanguage server itself renders it.\n\nPositions are the LSP's: line and character are 0-BASED and character counts\nUTF-16 code units, so an editor's 1-based line must have 1 subtracted before it\nis sent. The repository is named by slug and is always one in the caller's own\norg; rev pins a branch, tag or commit sha, and empty means the default branch.",
		Fields: map[string]string{
			"Answer.cold":         "Cold reports that this request paid to PREPARE the revision — the tree\nwrite, the dependency fetch and the language server's first index. It is\nthe billed event, surfaced so a caller can see what it was charged for.",
			"Answer.completions":  "Completions is complete's answer: the candidates at the position, typed and\nresolved through the repository's dependencies rather than guessed from text.",
			"Answer.diagnostics":  "Diagnostics is diagnostics' answer: every problem the server finds in the\nwhole file, position ignored. Empty means it found none.",
			"Answer.hover":        "Hover is hover's answer: the type and documentation as the language server\nitself renders them, so it is prose meant to be shown, not parsed.",
			"Answer.lang":         "Lang is the language the server that answered speaks (\"go\"), as the daemon\nreports it. Empty when the daemon named none.",
			"Answer.locations":    "Locations is locate's answer: where the symbol is defined, referenced, typed\nor implemented, per the relation asked for. Empty means the server resolved\nnothing there, which is an answer.",
			"Answer.op":           "Op is the question that was asked: hover, locate, symbols, diagnostics or\ncomplete. It names which result field below is the populated one.",
			"Answer.path":         "Path is the repo-relative file the question was about, echoed back.",
			"Answer.repo":         "Repo is the repository the question was about, echoed back.",
			"Answer.rev":          "Rev is the RESOLVED commit sha, never the branch or tag that was asked for.\nIt is what makes an answer re-askable: a branch moves, this does not.",
			"Answer.symbols":      "Symbols is symbols' answer: the file's whole outline, position ignored.",
			"Completion.detail":   "Detail is the server's short elaboration, typically the type or signature.",
			"Completion.kind":     "Kind is the LSP CompletionItemKind number (2 method, 3 function, 5 field, 6\nvariable, …), passed through as the protocol spells it.",
			"Completion.label":    "Label is the text a client would insert, and what an editor lists.",
			"Diagnostic.code":     "Code is the checker's own identifier for the rule, a string or a number\ndepending on the server. Absent when it published none.",
			"Diagnostic.message":  "Message is the problem in the server's own words, meant to be shown.",
			"Diagnostic.range":    "Range is the span the problem is about.",
			"Diagnostic.severity": "Severity is the LSP's: 1 error, 2 warning, 3 information, 4 hint. A file with\nonly 3s and 4s still compiles.",
			"Diagnostic.source":   "Source is which checker reported it (\"compiler\", \"go vet\", a linter's name),\nwhich is what separates a build error from a style opinion.",
			"Location.external":   "External is true when the answer left the repository — the case a static index\ncannot answer, and the reason this service resolves through dependencies.",
			"Location.path":       "Path is repo-relative while External is false, and the module coordinate\n(\"golang.org/x/mod@v0.14.0/semver/semver.go\") once it is true.",
			"Location.range":      "Range is the span inside that file, in LSP positions.",
			"Position.character":  "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification: not a byte offset and not a rune index. An emoji before the\ncursor counts as one here and as two in Go's arithmetic.",
			"Position.line":       "Line is 0-BASED, per the LSP specification — one less than the line an editor\nshows a human.",
			"Query.character":     "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification — not a byte offset and not a rune index.",
			"Query.line":          "Line is 0-based, per the LSP specification.",
			"Query.path":          "Path is the repo-relative file, e.g. \"apps/lsp/lsp.go\".",
			"Query.relation":      "Relation refines locate: definition, reference, type or implementation.\nEmpty means definition. Every other op ignores it.",
			"Query.repo":          "Repo is the repository NAME within the caller's own org, e.g. \"cloud\".\nNot a URL and not an owner/name pair: the owner is the validated\nprincipal's org, so this names a repository the caller already owns.",
			"Query.rev":           "Rev is a branch, tag or commit sha. Empty means the default branch. It is\nresolved to a commit before anything else happens, so an answer is always\nabout one immutable tree.",
			"Range.end":           "End is the position just past the span, excluded — the range is half-open, so\nan empty range has Start equal to End.",
			"Range.start":         "Start is the first position in the span, included.",
			"Symbol.detail":       "Detail is the server's short elaboration, typically the signature. Absent when\nit offered none.",
			"Symbol.kind":         "Kind is the LSP SymbolKind number (5 class, 6 method, 12 function, 23 struct,\n…), passed through rather than translated to a word — these callers already\nspeak LSP, and inventing a second vocabulary is how the two drift.",
			"Symbol.name":         "Name is the declared identifier.",
			"Symbol.range":        "Range is the declaration's span in the file.",
		},
		Example: json.RawMessage(`{"repo":"cloud","path":"apps/lsp/lsp.go","line":120,"character":18}`),
	})
	zip.Describe("POST /v1/lsp/locate", zip.Doc{
		Description: "Finds where a symbol lives: its definition, its references, its type or\nits implementations, chosen by relation (definition, reference, type,\nimplementation — empty means definition).\n\nIt resolves THROUGH dependencies. An answer whose external flag is set left the\nrepository, and its path is then the module coordinate it landed in — which is\nthe question a static index cannot answer and this service exists for.",
		Fields: map[string]string{
			"Answer.cold":         "Cold reports that this request paid to PREPARE the revision — the tree\nwrite, the dependency fetch and the language server's first index. It is\nthe billed event, surfaced so a caller can see what it was charged for.",
			"Answer.completions":  "Completions is complete's answer: the candidates at the position, typed and\nresolved through the repository's dependencies rather than guessed from text.",
			"Answer.diagnostics":  "Diagnostics is diagnostics' answer: every problem the server finds in the\nwhole file, position ignored. Empty means it found none.",
			"Answer.hover":        "Hover is hover's answer: the type and documentation as the language server\nitself renders them, so it is prose meant to be shown, not parsed.",
			"Answer.lang":         "Lang is the language the server that answered speaks (\"go\"), as the daemon\nreports it. Empty when the daemon named none.",
			"Answer.locations":    "Locations is locate's answer: where the symbol is defined, referenced, typed\nor implemented, per the relation asked for. Empty means the server resolved\nnothing there, which is an answer.",
			"Answer.op":           "Op is the question that was asked: hover, locate, symbols, diagnostics or\ncomplete. It names which result field below is the populated one.",
			"Answer.path":         "Path is the repo-relative file the question was about, echoed back.",
			"Answer.repo":         "Repo is the repository the question was about, echoed back.",
			"Answer.rev":          "Rev is the RESOLVED commit sha, never the branch or tag that was asked for.\nIt is what makes an answer re-askable: a branch moves, this does not.",
			"Answer.symbols":      "Symbols is symbols' answer: the file's whole outline, position ignored.",
			"Completion.detail":   "Detail is the server's short elaboration, typically the type or signature.",
			"Completion.kind":     "Kind is the LSP CompletionItemKind number (2 method, 3 function, 5 field, 6\nvariable, …), passed through as the protocol spells it.",
			"Completion.label":    "Label is the text a client would insert, and what an editor lists.",
			"Diagnostic.code":     "Code is the checker's own identifier for the rule, a string or a number\ndepending on the server. Absent when it published none.",
			"Diagnostic.message":  "Message is the problem in the server's own words, meant to be shown.",
			"Diagnostic.range":    "Range is the span the problem is about.",
			"Diagnostic.severity": "Severity is the LSP's: 1 error, 2 warning, 3 information, 4 hint. A file with\nonly 3s and 4s still compiles.",
			"Diagnostic.source":   "Source is which checker reported it (\"compiler\", \"go vet\", a linter's name),\nwhich is what separates a build error from a style opinion.",
			"Location.external":   "External is true when the answer left the repository — the case a static index\ncannot answer, and the reason this service resolves through dependencies.",
			"Location.path":       "Path is repo-relative while External is false, and the module coordinate\n(\"golang.org/x/mod@v0.14.0/semver/semver.go\") once it is true.",
			"Location.range":      "Range is the span inside that file, in LSP positions.",
			"Position.character":  "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification: not a byte offset and not a rune index. An emoji before the\ncursor counts as one here and as two in Go's arithmetic.",
			"Position.line":       "Line is 0-BASED, per the LSP specification — one less than the line an editor\nshows a human.",
			"Query.character":     "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification — not a byte offset and not a rune index.",
			"Query.line":          "Line is 0-based, per the LSP specification.",
			"Query.path":          "Path is the repo-relative file, e.g. \"apps/lsp/lsp.go\".",
			"Query.relation":      "Relation refines locate: definition, reference, type or implementation.\nEmpty means definition. Every other op ignores it.",
			"Query.repo":          "Repo is the repository NAME within the caller's own org, e.g. \"cloud\".\nNot a URL and not an owner/name pair: the owner is the validated\nprincipal's org, so this names a repository the caller already owns.",
			"Query.rev":           "Rev is a branch, tag or commit sha. Empty means the default branch. It is\nresolved to a commit before anything else happens, so an answer is always\nabout one immutable tree.",
			"Range.end":           "End is the position just past the span, excluded — the range is half-open, so\nan empty range has Start equal to End.",
			"Range.start":         "Start is the first position in the span, included.",
			"Symbol.detail":       "Detail is the server's short elaboration, typically the signature. Absent when\nit offered none.",
			"Symbol.kind":         "Kind is the LSP SymbolKind number (5 class, 6 method, 12 function, 23 struct,\n…), passed through rather than translated to a word — these callers already\nspeak LSP, and inventing a second vocabulary is how the two drift.",
			"Symbol.name":         "Name is the declared identifier.",
			"Symbol.range":        "Range is the declaration's span in the file.",
		},
		Example: json.RawMessage(`{"repo":"cloud","path":"apps/lsp/lsp.go","line":120,"character":18,"relation":"definition"}`),
	})
	zip.Describe("POST /v1/lsp/symbols", zip.Doc{
		Description: "Outlines one file: every declaration in it, with its kind and its span.\nThe position is ignored — the answer is the whole file.",
		Fields: map[string]string{
			"Answer.cold":         "Cold reports that this request paid to PREPARE the revision — the tree\nwrite, the dependency fetch and the language server's first index. It is\nthe billed event, surfaced so a caller can see what it was charged for.",
			"Answer.completions":  "Completions is complete's answer: the candidates at the position, typed and\nresolved through the repository's dependencies rather than guessed from text.",
			"Answer.diagnostics":  "Diagnostics is diagnostics' answer: every problem the server finds in the\nwhole file, position ignored. Empty means it found none.",
			"Answer.hover":        "Hover is hover's answer: the type and documentation as the language server\nitself renders them, so it is prose meant to be shown, not parsed.",
			"Answer.lang":         "Lang is the language the server that answered speaks (\"go\"), as the daemon\nreports it. Empty when the daemon named none.",
			"Answer.locations":    "Locations is locate's answer: where the symbol is defined, referenced, typed\nor implemented, per the relation asked for. Empty means the server resolved\nnothing there, which is an answer.",
			"Answer.op":           "Op is the question that was asked: hover, locate, symbols, diagnostics or\ncomplete. It names which result field below is the populated one.",
			"Answer.path":         "Path is the repo-relative file the question was about, echoed back.",
			"Answer.repo":         "Repo is the repository the question was about, echoed back.",
			"Answer.rev":          "Rev is the RESOLVED commit sha, never the branch or tag that was asked for.\nIt is what makes an answer re-askable: a branch moves, this does not.",
			"Answer.symbols":      "Symbols is symbols' answer: the file's whole outline, position ignored.",
			"Completion.detail":   "Detail is the server's short elaboration, typically the type or signature.",
			"Completion.kind":     "Kind is the LSP CompletionItemKind number (2 method, 3 function, 5 field, 6\nvariable, …), passed through as the protocol spells it.",
			"Completion.label":    "Label is the text a client would insert, and what an editor lists.",
			"Diagnostic.code":     "Code is the checker's own identifier for the rule, a string or a number\ndepending on the server. Absent when it published none.",
			"Diagnostic.message":  "Message is the problem in the server's own words, meant to be shown.",
			"Diagnostic.range":    "Range is the span the problem is about.",
			"Diagnostic.severity": "Severity is the LSP's: 1 error, 2 warning, 3 information, 4 hint. A file with\nonly 3s and 4s still compiles.",
			"Diagnostic.source":   "Source is which checker reported it (\"compiler\", \"go vet\", a linter's name),\nwhich is what separates a build error from a style opinion.",
			"Location.external":   "External is true when the answer left the repository — the case a static index\ncannot answer, and the reason this service resolves through dependencies.",
			"Location.path":       "Path is repo-relative while External is false, and the module coordinate\n(\"golang.org/x/mod@v0.14.0/semver/semver.go\") once it is true.",
			"Location.range":      "Range is the span inside that file, in LSP positions.",
			"Position.character":  "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification: not a byte offset and not a rune index. An emoji before the\ncursor counts as one here and as two in Go's arithmetic.",
			"Position.line":       "Line is 0-BASED, per the LSP specification — one less than the line an editor\nshows a human.",
			"Query.character":     "Character is a 0-based UTF-16 code-unit offset within Line, per the LSP\nspecification — not a byte offset and not a rune index.",
			"Query.line":          "Line is 0-based, per the LSP specification.",
			"Query.path":          "Path is the repo-relative file, e.g. \"apps/lsp/lsp.go\".",
			"Query.relation":      "Relation refines locate: definition, reference, type or implementation.\nEmpty means definition. Every other op ignores it.",
			"Query.repo":          "Repo is the repository NAME within the caller's own org, e.g. \"cloud\".\nNot a URL and not an owner/name pair: the owner is the validated\nprincipal's org, so this names a repository the caller already owns.",
			"Query.rev":           "Rev is a branch, tag or commit sha. Empty means the default branch. It is\nresolved to a commit before anything else happens, so an answer is always\nabout one immutable tree.",
			"Range.end":           "End is the position just past the span, excluded — the range is half-open, so\nan empty range has Start equal to End.",
			"Range.start":         "Start is the first position in the span, included.",
			"Symbol.detail":       "Detail is the server's short elaboration, typically the signature. Absent when\nit offered none.",
			"Symbol.kind":         "Kind is the LSP SymbolKind number (5 class, 6 method, 12 function, 23 struct,\n…), passed through rather than translated to a word — these callers already\nspeak LSP, and inventing a second vocabulary is how the two drift.",
			"Symbol.name":         "Name is the declared identifier.",
			"Symbol.range":        "Range is the declaration's span in the file.",
		},
		Example: json.RawMessage(`{"repo":"cloud","path":"apps/lsp/lsp.go"}`),
	})
}
