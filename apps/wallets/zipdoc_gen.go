// Code generated by zipdoc; DO NOT EDIT.

package wallets

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/wallets", zip.Doc{
		Description: "listWallets lists the caller's wallets, narrowed within the org. Project, agent\nand account are optional filters — the API face of the ONE scope lookup path —\nwhile org is always the bound isolation boundary, so a caller can never widen\npast its own org.",
		Fields: map[string]string{
			"walletFilter.account":      "Account narrows to one account grouping; empty means every account.",
			"walletFilter.agent":        "Agent narrows to one agent; empty means every agent.",
			"walletFilter.project":      "Project narrows to one org project; empty means every project.",
			"walletList.wallets":        "Wallets are the caller's own wallets matching the narrowings.",
			"walletView.accountId":      "AccountID is the account grouping the wallet belongs to.",
			"walletView.address":        "Address is the wallet's on-chain address, as custody provisioned it.",
			"walletView.agent":          "Agent is the agent the wallet is narrowed to, absent when unassigned.",
			"walletView.chain":          "Chain is the EVM chain the wallet addresses, empty when chain-agnostic.",
			"walletView.createdAt":      "CreatedAt is the creation time, unix seconds.",
			"walletView.custody":        "Custody is the signing backend: kms, mpc, treasury or safe.",
			"walletView.financeAccount": "FinanceAccount is the ledger account this wallet backs, absent when unbound.",
			"walletView.id":             "ID is the wallet id.",
			"walletView.name":           "Name is the human label for the wallet.",
			"walletView.org":            "Org is the owning tenant — the isolation boundary, never crossed.",
			"walletView.project":        "Project is the org project the wallet is narrowed to, absent when org-wide.",
			"walletView.tier":           "Tier is one of the nine wallet tiers, e.g. hot or cold.",
		},
		Response: json.RawMessage(`{"wallets":[{"id":"wal_3d81","org":"acme","accountId":"acct_9f2c","name":"ops hot wallet","custody":"kms","tier":"hot","chain":"36963","address":"0x5b1c…","createdAt":1780000000}]}`),
	})
	zip.Describe("GET /v1/wallets/:id", zip.Doc{
		Description: "getWallet reads one wallet of the caller's org. Another org's id is reported\nnot-found, so the wallet id space leaks no existence across tenants.",
		Fields: map[string]string{
			"walletRef.id":              "ID is the wallet id from the path.",
			"walletView.accountId":      "AccountID is the account grouping the wallet belongs to.",
			"walletView.address":        "Address is the wallet's on-chain address, as custody provisioned it.",
			"walletView.agent":          "Agent is the agent the wallet is narrowed to, absent when unassigned.",
			"walletView.chain":          "Chain is the EVM chain the wallet addresses, empty when chain-agnostic.",
			"walletView.createdAt":      "CreatedAt is the creation time, unix seconds.",
			"walletView.custody":        "Custody is the signing backend: kms, mpc, treasury or safe.",
			"walletView.financeAccount": "FinanceAccount is the ledger account this wallet backs, absent when unbound.",
			"walletView.id":             "ID is the wallet id.",
			"walletView.name":           "Name is the human label for the wallet.",
			"walletView.org":            "Org is the owning tenant — the isolation boundary, never crossed.",
			"walletView.project":        "Project is the org project the wallet is narrowed to, absent when org-wide.",
			"walletView.tier":           "Tier is one of the nine wallet tiers, e.g. hot or cold.",
		},
		Example: json.RawMessage(`{"id":"wal_3d81"}`),
	})
	zip.Describe("GET /v1/wallets/accounts", zip.Doc{
		Description: "listAccounts lists the wallet groupings the caller's org owns. Org is the bound\nisolation boundary, so another tenant's accounts are never returned.",
		Fields: map[string]string{
			"walletAccount.createdAt":    "CreatedAt is the creation time, unix seconds.",
			"walletAccount.id":           "ID is the account id, the value a wallet's accountId references.",
			"walletAccount.name":         "Name is the human label for the grouping.",
			"walletAccount.org":          "Org is the owning tenant.",
			"walletAccountList.accounts": "Accounts are the wallet groupings owned by the caller's org.",
		},
		Response: json.RawMessage(`{"accounts":[{"id":"acct_9f2c","org":"acme","name":"treasury","createdAt":1780000000}]}`),
	})
	zip.Describe("POST /v1/wallets", zip.Doc{
		Description: "createWallet provisions one wallet under an account of the caller's org. The\ncustody backend creates the signing material and returns its address; the\nwallet's scope (org, ambient project, agent, account) is what its key material\nis addressed by, so nothing here can reach another tenant's keys.",
		Fields: map[string]string{
			"createWalletReq.accountId": "AccountID is the account grouping the wallet belongs to. Required, and it\nmust already exist in the caller's org.",
			"createWalletReq.agent":     "Agent optionally narrows the wallet to one agent within the org.",
			"createWalletReq.chain":     "Chain is the EVM chain the wallet addresses; empty leaves it chain-agnostic.",
			"createWalletReq.custody":   "Custody selects the signing backend: kms, mpc, treasury or safe. Empty takes\nthe deployment's default; mpc/treasury/safe are 503 until the ring is wired.",
			"createWalletReq.name":      "Name is the human label for the wallet.",
			"createWalletReq.tier":      "Tier is one of the nine wallet tiers (hot, warm, cold, gas, bridge,\ncontract_admin, validator, quarantine, disaster_recovery). Empty means hot.",
			"walletView.accountId":      "AccountID is the account grouping the wallet belongs to.",
			"walletView.address":        "Address is the wallet's on-chain address, as custody provisioned it.",
			"walletView.agent":          "Agent is the agent the wallet is narrowed to, absent when unassigned.",
			"walletView.chain":          "Chain is the EVM chain the wallet addresses, empty when chain-agnostic.",
			"walletView.createdAt":      "CreatedAt is the creation time, unix seconds.",
			"walletView.custody":        "Custody is the signing backend: kms, mpc, treasury or safe.",
			"walletView.financeAccount": "FinanceAccount is the ledger account this wallet backs, absent when unbound.",
			"walletView.id":             "ID is the wallet id.",
			"walletView.name":           "Name is the human label for the wallet.",
			"walletView.org":            "Org is the owning tenant — the isolation boundary, never crossed.",
			"walletView.project":        "Project is the org project the wallet is narrowed to, absent when org-wide.",
			"walletView.tier":           "Tier is one of the nine wallet tiers, e.g. hot or cold.",
		},
		Example: json.RawMessage(`{"accountId":"acct_9f2c","name":"ops hot wallet","custody":"kms","tier":"hot","chain":"36963"}`),
	})
	zip.Describe("POST /v1/wallets/:id/keys", zip.Doc{
		Description: "rotateKeys rolls one wallet's signing material. The new material is created by\nthe wallet's own custody backend and its address persisted, while the wallet id\nand scope are unchanged, so every reference to the wallet keeps resolving.",
		Fields: map[string]string{
			"walletRef.id":              "ID is the wallet id from the path.",
			"walletView.accountId":      "AccountID is the account grouping the wallet belongs to.",
			"walletView.address":        "Address is the wallet's on-chain address, as custody provisioned it.",
			"walletView.agent":          "Agent is the agent the wallet is narrowed to, absent when unassigned.",
			"walletView.chain":          "Chain is the EVM chain the wallet addresses, empty when chain-agnostic.",
			"walletView.createdAt":      "CreatedAt is the creation time, unix seconds.",
			"walletView.custody":        "Custody is the signing backend: kms, mpc, treasury or safe.",
			"walletView.financeAccount": "FinanceAccount is the ledger account this wallet backs, absent when unbound.",
			"walletView.id":             "ID is the wallet id.",
			"walletView.name":           "Name is the human label for the wallet.",
			"walletView.org":            "Org is the owning tenant — the isolation boundary, never crossed.",
			"walletView.project":        "Project is the org project the wallet is narrowed to, absent when org-wide.",
			"walletView.tier":           "Tier is one of the nine wallet tiers, e.g. hot or cold.",
		},
		Example: json.RawMessage(`{"id":"wal_3d81"}`),
	})
	zip.Describe("POST /v1/wallets/:id/safe-tx", zip.Doc{
		Description: "proposeSafeTx composes propose and MPC-sign for a safe-custody wallet. The ring\ncomputes the EIP-712 Safe-tx hash — bound to the Safe contract and chain — and\nreturns it with the threshold (r,s) its MPC produced, which is the owner\napproval; any other custody kind is a 400.",
		Fields: map[string]string{
			"safeTxOut.r":           "R is the r component of the threshold signature.",
			"safeTxOut.s":           "S is the s component of the threshold signature.",
			"safeTxOut.safeAddress": "SafeAddress is the Safe contract address.",
			"safeTxOut.safeTxHash":  "SafeTxHash is the EIP-712 Safe-tx hash the ring computed.",
			"safeTxOut.walletId":    "WalletID is the safe-custody wallet the transaction was proposed for.",
			"safeTxReq.chainId":     "ChainID is the EVM chain the Safe is bound to; 0 takes the wallet's chain.",
			"safeTxReq.data":        "Data is the calldata, hex, empty for a plain transfer.",
			"safeTxReq.id":          "ID is the wallet id from the path; it must be a safe-custody wallet.",
			"safeTxReq.nonce":       "Nonce is the Safe's transaction nonce.",
			"safeTxReq.to":          "To is the destination address of the Safe transaction.",
			"safeTxReq.value":       "Value is the native-token amount, as a decimal string in base units.",
		},
		Example: json.RawMessage(`{"id":"wal_3d81","to":"0x5b1c9e7a3f4d20e8b6c1a9d7f3025e4b8c6a1d90","value":"1000000000000000000","data":"0x","chainId":36963,"nonce":7}`),
	})
	zip.Describe("POST /v1/wallets/:id/sign", zip.Doc{
		Description: "sign signs a 32-byte digest with one wallet of the caller's org. Signing runs in\nthat wallet's own custody backend, so the key material never leaves it; supply\ndigest directly, or message to have it hashed.",
		Fields: map[string]string{
			"signOut.address":   "Address is the wallet's on-chain address.",
			"signOut.digest":    "Digest is the 32-byte digest that was signed, 0x-prefixed hex.",
			"signOut.signature": "Signature is the produced signature, 0x-prefixed hex.",
			"signOut.walletId":  "WalletID is the wallet that signed.",
			"signReq.digest":    "Digest is a pre-computed 32-byte digest, hex, with or without the 0x prefix.",
			"signReq.id":        "ID is the wallet id from the path.",
			"signReq.message":   "Message is plain text to hash into the digest. Ignored when digest is set.",
		},
		Example: json.RawMessage(`{"id":"wal_3d81","message":"transfer 10 to 0x5b1c"}`),
	})
	zip.Describe("POST /v1/wallets/accounts", zip.Doc{
		Description: "createAccount creates a named wallet grouping in the caller's org. An account is\nthe addressing narrowing a wallet is later assigned to; it holds no key material.",
		Fields: map[string]string{
			"createAccountReq.name":   "Name is the human label for the grouping. Required.",
			"walletAccount.createdAt": "CreatedAt is the creation time, unix seconds.",
			"walletAccount.id":        "ID is the account id, the value a wallet's accountId references.",
			"walletAccount.name":      "Name is the human label for the grouping.",
			"walletAccount.org":       "Org is the owning tenant.",
		},
		Example: json.RawMessage(`{"name":"treasury"}`),
	})
}
