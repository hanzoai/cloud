// Code generated by zipdoc; DO NOT EDIT.

package code

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/code GET /v1/code/ask", zip.Doc{
		Description: "Answers a question about the caller org's code with a CITED answer:\nretrieval packs grounding context, then the synthesizer writes the answer over\nexactly those spans, which come back alongside it. It never answers without\ngrounding — with no matched code the answer is empty and says so, and with no\nsynthesizer available the citations still come back with \"degraded\": true so\nthe caller can reason over the spans itself.",
		Fields: map[string]string{
			"AskAnswer.answer":    "Answer is the synthesized prose. EMPTY is a real answer here: nothing in the\nindex matched, or synthesis was unavailable — read `degraded` and `citations`\nto tell those apart. It is never written without grounding.",
			"AskAnswer.citations": "Citations are the exact regions the answer was grounded on, and they are the\npoint: an answer is checkable only because every claim in it can be read back\nat a file and line. Present even when Answer is empty.",
			"AskAnswer.degraded":  "Degraded is true when retrieval worked but no synthesizer was reachable. The\ncitations are still real code, so a caller can answer from them itself; a\ncaller that treats this like an error throws away a usable result.",
			"AskAnswer.question":  "Question is the ask, echoed back.",
			"Citation.endLine":    "EndLine is its last line, inclusive.",
			"Citation.file":       "File is the path inside the repo, relative to its root.",
			"Citation.line":       "Line is the first line of the cited region, 1-based.",
			"Citation.repo":       "Repo is the repository the cited code lives in (\"owner/name\"), absent when the\nask was already scoped to one.",
			"Citation.symbol":     "Symbol is the declaration the region belongs to, when it belongs to one.",
			"askIn.q":             "Q is the question to answer. Required, max 4000 bytes.",
			"askIn.repo":          "Repo narrows retrieval to one repository. Empty searches every repo the org\nhas indexed.",
		},
		Example: json.RawMessage(`{"q":"where is the per-org SQLite file opened","repo":"cloud"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/code GET /v1/code/file", zip.Doc{
		Description: "Returns the INDEXED content of one file — read_file over the chunks the\nsearch tiers hold, for pulling up code an agent just found. It is NOT\nbyte-verbatim: the git object plane is the source of record for exact bytes,\nhistory and blame. A file absent from the index is a 404, so an agent can tell\n\"not indexed\" from \"empty file\".",
		Fields: map[string]string{
			"fileContent.content": "Content is the file's text as the index stored it. It is NOT guaranteed\nbyte-verbatim — the git object plane is the source of record for exact\nbytes, history and blame.",
			"fileContent.lang":    "Lang is the detected language.",
			"fileContent.path":    "Path echoes the file that was read.",
			"fileContent.repo":    "Repo echoes the repository it came from.",
			"fileIn.path":         "Path is the file's repo-relative path. Required.",
			"fileIn.repo":         "Repo is the repository the file belongs to. REQUIRED.",
		},
		Example: json.RawMessage(`{"repo":"cloud","path":"apps/code/store.go"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/code GET /v1/code/search", zip.Doc{
		Description: "Finds code in the caller org's index across three orthogonal retrieval\ntiers fused by reciprocal-rank fusion: lexical (FTS5 trigram over\ncode-tokenized text), symbolic (real definition and reference edges), and\nsemantic (embedding cosine over AST-boundary chunks). Pick one tier with\n`type`, or leave it to run all three as hybrid, which is what a coding agent\nusually wants. It is FAIL-HONEST: a retrieval outage answers 200 with an empty\nresult set and \"degraded\": true rather than a 5xx, so an agent degrades instead\nof stalling. A malformed regex is a 400.",
		Fields: map[string]string{
			"Span.endLine":           "EndLine is the last line of the span, inclusive. It equals Line for a\none-line span rather than being zero or absent.",
			"Span.file":              "File is the path inside the repo, relative to its root and never absolute.",
			"Span.kind":              "Kind is what the indexer decided this chunk IS — \"func\", \"method\", \"type\",\n\"struct\", \"interface\", \"var\", \"const\", or \"block\" for a run of code that\ndeclares nothing. Absent when the chunker could not classify it.",
			"Span.line":              "Line is where the span starts, 1-based, as an editor counts.",
			"Span.repo":              "Repo is the indexed repository the span was found in, as it was indexed\n(\"owner/name\"). A search may be scoped to one repo or run across all of them,\nso this is how a caller tells the results apart.",
			"Span.role":              "context: match | definition | caller",
			"Span.score":             "Score ranks this span against the OTHERS IN THE SAME RESPONSE and means\nnothing across responses or between tiers: the hybrid tier's number is a\nreciprocal-rank fusion sum (Σ 1/(60+rank), so tenths at best), the symbol\ntier's is a descending position count, and the text and semantic tiers pass\nthrough bm25 and cosine. Compare within a list; never threshold on it.",
			"Span.snippet":           "Snippet is the code itself: a bounded excerpt on /search, the whole chunk on\n/context — which is why the same type serves both and why a /context span is\nthe one an agent pastes into its window.",
			"Span.symbol":            "Symbol is the declared name, when the span declares one. Absent on a block.",
			"Span.tier":              "Tier is which retrieval produced the span: \"hybrid\" (the default — all three\nfused), \"text\" (trigram/FTS), \"regex\", \"semantic\" (vector), or \"symbol\". It is\nwhat explains a Score, so the two travel together.",
			"searchIn.limit":         "Limit caps how many spans come back: default 20, maximum 100. A value that\nis not a positive integer reads as the default.",
			"searchIn.q":             "Q is the search query. Required, max 4000 bytes. For type=regex it is a\nregular expression; for type=symbol it is a symbol name.",
			"searchIn.repo":          "Repo narrows to one repository. Empty searches every repo the org has indexed.",
			"searchIn.type":          "Type selects the retrieval tier: \"text\" (FTS5 trigram), \"regex\",\n\"symbol\" (definitions), \"semantic\" (embeddings) or \"hybrid\". Anything\nelse — including empty — reads as hybrid.",
			"searchResults.degraded": "Degraded is true when retrieval failed and the empty result set is an\noutage rather than a real absence of matches. Absent on a healthy answer.",
			"searchResults.query":    "Query echoes the query that was run.",
			"searchResults.results":  "Results are the matching spans, best first. Never null — an empty search is\nan empty array.",
			"searchResults.type":     "Type echoes the retrieval tier that ran, after defaulting.",
		},
		Example: json.RawMessage(`{"q":"func openStore","type":"hybrid","repo":"cloud","limit":20}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/code GET /v1/code/tree", zip.Doc{
		Description: "Returns one repository's file structure with a per-file symbol count —\nget_repo_structure over the org's own index, with no git checkout involved. A\nrepository that has not been indexed answers an empty tree rather than an\nerror, so an agent can tell \"nothing here\" without handling a failure.",
		Fields: map[string]string{
			"TreeEntry.lang":    "Lang is the language the indexer parsed the file as (\"go\", \"python\", …), or\nempty when it recognised none — in which case Symbols is 0 because nothing was\nextracted, not because the file declares nothing.",
			"TreeEntry.path":    "Path is the file, relative to the repo root. The list is ordered by it, so a\nreader can see module layout without sorting.",
			"TreeEntry.symbols": "Symbols is how many top-level declarations the file defines. A file with none\nis still listed: the file set is the authority here and the counts decorate it.",
			"repoTree.files":    "Files are the repo's indexed files in path order, each with its language\nand how many symbols it defines. Never null.",
			"repoTree.repo":     "Repo echoes the repository that was walked.",
			"treeIn.repo":       "Repo is the repository to walk. REQUIRED — a tree is repo-scoped.",
		},
		Example: json.RawMessage(`{"repo":"cloud"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/code POST /code/index", zip.Doc{
		Description: "Reconciles one repo's whole tree, pruning what the push removed.\n\nIT IS THE SAME PIPELINE the POST /v1/code/index handler runs — same per-file\ncaps, same prune semantics — because a second indexing path would drift from\nthe first and index differently depending on who asked.\n\nAn unmounted service answers an EMPTY reconcile rather than an error: this is a\nbackground enrichment reached from a push reactor, and a deployment that hosts\nno code index is not a fault in the push that landed.",
	})
	zip.Describe("github.com/hanzoai/cloud/apps/code POST /v1/code/ask", zip.Doc{
		Description: "Is askGet with the question in the request BODY, for a question too\nlong or too awkward to put in a URL. `query` and `repo` in the body take\nprecedence over `?q=` and `?repo=`; either source works alone.",
		Fields: map[string]string{
			"AskAnswer.answer":    "Answer is the synthesized prose. EMPTY is a real answer here: nothing in the\nindex matched, or synthesis was unavailable — read `degraded` and `citations`\nto tell those apart. It is never written without grounding.",
			"AskAnswer.citations": "Citations are the exact regions the answer was grounded on, and they are the\npoint: an answer is checkable only because every claim in it can be read back\nat a file and line. Present even when Answer is empty.",
			"AskAnswer.degraded":  "Degraded is true when retrieval worked but no synthesizer was reachable. The\ncitations are still real code, so a caller can answer from them itself; a\ncaller that treats this like an error throws away a usable result.",
			"AskAnswer.question":  "Question is the ask, echoed back.",
			"Citation.endLine":    "EndLine is its last line, inclusive.",
			"Citation.file":       "File is the path inside the repo, relative to its root.",
			"Citation.line":       "Line is the first line of the cited region, 1-based.",
			"Citation.repo":       "Repo is the repository the cited code lives in (\"owner/name\"), absent when the\nask was already scoped to one.",
			"Citation.symbol":     "Symbol is the declaration the region belongs to, when it belongs to one.",
			"askPostIn.query":     "Query is the question, from the BODY. Takes precedence over `?q=`.",
			"askPostIn.repo":      "Repo is the repository narrowing, from the BODY. Takes precedence over `?repo=`.",
		},
		Example: json.RawMessage(`{"query":"where is the per-org SQLite file opened","repo":"cloud"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/code POST /v1/code/context", zip.Doc{
		Description: "Packs the most relevant code for a query into a token budget — THE\nprimitive for a coding agent that has to decide what to put in a prompt. It\nretrieves seed spans, expands each with the definitions it calls and its key\ncallers, then greedily fills the budget, so the answer is a coherent slice of\nthe codebase rather than a list of disconnected matches. The top match is\nalways included, truncated if it alone overflows, so a matched query never\ncomes back empty. A retrieval outage answers 200 with an empty bundle rather\nthan a 5xx.",
		Fields: map[string]string{
			"ContextBundle.budgetTokens": "BudgetTokens is the ceiling the caller asked for. Packing stops under it, so\nthis is a bound and not a target.",
			"ContextBundle.query":        "Query is the ask this bundle was packed for, echoed back so a cached or\nforwarded bundle still says what it answers.",
			"ContextBundle.repo":         "Repo narrows the retrieval to one repository. Absent means every indexed repo\nwas searched.",
			"ContextBundle.spans":        "Spans are the packed chunks, most relevant first, each expanded with the\ndefinitions it calls and its notable callers. The top match is always present\neven if it had to be truncated to fit, so a matched query never comes back\nwith nothing.",
			"ContextBundle.usedTokens":   "UsedTokens is what the returned spans actually cost, by the same estimate the\npacker used (roughly one token per four characters — an estimate, not a\ntokenizer's count, so size a real window with headroom).",
			"Span.endLine":               "EndLine is the last line of the span, inclusive. It equals Line for a\none-line span rather than being zero or absent.",
			"Span.file":                  "File is the path inside the repo, relative to its root and never absolute.",
			"Span.kind":                  "Kind is what the indexer decided this chunk IS — \"func\", \"method\", \"type\",\n\"struct\", \"interface\", \"var\", \"const\", or \"block\" for a run of code that\ndeclares nothing. Absent when the chunker could not classify it.",
			"Span.line":                  "Line is where the span starts, 1-based, as an editor counts.",
			"Span.repo":                  "Repo is the indexed repository the span was found in, as it was indexed\n(\"owner/name\"). A search may be scoped to one repo or run across all of them,\nso this is how a caller tells the results apart.",
			"Span.role":                  "context: match | definition | caller",
			"Span.score":                 "Score ranks this span against the OTHERS IN THE SAME RESPONSE and means\nnothing across responses or between tiers: the hybrid tier's number is a\nreciprocal-rank fusion sum (Σ 1/(60+rank), so tenths at best), the symbol\ntier's is a descending position count, and the text and semantic tiers pass\nthrough bm25 and cosine. Compare within a list; never threshold on it.",
			"Span.snippet":               "Snippet is the code itself: a bounded excerpt on /search, the whole chunk on\n/context — which is why the same type serves both and why a /context span is\nthe one an agent pastes into its window.",
			"Span.symbol":                "Symbol is the declared name, when the span declares one. Absent on a block.",
			"Span.tier":                  "Tier is which retrieval produced the span: \"hybrid\" (the default — all three\nfused), \"text\" (trigram/FTS), \"regex\", \"semantic\" (vector), or \"symbol\". It is\nwhat explains a Score, so the two travel together.",
			"contextIn.budgetTokens":     "BudgetTokens caps the bundle's size. Clamped to [256, 32000]; 0 or absent\nuses 4000.",
			"contextIn.query":            "Query is what to retrieve context for. Required, max 4000 bytes.",
			"contextIn.repo":             "Repo narrows retrieval to one repository. Empty searches every repo the org\nhas indexed.",
		},
		Example: json.RawMessage(`{"query":"how does the store open a per-org database","budgetTokens":4000,"repo":"cloud"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/code POST /v1/code/index", zip.Doc{
		Description: "(re)indexes a repository for the caller's org, incrementally: files whose\ncontent hash is unchanged are skipped, so re-sending a whole tree is cheap.\nEach file is parsed for symbols, split at AST boundaries and — when the\nsemantic tier is available — embedded, which is what makes it searchable across\nall three retrieval tiers. Pass `prune` to also DELETE indexed files absent\nfrom the request, which turns the call into a full sync; without it the call is\nan upsert. The index is written to the caller org's own physically separate\ndatabase.",
		Fields: map[string]string{
			"fileInput.content":    "Content is the file's full text. Max 1 MiB per file; binary files should\nsimply be omitted rather than sent.",
			"fileInput.path":       "Path is the file's repo-relative path, e.g. \"internal/store/db.go\".",
			"indexIn.files":        "Files is the full set of files to index. Required and non-empty; max 20000\nfiles, 1 MiB per file and 1 GiB in total. Unchanged files are skipped by\ncontent hash, so re-sending the whole tree is cheap.",
			"indexIn.prune":        "Prune deletes indexed files that are NOT in this request — which makes the\ncall a full sync of the repo rather than an upsert. Only pass it when Files\nis the complete tree.",
			"indexIn.repo":         "Repo is the repository label to index under. Required, max 200 bytes. It is\na stored column value, not a filesystem path.",
			"indexResult.chunks":   "Chunks is how many AST-boundary chunks the repo holds after this pass.",
			"indexResult.files":    "Files is how many files the repo holds after this pass.",
			"indexResult.indexed":  "Indexed is how many files were parsed and written on this pass.",
			"indexResult.pruned":   "Pruned is how many stored files were deleted because prune was set and they\nwere absent from the request.",
			"indexResult.repo":     "Repo is the repository that was indexed.",
			"indexResult.semantic": "Semantic reports whether the semantic tier was available for this pass. When\nfalse the index is lexical + symbolic only and hybrid search still works.",
			"indexResult.skipped":  "Skipped is how many files were unchanged by content hash and left alone.",
			"indexResult.symbols":  "Symbols is how many symbol definitions the repo holds after this pass.",
			"indexResult.vectors":  "Vectors is how many of those chunks carry an embedding.",
		},
		Example: json.RawMessage(`{"repo":"cloud","files":[{"path":"main.go","content":"package main\n"}],"prune":true}`),
	})
}
