// Code generated by zipdoc; DO NOT EDIT.

package knowledge

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge DELETE /v1/knowledge/connectors/:provider", zip.Doc{
		Description: "Revokes a connection: it tombstones the stored credential\nso a later sync cannot reuse it, purges this provider's points from the org's\nvector namespace, and marks the connector disconnected. The documents already\ningested stay in the org's store — they are the org's own data — but stop being\nretrievable by search; a caller deletes them through the document surface.",
		Fields: map[string]string{
			"connectionOut.account":  "Account names the connected external account, when the provider reports one.",
			"connectionOut.provider": "Provider is the connector this answer is about.",
			"connectionOut.status":   "Status is the connection state: connected or disconnected.",
			"providerIn.provider":    "Provider is the connector to act on: github, slack, google or notion.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge GET /v1/knowledge/connectors", zip.Doc{
		Description: "Returns every supported knowledge connector with THIS org's\nconnection state and the REAL number of documents each has ingested into the\norg's store. A provider that is configured for the deployment but not yet\nconnected appears as disconnected, so the console can offer a Connect button.\nNo secret is ever returned.",
		Fields: map[string]string{
			"connectorView.account":      "Account names the connected external account. Absent until the org connects.",
			"connectorView.configured":   "Configured is true when this deployment holds OAuth credentials for the provider.",
			"connectorView.docCount":     "DocCount is the live count of this provider's documents in the org's store.",
			"connectorView.error":        "Error is the last sync failure, if any. Absent until the org connects.",
			"connectorView.kind":         "Kind is \"native\" for a first-party Go connector, \"piece\" for a long-tail one.",
			"connectorView.lastSync":     "LastSync is when the last pull finished. Absent until the org connects.",
			"connectorView.provider":     "Provider is the connector's id.",
			"connectorView.status":       "Status is connected, disconnected, syncing or error.",
			"kbConnectorsOut.connectors": "Connectors is every supported provider with this org's connection state.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge GET /v1/knowledge/connectors/:provider/callback", zip.Doc{
		Description: "CompleteConnectorOAuth finishes an OAuth connection: it exchanges the\nprovider's code for a token, seals that token in KMS, and records the\nconnection. THE ORG COMES FROM THE SIGNED STATE, not from a header and not from\nthe provider, so an attacker cannot bind their own account to someone else's\norg — a tampered, expired or foreign-provider state is refused outright. The\ntoken itself is never returned, never written into the document, and never\nlogged; the document holds only its KMS path.",
		Fields: map[string]string{
			"callbackIn.code":        "Code is the provider's authorization code, exchanged for a token.",
			"callbackIn.error":       "Error is the provider's denial reason when the user refused consent.",
			"callbackIn.provider":    "Provider is the connector completing its flow, from the path.",
			"callbackIn.state":       "State is the org-bound value this server signed at connect time.",
			"connectionOut.account":  "Account names the connected external account, when the provider reports one.",
			"connectionOut.provider": "Provider is the connector this answer is about.",
			"connectionOut.status":   "Status is the connection state: connected or disconnected.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge GET /v1/knowledge/connectors/:provider/connect", zip.Doc{
		Description: "StartConnectorOAuth returns the provider authorize URL the console opens to\nconnect this org's account. There is no server-side redirect — the console\nstays in control of the navigation. The URL carries a state this server SIGNED\nover the caller's validated org, so the connection the callback completes can\nonly ever land in that org.",
		Fields: map[string]string{
			"kbAuthorizeOut.authorizeUrl": "AuthorizeURL is the provider's authorize endpoint with an org-bound signed state.",
			"providerIn.provider":         "Provider is the connector to act on: github, slack, google or notion.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge GET /v1/knowledge/connectors/catalog", zip.Doc{
		Description: "Returns the ONE catalog of everything a caller can\nconnect: every first-party connector and every long-tail one, in a single list\nsorted by provider. `configured` reports whether this deployment holds OAuth\ncredentials for a source, so the console can show Connect rather than a dead\nbutton, and `kind` is a badge only — the connect and sync lifecycle is\nidentical for both. The catalog itself is org-independent; a validated\nprincipal is still required. It is metadata only: no secret is ever returned.",
		Fields: map[string]string{
			"catalogEntry.configured":  "Configured is whether THIS DEPLOYMENT holds the OAuth client credentials for\nthe provider. False means Connect would dead-end, so the console can offer it\ndisabled instead of broken. It is deployment-wide and says nothing about\nwhether the caller's org has connected the source — that is the connector\nlist's `status`.",
			"catalogEntry.description": "Description is one line of shop copy: what connecting this source pulls in.\nNative connectors carry written prose; a piece-backed one reads\n\"activepieces connector (<piece>)\".",
			"catalogEntry.displayName": "DisplayName is the label to show a person. First-party connectors carry a\nwritten name (\"GitHub\", \"Google Drive\"); a piece-backed one falls back to the\nprovider capitalized, because the rich activepieces metadata lives behind a\ncross-service call this read will not make.",
			"catalogEntry.kind":        "\"native\" | \"piece\"",
			"catalogEntry.provider":    "Provider is the source's id and the address every connector op takes it by\n(/v1/knowledge/connectors/:provider). One of github, slack, google, notion.",
			"catalogOut.connectors":    "Connectors is every connectable source, sorted by provider.",
		},
		Response: json.RawMessage(`{"connectors":[{"provider":"github","displayName":"GitHub","description":"Repositories, READMEs, and issues.","kind":"native","configured":true}]}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge GET /v1/knowledge/graph", zip.Doc{
		Description: "Returns the caller org's knowledge as a node/edge graph\nshaped for a force-directed renderer: pages, memories and synced sources as\nnodes; the page parent tree, the wikilinks between pages, and each source's\nconnector provenance as edges. Wikilink targets are resolved HERE by title or\nslug, so a rename never needs an edge rewrite and a link that matches no page\nrenders as its own \"unresolved\" node instead of vanishing. ?project= narrows\nit. A store outage degrades to an honest empty graph, never a 5xx.",
		Fields: map[string]string{
			"graphEdge.from":    "From is the id of the node the edge starts at: the child page for a parent\nedge, the page holding the wikilink for a link edge, the kb.source for a\nprovenance edge. Always one of Nodes.",
			"graphEdge.kind":    "parent | link | provenance",
			"graphEdge.to":      "To is the id of the node the edge points at: the parent page, the linked\npage, the kb.connector. Always one of Nodes — a wikilink matching no page\npoints at a synthetic \"unresolved:<lowercased title>\" node rather than\ndangling.",
			"graphIn.project":   "Project narrows the graph to one project scope. Empty reads the whole org.",
			"graphNode.id":      "\"<doctype>:<name>\" — globally unique, click-to-open key",
			"graphNode.name":    "the document name (empty for synthetic nodes)",
			"graphNode.project": "Project is the project scope the underlying document was saved under. Absent\nfor a document saved with none, and for the synthetic nodes — unresolved link\ntargets and connectors belong to no project. When ?project= narrows the\ngraph, every page, memory and source node carries that value.",
			"graphNode.title":   "display label",
			"graphNode.type":    "kb.page | kb.memory | kb.source | kb.connector | unresolved",
			"graphOut.degraded": "Degraded is true when the store was unreachable and this graph is honestly\nempty rather than wrong. Absent on a normal answer.",
			"graphOut.edges":    "Edges are the parent tree, the resolved wikilinks and the connector provenance.",
			"graphOut.nodes":    "Nodes are the pages, memories, sources, connectors and unresolved link targets.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge POST /v1/knowledge/connectors/:provider/sync", zip.Doc{
		Description: "Pulls the provider's documents for the caller's org and files\nthem as knowledge sources, which the store's own hook then indexes — so a\nsynced document is retrievable exactly like a hand-written page. The org is the\nvalidated tenant and the credential is read from KMS, so an org can only ever\nsync its own connection. A provider failure is reported honestly (502) and\nrecorded on the connector rather than silently swallowed.",
		Fields: map[string]string{
			"kbSyncOut.ingested":  "Ingested is how many documents landed in the org's knowledge store.",
			"kbSyncOut.provider":  "Provider is the connector that was pulled.",
			"providerIn.provider": "Provider is the connector to act on: github, slack, google or notion.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge POST /v1/knowledge/import", zip.Doc{
		Description: "Reads the uploaded export, dispatches to the format normalizer, and\nfiles the resulting pages. `format` (query) selects the normalizer; `project`\n(query, optional) scopes every imported page to a project.",
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge POST /v1/knowledge/reindex", zip.Doc{
		Description: "Rebuilds the caller org's retrieval from its documents: the vector\ncollection is dropped and created again at the configured embedding size and\nevery page, memory and source is embedded into it; the lexical index is\nreconciled to the same set. It is what an operator runs after the embedding\nmodel or its dimension changes, and what puts an org's retrieval right after\na vector outage. It requires ORG ADMIN and runs inline: an org's knowledge is\na few thousand documents, and the answer is the count.\n\nThe request has no body. Response: {\"vectors\": 412, \"lexical\": 412, \"removed\": 3, \"failed\": 0}",
		Fields: map[string]string{
			"reindexOut.failed":  "Failed is how many documents could not be embedded; each is logged with\nits name, and the rest of the rebuild went on without it.",
			"reindexOut.lexical": "Lexical is how many rows the org's lexical index holds now; 0 in a\ndeployment without the index app.",
			"reindexOut.removed": "Removed is how many rows the lexical index held for documents that no\nlonger exist; 0 without the index app.",
			"reindexOut.vectors": "Vectors is how many documents were embedded and written to the org's\ncollection, which was dropped and created again at the configured size.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/knowledge POST /v1/knowledge/search", zip.Doc{
		Description: "Runs a semantic search over the caller org's own knowledge —\nits wiki pages, its agent memories and everything its connectors have synced —\nand returns the matching passages. This is the RAG entry point: an agent asks\n\"what does this org know about X\" and the org's OWN vector namespace answers.\nThe org comes from the validated principal, and both the collection and the\npayload filter are pinned to it, so cross-tenant retrieval is impossible. An\nunreachable index returns an honest empty result set with degraded=true, never\na 5xx.",
		Fields: map[string]string{
			"hit.doctype":        "DocType is which kind of knowledge matched, by address: kb.page (a wiki\npage), kb.memory (a unit of agent memory) or kb.source (a document a\nconnector ingested).\nThose three are the whole indexed set, and searchIn.DocTypes filters on them.",
			"hit.name":           "Name is the document's name in the framework store — the id to read or open\nit with. Unique per (org, doctype), so it identifies the document with\nDocType and not alone.",
			"hit.project":        "Project is the project scope the document was saved under. Absent for a\ndocument saved with none, which is also why a project-scoped query cannot\nreach it.",
			"hit.provider":       "Provider is the connector that ingested the document — github, slack, google\nor notion. Absent for a page or memory written in the product, which came\nfrom no connector.",
			"hit.score":          "Score is the cosine similarity between the query's embedding and the\ndocument's, from -1 to 1, higher being closer — the collection is created\nwith Cosine distance. Hits arrive ordered by it, descending. There is no\nabsolute cutoff: what counts as a good score moves with the query and the\nembedding model, so compare scores within one response and not across\nqueries.",
			"hit.title":          "Title is the document's title as it was indexed. Empty for a document saved\nwithout one; it is a label to show, never the id (that is Name).",
			"hit.url":            "URL is the document's link back into the app it was ingested from. Absent\nwhen the indexed payload carries none, which is the normal case for pages and\nmemories.",
			"searchIn.doctypes":  "DocTypes restricts retrieval to a subset of the indexed knowledge doctypes\n(kb.page, kb.memory, kb.source). An empty or foreign list reads all of them.",
			"searchIn.limit":     "Limit bounds the hits returned. Default 10, maximum 50.",
			"searchIn.project":   "Project narrows retrieval to one project scope.",
			"searchIn.query":     "Query is the natural-language question. Required.",
			"searchOut.degraded": "Degraded is true when the index was unreachable and this answer is honestly\nempty rather than wrong — a RAG caller continues with no context instead of\nfailing the turn. Absent on a normal answer.",
			"searchOut.hits":     "Hits are the matching passages, most relevant first.",
		},
		Example: json.RawMessage(`{"query":"how do we rotate the signing key","limit":5}`),
	})
}
