// Code generated by zipdoc; DO NOT EDIT.

package channels

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/channels", zip.Doc{
		Description: "list returns every chat transport with the caller org's facts attached.\n\nThe registry is closed and the order is fixed, so an unconnected transport is\nstill listed — with connected:false, its default policy, and no account.\nEach row carries the org's connection, its DM and group policy, and how many\npairing requests are waiting for an admin.",
		Fields: map[string]string{
			"channelList.channels":       "Channels is every transport in the closed registry, in fixed order —\npresent whether or not the org has connected it.",
			"channelView.account":        "Account is the connected account's external id, lower-cased.",
			"channelView.accountLabel":   "AccountLabel is that account's human label — never swapped with Account.",
			"channelView.capabilities":   "Capabilities is what the transport renders natively.",
			"channelView.connected":      "Connected reports whether this org has an authenticated connection to it.",
			"channelView.dmPolicy":       "DMPolicy is who may DM the org on this channel: pairing, allowlist or open.",
			"channelView.groupPolicy":    "GroupPolicy governs group and thread surfaces: open, allowlist or disabled.",
			"channelView.id":             "ID is the transport id: discord, slack, teams or telegram.",
			"channelView.pendingPairing": "PendingPairing is how many unexpired pairing requests await approval here.",
		},
		Response: json.RawMessage(`{"channels":[{"id":"slack","connected":true,"account":"t01abc","accountLabel":"Acme","capabilities":{"dm":true,"group":true,"thread":true,"media":false,"actions":false},"dmPolicy":"pairing","groupPolicy":"open","pendingPairing":2}]}`),
	})
	zip.Describe("GET /v1/channels/allowlist", zip.Doc{
		Description: "allowlist returns one channel's access policy for the caller's org.\n\nIt answers with both allow sources: the config-source entries this surface\nowns, and the pairing-source entries approving a pairing minted, which are\nread-only here.\nA channel outside the closed transport registry is not found.",
		Fields: map[string]string{
			"allowlistView.accessGroups": "AccessGroups is the org's named access groups, keyed group then channel.",
			"allowlistView.dm":           "DM is the config-source DM allowlist — the entries this endpoint owns.",
			"allowlistView.dmPolicy":     "DMPolicy is who may DM the org on this channel.",
			"allowlistView.group":        "Group is the config-source group allowlist.",
			"allowlistView.groupPolicy":  "GroupPolicy governs group and thread surfaces on this channel.",
			"allowlistView.paired":       "Paired is the pairing-source entries, read-only here: they are minted by\napproving a pairing and cannot be edited away by a policy write.",
			"channelRef.channel":         "Channel is the transport id: discord, slack, teams or telegram.",
		},
		Example:  json.RawMessage(`{"channel":"slack"}`),
		Response: json.RawMessage(`{"dmPolicy":"pairing","groupPolicy":"open","dm":["u01"],"group":[],"paired":["u07"],"accessGroups":{}}`),
	})
	zip.Describe("GET /v1/channels/inbox", zip.Doc{
		Description: "inbox returns the org's stored inbound messages, oldest first.\n\nIt is a cursor read: pass the cursor a page returns back as since to get the\nnext one, and an empty page returns the cursor it was given.\nOnly the caller's own org's messages are ever visible.",
		Fields: map[string]string{
			"inboxPage.cursor":     "Cursor is the last row id in this page — pass it back as since.\nAn empty page returns the cursor it was given.",
			"inboxPage.messages":   "Messages is the page, oldest first.",
			"inboxQuery.limit":     "Limit caps the rows returned; 0 takes the store's own default.",
			"inboxQuery.since":     "Since is the exclusive row-id cursor to resume from; 0 starts at the oldest.",
			"inboxView.account":    "Account is the connected account that received it.",
			"inboxView.channel":    "Channel is the transport the message arrived on.",
			"inboxView.createdAt":  "CreatedAt is unix seconds.",
			"inboxView.id":         "ID is the row id, and the cursor to resume from.",
			"inboxView.replyTo":    "ReplyTo is the message this one replied to, when any.",
			"inboxView.roomId":     "RoomID is the transport's room identifier.",
			"inboxView.roomKind":   "RoomKind is dm, group or thread.",
			"inboxView.sender":     "Sender is the transport-side sender identity.",
			"inboxView.senderUser": "SenderUser is the Hanzo user it maps to, when one is known.",
			"inboxView.text":       "Text is the message body, downgraded to text.",
		},
		Example:  json.RawMessage(`{"since":4210,"limit":50}`),
		Response: json.RawMessage(`{"messages":[{"id":4211,"channel":"slack","account":"t01abc","roomId":"D01","roomKind":"dm","sender":"u01","text":"ship it","createdAt":1780000000}],"cursor":4211}`),
	})
	zip.Describe("GET /v1/channels/pairing", zip.Doc{
		Description: "pairing returns the org's pending, unexpired pairing requests.\n\nA code is a capability string an org member shows an admin to be approved, so\nit is returned here and never logged.\nExpired requests are not listed.",
		Fields: map[string]string{
			"pairingPending.pending": "Pending is every unexpired request awaiting an admin decision.",
			"pairingView.channel":    "Channel is the transport the request arrived on.",
			"pairingView.code":       "Code is the capability string shown to org members for approval.",
			"pairingView.createdAt":  "CreatedAt is unix seconds of the first sighting.",
			"pairingView.lastSeen":   "LastSeen is unix seconds of the most recent sighting.",
			"pairingView.sender":     "Sender is the transport-side identity asking to be paired.",
		},
		Response: json.RawMessage(`{"pending":[{"channel":"telegram","sender":"5551234","code":"pair-7f3a","createdAt":1780000000,"lastSeen":1780000600}]}`),
	})
	zip.Describe("POST /v1/channels/pairing/approve", zip.Doc{
		Description: "approve turns a pending pairing code into a standing allow entry.\n\nIt is admin-gated: a SuperAdmin, or an admin of the caller's own org.\nThe first approval an org ever makes also seats its owner, reported as\nownerBootstrapped; every later one does not.\nAn unknown or expired code is not found, and nothing is written.",
		Fields: map[string]string{
			"pairingApproval.channel":           "Channel is the transport the request arrived on.",
			"pairingApproval.code":              "Code is the pairing code shown to the org, exactly as issued.",
			"pairingApproved.ownerBootstrapped": "OwnerBootstrapped reports that this approval also seated the org's first\nowner — the one-time bootstrap, false on every later approval.",
			"pairingApproved.sender":            "Sender is the transport-side identity now allowed to DM the org.",
		},
		Example:  json.RawMessage(`{"channel":"telegram","code":"pair-7f3a"}`),
		Response: json.RawMessage(`{"sender":"5551234","ownerBootstrapped":false}`),
	})
	zip.Describe("PUT /v1/channels/allowlist", zip.Doc{
		Description: "setAllowlist edits one channel's access policy and returns it as it now stands.\n\nIt is admin-gated: a SuperAdmin, or an admin of the caller's own org.\nThe edit is partial — an omitted policy or a null list is left exactly as it\nwas — and it owns the CONFIG source only, so a policy write can never revoke\nan approved pairing.\nThe answer is the same shape the read returns, so both verbs speak one shape.",
		Fields: map[string]string{
			"allowlistUpdate.accessGroups": "AccessGroups REPLACES the org's named access groups, keyed group then\nchannel. Null leaves them unchanged.",
			"allowlistUpdate.channel":      "Channel is the transport id to edit: discord, slack, teams or telegram.",
			"allowlistUpdate.dm":           "DM REPLACES the config-source DM allowlist.\nNull leaves it unchanged; approved pairings are a different source and are\nnever revoked by this edit.",
			"allowlistUpdate.dmPolicy":     "DMPolicy sets who may DM the org: pairing, allowlist or open.\nEmpty leaves it unchanged.",
			"allowlistUpdate.group":        "Group REPLACES the config-source group allowlist. Null leaves it unchanged.",
			"allowlistUpdate.groupPolicy":  "GroupPolicy sets group and thread access: open, allowlist or disabled.\nEmpty leaves it unchanged.",
			"allowlistView.accessGroups":   "AccessGroups is the org's named access groups, keyed group then channel.",
			"allowlistView.dm":             "DM is the config-source DM allowlist — the entries this endpoint owns.",
			"allowlistView.dmPolicy":       "DMPolicy is who may DM the org on this channel.",
			"allowlistView.group":          "Group is the config-source group allowlist.",
			"allowlistView.groupPolicy":    "GroupPolicy governs group and thread surfaces on this channel.",
			"allowlistView.paired":         "Paired is the pairing-source entries, read-only here: they are minted by\napproving a pairing and cannot be edited away by a policy write.",
		},
		Example:  json.RawMessage(`{"channel":"slack","dmPolicy":"allowlist","dm":["u01","u02"]}`),
		Response: json.RawMessage(`{"dmPolicy":"allowlist","groupPolicy":"open","dm":["u01","u02"],"group":[],"paired":["u07"],"accessGroups":{}}`),
	})
}
