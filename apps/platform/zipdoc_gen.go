// Code generated by zipdoc; DO NOT EDIT.

package platform

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/builds", zip.Doc{
		Description: "listBuilds returns the org's recorded BuildKit builds, newest first.\nEach row is joined to its app for the source repo and to its deployment for\nthe commit.\nA build is triggered by deploying a git-source app — the ONE build trigger —\nso this is list-only, and an org that has built nothing gets an empty list\nrather than a fabricated one.",
		Fields: map[string]string{
			"buildList.builds": "Builds is the org's real build records, joined to their app and commit.",
		},
	})
	zip.Describe("GET /v1/environments", zip.Doc{
		Description: "listEnvironments aggregates the org's apps into their deploy targets.\nAn environment is not a standalone record — it is the Application.Environment\nscope — so this is list-only, and a new environment appears the moment an app\ntargets it.",
		Fields: map[string]string{
			"environmentList.environments": "Environments is one row per environment any of the org's apps targets.",
		},
	})
	zip.Describe("GET /v1/pipelines", zip.Doc{
		Description: "listPipelines projects each of the org's apps as a CI/CD pipeline.\nA row carries the app's build source (repo or image) and the status and timing\nof its latest deployment run.\nThere is one pipeline per app — a pipeline is created by creating an app — so\nthis is list-only.",
		Fields: map[string]string{
			"pipelineList.pipelines": "Pipelines is one row per app, carrying its latest deployment run.",
		},
	})
	zip.Describe("GET /v1/releases", zip.Doc{
		Description: "listReleases returns the org's released versions.\nA release is a deployment that actually reached the cluster (deploying or\nlive), so a build that never rolled out is not one.\nReleases are created by deploying — the ONE deploy path — so this is\nlist-only, and it reports real deployment history and nothing else.",
		Fields: map[string]string{
			"releaseList.releases": "Releases is one row per deployment that actually reached the cluster.",
		},
	})
}
