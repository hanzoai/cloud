// Code generated by zipdoc; DO NOT EDIT.

package agents

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/agents DELETE /v1/agents/:ref", zip.Doc{
		Description: "Removes an agent and every run recorded against it. Answers 204.",
		Fields: map[string]string{
			"agentRef.ref": "Ref is the agent's public id (the agent_… handle create and list return) or\nits org-unique name, from the path. Either resolves the same agent.",
		},
		Example: json.RawMessage(`{"ref":"helper"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents DELETE /v1/agents/targets/:id", zip.Doc{
		Description: "Deregisters one machine. Only its owner, or an org admin, may\nremove it; an unknown id, a cross-org id and a machine owned by someone else\nall answer the same not-found, so a probe learns nothing about what exists.",
		Fields: map[string]string{
			"targetDeleted.deleted": "Deleted is true when the target was removed.",
			"targetDeleted.id":      "ID is the target that was removed.",
			"targetRef.id":          "ID is the target to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"tgt_1"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents", zip.Doc{
		Description: "Returns every agent defined in the caller's org, each with the\nnumber of runs recorded against it.",
		Fields: map[string]string{
			"agentList.agents":           "Agents is the org's agents, each carrying its recorded run count.",
			"agentView.avatar":           "Avatar is an image the agent is drawn as — a link to one, or the bytes\ninline as a data URL, up to 96 KiB. Emoji is the one glyph a caller picked\nwhen they had no image. At most one is ever set; neither means the agent is\ndrawn as its initial, the same way a person with no photo is. Both are\niam/pkg/schema's Mark, so a face means the same thing on an agent as it\ndoes on a person or an org.\nAvatar is the agent's picture: an image URL, or the image itself inline as a\ndata URL up to 96 KiB. Empty when the agent has no image.",
			"agentView.computeRef":       "ComputeRef is the visor machine this bot is bound to, opaque here: this\npackage stores and echoes it, and the binding's lifecycle belongs elsewhere.\nEmpty means unbound, which is what every one-shot agent is.",
			"agentView.createdAt":        "CreatedAt is when the agent was defined, RFC 3339 in UTC to the second.",
			"agentView.description":      "Description is the one line another agent reads when deciding whether to call\nthis one: the tool catalogue publishes it as the description of `agent_<name>`,\nfalling back to \"agent <name>\" when it is empty. It is not part of the prompt —\nInstructions is — so writing the behaviour here reaches the caller and not the\nmodel.",
			"agentView.emoji":            "Emoji is the single glyph a caller picked when they had no image. At most one\nof avatar and emoji is ever set; neither means the agent is drawn as its\ninitial, the same way a person with no photo is.",
			"agentView.executionMode":    "ExecutionMode is one-shot or long-running, and it decides who may start this\nagent. one-shot runs only when something POSTs to it; long-running is\nadditionally invoked by the scheduler on Schedule, once a minute against the\ncron. An org's long-running agents are capped, so a switch INTO it can be\nrefused with 409.",
			"agentView.id":               "ID is the agent's stable handle, minted here as \"agent_\" + 32 hex characters\nof crypto/rand. A caller cannot choose it, and it never changes — unlike Name,\nwhich is the other way to address the same agent.",
			"agentView.model":            "Model is the Zen model this agent runs on, and it is always OUR name for it:\nwrites normalize through cloud.ZenModel and the read normalizes again, so an\nupstream family name never leaves here even from a row written before that\nrule existed. A create that named none took the deployment's configured\ndefault, so this is where a caller learns which model it actually got.",
			"agentView.name":             "Name is the agent's org-unique handle, matching\n^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. It addresses the agent everywhere ID does,\nit is what a run row records, and it is the suffix of the `agent_<name>` tool\nother agents call this one by. Set once at create; no update route moves it,\nbecause moving it would orphan that history.",
			"agentView.runs":             "Runs is how many executions the org has recorded against this agent, counted\nat read time. The list and update reads count the WHOLE history; the detail\nread reports the size of the RecentRuns page it carries, which stops at 20 —\nso a detail row saying 20 means \"at least 20\", not \"exactly 20\".",
			"agentView.schedule":         "Schedule is the 5-field cron the scheduler fires a long-running agent on,\nevaluated once a minute. Required for long-running and DROPPED for one-shot —\na one-shot agent's schedule is not stored, so absence here is the mode's\nanswer rather than a value nobody set.",
			"agentView.serviceAccountId": "ServiceAccountID is the IAM agent service account (<org>-<agent>) a scheduled\nrun is billed AS. It is what makes an autonomous run attributable to a\nprincipal rather than only to the org; empty means the org itself wears the\nspend.",
			"agentView.status":           "Status is the agent's readiness, and today it is \"ready\" on every row: an\nagent is a definition rather than a provisioned thing, so nothing transitions\nit. Server-set at create; no route accepts it.",
			"agentView.tools":            "Tools are the tool names this agent may call, and the list IS the authority:\nan agent that declares none gets none. The single entry \"*\" means whatever the\nfleet's MCP server serves at the moment of the run, resolved per run rather\nthan frozen here, which is how the default assistant reaches subsystems that\nshipped after it was defined. Empty array, never null.",
			"agentView.updatedAt":        "UpdatedAt is the last time any field above was written, same format. It moves\non an update to the DEFINITION and never on a run, so a busy agent nobody has\nedited keeps an old one.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/:ref", zip.Doc{
		Description: "Returns one agent with its system prompt and its 20 most recent runs.\nThe ref is the agent's public id or its org-unique name — a created agent is\nimmediately gettable by whatever create handed back.",
		Fields: map[string]string{
			"agentDetail.instructions":      "Instructions is the agent's system prompt, verbatim, up to 32 KiB. It is the\none field the list read withholds, because it is the agent's whole behaviour\nand a page of them would be a page of prompts.",
			"agentDetail.recentRuns":        "RecentRuns is the agent's 20 most recent executions, newest first. It is a\nwindow on the history, not the history: the count beside it is `runs`.",
			"agentRef.ref":                  "Ref is the agent's public id (the agent_… handle create and list return) or\nits org-unique name, from the path. Either resolves the same agent.",
			"agentRunView.actor":            "Actor is the \"org/sub\" identity the run was executed and billed AS. Empty\nmeans there was no PERSON — a schedule or a service token — which is a\ndifferent fact from \"we do not know\", and the difference is what an audit\nasks about.",
			"agentRunView.agent":            "What an operator needs to answer \"what ran, for whom, and what did it do\" —\nand, through traceId, to leave this record for the waterfall of the very\nsame run rather than a search that hopefully lands near it.\n\nAgent is on the row because the org-wide feed lists runs across agents, and\na run that cannot name its agent is an orphan in exactly the view built to\nmake sense of many of them. Every field is omitempty: a run recorded before\nthese columns existed reports absence rather than a zero it never measured.",
			"agentRunView.completionTokens": "CompletionTokens is the same measurement for what the model produced, on the\nsame final completion. It is a count of TOKENS, not of turns and not of money.",
			"agentRunView.createdAt":        "CreatedAt is when the run finished, RFC 3339 in UTC to the second — the\nduration above already says how long it had been going.",
			"agentRunView.durationMs":       "DurationMs is wall-clock milliseconds around the completion, including a\nfailover's retries. It is time SPENT, not time billed.",
			"agentRunView.error":            "Error is why an \"ok\"-less run failed, as the failing call reported it. Empty\non every successful run.",
			"agentRunView.id":               "ID is the run's handle, minted as \"run_\" + 32 hex characters. It is the key\nthe metering ledger records this run's per-round token spend under, so it is\nhow a bill and a run are joined.",
			"agentRunView.input":            "Input is the text the run was given, verbatim.",
			"agentRunView.model":            "Model is the model that actually SERVED this run, which is not always the one\nthe agent is defined on — a failover records what answered. Normalized to our\nname on the way out; the stored row is left exactly as it happened, because a\nrun is a record and rewriting it would be worse than the name it carries.",
			"agentRunView.output":           "Output is what the model produced. Empty on an error run, and empty is also a\nlegitimate answer from a run that succeeded with nothing to say — Status is\nwhat separates those.",
			"agentRunView.promptTokens":     "PromptTokens is what the gateway reported for the run's FINAL completion, and\nonly that one — a tool loop's earlier rounds are the metering ledger's account,\njoined by this run's id. Reading it as the run's total spend undercounts a\nloop.",
			"agentRunView.status":           "Status is the run's outcome, and there are exactly two: \"ok\" when the model\nanswered, \"error\" when it did not. It is written when the run ends, so no row\nhere is in flight.",
			"agentRunView.toolCalls":        "ToolCalls is how many tool dispatches the run made — a count of ACTIONS, which\nis a different measurement from the token counts above and from the turns a\nbuild reports. Zero is a run that answered straight from the model.",
			"agentRunView.traceId":          "TraceID is the trace this run IS, so the record and its spans are one thing to\nmove between: it opens the waterfall for THIS run rather than a search that\nlands near it. Empty when the process had no tracer, never a fabricated id.",
			"agentView.avatar":              "Avatar is an image the agent is drawn as — a link to one, or the bytes\ninline as a data URL, up to 96 KiB. Emoji is the one glyph a caller picked\nwhen they had no image. At most one is ever set; neither means the agent is\ndrawn as its initial, the same way a person with no photo is. Both are\niam/pkg/schema's Mark, so a face means the same thing on an agent as it\ndoes on a person or an org.\nAvatar is the agent's picture: an image URL, or the image itself inline as a\ndata URL up to 96 KiB. Empty when the agent has no image.",
			"agentView.computeRef":          "ComputeRef is the visor machine this bot is bound to, opaque here: this\npackage stores and echoes it, and the binding's lifecycle belongs elsewhere.\nEmpty means unbound, which is what every one-shot agent is.",
			"agentView.createdAt":           "CreatedAt is when the agent was defined, RFC 3339 in UTC to the second.",
			"agentView.description":         "Description is the one line another agent reads when deciding whether to call\nthis one: the tool catalogue publishes it as the description of `agent_<name>`,\nfalling back to \"agent <name>\" when it is empty. It is not part of the prompt —\nInstructions is — so writing the behaviour here reaches the caller and not the\nmodel.",
			"agentView.emoji":               "Emoji is the single glyph a caller picked when they had no image. At most one\nof avatar and emoji is ever set; neither means the agent is drawn as its\ninitial, the same way a person with no photo is.",
			"agentView.executionMode":       "ExecutionMode is one-shot or long-running, and it decides who may start this\nagent. one-shot runs only when something POSTs to it; long-running is\nadditionally invoked by the scheduler on Schedule, once a minute against the\ncron. An org's long-running agents are capped, so a switch INTO it can be\nrefused with 409.",
			"agentView.id":                  "ID is the agent's stable handle, minted here as \"agent_\" + 32 hex characters\nof crypto/rand. A caller cannot choose it, and it never changes — unlike Name,\nwhich is the other way to address the same agent.",
			"agentView.model":               "Model is the Zen model this agent runs on, and it is always OUR name for it:\nwrites normalize through cloud.ZenModel and the read normalizes again, so an\nupstream family name never leaves here even from a row written before that\nrule existed. A create that named none took the deployment's configured\ndefault, so this is where a caller learns which model it actually got.",
			"agentView.name":                "Name is the agent's org-unique handle, matching\n^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. It addresses the agent everywhere ID does,\nit is what a run row records, and it is the suffix of the `agent_<name>` tool\nother agents call this one by. Set once at create; no update route moves it,\nbecause moving it would orphan that history.",
			"agentView.runs":                "Runs is how many executions the org has recorded against this agent, counted\nat read time. The list and update reads count the WHOLE history; the detail\nread reports the size of the RecentRuns page it carries, which stops at 20 —\nso a detail row saying 20 means \"at least 20\", not \"exactly 20\".",
			"agentView.schedule":            "Schedule is the 5-field cron the scheduler fires a long-running agent on,\nevaluated once a minute. Required for long-running and DROPPED for one-shot —\na one-shot agent's schedule is not stored, so absence here is the mode's\nanswer rather than a value nobody set.",
			"agentView.serviceAccountId":    "ServiceAccountID is the IAM agent service account (<org>-<agent>) a scheduled\nrun is billed AS. It is what makes an autonomous run attributable to a\nprincipal rather than only to the org; empty means the org itself wears the\nspend.",
			"agentView.status":              "Status is the agent's readiness, and today it is \"ready\" on every row: an\nagent is a definition rather than a provisioned thing, so nothing transitions\nit. Server-set at create; no route accepts it.",
			"agentView.tools":               "Tools are the tool names this agent may call, and the list IS the authority:\nan agent that declares none gets none. The single entry \"*\" means whatever the\nfleet's MCP server serves at the moment of the run, resolved per run rather\nthan frozen here, which is how the default assistant reaches subsystems that\nshipped after it was defined. Empty array, never null.",
			"agentView.updatedAt":           "UpdatedAt is the last time any field above was written, same format. It moves\non an update to the DEFINITION and never on a run, so a busy agent nobody has\nedited keeps an old one.",
		},
		Example: json.RawMessage(`{"ref":"helper"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/:ref/runs", zip.Doc{
		Description: "Returns one agent's execution history, newest first — each run's\ninput, its output or its error, and how long it took. Every row is a run that\nactually happened.",
		Fields: map[string]string{
			"agentRunView.actor":            "Actor is the \"org/sub\" identity the run was executed and billed AS. Empty\nmeans there was no PERSON — a schedule or a service token — which is a\ndifferent fact from \"we do not know\", and the difference is what an audit\nasks about.",
			"agentRunView.agent":            "What an operator needs to answer \"what ran, for whom, and what did it do\" —\nand, through traceId, to leave this record for the waterfall of the very\nsame run rather than a search that hopefully lands near it.\n\nAgent is on the row because the org-wide feed lists runs across agents, and\na run that cannot name its agent is an orphan in exactly the view built to\nmake sense of many of them. Every field is omitempty: a run recorded before\nthese columns existed reports absence rather than a zero it never measured.",
			"agentRunView.completionTokens": "CompletionTokens is the same measurement for what the model produced, on the\nsame final completion. It is a count of TOKENS, not of turns and not of money.",
			"agentRunView.createdAt":        "CreatedAt is when the run finished, RFC 3339 in UTC to the second — the\nduration above already says how long it had been going.",
			"agentRunView.durationMs":       "DurationMs is wall-clock milliseconds around the completion, including a\nfailover's retries. It is time SPENT, not time billed.",
			"agentRunView.error":            "Error is why an \"ok\"-less run failed, as the failing call reported it. Empty\non every successful run.",
			"agentRunView.id":               "ID is the run's handle, minted as \"run_\" + 32 hex characters. It is the key\nthe metering ledger records this run's per-round token spend under, so it is\nhow a bill and a run are joined.",
			"agentRunView.input":            "Input is the text the run was given, verbatim.",
			"agentRunView.model":            "Model is the model that actually SERVED this run, which is not always the one\nthe agent is defined on — a failover records what answered. Normalized to our\nname on the way out; the stored row is left exactly as it happened, because a\nrun is a record and rewriting it would be worse than the name it carries.",
			"agentRunView.output":           "Output is what the model produced. Empty on an error run, and empty is also a\nlegitimate answer from a run that succeeded with nothing to say — Status is\nwhat separates those.",
			"agentRunView.promptTokens":     "PromptTokens is what the gateway reported for the run's FINAL completion, and\nonly that one — a tool loop's earlier rounds are the metering ledger's account,\njoined by this run's id. Reading it as the run's total spend undercounts a\nloop.",
			"agentRunView.status":           "Status is the run's outcome, and there are exactly two: \"ok\" when the model\nanswered, \"error\" when it did not. It is written when the run ends, so no row\nhere is in flight.",
			"agentRunView.toolCalls":        "ToolCalls is how many tool dispatches the run made — a count of ACTIONS, which\nis a different measurement from the token counts above and from the turns a\nbuild reports. Zero is a run that answered straight from the model.",
			"agentRunView.traceId":          "TraceID is the trace this run IS, so the record and its spans are one thing to\nmove between: it opens the waterfall for THIS run rather than a search that\nlands near it. Empty when the process had no tracer, never a fabricated id.",
			"runList.runs":                  "Runs is the agent's executions, newest first.",
			"runsQuery.limit":               "Limit caps how many runs come back, newest first. Absent, zero or out of\nrange (1..200) reads as 50.",
			"runsQuery.ref":                 "Ref is the agent's public id or its org-unique name, from the path.",
		},
		Example: json.RawMessage(`{"ref":"helper","limit":20}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/activity", zip.Doc{
		Description: "Serves the org-wide recent-activity feed. Events are REAL: each\nrecorded run is an invoked (ok) or failed (error) event; each agent's own\ncreate/update timestamps are created/updated events. Merged, newest first,\ncapped. Nothing is invented — an org with no agents and no runs gets [].",
		Fields: map[string]string{
			"activityFeed.activity": "Activity is the merged run/create/update events, newest first, capped at 50.",
			"activityView.agent":    "agent name",
			"activityView.at":       "RFC3339 UTC",
			"activityView.id":       "ID identifies the event, and its shape says which kind it is: a run event\ncarries the run's own id, while an agent event is the agent id suffixed\n\":created\" or \":updated\". Unique within a feed, and not an address — there is\nnothing to fetch it by.",
			"activityView.kind":     "invoked|failed|created|updated (from real events)",
			"activityView.message":  "Message is the line to render, already bounded: \"Invoked <model>\" for a run\nthat worked, the run's own error truncated to 200 characters for one that did\nnot (or \"Run failed\" when it said nothing), and a fixed phrase for the two\nagent events. Nothing here is invented — every event is a row that exists.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/builds", zip.Doc{
		Description: "Returns the public index of every published build, most recently\nupdated first, so a gallery can link straight to the story behind each product.\nPUBLIC, no tenancy: publishing is the author's act, and only published root\nsessions appear here.",
		Fields: map[string]string{
			"buildList.builds":       "Builds is every published build, most recently updated first.",
			"buildSummary.agent":     "Agent is the label the surface that did the work calls itself by.",
			"buildSummary.endedAt":   "EndedAt is when it finished, same format. Empty means it is still going.",
			"buildSummary.org":       "Org and Project are the build's public ADDRESS — the pair the full story is\nread at, and the pair a visitor sees in the URL bar. Not a tenant key: this\nindex is anonymous and lists only what authors published.",
			"buildSummary.project":   "Project is the product's slug, the second half of that address.",
			"buildSummary.repo":      "Repo is the repository the work was done in, as the session reported it.",
			"buildSummary.session":   "Session is the agent session behind the build, and the value its commits name\nin their `Hanzo-Session:` trailer.",
			"buildSummary.startedAt": "StartedAt is when the session opened, RFC 3339 in UTC.",
			"buildSummary.status":    "Status is the session's own: running, paused, done or error — so a card can\nshow a build still being written.",
			"buildSummary.title":     "Title is the human line for the card. Sent even when empty, like every field\nhere, because that is what this route has always sent.",
			"buildSummary.turns":     "Turns is HOW MANY turns the transcript holds — a COUNT, unlike the `turn` on\neach turn of the full story, which is that turn's position. The full read\nreturns at most 1000 of them; this number is not capped.",
			"buildsQuery.limit":      "Limit caps the page. Absent, zero or over 500 reads as 100.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/builds/:org/:project", zip.Doc{
		Description: "Returns the readable build of one product: the agent session that\nproduced it, turn by turn — the prompts, the reasoning, the commits each turn\nproduced — plus the exact `git log` that re-derives every commit binding from\ngit itself, so nothing here has to be taken on trust.\n\nPUBLIC, no tenancy: it answers only for a session its author explicitly\npublished, which is what makes it safe to be anonymous. An unpublished session\nis invisible here no matter who asks; its owner reads it through the org-scoped\n/v1/agents/sessions routes, which need a validated principal.",
		Fields: map[string]string{
			"buildRef.org":        "Org is the org that published the build, from the path.",
			"buildRef.project":    "Project is the product's slug, from the path.",
			"buildTurn.actor":     "Actor is who took the turn. A deploy turn's actor is the literal \"deploy\",\nbecause nobody took it.",
			"buildTurn.at":        "At is when the turn was recorded, RFC 3339 in UTC to the second.",
			"buildTurn.body":      "Body is the readable text of the turn, taken from the stored event's `text`.\nEmpty when the event carried a payload of some other shape — this route reads\ntranscripts and does not invent prose for turns that are not one.",
			"buildTurn.commit":    "Commit is the full sha this turn produced, empty when the turn changed\nnothing. It is ECHOED from the transcript, and the authority is the commit\nitself: it carries the `Hanzo-Session:`/`Hanzo-Turn:` trailer, or a note under\nrefs/notes/hanzo-provenance saying the same, so the claim is checkable at\nsource with the command in `verify`.",
			"buildTurn.kind":      "Kind is what the turn was, from the log's closed six: message, tool-call,\nspawn, log, status, control. A deploy arrives as a `status` turn.",
			"buildTurn.subject":   "Subject is that commit's subject line, from the same transcript, so a reader\nsees what the commit says without fetching the repository.",
			"buildTurn.turn":      "Seq is this turn's POSITION in the session's log — monotonic from 1, per\nsession — and it is what a commit's `Hanzo-Turn:` trailer names. It is not a\ncount of anything: the count is `turns` on the summary beside it.",
			"buildView.agent":     "Agent is the label the surface that did the work calls itself by.",
			"buildView.endedAt":   "EndedAt is when it finished, same format. Empty means it has not — the build\nis still going.",
			"buildView.model":     "Model is the model that did the work, taken from the FIRST turn whose body\nnames one — a transcript states it, this route does not resolve it. Empty when\nno turn said.",
			"buildView.org":       "Org is the org that published this build, echoed from the URL. It is part of\nthe build's public ADDRESS and not a tenant key — this route is anonymous, and\nthe only rows it can reach are ones an author explicitly published.",
			"buildView.project":   "Project is the product's slug, the other half of that address.",
			"buildView.repo":      "Repo is the repository the work was done in, as the session reported it.",
			"buildView.session":   "Session is the id of the agent session this story IS — the same value a\nproduced commit carries in its `Hanzo-Session:` trailer, which is what ties\nthe repository's history to this page.",
			"buildView.startedAt": "StartedAt is when the session opened, RFC 3339 in UTC.",
			"buildView.status":    "Status is the session's own: running, paused, done or error. A build can be\nread while it is still being written, so this is not always terminal — and an\n`error` build is still a readable story, not a missing page.",
			"buildView.title":     "Title is the human line the session was opened or renamed with. Empty when\nnobody gave it one.",
			"buildView.turns":     "Turns is the whole transcript, oldest first, capped at 1000: a published build\nis a story to read down, not an archive to page.",
			"buildView.verify":    "Verify is the exact command that re-derives every commit binding below\nstraight from git, so nothing here has to be taken on trust.",
		},
		Example: json.RawMessage(`{"org":"hanzo","project":"landing"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/metrics", zip.Doc{
		Description: "Serves the invocations-over-time histogram for the org's Agents\ndashboard. Every point is a REAL count of recorded runs in that time bucket —\none series line per agent that ran in the window. The Resource Usage rollup is\nall-null because this store meters no CPU/memory/storage/cost; the console\nrenders those as \"—\" rather than a fabricated figure. No runs => empty series\n(an honest \"not connected / no activity yet\"), never a synthesized trend.",
		Fields: map[string]string{
			"metricsQuery.range":           "Range is the window to bucket: 24H, 7D or 30D. Anything else reads as 30D.",
			"metricsView.range":            "echoes the requested window (24H|7D|30D)",
			"metricsView.resource":         "Resource is the Resource Usage panel's rollup, and every field of it is\ncurrently null — see resourceUsage. It is present rather than omitted so a\npanel renders \"—\" instead of guessing.",
			"metricsView.series":           "per-agent invocation histogram (real)",
			"resourceUsage.costCents":      "CostCents would be the window's spend in cents. Always null here — the money\na run costs is the metering ledger's, joined by the run id, and repeating it\nfrom this side would be a second number that could disagree with the bill.",
			"resourceUsage.cpuVcpuHours":   "CPUVcpuHours would be vCPU-hours over the window. Always null: this store\nholds agent definitions and run I/O, and nothing here meters a CPU. Null is\nthe honest answer and 0 would be a claim.",
			"resourceUsage.memGbHours":     "MemGbHours would be gigabyte-hours of memory. Always null, same reason.",
			"resourceUsage.storageIoBytes": "StorageIoBytes would be bytes moved to and from storage. Always null, same\nreason.",
			"seriesLine.key":               "agent name",
			"seriesLine.points":            "Points is one bucket per interval across the whole window, in time order and\nnever sparse: a bucket with no runs is present with v 0, so two lines drawn\nfrom two agents share an x-axis without the client aligning anything. The\nwindow decides the count — 24 hourly for 24H, 7 daily, 30 daily.",
			"seriesPoint.t":                "bucket start, RFC3339 UTC",
			"seriesPoint.v":                "real invocation count in the bucket",
		},
		Example: json.RawMessage(`{"range":"7D"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/runs", zip.Doc{
		Description: "Returns the org's agent runs across EVERY agent, newest first —\nwhat ran here, for whom, on which model, how long it took, and why it failed.\n\nIt is the feed the per-agent history could not be: an operator asking \"what is\nthis tenant's agent plane doing\" does not start out knowing an agent ref, and\nanswering by listing the agents and then paging each one's history is N+1 round\ntrips to reconstruct one ordering the database already has (RunsSince, ordered\nby created_at over the org index).\n\nThe org is the CALLER's, resolved from identity by tenantStore — never a\nparameter. There is deliberately no org field on orgRunsQuery to forge: run\nhistory is the tenant's own record, and the only tenant this can answer for is\nthe one asking.",
		Fields: map[string]string{
			"agentRunView.actor":            "Actor is the \"org/sub\" identity the run was executed and billed AS. Empty\nmeans there was no PERSON — a schedule or a service token — which is a\ndifferent fact from \"we do not know\", and the difference is what an audit\nasks about.",
			"agentRunView.agent":            "What an operator needs to answer \"what ran, for whom, and what did it do\" —\nand, through traceId, to leave this record for the waterfall of the very\nsame run rather than a search that hopefully lands near it.\n\nAgent is on the row because the org-wide feed lists runs across agents, and\na run that cannot name its agent is an orphan in exactly the view built to\nmake sense of many of them. Every field is omitempty: a run recorded before\nthese columns existed reports absence rather than a zero it never measured.",
			"agentRunView.completionTokens": "CompletionTokens is the same measurement for what the model produced, on the\nsame final completion. It is a count of TOKENS, not of turns and not of money.",
			"agentRunView.createdAt":        "CreatedAt is when the run finished, RFC 3339 in UTC to the second — the\nduration above already says how long it had been going.",
			"agentRunView.durationMs":       "DurationMs is wall-clock milliseconds around the completion, including a\nfailover's retries. It is time SPENT, not time billed.",
			"agentRunView.error":            "Error is why an \"ok\"-less run failed, as the failing call reported it. Empty\non every successful run.",
			"agentRunView.id":               "ID is the run's handle, minted as \"run_\" + 32 hex characters. It is the key\nthe metering ledger records this run's per-round token spend under, so it is\nhow a bill and a run are joined.",
			"agentRunView.input":            "Input is the text the run was given, verbatim.",
			"agentRunView.model":            "Model is the model that actually SERVED this run, which is not always the one\nthe agent is defined on — a failover records what answered. Normalized to our\nname on the way out; the stored row is left exactly as it happened, because a\nrun is a record and rewriting it would be worse than the name it carries.",
			"agentRunView.output":           "Output is what the model produced. Empty on an error run, and empty is also a\nlegitimate answer from a run that succeeded with nothing to say — Status is\nwhat separates those.",
			"agentRunView.promptTokens":     "PromptTokens is what the gateway reported for the run's FINAL completion, and\nonly that one — a tool loop's earlier rounds are the metering ledger's account,\njoined by this run's id. Reading it as the run's total spend undercounts a\nloop.",
			"agentRunView.status":           "Status is the run's outcome, and there are exactly two: \"ok\" when the model\nanswered, \"error\" when it did not. It is written when the run ends, so no row\nhere is in flight.",
			"agentRunView.toolCalls":        "ToolCalls is how many tool dispatches the run made — a count of ACTIONS, which\nis a different measurement from the token counts above and from the turns a\nbuild reports. Zero is a run that answered straight from the model.",
			"agentRunView.traceId":          "TraceID is the trace this run IS, so the record and its spans are one thing to\nmove between: it opens the waterfall for THIS run rather than a search that\nlands near it. Empty when the process had no tracer, never a fabricated id.",
			"orgRunsQuery.limit":            "Limit caps how many runs come back, newest first. Absent, zero or out of\nrange (1..200) reads as 50.",
			"orgRunsQuery.status":           "Status keeps only runs with this outcome (\"ok\" or \"error\"). Empty keeps\nboth. It is the filter an operator reaches for first — \"show me what broke\"\n— and answering it here rather than by paging the whole history client-side\nis the difference between a usable feed and a download.",
			"runList.runs":                  "Runs is the agent's executions, newest first.",
		},
		Example: json.RawMessage(`{"limit":20,"status":"error"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/sessions", zip.Doc{
		Description: "Returns the caller org's live sessions, newest first — each with\nits event count, its direct-child count and a one-line preview of its latest\nevent. With no filter it returns ROOT sessions only, so a dashboard shows one\nrow per flow rather than one per subagent; ?root= or ?parent= descends.",
		Fields: map[string]string{
			"lastEventView.actor":         "Actor is who produced the turn, defaulted to the calling principal when the\nwriter named nobody.",
			"lastEventView.at":            "At is when the turn was recorded, RFC 3339 in UTC to the second.",
			"lastEventView.kind":          "Kind is what the turn was, from the log's closed six: message, tool-call,\nspawn, log, status, control.",
			"lastEventView.preview":       "Preview is the first 240 bytes of the event's payload, cut without regard for\nthe JSON inside it — it is a string to SHOW, never a value to parse. Read the\ndetail or the stream for the whole payload.",
			"lastEventView.seq":           "Seq is that event's position in the session's log — monotonic from 1, per\nsession. A reader holding it can ask the detail or stream reads for\neverything after it, so this doubles as the list's resume cursor.",
			"sessionList.sessions":        "Sessions is the matching sessions, each with its event and child counts and\na one-line preview of its latest event.",
			"sessionProgress.activity":    "Activity is the one line saying what the run is doing right now (\"running\nthe reaper's tests\"), up to 120 characters, in the model's words. Empty\nwhen nothing has estimated it yet.",
			"sessionProgress.at":          "At is when this was determined, RFC 3339 in UTC to the second. Read it as\nthe estimate's AGE: an estimate is not refreshed while nothing has\nhappened, and a stale one beside a run that is still moving is itself worth\nseeing. Empty when nothing has estimated it yet.",
			"sessionProgress.estimated":   "Estimated says a MODEL produced this, from the run's transcript, and it may\nbe wrong. False means the session's own row said it: a finished run is 100%\nbecause it finished, not because anything guessed. Never treat a true here\nas a measurement — it is the reason to look, not the answer.",
			"sessionProgress.pct":         "Pct is how much of the run is done, 0 to 100. THE KEY IS ABSENT when\nprogress is indeterminate — a run nobody can estimate is not a run that has\ndone nothing, and rendering the second for the first is the mistake this\nomission exists to make impossible. Read `phase` before reaching for it.",
			"sessionProgress.phase":       "Phase is what shape the run is in: running, blocked, done, error, or\nunknown when nothing has estimated it yet. blocked means the transcript\nshows the run waiting on something — an approval, a credential, an answer —\nwhich is the one state the running surface cannot report about itself.\nerror only ever comes from the session's own terminal status.",
			"sessionQuery.limit":          "Limit caps the page. Absent, zero or over 500 reads as 100.",
			"sessionQuery.parent":         "Parent scopes the page to the direct children of one session. Ignored when\nroot is set; with neither, only ROOT sessions come back.",
			"sessionQuery.project":        "Project filters to the sessions tagged with one product slug.",
			"sessionQuery.room":           "Room filters to the sessions started in one collaborative room — the query a\nspace view runs to show what has been run in it.",
			"sessionQuery.root":           "Root scopes the page to one subagent tree (its root session id).",
			"sessionQuery.status":         "Status filters to running, paused, done or error.",
			"sessionView.account":         "Account is which subscription or API account under that provider served it.\nTogether with Provider it is what a login revoke matches on to stop the\nsessions a withdrawn account was paying for.",
			"sessionView.actor":           "Actor is WHO this session belongs to, as \"org/sub\" — the same identity a run\nis billed under. A register that names none takes the calling principal. It is\nwhat scopes a login revoke, so a session with the wrong actor is a session the\nright person cannot stop.",
			"sessionView.agent":           "Agent is the label the surface running this session calls itself by\n(\"hanzo-dev\"), up to 128 characters. Required at register. It is free text,\nnot a reference: it need not name a defined agent, and nothing resolves it.",
			"sessionView.children":        "Children is the DIRECT fan-out — how many sessions name this one as parent —\nand not the size of the subtree. Read the tree for that.",
			"sessionView.createdAt":       "CreatedAt is when the row was written, same format. Every path that opens a\nsession stamps it and StartedAt from one clock reading, so the two are equal\non every session this surface has ever produced.",
			"sessionView.cwd":             "Cwd is the directory the session is working in NOW, not the one it started in:\na linked shell moves around, and a card showing where `hanzo link` was run\nanswers \"which work is this\" with something that was true once.",
			"sessionView.endedAt":         "EndedAt is when it reached done or error, same format. Empty while it is still\nrunning or paused, which is how absence reads here: not over yet.",
			"sessionView.events":          "Events is how many turns the session's log holds, counted at read time. It is\nthe whole log, however few of them RecentEvents carries.",
			"sessionView.host":            "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.id":              "ID is the session's handle, minted here as \"sess_\" + 32 hex characters. Every\nlater read, patch, event append and control command is addressed with it, and\na caller cannot choose it.",
			"sessionView.lastEvent":       "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":             "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.parentSessionId": "ParentSessionID is the session that spawned this one, making this a subagent\nof it. Empty means this session is a root — a flow of its own. A parent always\nbelongs to the same org, so a tree never crosses a tenant.",
			"sessionView.progress":        "Progress is how far along this run is — a share of its goal, a phase, and a\nline saying what it is doing. Always present, so a board never branches on\nwhether it is there; `phase` says \"unknown\" when nothing has estimated it.\nIt is a MODEL ESTIMATE wherever `estimated` is true, and the row's own word\nwhere it is false. See progress.go.",
			"sessionView.project":         "The readable build: the product this session built and whether its story\nis public (provenance.go).",
			"sessionView.provider":        "Provider is the linked AI account's provider (claude | codex | hanzo | …) that\nserved this run. Empty when the surface did not say.",
			"sessionView.published":       "Published is the author's decision to let anyone read this session's story at\nthe public build route. It only ever widens READ access to a session that\nalready exists and grants nothing else; false, an unpublished session is\ninvisible there no matter who asks. It cannot be true without a Project,\nbecause that route is keyed on (org, project).",
			"sessionView.repo":            "Repo is the code the session is working on, as the surface reported it. It is\ntruth the SURFACE states, so it is a label rather than something resolved here.",
			"sessionView.room":            "Room is the collaborative room this run was started in (HIP-0523), empty\nwhen it came from anywhere else — a CLI, a schedule, an API call. It is what\nlets a space view show the runs of one room beside its messages.",
			"sessionView.rootSessionId":   "RootSessionID is the top of this session's tree, inherited from the parent and\nshared by every node in one flow. A root session's own id, when it has no\nparent. It is the key one indexed read pulls a whole flow by, and what ?root=\nnarrows a list or a stream to.",
			"sessionView.startedAt":       "StartedAt is when the session opened, RFC 3339 in UTC to the second.",
			"sessionView.status":          "Status is one of exactly four: running, paused, done, error. running and\npaused are LIVE; done and error are TERMINAL and monotonic — once a session\nreaches one it can never go back, because reopening a finished run would\nfabricate liveness. A control command never moves it: the surface running the\nagent reports the new status, and until it does the command is only recorded.",
			"sessionView.target":          "Target is the registered run-target this session is dispatched to — a machine\nthe org claimed, resolved same-org when it was set, so it can never point at\nanother tenant's computer. Empty means the session names no machine.",
			"sessionView.taskRunId":       "TaskRunID is that workflow's particular run. A workflow is the definition and\na run is one execution of it, which is why both are carried.",
			"sessionView.taskWorkflowId":  "TaskWorkflowID is the hanzoai/tasks durable workflow that actually EXECUTES\nthis session — this registry is the view, control and stream layer over it.\nSet, a control command is FORWARDED to that engine; empty, the running surface\npolls for commands instead, which is every session today.",
			"sessionView.terminal":        "Terminal is where this session can be WATCHED — the URL the machine\npublished for its live terminal. Omitted when it publishes none.",
			"sessionView.title":           "Title is the human line a card shows (\"ship the landing page\"), up to 512\ncharacters. Free text, and the one field a surface may rewrite as the work\nturns out to be something else.",
			"sessionView.updatedAt":       "UpdatedAt is the session's last-activity clock, same format. It moves on a\nwrite to the row — a status, a title, a re-dispatch — AND on every appended\nturn, because the append bumps it in the same transaction. The list is ordered\non CreatedAt, so this is the field that says whether a session is still saying\nanything.",
		},
		Example: json.RawMessage(`{"status":"running","limit":20}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/sessions/:id", zip.Doc{
		Description: "Returns one session with its direct child sessions and its 50 most\nrecent events, oldest of those first.",
		Fields: map[string]string{
			"eventView.actor":             "Actor is who produced the turn. A write that names nobody takes the calling\nprincipal, so this is rarely empty in practice.",
			"eventView.createdAt":         "CreatedAt is when the turn was recorded, RFC 3339 in UTC to the second. Seconds\nare coarse enough that two turns can share one, which is why Seq and not this\nis the order.",
			"eventView.id":                "ID is the event's own handle, minted as \"evt_\" + 32 hex characters. It\nidentifies the turn; Seq is what ORDERS it.",
			"eventView.kind":              "Kind is what the turn IS, from a closed six: message (a model turn),\ntool-call, spawn (a subagent started), log, status, control (a steering\ncommand the running surface consumes). Anything else is refused at the write.",
			"eventView.payload":           "Payload is the turn's body, embedded as JSON rather than as a string —\nwhatever the writer sent, up to 64 KiB, checked for well-formedness and\nscanned for credentials before it was stored. Its SHAPE is the writer's\nbusiness and varies by Kind; this surface does not interpret it.",
			"eventView.seq":               "Seq is the turn's position in this session's log: monotonic from 1, assigned\nby the store inside the insert, and unique PER SESSION rather than globally.\nIt is the cursor a reader resumes from after a reconnect — ask for everything\nafter your last-seen seq.",
			"eventView.sessionId":         "SessionID is the session this turn belongs to. Carried on every event so a\nstream frame stands alone — a subscriber watching a whole tree gets turns from\nseveral sessions down one connection.",
			"lastEventView.actor":         "Actor is who produced the turn, defaulted to the calling principal when the\nwriter named nobody.",
			"lastEventView.at":            "At is when the turn was recorded, RFC 3339 in UTC to the second.",
			"lastEventView.kind":          "Kind is what the turn was, from the log's closed six: message, tool-call,\nspawn, log, status, control.",
			"lastEventView.preview":       "Preview is the first 240 bytes of the event's payload, cut without regard for\nthe JSON inside it — it is a string to SHOW, never a value to parse. Read the\ndetail or the stream for the whole payload.",
			"lastEventView.seq":           "Seq is that event's position in the session's log — monotonic from 1, per\nsession. A reader holding it can ask the detail or stream reads for\neverything after it, so this doubles as the list's resume cursor.",
			"sessionDetail.childSessions": "Children is the session's DIRECT children, one level down, each with its own\ncounts. The promoted `children` integer beside it is how many there are; this\nis who they are. For the whole subtree, read the tree.",
			"sessionDetail.recentEvents":  "RecentEvents is the 50 most recent turns, OLDEST of those first — a transcript\nto read down, not a feed. The promoted `events` integer says how many the log\nholds in total; page the rest from a seq.",
			"sessionProgress.activity":    "Activity is the one line saying what the run is doing right now (\"running\nthe reaper's tests\"), up to 120 characters, in the model's words. Empty\nwhen nothing has estimated it yet.",
			"sessionProgress.at":          "At is when this was determined, RFC 3339 in UTC to the second. Read it as\nthe estimate's AGE: an estimate is not refreshed while nothing has\nhappened, and a stale one beside a run that is still moving is itself worth\nseeing. Empty when nothing has estimated it yet.",
			"sessionProgress.estimated":   "Estimated says a MODEL produced this, from the run's transcript, and it may\nbe wrong. False means the session's own row said it: a finished run is 100%\nbecause it finished, not because anything guessed. Never treat a true here\nas a measurement — it is the reason to look, not the answer.",
			"sessionProgress.pct":         "Pct is how much of the run is done, 0 to 100. THE KEY IS ABSENT when\nprogress is indeterminate — a run nobody can estimate is not a run that has\ndone nothing, and rendering the second for the first is the mistake this\nomission exists to make impossible. Read `phase` before reaching for it.",
			"sessionProgress.phase":       "Phase is what shape the run is in: running, blocked, done, error, or\nunknown when nothing has estimated it yet. blocked means the transcript\nshows the run waiting on something — an approval, a credential, an answer —\nwhich is the one state the running surface cannot report about itself.\nerror only ever comes from the session's own terminal status.",
			"sessionRef.id":               "ID is the session to act on, from the path.",
			"sessionView.account":         "Account is which subscription or API account under that provider served it.\nTogether with Provider it is what a login revoke matches on to stop the\nsessions a withdrawn account was paying for.",
			"sessionView.actor":           "Actor is WHO this session belongs to, as \"org/sub\" — the same identity a run\nis billed under. A register that names none takes the calling principal. It is\nwhat scopes a login revoke, so a session with the wrong actor is a session the\nright person cannot stop.",
			"sessionView.agent":           "Agent is the label the surface running this session calls itself by\n(\"hanzo-dev\"), up to 128 characters. Required at register. It is free text,\nnot a reference: it need not name a defined agent, and nothing resolves it.",
			"sessionView.children":        "Children is the DIRECT fan-out — how many sessions name this one as parent —\nand not the size of the subtree. Read the tree for that.",
			"sessionView.createdAt":       "CreatedAt is when the row was written, same format. Every path that opens a\nsession stamps it and StartedAt from one clock reading, so the two are equal\non every session this surface has ever produced.",
			"sessionView.cwd":             "Cwd is the directory the session is working in NOW, not the one it started in:\na linked shell moves around, and a card showing where `hanzo link` was run\nanswers \"which work is this\" with something that was true once.",
			"sessionView.endedAt":         "EndedAt is when it reached done or error, same format. Empty while it is still\nrunning or paused, which is how absence reads here: not over yet.",
			"sessionView.events":          "Events is how many turns the session's log holds, counted at read time. It is\nthe whole log, however few of them RecentEvents carries.",
			"sessionView.host":            "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.id":              "ID is the session's handle, minted here as \"sess_\" + 32 hex characters. Every\nlater read, patch, event append and control command is addressed with it, and\na caller cannot choose it.",
			"sessionView.lastEvent":       "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":             "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.parentSessionId": "ParentSessionID is the session that spawned this one, making this a subagent\nof it. Empty means this session is a root — a flow of its own. A parent always\nbelongs to the same org, so a tree never crosses a tenant.",
			"sessionView.progress":        "Progress is how far along this run is — a share of its goal, a phase, and a\nline saying what it is doing. Always present, so a board never branches on\nwhether it is there; `phase` says \"unknown\" when nothing has estimated it.\nIt is a MODEL ESTIMATE wherever `estimated` is true, and the row's own word\nwhere it is false. See progress.go.",
			"sessionView.project":         "The readable build: the product this session built and whether its story\nis public (provenance.go).",
			"sessionView.provider":        "Provider is the linked AI account's provider (claude | codex | hanzo | …) that\nserved this run. Empty when the surface did not say.",
			"sessionView.published":       "Published is the author's decision to let anyone read this session's story at\nthe public build route. It only ever widens READ access to a session that\nalready exists and grants nothing else; false, an unpublished session is\ninvisible there no matter who asks. It cannot be true without a Project,\nbecause that route is keyed on (org, project).",
			"sessionView.repo":            "Repo is the code the session is working on, as the surface reported it. It is\ntruth the SURFACE states, so it is a label rather than something resolved here.",
			"sessionView.room":            "Room is the collaborative room this run was started in (HIP-0523), empty\nwhen it came from anywhere else — a CLI, a schedule, an API call. It is what\nlets a space view show the runs of one room beside its messages.",
			"sessionView.rootSessionId":   "RootSessionID is the top of this session's tree, inherited from the parent and\nshared by every node in one flow. A root session's own id, when it has no\nparent. It is the key one indexed read pulls a whole flow by, and what ?root=\nnarrows a list or a stream to.",
			"sessionView.startedAt":       "StartedAt is when the session opened, RFC 3339 in UTC to the second.",
			"sessionView.status":          "Status is one of exactly four: running, paused, done, error. running and\npaused are LIVE; done and error are TERMINAL and monotonic — once a session\nreaches one it can never go back, because reopening a finished run would\nfabricate liveness. A control command never moves it: the surface running the\nagent reports the new status, and until it does the command is only recorded.",
			"sessionView.target":          "Target is the registered run-target this session is dispatched to — a machine\nthe org claimed, resolved same-org when it was set, so it can never point at\nanother tenant's computer. Empty means the session names no machine.",
			"sessionView.taskRunId":       "TaskRunID is that workflow's particular run. A workflow is the definition and\na run is one execution of it, which is why both are carried.",
			"sessionView.taskWorkflowId":  "TaskWorkflowID is the hanzoai/tasks durable workflow that actually EXECUTES\nthis session — this registry is the view, control and stream layer over it.\nSet, a control command is FORWARDED to that engine; empty, the running surface\npolls for commands instead, which is every session today.",
			"sessionView.terminal":        "Terminal is where this session can be WATCHED — the URL the machine\npublished for its live terminal. Omitted when it publishes none.",
			"sessionView.title":           "Title is the human line a card shows (\"ship the landing page\"), up to 512\ncharacters. Free text, and the one field a surface may rewrite as the work\nturns out to be something else.",
			"sessionView.updatedAt":       "UpdatedAt is the session's last-activity clock, same format. It moves on a\nwrite to the row — a status, a title, a re-dispatch — AND on every appended\nturn, because the append bumps it in the same transaction. The list is ordered\non CreatedAt, so this is the field that says whether a session is still saying\nanything.",
		},
		Example: json.RawMessage(`{"id":"sess_1"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/sessions/:id/control", zip.Doc{
		Description: "Returns the steering commands (pause/resume/stop/message)\nrecorded against the caller's own session that are newer than the cursor,\noldest first, with the cursor to poll from next. It is how a locally started\n`hanzo code` session — which is not task-backed, so nothing forwards its\ncommands to an execution engine — consumes what the dashboard posted. Read-only\nand bounded at 200 per poll, so a steady poll is cheap and an applied command is\nnever redelivered.",
		Fields: map[string]string{
			"controlCommandView.command": "Command is what was asked, from a closed four: pause, resume, stop, message.\nIt is an INTENT — the poller decides what to do about it, and the session's\nstatus changes only when the poller reports back that it did.",
			"controlCommandView.message": "Message is the text that came with the command: what to say into the run for\n`message`, and the cancellation reason for `stop`. Up to 16 KiB. Empty on a\nbare pause or resume.",
			"controlCommandView.payload": "Payload is the structured half a caller sent instead of (or beside) Message,\nembedded as JSON. Its shape is the caller's business; this surface carries it\nthrough untouched.",
			"controlCommandView.seq":     "Seq is this command's position in the session's log — the same monotonic\nnumber every other turn is ordered by, so a command sits in the transcript\nwhere it was issued. Send the highest one you applied back as `after` and it\nis never redelivered.",
			"controlDrain.commands":      "Commands is the session's control commands newer than the cursor, oldest first.",
			"controlDrain.cursor":        "Cursor is the seq to send as `after` on the next poll — the highest seq in\nthis page, or the cursor sent in when the page is empty.",
			"controlDrainIn.after":       "After is the last seq this poller applied; only commands newer than it come\nback. Absent or negative reads as 0, which drains from the beginning.",
			"controlDrainIn.id":          "ID is the session whose commands are being drained, from the path.",
		},
		Example: json.RawMessage(`{"id":"sess_1","after":12}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/sessions/:id/progress", zip.Doc{
		Description: "Returns how far along one run is: the share of its goal that\nis done, whether it is running, blocked or finished, and a line saying what it\nis doing right now.\n\nIt is a MODEL ESTIMATE read off the run's own transcript, not a measurement —\n`estimated` says so on every answer, and a run whose progress cannot be told\nreports phase \"unknown\" with no percentage rather than a zero it does not\nmean. A session that has already finished answers from its own status instead,\nand is marked not estimated.\n\nThe list and detail reads carry the same value; this address is the one that\nWAITS. Where the stored estimate has gone stale it is remade before answering,\nso a human deciding whether to step into a run gets a current reading rather\nthan the last poll's — which costs one small completion, charged to the same\nwallet the session already names, at most once every thirty seconds per run.",
		Fields: map[string]string{
			"sessionProgress.activity":  "Activity is the one line saying what the run is doing right now (\"running\nthe reaper's tests\"), up to 120 characters, in the model's words. Empty\nwhen nothing has estimated it yet.",
			"sessionProgress.at":        "At is when this was determined, RFC 3339 in UTC to the second. Read it as\nthe estimate's AGE: an estimate is not refreshed while nothing has\nhappened, and a stale one beside a run that is still moving is itself worth\nseeing. Empty when nothing has estimated it yet.",
			"sessionProgress.estimated": "Estimated says a MODEL produced this, from the run's transcript, and it may\nbe wrong. False means the session's own row said it: a finished run is 100%\nbecause it finished, not because anything guessed. Never treat a true here\nas a measurement — it is the reason to look, not the answer.",
			"sessionProgress.pct":       "Pct is how much of the run is done, 0 to 100. THE KEY IS ABSENT when\nprogress is indeterminate — a run nobody can estimate is not a run that has\ndone nothing, and rendering the second for the first is the mistake this\nomission exists to make impossible. Read `phase` before reaching for it.",
			"sessionProgress.phase":     "Phase is what shape the run is in: running, blocked, done, error, or\nunknown when nothing has estimated it yet. blocked means the transcript\nshows the run waiting on something — an approval, a credential, an answer —\nwhich is the one state the running surface cannot report about itself.\nerror only ever comes from the session's own terminal status.",
			"sessionRef.id":             "ID is the session to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"sess_1"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/sessions/:id/tree", zip.Doc{
		Description: "Returns the subagent-flow graph rooted at this session: the session,\nits children, their children, each node carrying its own event count. One\nindexed read pulls the whole flow (every node of a flow shares a root id), so\nthe shape is assembled in memory rather than by walking the store per node.",
		Fields: map[string]string{
			"lastEventView.actor":         "Actor is who produced the turn, defaulted to the calling principal when the\nwriter named nobody.",
			"lastEventView.at":            "At is when the turn was recorded, RFC 3339 in UTC to the second.",
			"lastEventView.kind":          "Kind is what the turn was, from the log's closed six: message, tool-call,\nspawn, log, status, control.",
			"lastEventView.preview":       "Preview is the first 240 bytes of the event's payload, cut without regard for\nthe JSON inside it — it is a string to SHOW, never a value to parse. Read the\ndetail or the stream for the whole payload.",
			"lastEventView.seq":           "Seq is that event's position in the session's log — monotonic from 1, per\nsession. A reader holding it can ask the detail or stream reads for\neverything after it, so this doubles as the list's resume cursor.",
			"sessionProgress.activity":    "Activity is the one line saying what the run is doing right now (\"running\nthe reaper's tests\"), up to 120 characters, in the model's words. Empty\nwhen nothing has estimated it yet.",
			"sessionProgress.at":          "At is when this was determined, RFC 3339 in UTC to the second. Read it as\nthe estimate's AGE: an estimate is not refreshed while nothing has\nhappened, and a stale one beside a run that is still moving is itself worth\nseeing. Empty when nothing has estimated it yet.",
			"sessionProgress.estimated":   "Estimated says a MODEL produced this, from the run's transcript, and it may\nbe wrong. False means the session's own row said it: a finished run is 100%\nbecause it finished, not because anything guessed. Never treat a true here\nas a measurement — it is the reason to look, not the answer.",
			"sessionProgress.pct":         "Pct is how much of the run is done, 0 to 100. THE KEY IS ABSENT when\nprogress is indeterminate — a run nobody can estimate is not a run that has\ndone nothing, and rendering the second for the first is the mistake this\nomission exists to make impossible. Read `phase` before reaching for it.",
			"sessionProgress.phase":       "Phase is what shape the run is in: running, blocked, done, error, or\nunknown when nothing has estimated it yet. blocked means the transcript\nshows the run waiting on something — an approval, a credential, an answer —\nwhich is the one state the running surface cannot report about itself.\nerror only ever comes from the session's own terminal status.",
			"sessionRef.id":               "ID is the session to act on, from the path.",
			"sessionView.account":         "Account is which subscription or API account under that provider served it.\nTogether with Provider it is what a login revoke matches on to stop the\nsessions a withdrawn account was paying for.",
			"sessionView.actor":           "Actor is WHO this session belongs to, as \"org/sub\" — the same identity a run\nis billed under. A register that names none takes the calling principal. It is\nwhat scopes a login revoke, so a session with the wrong actor is a session the\nright person cannot stop.",
			"sessionView.agent":           "Agent is the label the surface running this session calls itself by\n(\"hanzo-dev\"), up to 128 characters. Required at register. It is free text,\nnot a reference: it need not name a defined agent, and nothing resolves it.",
			"sessionView.children":        "Children is the DIRECT fan-out — how many sessions name this one as parent —\nand not the size of the subtree. Read the tree for that.",
			"sessionView.createdAt":       "CreatedAt is when the row was written, same format. Every path that opens a\nsession stamps it and StartedAt from one clock reading, so the two are equal\non every session this surface has ever produced.",
			"sessionView.cwd":             "Cwd is the directory the session is working in NOW, not the one it started in:\na linked shell moves around, and a card showing where `hanzo link` was run\nanswers \"which work is this\" with something that was true once.",
			"sessionView.endedAt":         "EndedAt is when it reached done or error, same format. Empty while it is still\nrunning or paused, which is how absence reads here: not over yet.",
			"sessionView.events":          "Events is how many turns the session's log holds, counted at read time. It is\nthe whole log, however few of them RecentEvents carries.",
			"sessionView.host":            "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.id":              "ID is the session's handle, minted here as \"sess_\" + 32 hex characters. Every\nlater read, patch, event append and control command is addressed with it, and\na caller cannot choose it.",
			"sessionView.lastEvent":       "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":             "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.parentSessionId": "ParentSessionID is the session that spawned this one, making this a subagent\nof it. Empty means this session is a root — a flow of its own. A parent always\nbelongs to the same org, so a tree never crosses a tenant.",
			"sessionView.progress":        "Progress is how far along this run is — a share of its goal, a phase, and a\nline saying what it is doing. Always present, so a board never branches on\nwhether it is there; `phase` says \"unknown\" when nothing has estimated it.\nIt is a MODEL ESTIMATE wherever `estimated` is true, and the row's own word\nwhere it is false. See progress.go.",
			"sessionView.project":         "The readable build: the product this session built and whether its story\nis public (provenance.go).",
			"sessionView.provider":        "Provider is the linked AI account's provider (claude | codex | hanzo | …) that\nserved this run. Empty when the surface did not say.",
			"sessionView.published":       "Published is the author's decision to let anyone read this session's story at\nthe public build route. It only ever widens READ access to a session that\nalready exists and grants nothing else; false, an unpublished session is\ninvisible there no matter who asks. It cannot be true without a Project,\nbecause that route is keyed on (org, project).",
			"sessionView.repo":            "Repo is the code the session is working on, as the surface reported it. It is\ntruth the SURFACE states, so it is a label rather than something resolved here.",
			"sessionView.room":            "Room is the collaborative room this run was started in (HIP-0523), empty\nwhen it came from anywhere else — a CLI, a schedule, an API call. It is what\nlets a space view show the runs of one room beside its messages.",
			"sessionView.rootSessionId":   "RootSessionID is the top of this session's tree, inherited from the parent and\nshared by every node in one flow. A root session's own id, when it has no\nparent. It is the key one indexed read pulls a whole flow by, and what ?root=\nnarrows a list or a stream to.",
			"sessionView.startedAt":       "StartedAt is when the session opened, RFC 3339 in UTC to the second.",
			"sessionView.status":          "Status is one of exactly four: running, paused, done, error. running and\npaused are LIVE; done and error are TERMINAL and monotonic — once a session\nreaches one it can never go back, because reopening a finished run would\nfabricate liveness. A control command never moves it: the surface running the\nagent reports the new status, and until it does the command is only recorded.",
			"sessionView.target":          "Target is the registered run-target this session is dispatched to — a machine\nthe org claimed, resolved same-org when it was set, so it can never point at\nanother tenant's computer. Empty means the session names no machine.",
			"sessionView.taskRunId":       "TaskRunID is that workflow's particular run. A workflow is the definition and\na run is one execution of it, which is why both are carried.",
			"sessionView.taskWorkflowId":  "TaskWorkflowID is the hanzoai/tasks durable workflow that actually EXECUTES\nthis session — this registry is the view, control and stream layer over it.\nSet, a control command is FORWARDED to that engine; empty, the running surface\npolls for commands instead, which is every session today.",
			"sessionView.terminal":        "Terminal is where this session can be WATCHED — the URL the machine\npublished for its live terminal. Omitted when it publishes none.",
			"sessionView.title":           "Title is the human line a card shows (\"ship the landing page\"), up to 512\ncharacters. Free text, and the one field a surface may rewrite as the work\nturns out to be something else.",
			"sessionView.updatedAt":       "UpdatedAt is the session's last-activity clock, same format. It moves on a\nwrite to the row — a status, a title, a re-dispatch — AND on every appended\nturn, because the append bumps it in the same transaction. The list is ordered\non CreatedAt, so this is the field that says whether a session is still saying\nanything.",
			"treeNode.children":           "Children is this node's direct children, each a whole node, so the array nests\nto the depth of the flow. A leaf carries null rather than an empty array. The\nsubtree is materialised in full, up to 10000 nodes, out of one indexed read of\nthe root; nothing is walked node by node.",
			"treeNode.session":            "Session is this node's own session, carrying its event count and its direct\nfan-out. It is the same shape the list and detail reads answer with, minus the\nlast-event preview, which the tree does not fetch.",
		},
		Example: json.RawMessage(`{"id":"sess_1"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/sessions/stream", zip.Doc{
		Description: "Is GET /v1/agents/sessions/stream — a Server-Sent Events feed of\nlive session + event updates for the caller's org. Optional ?root=<id> scopes\nthe feed to one subagent tree. Org-scoped (fail-closed): a subscriber only ever\nreceives its own tenant's updates because the bus filters on org.\n\nThis handler streams over BOTH the plain HTTP listener and the ZAP machine\ntransport with no transport-specific code (zip SendStreamWriter is transport-\nagnostic). Everything the stream loop needs is captured BEFORE SendStreamWriter\nso the loop never touches the request Ctx after the handler returns (fasthttp\nrecycles it) — client-gone is detected by a flush error, bounded by a 25s\nheartbeat.",
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/targets", zip.Doc{
		Description: "Returns every machine registered to the caller's org, newest\nfirst, each with its live session load.",
		Fields: map[string]string{
			"GPU.memory":           "VRAM bytes, 0 = unknown",
			"GPU.model":            "\"GB10\", \"8060S\", \"RTX 4090\"",
			"GPU.vendor":           "nvidia | amd | apple | intel | ...",
			"Metrics.at":           "unix seconds, server-stamped",
			"Metrics.gpuUtil":      "0..1 aggregate utilization",
			"Metrics.load1":        "Load1 is the machine's own one-minute load average — a count of runnable and\nuninterruptible tasks, NOT a percentage and NOT already divided by core\ncount, so it is read against Spec.CPUs: 8.0 is idle on 16 cores and swamped\non 4. Coerced finite and non-negative on write, so 0 means either genuinely\nidle or nothing reported.",
			"Metrics.load15":       "Load15 is the same figure over fifteen. The three together are what separate\na machine that is busy right now from one that has been busy all along —\nwhich is the question a dispatcher is really asking.",
			"Metrics.load5":        "Load5 is the same figure averaged over five minutes.",
			"Metrics.memFree":      "bytes",
			"Metrics.memUsed":      "bytes",
			"Spec.arch":            "amd64 | arm64 | ...",
			"Spec.cpus":            "logical cores",
			"Spec.gpus":            "GPUs is every accelerator the machine advertises, one entry each, capped at\n32 on write. Empty means the probe found none — and that is the answer a\nNeed is checked against, so a machine with no entry here clears no\naccelerator floor. The list is not vendor-filtered: what satisfies a job is\ncounts and VRAM, never a brand (see Need).",
			"Spec.memory":          "total RAM, bytes",
			"Spec.os":              "linux | darwin | windows",
			"targetList.targets":   "Targets is every target registered to the caller's org.",
			"targetView.capacity":  "Capacity is a human summary of what the machine has (\"8 vCPU / 32G\", \"1× GB10\"),\nup to 256 characters. Prose for a card — Spec is the same thing in a form a\nscheduler can read, and nothing derives one from the other.",
			"targetView.createdAt": "CreatedAt is when the machine was first registered, RFC 3339 in UTC. A re-link\nrefreshes the row and leaves this alone, so it dates the machine and not the\nconnection.",
			"targetView.host":      "Host is the hostname sessions on this machine report, and it is a JOIN KEY, not\na label: a session naming this host counts against the load below even when it\nnames no target id, and a re-link of the same (org, host, owner) refreshes this\nrow instead of creating a second. Empty means the machine is addressable only\nby ID.",
			"targetView.id":        "ID is the machine's handle, minted as \"tgt_\" + 32 hex characters. It is what a\nsession records to say it ran here, and what every later patch, claim or\ndelete addresses.",
			"targetView.kind":      "Kind is what sort of destination this is, from a closed five: laptop | cloud |\ngpu | cluster | machine. A register that named none is a `machine`.",
			"targetView.label":     "Label is the name a person gave the machine (\"workshop\"), up to 128\ncharacters. Required at register, free text, and the only field here meant for\nreading rather than matching.",
			"targetView.metrics":   "Metrics is what the machine was DOING at its last heartbeat — loadavg, memory,\naccelerator utilization. Absent when it has never beaten. It is a SNAPSHOT:\nthe series over time lives in the fleet samples, not here.",
			"targetView.metricsAt": "MetricsAt is when that heartbeat was recorded, RFC 3339 in UTC, and the SERVER\nstamps it — a client cannot backdate or forge the staleness clock. Absent means\nnever beaten, which is exactly the case where Status is taken at its word.",
			"targetView.running":   "Running is how many of those are in `running` right now — the number a\ndispatcher weighs against Capacity. paused sessions are in Sessions and not\nhere.",
			"targetView.sessions":  "Sessions is how many of the org's sessions are mapped to this machine, by\ntarget id OR by matching Host. All of them, whatever their status.",
			"targetView.spec":      "Spec is what the machine IS — os, arch, cores, RAM, accelerators — the static\nhalf, changed only when something reports it again. Absent when nothing has\never been reported, and a scheduler reads absence as \"cannot satisfy a floor\"\nrather than as \"no limits\".",
			"targetView.status":    "Status is the EFFECTIVE liveness — online | offline | draining — not the\nstored one. offline and draining are operator INTENT and are reported as they\nstand; `online` is checked against the heartbeat, and a machine that has beaten\nbefore but not in the last 90 seconds reports offline whatever its row says. A\ntarget that has NEVER beaten keeps its stored status, because a hand-registered\ndestination has no fact to check.",
			"targetView.updatedAt": "UpdatedAt is the last write to the row, same format — which for a beating\nmachine is its last heartbeat, since a heartbeat IS a write.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents GET /v1/agents/targets/:id", zip.Doc{
		Description: "Returns one registered machine, with its live session load.",
		Fields: map[string]string{
			"GPU.memory":           "VRAM bytes, 0 = unknown",
			"GPU.model":            "\"GB10\", \"8060S\", \"RTX 4090\"",
			"GPU.vendor":           "nvidia | amd | apple | intel | ...",
			"Metrics.at":           "unix seconds, server-stamped",
			"Metrics.gpuUtil":      "0..1 aggregate utilization",
			"Metrics.load1":        "Load1 is the machine's own one-minute load average — a count of runnable and\nuninterruptible tasks, NOT a percentage and NOT already divided by core\ncount, so it is read against Spec.CPUs: 8.0 is idle on 16 cores and swamped\non 4. Coerced finite and non-negative on write, so 0 means either genuinely\nidle or nothing reported.",
			"Metrics.load15":       "Load15 is the same figure over fifteen. The three together are what separate\na machine that is busy right now from one that has been busy all along —\nwhich is the question a dispatcher is really asking.",
			"Metrics.load5":        "Load5 is the same figure averaged over five minutes.",
			"Metrics.memFree":      "bytes",
			"Metrics.memUsed":      "bytes",
			"Spec.arch":            "amd64 | arm64 | ...",
			"Spec.cpus":            "logical cores",
			"Spec.gpus":            "GPUs is every accelerator the machine advertises, one entry each, capped at\n32 on write. Empty means the probe found none — and that is the answer a\nNeed is checked against, so a machine with no entry here clears no\naccelerator floor. The list is not vendor-filtered: what satisfies a job is\ncounts and VRAM, never a brand (see Need).",
			"Spec.memory":          "total RAM, bytes",
			"Spec.os":              "linux | darwin | windows",
			"targetRef.id":         "ID is the target to act on, from the path.",
			"targetView.capacity":  "Capacity is a human summary of what the machine has (\"8 vCPU / 32G\", \"1× GB10\"),\nup to 256 characters. Prose for a card — Spec is the same thing in a form a\nscheduler can read, and nothing derives one from the other.",
			"targetView.createdAt": "CreatedAt is when the machine was first registered, RFC 3339 in UTC. A re-link\nrefreshes the row and leaves this alone, so it dates the machine and not the\nconnection.",
			"targetView.host":      "Host is the hostname sessions on this machine report, and it is a JOIN KEY, not\na label: a session naming this host counts against the load below even when it\nnames no target id, and a re-link of the same (org, host, owner) refreshes this\nrow instead of creating a second. Empty means the machine is addressable only\nby ID.",
			"targetView.id":        "ID is the machine's handle, minted as \"tgt_\" + 32 hex characters. It is what a\nsession records to say it ran here, and what every later patch, claim or\ndelete addresses.",
			"targetView.kind":      "Kind is what sort of destination this is, from a closed five: laptop | cloud |\ngpu | cluster | machine. A register that named none is a `machine`.",
			"targetView.label":     "Label is the name a person gave the machine (\"workshop\"), up to 128\ncharacters. Required at register, free text, and the only field here meant for\nreading rather than matching.",
			"targetView.metrics":   "Metrics is what the machine was DOING at its last heartbeat — loadavg, memory,\naccelerator utilization. Absent when it has never beaten. It is a SNAPSHOT:\nthe series over time lives in the fleet samples, not here.",
			"targetView.metricsAt": "MetricsAt is when that heartbeat was recorded, RFC 3339 in UTC, and the SERVER\nstamps it — a client cannot backdate or forge the staleness clock. Absent means\nnever beaten, which is exactly the case where Status is taken at its word.",
			"targetView.running":   "Running is how many of those are in `running` right now — the number a\ndispatcher weighs against Capacity. paused sessions are in Sessions and not\nhere.",
			"targetView.sessions":  "Sessions is how many of the org's sessions are mapped to this machine, by\ntarget id OR by matching Host. All of them, whatever their status.",
			"targetView.spec":      "Spec is what the machine IS — os, arch, cores, RAM, accelerators — the static\nhalf, changed only when something reports it again. Absent when nothing has\never been reported, and a scheduler reads absence as \"cannot satisfy a floor\"\nrather than as \"no limits\".",
			"targetView.status":    "Status is the EFFECTIVE liveness — online | offline | draining — not the\nstored one. offline and draining are operator INTENT and are reported as they\nstand; `online` is checked against the heartbeat, and a machine that has beaten\nbefore but not in the last 90 seconds reports offline whatever its row says. A\ntarget that has NEVER beaten keeps its stored status, because a hand-registered\ndestination has no fact to check.",
			"targetView.updatedAt": "UpdatedAt is the last write to the row, same format — which for a beating\nmachine is its last heartbeat, since a heartbeat IS a write.",
		},
		Example: json.RawMessage(`{"id":"tgt_1"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents PATCH /v1/agents/:ref", zip.Doc{
		Description: "Changes an agent in place. Every field is optional; a field the\nrequest omits keeps its stored value. The resulting mode+schedule are\nre-validated together, so a partial update can never leave a long-running\nagent without the cron the scheduler needs to fire it, and a transition INTO\nlong-running counts against the per-org cap on scheduled agents.",
		Fields: map[string]string{
			"agentView.avatar":               "Avatar is an image the agent is drawn as — a link to one, or the bytes\ninline as a data URL, up to 96 KiB. Emoji is the one glyph a caller picked\nwhen they had no image. At most one is ever set; neither means the agent is\ndrawn as its initial, the same way a person with no photo is. Both are\niam/pkg/schema's Mark, so a face means the same thing on an agent as it\ndoes on a person or an org.\nAvatar is the agent's picture: an image URL, or the image itself inline as a\ndata URL up to 96 KiB. Empty when the agent has no image.",
			"agentView.computeRef":           "ComputeRef is the visor machine this bot is bound to, opaque here: this\npackage stores and echoes it, and the binding's lifecycle belongs elsewhere.\nEmpty means unbound, which is what every one-shot agent is.",
			"agentView.createdAt":            "CreatedAt is when the agent was defined, RFC 3339 in UTC to the second.",
			"agentView.description":          "Description is the one line another agent reads when deciding whether to call\nthis one: the tool catalogue publishes it as the description of `agent_<name>`,\nfalling back to \"agent <name>\" when it is empty. It is not part of the prompt —\nInstructions is — so writing the behaviour here reaches the caller and not the\nmodel.",
			"agentView.emoji":                "Emoji is the single glyph a caller picked when they had no image. At most one\nof avatar and emoji is ever set; neither means the agent is drawn as its\ninitial, the same way a person with no photo is.",
			"agentView.executionMode":        "ExecutionMode is one-shot or long-running, and it decides who may start this\nagent. one-shot runs only when something POSTs to it; long-running is\nadditionally invoked by the scheduler on Schedule, once a minute against the\ncron. An org's long-running agents are capped, so a switch INTO it can be\nrefused with 409.",
			"agentView.id":                   "ID is the agent's stable handle, minted here as \"agent_\" + 32 hex characters\nof crypto/rand. A caller cannot choose it, and it never changes — unlike Name,\nwhich is the other way to address the same agent.",
			"agentView.model":                "Model is the Zen model this agent runs on, and it is always OUR name for it:\nwrites normalize through cloud.ZenModel and the read normalizes again, so an\nupstream family name never leaves here even from a row written before that\nrule existed. A create that named none took the deployment's configured\ndefault, so this is where a caller learns which model it actually got.",
			"agentView.name":                 "Name is the agent's org-unique handle, matching\n^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. It addresses the agent everywhere ID does,\nit is what a run row records, and it is the suffix of the `agent_<name>` tool\nother agents call this one by. Set once at create; no update route moves it,\nbecause moving it would orphan that history.",
			"agentView.runs":                 "Runs is how many executions the org has recorded against this agent, counted\nat read time. The list and update reads count the WHOLE history; the detail\nread reports the size of the RecentRuns page it carries, which stops at 20 —\nso a detail row saying 20 means \"at least 20\", not \"exactly 20\".",
			"agentView.schedule":             "Schedule is the 5-field cron the scheduler fires a long-running agent on,\nevaluated once a minute. Required for long-running and DROPPED for one-shot —\na one-shot agent's schedule is not stored, so absence here is the mode's\nanswer rather than a value nobody set.",
			"agentView.serviceAccountId":     "ServiceAccountID is the IAM agent service account (<org>-<agent>) a scheduled\nrun is billed AS. It is what makes an autonomous run attributable to a\nprincipal rather than only to the org; empty means the org itself wears the\nspend.",
			"agentView.status":               "Status is the agent's readiness, and today it is \"ready\" on every row: an\nagent is a definition rather than a provisioned thing, so nothing transitions\nit. Server-set at create; no route accepts it.",
			"agentView.tools":                "Tools are the tool names this agent may call, and the list IS the authority:\nan agent that declares none gets none. The single entry \"*\" means whatever the\nfleet's MCP server serves at the moment of the run, resolved per run rather\nthan frozen here, which is how the default assistant reaches subsystems that\nshipped after it was defined. Empty array, never null.",
			"agentView.updatedAt":            "UpdatedAt is the last time any field above was written, same format. It moves\non an update to the DEFINITION and never on a run, so a busy agent nobody has\nedited keeps an old one.",
			"updateAgentIn.avatar":           "Avatar and Emoji re-draw the agent. Sending either replaces the pair, so\nsetting an image clears a glyph and \"\" for both goes back to the initial —\nthere is no state where a row holds two answers.",
			"updateAgentIn.computeRef":       "ComputeRef re-binds (or, with \"\", unbinds) the visor machine. Opaque here.",
			"updateAgentIn.description":      "Description replaces the line other agents read in the tool catalogue.",
			"updateAgentIn.emoji":            "Emoji re-draws the agent as a glyph. Sending either of the pair replaces\nBOTH, so setting a glyph clears an image and \"\" for both goes back to the\ninitial — there is no state where a row holds two answers.",
			"updateAgentIn.executionMode":    "ExecutionMode switches between one-shot and long-running. The RESULTING\nmode+schedule are validated together, so switching to long-running without a\nstored or supplied cron is refused rather than accepted into an agent the\nscheduler would skip forever. A switch INTO long-running counts against the\nper-org cap and can be a 409.",
			"updateAgentIn.instructions":     "Instructions replaces the system prompt whole, up to 32 KiB. There is no\nappend: a prompt is one text, and sending \"\" clears it.",
			"updateAgentIn.model":            "Model re-points the agent at another model, checked against the gateway's\nserved catalogue exactly as create checks it. Empty STRING is refused — say\nnothing to keep the current one. Past runs keep the model that served them.",
			"updateAgentIn.ref":              "Ref is the agent to update — its public id or org-unique name, from the path.",
			"updateAgentIn.schedule":         "Schedule replaces the cron. It is validated against the mode this update\nleaves behind, and dropped if that mode is one-shot.",
			"updateAgentIn.serviceAccountId": "ServiceAccountID re-points (or, with \"\", clears) the IAM service account a\nscheduled run is billed as. Clearing it puts that spend back on the org.",
			"updateAgentIn.tools":            "Tools replaces the whole allow-list, it does not add to it. Sending [] takes\nevery tool away, which is the only way to say that.",
		},
		Example: json.RawMessage(`{"ref":"helper","instructions":"be terse and cite sources"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents PATCH /v1/agents/sessions/:id", zip.Doc{
		Description: "Updates a session's surface-owned truth: its status, its title,\nthe run-target it is dispatched to, and the product it built plus whether that\nbuild's story is public. A FINISHED session stays finished — reopening a\ndone/error run would fabricate liveness — and publishing is refused unless the\nsession names the project it built, because the public build route is keyed on\n(org, project).",
		Fields: map[string]string{
			"lastEventView.actor":         "Actor is who produced the turn, defaulted to the calling principal when the\nwriter named nobody.",
			"lastEventView.at":            "At is when the turn was recorded, RFC 3339 in UTC to the second.",
			"lastEventView.kind":          "Kind is what the turn was, from the log's closed six: message, tool-call,\nspawn, log, status, control.",
			"lastEventView.preview":       "Preview is the first 240 bytes of the event's payload, cut without regard for\nthe JSON inside it — it is a string to SHOW, never a value to parse. Read the\ndetail or the stream for the whole payload.",
			"lastEventView.seq":           "Seq is that event's position in the session's log — monotonic from 1, per\nsession. A reader holding it can ask the detail or stream reads for\neverything after it, so this doubles as the list's resume cursor.",
			"patchSessionIn.cwd":          "Cwd is where the session is working NOW.\n\nIt was write-once — captured at register and never again — which is right\nfor a run that starts in a directory and stays there, and wrong for a linked\nshell, which is a place a person moves around in. The console showed the\ndirectory `hanzo link` happened to be run from and kept showing it after the\nshell had walked away, so the field answered \"which work is this\" with an\nanswer that was true once. A pointer, so an unchanged path is an omitted\nfield rather than a repeated write.",
			"patchSessionIn.id":           "ID is the session to update, from the path.",
			"patchSessionIn.project":      "Project tags the product this session built; Published is the author's\ndecision to let anyone read the story (provenance.go). Both are pointers so\n\"absent\" and \"cleared\" are different requests.",
			"patchSessionIn.published":    "Published opens the session's story to the public build route; false withdraws\nit, and withdrawing is always allowed. PUBLISHING is refused unless the\nsession names a Project — the one set in this same request, or the one already\nstored — because that route is keyed on (org, project). It widens READ access\nto what is already there and grants nothing else.",
			"patchSessionIn.status":       "Status moves the session to running, paused, done or error. A session that has\nalready finished refuses any change with 409 — done and error are monotonic —\nand moving INTO one stamps the end time. This is the surface REPORTING what\nhappened; a control command never writes it.",
			"patchSessionIn.target":       "Target re-dispatches a session to a run-target (the #48 association). \"\" detaches.",
			"patchSessionIn.terminal":     "Terminal publishes (or, with \"\", withdraws) the URL this session's live\nterminal can be watched at. A pointer so \"absent\" and \"withdrawn\" are\ndifferent requests: a session that stops sharing must be able to say so.",
			"patchSessionIn.title":        "Title rewrites the human line, up to 512 characters — usually because the work\nturned out to be something other than what it was opened as.",
			"sessionProgress.activity":    "Activity is the one line saying what the run is doing right now (\"running\nthe reaper's tests\"), up to 120 characters, in the model's words. Empty\nwhen nothing has estimated it yet.",
			"sessionProgress.at":          "At is when this was determined, RFC 3339 in UTC to the second. Read it as\nthe estimate's AGE: an estimate is not refreshed while nothing has\nhappened, and a stale one beside a run that is still moving is itself worth\nseeing. Empty when nothing has estimated it yet.",
			"sessionProgress.estimated":   "Estimated says a MODEL produced this, from the run's transcript, and it may\nbe wrong. False means the session's own row said it: a finished run is 100%\nbecause it finished, not because anything guessed. Never treat a true here\nas a measurement — it is the reason to look, not the answer.",
			"sessionProgress.pct":         "Pct is how much of the run is done, 0 to 100. THE KEY IS ABSENT when\nprogress is indeterminate — a run nobody can estimate is not a run that has\ndone nothing, and rendering the second for the first is the mistake this\nomission exists to make impossible. Read `phase` before reaching for it.",
			"sessionProgress.phase":       "Phase is what shape the run is in: running, blocked, done, error, or\nunknown when nothing has estimated it yet. blocked means the transcript\nshows the run waiting on something — an approval, a credential, an answer —\nwhich is the one state the running surface cannot report about itself.\nerror only ever comes from the session's own terminal status.",
			"sessionView.account":         "Account is which subscription or API account under that provider served it.\nTogether with Provider it is what a login revoke matches on to stop the\nsessions a withdrawn account was paying for.",
			"sessionView.actor":           "Actor is WHO this session belongs to, as \"org/sub\" — the same identity a run\nis billed under. A register that names none takes the calling principal. It is\nwhat scopes a login revoke, so a session with the wrong actor is a session the\nright person cannot stop.",
			"sessionView.agent":           "Agent is the label the surface running this session calls itself by\n(\"hanzo-dev\"), up to 128 characters. Required at register. It is free text,\nnot a reference: it need not name a defined agent, and nothing resolves it.",
			"sessionView.children":        "Children is the DIRECT fan-out — how many sessions name this one as parent —\nand not the size of the subtree. Read the tree for that.",
			"sessionView.createdAt":       "CreatedAt is when the row was written, same format. Every path that opens a\nsession stamps it and StartedAt from one clock reading, so the two are equal\non every session this surface has ever produced.",
			"sessionView.cwd":             "Cwd is the directory the session is working in NOW, not the one it started in:\na linked shell moves around, and a card showing where `hanzo link` was run\nanswers \"which work is this\" with something that was true once.",
			"sessionView.endedAt":         "EndedAt is when it reached done or error, same format. Empty while it is still\nrunning or paused, which is how absence reads here: not over yet.",
			"sessionView.events":          "Events is how many turns the session's log holds, counted at read time. It is\nthe whole log, however few of them RecentEvents carries.",
			"sessionView.host":            "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.id":              "ID is the session's handle, minted here as \"sess_\" + 32 hex characters. Every\nlater read, patch, event append and control command is addressed with it, and\na caller cannot choose it.",
			"sessionView.lastEvent":       "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":             "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.parentSessionId": "ParentSessionID is the session that spawned this one, making this a subagent\nof it. Empty means this session is a root — a flow of its own. A parent always\nbelongs to the same org, so a tree never crosses a tenant.",
			"sessionView.progress":        "Progress is how far along this run is — a share of its goal, a phase, and a\nline saying what it is doing. Always present, so a board never branches on\nwhether it is there; `phase` says \"unknown\" when nothing has estimated it.\nIt is a MODEL ESTIMATE wherever `estimated` is true, and the row's own word\nwhere it is false. See progress.go.",
			"sessionView.project":         "The readable build: the product this session built and whether its story\nis public (provenance.go).",
			"sessionView.provider":        "Provider is the linked AI account's provider (claude | codex | hanzo | …) that\nserved this run. Empty when the surface did not say.",
			"sessionView.published":       "Published is the author's decision to let anyone read this session's story at\nthe public build route. It only ever widens READ access to a session that\nalready exists and grants nothing else; false, an unpublished session is\ninvisible there no matter who asks. It cannot be true without a Project,\nbecause that route is keyed on (org, project).",
			"sessionView.repo":            "Repo is the code the session is working on, as the surface reported it. It is\ntruth the SURFACE states, so it is a label rather than something resolved here.",
			"sessionView.room":            "Room is the collaborative room this run was started in (HIP-0523), empty\nwhen it came from anywhere else — a CLI, a schedule, an API call. It is what\nlets a space view show the runs of one room beside its messages.",
			"sessionView.rootSessionId":   "RootSessionID is the top of this session's tree, inherited from the parent and\nshared by every node in one flow. A root session's own id, when it has no\nparent. It is the key one indexed read pulls a whole flow by, and what ?root=\nnarrows a list or a stream to.",
			"sessionView.startedAt":       "StartedAt is when the session opened, RFC 3339 in UTC to the second.",
			"sessionView.status":          "Status is one of exactly four: running, paused, done, error. running and\npaused are LIVE; done and error are TERMINAL and monotonic — once a session\nreaches one it can never go back, because reopening a finished run would\nfabricate liveness. A control command never moves it: the surface running the\nagent reports the new status, and until it does the command is only recorded.",
			"sessionView.target":          "Target is the registered run-target this session is dispatched to — a machine\nthe org claimed, resolved same-org when it was set, so it can never point at\nanother tenant's computer. Empty means the session names no machine.",
			"sessionView.taskRunId":       "TaskRunID is that workflow's particular run. A workflow is the definition and\na run is one execution of it, which is why both are carried.",
			"sessionView.taskWorkflowId":  "TaskWorkflowID is the hanzoai/tasks durable workflow that actually EXECUTES\nthis session — this registry is the view, control and stream layer over it.\nSet, a control command is FORWARDED to that engine; empty, the running surface\npolls for commands instead, which is every session today.",
			"sessionView.terminal":        "Terminal is where this session can be WATCHED — the URL the machine\npublished for its live terminal. Omitted when it publishes none.",
			"sessionView.title":           "Title is the human line a card shows (\"ship the landing page\"), up to 512\ncharacters. Free text, and the one field a surface may rewrite as the work\nturns out to be something else.",
			"sessionView.updatedAt":       "UpdatedAt is the session's last-activity clock, same format. It moves on a\nwrite to the row — a status, a title, a re-dispatch — AND on every appended\nturn, because the append bumps it in the same transaction. The list is ordered\non CreatedAt, so this is the field that says whether a session is still saying\nanything.",
		},
		Example: json.RawMessage(`{"id":"sess_1","status":"done"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents PATCH /v1/agents/targets/:id", zip.Doc{
		Description: "Updates one machine in place. Every field is optional; a field the\nrequest omits is left alone. A metrics patch IS a heartbeat — the server stamps\nits own clock, so a client can neither forge nor backdate staleness.",
		Fields: map[string]string{
			"GPU.memory":             "VRAM bytes, 0 = unknown",
			"GPU.model":              "\"GB10\", \"8060S\", \"RTX 4090\"",
			"GPU.vendor":             "nvidia | amd | apple | intel | ...",
			"Metrics.at":             "unix seconds, server-stamped",
			"Metrics.gpuUtil":        "0..1 aggregate utilization",
			"Metrics.load1":          "Load1 is the machine's own one-minute load average — a count of runnable and\nuninterruptible tasks, NOT a percentage and NOT already divided by core\ncount, so it is read against Spec.CPUs: 8.0 is idle on 16 cores and swamped\non 4. Coerced finite and non-negative on write, so 0 means either genuinely\nidle or nothing reported.",
			"Metrics.load15":         "Load15 is the same figure over fifteen. The three together are what separate\na machine that is busy right now from one that has been busy all along —\nwhich is the question a dispatcher is really asking.",
			"Metrics.load5":          "Load5 is the same figure averaged over five minutes.",
			"Metrics.memFree":        "bytes",
			"Metrics.memUsed":        "bytes",
			"Spec.arch":              "amd64 | arm64 | ...",
			"Spec.cpus":              "logical cores",
			"Spec.gpus":              "GPUs is every accelerator the machine advertises, one entry each, capped at\n32 on write. Empty means the probe found none — and that is the answer a\nNeed is checked against, so a machine with no entry here clears no\naccelerator floor. The list is not vendor-filtered: what satisfies a job is\ncounts and VRAM, never a brand (see Need).",
			"Spec.memory":            "total RAM, bytes",
			"Spec.os":                "linux | darwin | windows",
			"patchTargetIn.capacity": "Capacity rewrites the human summary, up to 256 characters. \"\" clears it.",
			"patchTargetIn.host":     "Host re-points the hostname sessions are matched by. Moving it moves the load:\nthe session counts follow the new name from the next read.",
			"patchTargetIn.id":       "ID is the target to update, from the path.",
			"patchTargetIn.kind":     "Kind re-files it under laptop | cloud | gpu | cluster | machine.",
			"patchTargetIn.label":    "Label renames the machine, up to 128 characters. Empty STRING is refused — a\ntarget with no name is a row nobody can pick out of a fleet.",
			"patchTargetIn.metrics":  "Metrics replaces the live sample, and sending one IS A HEARTBEAT: the server\nstamps the time and appends the point to the fleet series. Sending an all-zero\nsample CLEARS the heartbeat — the machine goes back to having no liveness fact\nat all, and its stored status is taken at face value again.",
			"patchTargetIn.spec":     "Spec replaces the static capability whole, sanitized and clamped the same way\na register's is.",
			"patchTargetIn.status":   "Status sets operator INTENT: online | offline | draining. Draining is how a\nmachine is taken out of dispatch without ending what is already on it. What\ncomes back may still read offline, because the heartbeat outranks the intent.",
			"targetView.capacity":    "Capacity is a human summary of what the machine has (\"8 vCPU / 32G\", \"1× GB10\"),\nup to 256 characters. Prose for a card — Spec is the same thing in a form a\nscheduler can read, and nothing derives one from the other.",
			"targetView.createdAt":   "CreatedAt is when the machine was first registered, RFC 3339 in UTC. A re-link\nrefreshes the row and leaves this alone, so it dates the machine and not the\nconnection.",
			"targetView.host":        "Host is the hostname sessions on this machine report, and it is a JOIN KEY, not\na label: a session naming this host counts against the load below even when it\nnames no target id, and a re-link of the same (org, host, owner) refreshes this\nrow instead of creating a second. Empty means the machine is addressable only\nby ID.",
			"targetView.id":          "ID is the machine's handle, minted as \"tgt_\" + 32 hex characters. It is what a\nsession records to say it ran here, and what every later patch, claim or\ndelete addresses.",
			"targetView.kind":        "Kind is what sort of destination this is, from a closed five: laptop | cloud |\ngpu | cluster | machine. A register that named none is a `machine`.",
			"targetView.label":       "Label is the name a person gave the machine (\"workshop\"), up to 128\ncharacters. Required at register, free text, and the only field here meant for\nreading rather than matching.",
			"targetView.metrics":     "Metrics is what the machine was DOING at its last heartbeat — loadavg, memory,\naccelerator utilization. Absent when it has never beaten. It is a SNAPSHOT:\nthe series over time lives in the fleet samples, not here.",
			"targetView.metricsAt":   "MetricsAt is when that heartbeat was recorded, RFC 3339 in UTC, and the SERVER\nstamps it — a client cannot backdate or forge the staleness clock. Absent means\nnever beaten, which is exactly the case where Status is taken at its word.",
			"targetView.running":     "Running is how many of those are in `running` right now — the number a\ndispatcher weighs against Capacity. paused sessions are in Sessions and not\nhere.",
			"targetView.sessions":    "Sessions is how many of the org's sessions are mapped to this machine, by\ntarget id OR by matching Host. All of them, whatever their status.",
			"targetView.spec":        "Spec is what the machine IS — os, arch, cores, RAM, accelerators — the static\nhalf, changed only when something reports it again. Absent when nothing has\never been reported, and a scheduler reads absence as \"cannot satisfy a floor\"\nrather than as \"no limits\".",
			"targetView.status":      "Status is the EFFECTIVE liveness — online | offline | draining — not the\nstored one. offline and draining are operator INTENT and are reported as they\nstand; `online` is checked against the heartbeat, and a machine that has beaten\nbefore but not in the last 90 seconds reports offline whatever its row says. A\ntarget that has NEVER beaten keeps its stored status, because a hand-registered\ndestination has no fact to check.",
			"targetView.updatedAt":   "UpdatedAt is the last write to the row, same format — which for a beating\nmachine is its last heartbeat, since a heartbeat IS a write.",
		},
		Example: json.RawMessage(`{"id":"tgt_1","status":"draining"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /agents/roster", zip.Doc{
		Description: "Answers the caller's own agents.\n\nThe org is the caller's plane identity and NEVER an argument — plane.RosterIn\nhas no fields at all, so a cross-tenant read is unrepresentable here rather\nthan merely refused. That is the opposite choice from planeRunOnBehalf, which\ndoes take an org, and the difference is the direction of the act: that op\nSPENDS the named org's own balance under its own agent and reads nothing\nacross tenants, while this one ENUMERATES rows and is exactly the shape a\nstated org would leak.\n\nAnonymous is refused rather than defaulted: a roster read arriving with no\nprincipal must fail, not pick a tenant.\n\nA named handler, not a closure, so zipdoc can lift this prose into the registry.",
		Fields: map[string]string{
			"AgentBrief.id":     "ID is the agent id — the ref a run names and the value a member id derives\nfrom, so it must be stable across a reconcile.",
			"AgentBrief.name":   "Name is the display name a person reads in a member list.",
			"AgentBrief.status": "Status is the registry status VERBATIM, never a boolean. The caller decides\nwhat counts as live: apps/team reads empty/active/ready as a live member and\nanything else as one that keeps its authorship and drops out of the roster,\nand folding that judgement into a bool here would move the policy to the\nwrong side of the wire.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /agents/run-on-behalf", zip.Doc{
		Description: "Answers a bridge's turn.\n\nUnlike the session ops, the org travels IN the request rather than being taken\nfrom the caller's plane identity: the tenant here is the one that connected the\nSlack workspace, resolved by the bridge from the signed team_id, and the bridge\nplugin's own identity is not it. That is safe because this op only SPENDS the\nnamed org's own balance under its own agent — it reads nothing across tenants —\nand because the subject must be a link the bridge already proved.\n\nAn empty subject is refused rather than defaulted. A turn that lost its caller\nmust not run AS THE ORG: that would bill the tenant for an unattributable act\nand hand an unlinked user the org's agent.",
		Fields: map[string]string{
			"RunOnBehalfIn.history": "History is the conversation this Input arrived in, oldest first and NOT\nincluding Input itself. Empty is a first message, which is a real answer and\nnot a missing one.\n\nIt rides the turn because the answering side has no way to obtain it: the\nroom is a fact about the transport the bridge is on, and only the bridge knows\nit. Without this the agent saw one message with nothing around it, which is\nhow \"try again\" became \"what would you like me to help you with\".",
			"RunOnBehalfIn.input":   "Input is the user's message, already stripped of the leading @mention.",
			"RunOnBehalfIn.model":   "Model is the ASKER's own choice, empty when they have not made one. It is a\npreference of the person, not a property of the agent, which is why it rides\nthe turn instead of being written into an agent row: two people in one\nworkspace can prefer different models of the same assistant.",
			"RunOnBehalfIn.org":     "Org is the isolation gate, the tenant, and the balance the run bills.",
			"RunOnBehalfIn.ref":     "Ref names the agent to run.",
			"RunOnBehalfIn.subject": "Subject is the caller's LINKED Hanzo identity, unqualified. Attribution and\nauthorization both hang off it, so a turn can never run as nobody: the\nanswering side refuses an empty subject rather than falling back to the org.",
			"RunOnBehalfOut.error":  "Why a non-\"ok\" run failed. A run that EXECUTED and whose model failed comes\nback with a nil error and a status, so without this the cause was reachable\nonly by finding the run row by hand — which is how a broken inference wire\nsurvived a day of looking while the bridge posted a generic sentence.",
			"Turn.sender":           "Sender is the platform user id, and Self marks the assistant's own turns so a\nreader can tell a question from its answer without parsing either.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /agents/sessions/count", zip.Doc{
		Description: "Answers the active-session count the device view shows,\nunder the same tenancy and actor rules as the stop above.",
		Fields: map[string]string{
			"SessionMatchIn.host":    "Host/Provider/Account narrow WITHIN the actor's own sessions; empty is any.",
			"SessionMatchIn.subject": "Subject is the revoking user, unqualified. The answering side qualifies it.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /agents/sessions/stop", zip.Doc{
		Description: "Tears down every live session of the CALLER's org matching\nthe revoking subject, and reports how many it stopped.\n\nThe org is the caller's plane identity and never the argument — plane\n.SessionMatchIn has no org field, deliberately, because this op STOPS things\nand a caller able to state the org could stop a co-tenant's work. Anonymous is\nrefused rather than defaulted: a teardown arriving with no principal must\nfail, not pick a tenant.\n\nThe actor is built HERE, from the org the plane proved and the subject the\ncaller names, so the HIGH-1 actor scoping (a revoke stops only that user's own\nsessions) is enforced by the side that owns the store rather than trusted from\nthe wire.\n\nA named handler, not a closure, so zipdoc can lift this prose into the registry.",
		Fields: map[string]string{
			"SessionMatchIn.host":    "Host/Provider/Account narrow WITHIN the actor's own sessions; empty is any.",
			"SessionMatchIn.subject": "Subject is the revoking user, unqualified. The answering side qualifies it.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents", zip.Doc{
		Description: "Defines an agent in the caller's org: a model, a system prompt\n(instructions) and a set of tool names. The name must be unique in the org and\nmatch ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. An omitted model takes the\ndeployment's configured default; a named one is checked against the gateway's\nserved catalog, so a model this deployment never serves is refused here rather\nthan failing at run time. A long-running agent must carry a 5-field cron\nschedule (the scheduler would otherwise never fire it) and counts against a\nper-org cap on scheduled agents.",
		Fields: map[string]string{
			"agentView.avatar":               "Avatar is an image the agent is drawn as — a link to one, or the bytes\ninline as a data URL, up to 96 KiB. Emoji is the one glyph a caller picked\nwhen they had no image. At most one is ever set; neither means the agent is\ndrawn as its initial, the same way a person with no photo is. Both are\niam/pkg/schema's Mark, so a face means the same thing on an agent as it\ndoes on a person or an org.\nAvatar is the agent's picture: an image URL, or the image itself inline as a\ndata URL up to 96 KiB. Empty when the agent has no image.",
			"agentView.computeRef":           "ComputeRef is the visor machine this bot is bound to, opaque here: this\npackage stores and echoes it, and the binding's lifecycle belongs elsewhere.\nEmpty means unbound, which is what every one-shot agent is.",
			"agentView.createdAt":            "CreatedAt is when the agent was defined, RFC 3339 in UTC to the second.",
			"agentView.description":          "Description is the one line another agent reads when deciding whether to call\nthis one: the tool catalogue publishes it as the description of `agent_<name>`,\nfalling back to \"agent <name>\" when it is empty. It is not part of the prompt —\nInstructions is — so writing the behaviour here reaches the caller and not the\nmodel.",
			"agentView.emoji":                "Emoji is the single glyph a caller picked when they had no image. At most one\nof avatar and emoji is ever set; neither means the agent is drawn as its\ninitial, the same way a person with no photo is.",
			"agentView.executionMode":        "ExecutionMode is one-shot or long-running, and it decides who may start this\nagent. one-shot runs only when something POSTs to it; long-running is\nadditionally invoked by the scheduler on Schedule, once a minute against the\ncron. An org's long-running agents are capped, so a switch INTO it can be\nrefused with 409.",
			"agentView.id":                   "ID is the agent's stable handle, minted here as \"agent_\" + 32 hex characters\nof crypto/rand. A caller cannot choose it, and it never changes — unlike Name,\nwhich is the other way to address the same agent.",
			"agentView.model":                "Model is the Zen model this agent runs on, and it is always OUR name for it:\nwrites normalize through cloud.ZenModel and the read normalizes again, so an\nupstream family name never leaves here even from a row written before that\nrule existed. A create that named none took the deployment's configured\ndefault, so this is where a caller learns which model it actually got.",
			"agentView.name":                 "Name is the agent's org-unique handle, matching\n^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. It addresses the agent everywhere ID does,\nit is what a run row records, and it is the suffix of the `agent_<name>` tool\nother agents call this one by. Set once at create; no update route moves it,\nbecause moving it would orphan that history.",
			"agentView.runs":                 "Runs is how many executions the org has recorded against this agent, counted\nat read time. The list and update reads count the WHOLE history; the detail\nread reports the size of the RecentRuns page it carries, which stops at 20 —\nso a detail row saying 20 means \"at least 20\", not \"exactly 20\".",
			"agentView.schedule":             "Schedule is the 5-field cron the scheduler fires a long-running agent on,\nevaluated once a minute. Required for long-running and DROPPED for one-shot —\na one-shot agent's schedule is not stored, so absence here is the mode's\nanswer rather than a value nobody set.",
			"agentView.serviceAccountId":     "ServiceAccountID is the IAM agent service account (<org>-<agent>) a scheduled\nrun is billed AS. It is what makes an autonomous run attributable to a\nprincipal rather than only to the org; empty means the org itself wears the\nspend.",
			"agentView.status":               "Status is the agent's readiness, and today it is \"ready\" on every row: an\nagent is a definition rather than a provisioned thing, so nothing transitions\nit. Server-set at create; no route accepts it.",
			"agentView.tools":                "Tools are the tool names this agent may call, and the list IS the authority:\nan agent that declares none gets none. The single entry \"*\" means whatever the\nfleet's MCP server serves at the moment of the run, resolved per run rather\nthan frozen here, which is how the default assistant reaches subsystems that\nshipped after it was defined. Empty array, never null.",
			"agentView.updatedAt":            "UpdatedAt is the last time any field above was written, same format. It moves\non an update to the DEFINITION and never on a run, so a busy agent nobody has\nedited keeps an old one.",
			"createAgentIn.avatar":           "Avatar and Emoji are how the agent APPEARS. An image wins when both are\ngiven — it is the thing somebody made — and both empty leaves the agent\ndrawn as its initial. Validated by iam/pkg/schema, the same rule a person's\navatar passes, so the 96 KiB bound and the accepted URL forms are stated\nonce for every subject that has a face.",
			"createAgentIn.computeRef":       "ComputeRef optionally binds this bot to a visor machine. Opaque here, bounded\nat 256 characters, and not resolved — this package stores the reference and\nthe binding's lifecycle belongs elsewhere.",
			"createAgentIn.description":      "Description is the one line published as the description of the `agent_<name>`\ntool, which is how another agent decides whether to call this one. Optional,\nand worth writing for exactly that reason.",
			"createAgentIn.emoji":            "Emoji is the single glyph shown when there is no image. An image WINS when\nboth are given — it is the thing somebody made — and both empty leaves the\nagent drawn as its initial.",
			"createAgentIn.executionMode":    "ExecutionMode is one-shot or long-running. Empty takes one-shot, which runs\nonly when something POSTs to it. long-running additionally requires Schedule,\nand counts against a per-org cap that answers 409 when it is full.",
			"createAgentIn.instructions":     "Instructions is the system prompt, up to 32 KiB, stored verbatim. This is what\nthe model reads; Description is what other CALLERS read.",
			"createAgentIn.model":            "Model names the model to run on. Omit it to take the deployment's configured\ndefault; name one and it is checked against the gateway's served catalogue\nhere, so a model this deployment cannot serve is refused now rather than at\nthe first run. Stored under our own name for it, whatever spelling arrives.",
			"createAgentIn.name":             "Name is the agent's org-unique handle and the only required field. It must\nmatch ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$, and a name already taken in this org\nis a 409 rather than an overwrite. It is permanent: no update route moves it.",
			"createAgentIn.schedule":         "Schedule is the 5-field cron a long-running agent fires on, parsed here so a\nbad expression is a 400 and not an agent that silently never runs. Required\nwith long-running; DISCARDED for one-shot rather than stored unused.",
			"createAgentIn.serviceAccountId": "ServiceAccountID optionally names the IAM agent service account (<org>-<agent>)\na scheduled run should be billed AS, so an autonomous run is attributable to a\nprincipal rather than only to the org. Same 256-character bound, also\nunresolved here.",
			"createAgentIn.tools":            "Tools are the tool names this agent may call. Omitted or empty grants NONE —\nthat default is the agent's authority and is not widened anywhere. The single\nentry \"*\" means whatever the fleet's MCP server serves at the time of each run.",
		},
		Example: json.RawMessage(`{"name":"helper","model":"enso-flash","instructions":"be terse"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/:ref/run", zip.Doc{
		Description: "Executes the agent: it composes the agent's instructions with the caller\ninput and runs a real chat completion via the in-process AI client, then\nrecords the run. Every returned run reflects an execution that actually\nhappened — an inference failure is recorded and returned as an error run, not\nhidden and not fabricated.",
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/sessions", zip.Doc{
		Description: "Opens a live agent session in the caller's org — the row every\nsurface (the CLI's outer agent, hanzo.bot, the console, chat) hangs its\nactivity off. A session with a parentSessionId becomes a subagent of that\nsession and inherits its root, so one flow is one tree; without one it is\nitself a root. Registering with a terminal status records a session that has\nalready finished.",
		Fields: map[string]string{
			"lastEventView.actor":         "Actor is who produced the turn, defaulted to the calling principal when the\nwriter named nobody.",
			"lastEventView.at":            "At is when the turn was recorded, RFC 3339 in UTC to the second.",
			"lastEventView.kind":          "Kind is what the turn was, from the log's closed six: message, tool-call,\nspawn, log, status, control.",
			"lastEventView.preview":       "Preview is the first 240 bytes of the event's payload, cut without regard for\nthe JSON inside it — it is a string to SHOW, never a value to parse. Read the\ndetail or the stream for the whole payload.",
			"lastEventView.seq":           "Seq is that event's position in the session's log — monotonic from 1, per\nsession. A reader holding it can ask the detail or stream reads for\neverything after it, so this doubles as the list's resume cursor.",
			"registerReq.account":         "Account is which subscription or API account under that provider served the\nrun, up to 256 characters. It is what lets a revoke of that login stop exactly\nthe sessions it was paying for.",
			"registerReq.actor":           "Actor is the \"org/sub\" identity to record the session under, up to 256\ncharacters. Omit it and the calling principal is used, which is almost always\nwhat you want: it is what a login revoke matches on to stop this session.",
			"registerReq.agent":           "Agent is the label the surface opening this session calls itself by\n(\"hanzo-dev\"). REQUIRED, up to 128 characters, and free text — nothing\nresolves it against a defined agent.",
			"registerReq.cwd":             "Cwd is the directory the session starts in, up to 1024 characters. It can be\nmoved later, because a linked shell walks around.",
			"registerReq.host":            "Execution context — where this session runs (all optional).",
			"registerReq.parentSessionId": "ParentSessionID makes this a subagent of that session: it inherits the\nparent's root, so one flow stays one tree. The parent must exist IN THE SAME\nORG — a foreign or unknown id is a 400, never a tree across tenants. Empty\nopens a root session.",
			"registerReq.project":         "The readable build (provenance.go): which product this session builds, and\nwhether its story may be read by the world.",
			"registerReq.provider":        "Account tag — the linked AI account this session ran under (login manager).",
			"registerReq.published":       "Published opens this session's story to the public build route. It is refused\nwithout a Project, because that route is keyed on (org, project) — a build\nwith no product is not a story anyone can open. False keeps it org-only.",
			"registerReq.repo":            "Repo is the code being worked on, up to 512 characters. A label the surface\nstates; nothing resolves it against the forge.",
			"registerReq.room":            "Room is the collaborative room this run was started in (HIP-0523), so a\nspace view can list the sessions of one room. It is PROVENANCE and is set\nonly here: there is deliberately no way to move a session to another room, so\nit is absent from the patch input and from UpdateSession's SET list.",
			"registerReq.status":          "Status opens the session in one of running, paused, done or error. Empty means\nrunning. A TERMINAL status here (done, error) records a session that has\nalready finished — its end time is stamped now — and nothing can move it\nafterwards.",
			"registerReq.target":          "Target names a run-target the org has registered. Unlike Host and Repo it IS\nresolved: a target that does not exist in this org is a 400, so a session can\nnever claim to run on another tenant's machine. Empty names no machine.",
			"registerReq.taskRunId":       "TaskRunID is that workflow's particular run, same bound. Recorded, not\nresolved: this surface does not check the workflow exists.",
			"registerReq.taskWorkflowId":  "TaskWorkflowID links this session to the hanzoai/tasks workflow that executes\nit, up to 256 characters. Set it and control commands are forwarded to that\nengine; leave it and the running surface polls for them instead.",
			"registerReq.terminal":        "Terminal is the URL this session's live terminal is published at, so the\nconsole can watch it. Optional — a session that publishes nothing is still\na session.",
			"registerReq.title":           "Title is the human line a card shows, up to 512 characters. Optional, and\nchangeable later.",
			"sessionProgress.activity":    "Activity is the one line saying what the run is doing right now (\"running\nthe reaper's tests\"), up to 120 characters, in the model's words. Empty\nwhen nothing has estimated it yet.",
			"sessionProgress.at":          "At is when this was determined, RFC 3339 in UTC to the second. Read it as\nthe estimate's AGE: an estimate is not refreshed while nothing has\nhappened, and a stale one beside a run that is still moving is itself worth\nseeing. Empty when nothing has estimated it yet.",
			"sessionProgress.estimated":   "Estimated says a MODEL produced this, from the run's transcript, and it may\nbe wrong. False means the session's own row said it: a finished run is 100%\nbecause it finished, not because anything guessed. Never treat a true here\nas a measurement — it is the reason to look, not the answer.",
			"sessionProgress.pct":         "Pct is how much of the run is done, 0 to 100. THE KEY IS ABSENT when\nprogress is indeterminate — a run nobody can estimate is not a run that has\ndone nothing, and rendering the second for the first is the mistake this\nomission exists to make impossible. Read `phase` before reaching for it.",
			"sessionProgress.phase":       "Phase is what shape the run is in: running, blocked, done, error, or\nunknown when nothing has estimated it yet. blocked means the transcript\nshows the run waiting on something — an approval, a credential, an answer —\nwhich is the one state the running surface cannot report about itself.\nerror only ever comes from the session's own terminal status.",
			"sessionView.account":         "Account is which subscription or API account under that provider served it.\nTogether with Provider it is what a login revoke matches on to stop the\nsessions a withdrawn account was paying for.",
			"sessionView.actor":           "Actor is WHO this session belongs to, as \"org/sub\" — the same identity a run\nis billed under. A register that names none takes the calling principal. It is\nwhat scopes a login revoke, so a session with the wrong actor is a session the\nright person cannot stop.",
			"sessionView.agent":           "Agent is the label the surface running this session calls itself by\n(\"hanzo-dev\"), up to 128 characters. Required at register. It is free text,\nnot a reference: it need not name a defined agent, and nothing resolves it.",
			"sessionView.children":        "Children is the DIRECT fan-out — how many sessions name this one as parent —\nand not the size of the subtree. Read the tree for that.",
			"sessionView.createdAt":       "CreatedAt is when the row was written, same format. Every path that opens a\nsession stamps it and StartedAt from one clock reading, so the two are equal\non every session this surface has ever produced.",
			"sessionView.cwd":             "Cwd is the directory the session is working in NOW, not the one it started in:\na linked shell moves around, and a card showing where `hanzo link` was run\nanswers \"which work is this\" with something that was true once.",
			"sessionView.endedAt":         "EndedAt is when it reached done or error, same format. Empty while it is still\nrunning or paused, which is how absence reads here: not over yet.",
			"sessionView.events":          "Events is how many turns the session's log holds, counted at read time. It is\nthe whole log, however few of them RecentEvents carries.",
			"sessionView.host":            "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.id":              "ID is the session's handle, minted here as \"sess_\" + 32 hex characters. Every\nlater read, patch, event append and control command is addressed with it, and\na caller cannot choose it.",
			"sessionView.lastEvent":       "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":             "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.parentSessionId": "ParentSessionID is the session that spawned this one, making this a subagent\nof it. Empty means this session is a root — a flow of its own. A parent always\nbelongs to the same org, so a tree never crosses a tenant.",
			"sessionView.progress":        "Progress is how far along this run is — a share of its goal, a phase, and a\nline saying what it is doing. Always present, so a board never branches on\nwhether it is there; `phase` says \"unknown\" when nothing has estimated it.\nIt is a MODEL ESTIMATE wherever `estimated` is true, and the row's own word\nwhere it is false. See progress.go.",
			"sessionView.project":         "The readable build: the product this session built and whether its story\nis public (provenance.go).",
			"sessionView.provider":        "Provider is the linked AI account's provider (claude | codex | hanzo | …) that\nserved this run. Empty when the surface did not say.",
			"sessionView.published":       "Published is the author's decision to let anyone read this session's story at\nthe public build route. It only ever widens READ access to a session that\nalready exists and grants nothing else; false, an unpublished session is\ninvisible there no matter who asks. It cannot be true without a Project,\nbecause that route is keyed on (org, project).",
			"sessionView.repo":            "Repo is the code the session is working on, as the surface reported it. It is\ntruth the SURFACE states, so it is a label rather than something resolved here.",
			"sessionView.room":            "Room is the collaborative room this run was started in (HIP-0523), empty\nwhen it came from anywhere else — a CLI, a schedule, an API call. It is what\nlets a space view show the runs of one room beside its messages.",
			"sessionView.rootSessionId":   "RootSessionID is the top of this session's tree, inherited from the parent and\nshared by every node in one flow. A root session's own id, when it has no\nparent. It is the key one indexed read pulls a whole flow by, and what ?root=\nnarrows a list or a stream to.",
			"sessionView.startedAt":       "StartedAt is when the session opened, RFC 3339 in UTC to the second.",
			"sessionView.status":          "Status is one of exactly four: running, paused, done, error. running and\npaused are LIVE; done and error are TERMINAL and monotonic — once a session\nreaches one it can never go back, because reopening a finished run would\nfabricate liveness. A control command never moves it: the surface running the\nagent reports the new status, and until it does the command is only recorded.",
			"sessionView.target":          "Target is the registered run-target this session is dispatched to — a machine\nthe org claimed, resolved same-org when it was set, so it can never point at\nanother tenant's computer. Empty means the session names no machine.",
			"sessionView.taskRunId":       "TaskRunID is that workflow's particular run. A workflow is the definition and\na run is one execution of it, which is why both are carried.",
			"sessionView.taskWorkflowId":  "TaskWorkflowID is the hanzoai/tasks durable workflow that actually EXECUTES\nthis session — this registry is the view, control and stream layer over it.\nSet, a control command is FORWARDED to that engine; empty, the running surface\npolls for commands instead, which is every session today.",
			"sessionView.terminal":        "Terminal is where this session can be WATCHED — the URL the machine\npublished for its live terminal. Omitted when it publishes none.",
			"sessionView.title":           "Title is the human line a card shows (\"ship the landing page\"), up to 512\ncharacters. Free text, and the one field a surface may rewrite as the work\nturns out to be something else.",
			"sessionView.updatedAt":       "UpdatedAt is the session's last-activity clock, same format. It moves on a\nwrite to the row — a status, a title, a re-dispatch — AND on every appended\nturn, because the append bumps it in the same transaction. The list is ordered\non CreatedAt, so this is the field that says whether a session is still saying\nanything.",
		},
		Example: json.RawMessage(`{"agent":"hanzo-dev","title":"ship the landing page","host":"gpu-01"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/sessions/:id/events", zip.Doc{
		Description: "Records one turn of a session's transcript and answers 201 with it.\n\nA `progress` turn additionally MOVES THE SESSION'S PROGRESS, marked as the run's\nown word rather than an estimate, and pushes the updated session onto the live\nstream — so a board's bar follows the run without polling and without a second\nwrite path. See progress.go.\n\nTHE TURN IS SCANNED BEFORE IT IS STORED. The same engine the code-security\nsurface runs reads the payload at this boundary, and a credential in it refuses\nthe append with 422 rather than redacting it — a redacted transcript is one\nthat still had the secret in it once, and this way the author learns which\nvalue to rotate. The refusal carries every finding: the rule, the severity, the\nline, a MASKED preview and the fingerprint. The secret is never in the answer.",
		Fields: map[string]string{
			"eventIn.actor":       "Actor is who produced the turn. Empty takes the validated caller, which is\nwhat an agent writing its own transcript wants; naming one is for a surface\nrecording on somebody else's behalf.",
			"eventIn.id":          "ID is the session to append to, from the path.",
			"eventIn.kind":        "Kind is what this turn IS: message, tool-call, spawn, log, status, control\nor progress. Anything else is refused — the vocabulary is closed so a\nreader can branch on it.",
			"eventIn.payload":     "Payload is the turn's own body, any valid JSON up to 64 KiB. It is SCANNED\nfor credentials before it is stored, and a hit refuses the whole append with\n422 and the findings — this plane never redacts a secret into a transcript\nit then keeps.\n\nIts SHAPE is the writer's business for every kind but one: a `progress`\nturn is the run reporting on itself in a shape this surface publishes —\n{\"pct\":0-100 (optional), \"phase\":\"running|blocked|done\",\n\"activity\":\"≤120 chars\"} — so a malformed one is refused with 400 instead\nof being stored as prose nobody reads.",
			"eventView.actor":     "Actor is who produced the turn. A write that names nobody takes the calling\nprincipal, so this is rarely empty in practice.",
			"eventView.createdAt": "CreatedAt is when the turn was recorded, RFC 3339 in UTC to the second. Seconds\nare coarse enough that two turns can share one, which is why Seq and not this\nis the order.",
			"eventView.id":        "ID is the event's own handle, minted as \"evt_\" + 32 hex characters. It\nidentifies the turn; Seq is what ORDERS it.",
			"eventView.kind":      "Kind is what the turn IS, from a closed six: message (a model turn),\ntool-call, spawn (a subagent started), log, status, control (a steering\ncommand the running surface consumes). Anything else is refused at the write.",
			"eventView.payload":   "Payload is the turn's body, embedded as JSON rather than as a string —\nwhatever the writer sent, up to 64 KiB, checked for well-formedness and\nscanned for credentials before it was stored. Its SHAPE is the writer's\nbusiness and varies by Kind; this surface does not interpret it.",
			"eventView.seq":       "Seq is the turn's position in this session's log: monotonic from 1, assigned\nby the store inside the insert, and unique PER SESSION rather than globally.\nIt is the cursor a reader resumes from after a reconnect — ask for everything\nafter your last-seen seq.",
			"eventView.sessionId": "SessionID is the session this turn belongs to. Carried on every event so a\nstream frame stands alone — a subscriber watching a whole tree gets turns from\nseveral sessions down one connection.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/sessions/:id/message", zip.Doc{
		Description: "Sends a steering message to a running session — the endpoint a\nhuman or another agent interrupts through. It requires a `message` or a\n`payload`; the other three commands do not.",
		Fields: map[string]string{
			"controlIn.id":            "ID is the session to steer, from the path.",
			"controlIn.message":       "Message is free text for the running agent, up to 16 KiB. On a stop it is\nrecorded as the cancellation reason.",
			"controlIn.payload":       "Payload is a structured argument for the command: any valid JSON up to\n64 KiB, scanned for credentials before it is stored (a hit refuses the\ncommand with 422 and the findings). A task-backed session receives it as\nthe forwarded signal's argument.",
			"controlResult.command":   "Command is the verb that was recorded: pause, resume, stop or message.",
			"controlResult.event":     "Event is the durable control event the command became. The intent is\nrecorded whether or not it reached an engine, which is what makes a\nstream-consuming surface able to act on it.",
			"controlResult.forwarded": "Forwarded is whether the command also reached the durable-execution engine.\nFALSE IS NOT A FAILURE: a session with no workflow link, or a deployment\nwith no tasks backend, is record-only by design. A forward that was\nattempted and failed is a 502, never a false here.",
			"eventView.actor":         "Actor is who produced the turn. A write that names nobody takes the calling\nprincipal, so this is rarely empty in practice.",
			"eventView.createdAt":     "CreatedAt is when the turn was recorded, RFC 3339 in UTC to the second. Seconds\nare coarse enough that two turns can share one, which is why Seq and not this\nis the order.",
			"eventView.id":            "ID is the event's own handle, minted as \"evt_\" + 32 hex characters. It\nidentifies the turn; Seq is what ORDERS it.",
			"eventView.kind":          "Kind is what the turn IS, from a closed six: message (a model turn),\ntool-call, spawn (a subagent started), log, status, control (a steering\ncommand the running surface consumes). Anything else is refused at the write.",
			"eventView.payload":       "Payload is the turn's body, embedded as JSON rather than as a string —\nwhatever the writer sent, up to 64 KiB, checked for well-formedness and\nscanned for credentials before it was stored. Its SHAPE is the writer's\nbusiness and varies by Kind; this surface does not interpret it.",
			"eventView.seq":           "Seq is the turn's position in this session's log: monotonic from 1, assigned\nby the store inside the insert, and unique PER SESSION rather than globally.\nIt is the cursor a reader resumes from after a reconnect — ask for everything\nafter your last-seen seq.",
			"eventView.sessionId":     "SessionID is the session this turn belongs to. Carried on every event so a\nstream frame stands alone — a subscriber watching a whole tree gets turns from\nseveral sessions down one connection.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/sessions/:id/pause", zip.Doc{
		Description: "Asks a running session to pause. Recorded durably, and forwarded\nto the durable-execution engine when the session is task-backed.",
		Fields: map[string]string{
			"controlIn.id":            "ID is the session to steer, from the path.",
			"controlIn.message":       "Message is free text for the running agent, up to 16 KiB. On a stop it is\nrecorded as the cancellation reason.",
			"controlIn.payload":       "Payload is a structured argument for the command: any valid JSON up to\n64 KiB, scanned for credentials before it is stored (a hit refuses the\ncommand with 422 and the findings). A task-backed session receives it as\nthe forwarded signal's argument.",
			"controlResult.command":   "Command is the verb that was recorded: pause, resume, stop or message.",
			"controlResult.event":     "Event is the durable control event the command became. The intent is\nrecorded whether or not it reached an engine, which is what makes a\nstream-consuming surface able to act on it.",
			"controlResult.forwarded": "Forwarded is whether the command also reached the durable-execution engine.\nFALSE IS NOT A FAILURE: a session with no workflow link, or a deployment\nwith no tasks backend, is record-only by design. A forward that was\nattempted and failed is a 502, never a false here.",
			"eventView.actor":         "Actor is who produced the turn. A write that names nobody takes the calling\nprincipal, so this is rarely empty in practice.",
			"eventView.createdAt":     "CreatedAt is when the turn was recorded, RFC 3339 in UTC to the second. Seconds\nare coarse enough that two turns can share one, which is why Seq and not this\nis the order.",
			"eventView.id":            "ID is the event's own handle, minted as \"evt_\" + 32 hex characters. It\nidentifies the turn; Seq is what ORDERS it.",
			"eventView.kind":          "Kind is what the turn IS, from a closed six: message (a model turn),\ntool-call, spawn (a subagent started), log, status, control (a steering\ncommand the running surface consumes). Anything else is refused at the write.",
			"eventView.payload":       "Payload is the turn's body, embedded as JSON rather than as a string —\nwhatever the writer sent, up to 64 KiB, checked for well-formedness and\nscanned for credentials before it was stored. Its SHAPE is the writer's\nbusiness and varies by Kind; this surface does not interpret it.",
			"eventView.seq":           "Seq is the turn's position in this session's log: monotonic from 1, assigned\nby the store inside the insert, and unique PER SESSION rather than globally.\nIt is the cursor a reader resumes from after a reconnect — ask for everything\nafter your last-seen seq.",
			"eventView.sessionId":     "SessionID is the session this turn belongs to. Carried on every event so a\nstream frame stands alone — a subscriber watching a whole tree gets turns from\nseveral sessions down one connection.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/sessions/:id/resume", zip.Doc{
		Description: "Asks a paused session to continue, on the same terms as a pause.",
		Fields: map[string]string{
			"controlIn.id":            "ID is the session to steer, from the path.",
			"controlIn.message":       "Message is free text for the running agent, up to 16 KiB. On a stop it is\nrecorded as the cancellation reason.",
			"controlIn.payload":       "Payload is a structured argument for the command: any valid JSON up to\n64 KiB, scanned for credentials before it is stored (a hit refuses the\ncommand with 422 and the findings). A task-backed session receives it as\nthe forwarded signal's argument.",
			"controlResult.command":   "Command is the verb that was recorded: pause, resume, stop or message.",
			"controlResult.event":     "Event is the durable control event the command became. The intent is\nrecorded whether or not it reached an engine, which is what makes a\nstream-consuming surface able to act on it.",
			"controlResult.forwarded": "Forwarded is whether the command also reached the durable-execution engine.\nFALSE IS NOT A FAILURE: a session with no workflow link, or a deployment\nwith no tasks backend, is record-only by design. A forward that was\nattempted and failed is a 502, never a false here.",
			"eventView.actor":         "Actor is who produced the turn. A write that names nobody takes the calling\nprincipal, so this is rarely empty in practice.",
			"eventView.createdAt":     "CreatedAt is when the turn was recorded, RFC 3339 in UTC to the second. Seconds\nare coarse enough that two turns can share one, which is why Seq and not this\nis the order.",
			"eventView.id":            "ID is the event's own handle, minted as \"evt_\" + 32 hex characters. It\nidentifies the turn; Seq is what ORDERS it.",
			"eventView.kind":          "Kind is what the turn IS, from a closed six: message (a model turn),\ntool-call, spawn (a subagent started), log, status, control (a steering\ncommand the running surface consumes). Anything else is refused at the write.",
			"eventView.payload":       "Payload is the turn's body, embedded as JSON rather than as a string —\nwhatever the writer sent, up to 64 KiB, checked for well-formedness and\nscanned for credentials before it was stored. Its SHAPE is the writer's\nbusiness and varies by Kind; this surface does not interpret it.",
			"eventView.seq":           "Seq is the turn's position in this session's log: monotonic from 1, assigned\nby the store inside the insert, and unique PER SESSION rather than globally.\nIt is the cursor a reader resumes from after a reconnect — ask for everything\nafter your last-seen seq.",
			"eventView.sessionId":     "SessionID is the session this turn belongs to. Carried on every event so a\nstream frame stands alone — a subscriber watching a whole tree gets turns from\nseveral sessions down one connection.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/sessions/:id/stop", zip.Doc{
		Description: "Ends a running session. `message` is recorded as the cancellation\nreason, which is what a later reader of the transcript sees.\n\nSTOPPING IS NOT DELETING: the session, its transcript and anything it produced\nstay readable. A session that has already finished is 409 rather than a second\nstop.",
		Fields: map[string]string{
			"controlIn.id":            "ID is the session to steer, from the path.",
			"controlIn.message":       "Message is free text for the running agent, up to 16 KiB. On a stop it is\nrecorded as the cancellation reason.",
			"controlIn.payload":       "Payload is a structured argument for the command: any valid JSON up to\n64 KiB, scanned for credentials before it is stored (a hit refuses the\ncommand with 422 and the findings). A task-backed session receives it as\nthe forwarded signal's argument.",
			"controlResult.command":   "Command is the verb that was recorded: pause, resume, stop or message.",
			"controlResult.event":     "Event is the durable control event the command became. The intent is\nrecorded whether or not it reached an engine, which is what makes a\nstream-consuming surface able to act on it.",
			"controlResult.forwarded": "Forwarded is whether the command also reached the durable-execution engine.\nFALSE IS NOT A FAILURE: a session with no workflow link, or a deployment\nwith no tasks backend, is record-only by design. A forward that was\nattempted and failed is a 502, never a false here.",
			"eventView.actor":         "Actor is who produced the turn. A write that names nobody takes the calling\nprincipal, so this is rarely empty in practice.",
			"eventView.createdAt":     "CreatedAt is when the turn was recorded, RFC 3339 in UTC to the second. Seconds\nare coarse enough that two turns can share one, which is why Seq and not this\nis the order.",
			"eventView.id":            "ID is the event's own handle, minted as \"evt_\" + 32 hex characters. It\nidentifies the turn; Seq is what ORDERS it.",
			"eventView.kind":          "Kind is what the turn IS, from a closed six: message (a model turn),\ntool-call, spawn (a subagent started), log, status, control (a steering\ncommand the running surface consumes). Anything else is refused at the write.",
			"eventView.payload":       "Payload is the turn's body, embedded as JSON rather than as a string —\nwhatever the writer sent, up to 64 KiB, checked for well-formedness and\nscanned for credentials before it was stored. Its SHAPE is the writer's\nbusiness and varies by Kind; this surface does not interpret it.",
			"eventView.seq":           "Seq is the turn's position in this session's log: monotonic from 1, assigned\nby the store inside the insert, and unique PER SESSION rather than globally.\nIt is the cursor a reader resumes from after a reconnect — ask for everything\nafter your last-seen seq.",
			"eventView.sessionId":     "SessionID is the session this turn belongs to. Carried on every event so a\nstream frame stands alone — a subscriber watching a whole tree gets turns from\nseveral sessions down one connection.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/targets", zip.Doc{
		Description: "Registers a machine as an agent target, or re-links one that is\nalready registered. Re-linking is idempotent and keyed on org+host+owner, so a\nmachine that reconnects refreshes its own row rather than piling up duplicates;\nit answers 200, while a first registration answers 201.",
		Fields: map[string]string{
			"GPU.memory":           "VRAM bytes, 0 = unknown",
			"GPU.model":            "\"GB10\", \"8060S\", \"RTX 4090\"",
			"GPU.vendor":           "nvidia | amd | apple | intel | ...",
			"Metrics.at":           "unix seconds, server-stamped",
			"Metrics.gpuUtil":      "0..1 aggregate utilization",
			"Metrics.load1":        "Load1 is the machine's own one-minute load average — a count of runnable and\nuninterruptible tasks, NOT a percentage and NOT already divided by core\ncount, so it is read against Spec.CPUs: 8.0 is idle on 16 cores and swamped\non 4. Coerced finite and non-negative on write, so 0 means either genuinely\nidle or nothing reported.",
			"Metrics.load15":       "Load15 is the same figure over fifteen. The three together are what separate\na machine that is busy right now from one that has been busy all along —\nwhich is the question a dispatcher is really asking.",
			"Metrics.load5":        "Load5 is the same figure averaged over five minutes.",
			"Metrics.memFree":      "bytes",
			"Metrics.memUsed":      "bytes",
			"Spec.arch":            "amd64 | arm64 | ...",
			"Spec.cpus":            "logical cores",
			"Spec.gpus":            "GPUs is every accelerator the machine advertises, one entry each, capped at\n32 on write. Empty means the probe found none — and that is the answer a\nNeed is checked against, so a machine with no entry here clears no\naccelerator floor. The list is not vendor-filtered: what satisfies a job is\ncounts and VRAM, never a brand (see Need).",
			"Spec.memory":          "total RAM, bytes",
			"Spec.os":              "linux | darwin | windows",
			"targetReq.capacity":   "Capacity is a human summary of the machine's size, up to 256 characters. Prose\nonly; a scheduler reads Spec.",
			"targetReq.host":       "Host is the hostname sessions on this machine will report. It is what makes a\nre-link IDEMPOTENT: the same (org, host, owner) refreshes the existing row and\nanswers 200, while a request with no host always creates a new target and\nanswers 201. It never adopts a row owned by somebody else.",
			"targetReq.kind":       "Kind is laptop | cloud | gpu | cluster | machine. Empty registers a `machine`;\nanything outside the five is a 400.",
			"targetReq.label":      "Label is the name to show for this machine, up to 128 characters. REQUIRED —\nit is the only field here a person reads.",
			"targetReq.metrics":    "Metrics is a live sample, and sending one IS A HEARTBEAT: it refreshes the row\nand starts the 90-second liveness window, and it is appended to the fleet\nseries as one point. Its own `at` is ignored — the server stamps the time, so\na client can never age or backdate its own machine. Omit it to register a\nmachine without claiming it is alive.",
			"targetReq.spec":       "Spec is the machine's static capability — os, arch, cores, RAM, accelerators.\nEvery field is bounded on write and at most 32 accelerators are accepted, so\nwhat comes back may be clamped. Omit it for a destination nothing probes.",
			"targetReq.status":     "Status is online | offline | draining. Empty registers `online`. It states\nINTENT — a heartbeat is what decides whether an online machine is actually\nreachable, so declaring online does not make it so.",
			"targetView.capacity":  "Capacity is a human summary of what the machine has (\"8 vCPU / 32G\", \"1× GB10\"),\nup to 256 characters. Prose for a card — Spec is the same thing in a form a\nscheduler can read, and nothing derives one from the other.",
			"targetView.createdAt": "CreatedAt is when the machine was first registered, RFC 3339 in UTC. A re-link\nrefreshes the row and leaves this alone, so it dates the machine and not the\nconnection.",
			"targetView.host":      "Host is the hostname sessions on this machine report, and it is a JOIN KEY, not\na label: a session naming this host counts against the load below even when it\nnames no target id, and a re-link of the same (org, host, owner) refreshes this\nrow instead of creating a second. Empty means the machine is addressable only\nby ID.",
			"targetView.id":        "ID is the machine's handle, minted as \"tgt_\" + 32 hex characters. It is what a\nsession records to say it ran here, and what every later patch, claim or\ndelete addresses.",
			"targetView.kind":      "Kind is what sort of destination this is, from a closed five: laptop | cloud |\ngpu | cluster | machine. A register that named none is a `machine`.",
			"targetView.label":     "Label is the name a person gave the machine (\"workshop\"), up to 128\ncharacters. Required at register, free text, and the only field here meant for\nreading rather than matching.",
			"targetView.metrics":   "Metrics is what the machine was DOING at its last heartbeat — loadavg, memory,\naccelerator utilization. Absent when it has never beaten. It is a SNAPSHOT:\nthe series over time lives in the fleet samples, not here.",
			"targetView.metricsAt": "MetricsAt is when that heartbeat was recorded, RFC 3339 in UTC, and the SERVER\nstamps it — a client cannot backdate or forge the staleness clock. Absent means\nnever beaten, which is exactly the case where Status is taken at its word.",
			"targetView.running":   "Running is how many of those are in `running` right now — the number a\ndispatcher weighs against Capacity. paused sessions are in Sessions and not\nhere.",
			"targetView.sessions":  "Sessions is how many of the org's sessions are mapped to this machine, by\ntarget id OR by matching Host. All of them, whatever their status.",
			"targetView.spec":      "Spec is what the machine IS — os, arch, cores, RAM, accelerators — the static\nhalf, changed only when something reports it again. Absent when nothing has\never been reported, and a scheduler reads absence as \"cannot satisfy a floor\"\nrather than as \"no limits\".",
			"targetView.status":    "Status is the EFFECTIVE liveness — online | offline | draining — not the\nstored one. offline and draining are operator INTENT and are reported as they\nstand; `online` is checked against the heartbeat, and a machine that has beaten\nbefore but not in the last 90 seconds reports offline whatever its row says. A\ntarget that has NEVER beaten keeps its stored status, because a hand-registered\ndestination has no fact to check.",
			"targetView.updatedAt": "UpdatedAt is the last write to the row, same format — which for a beating\nmachine is its last heartbeat, since a heartbeat IS a write.",
		},
		Example: json.RawMessage(`{"label":"workshop","kind":"gpu","host":"gpu-01"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/targets/:id/claim", zip.Doc{
		Description: "ClaimRoutedRun is the machine's long poll for work: it authenticates the\ndaemon, stamps the liveness the dispatch gate reads (the poll IS the proof a\nrunner is listening), and waits up to 25 seconds for the next run addressed to\nTHIS machine. It answers the run when one arrives and 204 with no body when the\nwindow elapses, on which the daemon re-polls immediately.\n\nTWO independent proofs are required and both fail closed to the same 403: the\ncaller must own this machine (or be an org admin) AND present its claim key in\nX-Target-Key. A run offered to one machine is unreachable from another's claim.",
		Fields: map[string]string{
			"routedRunOut.base":           "Base is the branch to start FROM. Empty means the repository's default —\nresolve it on the machine, since the machine is the one holding the clone.",
			"routedRunOut.branch":         "Branch is the ref the run must push its work to, and the ONLY one it is\npermitted to write: the forge's ref policy refuses anything else from this\nrun's credential. It is decided at dispatch and exists before the work does.",
			"routedRunOut.cloneUrl":       "CloneURL is how to fetch the repository. It carries NO credential — the\nmachine authenticates with the git identity it already holds — which is why\nthis whole shape is safe to hand to a claimed runner.",
			"routedRunOut.project":        "Project is the product slug the run is filed under, so the machine can tag\nwhat it produces. Empty when the dispatch named none.",
			"routedRunOut.prompt":         "Prompt is the task, in full, as the person wrote it. There is no second field\nfor context.",
			"routedRunOut.repo":           "Repo is the repository to work in and CloneURL is how to fetch it.",
			"routedRunOut.sessionId":      "SessionID is the live session opened at dispatch; the machine streams its\nturns into it.",
			"routedRunOut.timeoutSeconds": "TimeoutSeconds bounds the run on the machine; 0 means the machine's own default.",
			"targetRef.id":                "ID is the target to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"tgt_1"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/targets/:id/key", zip.Doc{
		Description: "Mints (or rotates) the claim key a `hanzo code --serve`\ndaemon presents to claim work for this machine, and returns it ONCE: only its\nSHA-256 hash is stored. Rotating supersedes any prior daemon, so only the\nmachine's owner — or an org admin — may call it; every other caller gets the\nsame not-found an unknown id gets, and learns nothing about what exists.",
		Fields: map[string]string{
			"claimKeyOut.claimKey": "ClaimKey is the capability itself. It is returned ONCE and never again — only\nits SHA-256 hash is stored — so a daemon that loses it mints a new one.",
			"claimKeyOut.targetId": "TargetID is the machine the key authenticates.",
			"targetRef.id":         "ID is the target to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"tgt_1"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/agents POST /v1/agents/targets/:id/runs/:runId/report", zip.Doc{
		Description: "Completes a claimed run: it delivers the terminal result to the\nrun's durable owner, which is what lets that workflow finish. Scoped to (org,\ntarget, run) and claim-key authenticated, so a machine can only ever report a\nrun it legitimately holds. Idempotent — a report for an unknown or\nalready-finished run answers delivered:false rather than failing, because the\nsession's terminal state was already set by the machine's own stream.",
		Fields: map[string]string{
			"reportOut.delivered":   "Delivered is true when a waiting durable owner received this result. False\nmeans there was none to deliver to — an unknown or already-finished run — which\nis a clean no-op, not an error.",
			"reportRunIn.branch":    "Branch, CommitSha and Diffstat describe what the run produced; Error is the\nfailure when OK is false. Each is clamped, never rejected.",
			"reportRunIn.changed":   "Changed says whether the run produced any commit. It is INDEPENDENT of OK: a\nrun can succeed and change nothing (there was nothing to do), and a run can\nfail after committing some of its work. Two questions, two booleans.",
			"reportRunIn.commitSha": "CommitSha is the tip the run pushed, clamped to 128 characters. Empty when it\npushed nothing, which is the same case Changed reports false for.",
			"reportRunIn.diffstat":  "Diffstat is the run's own summary of what it changed, as text, clamped to\n64 KiB. Free-form: it is shown, never parsed.",
			"reportRunIn.error":     "Error is why the run failed, clamped to 64 KiB. It is CLAMPED rather than\nrefused — a truncated reason is worth more than a rejected report, because a\nrejected report leaves the durable workflow waiting forever.",
			"reportRunIn.id":        "ID is the machine reporting, from the path.",
			"reportRunIn.ok":        "OK is whether the run succeeded; Changed whether it produced any commit.",
			"reportRunIn.runId":     "RunID is the routed run being completed, from the path.",
		},
		Example: json.RawMessage(`{"id":"tgt_1","runId":"run_1","ok":true,"changed":true,"branch":"hanzo/fix"}`),
	})
}
