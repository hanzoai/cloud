// Code generated by zipdoc; DO NOT EDIT.

package agents

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/agents/:ref", zip.Doc{
		Description: "deleteAgent removes one agent and its recorded run history, answering 204.\nThe ref resolves by public id or org-unique name.\nAn agent in another org, or one already gone, reads as not-found.",
		Fields: map[string]string{
			"agentRef.ref": "Ref is the agent's public id or its org-unique name, from the path.",
		},
		Example: json.RawMessage(`{"ref":"triage"}`),
	})
	zip.Describe("DELETE /v1/agents/targets/:id", zip.Doc{
		Description: "deleteTarget deregisters one machine.\nOnly its owner, or an org admin, may remove it; an unknown id, a cross-org id\nand a machine owned by someone else all answer the same not-found, so a probe\nlearns nothing about what exists.",
		Fields: map[string]string{
			"targetDeleted.deleted": "Deleted is true when the target was removed.",
			"targetDeleted.id":      "ID is the target that was removed.",
			"targetRef.id":          "ID is the target to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"tgt_1"}`),
	})
	zip.Describe("GET /v1/agents", zip.Doc{
		Description: "listAgents returns every agent in the caller's org with its run count.\nThe count is of runs actually recorded, so it matches what the run history\nwill show.",
		Fields: map[string]string{
			"agentList.agents": "Agents is the org's agents, each with its recorded run count.",
		},
	})
	zip.Describe("GET /v1/agents/:ref", zip.Doc{
		Description: "getAgent returns one agent's configuration and its 20 most recent runs.\nThe ref is the agent's public id or its org-unique name — either resolves.\nAn agent in another org reads as not-found.",
		Fields: map[string]string{
			"agentRef.ref": "Ref is the agent's public id or its org-unique name, from the path.",
		},
		Example: json.RawMessage(`{"ref":"triage"}`),
	})
	zip.Describe("GET /v1/agents/:ref/runs", zip.Doc{
		Description: "listAgentRuns returns one agent's recorded run history, newest first.\nEvery row is an execution that really happened — a run whose model failed is\nrecorded with status error and its upstream message, never dropped.",
		Fields: map[string]string{
			"runList.runs":    "Runs is the agent's recorded runs, newest first. Every row is a run that\nactually executed — a failed run is recorded as one, never hidden.",
			"runsQuery.limit": "Limit caps the runs returned; 0 means 50.",
			"runsQuery.ref":   "Ref is the agent whose runs to read: its public id or its org-unique\nname, from the path.",
		},
		Example: json.RawMessage(`{"ref":"triage","limit":20}`),
	})
	zip.Describe("GET /v1/agents/activity", zip.Doc{
		Description: "agentActivity serves the org-wide recent-activity feed, newest first.\nEvery event is real — a recorded run becomes an invoked or failed event, and\nan agent's own timestamps become created and updated events.\nAn org with no agents and no runs gets an empty feed.",
		Fields: map[string]string{
			"activityFeed.activity": "Activity is the merged event feed, newest first and capped at 50.",
			"activityView.agent":    "agent name",
			"activityView.at":       "RFC3339 UTC",
			"activityView.kind":     "invoked|failed|created|updated (from real events)",
		},
	})
	zip.Describe("GET /v1/agents/metrics", zip.Doc{
		Description: "agentMetrics serves the org's invocations-over-time histogram.\nThere is one series line per agent that ran in the window, and every point is\na real count of recorded runs in that bucket — an org with no activity gets\nan empty series, never a synthesized trend.\nThe resource rollup is all-null because this store meters no CPU, memory,\nstorage or cost, so the console renders a dash rather than a fabricated\nfigure.",
		Fields: map[string]string{
			"metricsQuery.range": "Range is the window to bucket over: 24H, 7D or 30D (the default).",
			"metricsView.range":  "echoes the requested window (24H|7D|30D)",
			"metricsView.series": "per-agent invocation histogram (real)",
			"seriesLine.key":     "agent name",
			"seriesPoint.t":      "bucket start, RFC3339 UTC",
			"seriesPoint.v":      "real invocation count in the bucket",
		},
		Example: json.RawMessage(`{"range":"7D"}`),
	})
	zip.Describe("GET /v1/agents/sessions", zip.Doc{
		Description: "listSessions returns the caller org's agent sessions, newest first.\nIt narrows by flow (root), by direct parent, by status, by the product a\nsession built, or by any combination of those.\nEach row carries its event and child counts and its most recent event, so a\nboard can render a flow without a second read per session.",
		Fields: map[string]string{
			"sessionList.sessions":  "Sessions is the matching sessions, each with its event and child counts\nand its most recent event.",
			"sessionQuery.limit":    "Limit caps the rows returned.",
			"sessionQuery.parent":   "Parent narrows to the direct children of one session.",
			"sessionQuery.project":  "Project narrows to the sessions that built one product.",
			"sessionQuery.root":     "Root narrows to one flow: every session sharing this root id.",
			"sessionQuery.status":   "Status narrows to running, paused, done or error.",
			"sessionView.host":      "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.lastEvent": "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":       "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.project":   "The readable build: the product this session built and whether its story\nis public (provenance.go).",
		},
		Example: json.RawMessage(`{"status":"running","limit":50}`),
	})
	zip.Describe("GET /v1/agents/sessions/:id", zip.Doc{
		Description: "getSession returns one session with its direct children and recent events.\nA session in another org reads as not-found, so a probe learns nothing about\nwhat exists elsewhere.",
		Fields: map[string]string{
			"sessionRef.id":         "ID is the session to act on, from the path.",
			"sessionView.host":      "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.lastEvent": "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":       "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.project":   "The readable build: the product this session built and whether its story\nis public (provenance.go).",
		},
		Example: json.RawMessage(`{"id":"sess_1"}`),
	})
	zip.Describe("GET /v1/agents/sessions/:id/tree", zip.Doc{
		Description: "sessionTree returns the subagent tree rooted at one session.\nEvery node carries its own event count and fan-out, so a flow renders from one\nread; the whole tree is pulled by a single indexed query on the shared root id\nand assembled in memory.",
		Fields: map[string]string{
			"sessionRef.id":         "ID is the session to act on, from the path.",
			"sessionView.host":      "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.lastEvent": "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":       "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.project":   "The readable build: the product this session built and whether its story\nis public (provenance.go).",
		},
		Example: json.RawMessage(`{"id":"sess_1"}`),
	})
	zip.Describe("GET /v1/agents/targets", zip.Doc{
		Description: "listTargets returns every machine registered to the caller's org.\nRows come newest first, each carrying the machine's spec, its last\nutilisation sample and the live session load dispatched to it.",
		Fields: map[string]string{
			"GPU.memory":         "VRAM bytes, 0 = unknown",
			"GPU.model":          "\"GB10\", \"8060S\", \"RTX 4090\"",
			"GPU.vendor":         "nvidia | amd | apple | intel | ...",
			"Metrics.at":         "unix seconds, server-stamped",
			"Metrics.gpuUtil":    "0..1 aggregate utilization",
			"Metrics.memFree":    "bytes",
			"Metrics.memUsed":    "bytes",
			"Spec.arch":          "amd64 | arm64 | ...",
			"Spec.cpus":          "logical cores",
			"Spec.memory":        "total RAM, bytes",
			"Spec.os":            "linux | darwin | windows",
			"targetList.targets": "Targets is every target registered to the caller's org.",
		},
	})
	zip.Describe("GET /v1/agents/targets/:id", zip.Doc{
		Description: "getTarget returns one registered machine with its live session load.\nAn id from another org reads as not-found, so a probe learns nothing about\nwhat exists elsewhere.",
		Fields: map[string]string{
			"GPU.memory":      "VRAM bytes, 0 = unknown",
			"GPU.model":       "\"GB10\", \"8060S\", \"RTX 4090\"",
			"GPU.vendor":      "nvidia | amd | apple | intel | ...",
			"Metrics.at":      "unix seconds, server-stamped",
			"Metrics.gpuUtil": "0..1 aggregate utilization",
			"Metrics.memFree": "bytes",
			"Metrics.memUsed": "bytes",
			"Spec.arch":       "amd64 | arm64 | ...",
			"Spec.cpus":       "logical cores",
			"Spec.memory":     "total RAM, bytes",
			"Spec.os":         "linux | darwin | windows",
			"targetRef.id":    "ID is the target to act on, from the path.",
		},
		Example: json.RawMessage(`{"id":"tgt_1"}`),
	})
	zip.Describe("PATCH /v1/agents/:ref", zip.Doc{
		Description: "updateAgent changes one agent in place, leaving every omitted field alone.\nThe resulting mode and schedule are re-validated together, so a partial\nupdate can never leave a long-running agent without a cron the scheduler can\nread, and a move INTO long-running is held to the same per-org cap as a\ncreate.",
		Fields: map[string]string{
			"updateIn.ref":               "Ref is the agent to update: its public id or its org-unique name, from\nthe path.",
			"updateReq.computeRef":       "ComputeRef re-points the compute target runs are dispatched to.",
			"updateReq.description":      "Description replaces the human note.",
			"updateReq.executionMode":    "ExecutionMode moves the agent between on-demand and long-running.",
			"updateReq.instructions":     "Instructions replaces the agent's system prompt.",
			"updateReq.model":            "Model re-points the agent at another model; it is validated against the\ngateway's served catalog and may not be set empty.",
			"updateReq.schedule":         "Schedule replaces the cron cadence a long-running agent runs on.",
			"updateReq.serviceAccountId": "ServiceAccountID re-points the identity the agent acts as.",
			"updateReq.tools":            "Tools replaces the whole tool list.",
		},
		Example: json.RawMessage(`{"ref":"triage","schedule":"0 * * * *","executionMode":"long-running"}`),
	})
	zip.Describe("PATCH /v1/agents/sessions/:id", zip.Doc{
		Description: "patchSession updates one session in place, leaving every omitted field alone.\nA finished session stays finished: reopening a done or error run would\nfabricate liveness, so a status change away from a terminal state is refused.\nPublishing requires the session to name the product it built, because the\npublic build route is keyed on (org, project), and a target must be one\nregistered to the caller's own org.",
		Fields: map[string]string{
			"patchSessionIn.id":       "ID is the session to update, from the path.",
			"patchSessionReq.project": "Project tags the product this session built; Published is the author's\ndecision to let anyone read the story (provenance.go). Both are pointers so\n\"absent\" and \"cleared\" are different requests.",
			"patchSessionReq.target":  "Target re-dispatches a session to a run-target (the #48 association). \"\" detaches.",
			"sessionView.host":        "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.lastEvent":   "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":         "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.project":     "The readable build: the product this session built and whether its story\nis public (provenance.go).",
		},
		Example: json.RawMessage(`{"id":"sess_1","status":"done","published":true}`),
	})
	zip.Describe("PATCH /v1/agents/targets/:id", zip.Doc{
		Description: "patchTarget updates one machine in place, leaving every omitted field alone.\nA metrics patch IS a heartbeat — the server stamps its own clock, so a client\ncan neither forge nor backdate staleness.\nOnly the machine's owner, or an org admin, may patch it; anyone else gets the\nsame not-found an unknown id gives.",
		Fields: map[string]string{
			"GPU.memory":              "VRAM bytes, 0 = unknown",
			"GPU.model":               "\"GB10\", \"8060S\", \"RTX 4090\"",
			"GPU.vendor":              "nvidia | amd | apple | intel | ...",
			"Metrics.at":              "unix seconds, server-stamped",
			"Metrics.gpuUtil":         "0..1 aggregate utilization",
			"Metrics.memFree":         "bytes",
			"Metrics.memUsed":         "bytes",
			"Spec.arch":               "amd64 | arm64 | ...",
			"Spec.cpus":               "logical cores",
			"Spec.memory":             "total RAM, bytes",
			"Spec.os":                 "linux | darwin | windows",
			"patchTargetIn.id":        "ID is the target to update, from the path.",
			"patchTargetReq.capacity": "Capacity re-states how much work the machine can take.",
			"patchTargetReq.host":     "Host re-states the machine's hostname.",
			"patchTargetReq.kind":     "Kind re-declares what the machine is: laptop, cloud, gpu, cluster or\nmachine.",
			"patchTargetReq.label":    "Label renames the machine in mission control; it may not be set empty.",
			"patchTargetReq.metrics":  "Metrics is a fresh utilisation sample; sending one IS the heartbeat, and\nthe server stamps its own clock.",
			"patchTargetReq.spec":     "Spec re-states the machine's hardware.",
			"patchTargetReq.status":   "Status moves the machine between online, offline and draining — draining\nis how a machine is retired without killing the work already on it.",
		},
		Example: json.RawMessage(`{"id":"tgt_1","status":"draining"}`),
	})
	zip.Describe("POST /v1/agents", zip.Doc{
		Description: "createAgent registers a new agent in the caller's org and answers 201.\nThe name must be unique in the org, and the model is checked against the\nmodels this gateway actually serves, so an unservable model is refused here\nrather than failing at the first run.\nA long-running agent needs a cron schedule, and the org's long-running\nagents are capped.",
		Fields: map[string]string{
			"createReq.computeRef":       "ComputeRef names the compute target runs are dispatched to.",
			"createReq.description":      "Description is a human note about what the agent is for.",
			"createReq.executionMode":    "ExecutionMode is how the agent runs: on-demand, or long-running on a cron\nschedule.",
			"createReq.instructions":     "Instructions is the agent's system prompt, prepended to every run input.",
			"createReq.model":            "Model is the model the agent runs on. It is validated against the\ngateway's served catalog; omitted, it falls back to the deployment\ndefault.",
			"createReq.name":             "Name is the agent's org-unique handle; it must match\n^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. Required.",
			"createReq.schedule":         "Schedule is the cron cadence a long-running agent runs on. Required when\nexecutionMode is long-running.",
			"createReq.serviceAccountId": "ServiceAccountID names the identity the agent acts as.",
			"createReq.tools":            "Tools names the tools the agent may call.",
		},
		Example: json.RawMessage(`{"name":"triage","model":"zen","instructions":"Triage inbound support mail.","executionMode":"on-demand"}`),
	})
	zip.Describe("POST /v1/agents/sessions", zip.Doc{
		Description: "registerSession opens a session — one agent run, recorded as it happens.\nA session with a parentSessionId joins that parent's flow and inherits its\nroot, so a subagent tree is one addressable unit; the parent must be in the\ncaller's OWN org, so a tree can never cross a tenant boundary.\nStatus defaults to running, actor defaults to the validated caller, and a\nsession that names a target must name one registered to the same org.",
		Fields: map[string]string{
			"registerReq.host":      "Execution context — where this session runs (all optional).",
			"registerReq.project":   "The readable build (provenance.go): which product this session builds, and\nwhether its story may be read by the world.",
			"registerReq.provider":  "Account tag — the linked AI account this session ran under (login manager).",
			"sessionView.host":      "Execution context (mission-control): the machine/repo/cwd a card shows and\nthe run-target a session is dispatched to. Omitted when a surface didn't report it.",
			"sessionView.lastEvent": "LastEvent is the compact latest-activity line for the list projection (nil in\nregister/patch/tree responses; set by list + detail). It lets a swipe card show\na live one-line preview without fetching full detail.",
			"sessionView.org":       "Org is the caller's OWN tenant, echoed so a client can build the public\nbuild URL (/builds/:org/:project) without a second call or a guess. It is\nnever another tenant's — every read is org-scoped before it gets here.",
			"sessionView.project":   "The readable build: the product this session built and whether its story\nis public (provenance.go).",
		},
		Example: json.RawMessage(`{"agent":"triage","title":"Fix the flaky import test","repo":"hanzoai/cloud"}`),
	})
	zip.Describe("POST /v1/agents/targets", zip.Doc{
		Description: "registerTarget registers a machine as an agent target, or re-links one.\nRe-linking is idempotent and keyed on org+host+owner, so a machine that\nreconnects refreshes its own row rather than piling up duplicates; it answers\n200, while a first registration answers 201.\nThe registering principal owns the machine — only it, or an org admin, may\nlater patch, claim or remove it.",
		Fields: map[string]string{
			"GPU.memory":         "VRAM bytes, 0 = unknown",
			"GPU.model":          "\"GB10\", \"8060S\", \"RTX 4090\"",
			"GPU.vendor":         "nvidia | amd | apple | intel | ...",
			"Metrics.at":         "unix seconds, server-stamped",
			"Metrics.gpuUtil":    "0..1 aggregate utilization",
			"Metrics.memFree":    "bytes",
			"Metrics.memUsed":    "bytes",
			"Spec.arch":          "amd64 | arm64 | ...",
			"Spec.cpus":          "logical cores",
			"Spec.memory":        "total RAM, bytes",
			"Spec.os":            "linux | darwin | windows",
			"targetReq.capacity": "Capacity is a free-text note about how much work the machine can take.",
			"targetReq.host":     "Host is the machine's stable hostname. It is the re-link key: a machine\nthat reconnects under the same host refreshes its own row.",
			"targetReq.kind":     "Kind is what the machine is: laptop, cloud, gpu, cluster or machine\n(the default).",
			"targetReq.label":    "Label is the machine's display name in mission control. Required.",
			"targetReq.metrics":  "Metrics is a live utilisation sample. Sending one registers a heartbeat;\nthe server stamps its own clock, so staleness cannot be forged.",
			"targetReq.spec":     "Spec is the machine's hardware — cpu, memory, gpus.",
			"targetReq.status":   "Status is the dispatch state: online (the default), offline or draining.",
		},
		Example: json.RawMessage(`{"label":"workshop","kind":"gpu","host":"gpu-01"}`),
	})
}
