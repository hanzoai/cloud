// Code generated by zipdoc; DO NOT EDIT.

package content

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/content/board", zip.Doc{
		Description: "Aggregates the caller org's marketing content across every publishable\ncontent type into ONE queue board — the cross-type read the framework's\nper-DocType list cannot give. It never fails on a partial outage: a content type\nthe org has not installed, or one whose search errors, is skipped and logged\nrather than failing the whole board.",
		Fields: map[string]string{
			"boardItem.doctype":   "DocType is which content type the row came from: Campaign, SocialPost or\nAsset. The board spans all three at once, so this is what tells them apart.",
			"boardItem.name":      "Name is the document within that type. (doctype, name) is the pair every\n/v1/content write addresses an item by.",
			"boardItem.project":   "Project is the brand/site sub-scope within the org. Absent for an item held\nat org level rather than under one brand.",
			"boardItem.status":    "Status is the lifecycle state: draft, in_review, approved, queued, published\nor archived. It decides what a reader may see — the public site pulls\nexactly \"published\" and nothing else — so it is a visibility fact, not a\nworkflow label.",
			"boardItem.title":     "Title is the item's headline, read from its type's own title field. Empty\nfor a document that has none.",
			"boardItem.updatedAt": "UpdatedAt is unix seconds of the document's last write, and the key the\nboard sorts on, newest first.",
			"boardPage.count":     "Count is the number of rows in THIS page — never the org's total.",
			"boardPage.data":      "Data is the matching items, most recently updated first.",
			"boardQuery.doctype":  "DocType keeps only one content type; omitted, the board spans every\npublishable type. An unknown type is refused.",
			"boardQuery.limit":    "Limit caps the rows returned, clamped to 1000. Defaults to 200, which is also\nwhat a non-positive or unparseable value takes.",
			"boardQuery.project":  "Project keeps only items in one brand/site sub-scope.",
			"boardQuery.status":   "Status keeps only items in one lifecycle state (draft, in_review, approved,\nqueued, published, archived). An undefined state is refused.",
		},
		Example: json.RawMessage(`{"status":"queued","limit":50}`),
	})
	zip.Describe("GET /v1/content/channels", zip.Doc{
		Description: "Lists the distribution channels the caller's org has connected — the\nsocial integrations a publish can target. A deployment with no distribution edge\nwired answers 503 rather than an empty list that would read as \"no channels\".",
		Fields: map[string]string{
			"Channel.disabled": "Disabled is true for a channel the org switched off at the social edge. It\nis still listed — this is what the org has CONNECTED, not what it can post\nto — but a publish never targets it, neither by name nor as part of the\n\"every channel\" default.",
			"Channel.id":       "ID is the social integration id a post targets. It is the exact value to put\nin a content item's `channels` list to reach this one connected account.",
			"Channel.name":     "Name is the account label as the org connected it — the handle a human\nrecognises. It is never an address: a publish resolves channels by ID or by\nProvider and never by this.",
			"Channel.provider": "Provider is the network behind the integration: \"x\", \"instagram\", \"tiktok\"\nand the rest of what the org connected. Naming a provider in a publish\ntargets EVERY connected account of it, so it is the coarse handle where ID\nis the precise one.",
			"channelList.data": "Data is every social channel the caller's org has connected, disabled ones\nincluded (Disabled says which).",
		},
	})
	zip.Describe("GET /v1/content/lifecycle", zip.Doc{
		Description: "Returns the ONE marketing-content state machine: the ordered\nlifecycle states, which state a fresh document starts in, which one is publicly\nlive, and the legal successors of every state. The console builds its board\ncolumns and its per-item action buttons from this single answer, so the UI and\nthe write-time enforcement hook can never disagree about what is legal.",
		Fields: map[string]string{
			"stateGraph.initial":     "Initial is the state a fresh document starts in — \"draft\". A stored document\nwith no status at all is read as this too.",
			"stateGraph.live":        "Live is the ONE state that is publicly readable — \"published\". The site\npulls only documents in it, so reaching Live IS site-publish; every other\nstate is invisible to a reader.",
			"stateGraph.states":      "States is every lifecycle state in canonical order: draft, in_review,\napproved, queued, published, archived. The console lays its board columns\nout in exactly this order, so the order is part of the answer.",
			"stateGraph.transitions": "Transitions maps each state to the states it may move to. A target absent\nfrom a state's list is REFUSED, at the endpoint and again at the storage\nboundary — this is the whole rule, not a hint for the UI. A state never\nlists itself; a move that changes nothing is always legal.",
		},
	})
	zip.Describe("POST /v1/content/:doctype/:name/transition", zip.Doc{
		Description: "Moves one content item to a new lifecycle state and, on the move to\npublished, fans it out to the item's channels. The edge must be legal for the\nitem's current state — an illegal move is refused with 409 — and the status write\nre-validates it at the storage boundary. Distribution is best effort: its honest\nstate is reported on the result and a distribution failure never rolls the status\nchange back.",
		Fields: map[string]string{
			"ChannelResult.channel":         "the social integration id targeted",
			"ChannelResult.error":           "short reason, when it failed",
			"ChannelResult.externalId":      "social post id, when it went out",
			"ChannelResult.provider":        "\"x\" | \"instagram\" | ... when known",
			"ChannelResult.status":          "\"distributed\" | \"scheduled\" | \"failed\"",
			"PublishResult.channels":        "Channels is the channel list read off the content document — integration ids\nor provider names, as the item declares them. Empty when the item names\nnone, which targets every connected, enabled channel. It is what was ASKED\nfor; Results is what happened.",
			"PublishResult.externalIds":     "ExternalIDs maps channel id → the post id that channel returned, merged with\neverything earlier publishes recorded. Successes only, and it is the\nidempotency ledger: a channel named here is skipped by every later publish\nof this item, so the map only ever grows.",
			"PublishResult.results":         "Results is the outcome per channel — which went out, which did not and why —\ncovering the whole fan-out including failures, so partial success is never\nflattened into one verdict. A channel the org has not connected appears here\nas failed with \"channel not connected\".",
			"PublishResult.status":          "Status is the ONE headline, drawn from: \"distributed\" (something is on\nrecord and went out now), \"scheduled\" (same, handed to the channel's own\nscheduler for later), \"failed\" (nothing is on record — this fan-out missed\nentirely and no earlier one landed), \"in_progress\" (another publisher holds\nthe item, so this call posted NOTHING and the caller retries), and\n\"not_configured\" (no distribution edge is wired; a transition records it\ninstead of failing). A partial fan-out is \"distributed\"/\"scheduled\", never\n\"failed\" — the per-channel truth is in Results.",
			"StorefrontResult.imageUrl":     "ImageURL is the absolute URL the listing's headerImage now points at. The\nimage is REFERENCED, not copied — it stays in the org's studio output\nbucket, so removing it there empties the storefront tile.",
			"StorefrontResult.slug":         "Slug is the product handle the image was attached to. It IS the asset's\n`design` field — that equality is the whole join between the studio and the\ncatalog, which is why an asset with no design produces no storefront result\nat all.",
			"StorefrontResult.status":       "Status is one of \"published\" (the product image was set), \"not_configured\"\n(no commerce edge, no store provisioned for the org, or a token that is not\nadmin on the store — a fail-closed no-op) or \"failed\" (commerce answered and\nerrored). None of the three fails the transition that produced it.",
			"StorefrontResult.store":        "Store is the commerce store id the image landed in, resolved for the org\nmid-call. Present only on \"published\": a result that never got that far\ncarries none.",
			"TransitionResult.distribution": "Distribution is the channel fan-out this move triggered. Present ONLY on the\nmove to published, the single edge that distributes — so its absence means\nno fan-out was attempted, never that one failed quietly. A fan-out that DID\nfail is present carrying its own honest status, because distribution never\nrolls the status change back.",
			"TransitionResult.doctype":      "DocType is the content type that moved — Campaign, SocialPost or Asset —\nechoed from the path.",
			"TransitionResult.from":         "From is the state the item held when it was read. A document carrying no\nstatus yet reads as \"draft\".",
			"TransitionResult.name":         "Name is the document that moved, echoed from the path.",
			"TransitionResult.storefront":   "Storefront is the catalog side effect, present only when a published Asset\nwas product imagery — it carries a design and a kind of ecom, product or\nlifestyle. Absent for everything else, so absence reads as \"not catalog\nimagery\" rather than \"the catalog failed\".",
			"TransitionResult.to":           "To is the state it holds now. From == To on an idempotent re-transition,\nwhich is legal and is where a caller that lost a publish race lands.",
			"transitionIn.doctype":          "DocType is the content type to act on, from the path.",
			"transitionIn.name":             "Name is the document to act on, from the path.",
			"transitionIn.scheduleAt":       "ScheduleAt is an ISO-8601 go-live time handed to the channel's own scheduler;\n\"\" distributes now.",
			"transitionIn.to":               "To is the lifecycle state to move to. Required, and the move must be a legal\nedge from the item's current state.",
		},
		Example: json.RawMessage(`{"doctype":"marketing.SocialPost","name":"spring-teaser","to":"published"}`),
	})
	zip.Describe("POST /v1/content/generate", zip.Doc{
		Description: "Draft a piece of marketing content and file it in the CMS as a draft.\n\nAnswers 201 with the created draft's identity — {doctype, name, status} — and the\ndocument itself lands in the CMS through the SAME validate and lifecycle-hook\npipeline an ordinary create runs. This is a WRITE, not a preview: there is no\ndry-run, and every call that succeeds leaves a document behind.\n\n`doctype` picks which of two generation planes runs, and they are the only two.\nCampaign and SocialPost are drafted as brand COPY on the platform AI plane (zen5 by\ndefault, overridable per request with `model` or per deployment); Asset is a studio\nimage render the AI plane never sees. Everything else about the call is identical.\n\nMONEY, metered in exactly one place per mode and never both. Copy rides the\nplatform's own inference meter — the org's balance is authorised before the model\ncall and debited at the exact token cost after — so content never re-bills it. A\nstudio render is invisible to that meter, so content is the sole meter for it: the\norg is gated BEFORE the GPU compute and refused 402 when out of funds or over its\nspend cap, and the debit is recorded only once the render actually returns, because\nthe billable event is the consumed compute and not the CMS row. `project` rides the\nBODY rather than a server-minted identity claim, so it attributes spend but a\nproject-scoped cap stays soft on it — the org is the value that is enforced.\n\nThe org is the caller's own, resolved once from the validated principal and never\nread from the body; a caller without one is refused 403. Status is not the\ngenerator's to choose: a generated item is ALWAYS a draft, and the storage-boundary\nhook enforces that a second time.\n\nIt fails closed rather than inventing anything. An unknown content type is 404 and a\ndeployment whose marketing module is not installed is 409 naming the install call.\nAn AI plane or studio that is unconfigured or unreachable, a graph the studio\nrejects, and a render that does not return in time all degrade to 503 — never\nfabricated copy, never a fake render. A `source_media` that fails the SSRF and\ntraversal validator is 400 raised before the billing gate and before the studio is\ncontacted, so a hostile source never costs the caller anything.",
		Fields: map[string]string{
			"GenerateInput.brief":        "the brief/goal driving copy generation",
			"GenerateInput.channels":     "target channels (SocialPost)",
			"GenerateInput.design":       "studio design slug (asset source)",
			"GenerateInput.doctype":      "marketing.Campaign | marketing.SocialPost | marketing.Asset",
			"GenerateInput.kind":         "asset kind: ecom|product|lifestyle|hover|hero",
			"GenerateInput.model":        "optional zen model override (copy)",
			"GenerateInput.product":      "commerce product handle (copy context)",
			"GenerateInput.project":      "brand/site sub-scope (billing + tenancy axis)",
			"GenerateInput.source_media": "asset source image (design CAD/photo)",
			"GenerateInput.title":        "optional explicit title",
			"GenerateInput.tone":         "tone override for a single draft",
			"GenerateInput.voice":        "brand-voice guidance for the copy director",
			"GenerateResult.doctype":     "the marketing type the draft was filed as",
			"GenerateResult.name":        "the new document's name — its address for every later call",
			"GenerateResult.status":      "always \"draft\"; the lifecycle owns the initial state",
		},
	})
	zip.Describe("POST /v1/content/publish", zip.Doc{
		Description: "Publish distributes one CMS content item to the channels recorded on it and\nreturns the honest per-channel outcome. The item names itself — its caption,\nmedia and channel list are read from the stored document, not from this request.\nIt is idempotent per channel (a channel already posted for this item is skipped),\nand a publish that loses the per-item lease to a live publisher answers status\n\"in_progress\" having posted nothing.",
		Fields: map[string]string{
			"ChannelResult.channel":     "the social integration id targeted",
			"ChannelResult.error":       "short reason, when it failed",
			"ChannelResult.externalId":  "social post id, when it went out",
			"ChannelResult.provider":    "\"x\" | \"instagram\" | ... when known",
			"ChannelResult.status":      "\"distributed\" | \"scheduled\" | \"failed\"",
			"PublishInput.doctype":      "DocType is the content type holding the item: Campaign, SocialPost or Asset.\nAny other name is refused as an unknown content type.",
			"PublishInput.name":         "Name is the document within that type — the item to distribute. Its caption,\nmedia and channel list come off the stored document, so this names WHICH\nitem and says nothing about what goes out.",
			"PublishInput.scheduleAt":   "ScheduleAt hands a future go-live to the channel's own scheduler, as an\nISO-8601 time. Empty posts now.",
			"PublishResult.channels":    "Channels is the channel list read off the content document — integration ids\nor provider names, as the item declares them. Empty when the item names\nnone, which targets every connected, enabled channel. It is what was ASKED\nfor; Results is what happened.",
			"PublishResult.externalIds": "ExternalIDs maps channel id → the post id that channel returned, merged with\neverything earlier publishes recorded. Successes only, and it is the\nidempotency ledger: a channel named here is skipped by every later publish\nof this item, so the map only ever grows.",
			"PublishResult.results":     "Results is the outcome per channel — which went out, which did not and why —\ncovering the whole fan-out including failures, so partial success is never\nflattened into one verdict. A channel the org has not connected appears here\nas failed with \"channel not connected\".",
			"PublishResult.status":      "Status is the ONE headline, drawn from: \"distributed\" (something is on\nrecord and went out now), \"scheduled\" (same, handed to the channel's own\nscheduler for later), \"failed\" (nothing is on record — this fan-out missed\nentirely and no earlier one landed), \"in_progress\" (another publisher holds\nthe item, so this call posted NOTHING and the caller retries), and\n\"not_configured\" (no distribution edge is wired; a transition records it\ninstead of failing). A partial fan-out is \"distributed\"/\"scheduled\", never\n\"failed\" — the per-channel truth is in Results.",
		},
		Example: json.RawMessage(`{"doctype":"marketing.SocialPost","name":"spring-teaser"}`),
	})
}
