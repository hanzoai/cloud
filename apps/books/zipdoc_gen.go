// Code generated by zipdoc; DO NOT EDIT.

package books

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/books/accounts", zip.Doc{
		Description: "Returns the org's chart of accounts — the seeded fixed chart every\nposting key in the ledger refers to.",
		Fields: map[string]string{
			"Account.name":     "Name is the account's human name, for a statement's line label.",
			"Account.number":   "Number is the posting key every voucher leg, rule and report references —\nstable, and the reason the chart is a fixed value rather than a table anybody\ncan edit. It looks numeric and is a string: \"1000\" sorts and compares as text.",
			"Account.party":    "Party marks the account as carrying a SUBLEDGER — receivable is money owed to\nus, payable money we owe — so a leg posted here also writes a payment-ledger\nrow against a counterparty. Absent means no subledger: a bank, wallet, revenue\nor cost account tracks no counterparty at all.",
			"Account.type":     "Type is the account's fundamental class, which is also its NORMAL balance\nside: asset and expense are debit-normal, liability, income and equity\ncredit-normal.",
			"ledgerIn.sandbox": "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\"; anything else\nreads the live one.",
		},
		Example: json.RawMessage(`{"sandbox":"false"}`),
	})
	zip.Describe("GET /v1/books/bank/transactions", zip.Doc{
		Description: "Returns the org's normalized bank transactions, newest first —\nevery row the import and connector paths have ingested, with its amount in exact cents,\nits direction, and whether it has been matched to a voucher yet.",
		Fields: map[string]string{
			"BankTxnRow.amountCents":    "AmountCents is the size of the movement in whole cents, always POSITIVE —\ndirection carries the sign, so a caller must read both to know which way money\nwent.",
			"BankTxnRow.connector":      "Connector names the feed this row arrived on — which bank or processor\nconnection it was synced from. With externalId it is the row's identity, so\nre-syncing the same statement never books a second copy.",
			"BankTxnRow.currency":       "Currency is the ISO code the bank reported the line in.",
			"BankTxnRow.description":    "Description is the statement memo as the bank wrote it.",
			"BankTxnRow.direction":      "Direction is which way the money moved: an inflow into the account or an\noutflow from it, from the org's point of view.",
			"BankTxnRow.externalId":     "ExternalID is the bank's OWN id for the line, carried verbatim. It is unique\nonly within its connector.",
			"BankTxnRow.matchedVoucher": "MatchedVoucher names the ledger voucher this line was reconciled against —\nthe bill it paid, or the settlement it cleared. Absent when nothing matched,\nwhich for an inflow is what raises a question.",
			"BankTxnRow.merchant":       "Merchant is the counterparty the feed identified, where it did.",
			"BankTxnRow.postedAt":       "PostedAt is the bank's posting date for the line, not when we synced it.",
			"BankTxnRow.status":         "Status is where the line got to: posted (an outflow booked straight to an\nexpense), settled (an outflow that paid down a scanned bill), reconciled (an\ninflow that cleared a pending settlement), transfer (a move between the org's\nown accounts, recorded but with no effect on the books), or unmatched (an\ninflow nobody could place, which is waiting on a human answer).",
			"bankLimitIn.limit":         "Limit caps how many rows come back; 500 when absent or not positive.",
			"bankLimitIn.sandbox":       "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\".",
		},
		Example: json.RawMessage(`{"limit":100}`),
	})
	zip.Describe("GET /v1/books/bank/unreconciled", zip.Doc{
		Description: "Returns the org's unmatched bank inflows and their open clarifying\nquestions — the queue a human answers so an unexplained deposit is never guessed into\nrevenue.",
		Fields: map[string]string{
			"BankQuestion.connector":       "Connector names the feed the unplaceable line arrived on. With externalId it\nidentifies both the question and the bank line it is about, so re-syncing the\nsame deposit never asks twice.",
			"BankQuestion.createdAt":       "CreatedAt is when the question was raised.",
			"BankQuestion.externalId":      "ExternalID is the bank's own id for the line in question.",
			"BankQuestion.prompt":          "Prompt is the question put to the founder in plain language — what this money\nwas, since the books cannot place it on their own.",
			"BankQuestion.status":          "Status is whether the question is still open or has been answered.",
			"BankTxnRow.amountCents":       "AmountCents is the size of the movement in whole cents, always POSITIVE —\ndirection carries the sign, so a caller must read both to know which way money\nwent.",
			"BankTxnRow.connector":         "Connector names the feed this row arrived on — which bank or processor\nconnection it was synced from. With externalId it is the row's identity, so\nre-syncing the same statement never books a second copy.",
			"BankTxnRow.currency":          "Currency is the ISO code the bank reported the line in.",
			"BankTxnRow.description":       "Description is the statement memo as the bank wrote it.",
			"BankTxnRow.direction":         "Direction is which way the money moved: an inflow into the account or an\noutflow from it, from the org's point of view.",
			"BankTxnRow.externalId":        "ExternalID is the bank's OWN id for the line, carried verbatim. It is unique\nonly within its connector.",
			"BankTxnRow.matchedVoucher":    "MatchedVoucher names the ledger voucher this line was reconciled against —\nthe bill it paid, or the settlement it cleared. Absent when nothing matched,\nwhich for an inflow is what raises a question.",
			"BankTxnRow.merchant":          "Merchant is the counterparty the feed identified, where it did.",
			"BankTxnRow.postedAt":          "PostedAt is the bank's posting date for the line, not when we synced it.",
			"BankTxnRow.status":            "Status is where the line got to: posted (an outflow booked straight to an\nexpense), settled (an outflow that paid down a scanned bill), reconciled (an\ninflow that cleared a pending settlement), transfer (a move between the org's\nown accounts, recorded but with no effect on the books), or unmatched (an\ninflow nobody could place, which is waiting on a human answer).",
			"ledgerIn.sandbox":             "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\"; anything else\nreads the live one.",
			"unreconciledOut.questions":    "Questions is the open clarifying question per unmatched inflow.",
			"unreconciledOut.transactions": "Transactions is every bank row still unmatched against the ledger.",
		},
		Example: json.RawMessage(`{"sandbox":"false"}`),
	})
	zip.Describe("GET /v1/books/export", zip.Doc{
		Description: "Returns the complete financial package for the caller's org over\n(from, to]: the trial balance, the P&L, the balance sheet, and the GL detail behind\nthem — the four statements a tax preparer or an investor asks for, assembled from the\none ledger in a single read so they cannot disagree with each other.",
		Fields: map[string]string{
			"BalanceLine.account":           "Account is the chart-of-accounts number this line reports on. ABSENT marks a\nDERIVED line that no account holds — retained earnings is the one such line,\ncomputed from cumulative income minus expense.",
			"BalanceLine.amount":            "Amount is the balance as of the statement date, in whole cents, in its NATURAL\nsign: positive when the account behaved normally, on all three sides. Assets\nare debit-normal and shown as stored; liabilities and equity are credit-normal\nand flipped once here for display. A negative asset is a real overdraft, not a\nsign convention.",
			"BalanceLine.name":              "Name is the account's human name, or the derived line's own name.",
			"BalanceLine.type":              "Type is the account's fundamental class. Absent on a derived line, which\nbelongs to no account and therefore has none.",
			"BalanceSheet.asOf":             "AsOf is the posting time the statement is taken at, inclusive. A balance sheet\nis a snapshot, not a window, so there is no From. Absent means as of now.",
			"BalanceSheet.assets":           "Assets are what the org OWNS at that instant, one line per account that has a\nbalance. Cash, receivables, funds captured but not yet settled.",
			"BalanceSheet.balanced":         "Balanced is whether assets equal liabilities plus equity — the accounting\nequation, computed from the totals above rather than assumed. False means the\nledger is broken, not that the statement is.",
			"BalanceSheet.equity":           "Equity is what is left over for the owners. It carries a DERIVED retained\nearnings line holding cumulative income minus expense, because this ledger has\nno period close that sweeps the P&L into equity — without that line the\nequation would not close.",
			"BalanceSheet.liabilities":      "Liabilities are what the org OWES — including customers' unspent prepaid\ncredit, which is their money until it is consumed and so is carried here\nrather than counted as revenue.",
			"BalanceSheet.totalAssets":      "TotalAssets is the sum of the asset lines, in cents.",
			"BalanceSheet.totalEquity":      "TotalEquity is the sum of the equity lines including retained earnings, in cents.",
			"BalanceSheet.totalLiabilities": "TotalLiabilities is the sum of the liability lines, in cents.",
			"FinancialPackage.balanceSheet": "BalanceSheet is struck as of the period END, not the start.",
			"FinancialPackage.from":         "From opens the reporting period. Absent means from the beginning of the ledger.",
			"FinancialPackage.generatedAt":  "GeneratedAt is when the bundle was assembled — the moment the statements were\nstruck, which is what makes two exports of the same period comparable.",
			"FinancialPackage.gl":           "GL is the newest slice of ledger detail, as the audit trail behind the\nstatements. It is CAPPED, so on a busy ledger it is a sample rather than the\nfull support for the figures above.",
			"FinancialPackage.org":          "Org is the organisation whose books these are — the validated caller's own,\nstamped so a downloaded bundle still says whose it is.",
			"FinancialPackage.pnl":          "PnL is the income statement for the period, on an accrual basis.",
			"FinancialPackage.to":           "To closes it. Absent means up to now.",
			"FinancialPackage.trialBalance": "TrialBalance is the proof the ledger balances over the period.",
			"GLRow.account":                 "Account is the chart-of-accounts number this leg posts to.",
			"GLRow.against":                 "Against names the OTHER accounts in the same voucher — the contra side of this\nleg — so a single row reads as an entry rather than as half of one.",
			"GLRow.credit":                  "Credit is the amount credited to that account, in whole cents.",
			"GLRow.debit":                   "Debit is the amount debited to that account, in whole cents. Exactly one of\ndebit and credit is non-zero on a leg; a negative amount is never used to mean\nthe other side.",
			"GLRow.id":                      "ID is the entry's position in the ledger. The ledger is append-only, so ids\nascend with posting order and a higher id is a later entry.",
			"GLRow.postingAt":               "PostingAt is the accounting date this entry belongs to — what the reports\nwindow on, which need not be when the row was written.",
			"GLRow.remarks":                 "Remarks is the memo carried onto the entry, for a human reading the ledger.",
			"GLRow.sourceId":                "SourceID identifies that originating record within its kind.",
			"GLRow.sourceKind":              "SourceKind is what caused the posting: a bank line, a scanned document, a\ncommerce sale. With sourceId it traces the entry back to the thing that\nproduced it.",
			"PnL.expense":                   "Expense is the cost lines that moved in the period, one per account.",
			"PnL.from":                      "From opens the period and is EXCLUSIVE — movement strictly after it, matching\nthe trial balance's opening boundary so the two reports agree on what belongs\nto a period. Absent means from the beginning of the ledger.",
			"PnL.income":                    "Income is the revenue lines that moved in the period, one per account.\nAccounts that did not move are omitted rather than listed at zero.",
			"PnL.netIncome":                 "NetIncome is totalIncome minus totalExpense, in cents. Negative is a loss.",
			"PnL.to":                        "To closes the period and is inclusive. Absent means up to now.",
			"PnL.totalExpense":              "TotalExpense is cost MATCHED to that revenue, in cents, including accrued\ninfrastructure that has not been billed yet.",
			"PnL.totalIncome":               "TotalIncome is revenue RECOGNIZED in the period, in cents — accrual, not cash,\nso a prepaid top-up is not in it until the credit is consumed.",
			"PnLLine.account":               "Account is the chart-of-accounts number this line reports on.",
			"PnLLine.amount":                "Amount is the account's movement over the period in whole cents, in its\nNATURAL sign: positive when the account behaved normally, for income and\nexpense alike. Income is credit-normal so its stored net is flipped once here\nfor display; the ledger underneath is never sign-flipped. A negative amount\ntherefore means the account ran backwards — a refunded sale, a reversed cost.",
			"PnLLine.name":                  "Name is that account's human name from the fixed chart.",
			"PnLLine.type":                  "Type is the account's fundamental class, which on this statement is always\nincome or expense — it tells a reader which half of the statement the line\ncame from without re-deriving it from the array it arrived in.",
			"TrialBalance.balanced":         "Balanced is the proof this report exists to give: whether total debits equal\ntotal credits. It is computed from the rows above, never assumed, and false\nmeans the ledger itself is broken rather than that the report is wrong.",
			"TrialBalance.from":             "From is the posting time the window opens at, as it was asked for. Absent\nmeans the report runs from the beginning of the ledger.",
			"TrialBalance.rows":             "Rows are the accounts that MOVED in one of the windows. An account that never\nmoved is omitted rather than listed at zero, so this is shorter than the chart.",
			"TrialBalance.to":               "To is the posting time the window closes at, inclusive. Absent means \"up to\nnow\" — every posting the ledger holds.",
			"TrialBalance.totalCredit":      "TotalCredit is the sum of every row's closing credit column, in cents.",
			"TrialBalance.totalDebit":       "TotalDebit is the sum of every row's CLOSING debit column, in cents.",
			"TrialBalanceRow.account":       "Account is the chart-of-accounts NUMBER this line reports on (\"1000\", \"4000\")\n— the stable posting key, not a display label.",
			"TrialBalanceRow.closingCredit": "ClosingCredit is that closing balance in cents when it is a credit balance.",
			"TrialBalanceRow.closingDebit":  "ClosingDebit is the balance at the end of the window, in cents, when it is a\ndebit balance. This is the column the report's totals are summed from.",
			"TrialBalanceRow.credit":        "Credit is the same window movement in cents when it was net credit.",
			"TrialBalanceRow.debit":         "Debit is the account's MOVEMENT within the window — closing minus opening, not\nthe closing balance — in cents, when that movement was net debit. Zero when the\naccount moved net credit.",
			"TrialBalanceRow.name":          "Name is that account's human name from the fixed chart.",
			"TrialBalanceRow.openingCredit": "OpeningCredit is the same opening balance in cents when it fell on the credit\nside. Zero when the balance was a debit one.",
			"TrialBalanceRow.openingDebit":  "OpeningDebit is the account's balance before the window began, in whole cents,\nwhen that balance was on the debit side. Zero when the balance was a credit\none — the pair is exclusive, never two halves of one number.",
			"TrialBalanceRow.type":          "Type is the account's fundamental class — asset, liability, income, expense or\nequity — which is also its normal balance side. It is carried for presentation\nand does NOT decide which column an amount lands in: placement follows the\nsign of the real net, so a contra balance shows up as one.",
			"exportIn.format":               "Format is the export encoding. Only \"json\" is supported; empty means json.",
			"exportIn.from":                 "From is the RFC3339 start of the window, exclusive. Empty means all time.",
			"exportIn.limit":                "Limit caps the GL detail rows included as the audit trail; 5000 when absent\nor not positive.",
			"exportIn.sandbox":              "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\".",
			"exportIn.to":                   "To is the RFC3339 end of the window, inclusive. Empty means up to now.",
		},
		Example: json.RawMessage(`{"from":"2026-01-01T00:00:00Z","to":"2026-12-31T23:59:59Z","format":"json"}`),
	})
	zip.Describe("GET /v1/books/gl", zip.Doc{
		Description: "ListGL returns the org's most recent GL Entry rows, newest first. This is the raw\ndouble-entry detail behind every statement: one row per leg, with its debit, credit,\nposting time and the source that booked it.",
		Fields: map[string]string{
			"GLRow.account":    "Account is the chart-of-accounts number this leg posts to.",
			"GLRow.against":    "Against names the OTHER accounts in the same voucher — the contra side of this\nleg — so a single row reads as an entry rather than as half of one.",
			"GLRow.credit":     "Credit is the amount credited to that account, in whole cents.",
			"GLRow.debit":      "Debit is the amount debited to that account, in whole cents. Exactly one of\ndebit and credit is non-zero on a leg; a negative amount is never used to mean\nthe other side.",
			"GLRow.id":         "ID is the entry's position in the ledger. The ledger is append-only, so ids\nascend with posting order and a higher id is a later entry.",
			"GLRow.postingAt":  "PostingAt is the accounting date this entry belongs to — what the reports\nwindow on, which need not be when the row was written.",
			"GLRow.remarks":    "Remarks is the memo carried onto the entry, for a human reading the ledger.",
			"GLRow.sourceId":   "SourceID identifies that originating record within its kind.",
			"GLRow.sourceKind": "SourceKind is what caused the posting: a bank line, a scanned document, a\ncommerce sale. With sourceId it traces the entry back to the thing that\nproduced it.",
			"glIn.limit":       "Limit caps how many rows come back; 500 when absent or not positive.",
			"glIn.sandbox":     "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\".",
		},
		Example: json.RawMessage(`{"limit":100}`),
	})
	zip.Describe("GET /v1/books/inbox", zip.Doc{
		Description: "Returns the org's open document queue — everything uploaded but not yet\nbooked, newest first, each with its extracted summary and the confidence the scanner\nresolved its category at. A booked document drops out of the queue.",
		Fields: map[string]string{
			"Extracted.category":   "Category is the expense bucket the SCANNER guessed, as a slug — a hint only.\nVendor rules override it whenever they know better, so this is the model's\nreading and not the account the entry will land on.",
			"Extracted.currency":   "Currency is the ISO code the document is denominated in.",
			"Extracted.issuedAt":   "IssuedAt is the document's OWN date as YYYY-MM-DD — when the bill was issued,\nwhich is not when it was uploaded or when it will post.",
			"Extracted.lineItems":  "LineItems are the individual lines read off the document, where it had any.\nThey need not sum to totalCents: a document may carry lines the scanner could\nnot read, and the total is taken from the total.",
			"Extracted.merchant":   "Merchant is the supplier as printed on the document.",
			"Extracted.note":       "Note is anything else worth carrying from the document that has no field of\nits own.",
			"Extracted.taxCents":   "TaxCents is how much of that total is tax, in cents. It is part of totalCents,\nnot additional to it.",
			"Extracted.totalCents": "TotalCents is the document total in whole cents, tax INCLUDED.",
			"InboxItem.category":   "Category is the expense account the scanner proposed, as a chart number — a\nPROPOSAL, not a posting: nothing is booked until it is accepted.",
			"InboxItem.confidence": "Confidence is how sure the scanner is of that reading, and is the signal for\nwhether a person needs to check it before it is booked.",
			"InboxItem.createdAt":  "CreatedAt is when the document was uploaded.",
			"InboxItem.extracted":  "Extracted is what the scanner read off the document. Absent until it has been\nscanned, so its absence is \"not read yet\", never \"nothing on it\".",
			"InboxItem.filename":   "Filename is the name the document was uploaded under, for a person to\nrecognise it by. It is not part of the item's identity.",
			"InboxItem.id":         "ID is the CONTENT HASH of the uploaded bytes, which is what makes the queue\nidempotent: re-uploading the same document returns this item rather than\nadding a second one. It is also the id the scan of this document carries.",
			"InboxItem.status":     "Status is where the document is in the queue — unsorted until the scanner has\nread it, and thereafter whether it is waiting on a person or has been booked.",
			"InboxItem.vendor":     "Vendor is the supplier the scanner identified, surfaced beside the item so a\nqueue renders without opening each document.",
			"LineItem.amountCents": "AmountCents is that line's amount in whole cents. The scanner is instructed to\nreturn integer cents rather than a decimal, so no float rounding can enter the\nledger through here.",
			"LineItem.description": "Description is the line as it appears on the document.",
			"inboxOut.items":       "Items is every document still unsorted or in draft, newest first.",
			"ledgerIn.sandbox":     "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\"; anything else\nreads the live one.",
		},
		Example: json.RawMessage(`{"sandbox":"false"}`),
	})
	zip.Describe("GET /v1/books/metrics", zip.Doc{
		Description: "Metrics returns the org's deterministic SaaS-metrics snapshot over an optional\n(from, to] window — MRR, ARR, revenue, COGS, burn, gross margin, net income, cash,\ndeferred revenue, monthly burn and runway — as raw int64-cent figures AND the same\nfigures already formatted. Every number is the ledger, aggregated the one way the books\ndefine it, never a guess; it is the grounded read the unified /v1/ask advisor replays.",
		Fields: map[string]string{
			"Figure.label":                    "Label names the metric, e.g. \"MRR\" or \"Runway\".",
			"Figure.period":                   "Period is the window the figure covers, e.g. \"2026-07\" or \"all-time\".",
			"Figure.value":                    "Value is the figure already formatted through books' own money formatter, so a\nconsumer never re-derives it.",
			"MetricsResponse.arr":             "ARR is annualized recurring revenue in cents (MRR × 12).",
			"MetricsResponse.burn":            "Burn is total expense in cents over the period.",
			"MetricsResponse.cash":            "Cash is the bank + processor-clearing balance in cents as of To.",
			"MetricsResponse.cogs":            "COGS is cost of goods sold in cents over the period.",
			"MetricsResponse.deferredRevenue": "DeferredRevenue is the customer-wallet liability in cents as of To.",
			"MetricsResponse.figures":         "Figures is the same snapshot rendered through books' one money formatter.",
			"MetricsResponse.from":            "From is the RFC3339 start of the reporting window, exclusive; absent for all time.",
			"MetricsResponse.grossMarginBps":  "GrossMarginBps is GrossProfit / Revenue in basis points (7000 = 70%).",
			"MetricsResponse.grossProfit":     "GrossProfit is Revenue − COGS, in cents.",
			"MetricsResponse.monthlyBurn":     "MonthlyBurn is net cash burned per month in cents; 0 when not losing cash.",
			"MetricsResponse.months":          "Months is the window length in whole months used to normalize MRR and burn.",
			"MetricsResponse.mrr":             "MRR is monthly recurring revenue in cents.",
			"MetricsResponse.netIncome":       "NetIncome is Revenue − Burn, in cents.",
			"MetricsResponse.period":          "Period is the human window label, e.g. \"2026-07\" or \"all-time\".",
			"MetricsResponse.revenue":         "Revenue is recognized revenue in cents over the period.",
			"MetricsResponse.runwayMonths":    "RunwayMonths is Cash / MonthlyBurn; -1 means infinite (the org is not burning).",
			"MetricsResponse.to":              "To is the RFC3339 end of the reporting window, inclusive; absent for up to now.",
			"periodIn.from":                   "From is the RFC3339 start of the window, exclusive. Empty means all time.",
			"periodIn.sandbox":                "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\".",
			"periodIn.to":                     "To is the RFC3339 end of the window, inclusive. Empty means up to now.",
		},
		Example: json.RawMessage(`{"from":"2026-01-01T00:00:00Z","to":"2026-06-30T23:59:59Z"}`),
	})
	zip.Describe("GET /v1/books/pnl", zip.Doc{
		Description: "Returns the org's accrual-basis Profit & Loss over an optional (from, to]\nwindow of RFC3339 posting times: recognized revenue, matched cost, and the net.",
		Fields: map[string]string{
			"PnL.expense":      "Expense is the cost lines that moved in the period, one per account.",
			"PnL.from":         "From opens the period and is EXCLUSIVE — movement strictly after it, matching\nthe trial balance's opening boundary so the two reports agree on what belongs\nto a period. Absent means from the beginning of the ledger.",
			"PnL.income":       "Income is the revenue lines that moved in the period, one per account.\nAccounts that did not move are omitted rather than listed at zero.",
			"PnL.netIncome":    "NetIncome is totalIncome minus totalExpense, in cents. Negative is a loss.",
			"PnL.to":           "To closes the period and is inclusive. Absent means up to now.",
			"PnL.totalExpense": "TotalExpense is cost MATCHED to that revenue, in cents, including accrued\ninfrastructure that has not been billed yet.",
			"PnL.totalIncome":  "TotalIncome is revenue RECOGNIZED in the period, in cents — accrual, not cash,\nso a prepaid top-up is not in it until the credit is consumed.",
			"PnLLine.account":  "Account is the chart-of-accounts number this line reports on.",
			"PnLLine.amount":   "Amount is the account's movement over the period in whole cents, in its\nNATURAL sign: positive when the account behaved normally, for income and\nexpense alike. Income is credit-normal so its stored net is flipped once here\nfor display; the ledger underneath is never sign-flipped. A negative amount\ntherefore means the account ran backwards — a refunded sale, a reversed cost.",
			"PnLLine.name":     "Name is that account's human name from the fixed chart.",
			"PnLLine.type":     "Type is the account's fundamental class, which on this statement is always\nincome or expense — it tells a reader which half of the statement the line\ncame from without re-deriving it from the array it arrived in.",
			"periodIn.from":    "From is the RFC3339 start of the window, exclusive. Empty means all time.",
			"periodIn.sandbox": "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\".",
			"periodIn.to":      "To is the RFC3339 end of the window, inclusive. Empty means up to now.",
		},
		Example: json.RawMessage(`{"from":"2026-01-01T00:00:00Z","to":"2026-03-31T23:59:59Z"}`),
	})
	zip.Describe("GET /v1/books/position", zip.Doc{
		Description: "Returns the org's Balance Sheet as of `to` (empty = all time), with the\nAssets == Liabilities + Equity equation proof.",
		Fields: map[string]string{
			"BalanceLine.account":           "Account is the chart-of-accounts number this line reports on. ABSENT marks a\nDERIVED line that no account holds — retained earnings is the one such line,\ncomputed from cumulative income minus expense.",
			"BalanceLine.amount":            "Amount is the balance as of the statement date, in whole cents, in its NATURAL\nsign: positive when the account behaved normally, on all three sides. Assets\nare debit-normal and shown as stored; liabilities and equity are credit-normal\nand flipped once here for display. A negative asset is a real overdraft, not a\nsign convention.",
			"BalanceLine.name":              "Name is the account's human name, or the derived line's own name.",
			"BalanceLine.type":              "Type is the account's fundamental class. Absent on a derived line, which\nbelongs to no account and therefore has none.",
			"BalanceSheet.asOf":             "AsOf is the posting time the statement is taken at, inclusive. A balance sheet\nis a snapshot, not a window, so there is no From. Absent means as of now.",
			"BalanceSheet.assets":           "Assets are what the org OWNS at that instant, one line per account that has a\nbalance. Cash, receivables, funds captured but not yet settled.",
			"BalanceSheet.balanced":         "Balanced is whether assets equal liabilities plus equity — the accounting\nequation, computed from the totals above rather than assumed. False means the\nledger is broken, not that the statement is.",
			"BalanceSheet.equity":           "Equity is what is left over for the owners. It carries a DERIVED retained\nearnings line holding cumulative income minus expense, because this ledger has\nno period close that sweeps the P&L into equity — without that line the\nequation would not close.",
			"BalanceSheet.liabilities":      "Liabilities are what the org OWES — including customers' unspent prepaid\ncredit, which is their money until it is consumed and so is carried here\nrather than counted as revenue.",
			"BalanceSheet.totalAssets":      "TotalAssets is the sum of the asset lines, in cents.",
			"BalanceSheet.totalEquity":      "TotalEquity is the sum of the equity lines including retained earnings, in cents.",
			"BalanceSheet.totalLiabilities": "TotalLiabilities is the sum of the liability lines, in cents.",
			"asOfIn.sandbox":                "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\".",
			"asOfIn.to":                     "To is the RFC3339 instant the statement is struck as of. Empty means all time.",
		},
		Example: json.RawMessage(`{"to":"2026-03-31T23:59:59Z"}`),
	})
	zip.Describe("GET /v1/books/questions", zip.Doc{
		Description: "Returns the clarifying questions the caller's own recent GL raises — the\nunusual postings a founder should look at (outliers, reversals, round-offs, uncosted\nrevenue, an overdrawn wallet), sharpest first. An empty list means the books look clean;\nthe detector is deterministic over the ledger and invents nothing.",
		Fields: map[string]string{
			"Question.account":            "Account is the chart number the questioned entry posted to, where one applies.",
			"Question.amount":             "Amount is the figure that makes the question concrete, already FORMATTED for\ndisplay with its currency symbol — a string, not cents, and not for arithmetic.",
			"Question.id":                 "ID is the source transaction the question is about, so answering it leads\nstraight back to the entry that raised it.",
			"Question.kind":               "Kind is what looked wrong: outlier (a charge far above the usual), reversal (a\nposting undone), roundoff (a balancing plug big enough to be worth explaining),\nuncosted (revenue booked with no cost matched to it), or overdrawn (a wallet\nspent past its balance).",
			"Question.postedAt":           "PostedAt anchors the question in time — when the entry it concerns posted.",
			"Question.text":               "Text is the question itself, written for a founder to answer directly.",
			"QuestionsResponse.questions": "Questions are what the books want explained, SHARPEST FIRST — largest amounts\nahead of smaller ones, and capped, so this is the top of the list rather than\neverything. Empty means the ledger looks clean; the detector is deterministic\nover what was posted and invents nothing.",
			"ledgerIn.sandbox":            "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\"; anything else\nreads the live one.",
		},
		Example: json.RawMessage(`{"sandbox":"false"}`),
	})
	zip.Describe("GET /v1/books/rules", zip.Doc{
		Description: "Returns the org's auto-categorization rules, highest priority first. A rule\nis a standing instruction — \"anything whose merchant contains X books to category Y\" —\nand it overrides a vendor's default category, so this is the list that decides how a\nfuture bill classifies itself.",
		Fields: map[string]string{
			"Rule.category":    "Category is the COA expense account a matching bill books to. An upsert normalizes\na slug (\"cloud\") to its account number.",
			"Rule.pattern":     "Pattern is the merchant substring the rule matches on, case-insensitively. It is\nalso the key an upsert writes by.",
			"Rule.priority":    "Priority breaks ties: when several patterns match, the highest wins.",
			"ledgerIn.sandbox": "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\"; anything else\nreads the live one.",
			"rulesOut.rules":   "Rules is every rule the org has set, highest priority first — the order they\nare matched in.",
		},
		Example: json.RawMessage(`{"sandbox":"false"}`),
	})
	zip.Describe("GET /v1/books/transactions", zip.Doc{
		Description: "Returns the org's booked ledger as a single-line register, newest\nfirst: one row per voucher, with its date, description, vendor, category, source and\namount in exact cents. It is the double-entry ledger projected to the register a human\nreads, filterable by posting-time window, category and vendor. Strictly read-only — it\nrestates the books, it never moves them.",
		Fields: map[string]string{
			"Txn.amountCents":              "AmountCents is the voucher's total, in whole cents — its total debit, which\nequals its total credit because every voucher balances. It is the size of the\nentry and carries no direction; the category says which way it went.",
			"Txn.category":                 "Category is the chart-of-accounts NUMBER of the income or expense account this\nvoucher touched — where it lands on the P&L, not a free-text label.",
			"Txn.categoryName":             "CategoryName is that account's human name, so a caller need not carry the\nchart to render the row.",
			"Txn.date":                     "Date is when the voucher POSTED — the accounting date the reports window on,\nwhich for an imported bank row is the bank's date and not the day it landed\nhere.",
			"Txn.description":              "Description is the line a person reads: the memo carried in from the source.",
			"Txn.source":                   "Source is where the entry came from: bank_txn for an imported statement line,\nscan for a receipt or bill read by the scanner, commerce_txn for a sale\nbooked by the store.",
			"Txn.vendor":                   "Vendor is the counterparty, resolved from whatever the source knew — a bank\nrow's merchant, a scanned bill's supplier. Absent when the source named none.",
			"Txn.voucherId":                "VoucherID identifies the underlying double-entry voucher, so a caller can open\nthe full set of legs behind this single register line.",
			"transactionsOut.transactions": "Transactions is the matching register rows, newest first.",
			"txnQuery.category":            "Category filters to one COA account, named by number (\"5300\") or by category\nslug (\"software\").",
			"txnQuery.from":                "From is the RFC3339 start of the posting-time window, inclusive.",
			"txnQuery.limit":               "Limit caps how many rows come back; 200 when absent or not positive.",
			"txnQuery.sandbox":             "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\".",
			"txnQuery.to":                  "To is the RFC3339 end of the posting-time window, inclusive.",
			"txnQuery.vendor":              "Vendor filters to rows whose vendor or description contains this text,\ncase-insensitively.",
		},
		Example: json.RawMessage(`{"category":"software","limit":50}`),
	})
	zip.Describe("GET /v1/books/trial", zip.Doc{
		Description: "Returns the org's trial balance over an optional [from, to] window of\nRFC3339 posting times, including the opening/closing columns and the\nTotalDebit == TotalCredit proof that the books balance.",
		Fields: map[string]string{
			"TrialBalance.balanced":         "Balanced is the proof this report exists to give: whether total debits equal\ntotal credits. It is computed from the rows above, never assumed, and false\nmeans the ledger itself is broken rather than that the report is wrong.",
			"TrialBalance.from":             "From is the posting time the window opens at, as it was asked for. Absent\nmeans the report runs from the beginning of the ledger.",
			"TrialBalance.rows":             "Rows are the accounts that MOVED in one of the windows. An account that never\nmoved is omitted rather than listed at zero, so this is shorter than the chart.",
			"TrialBalance.to":               "To is the posting time the window closes at, inclusive. Absent means \"up to\nnow\" — every posting the ledger holds.",
			"TrialBalance.totalCredit":      "TotalCredit is the sum of every row's closing credit column, in cents.",
			"TrialBalance.totalDebit":       "TotalDebit is the sum of every row's CLOSING debit column, in cents.",
			"TrialBalanceRow.account":       "Account is the chart-of-accounts NUMBER this line reports on (\"1000\", \"4000\")\n— the stable posting key, not a display label.",
			"TrialBalanceRow.closingCredit": "ClosingCredit is that closing balance in cents when it is a credit balance.",
			"TrialBalanceRow.closingDebit":  "ClosingDebit is the balance at the end of the window, in cents, when it is a\ndebit balance. This is the column the report's totals are summed from.",
			"TrialBalanceRow.credit":        "Credit is the same window movement in cents when it was net credit.",
			"TrialBalanceRow.debit":         "Debit is the account's MOVEMENT within the window — closing minus opening, not\nthe closing balance — in cents, when that movement was net debit. Zero when the\naccount moved net credit.",
			"TrialBalanceRow.name":          "Name is that account's human name from the fixed chart.",
			"TrialBalanceRow.openingCredit": "OpeningCredit is the same opening balance in cents when it fell on the credit\nside. Zero when the balance was a debit one.",
			"TrialBalanceRow.openingDebit":  "OpeningDebit is the account's balance before the window began, in whole cents,\nwhen that balance was on the debit side. Zero when the balance was a credit\none — the pair is exclusive, never two halves of one number.",
			"TrialBalanceRow.type":          "Type is the account's fundamental class — asset, liability, income, expense or\nequity — which is also its normal balance side. It is carried for presentation\nand does NOT decide which column an amount lands in: placement follows the\nsign of the real net, so a contra balance shows up as one.",
			"periodIn.from":                 "From is the RFC3339 start of the window, exclusive. Empty means all time.",
			"periodIn.sandbox":              "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\".",
			"periodIn.to":                   "To is the RFC3339 end of the window, inclusive. Empty means up to now.",
		},
		Example: json.RawMessage(`{"from":"2026-01-01T00:00:00Z","to":"2026-03-31T23:59:59Z"}`),
	})
	zip.Describe("GET /v1/books/vendors", zip.Doc{
		Description: "Returns the org's vendor book: each canonical vendor, the alias spellings a\nreceipt may print it under, and the expense account new bills from it default to. A\nvendor here is what makes a scanned bill self-classify instead of asking again.",
		Fields: map[string]string{
			"VendorRow.aliases":         "Aliases are the other spellings a receipt may print the vendor under; a scan\nmatching any of them resolves to this vendor.",
			"VendorRow.canonical":       "Canonical is the vendor's one true name, and the key an upsert writes by.",
			"VendorRow.defaultCategory": "DefaultCategory is the COA expense account new bills from this vendor book to.\nAn upsert normalizes a slug (\"software\") to its account number.",
			"ledgerIn.sandbox":          "Sandbox reads the org's SANDBOX ledger when it is exactly \"true\"; anything else\nreads the live one.",
			"vendorsOut.vendors":        "Vendors is every vendor the org has recorded, canonical name ascending.",
		},
		Example: json.RawMessage(`{"sandbox":"false"}`),
	})
	zip.Describe("POST /books/figures", zip.Doc{
		Description: "Answers the caller's own all-time ledger snapshot as formatted\nfigures.\n\nThe org is the CALLER's plane identity and there is no argument that could\nname another: [plane.FiguresIn] is empty, and the identity zip forwards is the\none the gateway minted. Anonymous is REFUSED rather than defaulted — a books\nread with no principal behind it is not a read of nobody's ledger, it is a\nread of the first org whose name a bug supplies.\n\nThe LIVE ledger, never the sandbox. A sandbox figure narrated as an answer\nabout the business would be a fabricated number wearing a real one's label,\nand the selector that would let a caller ask for it is the same argument this\nop refuses to grow.",
	})
	zip.Describe("POST /v1/books/ask", zip.Doc{
		Description: "Answers a plain-language question about the caller's own books — \"what is my\nMRR?\", \"how long is my runway?\" — with figures taken from their ledger, never a guessed\nnumber. A deterministic keyword router picks the intent and reads the real metrics, and\nthose figures, followups and report sources are computed BEFORE any model call and are\nnever altered by one: the optional narration client only rephrases the sentence, and it\ndegrades silently to the templated answer when no AI plane is wired. It is strictly\nread-only — it restates the books, it never posts to them.",
		Fields: map[string]string{
			"AskRequest.from":       "From is the RFC3339 start of the metric window. Empty means all time, treated as a\nsingle reporting period (see monthsBetween).",
			"AskRequest.question":   "Question is the plain-language question about the org's books, e.g. \"what is my\nMRR?\". Longer than 2000 characters is truncated, never refused.",
			"AskRequest.to":         "To is the RFC3339 end of the metric window. Empty means up to now.",
			"AskResponse.answer":    "Answer is one or two sentences answering the question, every number in it taken\nfrom Figures.",
			"AskResponse.figures":   "Figures are the grounded numbers the answer states, each already formatted.",
			"AskResponse.followups": "Followups are sharper questions to ask next, chosen from the same intent.",
			"AskResponse.sources":   "Sources name the books reports the figures were computed from — \"pnl\",\n\"position\", \"trial\".",
			"Figure.label":          "Label names the metric, e.g. \"MRR\" or \"Runway\".",
			"Figure.period":         "Period is the window the figure covers, e.g. \"2026-07\" or \"all-time\".",
			"Figure.value":          "Value is the figure already formatted through books' own money formatter, so a\nconsumer never re-derives it.",
		},
		Example: json.RawMessage(`{"question":"how long is my runway?"}`),
	})
	zip.Describe("POST /v1/books/bank/sync", zip.Doc{
		Description: "Pulls every connected bank (Plaid/Teller) for the caller's org, maps each\nfetched transaction to a posting and books it idempotently, then advances that\nconnector's cursor so the next sync resumes where this one stopped. One connector's\noutage is skipped rather than failing the whole sync. It reports the batch: how many\ntransactions were seen, how many vouchers posted, how many inflows reconciled against\nthe processor clearing account, how many raised a question, how many were own-account\ntransfers, and how many were already-processed no-ops. It is READ-ONLY against the\nbank — it ingests, it never sends money.",
		Fields: map[string]string{
			"BankTally.ingested":   "transactions seen",
			"BankTally.posted":     "vouchers newly posted (outflow + reconciled)",
			"BankTally.questions":  "unmatched inflows that raised a question",
			"BankTally.reconciled": "inflows cleared against Square-clearing",
			"BankTally.skipped":    "already-processed idempotent no-ops",
			"BankTally.transfers":  "own-account moves recorded (no P&L)",
		},
	})
	zip.Describe("POST /v1/books/rules", zip.Doc{
		Description: "Creates or updates one auto-categorization rule, keyed by its pattern —\nwriting a pattern that already exists REPLACES that row's category and priority. The\ncategory is normalized to a real COA expense account, and anything unrecognized becomes\n5900 Uncategorized rather than a guessed real account. It answers the row exactly as\nstored, so the caller sees the normalization. A rule overrides a vendor's default\ncategory, so this is the standing instruction that decides how a future bill classifies.",
		Fields: map[string]string{
			"Rule.category": "Category is the COA expense account a matching bill books to. An upsert normalizes\na slug (\"cloud\") to its account number.",
			"Rule.pattern":  "Pattern is the merchant substring the rule matches on, case-insensitively. It is\nalso the key an upsert writes by.",
			"Rule.priority": "Priority breaks ties: when several patterns match, the highest wins.",
		},
		Example: json.RawMessage(`{"pattern":"aws","category":"cloud","priority":5}`),
	})
	zip.Describe("POST /v1/books/scan/book", zip.Doc{
		Description: "Posts a reviewed scanned bill to the ledger. It is the scanner's ONLY write:\nthe voucher goes through the same post() choke point every other source uses, so it is\nchecked to balance (Σdebit == Σcredit) and is idempotent by (scan, scanId) — re-booking\nthe same scan answers posted=false and writes nothing. A bill whose economic identity\n(vendor, total, issue date) already posted under a DIFFERENT scan is refused 409 unless\noverride is set, which is what stops the same receipt re-scanned into a new file hash\nfrom double-booking. An unbalanced voucher is refused 400.",
		Fields: map[string]string{
			"BookRequest.override": "Override books this bill even when one of the SAME economic identity\n(vendor, total, issue date) already posted — the explicit human confirmation that a\nsame-looking bill is a genuine second spend, not the same receipt re-scanned.",
			"BookRequest.scanId":   "ScanID is the scanned document's file hash, as GET /v1/books/inbox and the scan\ndraft report it. It is the idempotency key: re-booking the same scan writes nothing.",
			"BookRequest.voucher":  "Voucher is the reviewed voucher to post. Its source is FORCED to (scan, scanId)\nserver-side, so it can never be booked under another source's key.",
			"BookResponse.posted":  "Posted is true when this call wrote the voucher, false when the same scan had\nalready booked and nothing was written.",
			"BookResponse.scanId":  "ScanID echoes the scan that was booked.",
			"Leg.account":          "Account is the chart-of-accounts number this side posts to, e.g. \"5300\".",
			"Leg.credit":           "Credit is the leg's credit in exact cents. Set this or Debit, not both.",
			"Leg.debit":            "Debit is the leg's debit in exact cents. Set this or Credit, not both.",
			"Voucher.description":  "Description is the human line for the event, e.g. the vendor a bill came from.",
			"Voucher.legs":         "Legs are the sides of the posting. They must balance: Σdebit == Σcredit, give or\ntake the 2¢ round-off allowance.",
			"Voucher.postingAt":    "PostingAt is the RFC3339 instant the event posts at — the time every statement\nwindow filters on.",
			"Voucher.sourceId":     "SourceID is the source event's own id within that namespace. Together with\nSourceKind it is the key that makes a repeat posting a no-op.",
			"Voucher.sourceKind":   "SourceKind is the idempotency namespace naming what booked this, e.g. \"scan\".",
		},
		Example: json.RawMessage(`{"scanId":"a5f3c1","voucher":{"description":"GitHub","legs":[{"account":"5300","debit":1234},{"account":"2001","credit":1234}]}}`),
	})
	zip.Describe("POST /v1/books/sync", zip.Doc{
		Description: "Sync ingests the caller's OWN org from commerce into BOTH ledgers (live and sandbox)\nand reports how many new vouchers posted to each. It is idempotent — money that has\nalready been booked posts nothing on a repeat — and it is read-only against commerce:\nit never mints a deposit, a credit or a payout, only the accounting twin of money that\nalready moved.",
		Fields: map[string]string{
			"syncTally.live":    "Live is the number of vouchers newly posted to the live ledger.",
			"syncTally.sandbox": "Sandbox is the number newly posted to the sandbox ledger.",
		},
	})
	zip.Describe("POST /v1/books/vendors", zip.Doc{
		Description: "Creates or updates one vendor in the org's vendor book, keyed by its\ncanonical name — writing a canonical name that already exists REPLACES that row's\naliases and default category. A category given as a slug (\"software\") is normalized to\nits real COA expense account, and anything unrecognized becomes 5900 Uncategorized\nrather than a guessed real account. It answers the row exactly as stored, so the caller\nsees the normalization. Recording a vendor is what makes future bills from it\nself-classify instead of asking again.",
		Fields: map[string]string{
			"VendorRow.aliases":         "Aliases are the other spellings a receipt may print the vendor under; a scan\nmatching any of them resolves to this vendor.",
			"VendorRow.canonical":       "Canonical is the vendor's one true name, and the key an upsert writes by.",
			"VendorRow.defaultCategory": "DefaultCategory is the COA expense account new bills from this vendor book to.\nAn upsert normalizes a slug (\"software\") to its account number.",
		},
		Example: json.RawMessage(`{"canonical":"GitHub","aliases":["github.com"],"defaultCategory":"software"}`),
	})
}
