// Code generated by zipdoc; DO NOT EDIT.

package index

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/index/version", zip.Doc{
		Description: "version reports which server answered, in the shape a Meilisearch client\nexpects. It names the dialect implementation rather than a Meilisearch release,\nso a client logging it is not misled about what it is talking to. It reads no\nstore and takes no tenant, which is why it is the one route here that always\nanswers.",
		Fields: map[string]string{
			"versionView.commitDate": "CommitDate is always empty: there is no upstream build to date.",
			"versionView.commitSha":  "CommitSha names the implementation rather than a build hash, so a client\nlogging it says which server answered.",
			"versionView.pkgVersion": "PkgVersion names the DIALECT implementation, not a Meilisearch release.",
		},
		Response: json.RawMessage(`{"pkgVersion":"1.0.0","commitSha":"hanzo-cloud","commitDate":""}`),
	})
}
