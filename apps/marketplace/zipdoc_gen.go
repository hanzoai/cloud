// Code generated by zipdoc; DO NOT EDIT.

package marketplace

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/marketplace/listings/:id", zip.Doc{
		Description: "unpublish removes one of the caller org's listings and answers 204. A listing\nbelonging to another org reads as not found.",
		Fields: map[string]string{
			"ListingRef.id": "ID is the listing id from the path, as returned by publish.",
		},
		Example: json.RawMessage(`{"id":"lst_4c1e9b7a2d6f0538e4a7c9b1d3f5027a"}`),
	})
	zip.Describe("GET /v1/marketplace", zip.Doc{
		Description: "discover lists every tool and agent resolvable in the caller's (org, project),\noverlaid with any public listing's title, category and price, and flags the\nones the caller already has installed.",
		Fields: map[string]string{
			"Discovery.items":          "Items is the catalog, one entry per resolvable tool or agent.",
			"ListingPrice.amountCents": "AmountCents is the per-call price in minor units.",
			"ListingPrice.currency":    "Currency is the ISO 4217 code; empty means USD.",
			"ListingPrice.recipient":   "Recipient is the seller payout wallet the x402 settlement targets.",
			"MarketItem.activated":     "Activated reports whether the tool is switched on for the caller's scope.",
			"MarketItem.category":      "Category is the listing's shop category, present only for a listed tool.",
			"MarketItem.description":   "Description is the tool's own one-line description.",
			"MarketItem.dispatchable":  "Dispatchable reports whether the tool can be called, not merely listed.",
			"MarketItem.inputSchema":   "Schema is the tool's JSON-Schema input contract, absent when it declares none.",
			"MarketItem.installed":     "Installed reports whether the tool is activated for the caller's scope.",
			"MarketItem.name":          "Name is the tool's registry name, unique in the caller's scope.",
			"MarketItem.price":         "Price is the per-call price, present only for a monetized listing.",
			"MarketItem.source":        "Source is the plane the tool comes from (builtin, mcp, connector, ...).",
			"MarketItem.title":         "Title is the listing's shop title, present only for a listed tool.",
		},
		Response: json.RawMessage(`{"items":[{"name":"conn_alpha","installed":true,"title":"Alpha"}]}`),
	})
	zip.Describe("GET /v1/marketplace/listings", zip.Doc{
		Description: "listListings returns the listings the caller's org has published, monetized or not.",
		Fields: map[string]string{
			"Listing.recipient":    "seller payout wallet (x402 Payee).",
			"ListingList.listings": "Listings is the caller org's listings; empty, never null, when it has none.",
		},
	})
	zip.Describe("POST /v1/marketplace/install", zip.Doc{
		Description: "install activates a tool for the caller's (org, project) — the marketplace\n\"install\" IS the tool-plane activation write, so the two never drift.",
		Fields: map[string]string{
			"InstallRequest.tool":     "Tool is the registry tool name.",
			"InstallResult.installed": "Installed is the resulting activation state for the caller's (org, project).",
			"InstallResult.tool":      "Tool is the tool the write applied to.",
		},
		Example:  json.RawMessage(`{"tool":"conn_alpha"}`),
		Response: json.RawMessage(`{"tool":"conn_alpha","installed":true}`),
	})
	zip.Describe("POST /v1/marketplace/listings", zip.Doc{
		Description: "publish records a listing for a tool the publisher can actually offer (it must\nresolve in the publisher's scope). A monetized listing (priceCents>0) MUST name a\nrecipient wallet — the x402 payout target. Ownership policy (who may monetize a\nplatform-owned tool) is a follow-on; existence is enforced here.",
		Fields: map[string]string{
			"Listing.recipient":          "seller payout wallet (x402 Payee).",
			"PublishRequest.category":    "Category groups the listing in discovery.",
			"PublishRequest.currency":    "Currency is the price's currency code.",
			"PublishRequest.description": "Description is the shop copy, at most 4096 characters.",
			"PublishRequest.priceCents":  "PriceCents is the per-call price in minor units; 0 publishes it free.",
			"PublishRequest.public":      "Public exposes the listing in every org's discovery, not just the publisher's.",
			"PublishRequest.recipient":   "Recipient is the seller payout wallet, required when priceCents is above 0.",
			"PublishRequest.title":       "Title is the shop title, required, at most 200 characters.",
			"PublishRequest.tool":        "Tool is the registry tool name to list; it must resolve in the publisher's scope.",
		},
		Example: json.RawMessage(`{"tool":"conn_alpha","title":"Alpha","priceCents":500,"currency":"usd","recipient":"0xabc","public":true}`),
	})
	zip.Describe("POST /v1/marketplace/uninstall", zip.Doc{
		Description: "uninstall deactivates a tool for the caller's (org, project).",
		Fields: map[string]string{
			"InstallRequest.tool":     "Tool is the registry tool name.",
			"InstallResult.installed": "Installed is the resulting activation state for the caller's (org, project).",
			"InstallResult.tool":      "Tool is the tool the write applied to.",
		},
		Example:  json.RawMessage(`{"tool":"conn_alpha"}`),
		Response: json.RawMessage(`{"tool":"conn_alpha","installed":false}`),
	})
}
