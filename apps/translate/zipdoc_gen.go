// Code generated by zipdoc; DO NOT EDIT.

package translate

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/translate/memory", zip.Doc{
		Description: "List returns the org's own translation-memory entries, newest first, optionally\nnarrowed to one target language and/or one position on the review ladder. It is\nthe review lane's read: what a human reviewer works through.\n\nThe org is ALWAYS the validated principal's org, never a request field, so one\ntenant can never read another's memory — the entries hold customer source text.",
		Fields: map[string]string{
			"MemoryEntry.actor":            "Actor is the validated user id that last wrote this entry by hand. Empty on an\nentry an engine produced, and on one written before attribution existed.",
			"MemoryEntry.glossary_version": "Glossary is the glossary VERSION the entry was translated under — the digest\nversion() derives from the terms, so changing a term changes the key and the\nstale rendering can never be served.",
			"MemoryEntry.source":           "Source is the ORIGINAL string this entry translates. Part of the identity.",
			"MemoryEntry.state":            "State is the entry's position on the review ladder: machine, suggested,\napproved or published.",
			"MemoryEntry.target":           "Target is the target language tag (BCP-47, e.g. \"es\" or \"pt-BR\"). Part of the\nidentity.",
			"MemoryEntry.text":             "Text is the stored translation. A memory hit returns it verbatim, which is the\nidempotence contract.",
			"MemoryEntry.tier":             "Tier is the engine tier the entry belongs to, quality or bulk. Part of the\nidentity: the two tiers keep separate renderings of the same source.",
			"MemoryEntry.updated_at":       "UpdatedAt is the unix second the entry last changed.",
			"MemoryPage.data":              "Data is the matching memory entries, newest first.",
			"MemoryQuery.limit":            "Limit caps the rows returned. Non-positive or unparseable means the server\ndefault (200); the ceiling is 1000.",
			"MemoryQuery.state":            "State narrows to one position on the review ladder: machine, suggested,\napproved or published.",
			"MemoryQuery.target":           "Target narrows to one target language tag (BCP-47, e.g. \"es\" or \"pt-BR\").",
		},
	})
	zip.Describe("PUT /v1/translate/memory", zip.Doc{
		Description: "Review records a human decision on one translation-memory entry, and returns the\nentry as stored. A human write always wins over the stored value, and once it lands\nat approved or published no machine write can move it again — which is what makes a\nlocale rebuild safe to run against reviewed work.\n\nThe org is ALWAYS the validated principal's org, never a request field, so a review\ncan only ever land in the caller's own memory.",
		Fields: map[string]string{
			"MemoryEntry.actor":            "Actor is the validated user id that last wrote this entry by hand. Empty on an\nentry an engine produced, and on one written before attribution existed.",
			"MemoryEntry.glossary_version": "Glossary is the glossary VERSION the entry was translated under — the digest\nversion() derives from the terms, so changing a term changes the key and the\nstale rendering can never be served.",
			"MemoryEntry.source":           "Source is the ORIGINAL string this entry translates. Part of the identity.",
			"MemoryEntry.state":            "State is the entry's position on the review ladder: machine, suggested,\napproved or published.",
			"MemoryEntry.target":           "Target is the target language tag (BCP-47, e.g. \"es\" or \"pt-BR\"). Part of the\nidentity.",
			"MemoryEntry.text":             "Text is the stored translation. A memory hit returns it verbatim, which is the\nidempotence contract.",
			"MemoryEntry.tier":             "Tier is the engine tier the entry belongs to, quality or bulk. Part of the\nidentity: the two tiers keep separate renderings of the same source.",
			"MemoryEntry.updated_at":       "UpdatedAt is the unix second the entry last changed.",
			"ReviewRequest.glossary":       "Glossary is the terminology the entry was translated under. Its VERSION — the\ndigest of the sorted terms — is part of the entry's identity, so editing a term\nyields a new entry rather than overwriting the old rendering.",
			"ReviewRequest.source":         "Source is the ORIGINAL string this entry translates. Required; part of the\nentry's identity, so a different source is a different entry.",
			"ReviewRequest.state":          "State is the entry's new position on the review ladder: suggested, approved or\npublished. `machine` is engine-only and is refused here — a human may not demote\na string back into the churn.",
			"ReviewRequest.target":         "Target is the target language tag (BCP-47, e.g. \"es\" or \"pt-BR\"). Required;\npart of the entry's identity.",
			"ReviewRequest.text":           "Text is the reviewed translation to store. A human write always wins over the\nstored value.",
			"ReviewRequest.tier":           "Tier is the engine tier the entry belongs to, quality (the default) or bulk.\nPart of the entry's identity: the two tiers keep separate renderings.",
		},
		Example: json.RawMessage(`{"source":"Hello","target":"es","text":"Hola","state":"approved"}`),
	})
}
