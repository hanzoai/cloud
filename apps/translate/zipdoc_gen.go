// Code generated by zipdoc; DO NOT EDIT.

package translate

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("GET /v1/translate/memory", zip.Doc{
		Description: "listMemory reads the caller org's stored translations, newest first.\nIt is the review lane's read — it reports what is remembered and translates\nnothing — and it narrows to one target language, one rung of the review\nladder, or both.",
		Fields: map[string]string{
			"Entry.glossary_version": "Glossary is the glossary VERSION the entry was translated under — the digest\nversion() derives from the terms, so changing a term changes the key and the\nstale rendering can never be served.",
			"MemoryPage.data":        "Data is the matching entries, newest first.",
			"MemoryQuery.limit":      "Limit caps the rows returned; 0 means 200 and nothing above 1000 is\nhonoured.",
			"MemoryQuery.state":      "State narrows to one rung of the review ladder: machine, suggested,\napproved or published. Empty reads every rung.",
			"MemoryQuery.target":     "Target narrows to one target language tag, e.g. \"es\" or \"pt-BR\"; empty\nreads every language.",
		},
		Example: json.RawMessage(`{"target":"es","state":"suggested","limit":50}`),
	})
	zip.Describe("PUT /v1/translate/memory", zip.Doc{
		Description: "reviewMemory stores one human-reviewed translation in the caller org's memory.\nA human write always wins over the stored value, and once an entry lands at\napproved or published no machine write can move it again — which is what\nmakes a locale rebuild safe to re-run.\n\nState may not be set to machine: that rung is the engine's, and a human write\nmay not demote a string back into the churn.",
		Fields: map[string]string{
			"Entry.glossary_version": "Glossary is the glossary VERSION the entry was translated under — the digest\nversion() derives from the terms, so changing a term changes the key and the\nstale rendering can never be served.",
			"ReviewRequest.glossary": "Glossary is the terminology the entry was translated under; its digest is\npart of the entry's identity.",
			"ReviewRequest.source":   "Source is the original string being reviewed. Required.",
			"ReviewRequest.state":    "State is the rung to move the entry to: suggested, approved or published.\nmachine is engine-only and is refused here.",
			"ReviewRequest.target":   "Target is the language tag the string was translated into, e.g. \"pt-BR\".",
			"ReviewRequest.text":     "Text is the reviewed translation to store.",
			"ReviewRequest.tier":     "Tier is the tier the entry was translated at — quality or bulk. It is part\nof the entry's identity, so it must match the call that produced it.",
		},
		Example: json.RawMessage(`{"source":"Save changes","target":"es","text":"Guardar cambios","state":"approved"}`),
	})
}
