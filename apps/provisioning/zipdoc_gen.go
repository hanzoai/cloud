// Code generated by zipdoc; DO NOT EDIT.

package provisioning

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning DELETE /v1/provisioning/datastore/:name", zip.Doc{
		Description: "Deprovisions one Hanzo Datastore warehouse. It reverts any app\ninstance bound to it back to Base BEFORE tearing down the org's dedicated\ninstance, then deletes the sealed credential and removes the metadata row.\nAnswers 204 with no body; a second call is a 404.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"warehouse"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning DELETE /v1/provisioning/docdb/:name", zip.Doc{
		Description: "DropDocDB deprovisions one Hanzo DocDB database. It reverts any app instance\nbound to it back to Base BEFORE tearing down the org's dedicated FerretDB\ninstance, then deletes the sealed credential and removes the metadata row.\nAnswers 204 with no body; a second call is a 404.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"sessions"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning DELETE /v1/provisioning/kv/:name", zip.Doc{
		Description: "DropKV deprovisions one Hanzo KV store. It reverts any app instance bound to\nit back to Base BEFORE tearing down the org's dedicated Valkey instance, then\ndeletes the sealed credential and removes the metadata row. Answers 204 with\nno body; a second call is a 404.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"sessions"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning DELETE /v1/provisioning/s3/:name", zip.Doc{
		Description: "Deletes one bucket from the shared object store and removes its\nmetadata row. Answers 204 with no body; a second call is a 404.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"uploads"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning DELETE /v1/provisioning/search/:name", zip.Doc{
		Description: "Deletes one search index from the shared backend and removes its\nmetadata row. Answers 204 with no body; a second call is a 404.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"products"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning DELETE /v1/provisioning/sql/:name", zip.Doc{
		Description: "DropSQL deprovisions one Hanzo SQL database. It reverts any app instance\nbound to it back to Base BEFORE tearing down the org's dedicated Postgres\ninstance — never a live app pointed at a deleted backend — then deletes the\nsealed credential and removes the metadata row. Answers 204 with no body; a\nsecond call is a 404, not a second delete.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning DELETE /v1/provisioning/vector/:name", zip.Doc{
		Description: "Deletes one vector collection from the shared backend and removes\nits metadata row. Answers 204 with no body; a second call is a 404.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"embeddings"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/admin/provisioning/vector/collections", zip.Doc{
		Description: "Lists every collection in the deployment's vector store\nwith its size and geometry, across all tenants.\n\nPer-collection detail is best-effort — one collection that fails to describe\nitself keeps its name and defaults (dimension 0, cosine) rather than blanking\nthe whole answer — and an unreachable Qdrant answers 200 with an EMPTY list, so\nthe panel shows an honest empty state instead of an error.",
		Fields: map[string]string{
			"vectorCollection.createdAt":       "CreatedAt is the collection's creation time (RFC 3339); Qdrant does not\nreport one, so it is empty today.",
			"vectorCollection.dimension":       "Dimension is the size of one vector in the collection.",
			"vectorCollection.distanceMetric":  "DistanceMetric is the collection's distance function; \"cosine\" when the\ncollection's detail could not be read.",
			"vectorCollection.name":            "Name is the collection name.",
			"vectorCollection.storageBytes":    "StorageBytes is the collection's on-disk size, omitted when unknown.",
			"vectorCollection.vectorCount":     "VectorCount is the collection's point count.",
			"vectorCollectionList.collections": "Collections is one row per Qdrant collection, sorted by name. Empty — never\nabsent — when the vector service cannot be reached.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/admin/provisioning/vector/stats", zip.Doc{
		Description: "Totals the collections, vectors and storage across the whole\nvector store.\n\nEvery figure is summed from the same per-collection detail the collections\nlisting returns, so the two panels can never disagree. An unreachable Qdrant\nanswers 200 with all zeros rather than an error.",
		Fields: map[string]string{
			"vectorStats.totalCollections":  "TotalCollections is how many collections the store holds.",
			"vectorStats.totalStorageBytes": "TotalStorageBytes is the sum of every collection's on-disk size.",
			"vectorStats.totalVectors":      "TotalVectors is the sum of every collection's point count.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/datastore", zip.Doc{
		Description: "Lists the caller org's Hanzo Datastore warehouses. Each one is\na DEDICATED analytical instance the org alone runs, so the host is that\ninstance's own in-cluster Service and the port is its HTTP port, 8123.",
		Fields: map[string]string{
			"provisionedSummary.createdAt": "CreatedAt is when the resource was provisioned, in unix seconds.",
			"provisionedSummary.host":      "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.\nNever the internal admin address of a shared backend.",
			"provisionedSummary.id":        "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedSummary.kind":      "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedSummary.name":      "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedSummary.port":      "Port is the port a client connects to on Host.",
			"provisionedSummary.status":    "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/datastore/:name", zip.Doc{
		Description: "Returns one Hanzo Datastore warehouse's metadata. It carries the\nwarehouse's status, its instance address and the admin user the instance\nbooted with — never the password. A still-booting instance reads\n\"provisioning\", reconciled from the operator's live view rather than the row.",
		Fields: map[string]string{
			"provisionedResource.database": "Database is the logical database, collection, index or bucket this\nresource resolves to on its backend.",
			"provisionedResource.host":     "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.",
			"provisionedResource.id":       "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedResource.kind":     "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedResource.name":     "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedResource.port":     "Port is the port a client connects to on Host.",
			"provisionedResource.status":   "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized. A dedicated resource's status is reconciled from the\noperator's live CR before this is answered, so it is never a stale ready.",
			"provisionedResource.username": "Username is the credential's user, for the kinds that mint one per\nresource. Absent for a kind whose backend authenticates with a shared,\nout-of-band key.",
			"resourceRef.name":             "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"warehouse"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/docdb", zip.Doc{
		Description: "ListDocDB lists the caller org's Hanzo DocDB document databases. Each one is\na DEDICATED FerretDB instance the org alone runs, speaking the MongoDB wire\nprotocol, so the host is that instance's own in-cluster Service and the port\nis 27017.",
		Fields: map[string]string{
			"provisionedSummary.createdAt": "CreatedAt is when the resource was provisioned, in unix seconds.",
			"provisionedSummary.host":      "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.\nNever the internal admin address of a shared backend.",
			"provisionedSummary.id":        "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedSummary.kind":      "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedSummary.name":      "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedSummary.port":      "Port is the port a client connects to on Host.",
			"provisionedSummary.status":    "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/docdb/:name", zip.Doc{
		Description: "GetDocDB returns one Hanzo DocDB database's metadata. It carries the\ndatabase's status, its instance address and the SCRAM user the instance was\nset up with — never the password. A still-booting instance reads\n\"provisioning\", reconciled from the operator's live view.",
		Fields: map[string]string{
			"provisionedResource.database": "Database is the logical database, collection, index or bucket this\nresource resolves to on its backend.",
			"provisionedResource.host":     "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.",
			"provisionedResource.id":       "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedResource.kind":     "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedResource.name":     "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedResource.port":     "Port is the port a client connects to on Host.",
			"provisionedResource.status":   "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized. A dedicated resource's status is reconciled from the\noperator's live CR before this is answered, so it is never a stale ready.",
			"provisionedResource.username": "Username is the credential's user, for the kinds that mint one per\nresource. Absent for a kind whose backend authenticates with a shared,\nout-of-band key.",
			"resourceRef.name":             "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"sessions"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/kv", zip.Doc{
		Description: "ListKV lists the caller org's Hanzo KV stores. Each one is a DEDICATED Valkey\ninstance the org alone runs, so the host is that instance's own in-cluster\nService and the port is 6379.",
		Fields: map[string]string{
			"provisionedSummary.createdAt": "CreatedAt is when the resource was provisioned, in unix seconds.",
			"provisionedSummary.host":      "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.\nNever the internal admin address of a shared backend.",
			"provisionedSummary.id":        "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedSummary.kind":      "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedSummary.name":      "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedSummary.port":      "Port is the port a client connects to on Host.",
			"provisionedSummary.status":    "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/kv/:name", zip.Doc{
		Description: "GetKV returns one Hanzo KV store's metadata. It carries the store's status,\nits instance address and the Valkey user it authenticates as (\"default\", the\nonly user a requirepass instance has) — never the password. A still-booting\ninstance reads \"provisioning\", reconciled from the operator's live view.",
		Fields: map[string]string{
			"provisionedResource.database": "Database is the logical database, collection, index or bucket this\nresource resolves to on its backend.",
			"provisionedResource.host":     "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.",
			"provisionedResource.id":       "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedResource.kind":     "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedResource.name":     "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedResource.port":     "Port is the port a client connects to on Host.",
			"provisionedResource.status":   "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized. A dedicated resource's status is reconciled from the\noperator's live CR before this is answered, so it is never a stale ready.",
			"provisionedResource.username": "Username is the credential's user, for the kinds that mint one per\nresource. Absent for a kind whose backend authenticates with a shared,\nout-of-band key.",
			"resourceRef.name":             "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"sessions"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/s3", zip.Doc{
		Description: "Lists the caller org's object-storage buckets. A bucket lives in an\nalready-live shared object store and is reached through the public gateway.\nThe names here are the friendly ones the org provisioned; the physical bucket\nis org-namespaced underneath, which is what keeps two tenants' buckets\ndistinct.",
		Fields: map[string]string{
			"provisionedSummary.createdAt": "CreatedAt is when the resource was provisioned, in unix seconds.",
			"provisionedSummary.host":      "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.\nNever the internal admin address of a shared backend.",
			"provisionedSummary.id":        "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedSummary.kind":      "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedSummary.name":      "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedSummary.port":      "Port is the port a client connects to on Host.",
			"provisionedSummary.status":    "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/s3/:name", zip.Doc{
		Description: "Returns one bucket's metadata. It carries the bucket's status and the\ngateway address it is reached at, and no username: the object store\nauthenticates with a shared, out-of-band key rather than a per-bucket\ncredential.",
		Fields: map[string]string{
			"provisionedResource.database": "Database is the logical database, collection, index or bucket this\nresource resolves to on its backend.",
			"provisionedResource.host":     "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.",
			"provisionedResource.id":       "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedResource.kind":     "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedResource.name":     "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedResource.port":     "Port is the port a client connects to on Host.",
			"provisionedResource.status":   "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized. A dedicated resource's status is reconciled from the\noperator's live CR before this is answered, so it is never a stale ready.",
			"provisionedResource.username": "Username is the credential's user, for the kinds that mint one per\nresource. Absent for a kind whose backend authenticates with a shared,\nout-of-band key.",
			"resourceRef.name":             "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"uploads"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/search", zip.Doc{
		Description: "Lists the caller org's search indexes. An index is a logical\nresource inside an already-live shared backend, so every one of them is\nreached through the public gateway rather than at an instance of its own.",
		Fields: map[string]string{
			"provisionedSummary.createdAt": "CreatedAt is when the resource was provisioned, in unix seconds.",
			"provisionedSummary.host":      "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.\nNever the internal admin address of a shared backend.",
			"provisionedSummary.id":        "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedSummary.kind":      "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedSummary.name":      "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedSummary.port":      "Port is the port a client connects to on Host.",
			"provisionedSummary.status":    "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/search/:name", zip.Doc{
		Description: "Returns one search index's metadata. It carries the index's status\nand the gateway address it is reached at, and no username: the backend\nauthenticates with a shared, out-of-band key rather than a per-index\ncredential.",
		Fields: map[string]string{
			"provisionedResource.database": "Database is the logical database, collection, index or bucket this\nresource resolves to on its backend.",
			"provisionedResource.host":     "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.",
			"provisionedResource.id":       "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedResource.kind":     "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedResource.name":     "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedResource.port":     "Port is the port a client connects to on Host.",
			"provisionedResource.status":   "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized. A dedicated resource's status is reconciled from the\noperator's live CR before this is answered, so it is never a stale ready.",
			"provisionedResource.username": "Username is the credential's user, for the kinds that mint one per\nresource. Absent for a kind whose backend authenticates with a shared,\nout-of-band key.",
			"resourceRef.name":             "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"products"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/sql", zip.Doc{
		Description: "ListSQL lists the caller org's Hanzo SQL databases. Each one is a DEDICATED\nPostgreSQL instance the org alone runs, so the host is that instance's own\nin-cluster Service and the port is 5432.",
		Fields: map[string]string{
			"provisionedSummary.createdAt": "CreatedAt is when the resource was provisioned, in unix seconds.",
			"provisionedSummary.host":      "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.\nNever the internal admin address of a shared backend.",
			"provisionedSummary.id":        "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedSummary.kind":      "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedSummary.name":      "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedSummary.port":      "Port is the port a client connects to on Host.",
			"provisionedSummary.status":    "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/sql/:name", zip.Doc{
		Description: "GetSQL returns one Hanzo SQL database's metadata. It carries the database's\nstatus, its instance address and the admin user Postgres booted with — never\nthe password, which is returned once at create and otherwise lives only in\nHanzo KMS. A still-booting instance reads \"provisioning\", reconciled from the\noperator's live view rather than from the row.",
		Fields: map[string]string{
			"provisionedResource.database": "Database is the logical database, collection, index or bucket this\nresource resolves to on its backend.",
			"provisionedResource.host":     "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.",
			"provisionedResource.id":       "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedResource.kind":     "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedResource.name":     "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedResource.port":     "Port is the port a client connects to on Host.",
			"provisionedResource.status":   "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized. A dedicated resource's status is reconciled from the\noperator's live CR before this is answered, so it is never a stale ready.",
			"provisionedResource.username": "Username is the credential's user, for the kinds that mint one per\nresource. Absent for a kind whose backend authenticates with a shared,\nout-of-band key.",
			"resourceRef.name":             "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/vector", zip.Doc{
		Description: "Lists the caller org's vector collections. A collection is a\nlogical resource inside an already-live shared backend, so every one of them\nis reached through the public gateway rather than at an instance of its own.",
		Fields: map[string]string{
			"provisionedSummary.createdAt": "CreatedAt is when the resource was provisioned, in unix seconds.",
			"provisionedSummary.host":      "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.\nNever the internal admin address of a shared backend.",
			"provisionedSummary.id":        "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedSummary.kind":      "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedSummary.name":      "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedSummary.port":      "Port is the port a client connects to on Host.",
			"provisionedSummary.status":    "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning GET /v1/provisioning/vector/:name", zip.Doc{
		Description: "Returns one vector collection's metadata. It carries the\ncollection's status and the gateway address it is reached at, and no username:\nthe backend authenticates with a shared, out-of-band key rather than a\nper-collection credential, so there is no per-resource user to report.",
		Fields: map[string]string{
			"provisionedResource.database": "Database is the logical database, collection, index or bucket this\nresource resolves to on its backend.",
			"provisionedResource.host":     "Host is the address that actually routes to this resource — a dedicated\ninstance's own in-cluster Service, or the public gateway for a shared one.",
			"provisionedResource.id":       "ID is the resource's server-minted handle, \"rs_\"-prefixed.",
			"provisionedResource.kind":     "Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.",
			"provisionedResource.name":     "Name is the org-unique slug the caller provisioned the resource under.",
			"provisionedResource.port":     "Port is the port a client connects to on Host.",
			"provisionedResource.status":   "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized. A dedicated resource's status is reconciled from the\noperator's live CR before this is answered, so it is never a stale ready.",
			"provisionedResource.username": "Username is the credential's user, for the kinds that mint one per\nresource. Absent for a kind whose backend authenticates with a shared,\nout-of-band key.",
			"resourceRef.name":             "Name is the resource's org-unique slug, from the path. Lower-cased and\ntrimmed before lookup, exactly as it was at create.",
		},
		Example: json.RawMessage(`{"name":"embeddings"}`),
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning POST /v1/provisioning/datastore", zip.Doc{
		Description: "Launches your org's OWN Hanzo Datastore instance and answers\nwith its `datastore://` connection string.\n\nThe instance is yours alone — a deployment in your own tenant namespace, so its\nadmin credential is naturally scoped to you and no other tenant shares the\nprocess. Off-cluster this fails closed with 503 rather than handing back a\nshared one.",
		Fields: map[string]string{
			"provisionRequest.instance":        "Instance binds a DEDICATED add-on to the app instance whose\n<instance>-addons Secret receives the <KIND>_URL (e.g. \"commerce\").\nOptional: empty means \"not instance-bound\" — the DSN is returned once and\nwired by the caller.",
			"provisionRequest.name":            "Name is the org-unique slug for the new resource, matching\n^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. Every physical name derives from it.",
			"provisionResult.connectionString": "ConnectionString is the ready-to-use DSN, credential included. RETURNED\nHERE ONCE: no read beside this one carries it, so a caller that does not\nkeep it must provision again.",
			"provisionResult.database":         "Database is the logical database, collection, index or bucket this resource\nresolves to on its backend. It is derived from Name under an org-namespacing\nhash, so it is not Name and two orgs cannot land on one.",
			"provisionResult.host":             "Host is the address that routes to this resource — a dedicated instance's\nown in-cluster Service, or the public gateway for a shared one. Never the\ninternal admin address of a shared backend.",
			"provisionResult.id":               "ID is the resource's server-minted handle, \"rs_\"-prefixed. The caller does\nnot choose it, and it is what every read and the delete address.",
			"provisionResult.kind":             "Kind is the product provisioned: sql, vector, datastore, kv, search, s3 or\ndocdb. It is the route that was called, not a body field.",
			"provisionResult.name":             "Name is the org-unique slug the caller asked for, lower-cased. Every\nphysical name on the backend derives from it.",
			"provisionResult.password":         "Password is the minted credential, in plaintext, for the kinds that have\none. RETURNED HERE ONCE — where KMS is configured it is sealed there and\nonly a reference is persisted; where it is not, it is stored nowhere at all.\nIt is never held in plaintext on either side.",
			"provisionResult.port":             "Port is the port a client connects to on Host.",
			"provisionResult.status":           "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator. A shared-backend create is \"ready\" here;\na dedicated one answers 201 still launching, and reaches ready only when a\nlater read reconciles it against the operator's live CR — never fabricated.",
			"provisionResult.username":         "Username is the credential's user, for the kinds that mint one per resource.\nAbsent for a kind whose backend authenticates with a shared, out-of-band key.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning POST /v1/provisioning/docdb", zip.Doc{
		Description: "CreateDocDB launches your org's OWN document-database instance and answers with\nits `mongodb://` connection string. It speaks the MongoDB wire protocol, so\nexisting MongoDB drivers connect unchanged.\n\nThe instance is yours alone — a deployment in your own tenant namespace, so its\nadmin credential is naturally scoped to you and no other tenant shares the\nprocess. Off-cluster this fails closed with 503 rather than handing back a\nshared one.",
		Fields: map[string]string{
			"provisionRequest.instance":        "Instance binds a DEDICATED add-on to the app instance whose\n<instance>-addons Secret receives the <KIND>_URL (e.g. \"commerce\").\nOptional: empty means \"not instance-bound\" — the DSN is returned once and\nwired by the caller.",
			"provisionRequest.name":            "Name is the org-unique slug for the new resource, matching\n^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. Every physical name derives from it.",
			"provisionResult.connectionString": "ConnectionString is the ready-to-use DSN, credential included. RETURNED\nHERE ONCE: no read beside this one carries it, so a caller that does not\nkeep it must provision again.",
			"provisionResult.database":         "Database is the logical database, collection, index or bucket this resource\nresolves to on its backend. It is derived from Name under an org-namespacing\nhash, so it is not Name and two orgs cannot land on one.",
			"provisionResult.host":             "Host is the address that routes to this resource — a dedicated instance's\nown in-cluster Service, or the public gateway for a shared one. Never the\ninternal admin address of a shared backend.",
			"provisionResult.id":               "ID is the resource's server-minted handle, \"rs_\"-prefixed. The caller does\nnot choose it, and it is what every read and the delete address.",
			"provisionResult.kind":             "Kind is the product provisioned: sql, vector, datastore, kv, search, s3 or\ndocdb. It is the route that was called, not a body field.",
			"provisionResult.name":             "Name is the org-unique slug the caller asked for, lower-cased. Every\nphysical name on the backend derives from it.",
			"provisionResult.password":         "Password is the minted credential, in plaintext, for the kinds that have\none. RETURNED HERE ONCE — where KMS is configured it is sealed there and\nonly a reference is persisted; where it is not, it is stored nowhere at all.\nIt is never held in plaintext on either side.",
			"provisionResult.port":             "Port is the port a client connects to on Host.",
			"provisionResult.status":           "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator. A shared-backend create is \"ready\" here;\na dedicated one answers 201 still launching, and reaches ready only when a\nlater read reconciles it against the operator's live CR — never fabricated.",
			"provisionResult.username":         "Username is the credential's user, for the kinds that mint one per resource.\nAbsent for a kind whose backend authenticates with a shared, out-of-band key.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning POST /v1/provisioning/kv", zip.Doc{
		Description: "CreateKV launches your org's OWN key-value instance and answers with its `kv://`\nconnection string.\n\nThe instance is yours alone — a deployment in your own tenant namespace, so its\nadmin credential is naturally scoped to you and no other tenant shares the\nprocess. Off-cluster this fails closed with 503 rather than handing back a\nshared one.",
		Fields: map[string]string{
			"provisionRequest.instance":        "Instance binds a DEDICATED add-on to the app instance whose\n<instance>-addons Secret receives the <KIND>_URL (e.g. \"commerce\").\nOptional: empty means \"not instance-bound\" — the DSN is returned once and\nwired by the caller.",
			"provisionRequest.name":            "Name is the org-unique slug for the new resource, matching\n^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. Every physical name derives from it.",
			"provisionResult.connectionString": "ConnectionString is the ready-to-use DSN, credential included. RETURNED\nHERE ONCE: no read beside this one carries it, so a caller that does not\nkeep it must provision again.",
			"provisionResult.database":         "Database is the logical database, collection, index or bucket this resource\nresolves to on its backend. It is derived from Name under an org-namespacing\nhash, so it is not Name and two orgs cannot land on one.",
			"provisionResult.host":             "Host is the address that routes to this resource — a dedicated instance's\nown in-cluster Service, or the public gateway for a shared one. Never the\ninternal admin address of a shared backend.",
			"provisionResult.id":               "ID is the resource's server-minted handle, \"rs_\"-prefixed. The caller does\nnot choose it, and it is what every read and the delete address.",
			"provisionResult.kind":             "Kind is the product provisioned: sql, vector, datastore, kv, search, s3 or\ndocdb. It is the route that was called, not a body field.",
			"provisionResult.name":             "Name is the org-unique slug the caller asked for, lower-cased. Every\nphysical name on the backend derives from it.",
			"provisionResult.password":         "Password is the minted credential, in plaintext, for the kinds that have\none. RETURNED HERE ONCE — where KMS is configured it is sealed there and\nonly a reference is persisted; where it is not, it is stored nowhere at all.\nIt is never held in plaintext on either side.",
			"provisionResult.port":             "Port is the port a client connects to on Host.",
			"provisionResult.status":           "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator. A shared-backend create is \"ready\" here;\na dedicated one answers 201 still launching, and reaches ready only when a\nlater read reconciles it against the operator's live CR — never fabricated.",
			"provisionResult.username":         "Username is the credential's user, for the kinds that mint one per resource.\nAbsent for a kind whose backend authenticates with a shared, out-of-band key.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning POST /v1/provisioning/s3", zip.Doc{
		Description: "Creates an S3-compatible bucket inside the already-running shared\nobject store and answers with the endpoint that reaches it.",
		Fields: map[string]string{
			"provisionRequest.instance":        "Instance binds a DEDICATED add-on to the app instance whose\n<instance>-addons Secret receives the <KIND>_URL (e.g. \"commerce\").\nOptional: empty means \"not instance-bound\" — the DSN is returned once and\nwired by the caller.",
			"provisionRequest.name":            "Name is the org-unique slug for the new resource, matching\n^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. Every physical name derives from it.",
			"provisionResult.connectionString": "ConnectionString is the ready-to-use DSN, credential included. RETURNED\nHERE ONCE: no read beside this one carries it, so a caller that does not\nkeep it must provision again.",
			"provisionResult.database":         "Database is the logical database, collection, index or bucket this resource\nresolves to on its backend. It is derived from Name under an org-namespacing\nhash, so it is not Name and two orgs cannot land on one.",
			"provisionResult.host":             "Host is the address that routes to this resource — a dedicated instance's\nown in-cluster Service, or the public gateway for a shared one. Never the\ninternal admin address of a shared backend.",
			"provisionResult.id":               "ID is the resource's server-minted handle, \"rs_\"-prefixed. The caller does\nnot choose it, and it is what every read and the delete address.",
			"provisionResult.kind":             "Kind is the product provisioned: sql, vector, datastore, kv, search, s3 or\ndocdb. It is the route that was called, not a body field.",
			"provisionResult.name":             "Name is the org-unique slug the caller asked for, lower-cased. Every\nphysical name on the backend derives from it.",
			"provisionResult.password":         "Password is the minted credential, in plaintext, for the kinds that have\none. RETURNED HERE ONCE — where KMS is configured it is sealed there and\nonly a reference is persisted; where it is not, it is stored nowhere at all.\nIt is never held in plaintext on either side.",
			"provisionResult.port":             "Port is the port a client connects to on Host.",
			"provisionResult.status":           "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator. A shared-backend create is \"ready\" here;\na dedicated one answers 201 still launching, and reaches ready only when a\nlater read reconciles it against the operator's live CR — never fabricated.",
			"provisionResult.username":         "Username is the credential's user, for the kinds that mint one per resource.\nAbsent for a kind whose backend authenticates with a shared, out-of-band key.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning POST /v1/provisioning/search", zip.Doc{
		Description: "Creates a search index inside the already-running shared search\nbackend and answers with the endpoint that reaches it.",
		Fields: map[string]string{
			"provisionRequest.instance":        "Instance binds a DEDICATED add-on to the app instance whose\n<instance>-addons Secret receives the <KIND>_URL (e.g. \"commerce\").\nOptional: empty means \"not instance-bound\" — the DSN is returned once and\nwired by the caller.",
			"provisionRequest.name":            "Name is the org-unique slug for the new resource, matching\n^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. Every physical name derives from it.",
			"provisionResult.connectionString": "ConnectionString is the ready-to-use DSN, credential included. RETURNED\nHERE ONCE: no read beside this one carries it, so a caller that does not\nkeep it must provision again.",
			"provisionResult.database":         "Database is the logical database, collection, index or bucket this resource\nresolves to on its backend. It is derived from Name under an org-namespacing\nhash, so it is not Name and two orgs cannot land on one.",
			"provisionResult.host":             "Host is the address that routes to this resource — a dedicated instance's\nown in-cluster Service, or the public gateway for a shared one. Never the\ninternal admin address of a shared backend.",
			"provisionResult.id":               "ID is the resource's server-minted handle, \"rs_\"-prefixed. The caller does\nnot choose it, and it is what every read and the delete address.",
			"provisionResult.kind":             "Kind is the product provisioned: sql, vector, datastore, kv, search, s3 or\ndocdb. It is the route that was called, not a body field.",
			"provisionResult.name":             "Name is the org-unique slug the caller asked for, lower-cased. Every\nphysical name on the backend derives from it.",
			"provisionResult.password":         "Password is the minted credential, in plaintext, for the kinds that have\none. RETURNED HERE ONCE — where KMS is configured it is sealed there and\nonly a reference is persisted; where it is not, it is stored nowhere at all.\nIt is never held in plaintext on either side.",
			"provisionResult.port":             "Port is the port a client connects to on Host.",
			"provisionResult.status":           "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator. A shared-backend create is \"ready\" here;\na dedicated one answers 201 still launching, and reaches ready only when a\nlater read reconciles it against the operator's live CR — never fabricated.",
			"provisionResult.username":         "Username is the credential's user, for the kinds that mint one per resource.\nAbsent for a kind whose backend authenticates with a shared, out-of-band key.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning POST /v1/provisioning/sql", zip.Doc{
		Description: "CreateSQL launches your org's OWN PostgreSQL instance and answers with its\n`postgres://` connection string.\n\nThe instance is yours alone — a deployment in your own tenant namespace, so its\nadmin credential is naturally scoped to you and no other tenant shares the\nprocess. Off-cluster, where there is no orchestrator to launch one, this fails\nclosed with 503 rather than handing back a shared one.",
		Fields: map[string]string{
			"provisionRequest.instance":        "Instance binds a DEDICATED add-on to the app instance whose\n<instance>-addons Secret receives the <KIND>_URL (e.g. \"commerce\").\nOptional: empty means \"not instance-bound\" — the DSN is returned once and\nwired by the caller.",
			"provisionRequest.name":            "Name is the org-unique slug for the new resource, matching\n^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. Every physical name derives from it.",
			"provisionResult.connectionString": "ConnectionString is the ready-to-use DSN, credential included. RETURNED\nHERE ONCE: no read beside this one carries it, so a caller that does not\nkeep it must provision again.",
			"provisionResult.database":         "Database is the logical database, collection, index or bucket this resource\nresolves to on its backend. It is derived from Name under an org-namespacing\nhash, so it is not Name and two orgs cannot land on one.",
			"provisionResult.host":             "Host is the address that routes to this resource — a dedicated instance's\nown in-cluster Service, or the public gateway for a shared one. Never the\ninternal admin address of a shared backend.",
			"provisionResult.id":               "ID is the resource's server-minted handle, \"rs_\"-prefixed. The caller does\nnot choose it, and it is what every read and the delete address.",
			"provisionResult.kind":             "Kind is the product provisioned: sql, vector, datastore, kv, search, s3 or\ndocdb. It is the route that was called, not a body field.",
			"provisionResult.name":             "Name is the org-unique slug the caller asked for, lower-cased. Every\nphysical name on the backend derives from it.",
			"provisionResult.password":         "Password is the minted credential, in plaintext, for the kinds that have\none. RETURNED HERE ONCE — where KMS is configured it is sealed there and\nonly a reference is persisted; where it is not, it is stored nowhere at all.\nIt is never held in plaintext on either side.",
			"provisionResult.port":             "Port is the port a client connects to on Host.",
			"provisionResult.status":           "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator. A shared-backend create is \"ready\" here;\na dedicated one answers 201 still launching, and reaches ready only when a\nlater read reconciles it against the operator's live CR — never fabricated.",
			"provisionResult.username":         "Username is the credential's user, for the kinds that mint one per resource.\nAbsent for a kind whose backend authenticates with a shared, out-of-band key.",
		},
	})
	zip.Describe("github.com/hanzoai/cloud/apps/provisioning POST /v1/provisioning/vector", zip.Doc{
		Description: "Creates a vector collection inside the already-running shared\nvector backend and answers with the endpoint that reaches it.",
		Fields: map[string]string{
			"provisionRequest.instance":        "Instance binds a DEDICATED add-on to the app instance whose\n<instance>-addons Secret receives the <KIND>_URL (e.g. \"commerce\").\nOptional: empty means \"not instance-bound\" — the DSN is returned once and\nwired by the caller.",
			"provisionRequest.name":            "Name is the org-unique slug for the new resource, matching\n^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. Every physical name derives from it.",
			"provisionResult.connectionString": "ConnectionString is the ready-to-use DSN, credential included. RETURNED\nHERE ONCE: no read beside this one carries it, so a caller that does not\nkeep it must provision again.",
			"provisionResult.database":         "Database is the logical database, collection, index or bucket this resource\nresolves to on its backend. It is derived from Name under an org-namespacing\nhash, so it is not Name and two orgs cannot land on one.",
			"provisionResult.host":             "Host is the address that routes to this resource — a dedicated instance's\nown in-cluster Service, or the public gateway for a shared one. Never the\ninternal admin address of a shared backend.",
			"provisionResult.id":               "ID is the resource's server-minted handle, \"rs_\"-prefixed. The caller does\nnot choose it, and it is what every read and the delete address.",
			"provisionResult.kind":             "Kind is the product provisioned: sql, vector, datastore, kv, search, s3 or\ndocdb. It is the route that was called, not a body field.",
			"provisionResult.name":             "Name is the org-unique slug the caller asked for, lower-cased. Every\nphysical name on the backend derives from it.",
			"provisionResult.password":         "Password is the minted credential, in plaintext, for the kinds that have\none. RETURNED HERE ONCE — where KMS is configured it is sealed there and\nonly a reference is persisted; where it is not, it is stored nowhere at all.\nIt is never held in plaintext on either side.",
			"provisionResult.port":             "Port is the port a client connects to on Host.",
			"provisionResult.status":           "Status is \"ready\", or \"provisioning\" while a dedicated instance is still\nbeing materialized by the operator. A shared-backend create is \"ready\" here;\na dedicated one answers 201 still launching, and reaches ready only when a\nlater read reconciles it against the operator's live CR — never fabricated.",
			"provisionResult.username":         "Username is the credential's user, for the kinds that mint one per resource.\nAbsent for a kind whose backend authenticates with a shared, out-of-band key.",
		},
	})
}
