// Code generated by zipdoc; DO NOT EDIT.

package provisioning

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/datastore/:name", zip.Doc{
		Description: "drop destroys one of the caller org's resources of this kind — the backend\nresource, its sealed secret and its metadata row — and answers 204. An\ninstance-bound add-on is reverted to Base FIRST, so no app is ever left\npointing at a deleted backend. The data is not recoverable.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("DELETE /v1/docdb/:name", zip.Doc{
		Description: "drop destroys one of the caller org's resources of this kind — the backend\nresource, its sealed secret and its metadata row — and answers 204. An\ninstance-bound add-on is reverted to Base FIRST, so no app is ever left\npointing at a deleted backend. The data is not recoverable.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("DELETE /v1/kv/:name", zip.Doc{
		Description: "drop destroys one of the caller org's resources of this kind — the backend\nresource, its sealed secret and its metadata row — and answers 204. An\ninstance-bound add-on is reverted to Base FIRST, so no app is ever left\npointing at a deleted backend. The data is not recoverable.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("DELETE /v1/object/:name", zip.Doc{
		Description: "drop destroys one of the caller org's resources of this kind — the backend\nresource, its sealed secret and its metadata row — and answers 204. An\ninstance-bound add-on is reverted to Base FIRST, so no app is ever left\npointing at a deleted backend. The data is not recoverable.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("DELETE /v1/search/:name", zip.Doc{
		Description: "drop destroys one of the caller org's resources of this kind — the backend\nresource, its sealed secret and its metadata row — and answers 204. An\ninstance-bound add-on is reverted to Base FIRST, so no app is ever left\npointing at a deleted backend. The data is not recoverable.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("DELETE /v1/sql/:name", zip.Doc{
		Description: "drop destroys one of the caller org's resources of this kind — the backend\nresource, its sealed secret and its metadata row — and answers 204. An\ninstance-bound add-on is reverted to Base FIRST, so no app is ever left\npointing at a deleted backend. The data is not recoverable.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("DELETE /v1/vector/:name", zip.Doc{
		Description: "drop destroys one of the caller org's resources of this kind — the backend\nresource, its sealed secret and its metadata row — and answers 204. An\ninstance-bound add-on is reverted to Base FIRST, so no app is ever left\npointing at a deleted backend. The data is not recoverable.",
		Fields: map[string]string{
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("GET /v1/datastore", zip.Doc{
		Description: "list returns every resource of this kind the caller's org has, with the endpoint\nthat actually routes to each — a dedicated instance's own in-cluster address, a\nshared-logical resource's public api.hanzo.ai gateway. Never a password.",
		Fields: map[string]string{
			"listItem.createdAt": "CreatedAt is when the resource was provisioned, unix seconds.",
			"listItem.host":      "Host is the customer-facing host.",
			"listItem.id":        "ID is the resource's cloud id.",
			"listItem.kind":      "Kind is which add-on this is.",
			"listItem.name":      "Name is the resource's name within the caller's org.",
			"listItem.port":      "Port is the customer-facing port.",
			"listItem.status":    "Status is \"ready\" or \"provisioning\".",
		},
	})
	zip.Describe("GET /v1/datastore/:name", zip.Doc{
		Description: "get returns one of the caller org's resources of this kind, at the endpoint that\nactually routes to it. A dedicated instance's status is reconciled against its\nlive cluster state first, so \"ready\" is never fabricated. Never a password —\nthat was returned once at create.",
		Fields: map[string]string{
			"getResp.database": "Database is the logical database, index, collection or bucket.",
			"getResp.host":     "Host is the customer-facing host.",
			"getResp.id":       "ID is the resource's cloud id.",
			"getResp.kind":     "Kind is which add-on this is.",
			"getResp.name":     "Name is the resource's name within the caller's org.",
			"getResp.port":     "Port is the customer-facing port.",
			"getResp.status":   "Status is \"ready\" or \"provisioning\"; a dedicated instance is reconciled\nagainst its live cluster state before it is reported.",
			"getResp.username": "Username is the resource's own account, when the backend mints one.",
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("GET /v1/docdb", zip.Doc{
		Description: "list returns every resource of this kind the caller's org has, with the endpoint\nthat actually routes to each — a dedicated instance's own in-cluster address, a\nshared-logical resource's public api.hanzo.ai gateway. Never a password.",
		Fields: map[string]string{
			"listItem.createdAt": "CreatedAt is when the resource was provisioned, unix seconds.",
			"listItem.host":      "Host is the customer-facing host.",
			"listItem.id":        "ID is the resource's cloud id.",
			"listItem.kind":      "Kind is which add-on this is.",
			"listItem.name":      "Name is the resource's name within the caller's org.",
			"listItem.port":      "Port is the customer-facing port.",
			"listItem.status":    "Status is \"ready\" or \"provisioning\".",
		},
	})
	zip.Describe("GET /v1/docdb/:name", zip.Doc{
		Description: "get returns one of the caller org's resources of this kind, at the endpoint that\nactually routes to it. A dedicated instance's status is reconciled against its\nlive cluster state first, so \"ready\" is never fabricated. Never a password —\nthat was returned once at create.",
		Fields: map[string]string{
			"getResp.database": "Database is the logical database, index, collection or bucket.",
			"getResp.host":     "Host is the customer-facing host.",
			"getResp.id":       "ID is the resource's cloud id.",
			"getResp.kind":     "Kind is which add-on this is.",
			"getResp.name":     "Name is the resource's name within the caller's org.",
			"getResp.port":     "Port is the customer-facing port.",
			"getResp.status":   "Status is \"ready\" or \"provisioning\"; a dedicated instance is reconciled\nagainst its live cluster state before it is reported.",
			"getResp.username": "Username is the resource's own account, when the backend mints one.",
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("GET /v1/kv", zip.Doc{
		Description: "list returns every resource of this kind the caller's org has, with the endpoint\nthat actually routes to each — a dedicated instance's own in-cluster address, a\nshared-logical resource's public api.hanzo.ai gateway. Never a password.",
		Fields: map[string]string{
			"listItem.createdAt": "CreatedAt is when the resource was provisioned, unix seconds.",
			"listItem.host":      "Host is the customer-facing host.",
			"listItem.id":        "ID is the resource's cloud id.",
			"listItem.kind":      "Kind is which add-on this is.",
			"listItem.name":      "Name is the resource's name within the caller's org.",
			"listItem.port":      "Port is the customer-facing port.",
			"listItem.status":    "Status is \"ready\" or \"provisioning\".",
		},
	})
	zip.Describe("GET /v1/kv/:name", zip.Doc{
		Description: "get returns one of the caller org's resources of this kind, at the endpoint that\nactually routes to it. A dedicated instance's status is reconciled against its\nlive cluster state first, so \"ready\" is never fabricated. Never a password —\nthat was returned once at create.",
		Fields: map[string]string{
			"getResp.database": "Database is the logical database, index, collection or bucket.",
			"getResp.host":     "Host is the customer-facing host.",
			"getResp.id":       "ID is the resource's cloud id.",
			"getResp.kind":     "Kind is which add-on this is.",
			"getResp.name":     "Name is the resource's name within the caller's org.",
			"getResp.port":     "Port is the customer-facing port.",
			"getResp.status":   "Status is \"ready\" or \"provisioning\"; a dedicated instance is reconciled\nagainst its live cluster state before it is reported.",
			"getResp.username": "Username is the resource's own account, when the backend mints one.",
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("GET /v1/object", zip.Doc{
		Description: "list returns every resource of this kind the caller's org has, with the endpoint\nthat actually routes to each — a dedicated instance's own in-cluster address, a\nshared-logical resource's public api.hanzo.ai gateway. Never a password.",
		Fields: map[string]string{
			"listItem.createdAt": "CreatedAt is when the resource was provisioned, unix seconds.",
			"listItem.host":      "Host is the customer-facing host.",
			"listItem.id":        "ID is the resource's cloud id.",
			"listItem.kind":      "Kind is which add-on this is.",
			"listItem.name":      "Name is the resource's name within the caller's org.",
			"listItem.port":      "Port is the customer-facing port.",
			"listItem.status":    "Status is \"ready\" or \"provisioning\".",
		},
	})
	zip.Describe("GET /v1/object/:name", zip.Doc{
		Description: "get returns one of the caller org's resources of this kind, at the endpoint that\nactually routes to it. A dedicated instance's status is reconciled against its\nlive cluster state first, so \"ready\" is never fabricated. Never a password —\nthat was returned once at create.",
		Fields: map[string]string{
			"getResp.database": "Database is the logical database, index, collection or bucket.",
			"getResp.host":     "Host is the customer-facing host.",
			"getResp.id":       "ID is the resource's cloud id.",
			"getResp.kind":     "Kind is which add-on this is.",
			"getResp.name":     "Name is the resource's name within the caller's org.",
			"getResp.port":     "Port is the customer-facing port.",
			"getResp.status":   "Status is \"ready\" or \"provisioning\"; a dedicated instance is reconciled\nagainst its live cluster state before it is reported.",
			"getResp.username": "Username is the resource's own account, when the backend mints one.",
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("GET /v1/search", zip.Doc{
		Description: "list returns every resource of this kind the caller's org has, with the endpoint\nthat actually routes to each — a dedicated instance's own in-cluster address, a\nshared-logical resource's public api.hanzo.ai gateway. Never a password.",
		Fields: map[string]string{
			"listItem.createdAt": "CreatedAt is when the resource was provisioned, unix seconds.",
			"listItem.host":      "Host is the customer-facing host.",
			"listItem.id":        "ID is the resource's cloud id.",
			"listItem.kind":      "Kind is which add-on this is.",
			"listItem.name":      "Name is the resource's name within the caller's org.",
			"listItem.port":      "Port is the customer-facing port.",
			"listItem.status":    "Status is \"ready\" or \"provisioning\".",
		},
	})
	zip.Describe("GET /v1/search/:name", zip.Doc{
		Description: "get returns one of the caller org's resources of this kind, at the endpoint that\nactually routes to it. A dedicated instance's status is reconciled against its\nlive cluster state first, so \"ready\" is never fabricated. Never a password —\nthat was returned once at create.",
		Fields: map[string]string{
			"getResp.database": "Database is the logical database, index, collection or bucket.",
			"getResp.host":     "Host is the customer-facing host.",
			"getResp.id":       "ID is the resource's cloud id.",
			"getResp.kind":     "Kind is which add-on this is.",
			"getResp.name":     "Name is the resource's name within the caller's org.",
			"getResp.port":     "Port is the customer-facing port.",
			"getResp.status":   "Status is \"ready\" or \"provisioning\"; a dedicated instance is reconciled\nagainst its live cluster state before it is reported.",
			"getResp.username": "Username is the resource's own account, when the backend mints one.",
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("GET /v1/sql", zip.Doc{
		Description: "list returns every resource of this kind the caller's org has, with the endpoint\nthat actually routes to each — a dedicated instance's own in-cluster address, a\nshared-logical resource's public api.hanzo.ai gateway. Never a password.",
		Fields: map[string]string{
			"listItem.createdAt": "CreatedAt is when the resource was provisioned, unix seconds.",
			"listItem.host":      "Host is the customer-facing host.",
			"listItem.id":        "ID is the resource's cloud id.",
			"listItem.kind":      "Kind is which add-on this is.",
			"listItem.name":      "Name is the resource's name within the caller's org.",
			"listItem.port":      "Port is the customer-facing port.",
			"listItem.status":    "Status is \"ready\" or \"provisioning\".",
		},
	})
	zip.Describe("GET /v1/sql/:name", zip.Doc{
		Description: "get returns one of the caller org's resources of this kind, at the endpoint that\nactually routes to it. A dedicated instance's status is reconciled against its\nlive cluster state first, so \"ready\" is never fabricated. Never a password —\nthat was returned once at create.",
		Fields: map[string]string{
			"getResp.database": "Database is the logical database, index, collection or bucket.",
			"getResp.host":     "Host is the customer-facing host.",
			"getResp.id":       "ID is the resource's cloud id.",
			"getResp.kind":     "Kind is which add-on this is.",
			"getResp.name":     "Name is the resource's name within the caller's org.",
			"getResp.port":     "Port is the customer-facing port.",
			"getResp.status":   "Status is \"ready\" or \"provisioning\"; a dedicated instance is reconciled\nagainst its live cluster state before it is reported.",
			"getResp.username": "Username is the resource's own account, when the backend mints one.",
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("GET /v1/vector", zip.Doc{
		Description: "list returns every resource of this kind the caller's org has, with the endpoint\nthat actually routes to each — a dedicated instance's own in-cluster address, a\nshared-logical resource's public api.hanzo.ai gateway. Never a password.",
		Fields: map[string]string{
			"listItem.createdAt": "CreatedAt is when the resource was provisioned, unix seconds.",
			"listItem.host":      "Host is the customer-facing host.",
			"listItem.id":        "ID is the resource's cloud id.",
			"listItem.kind":      "Kind is which add-on this is.",
			"listItem.name":      "Name is the resource's name within the caller's org.",
			"listItem.port":      "Port is the customer-facing port.",
			"listItem.status":    "Status is \"ready\" or \"provisioning\".",
		},
	})
	zip.Describe("GET /v1/vector/:name", zip.Doc{
		Description: "get returns one of the caller org's resources of this kind, at the endpoint that\nactually routes to it. A dedicated instance's status is reconciled against its\nlive cluster state first, so \"ready\" is never fabricated. Never a password —\nthat was returned once at create.",
		Fields: map[string]string{
			"getResp.database": "Database is the logical database, index, collection or bucket.",
			"getResp.host":     "Host is the customer-facing host.",
			"getResp.id":       "ID is the resource's cloud id.",
			"getResp.kind":     "Kind is which add-on this is.",
			"getResp.name":     "Name is the resource's name within the caller's org.",
			"getResp.port":     "Port is the customer-facing port.",
			"getResp.status":   "Status is \"ready\" or \"provisioning\"; a dedicated instance is reconciled\nagainst its live cluster state before it is reported.",
			"getResp.username": "Username is the resource's own account, when the backend mints one.",
			"resourceRef.name": "Name is the resource's name within the caller's org, as create was given it.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("POST /v1/datastore", zip.Doc{
		Description: "create provisions a resource of this kind for the caller's org and answers 201\nwith a routable connection string.\n\nTwo strategies share one preamble (auth, name validation, billing gate, dedup),\nand which one a kind takes decides the rest of the answer:\n\n  - DEDICATED (sql, kv, docdb, datastore) launches the org's OWN instance, so it\n    answers status \"provisioning\" at that instance's in-cluster address, and its\n    admin password is returned exactly ONCE here and is never readable again.\n    Naming an instance ALSO wires the DSN into that app's addons as <KIND>_URL,\n    switching it off Base onto this backend.\n  - SHARED-logical (vector, search, s3) creates a logical resource inside an\n    already-live shared backend, so it answers status \"ready\" at the public\n    api.hanzo.ai gateway, with no per-resource password — those backends\n    authenticate with an out-of-band key. instance is accepted and has no\n    effect: there is no per-org backend for an app to be pointed at.",
		Fields: map[string]string{
			"createReq.instance":          "Instance binds a DEDICATED add-on (sql, kv, docdb, datastore) to the app\ninstance whose <instance>-addons Secret receives the <KIND>_URL (e.g.\n\"commerce\"). Empty means not bound: the connection string is returned once and\nwired by the caller. The shared-logical kinds (vector, search, s3) validate it\nand ignore it — they have no per-org endpoint to point an app at.",
			"createReq.name":              "Name is the resource's name within the caller's org, a DNS-safe slug\n(^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$). The physical name on the backend is\nderived from it and the caller's org server-side.",
			"createResp.connectionString": "ConnectionString is the routable DSN. A shared-logical resource's is remapped\nonto the public endpoint; a dedicated instance's names its own in-cluster\nService, which is the address the org's app dials.",
			"createResp.database":         "Database is the logical database, index, collection or bucket that was made.",
			"createResp.host":             "Host is the CUSTOMER-facing host — never the internal admin address.",
			"createResp.id":               "ID is the resource's cloud id, \"rs_\" plus a random token.",
			"createResp.kind":             "Kind is which add-on this is: sql, vector, datastore, kv, search, s3 or docdb.",
			"createResp.name":             "Name is the resource's name within the caller's org.",
			"createResp.password":         "Password is the generated credential, returned exactly ONCE here and never\nreadable again. Absent for a backend that mints no per-resource password.",
			"createResp.port":             "Port is the customer-facing port for Host.",
			"createResp.status":           "Status is \"ready\" for a shared-logical resource, \"provisioning\" for a\ndedicated instance still coming up.",
			"createResp.username":         "Username is the resource's own account, when the backend mints one.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("POST /v1/docdb", zip.Doc{
		Description: "create provisions a resource of this kind for the caller's org and answers 201\nwith a routable connection string.\n\nTwo strategies share one preamble (auth, name validation, billing gate, dedup),\nand which one a kind takes decides the rest of the answer:\n\n  - DEDICATED (sql, kv, docdb, datastore) launches the org's OWN instance, so it\n    answers status \"provisioning\" at that instance's in-cluster address, and its\n    admin password is returned exactly ONCE here and is never readable again.\n    Naming an instance ALSO wires the DSN into that app's addons as <KIND>_URL,\n    switching it off Base onto this backend.\n  - SHARED-logical (vector, search, s3) creates a logical resource inside an\n    already-live shared backend, so it answers status \"ready\" at the public\n    api.hanzo.ai gateway, with no per-resource password — those backends\n    authenticate with an out-of-band key. instance is accepted and has no\n    effect: there is no per-org backend for an app to be pointed at.",
		Fields: map[string]string{
			"createReq.instance":          "Instance binds a DEDICATED add-on (sql, kv, docdb, datastore) to the app\ninstance whose <instance>-addons Secret receives the <KIND>_URL (e.g.\n\"commerce\"). Empty means not bound: the connection string is returned once and\nwired by the caller. The shared-logical kinds (vector, search, s3) validate it\nand ignore it — they have no per-org endpoint to point an app at.",
			"createReq.name":              "Name is the resource's name within the caller's org, a DNS-safe slug\n(^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$). The physical name on the backend is\nderived from it and the caller's org server-side.",
			"createResp.connectionString": "ConnectionString is the routable DSN. A shared-logical resource's is remapped\nonto the public endpoint; a dedicated instance's names its own in-cluster\nService, which is the address the org's app dials.",
			"createResp.database":         "Database is the logical database, index, collection or bucket that was made.",
			"createResp.host":             "Host is the CUSTOMER-facing host — never the internal admin address.",
			"createResp.id":               "ID is the resource's cloud id, \"rs_\" plus a random token.",
			"createResp.kind":             "Kind is which add-on this is: sql, vector, datastore, kv, search, s3 or docdb.",
			"createResp.name":             "Name is the resource's name within the caller's org.",
			"createResp.password":         "Password is the generated credential, returned exactly ONCE here and never\nreadable again. Absent for a backend that mints no per-resource password.",
			"createResp.port":             "Port is the customer-facing port for Host.",
			"createResp.status":           "Status is \"ready\" for a shared-logical resource, \"provisioning\" for a\ndedicated instance still coming up.",
			"createResp.username":         "Username is the resource's own account, when the backend mints one.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("POST /v1/kv", zip.Doc{
		Description: "create provisions a resource of this kind for the caller's org and answers 201\nwith a routable connection string.\n\nTwo strategies share one preamble (auth, name validation, billing gate, dedup),\nand which one a kind takes decides the rest of the answer:\n\n  - DEDICATED (sql, kv, docdb, datastore) launches the org's OWN instance, so it\n    answers status \"provisioning\" at that instance's in-cluster address, and its\n    admin password is returned exactly ONCE here and is never readable again.\n    Naming an instance ALSO wires the DSN into that app's addons as <KIND>_URL,\n    switching it off Base onto this backend.\n  - SHARED-logical (vector, search, s3) creates a logical resource inside an\n    already-live shared backend, so it answers status \"ready\" at the public\n    api.hanzo.ai gateway, with no per-resource password — those backends\n    authenticate with an out-of-band key. instance is accepted and has no\n    effect: there is no per-org backend for an app to be pointed at.",
		Fields: map[string]string{
			"createReq.instance":          "Instance binds a DEDICATED add-on (sql, kv, docdb, datastore) to the app\ninstance whose <instance>-addons Secret receives the <KIND>_URL (e.g.\n\"commerce\"). Empty means not bound: the connection string is returned once and\nwired by the caller. The shared-logical kinds (vector, search, s3) validate it\nand ignore it — they have no per-org endpoint to point an app at.",
			"createReq.name":              "Name is the resource's name within the caller's org, a DNS-safe slug\n(^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$). The physical name on the backend is\nderived from it and the caller's org server-side.",
			"createResp.connectionString": "ConnectionString is the routable DSN. A shared-logical resource's is remapped\nonto the public endpoint; a dedicated instance's names its own in-cluster\nService, which is the address the org's app dials.",
			"createResp.database":         "Database is the logical database, index, collection or bucket that was made.",
			"createResp.host":             "Host is the CUSTOMER-facing host — never the internal admin address.",
			"createResp.id":               "ID is the resource's cloud id, \"rs_\" plus a random token.",
			"createResp.kind":             "Kind is which add-on this is: sql, vector, datastore, kv, search, s3 or docdb.",
			"createResp.name":             "Name is the resource's name within the caller's org.",
			"createResp.password":         "Password is the generated credential, returned exactly ONCE here and never\nreadable again. Absent for a backend that mints no per-resource password.",
			"createResp.port":             "Port is the customer-facing port for Host.",
			"createResp.status":           "Status is \"ready\" for a shared-logical resource, \"provisioning\" for a\ndedicated instance still coming up.",
			"createResp.username":         "Username is the resource's own account, when the backend mints one.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("POST /v1/object", zip.Doc{
		Description: "create provisions a resource of this kind for the caller's org and answers 201\nwith a routable connection string.\n\nTwo strategies share one preamble (auth, name validation, billing gate, dedup),\nand which one a kind takes decides the rest of the answer:\n\n  - DEDICATED (sql, kv, docdb, datastore) launches the org's OWN instance, so it\n    answers status \"provisioning\" at that instance's in-cluster address, and its\n    admin password is returned exactly ONCE here and is never readable again.\n    Naming an instance ALSO wires the DSN into that app's addons as <KIND>_URL,\n    switching it off Base onto this backend.\n  - SHARED-logical (vector, search, s3) creates a logical resource inside an\n    already-live shared backend, so it answers status \"ready\" at the public\n    api.hanzo.ai gateway, with no per-resource password — those backends\n    authenticate with an out-of-band key. instance is accepted and has no\n    effect: there is no per-org backend for an app to be pointed at.",
		Fields: map[string]string{
			"createReq.instance":          "Instance binds a DEDICATED add-on (sql, kv, docdb, datastore) to the app\ninstance whose <instance>-addons Secret receives the <KIND>_URL (e.g.\n\"commerce\"). Empty means not bound: the connection string is returned once and\nwired by the caller. The shared-logical kinds (vector, search, s3) validate it\nand ignore it — they have no per-org endpoint to point an app at.",
			"createReq.name":              "Name is the resource's name within the caller's org, a DNS-safe slug\n(^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$). The physical name on the backend is\nderived from it and the caller's org server-side.",
			"createResp.connectionString": "ConnectionString is the routable DSN. A shared-logical resource's is remapped\nonto the public endpoint; a dedicated instance's names its own in-cluster\nService, which is the address the org's app dials.",
			"createResp.database":         "Database is the logical database, index, collection or bucket that was made.",
			"createResp.host":             "Host is the CUSTOMER-facing host — never the internal admin address.",
			"createResp.id":               "ID is the resource's cloud id, \"rs_\" plus a random token.",
			"createResp.kind":             "Kind is which add-on this is: sql, vector, datastore, kv, search, s3 or docdb.",
			"createResp.name":             "Name is the resource's name within the caller's org.",
			"createResp.password":         "Password is the generated credential, returned exactly ONCE here and never\nreadable again. Absent for a backend that mints no per-resource password.",
			"createResp.port":             "Port is the customer-facing port for Host.",
			"createResp.status":           "Status is \"ready\" for a shared-logical resource, \"provisioning\" for a\ndedicated instance still coming up.",
			"createResp.username":         "Username is the resource's own account, when the backend mints one.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("POST /v1/search", zip.Doc{
		Description: "create provisions a resource of this kind for the caller's org and answers 201\nwith a routable connection string.\n\nTwo strategies share one preamble (auth, name validation, billing gate, dedup),\nand which one a kind takes decides the rest of the answer:\n\n  - DEDICATED (sql, kv, docdb, datastore) launches the org's OWN instance, so it\n    answers status \"provisioning\" at that instance's in-cluster address, and its\n    admin password is returned exactly ONCE here and is never readable again.\n    Naming an instance ALSO wires the DSN into that app's addons as <KIND>_URL,\n    switching it off Base onto this backend.\n  - SHARED-logical (vector, search, s3) creates a logical resource inside an\n    already-live shared backend, so it answers status \"ready\" at the public\n    api.hanzo.ai gateway, with no per-resource password — those backends\n    authenticate with an out-of-band key. instance is accepted and has no\n    effect: there is no per-org backend for an app to be pointed at.",
		Fields: map[string]string{
			"createReq.instance":          "Instance binds a DEDICATED add-on (sql, kv, docdb, datastore) to the app\ninstance whose <instance>-addons Secret receives the <KIND>_URL (e.g.\n\"commerce\"). Empty means not bound: the connection string is returned once and\nwired by the caller. The shared-logical kinds (vector, search, s3) validate it\nand ignore it — they have no per-org endpoint to point an app at.",
			"createReq.name":              "Name is the resource's name within the caller's org, a DNS-safe slug\n(^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$). The physical name on the backend is\nderived from it and the caller's org server-side.",
			"createResp.connectionString": "ConnectionString is the routable DSN. A shared-logical resource's is remapped\nonto the public endpoint; a dedicated instance's names its own in-cluster\nService, which is the address the org's app dials.",
			"createResp.database":         "Database is the logical database, index, collection or bucket that was made.",
			"createResp.host":             "Host is the CUSTOMER-facing host — never the internal admin address.",
			"createResp.id":               "ID is the resource's cloud id, \"rs_\" plus a random token.",
			"createResp.kind":             "Kind is which add-on this is: sql, vector, datastore, kv, search, s3 or docdb.",
			"createResp.name":             "Name is the resource's name within the caller's org.",
			"createResp.password":         "Password is the generated credential, returned exactly ONCE here and never\nreadable again. Absent for a backend that mints no per-resource password.",
			"createResp.port":             "Port is the customer-facing port for Host.",
			"createResp.status":           "Status is \"ready\" for a shared-logical resource, \"provisioning\" for a\ndedicated instance still coming up.",
			"createResp.username":         "Username is the resource's own account, when the backend mints one.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("POST /v1/sql", zip.Doc{
		Description: "create provisions a resource of this kind for the caller's org and answers 201\nwith a routable connection string.\n\nTwo strategies share one preamble (auth, name validation, billing gate, dedup),\nand which one a kind takes decides the rest of the answer:\n\n  - DEDICATED (sql, kv, docdb, datastore) launches the org's OWN instance, so it\n    answers status \"provisioning\" at that instance's in-cluster address, and its\n    admin password is returned exactly ONCE here and is never readable again.\n    Naming an instance ALSO wires the DSN into that app's addons as <KIND>_URL,\n    switching it off Base onto this backend.\n  - SHARED-logical (vector, search, s3) creates a logical resource inside an\n    already-live shared backend, so it answers status \"ready\" at the public\n    api.hanzo.ai gateway, with no per-resource password — those backends\n    authenticate with an out-of-band key. instance is accepted and has no\n    effect: there is no per-org backend for an app to be pointed at.",
		Fields: map[string]string{
			"createReq.instance":          "Instance binds a DEDICATED add-on (sql, kv, docdb, datastore) to the app\ninstance whose <instance>-addons Secret receives the <KIND>_URL (e.g.\n\"commerce\"). Empty means not bound: the connection string is returned once and\nwired by the caller. The shared-logical kinds (vector, search, s3) validate it\nand ignore it — they have no per-org endpoint to point an app at.",
			"createReq.name":              "Name is the resource's name within the caller's org, a DNS-safe slug\n(^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$). The physical name on the backend is\nderived from it and the caller's org server-side.",
			"createResp.connectionString": "ConnectionString is the routable DSN. A shared-logical resource's is remapped\nonto the public endpoint; a dedicated instance's names its own in-cluster\nService, which is the address the org's app dials.",
			"createResp.database":         "Database is the logical database, index, collection or bucket that was made.",
			"createResp.host":             "Host is the CUSTOMER-facing host — never the internal admin address.",
			"createResp.id":               "ID is the resource's cloud id, \"rs_\" plus a random token.",
			"createResp.kind":             "Kind is which add-on this is: sql, vector, datastore, kv, search, s3 or docdb.",
			"createResp.name":             "Name is the resource's name within the caller's org.",
			"createResp.password":         "Password is the generated credential, returned exactly ONCE here and never\nreadable again. Absent for a backend that mints no per-resource password.",
			"createResp.port":             "Port is the customer-facing port for Host.",
			"createResp.status":           "Status is \"ready\" for a shared-logical resource, \"provisioning\" for a\ndedicated instance still coming up.",
			"createResp.username":         "Username is the resource's own account, when the backend mints one.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
	zip.Describe("POST /v1/vector", zip.Doc{
		Description: "create provisions a resource of this kind for the caller's org and answers 201\nwith a routable connection string.\n\nTwo strategies share one preamble (auth, name validation, billing gate, dedup),\nand which one a kind takes decides the rest of the answer:\n\n  - DEDICATED (sql, kv, docdb, datastore) launches the org's OWN instance, so it\n    answers status \"provisioning\" at that instance's in-cluster address, and its\n    admin password is returned exactly ONCE here and is never readable again.\n    Naming an instance ALSO wires the DSN into that app's addons as <KIND>_URL,\n    switching it off Base onto this backend.\n  - SHARED-logical (vector, search, s3) creates a logical resource inside an\n    already-live shared backend, so it answers status \"ready\" at the public\n    api.hanzo.ai gateway, with no per-resource password — those backends\n    authenticate with an out-of-band key. instance is accepted and has no\n    effect: there is no per-org backend for an app to be pointed at.",
		Fields: map[string]string{
			"createReq.instance":          "Instance binds a DEDICATED add-on (sql, kv, docdb, datastore) to the app\ninstance whose <instance>-addons Secret receives the <KIND>_URL (e.g.\n\"commerce\"). Empty means not bound: the connection string is returned once and\nwired by the caller. The shared-logical kinds (vector, search, s3) validate it\nand ignore it — they have no per-org endpoint to point an app at.",
			"createReq.name":              "Name is the resource's name within the caller's org, a DNS-safe slug\n(^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$). The physical name on the backend is\nderived from it and the caller's org server-side.",
			"createResp.connectionString": "ConnectionString is the routable DSN. A shared-logical resource's is remapped\nonto the public endpoint; a dedicated instance's names its own in-cluster\nService, which is the address the org's app dials.",
			"createResp.database":         "Database is the logical database, index, collection or bucket that was made.",
			"createResp.host":             "Host is the CUSTOMER-facing host — never the internal admin address.",
			"createResp.id":               "ID is the resource's cloud id, \"rs_\" plus a random token.",
			"createResp.kind":             "Kind is which add-on this is: sql, vector, datastore, kv, search, s3 or docdb.",
			"createResp.name":             "Name is the resource's name within the caller's org.",
			"createResp.password":         "Password is the generated credential, returned exactly ONCE here and never\nreadable again. Absent for a backend that mints no per-resource password.",
			"createResp.port":             "Port is the customer-facing port for Host.",
			"createResp.status":           "Status is \"ready\" for a shared-logical resource, \"provisioning\" for a\ndedicated instance still coming up.",
			"createResp.username":         "Username is the resource's own account, when the backend mints one.",
		},
		Example: json.RawMessage(`{"name":"orders"}`),
	})
}
