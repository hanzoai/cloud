// Code generated by zipdoc; DO NOT EDIT.

package ads

import (
	"encoding/json"

	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("DELETE /v1/ads/campaigns/:id", zip.Doc{
		Description: "deleteCampaign removes one of the caller org's ad campaigns and answers 204. It\ndeletes the stored plan only — a campaign already running at the provider is not\nstopped there.",
		Fields: map[string]string{
			"CampaignRef.id": "ID is the campaign id from the path, as returned by create.",
		},
		Example: json.RawMessage(`{"id":"camp_4c1e9b7a2d6f0538e4a7c9b1d3f5027a"}`),
	})
	zip.Describe("GET /v1/ads/campaigns", zip.Doc{
		Description: "listCampaigns returns the caller org's ad campaigns, optionally filtered by status.",
		Fields: map[string]string{
			"Campaign.account":     "provider ad-account ref (Meta act_<id>)",
			"Campaign.externalId":  "provider campaign id after a launch",
			"CampaignList.data":    "Data is the matching campaigns, newest first.",
			"CampaignQuery.limit":  "Limit caps the rows returned; 0 means 200 and nothing above 1000 is honoured.",
			"CampaignQuery.status": "Status narrows the list to one lifecycle state: draft, active, paused or completed.",
		},
		Example: json.RawMessage(`{"status":"active","limit":50}`),
	})
	zip.Describe("GET /v1/ads/campaigns/:id", zip.Doc{
		Description: "getCampaign returns one of the caller org's ad campaigns.",
		Fields: map[string]string{
			"Campaign.account":    "provider ad-account ref (Meta act_<id>)",
			"Campaign.externalId": "provider campaign id after a launch",
			"CampaignRef.id":      "ID is the campaign id from the path, as returned by create.",
		},
		Example: json.RawMessage(`{"id":"camp_4c1e9b7a2d6f0538e4a7c9b1d3f5027a"}`),
	})
	zip.Describe("GET /v1/ads/summary", zip.Doc{
		Description: "summary returns the caller org's campaign counts and money totals.",
		Fields: map[string]string{
			"AdsSummary.active":    "Active is how many of them are in the active state.",
			"AdsSummary.budget":    "Budget is the total budget across campaigns, in minor units.",
			"AdsSummary.campaigns": "Campaigns is how many campaigns the org has.",
			"AdsSummary.spend":     "Spend is the total recorded spend across campaigns, in minor units.",
		},
		Response: json.RawMessage(`{"campaigns":12,"active":3,"budget":500000,"spend":128400}`),
	})
	zip.Describe("POST /v1/ads/campaigns", zip.Doc{
		Description: "createCampaign registers an ad campaign in the caller's org. Name is required;\nplatform defaults to meta and status to draft, budget and spend are minor units\nclamped to >= 0, and the id, createdAt, updatedAt and externalId of the input are\nignored — the server assigns them. Nothing is spent: a campaign is a plan until it\nis launched.",
		Fields: map[string]string{
			"Campaign.account":    "provider ad-account ref (Meta act_<id>)",
			"Campaign.externalId": "provider campaign id after a launch",
		},
		Example: json.RawMessage(`{"name":"Spring Launch","platform":"meta","objective":"signups","budget":50000}`),
	})
	zip.Describe("POST /v1/ads/campaigns/:id/launch", zip.Doc{
		Description: "launchCampaign runs a stored campaign on its provider and starts spending. It holds\nno token: the org's connected ad-account credential is resolved from KMS at call\ntime and the launch FAILS CLOSED with 424 when the org has not connected that\nplatform, so it can never spend on a connection the org did not make. On success the\nprovider campaign id is recorded and the campaign goes active.",
		Fields: map[string]string{
			"Campaign.account":    "provider ad-account ref (Meta act_<id>)",
			"Campaign.externalId": "provider campaign id after a launch",
			"LaunchReq.account":   "Account sets or overrides the provider ad account to spend from; empty uses the\naccount already stored on the campaign.",
			"LaunchReq.id":        "ID is the campaign id from the path.",
		},
		Example: json.RawMessage(`{"id":"camp_4c1e9b7a2d6f0538e4a7c9b1d3f5027a","account":"act_1234567890"}`),
	})
	zip.Describe("PUT /v1/ads/campaigns/:id", zip.Doc{
		Description: "updateCampaign replaces one of the caller org's ad campaigns. It is a full replace,\nnot a patch: name is required and every omitted field resets to its default. The\ncampaign updated is the one the PATH names — a body id cannot retarget it — and\ncreatedAt and externalId are server-owned.",
		Fields: map[string]string{
			"Campaign.account":    "provider ad-account ref (Meta act_<id>)",
			"Campaign.externalId": "provider campaign id after a launch",
		},
		Example: json.RawMessage(`{"id":"camp_4c1e9b7a2d6f0538e4a7c9b1d3f5027a","name":"Spring Launch","platform":"meta","status":"paused","budget":75000}`),
	})
}
