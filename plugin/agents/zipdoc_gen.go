// Code generated by zipdoc; DO NOT EDIT.

package main

import (
	"github.com/zap-proto/zip"
)

func init() {
	zip.Describe("POST /agents/resolve-target", zip.Doc{
		Description: "Answers with the org's machine, or an error. It returns\nonly the id and the label — never the row — so a caller learns nothing about a\nmachine it did not already name, and a reference that matches nothing in THIS\norg is not found rather than somebody else's target.",
	})
	zip.Describe("POST /agents/route-run", zip.Doc{
		Description: "Puts the run on the durable engine IN THIS PROCESS.\n\nThat placement is the whole point of the op. The delivery activity offers the\nrun to an in-memory mailbox which the machine long-polls through this app's\nHTTP surface; enqueued anywhere else it would be offered to a mailbox nobody\nreads, and the workflow would spend its entire budget before failing. One\nprocess holds the engine, the mailbox and the completion — this one.",
		Fields: map[string]string{
			"RouteRunIn.actor":      "Actor + AgentRef are cloud-side attribution for the completion path (the\nsession close and the PR assignee). They never cross to the machine.",
			"RouteRunIn.forgeActor": "ForgeActor is the forge login the run acts as. It is CLOUD-SIDE, like Actor:\nthe completion opens the pull request as that person and the executing\nmachine never sees it.",
		},
	})
	zip.Describe("POST /agents/session/close", zip.Doc{})
	zip.Describe("POST /agents/session/event", zip.Doc{
		Fields: map[string]string{
			"SessionEventIn.kind": "\"tool-call\" | \"log\" | \"status\"",
		},
	})
	zip.Describe("POST /agents/session/open", zip.Doc{
		Description: "Opens the session. Target is a VALUE of the request, not a\nsecond op: \"no machine\" is what an ordinary sandbox run says, and splitting it\nin two would be two doors onto one OpenSessionOn.",
		Fields: map[string]string{
			"SessionOpenIn.actor":  "the linked Hanzo subject the run is attributed to",
			"SessionOpenIn.agent":  "agent label, e.g. \"hanzo\"",
			"SessionOpenIn.target": "Target tags the session with the machine a ROUTED run was sent to. Empty is\nthe ordinary cloud-sandbox session.",
		},
	})
	zip.Describe("POST /agents/target-gate", zip.Doc{
		Description: "Is fail-closed by construction: the only success is a nil\nerror from the gate itself, so a machine that is absent, offline or has no\nlive runner stops the dispatch here rather than downstream.",
	})
	zip.Describe("POST /v1/coding", zip.Doc{
		Description: "Runs a coding task on a repository: clones it into a sandbox, lets a model read\nand edit the code, run the tests, and push the work to a branch. Say the thing\nyou want done — \"fix\nthe failing auth test in hanzoai/cloud\" — and the run infers the repo, the\nbranch and the plan. No prefix, no ceremony.\n\nIt answers 202 with the run's handle the moment the run is ADMITTED — not when\nit finishes. A coding run takes minutes; holding a request open for one would\ntie a connection to a model loop and give the caller nothing it cannot get\nbetter from the session stream.\n\nThe handle is a session id, and that is deliberate: the session is already the\nrun's durable record and its live stream (/v1/agents/sessions/{id}/stream), so\nthis door does not grow a progress endpoint, a status endpoint or a cancel\nendpoint of its own. One way to watch a run, whoever started it.",
		Fields: map[string]string{
			"CodingStartIn.agentRef":       "AgentRef names a configured agent to run as, which is how an org pins a\nharness, a model and a prompt to a name. Empty runs the default agent.",
			"CodingStartIn.base":           "Base is the branch to start from. Empty takes the repository's default. The\nrun never writes here — it writes the agent branch it answers with.",
			"CodingStartIn.desktop":        "Desktop asks for a run with a SCREEN — an image carrying an X server — for a\ntask that has to drive a browser or another windowed program. False, the\ndefault, is a headless checkout, which is what writing code needs.",
			"CodingStartIn.project":        "Project scopes the run to one board's work when the org keeps more than one.\nEmpty is the org's default.",
			"CodingStartIn.prompt":         "Prompt is the task, in the words you would use with a colleague who has the\ncheckout open. It is the whole instruction: there is no second field for\ncontext, and a prompt that names files and the outcome it wants gets a run\nthat does not have to guess either.",
			"CodingStartIn.replyChannel":   "ReplyChannel / ReplyThread are WHERE THE RUN NARRATES ITSELF, when the door\nthat started it has somewhere for it to talk. Empty means nobody is\nlistening and the run simply does not narrate — which is the app door's\ncase, because /v1/coding hands back a session id and the session stream is\na better progress feed than any message could be.\n\nIt is an ADDRESS and not a token: the engine says \"put this text there\",\nand the process that owns the workspace's bot credential is the one that\nactually posts. So a run reports into a Slack thread without the engine\never holding the token that could post anywhere else in that workspace.",
			"CodingStartIn.repo":           "Repo is what to work on, as `owner/name` in the caller's own org. The engine\nresolves the clone URL and the push credential from the org itself, so this\nsays WHICH repository and never how to reach it.",
			"CodingStartIn.targetId":       "TargetID routes the run to a registered machine the org has claimed instead\nof to a sandbox in our cluster. Empty runs it here, which is the usual case.",
			"CodingStartIn.timeoutSeconds": "TimeoutSeconds bounds the whole run. Unset takes the default budget; a run\nthat hits the bound is stopped and reports what it had done by then.",
			"CodingStartIn.tool":           "Tool is which harness runs the prompt — dev | claude | codex | python |\nnode — and Desktop is whether the run needs a screen. Both are empty by\ndefault, which is `dev` with no screen, and that default is what every\ncaller gets until it says otherwise.\n\nThey are two fields because they are two questions. The harness decides\nwhat argv starts; the screen decides which image carries an X server. A\ncaller may want claude WITH a browser it can see, and a single enum would\nhave made that combination unsayable.",
			"CodingStarted.branch":         "Branch is the ref the run will push its work to, and the ONLY ref it is\npermitted to write. It exists before the work does, so it is safe to tell\nsomebody where to look while the run is still going.",
			"CodingStarted.repo":           "Repo is the repository the run was admitted against, echoed back as the\nengine resolved it.",
			"CodingStarted.routed":         "Routed says the run went to one of the org's own registered machines rather\nthan to a sandbox in our cluster. False is the ordinary case.",
			"CodingStarted.sessionId":      "SessionID is the run's handle: its durable record, and the id its live\nprogress streams under at /v1/agents/sessions/{sessionId}/stream. Every\nlater question about this run is asked with it.",
			"CodingStarted.targetId":       "TargetID names that machine when Routed is true, and is empty otherwise.",
		},
	})
}
