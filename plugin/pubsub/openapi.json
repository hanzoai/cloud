{
  "openapi": "3.1.0",
  "info": {
    "title": "Hanzo Cloud API",
    "description": "Package pubsub is the platform message bus: an embedded Hanzo PubSub node (NATS + JetStream) that binds the NATS client port (default :4222) and serves JetStream over the cloud data dir — the ONE durable log every other app publishes facts onto and consumes them from.",
    "version": "v1"
  },
  "servers": [
    {
      "url": "https://api.hanzo.ai"
    }
  ],
  "paths": {}
}
